# Message and pointer source checkpoints

<a id="src-2026-09-05-german-shorthand-render-reconciliation"></a>
#### Reviewed a12-dmkits handback rendering the German shorthands, closing the retired-terminal row, and confirming the computation carrier on render, reconciled inbound 2026-09-05

- `revision`: a12-dmkits `40f486c85`, the peer's tree reported quiescent. Oracles: `adapter.laws.RuleMessageGroupParameterDiffTest.theGermanShorthandsRenderTheSameCoordinatesAsTheirEnglishTwins` and `adapter.laws.ComputationMessageDiffTest`, Groovy-dynamic, generated Java and interpreter agreeing byte for byte; `validate.laws.RuleMessageParameterLanguageLawsTest` for the retired terminal. Kernel `30.8.1`.
- `route`: inbound peer measurement, not reproduced here. Recorded as provenance under the no-feedback-loop rule.
- `closes-a-blocked-item`: **what the German `$#LfdNr$` renders** was this project's one queued runtime question, blocked on a probe route that refuses on a non-CLEAN sibling. It is answered rather than unblocked. On a `de_DE`-condition model with a **repeatable** root, a rule at `/Lieferung/Posten` renders `eigen=[1] regel=[1] wurzel=[1]`, then `eigen=[2] regel=[2] wurzel=[2]`, then `eigen=[3] regel=[3] wurzel=[2]` — the absolute path, `$#RegelKontext$`, and `$#LfdNr$` respectively. The third row is the one that pays: `wurzel=` holds at `2` while `eigen=` advances to `3`, so the German shorthands render the same coordinates as their English twins and the root shorthand tracks the root's own index. On a nonrepeatable root it would be the constant `1`, the same limit this project recorded for `top=`.
- `renderer-provenance`: the German condition was produced by their `DslRenderer(ConditionLanguage.DE)` off a bound model rather than hand-spelled, so the keywords are the renderer's own and not a fixture author's guess — which is what makes the row evidence about the language rather than about a string.
- `retired-terminal-closed`: `$#Zeile$` draws `PARAM_INVALID_IN_NEW_WORLD` under **both** condition languages, on the same model pair the bilingual rows use, so the two tables share a fixture and cannot disagree. That is the separator [`SPEC-2026-09-05-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-03) named: a bundle merely lacking the other language's words would refuse it as an unknown group, and `INVALID_GROUP` is exactly what the other language's *shorthand* draws one row above.
- `computation-carrier-rendered`: a computation at a repeatable `/Invoice/Lines` with the message `row=[$#RuleGroup$] qty=[$Qty.value$]` renders `row=[2] qty=[3]` on both Kernel strategies, so the shorthand resolves against the generated rule exactly as in a hand-written one. Render was this project's explicit non-claim on [`SPEC-2026-09-05-06`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-06); it is taken as theirs.
- `technique-worth-keeping`: the peer names the design choice that made their own defect legible — a **non-group** parameter riding beside the group parameter in the same message. Without it, "renders no text at all" and "renders text but drops the group token" are indistinguishable, and the search goes to the token pattern rather than to the message source. Adopt it for any future row on this carrier.
- `peer-stated-limit`: their synthetic rule anchors at the computed field's **parent** group rather than the computation's declaring group; the two coincide in their fixture and in the common case. What `$#RuleGroup$` renders for a computation declared in one group and writing into another is unmeasured on their side, and they decline to extrapolate it — a cross-group repeatable target may not reach the runtime at all on their reading, but that is a reading and not a measurement.
- `peer-self-correction`: they had told this project that no Kernel runtime fixture there carried a `de_DE` condition language, and amended it in place once their own `RuntimeLaws.withRuleMessages` turned out to take a raw condition string. Recorded because *"our instrument cannot reach X"* is the sentence that ends an enquiry on both sides, and this one was withdrawn by its author before it did.
- `limit`: every row is the peer's measurement on the peer's carrier; this project reproduced none of it and holds it on reviewed provenance at the stated revision.

<a id="src-2026-09-05-group-parameter-render-reconciliation"></a>
#### Reviewed a12-dmkits handback completing the group position's bilingual table and separating the rendered coordinate on a doubly nested topology, reconciled inbound 2026-09-05

- `revision`: a12-dmkits `7da8df9e6`, the peer's own committed tree. Their oracles: `checkConsistency` via `KernelLaws.messageVerdict` for the admission table, locked in `validate.laws.RuleMessageParameterLanguageLawsTest`; tri-engine Groovy-dynamic, generated Java, and interpreter for the render rows, locked in `adapter.laws.RuleMessageGroupParameterDiffTest`. Kernel `30.8.1`.
- `route`: inbound peer measurement, not reproduced here. Recorded as provenance under the no-feedback-loop rule, so it creates no outbound entry.
- `bilingual-table-completed`: one model pair with the condition language as the only variable gives all four cells this project had measured in one direction only — on `en_US`, `$#RuleGroup$` and `$#RootGroup$` admitted while `$#RegelKontext$` and `$#LfdNr$` draw `INVALID_GROUP`; on `de_DE` every verdict reverses. `$#RegelKontext$` is the cell this project never authored. Their code note is the transferable half: the group position looks the other language's word up as a group **name**, so it is `INVALID_GROUP` and not the name position's `INVALID_ENTITY`.
- `render-separated`: **a topology this project's fixture could not reach separates two accounts that a single level conflates.** Under a **repeatable** root with two stacked repeatable levels, one message firing at consignment 2 / box 3 / item 4 renders `item=4 box=3 cons=2 rule=4 top=2` — four distinct integers, so "the coordinate of the level named" is separated from "the firing row's innermost coordinate whatever is named". It also settles `$#RootGroup$` on a repeatable root as *that root's own index*, which a nonrepeatable root's constant `1` cannot distinguish from the chain-root reading. Both were limits this project's own [rendered-index checkpoint](#src-message-group-parameter-rendered-index) carried.
- `quoting-runtime`: the per-segment quote escape **renders** identically to the bare spelling, `$#/'Consignment'/'Boxes'$` giving `3`. That is the runtime half of this project's admission-side quoting result, which had established only that an unnecessary quote is transparent to the lookup.
- `disposition`: [`SPEC-2026-09-05-02`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-02) is shipped on their side — `IG148` graduated to `IF323` and their interpreter now renders the position and agrees with both Kernel strategies. The bilingual half of [`SPEC-2026-09-05-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-03) is confirmed while its retired-terminal and language-switch claims stay open, and [`SPEC-2026-09-05-04`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-04)'s reading is conceded with their measurement still to come.
- `limit`: every row here is the peer's measurement on the peer's carrier. This project reproduced none of it, so it is held on reviewed peer provenance exactly as its revision states, and the render rows inherit their tri-engine oracle rather than this project's.

<a id="src-two-positions-share-the-root-gate"></a>
#### Both parameter positions share one root gate and then split, the name position answering `INVALID_ENTITY` where the group position answers `INVALID_GROUP`, measured locally 2026-09-05

- `revision`: launcher self-reported build a12-dmkits `226b2be175133dff45413b0a0f604ea219ced85b (dirty)`; `dmtool` 0.13.0, Kernel `30.8.1`. Seven `rule add --dry-run` children in one `batch`, every one `KERNEL_CONFIRMED`. Retained `raw-position-gates.json` SHA-256 `dca75ca2cade179b7ce662a8e69442104a14c1416ec780a3e32ac99add56d9b0`.
- `question`: [`spec/11`](../../spec/11-messages-and-custom.md) states the two-gate resolution — root segment first, then the rest — for the **group** position only. Whether the name position shares that split is a carrier crossing rather than an instance ([`LF116`](../LEAN-FINDINGS.md)).
- `claim`: **the root gate is shared and the second gate is position-specific.** A bad first segment draws `INVALID_ROOT_GROUP` in *both* positions, so the root check is one mechanism serving both. Below the root they differ: the name position draws `INVALID_ENTITY` and the group position `INVALID_GROUP`, on the same model and through the same route.
- `name-position-does-not-localize`: `INVALID_ENTITY` is returned for a bad **middle** segment and a bad **leaf** alike, so below the root that position does not report where the path failed. The group position's `INVALID_GROUP` behaves the same way. Only the root is separated.
- `controls`: the valid name-position and group-position parameters are both admitted in the same batch, so neither position is failing for a route or model reason.
- `independent-agreement`: this reaches the same code split a12-dmkits reported from their own bilingual work — that the group position looks a word up as a group *name* and so answers `INVALID_GROUP` rather than the name position's `INVALID_ENTITY` — from a different direction, undeclared paths rather than other-language terminals. Two routes to one distinction is why it is recorded as a mechanism rather than a code table.
- `limit`: static admission only, one model, `en_US`, rule carrier. Whether the shared root gate also holds on the computation message carrier is untested, though the terminal bundle is carrier-identical there.
- `sync`: no entry. The code split confirms a12-dmkits' own observation, and the shared root gate adds a cell neither estate had recorded rather than contradicting one; it is carried to the peer in conversation rather than as a request, since nothing they ship states otherwise.

<a id="src-name-position-index-terminal"></a>
#### `index(...)` is a live name-position terminal with three refusal codes of its own, and the group position has no index syntax at all, measured locally 2026-09-05

- `revision`: launcher self-reported build a12-dmkits `226b2be175133dff45413b0a0f604ea219ced85b (dirty)`; `dmtool` 0.13.0, Kernel `30.8.1`. Twenty `rule add --dry-run` children across four `batch` runs plus one `group modify --index-field`, every one `KERNEL_CONFIRMED`.
- `retained`: `raw-index-and-key.json` SHA-256 `ae00802553f50c5f1421baf3f9a42e6594f0e696f1c1d960cef64895717599fa`, `raw-index-terminal.json` SHA-256 `d055bc9a3e065868bc3783b0a50efb05f287f77cbde2bbfba2245290ee5c13fe`, `raw-index-positive.json` SHA-256 `cf05c483626627621c296c253f79e6058c69485e9dcf4b7795baaf4158079c9e`, `raw-index-spelling.json` SHA-256 `55d24abafeb4e667e82cd4a3cb221516ff07e3b967cbbf4cfbd497ffd136c50b`, indexed model `MsgIdx_DM.json` SHA-256 `366d8a44a7f9708c4125b0b527dde829ae4ab395f2494bbd71d89c91244faf4d`.
- `question`: [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) listed the group index and the name-position semantic-index suffix as unmeasured. The clause records `$#index(...)$` drawing plain `INVALID_GROUP` in the **group** position, which says nothing about the name position.
- `group-position-claim`: **there is no explicit index syntax in the group position.** `$#/Msg/Rows[1]$` and `$#/Msg/Rows[2]$` are both refused `MVK_LEXER_STANDARD_ERROR` — brackets are not in that grammar at all, so the position renders the firing row's own coordinate and cannot be pinned to another row. That closes the group-index row as a negative rather than leaving it open.
- `name-position-claim`: **`index(...)` is a live terminal there, and the two positions therefore have different terminal sets.** It draws three codes of its own rather than the ordinary entity refusal: `MVK_UNEXPECTED_TOKEN` for a capitalised `Index(` or a zero-arity `index()`, so the keyword is lowercase with arity one; `INVALID_INDEX_FIELD_PARAMETER` for a bare relative name `index(Marker)`; and `INVALID_INDEX_FIELD` for every path-shaped argument. Bare `$index$` with no parentheses is not a terminal at all — it draws `INVALID_ENTITY`, *"The group or field 'index' is not valid"*.
- `kernel-states-the-rule`: `INVALID_INDEX_FIELD` names its own three disjuncts — the argument *"is either no index-field, is referenced wrongly or is not used in the rule condition"*. That wording is why the code is read as a contract rather than a generic failure.
- `no-admitting-spelling-found`: **no positive witness, and the denominator is stated rather than the conclusion generalized.** Tried: absolute paths to a plain field, to a field used in the condition, to a nonexistent field and to a group; a group-relative path; a bare name; and all of those again after `group modify --index-field Marker` designated a row-key index field on the repeatable group. All refused. So a12's **semantic index** is a different concept from the group's row-key index field, and `--index-field` — the only index lever the verb manifest carries, enumerated rather than assumed ([`LF120`](../LEAN-FINDINGS.md)) — does not arm it. Recorded as *no witness known at this revision*, not as unauthorable.
- `semantic-key-gate`: the `For "k"` suffix is refused `MVK_INDEX_FOR_ERROR_TEXT_INVALID` on both an ordinary and an index-designated field, the Kernel stating the gate the clause already carries — usable *"only if it is used in the rule condition in the same way"*. Since no condition here uses a semantic index, this confirms the gate fires and does not measure what passes it.
- `controls`: a bare name parameter and a `.value` parameter are admitted through the identical route in every batch, so no refusal is the route's or the model's.
- `limit`: static admission only, one model, `en_US`, rule carrier. What `index(...)` renders when it is admitted is unmeasured because nothing admitted it here, and the condition-side spelling that would satisfy either gate is not established.
- `sync`: none behavioral for a12-dmkits — every row is a refusal surface this project had not mapped, and none contradicts either estate's clause. [`spec/11`](../../spec/11-messages-and-custom.md) gains the group-position negative and the name-position terminal set.

<a id="src-terminal-bundle-on-the-computation-carrier"></a>
#### The parameter terminal bundle is shared with the computation message carrier, where `RuleGroup` names the computation's own group, measured locally 2026-09-05

- `revision`: launcher self-reported build a12-dmkits `226b2be175133dff45413b0a0f604ea219ced85b (dirty)`; `dmtool` 0.13.0, Kernel `30.8.1`. Six `computation add --dry-run` children in one `batch`, every one `KERNEL_CONFIRMED`, `model check` valid with zero diagnostics.
- `retained`: `raw-computation-terminals.json` SHA-256 `d149fc3f362ba8c2152fdde403c8aa0d1cc2b30f6eea3eb59f9ec84b2e5ae9d9`, model `MsgComp_DM.json` SHA-256 `86881661e35efaddbbef6097c46623c6f26b0c15b9052b6e641d115118914345`.
- `question`: [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) listed "every non-rule parameter owner sharing the terminal bundle" as unmeasured. Every row establishing the bundle was authored on a **rule** message, so a computation message is a carrier crossing rather than an instance ([`LF116`](../LEAN-FINDINGS.md)) — and one of the terminals is named `RuleGroup`, which a computation does not obviously have.
- `claim`: **the bundle is carrier-identical, and `RuleGroup` is not rule-specific.** On a computation message at `/Msg/Rows`, the absolute `$#/Msg/Rows$`, both keyword shorthands `$#RuleGroup$` and `$#RootGroup$`, and a name-position field are all **admitted**, while the retired `$#Zeile$` draws `PARAM_INVALID_IN_NEW_WORLD` — the same code it draws on the rule carrier. So the terminal set, its admission, and its retirement class do not vary by parameter owner.
- `mechanism`: the Kernel's own refusal text calls the computation a rule — *"Rule with path '/Msg/Rows/NumComp'"* — which is why `RuleGroup` resolves rather than failing. The terminal names the **owning declaration's** group, and "rule" in its spelling is the Kernel's internal vocabulary rather than a restriction to validation rules. A consumer reading the terminal as rule-only would refuse an admitted computation message.
- `controls`: a parameterless message and a plain absolute path are admitted through the identical route, so neither the computation shape nor the message channel refuses on its own; the retired terminal is the only refusal in the batch, which is what isolates it.
- `limit`: **static admission only.** No row observes what `$#RuleGroup$` *renders* on a computation, so "names the computation's own group" is the resolution the admission implies and not a measured value; the rendered-index result is the rule carrier's alone. The runtime half needs the `:adapter:kernelProbe` route, which refuses on a non-CLEAN sibling tree. One model, one nesting depth, `en_US`.
- `sync`: **outbound**, as [`SPEC-2026-09-05-06`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-06). First recorded here as confirming-only, which was wrong: the clause described the bundle on the rule carrier throughout and said nothing about ownership, so stating that `RuleGroup` names the *owning declaration's* group adds a distinction rather than restating one. An implementation that scopes the terminal to validation rules passes every rule row and refuses admitted computation messages, which is a divergence a peer can reach.

<a id="src-parameter-token-versus-group-declaration-alphabet"></a>
#### The parameter's name token is wider than any group may be named, and a quoted segment is refused where the bare spelling reaches lookup, measured locally 2026-09-05

- `revision`: launcher self-reported build a12-dmkits `226b2be175133dff45413b0a0f604ea219ced85b (dirty)`; `dmtool` 0.13.0, Kernel `30.8.1`. Eight `rule add --dry-run` children in one `batch` plus two `group add` attempts, every one `KERNEL_CONFIRMED`.
- `retained`: `raw-quoted-alphabet.json` SHA-256 `8dcacbd42e1741b00ba6709a1c54361f00101b381f2425b551557a646cf3b5f8`, `add-1Rows.json` SHA-256 `47ab0695ae1de9d85db2fbbb5da08d3e7c0eae8c15be0c1b41360802a8295c76`, model `Msg_DM.json` SHA-256 `cbeee724b5344ada061d0e0d26563a93604fadd6fa790acbf252fe75cec02795`.
- `question`: [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) asked whether a **quoted** segment may carry characters a bare one may not. The clause asserted the quoted form "otherwise uses the same alphabet", which was never measured.
- `claim`: **the answer is no, and the two spellings fail at different layers rather than under one rule.** In the `$#…$` group position, `'Ro-ws'`, `'Ro ws'`, and `'1Rows'` are each refused `MVK_LEXER_STANDARD_ERROR`, while the bare `Ro-ws`, `Ro ws`, and `1Rows` each reach the group lookup and draw `INVALID_GROUP` — the same code the control `Nope`, a well-formed but undeclared name, draws. So a quoted segment carries *fewer* characters than a bare one at the point of refusal, not more.
- `declaration-alphabet`: **the token grammar is wider than any group may be named, and the Kernel states its own rule.** `group add` refuses both `1Rows` and `Ro-ws` with `MVK_PATH_INVALID` at `source: KERNEL`, explaining: *"A group name may only contain letters, numbers or underscores. Group names must begin with a letter or an underscore."* So a digit-initial or `:`-bearing parameter name is a **legal token that can never resolve** — its `INVALID_GROUP` is the correct and only possible answer, not an accident of the fixture. This is what makes the reachability half measured rather than assumed.
- `controls`: the valid `$#/Msg/Rows$` is admitted through the identical route, and the undeclared-but-well-formed `$#/Msg/Nope$` fixes what a pure lookup failure looks like, which is what lets the lexer code be read as a different layer rather than a different message.
- `limit`: **the bare hyphen and space rows are not separated and no claim is made about them.** `INVALID_GROUP` there is consistent with the lexer accepting the character into one name *and* with the parser failing over and reporting the raw argument; the echoed error text repeats the parameter verbatim either way. The separating witness would be a group actually bearing such a name, which the Kernel's declaration rule forbids, so this route cannot settle it. Only the quoted rows and the declaration alphabet are claimed here.
- `instrument`: the batch writes ANTLR lexer notes (`token recognition error at: ''Ro-'`) to **stderr** while the envelope goes to stdout, so a `2>&1` redirect corrupts the JSON. Recorded because the first run failed to parse for that reason and the cause looks like a tool defect until the streams are separated.
- `sync`: **outbound**, as [`SPEC-2026-09-05-05`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-05). a12-dmkits ships no surface for either code — its `diagnostics` catalog is `RK_*`/`LINT` only — so this corrects nothing of theirs and is sent because a peer validating parameter names against the token grammar would accept names no group can bear, and one refusing an illegal character uniformly would report a single code where the Kernel reports two.

<a id="src-rule-message-parameter-grammar"></a>
#### The rule-message parameter grammar: paths, quoting, category, and Base Year, measured locally 2026-08-24

- `revision`: clean a12-dmkits `108d047b787f0e049dcd2f4902f24d7523347a00`, dmtool `0.13.0`, kernel `30.8.1` built and runtime; the sibling worktree was empty before and after every command. Source basis read at a12-kernel `cb66e51fa7ab90b650698f861bf670754e2e1e66`.
- `route`: at the measured revision, **`rule check` could not reach this surface at all** because it took no message, so the oracle was `rule add --dry-run`, which the tool documented as kernel-validated before and after and which therefore ran the rule error-text checker. Two dmtool-authored models share one root group with a nonrepeatable subgroup, a Number named `Amount` at **both** levels, a String named `For`, a String named `index`, and an Enumeration with one declared category; one declares Base Year 2020 and the other declares none. Thirty-one decisions vary only the message text, each with a condition that references exactly the fields its message names.
- `instrument-limit`: at that revision an error-text refusal at `rule add --dry-run` wrote **no result envelope**: stdout was empty, the verdict was a prose line on stderr carrying the Kernel's own bracketed class, and the exit code was 2. Every refusal below was therefore read from that line rather than from `diagnostics`. The [2026-08-23 reconciliation](../SOURCES.md#src-2026-08-23-reconciliation) records the later standard-envelope fix and read-only `rule check --message` route.
- `path-claim`: the parameter's entity spec is the shared path grammar, admitted in every form this fragment represents: a bare name in its own scope, a group-qualified relative path, an absolute path, a parent walk, and a parent walk with an explicit turning point. A **wrong** turning point is refused `INVALID_ENTITY` naming `..Nope`, which is the live control showing the turning point arbitrated rather than being ignored. The value suffix composes with a path.
- `path-claim`: two malformed shapes carry classes this project does not reproduce, in the same refusing direction. An absolute path with a single segment is not a parse failure but `INVALID_ROOT_GROUP` naming `/Amount`, so the absolute form's first segment is the **root group**. A bare `..` with no field is not a parse failure either but `INVALID_FIELD`, so it parses as an entity and then fails the reference check. An empty path segment *is* a parse failure, `MVK_UNEXPECTED_TOKEN`.
- `quoting-claim`: the single-quote escape is load-bearing and this producer's exemption is real. A field named `For` is admitted as `$'For'$` and refused as `$For$` with `MVK_UNEXPECTED_TOKEN` — a **parse** class, not a dedicated needs-quoting class, which is why this project maps its own class to none. A field named `index` is admitted **both** quoted and unquoted, which is the exemption's live separator on the same model. `$RuleGroup$` as the whole spec is refused `INVALID_PARAMETER`.
- `baseyear-claim`: the English terminal really is `BaseYear`. `$BaseYear$`, `$BaseYear+3$`, and `$BaseYear-1$` are admitted on the configured model and all refused `NO_BASE_YEAR` on the unconfigured one, so the model's Base Year is the whole gate. `$BaseYear*2$` is refused `MVK_LEXER_STANDARD_ERROR` naming the character and its position, and `$BaseYear+$` is refused `MVK_EXPECTED_TOKEN_NOT_FOUND` expecting a digit sequence, so the offset syntax is exactly a sign and digits. `$'BaseYear'$` is refused `INVALID_ENTITY`: quoting the terminal turns it into a field name, which confirms the escape mechanism from the other side.
- `baseyear-claim`: the German spelling is **not** a terminal in an English model. `$Referenzjahr$` is refused `INVALID_ENTITY` as an unknown group or field, so a consumer that reused the German spelling would resolve the wrong entity rather than interpolate the year.
- `category-claim`: all four category gates fire distinctly on one model. A declared name is admitted; an undeclared one is `INVALID_CATEGORY` naming both the category and the field; an empty name is `NO_CATEGORY`; and the suffix on a Number field is `FIELD_NO_ENUM`. The value suffix on the same Enumeration field is admitted, so the two suffixes are independent rather than one gating the other.
- `local-divergence`: the original doubled-arrow fixture used an undeclared first category and therefore observed `INVALID_CATEGORY`; the [2026-08-23 reconciliation](../SOURCES.md#src-2026-08-23-reconciliation) supplied the separating three-row matrix. A declared first category reaches the trailing-syntax `MVK_UNEXPECTED_TOKEN`, an undeclared one draws `INVALID_CATEGORY`, and a non-Enumeration field draws `FIELD_NO_ENUM`. The local checked parser now preserves the first suffix long enough to reproduce those gates. Combining the value suffix with an arrow, `$Status.value->Group$`, remains a parse failure at both, `MVK_UNEXPECTED_TOKEN`, which confirms the two suffixes are alternatives.
- `local-divergence`: a second, smaller one was found and fixed while writing this checkpoint: a parse refusal quoted the spec it had split from rather than the whole authored parameter, which would send an author looking at text they never wrote.
- `persistence`: one accepted candidate carrying **three** parameter forms in a single message — a relative path with the value suffix, an absolute path, and `$BaseYear+1$` — was persisted, read back byte-identical through `rule read`, and the model re-checked `valid:true`, `KERNEL_CONFIRMED`.
- `integrity`: configured model `499e5b59ad31d03861675081474e6122f2d4e69c0f2920b626b845f6d921155f` (Base Year 2020, the categorised Enumeration, and the persisted candidate) carries twenty-nine rows; unconfigured model `bbcfefe9fbe01add64a85d01831521b97ff50195d118efd0e5fcf1c16345542f` carries the two Base-Year-absent rows. Every retained row was re-run against the exact bytes named here after the last model edit, because an earlier state of the configured model had neither the Base Year nor the Enumeration. `rule add --dry-run` persists nothing, so these two model files are the whole retained input.
- `limit`: this **static grammar checkpoint itself** covers one locale, one nonrepeatable subgroup, and no runtime observation. Runtime rendering is separately measured at the [parameter-rendering checkpoint](../SOURCES.md#src-rule-message-parameter-rendering), while the [2026-08-23 reconciliation](../SOURCES.md#src-2026-08-23-reconciliation) adds the declared-first-category doubled-arrow row, labelled Enumeration value, minimum-fraction default control, bilingual matrix, and exact locale presentation table. Still unmeasured: a field literally named `BaseYear` beside the terminal, the offset's magnitude bound, the remaining non-field parameter forms, and a label-provider result.
- `sync`: locally originated and behavioral on one clause. It upgrades [SPEC-2026-08-24-01](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-24-01) from source-derived to locally measured; no new outbound entry.

<a id="src-rule-message-parameter-rendering"></a>
#### What a rule-message parameter renders, measured locally 2026-08-24

- `revision`: clean a12-dmkits `108d047b787f0e049dcd2f4902f24d7523347a00`, dmtool `0.13.0`, kernel `30.8.1` built and runtime, recorded from the artifact header with `producer.source.state` `CLEAN`; the sibling worktree was empty before and after every command.
- `route`: the model of the [parameter-grammar checkpoint](../SOURCES.md#src-rule-message-parameter-grammar), extended with one label, one fractional Number, and six rules whose messages between them carry every parameter form this project represents, then **two** documents through `:adapter:kernelProbe` observing `validateFull` — one filling the Enumeration and the fractional field, one leaving both empty. Both codegen strategies produced **byte-identical** message lists on every row.
- `baseyear-claim`: the offset arithmetic is exactly the obvious one and the rendering is the plain decimal. Against a declared Base Year of 2020, `$BaseYear$` renders `2020`, `$BaseYear+3$` renders `2023`, and `$BaseYear-1$` renders `2019`, in one message. `$$` renders one `$` in the same message, so the escape survives beside real parameters.
- `category-claim`: a category access renders the **category token the field's current stored value maps to**, not the category's name and not the stored value. With unlabelled `Status` holding the first declared value `open`, `$Status->Group$` renders `A`, the `Group` token aligned with that value. `$Status.value$` in the same message renders the unlabelled fallback `open`, so the two suffixes read different things from one field. The [2026-08-23 reconciliation](../SOURCES.md#src-2026-08-23-reconciliation) adds the labelled-value separator.
- `name-claim`: an **unlabelled** field's `$Field$` renders its **short name** — `Amount` for `/Order/Amount` and `Status` for `/Order/Status` — on this route, which configures no label provider. A field carrying a declared label renders that label: `$Head/Amount$` renders `Head amount`. This narrows the canonical clause's last fallback, which says "the resolved identifier's path/index debug representation": on the kernel's own `validateFull` it is the short name. The route cannot distinguish "no provider" from "a provider returning the short name", so the claim is stated for this route rather than for the public service one.
- `value-claim`: a scale-0 Number renders bare — `$Head/Amount.value$` renders `7` — an **unfilled** String's value renders as the empty string, and an **unfilled** Number declaring two **minimum** fractional digits renders `0.00`, which is the canonical clause's stated per-format default reaching a real message on this route as well. The [2026-08-23 reconciliation](../SOURCES.md#src-2026-08-23-reconciliation) supplies the max-2/min-0 control that renders `0`.
- `empty-claim`: an **empty** Enumeration renders the empty string under **both** suffixes: `$Status->Group$` and `$Status.value$` each render nothing while `$Status$` still renders the short name in the same message. That was this project's own unmeasured default for the category access and is now the Kernel's.
- `polarity-claim`: the message's `type` channel separates the two polarities this project models. The rule whose condition carries a `FieldNotFilled` conjunct reports `OMISSION_ERROR` and lists both referenced fields in `fillToFix`; the three value-comparison rules report `VALUE_ERROR` with an empty `fillToFix`. That is a live cross-check of the omission/value distinction rather than a new rule.
- `local-divergence`: one, and it made the representation stronger. A category access had been represented as an opaque caller-supplied text run precisely because its rendering was unmeasured; it now renders through the declaration's own category mapping applied to the caller's stored token, so the policy sits in the theory instead of in the consumer. Its **empty** case then measured out to the same empty string this project had chosen, so that default stopped being a guess.
- `integrity`: model `342cb1338dec504ff6fe7a5f7db62848d228907537497eaffdf10a8723c8c8f1`; documents `f58245d871e7eef89694b06a237f199c3d4aca2b71fae543903e85d3f7b799fd` (filled) and `97f35aadf9cc7e2b7777b64b8ff06b196bf146539c4828fa8f9555132bc6cf59` (empty Enumeration and fraction); request `c79dbc7978c6c213c048bb92a4ca3e2016549d557c587224ef78f2d6d440a18e`; artifact `80c0f91d34bd697e6ecc44cbe495ff535fd8a0bb0b4d6bc0dbc1c3a4d0227f5c`. An earlier single-row run against a smaller model is superseded by these bytes and is not cited: the model gained the fractional field and two rules afterwards, so its hash no longer measures these rows.
- `limit`: one locale for *this* batch, one Enumeration with one category, no repeatable group, and no label provider. The [2026-08-23 reconciliation](../SOURCES.md#src-2026-08-23-reconciliation) separately closes the exact presentation map across ten locale tags and four presented kinds. A declared provider result including the empty one remains unreachable by this route rather than merely unmeasured. Not measured here: a stored value the category does not map (categories align one-to-one with values, so no such value is authorable), the group-index parameter, and every non-field form other than Base Year.
- `sync`: locally originated and behavioral on one clause. It adds the rendered rows to [SPEC-2026-08-24-01](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-24-01) and narrows the existing unlabelled-fallback sentence for this route; no new outbound entry.

<a id="src-rule-message-semantic-index"></a>
#### The semantic-index suffix on a rule-message parameter, measured locally 2026-08-23

- `revision`: clean a12-dmkits `108d047b787f0e049dcd2f4902f24d7523347a00`, dmtool `0.13.0`, kernel `30.8.1` built; the sibling worktree was empty before and after every command.
- `route`: the model of the [parameter-grammar checkpoint](../SOURCES.md#src-rule-message-parameter-grammar), extended with a repeatable `/Order/Rows` (max 4) declaring `Key` and `Payload`, plus a nonrepeatable `/Order/Wanted` to serve as a field-valued key. Eleven `rule add --dry-run` decisions vary the message parameter and the condition together; the same instrument limit applies, so each refusal is read from the stderr line and its bracketed class.
- `subject-claim`: **the gate's subject is the semantic index, not the field.** `$Rows/Key For "k1"$` is admitted against a condition of `FieldNotFilled(Rows/Payload For "k1")`, in which `Rows/Key` never appears — so a keyed parameter may name a field the condition does not reference at all, provided the condition keys that group by that key. The Kernel's own refusal text says exactly this ("A semantic index `Rows/Payload For "k1"` can be used in an error text only if it is used in the rule condition in the same way"), but only the admitted row isolates the subject, because every refusing row quotes a field *and* a key together.
- `membership-claim`: condition membership therefore belongs to the **unkeyed** form alone. The unkeyed `$Rows/Payload$` against that same keyed condition is refused `INVALID_FIELD` naming the field as referenced wrongly or unused, which is the ordinary unkeyed gate rather than an index class. The pairing is exact in both directions: a plain-read condition refuses the keyed parameter `MVK_INDEX_FOR_ERROR_TEXT_INVALID`, and so does a condition keyed by a *different* literal.
- `admission-claim`: three further shapes are admitted, each `KERNEL_CONFIRMED`. A literal key matching the condition's; a **field-valued** key, `For Wanted`, where the condition keys the same group by that same field; and the **value suffix before the key**, `$Rows/Payload.value For "k1"$`.
- `grammar-claim`: the suffix precedes the key and nothing else does. `$Rows/Payload For "k1".value$` is refused `MVK_UNEXPECTED_TOKEN` naming the `.`, so a suffix after the key is a parse failure rather than a differently-ordered spelling. An **unquoted** key, `For k1`, is refused `INVALID_ENTITY` naming `k1` as an invalid group or field: an unquoted key is a field reference that failed to resolve, not a token that failed to lex, which is what makes the quoted and field spellings the only two. A key that is itself keyed is refused `ILLEGAL_SEMANTIC_INDEX`, quoting the **inner** index rather than the whole parameter.
- `composition-claim`: the key composes with the **category** suffix. `$Status->Group For "k1"$` on a plainly-referenced Enumeration with a declared category is refused `MVK_INDEX_FOR_ERROR_TEXT_INVALID` — the index gate, reached with the category itself valid — so the key neither consumes nor displaces the suffix.
- `local-divergence`: one, and it corrected a wrong gate before it shipped. The first implementation read the refusal texts alone and gated a keyed parameter on whether the condition referenced *that field* through that key, because every refusing row named a field. The admitted `subject-claim` row shows the subject is the index, and the `membership-claim` row shows the field's own membership is not required at all. Since a semantic index needs a repeatable group and the modeled message fragment is nonrepeatable, that gate could never be posed there: the fragment now decodes a keyed parameter and refuses it as outside its own boundary, and deliberately maps that refusal to **no** Kernel class rather than to the pairing class it cannot evaluate.
- `local-divergence`: The measured grammar in front of the gate — well-formedness, suffix order, key spelling, nesting, category composition — is modeled exactly and separately locked.
- `integrity`: model `dec9bb424bc201ec63dd88809ab3e24b3258ba935159e535362fdceec869df44` after the index additions `25ff01889c61a2b27ff354b4d97402904bc94dd7f033f121bb6974e7858b3b2a`; decision batches `65522f1adf5ce909cec60fb5fd0664ce38d1d6efa630d070de51a5894ef05758` (seven rows) and `98504a9378a184efc569bc73402728b55c3bb64418184eed37e02c3e9e246fd9` (four rows). `rule add --dry-run` persists nothing, so every row rests on those model bytes. All eleven rows were re-run against this exact state when the checkpoint was written, rather than transcribed from the first run.
- `limit`: one repeatable level, one index field, validation phase only, and static admission only — nothing here is a **rendering** claim, so what a keyed parameter interpolates at runtime is unmeasured. Also unmeasured: whether a resolvable key field the condition never names is admitted (the measured field-valued row keys by a field the condition also carries), and which class a parameter violating the index gate *and* a category gate together draws.
- `sync`: locally originated and behavioral. Recorded on [SPEC-2026-08-24-01](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-24-01) with the two grammar checkpoints it extends.

<a id="src-message-group-parameter"></a>
#### The rule-message group parameter `$#…$`, measured locally 2026-08-29

- `revision`: a12-dmkits `4e78f8254bcf12f6d94fc23c5c3cd5e4906c8d04` (clean before and after), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime. Every row is one `rule check --message en_US=…` verdict, the real Kernel error-text checker, dispatched through a caller-tagged `batch`.
- `model`: the `FixedStar_DM` probe model, English condition language, one root `Probe` with repeatable `/Probe/Rows` (containing `Marker` and the subgroup `/Probe/Rows/Deeper`) and fixed `/Probe/Store` and `/Probe/Beside`. Every row uses the same rule: error field `/Probe/Rows/Marker`, condition `Length(Marker) > 3`.
- `confound`: the first attempt used `FieldNotFilled(Marker)` and every row — including the plain-text control — refused `MVK_NEG_CONDITION_IN_ITERATION`. The condition gate pre-empts the error-text checker entirely, so no message row means anything until a positive condition is used. The uniform control failure is the tell.
- `position`: `$#…$` is a **group** parameter position, distinct from the `$Field$` name position. The same word behaves differently in each: bare `$RootGroup$` draws `INVALID_ENTITY` as an entity lookup, while `$#RootGroup$` is admitted.
- `matrix`: the admitted set is the rule's own group and its ancestors. `$#/Probe/Rows$` (own) and `$#/Probe$` (root) are admitted; `$#/Probe/Rows/Deeper$` (a **declared** descendant), `$#/Probe/Store$` (sibling), and `$#/Probe/Nope$` (unknown) all draw `INVALID_GROUP`. Containment runs the opposite way from the computation declaring-group gate: here the *named* group must contain the rule's group.
- `spelling`: the path must be absolute. `$#Rows$` names a real group relative to the rule and still draws `INVALID_GROUP`, as do `$#Probe$`, `$#Store$`, and the field `$#Marker$`.
- `keywords`: `$#RootGroup$` and `$#RuleGroup$` are admitted keyword shorthands for the two endpoints of that chain. `$#LfdNr$`, the German spelling, draws `INVALID_GROUP` under an English condition language, matching the terminal-language rule the clause already states for the name position.
- `retired`: `$#Zeile$` and `$#Usb$` draw `PARAM_INVALID_IN_NEW_WORLD`, a code distinct from `INVALID_GROUP`. The Kernel recognizes both terminals and refuses them on the modern route, which supplies the exact code for the clause's existing assertion that they are not permitted in an A12 model.
- `unrecognized`: `$#Vordruckzeile$`, `$#Vordruckname$`, and `$#index(Rows)$` draw `INVALID_GROUP`, the same code as an arbitrary unknown name, so on the rule-owned carrier they are not terminals in this position at all.
- `controls`: `$Marker$`, `$Marker.value$`, and parameterless text are admitted in the same batch, and `$#NotATerminal$` is refused, so the admissions are not an unreachable-checker artifact.
- `limit`: static admission only, one carrier, one condition language, one root, and one nesting level. What each admitted parameter *renders* is unmeasured, as is the German condition language and every non-rule parameter owner that shares the terminal bundle.

<a id="src-german-condition-language-terminals"></a>
#### The group position's keyword set is selected by condition language in both directions, and the retired terminals are not, measured locally 2026-09-05

- `revision`: a12-dmkits `61ff2f4a4`, the sibling worktree carrying the peer's own work in progress and this project's runs all read-only through the CLI. `dmtool` 0.13.0, Kernel `30.8.1`. One `model new` at `de_DE`, one `config modify --condition-language`, six `rule add --dry-run` candidates, four persisted, `model check` valid with zero diagnostics, every child `KERNEL_CONFIRMED`.
- `question`: [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) carried two named items in this bundle — the German condition language, and **whether the retired-terminal set is selected by condition language at all**. The existing rows measured one direction only, on an English model, so the terminal-language rule rested on a single arm.
- `claim`: **the live keyword set follows the condition language in both directions, and the retired set does not follow it at all.** On a model whose condition language is `de_DE`, `$#LfdNr$` is admitted while `$#RuleGroup$` and `$#RootGroup$` both draw `INVALID_GROUP` — the exact mirror of the English rows, where those two are admitted and `$#LfdNr$` is refused. `$#Zeile$` keeps `PARAM_INVALID_IN_NEW_WORLD` under German, the same code it draws under English, so a retired terminal is recognized and refused whatever the language.
- `paths-are-language-independent`: the absolute-path spelling `$#/Ger/Zeilen$` is admitted in the same batch, and a parameterless message beside it, so the refusals above are the keyword set's and not an artifact of the German model or of this route.
- `switching-the-language-is-refused-once-rules-exist`: a separate and useful authoring fact. `config modify --condition-language de_DE` on a model whose rules were authored in English is **rejected by the Kernel**, one `MVK_UNEXPECTED_TOKEN` per rule naming the first English keyword it can no longer parse. So the condition language is a model-wide setting that cannot be flipped under existing conditions, and a German model must be configured before its rules are authored — which is how this record's model was built.
- `limit`: static admission only for the German rows; one repeatable level, one locale pairing (`de_DE` display with `de_DE` conditions), and the two live keyword shorthands plus one retired terminal. **Blocked, not unmeasured:** what `$#LfdNr$` *renders* — and therefore which endpoint of the chain it abbreviates — needs a runtime observation, and `:adapter:kernelProbe` could not run because the sibling's adapter test sources did not compile at this revision, the peer session being mid-refactor there. The four rules are persisted and the document and request are retained, so the run reproduces once that compiles.
- `bytes`: [`german-condition-language-terminals/`](../../evidence/kernel-30.8.1/captures/german-condition-language-terminals/); the model, six rule candidates, the two batch envelopes, the document, and the request are retained together, kept unprojected after [`LF135`](../LEAN-FINDINGS.md).
- `sync`: **locally originated.** [`spec/11`](../../spec/11-messages-and-custom.md) gains the second direction, the retired set's language independence, and the config-switch refusal, outbound as [`SPEC-2026-09-05-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-03).

<a id="src-message-group-parameter-rendered-index"></a>
#### An admitted group parameter renders the firing row's repetition index, measured locally 2026-09-05

- `revision`: a12-dmkits `388754f32b0348f48926f89adebe1bada1a91627`, the artifact reporting `source.state: CLEAN`. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies with `enginesAgree: true`. One `model new` plus one repeatable group and two fields, six `rule add` candidates, `model check` valid with zero diagnostics, and one `:adapter:kernelProbe` run over a three-row document.
- `question`: [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) carried what an admitted `$#…$` group parameter **renders** as its last open item in that bundle, static admission and both resolution gates having been measured. The Lean fragment took the bytes from its caller and said so, so nothing here was assumed.
- `claim`: **it renders the 1-based repetition index of the named group in the firing row, and `1` for a nonrepeatable group.** A per-row rule at `/Msg/Rows` fires on rows 2 and 3 and its messages read `[own=2]` and `[own=3]` for `$#/Msg/Rows$`, while `$#/Msg$` reads `[root=1]` on both.
- `separator`: the document fires on rows **2 and 3** and not on row 1, which is what distinguishes the row's own coordinate from a **firing ordinal** — the ordinal account predicts `1` and `2` and is refuted by the same two messages. A one-firing-row document would have been consistent with both, and with a rendered row *count* as well.
- `keywords-render-as-their-paths`: `$#RuleGroup$` renders exactly what the own-group path renders (`2`, `3`) and `$#RootGroup$` exactly what the root path renders (`1`, `1`), so the shorthands are abbreviations at the rendering stage too and not a separate value.
- `a-declared-label-does-not-reach-it`: **the position renders a coordinate unconditionally, and a label is the shape that could have shown otherwise.** A second model differing only in declared labels — `Line item` on the repeatable group, `The form` on the root — renders every group parameter **identically**: `2`, `3`, `1`, `1`. The rival that a label would be preferred where one exists is refuted rather than left open.
- `and-labels-do-render-here`: the row that makes that conclusive rather than vacuous is in the same run. Labelling the **field** changes `$Marker$` from `Marker` to `Line marker` in the same message, so labels are live in this model and this locale and simply do not reach the group position. Without it, "labels are inert in this configuration" would have explained the group rows just as well.
- `it-is-a-number-not-a-name`: the control in the same batch settles the position question. `$Marker$` in the **name** position renders `Marker`, the field's name, since the model declares no label; the group position in the same message renders a coordinate. So the two positions differ in what they produce and not only in what they admit.
- `keyword-refusal-reproduced`: `$#LfdNr$` draws `INVALID_GROUP` under this English-condition-language model, reproducing the [admission checkpoint](#src-message-group-parameter)'s row rather than adding to it; the other five candidates persist with `verification: KERNEL_CONFIRMED`.
- `local-consequence`: the Lean fragment's caller-supplied bytes are **gone**, replaced by the measured value: `MessageGroupInput` now carries a repetition index and `ValidationMessageInputs` loses its `group` provider entirely, so no caller can put arbitrary text into a rendered message any more. Only the nonrepeatable arm is reachable there and the cases lock exactly that, because the parameter's containment gate makes the named group a prefix of the rule's row group and this route's condition spine refuses a repeatable reference — so the fragment has no repeatable rule host to name one. The per-row arm is spec-stated and unreachable rather than modelled and untested.
- `limit`: one repeatable level, one nonrepeatable root, `en_US`, one locale, no declared group labels, `validateFull` only. Not measured: a group parameter under two repeatable levels, the German condition language, and rendering inside a computation's self-validation message. The declared-label shape was the one that could still have put text in this position and is measured in the rows above.
- `sibling-state`: the sibling worktree was clean when this ran, and the artifact's own `source.state: CLEAN` at that revision is the provenance. It went dirty **two seconds later** from the peer session's own editing, which [`LF137`](../LEAN-FINDINGS.md) records as a hazard of a live peer checkout rather than a defect in this run.
- `bytes`: [`message-group-parameter-rendering/`](../../evidence/kernel-30.8.1/captures/message-group-parameter-rendering/); the model, the six rule candidates, the document, the request, and the artifact are retained together. The label control is a **second model in `labelled/`** differing only in three `meta` calls, with the same document and request beside it, so the pair is one variation rather than two unrelated runs; its artifact is at a12-dmkits `3a29e0924826`.
- `sync`: **locally originated.** [`spec/11`](../../spec/11-messages-and-custom.md) gains the rendering rule beside the admission gates it already carried, outbound as [`SPEC-2026-09-05-02`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-05-02).

<a id="src-message-group-parameter-resolution"></a>
#### How the rule-message group parameter resolves, measured locally 2026-08-29

- `revision`: a12-dmkits `4e78f8254bcf12f6d94fc23c5c3cd5e4906c8d04` (clean before and after), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime. Every row is one `rule check --message en_US=…` verdict through a caller-tagged `batch`, all `KERNEL_CONFIRMED`. It refines the [admission checkpoint](../SOURCES.md#src-message-group-parameter), which used a one-root model and no quoted spelling.
- `model`: `GroupPos_DM`, authored with the structured verbs, kernel-valid by `model check`, with **two root groups**: `/Probe` (repeatable `/Probe/Rows`, its subgroup `/Probe/Rows/Deeper`, and fixed `/Probe/Store`) and `/Second` (with `/Second/Inner`). Rules carry a positive condition on their own group's field.
- `two-roots`: a model with more than one root group is authorable and kernel-valid. `group add --parent ""` is accepted `KERNEL_CONFIRMED`, the terminal `apply` gate committed it, and `model check` reports the model valid. This is the witness that makes the root shorthand's meaning measurable at all.
- `root-shorthand`: `$#RootGroup$` names the root of the **rule's own** ancestor chain, not a model-wide root. For a rule under `/Second/Inner` the shorthand is admitted while `$#/Probe$` and `$#/Probe/Rows$` — the other root and its subgroup — both draw `INVALID_GROUP`. Symmetrically a `/Probe/Rows` rule refuses `$#/Second$`. Admission alone cannot separate "resolves to the chain root" from "always admitted", since the chain root always contains the rule's group; what it does settle is that the primary root is not what the shorthand means.
- `two-stage`: an absolute path resolves in two gates the Kernel reports **apart**. An undeclared first segment draws `INVALID_ROOT_GROUP` whatever follows it — `$#/Nope$` and `$#/Nope/Rows$` alike, and `$#/probe$` too, so the root match is case-sensitive. A **declared** root followed by an unknown group (`$#/Probe/Nope$`, `$#/Second/Nope$`) or by a group that does not contain the rule draws `INVALID_GROUP`. Root existence therefore precedes containment and is a separate gate, which corrects this project's first account that one code covered the whole refusal.
- `quoting`: each segment takes the name grammar's single-quote escape and the quotes are erased before lookup: `$#/'Probe'/'Rows'$`, `$#/'Probe'/Rows$`, `$#/Probe/'Rows'$`, and `$#/'Probe'$` are all admitted, and `$#/Probe/'Nope'$` draws the ordinary `INVALID_GROUP`.
- `quoting-suppresses-terminals`: quoting is **not** merely erased first. `$#'RootGroup'$` and `$#'RuleGroup'$` draw `INVALID_GROUP` naming a group `RootGroup`/`RuleGroup`, and `$#'Zeile'$` draws `INVALID_GROUP` rather than the retired class its bare spelling draws. So the quote decides whether the word is a terminal at all, and the terminal match is on the raw argument.
- `lexical`: a malformed spelling is refused before any group is looked up, on codes distinct from both group classes: an unbalanced quote and an empty quoted segment draw `MVK_LEXER_STANDARD_ERROR` with a character position, a trailing separator draws `MVK_INCOMPLETE_INPUT`, and a doubled separator draws `MVK_UNEXPECTED_TOKEN`.
- `chain-depth`: a rule at `/Probe/Rows/Deeper` admits its own group, `/Probe/Rows`, `/Probe`, and `$#RootGroup$`, and refuses the sibling `/Probe/Store`, so the admitted set is the whole ancestor chain at any depth rather than one level up.
- `controls`: plain-text rows and `$Tag$`/`$Marker$` name-position rows are admitted in each batch, and every refusal carries a Kernel diagnostic, so no admission is an unreachable-checker artifact and no refusal is a front-end rejection.
- `limit`: static admission only, one condition language, and no runtime observation — what any admitted group parameter **renders** is still unmeasured, as is whether the retired-terminal set is selected by condition language, and whether a quoted segment may carry characters a bare one may not. `INVALID_ROOT_GROUP` was reached only through the absolute form; no keyword spelling can name a root the model does not declare, so that combination has no witness here.
- `sync`: locally originated and behavioral. It corrects and extends [`SPEC-2026-08-29-04`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-29-04) rather than opening a second entry, because that entry is still pending outbound.

<a id="src-message-address-dialects"></a>
#### A message address is written in one of two dialects, and which one depends on the operand, re-read locally 2026-08-31

- `route`: no new kernel run. Every row below is re-read from artifacts already retained under [`evidence/kernel-30.8.1/captures/`](../../evidence/kernel-30.8.1/captures/), each produced by `:adapter:kernelProbe` on both codegen strategies at its own recorded revision. The claim is about bytes this project already held; the re-reading is what is new.
- `two-dialects-claim`: **a `referenced` or `fillToFix` entry is written in one of two spellings, and one message routinely carries both.** The **instance** dialect is bare, index-one-elided, and varies with the document: `Order/Lines[2]/Produced`. The **declaration** dialect leads with a slash, elides nothing, and marks the iterated level: `/Order[1]/Lines[0]/Produced`. Across every retained artifact, 292 messages carry both spellings in one list, 60 carry only the slash form, and 876 only the bare form, over 22 captures.
- `framing-retracted`: **every row below is a correct reading of these artifacts and a wrong reading of the kernel, on the one point of what a "dialect" is** — the observing probe renders one typed pointer two ways, so the counts and exclusions hold about the bytes while the kind they distinguish is exact-versus-partially-known. Read "dialect" that way; the [selector checkpoint](#src-address-dialect-selector) owns the correction and [`LF130`](../LEAN-FINDINGS.md) the lesson.
- `bracket-claim`: **the slash dialect's `[0]` is not row zero — it marks the iterated level.** `/Order[1]/Lines[0]/Produced` is byte-identical across four documents of the same capture holding two rows, two rows, two rows, and **no rows at all**, so the number cannot be an instance coordinate. `Order` is nonrepeatable and still carries `[1]`.
- `ancestor-claim`: the levels *above* the iterated one carry their concrete **1-based** coordinate rather than a constant. One capture emits `/FullDateParentProbe_DM[1]/Projects[1]/Choices[0]/PromiseDate`, `.../Projects[2]/...`, and `.../Projects[3]/...` for a `Projects` group declared `max 3` — so `[3]` is the third row and not a fourth, which fixes the base as one and rules out the 0-based reading the `[0]` suffix invites.
- `separator`: two levels can be iterated at once. `/Probe[1]/Rows[0]/Inner[0]/I` comes from a two-level starred operand, so `[0]` is per-level rather than a single marker for the innermost, and an account reading it as "the last segment before the field" would produce `Rows[1]` there.
- `correlation`: the slash dialect appears where the address names a **resolved starred or group-scope operand** — group counts, aggregates over a repeatable group, and the source side of a first-filled scan — while the rule's own target field is written bare in the same message. That is a correlation over 22 captures rather than a measured cause; a message whose starred operand is written bare, or whose plain target field is written with a slash, would defeat it, and no retained row does either.
- `cause-narrowed`: **one candidate cause is excluded, on bytes retained 2026-08-31.** The [capacity consumer sweep](group-list-and-capacity-probes.md#src-capacity-consumer-sweep) carries three rules over the same repeatable `Tag` field — `FieldValuesNotUnique` over the **starred field**, the same operator over the **fixed unstarred group** `/Cap/TShell`, and a `NumberOfDifferentValues` ladder over both forms — and every one of them emits the byte-identical `/Cap[1]/TShell[1]/TRows[0]/Tag`. So the star is **not** what selects the slash dialect: an operand carrying no wildcard at all reaches it too, through a group whose expansion crosses a repeatable level.
- `crossing-excluded`: **repeatable crossing is excluded as the selector, within one capture.** The [nested alternating-Having capture](../../evidence/kernel-30.8.1/captures/nested-current-repetition-alternating-enumeration-having/artifact.json) at a12-dmkits `25e048ea7` emits `Order/Lines/Entries/Base` and `/Order[1]/Lines[1]/Entries[0]/Choice` from the same model and the same documents. Both fields sit two repeatable levels down, so depth and crossing are held constant while the dialect changes; and the slash one writes a **concrete** `[1]` at `Lines` beside `[0]` at `Entries`, so the marker is per level rather than a flag on the whole address. What varies is whether the reference is bound to a row by the rule's own iteration.
- `settler-was-filed-elsewhere`: that capture is named for a four-stage `CurrentRepetition` Enumeration `Having` chain and was retained to calibrate computation cascades. Nothing in its name, its question, or its `sync` row mentions an address; a search for "dialect" or "message address" reaches none of it. This is [`LF121`](../LEAN-FINDINGS.md) again at its usual range — the settling bytes are indexed by the question that motivated the probe, and the way in was to grep the retained artifacts for the *shape* of the answer, a bare and a slash address in one file, rather than for the words.
- `expansion-refuted`: **expansion falls to a reference inside the filter, and it falls under both readings of it.** The same capture's computation `TargetComp` computes `../Target` from `FirstFilledValue(DirectChoice, Entries*/Choice Having ([Entries/Third] == [$Limit] And [Entries/First] == [$Limit]))`, and its self-validation message lists six references in one channel: `/Order[1]/Lines[1]/Entries[0]/Choice`, `.../First`, and `.../Third` slash, beside bare `Order/Lines/DirectChoice`, `Order/Lines/Limit`, and `Order/Lines/Target`. Read expansion strictly — the reference came from a group or star operand rather than a direct field — and `[Entries/Third]` predicts bare where it measures slash. Read it by slot, the `Having` included, and `[$Limit]` in that same filter predicts slash where it measures bare. Both defeaters sit inside one `Having`, holding operand form, operator, depth, message, and document equal across the pair, so only the `$` capture varies.
- `cause-remaining`: **an unbound level** is the single surviving account — the unmarked candidate reference passes the star's unbound level and the captured one is bound at the outer row — and every reference in every retained capture fits it, the six above included. It stays a correlation because no row was produced to test it, but the pending document is now a **prediction** rather than a tie-break: a rule whose operand is a group expanding to **nonrepeatable fields only** must emit the **bare** dialect. The retained [`Kind_DM`](../../evidence/kernel-30.8.1/captures/distinct-count-first-operand-class/Kind_DM.json) declares such a group, `/Kind/StrG` over two nonrepeatable Strings, and `NumberOfDifferentValues(/Kind/StrG) == 1` is measured admitted, so it needs one `validateFull` row and no new model. A slash result there would leave no candidate standing.
- `filtered-star-coordinates`: that same message is the coordinate witness [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) asked for, in the **token** family. A filtered star's selected target and the unmarked field operands of its own `Having` are all referenced, at byte-identical coordinates: the one repeatable level above the star concrete at `Lines[1]`, the nonrepeatable root carrying `[1]`, and the starred level `[0]`. That witnesses [`spec/01`](../../spec/01-data-model.md)'s concrete-above and wildcard-at-or-below rule on a *filtered starred field*, and its `$`-resolves-at-the-captured-outer-row half on the same expression. The document instantiates one `Entries` row, so the `[0]` is again not a row. Number and Boolean or Confirm stay unwitnessed; this is one carrier, not the family ([`LF116`](../LEAN-FINDINGS.md)).
- `consumer-limit`: **the address does not identify the operand form that produced it.** The starred-field and fixed-group rules above are two different models and their references are byte-equal, so an Explain or Analyze consumer reading `referenced` cannot recover whether the author wrote a star or a group. That has to come from the checked condition, not from the message.
- `elision-reproduced`: the index-one elision is reproduced on a fresh capture in the formal-error channel: `Cap/NShell/NRows/Amount` for the violated row one and `Cap/NShell/NRows[3]/Amount` for row three, in the same artifact. The same cell that the slash dialect writes `/Cap[1]/NShell[1]/NRows[0]/Amount` is written bare with a **concrete** index here, so both spellings of one cell appear in one artifact and differ by channel rather than by document.
- `third-surface`: the rendered **message text** is a third spelling and carries no coordinate at all. `zuGrosseKontextnummer` renders `The index for group '/Probe/Tri/A' was specified with 3, but at most 2 is allowed.` — a leading-slash declaration path with no brackets — while the same finding's `field` and `referenced` carry the instance address `Probe/Tri/A[3]/B[2]/C/TriValue`. The offending index and the cap are separate rendered numbers rather than part of the path. So a consumer wanting the offending row must read `field`; the text names only which group's capacity was exceeded.
- `elision-witness`: that same `field` settles the elision within one string: `A[3]` is indexed and `B` beside `B[2]` is not, so repetition one is elided and higher repetitions are written, at two levels of one address. The row-level `zuGrosseZeile` addresses the violated row itself and renders a parameterless text: `Probe/Tri/A[3]`, `Probe/Tri/A/B[3]`, `DateTimeRows/Projects/Tasks[4]`.
- `bracket-corroborated`: the `[0]`-is-not-a-row fact is independently reproduced at a12-dmkits `a8bd8e864`, on its own fixture and byte-identical with zero rows and with four filled ones. That reading is stronger than this one on a point that matters: it comes from the pointer object's `repetitionIndexes()` rather than from a rendered string, so the `[0]` is **kernel data** and not a rendering artifact.
- `peer-cannot-settle`: the two-dialect finding itself is **not** corroborated there, and must not be recorded as if it were. That project re-renders every pointer through its own formatter, slash-led and eliding nothing, so both kernel spellings arrive as one and its uniform observation is a property of the instrument rather than a measurement. Eliding index one is precisely the transformation that formatter refuses, so the defeater named below cannot come from that estate at all — it has to come from these captures or from a raw kernel string neither project currently reads.
- `limit`: re-read evidence only, over the captures listed above. Not established: what the kernel's own accessor names these two shapes, whether a consumer is expected to resolve the declaration dialect against a document, and whether an over-limit or absent iterated level changes the `[0]`. No row here was produced to answer this question, which is exactly why the correlation is not stated as a mechanism.
- `local-consequence`: this reframes [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration)'s open item, which asked whether index-one elision is a rendering dialect or a channel difference. It is neither: one channel carries both spellings, and they differ in kind rather than in rendering. A `MessagePointer` widened to carry `fillToFix` must therefore represent two address shapes, not one shape with an optional index.

<a id="src-address-dialect-selector"></a>
#### An unbound repeatable level is what selects the slash dialect, measured locally 2026-08-31

- `revision`: one `:adapter:kernelProbe` request at a12-dmkits `8cad1224f86e91c46f0db3573adb4f0fe894a959`, the sibling checkout clean before and after and `producer.source.state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true`.
- `question`: the [dialect checkpoint](#src-message-address-dialects) had excluded the star, repeatable crossing, and expansion, leaving one account — a reference whose resolved path passes a repeatable level the rule's iteration did not bind. That account made a falsifiable prediction and nothing had tested it.
- `prediction`: recorded before the run. A group operand expanding to **nonrepeatable fields only** has no unbound level anywhere, so an unbound-level account predicts the **bare** dialect; the refuted expansion account predicted the slash one. A slash result would have left no candidate standing.
- `claim`: **bare, as predicted.** `NumberOfDifferentValues(/Kind/StrG) == 1` over a nonrepeatable group of two nonrepeatable Strings fires with `referenced` exactly `Kind/StrG/S1`, `Kind/StrG/S2` — no leading slash, no bracket, on both strategies. So the group operand's expansion is not what reaches the declaration dialect; passing an unbound repeatable level is.
- `separator`: the two-distinct-values document fires nothing, so the rule is a live gate rather than a shape that always reports.
- `denominator`: this is the surviving account of the four this project could name, with one confirmed prediction on a carrier chosen to separate it — not a proof that no fifth property covaries. The isolating half is the [`expansion-refuted`](#src-message-address-dialects) row's pair of references inside one `Having`, where only the `$` capture varies.
- `model`: the retained [`Kind_DM`](../../evidence/kernel-30.8.1/captures/distinct-count-first-operand-class/Kind_DM.json) with one rule added through `dmtool rule add`, `KERNEL_CONFIRMED`; the model was not hand-edited.
- `bytes`: [`group-operand-address-dialect/`](../../evidence/kernel-30.8.1/captures/group-operand-address-dialect/); SHA-256 model `e81129c2be66388f4d89f61e58ddc0647b75c293b264e978e71d33feee80f684`, request `d49de38538b3dfebab3db52350ac6b91ec6b08df21c8332b17d7da4fe35f4f23`, artifact `6f824a7a701331f02f9ba5663900952f0579db42047cef739ebab063640c4761`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `limit`: one nonrepeatable topology, `en_US`, one operator, static and runtime in one row. Not measured: a group operand mixing nonrepeatable and repeatable descendants, a keyed or semantic-index read, and whether an over-limit or absent iterated level changes the `[0]`.
- `peer-confirmed-unable`: the [`peer-cannot-settle`](#src-message-address-dialects) prediction held. At a12-dmkits `4e174d86a` the peer read its own `interpreter/…/eval/Pointer.kt` rather than taking the description: `formatPointer` prepends `/` to every segment, emits `[n]` on every group segment whatever its value, and drops the last part's index, so both kernel spellings arrive as one string and no comparison through that channel can corroborate or refute this.
- `peer-holds-the-data`: **the flattening is in their rendering and not in their observation.** `fmtPointer` builds that string from the kernel's `PartiallyKnownDocumentMultiPointer.fullName()` and `.repetitionIndexes()`, both of which they hold unrendered. So the corroborable form of this finding is not "two strings" but the index vector behind them, and their formatter in fact preserves the part that matters — a `0` survives it, only the index-one **elision** does not.
- `premise-checked`: they cautioned that A12 defines no parser and no renderer for a single **partially-known** pointer, having shipped and then removed a `format`/`parse`/`render` trio for one as an invented serialization, and asked which kernel class emits a pointer string before either project treats one as an A12-owned form. Their own `adapter/…/FieldPointer.java` narrows that caution rather than contradicting it: it states that a rendered *exact* pointer has one canonical A12 form — `Order/Items[2]/Count`, `[1]` omitted, citing `A12PointerFormat` and their KERNEL-FINDINGS — and their TypeScript consumer test pins `format("/Permit/Note", [1, 0])` to `Permit/Note[0]`. That is their account of A12's form, read here and not measured here; what it does supply is a reading under which the **bare** dialect is the canonical exact rendering and only the slash-led one is unattributed.
- `hypothesis`: the exchange above produced one testable claim — the two spellings are two pointer **kinds**, an exact address and a partially-known one, with an unbound repeatable level being what makes a reference partially known, predicting that every slash-led address carries a `0` in its index vector and no bare one does. This project reads rendered strings and cannot check a vector; a12-dmkits reads it on both channels and did.
- `vector-corroborated`: **that hypothesis holds, measured at a12-dmkits `b145ce565` as `KF228` and locked by `adapter.laws.MessagePointerVectorDiffTest`.** Two rules over the same referenced field, differing only in iteration scope, on one document with `Lines[1]` instantiated: a rule at the root `/Order` leaving the `Lines` level free gives `repetitionIndexes()` `[1, 0, 1]`, and the same reference from a rule bound to `/Order/Lines` gives `[1, 1, 1]`. The `0` sits exactly at the repeatable level the iteration left unbound and the concrete row replaces it when bound; the vector is positional over path parts and the trailing `1` is the field part in both. So the selector this project derived from rendered strings is confirmed on the typed value, through a channel their renderer flattens.
- `two-spellings-retracted`: **the "two spellings" half was a claim about the instrument, and is withdrawn.** `KernelRuntimeProbe.address` publishes one documented two-arm policy — canonical A12 when `toExactPointer()` succeeds, the interpreter's house form when it does not — so the bare and slash-led strings in every artifact here are that branch, not two forms the kernel emits. The branch predicate is *itself* the selector — `toExactPointer()` returns null exactly when a non-final level carries `0`, pinned by their `A12PointerDomainTest.aPartiallyKnownPointerConvertsToTheExactTypeOnlyWhenItIsOne` — so the correlation was analytic and could never have been kernel evidence about spellings. What survives is value-level: an address either names one cell or carries `0` at each unbound repeatable level, which is what [`spec/01`](../../spec/01-data-model.md) now states. [`LF130`](../LEAN-FINDINGS.md) owns the general lesson.
- `consumed-surface`: a12-dmkits retracted its own mirror of that correlation in `KF228` the same day, having recorded the agreement as corroboration, and its `docs/KERNEL-PROBE-SPEC.md` §4 now states that the rendering arm is a projection of the coordinate and must never be correlated with it. That is a prose warning on a contract this project reads; the artifact's shape, schema, and content are unchanged, so no retained bytes move.
- `sync`: outbound [`SPEC-2026-08-31-16`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-16), **answered**: the peer cannot corroborate through its renderer, exactly as the entry's acceptance condition anticipated, and then corroborated the substantive half through the typed vector instead. [`spec/01`](../../spec/01-data-model.md) drops the rendering claim and keeps the coordinate rule. Inbound from committed reviewed revisions, so it opens no new outbound entry.

<a id="src-message-construction-route"></a>

### Message construction

At kernel revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, [`RuleErrortextChecker`](../../../a12-kernel/kernel-tool/kernel-core-service/src/main/java/com/mgmtp/a12/kernel/core/service/internal/errortext/RuleErrortextChecker.java) and [`FehlertextUtil`](../../../a12-kernel/kernel-tool/kernel-core-service/src/main/java/com/mgmtp/a12/kernel/core/service/internal/errortext/FehlertextUtil.java) own nonempty text, line/control/encoding checks, even-dollar admission, and parameter checking in that order.

[`GrammarOnly_ErrorTextParameterParser.g4`](../../../a12-kernel/kernel-tool/kernel-core-service/src/main/antlr4/com/mgmtp/a12/kernel/core/service/internal/errortext/grammar/gen/GrammarOnly_ErrorTextParameterParser.g4) plus [`FullChecks_ErrorTextParameterParser`](../../../a12-kernel/kernel-tool/kernel-core-service/src/main/java/com/mgmtp/a12/kernel/core/service/internal/errortext/grammar/gen/FullChecks_ErrorTextParameterParser.java) own token grammar, model lookup, and condition-reference legality. The selected Lean capsule deliberately supports only printable ASCII plus one bare nonrepeatable field name; wider legal syntax is explicit insufficiency, not a claimed kernel rejection.

[`FehlertextBuilder`](../../../a12-kernel/kernel-tool/kernel-core-codegen-rules/src/main/java/com/mgmtp/a12/kernel/core/codegen/internal/generator/backingbeans/meta/FehlertextBuilder.java) splits accepted text while preserving empty dollar-delimited segments, normalizes name/value references, and retains `$$` for runtime decoding. The Java, Groovy, and JavaScript [`CodeGenRuleDefinition.st`](../../../a12-kernel/kernel-tool/kernel-core-codegen-rules/src/main/resources/internal/templates/validation/java/rulesDir/CodeGenRuleDefinition.st) dialects attach messages only in the fired branch.

[`MainValidatorController.substFehlertext`](../../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/core/MainValidatorController.java) performs one split/substitute/join pass: an empty parameter becomes one literal `$`, field name/value parameters resolve once, and inserted bytes are not reparsed.

Field-owned type-format and requiredness messages use distinct routes. [`FieldErrortextChecker`](../../../a12-kernel/kernel-tool/kernel-core-service/src/main/java/com/mgmtp/a12/kernel/core/service/internal/errortext/FieldErrortextChecker.java) runs the common encoding, line/control, and even-dollar checks before comparing delimited parameters with the locale-fixed words supplied by [`CommonUtils.CONST_FIELD`](../../../a12-kernel/kernel-tool/kernel-core-base-utils/src/main/java/com/mgmtp/a12/kernel/core/utils/internal/CommonUtils.java): `field`/`field.value` for US English and `Feld`/`Feld.Wert` for German.

The Java runtime helper [`FehlerHandler.transformFehlerText`](../../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/formatdef/error/FehlerHandler.java) performs fixed name replacement and display-value replacement directly, with a null display falling back to the raw supplied value, and contains no visible call to the rule path's empty-parameter decoder. a12-dmkits revision `ed0b0cd9f7110daa354c2404e7b9ca6df4f42fa3` supplies the missing observation: the `en_US` String-pattern producer accepts only lowercase `$field$`/`$field.value$`, rejects `$$` as an empty parameter, and inserts token-looking value bytes once; the measured requiredness producer instead accepts and preserves `$$`.

Dynamic Groovy and generated Java agree under the same explicit label-or-name provider profile.

Validation messages are rendered before public result assembly. `FehlerHandler` selects locale resources and `CheckCommand.ersetzeFeldParameter` performs the computation-target field token replacement. Authored `$Field$`/`$Field.value$` template handling is a different syntax and must not be reused for generated target text. Read-only inspection of a12-dmkits [`Message.kt`](../../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/eval/Message.kt) at revision `351bf9f28138783ef7ddc7d8c76db2ab4891a5d8` establishes a current peer gap: its nonempty placeholder regex covers name/value tokens but cannot decode `$$`, so the canonical literal-dollar case still needs an upstream implementation and maintained JVM/Node lock.

Computation produces one formal-message stream. `DocumentComputationResultImpl` partitions it solely by exact error pointer against nonempty computed instances. `PartiallyKnownDocumentMultiPointer` admits concrete coordinates plus wildcard and unknown; `CellAddr` alone cannot represent the full message-pointer domain. Payload text, code, and locale do not affect partition membership.
