# Repeatable temporal computation source checkpoints

<a id="src-date-range-repeatable-first-filled"></a>
#### Sibling-star DateRange `FirstFilledValue` into exact repeatable targets, measured locally 2026-08-27

- `revision`: the disposable model, computation, and runtime probe used the source-matched launcher at clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, which the retained artifact records as its producer with state `CLEAN`. dmtool `0.13.0`, kernel `30.8.1` built and runtime; the sibling worktree stayed unchanged before and after the probe.
- `route`: one `Europe/Berlin` model declares repeatable `Projects`, with sibling repeatable `Choices` and `Tasks` below each project. `Window` and computed `SelectedWindow` both use `yyyy-MM-dd` with `/`. A structured dry-run admits `FirstFilledValue(../Choices*/Window)` directly on the repeatable task target without a synthetic iteration guard. The committed disposable model then feeds `:adapter:kernelProbe`, observing `validateFull` and `compute` on dynamic Groovy and static Java.
- `correlation-claim`: each task row scans only its enclosing project's sibling choices. Two tasks under project 1 both receive `2024-06-01/2024-06-30`, while two tasks under project 2 both receive `2024-07-01/2024-07-31`; a global scan would give June to every row. The five results retain distinct exact target addresses, including the second task under each of the first two projects.
- `clearing-claim`: a third project with one stale task target and no choices returns `cleared`, so exhaustion does not preserve old target text. A separate document places malformed DateRange text before a valid source row; the malformed cell appears in both the full-validation and `formalErrorsInOperands` channels, and the dependent task target returns `cleared`. The artifact establishes the public prepass and result channels, not a causal error object on the target outcome.
- `strategy-claim`: both Kernel codegen strategies agree on every message, formal operand error, result address, value, and clear in both documents.
- `integrity`: model `433a51ac4b66bcfa086777976d91fc5b3f345cc65c2c2ea198a9a480641d6fa1`; request `c864a4f6b3a0f0bbc00d314490a9b90bc1761800e1e92dc10bb75f0fca07f1d1`; artifact `56deae30f86aef6bfec440d89468e841c180abfc7af7f2611965857957ba8165`. The retained bytes are under [`repeatable-date-range-first-filled`](../../evidence/kernel-30.8.1/captures/repeatable-date-range-first-filled/).
- `limit`: one exact DateRange profile, one sibling-star axis under one enclosing repeatable level, `Europe/Berlin`, at most two source and two target rows per parent, no target-local rejected attempt, and no computed-source dependency. Wider profiles, cross-spelling target rendering, year interpretation, root/global-star shapes, materialized document application, later validation, and pointer rendering remain unmeasured on this route.
- `sync`: locally originated and consistent with the existing direct-star component-set and first-filled clauses. It corrects the stale blanket reachability statement for generated computation rules in [`spec/09-computations.md`](../../spec/09-computations.md) and creates outbound reconciliation entry [`SPEC-2026-08-27-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-01--a-sibling-star-computation-may-target-a-repeatable-group-and-stays-parent-local).

<a id="src-datetime-repeatable-first-filled"></a>
#### Sibling-star DateTime `FirstFilledValue` into exact repeatable targets, measured locally 2026-08-27

- `revision`: the disposable model, computation, and runtime probe used the source-matched launcher at clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, which the retained artifact records as its producer with state `CLEAN`. dmtool `0.13.0`, kernel `30.8.1` built and runtime; the sibling worktree stayed unchanged before and after the probe.
- `route`: one `Europe/Berlin` model declares repeatable `Projects`, with sibling repeatable `Choices` and `Tasks` below each project. `Stamp` and computed `SelectedStamp` both use complete `yyyy-MM-dd'T'HH:mm:ss`. A structured dry-run admits `FirstFilledValue(../Choices*/Stamp)` directly on the repeatable task target without a synthetic iteration guard. The committed disposable model then feeds `:adapter:kernelProbe`, observing `validateFull` and `compute` on dynamic Groovy and static Java.
- `correlation-claim`: each task row scans only its enclosing project's sibling choices. Two tasks under project 1 both receive `2024-06-01T10:11:12`, while two tasks under project 2 both receive `2024-07-01T13:14:15`; a global scan would give the June instant to every row. The results retain distinct exact target addresses for both task rows under project 1 and the task under project 2.
- `representation-limit`: the sibling artifact observes the selected complete ISO text under one shared source/target format and one model zone, so it does not distinguish exact-instant selection plus target rendering from source-text copying. The Lean route deliberately retains the exact-instant representation and model-zone target renderer already owned by the direct DateTime account, with an incoherent stored/raw fixture that separates those internal accounts; this capture establishes sibling admission, correlation, exact result addresses, and observable target text only.
- `clearing-claim`: a third project with one stale task target and no choices returns `cleared`. A fourth project places malformed DateTime text before a later valid choice; the malformed cell appears in the formal operand channel and the dependent task target returns `cleared`. This records the public channels and does not assign a causal error object to the target outcome.
- `generated-rule-claim`: the artifact's only computation message is anchored at the exact stale second task target below project 1. Absent targets and the cleared stale target below project 3 do not report that message. No filled matching target was supplied, so this capture adds no matching-value control.
- `strategy-claim`: both Kernel codegen strategies agree on every message, formal operand error, result address, value, and clear.
- `integrity`: model `88d47b97977abe20dc5bb67e9408aceded442ca6cf939631df03df667c133a2c`; request `7f2595d9dd705a3d8e89e9f17d043c8fa7715aea15b98780aa341a87ce04e094`; artifact `ef2067dfeb994e7fb93f6e99840fb9f3d6573982b7f96a285d3e1658f92e62d4`. The retained bytes are under [`repeatable-datetime-first-filled`](../../evidence/kernel-30.8.1/captures/repeatable-datetime-first-filled/).
- `limit`: one complete DateTime format, one sibling-star axis under one enclosing repeatable level, `Europe/Berlin`, at most two source and two target rows per parent, no target-local rejected attempt, and no computed-source dependency. Other formats and zones, root or global stars, multi-axis sources, materialized document topology, later validation execution, and pointer rendering remain unmeasured on this route.
- `sync`: this complete DateTime sibling is accepted at reviewed a12-dmkits revision `38aeb23b5849b831f8ae6ee09579367e28ce982b` under [`SPEC-2026-08-27-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-01--a-sibling-star-computation-may-target-a-repeatable-group-and-stays-parent-local).

<a id="src-full-date-repeatable-first-filled"></a>
#### Checked sibling-star FULL Date target admission, measured locally 2026-08-27

- `revision`: clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, dmtool `0.13.0`, kernel `30.8.1` built and runtime. The sibling worktree was empty before and after the probe.
- `route`: a disposable dmtool-authored UTC model declared repeatable `Projects`, with sibling repeatable `Choices` and `Tasks` below each project. Source `/FullDateParentProbe_DM/Projects/Choices/PromiseDate` was FULL ISO Date without the pre-1900 check; computed target `/FullDateParentProbe_DM/Projects/Tasks/SelectedDate` was FULL ISO Date with `rejectBefore1900=true`. Structured field reads confirmed both policies.
- `static-claim`: source-matched `computation add --dry-run` and the persisted add admitted `FirstFilledValue(../Choices*/PromiseDate)` on the repeatable checked target with `verification: KERNEL_CONFIRMED` and no diagnostics. Structured read-back retained the exact target and operation, and terminal `model check` reported `valid:true` with Kernel verification. This is a positive authorability witness for the exact policy and topology, not an inference from a scalar computation or an adjacent format.
- `runtime-claim`: reviewed peer `RepeatableCheckedDateTargetDiffTest` establishes the matching dotted FULL profile across dynamic Groovy, generated Java, and the interpreter. Because that test did not execute the ISO declaration claimed here, a separate source-matched local probe at clean peer revision `38aeb23b5849b831f8ae6ee09579367e28ce982b` reconstructed the structured ISO model and ran both Kernel strategies. They agree that `1899-12-31` is an exact-address ERRORED attempt with no formal operand finding, `1900-01-01` and `2024-03-15` are values, and malformed `2024-02-31` clears with `datumFormatFalsch` in the formal operand channel.
- `integrity`: the retained [ISO model](../../evidence/kernel-30.8.1/captures/repeatable-full-date-first-filled-iso/FullDateParentProbe_DM.json), [request](../../evidence/kernel-30.8.1/captures/repeatable-full-date-first-filled-iso/request.json), and [artifact](../../evidence/kernel-30.8.1/captures/repeatable-full-date-first-filled-iso/artifact.json) hash to `43ae0f59f199e35d702e5c9720c0de992c05c5aaab9d7bc011231d55a009b155`, `b145602e558c0592c091a33267dad67faf4ad8c82f6e685c16ab876120b2e107`, and `a22b1b5b245bbae2386ba7a0dfe6771f253a1acd9b916f7e4f8727be0bd862ef`. The artifact reports dmtool `0.13.0`, Kernel `30.8.1` built and runtime, the exact clean peer revision, and `enginesAgree:true` for both rows.
- `limit`: one sibling-star axis below one shared repeatable parent, matching ISO and dotted FULL profiles, unchecked source and checked target, and pre-1900, boundary, ordinary, malformed, and parent-isolation controls. A checked source, cross-format assignment, and wider target policies remain outside.
- `sync`: accepted under [`SPEC-2026-08-27-02`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-02--a-repeatable-full-date-firstfilledvalue-target-may-enable-the-pre-1900-check).

<a id="src-time-repeatable-first-filled"></a>
#### Sibling-star Time `FirstFilledValue` target admission and runtime, measured locally 2026-08-27

- `revision`: clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, dmtool `0.13.0`, kernel `30.8.1` built and runtime. The sibling worktree was empty before and after model authoring, the runtime probes, and the focused constructor control.
- `route`: one retained structured dmtool model declares repeatable `Projects`, with sibling repeatable `Choices` and `Tasks` below each project. Source `SourceTime` and computed target `SelectedTime` both use the sole supported Time profile, `HH:mm:ss`. A second retained scalar model carries `TimeFromDateTime` and direct-star Time `FirstFilledValue` controls. Both models were created and atomically edited through dmtool with terminal Kernel confirmation, then supplied to the existing `:adapter:kernelProbe` route for dynamic Groovy and generated Java.
- `static-claim`: source-matched `computation add --dry-run` and the persisted add admitted `FirstFilledValue(../Choices*/SourceTime)` on the repeatable sibling target with `verification: KERNEL_CONFIRMED`. Structured computation read-back retained the exact operation, and terminal `model check` reported `valid:true` with Kernel verification. Removing only the star produced `FirstFilledValue(../Choices/SourceTime)` and was refused with `MVK_NO_WILDCARD`, separating the accepted reopened-axis route from an ordinary unstarred sibling read.
- `runtime-claim`: each physical task row scans only its enclosing project's complete sibling choice extent. An empty first row followed by `10:11:12` selects that clock for both tasks in project 1, while project 2 independently selects `13:14:15`. The selected value hides a later malformed choice. The source-identical project-1 task is absent from the changed-action projection; the stale project-1 task and empty project-2 task carry their exact addressed values. Both codegen strategies agree.
- `terminal-claim`: an empty sibling extent and a malformed-before-valid prefix each emit a clear only for the source-filled exact target; the neighboring present-empty target emits no action. The reached malformed cell remains in `formalErrorsInOperands`. The selected stale-target row reports the generated mismatch at that exact target, and its leading empty choice retains `OMISSION_ERROR` polarity.
- `classification-control`: source-identical `TimeFromDateTime` and direct-star Time `FirstFilledValue` produce no action, while a stale target yields the value and mismatch. The reviewed [ingress-route checkpoint](cross-layer-routes.md#src-temporal-construction-route) shows that the former scalar contrast also changed ingress. Scalar and repeatable Time reapplication both use stored-clock equality; V1-seeded non-String targets hold unequal unconverted text.
- `integrity`: the retained [addressed model](../../evidence/kernel-30.8.1/captures/repeatable-time-first-filled/TimeFirstFilledProbe_DM.json), [request](../../evidence/kernel-30.8.1/captures/repeatable-time-first-filled/request.json), and [artifact](../../evidence/kernel-30.8.1/captures/repeatable-time-first-filled/artifact.json) hash to `13cc369031bf020c43c0b8b72ace4ea3cb4fd175cd451270c4f787de068da18d`, `abc7b8b88ad98e797920389342ab91c643419855b7b89b0402521abb0ceb30b7`, and `66561ead4468cd655ad6c22960e4ec4671d7b2c58f86c189bc04adbf1b3b78fb`.
- `integrity`: the retained [scalar model](../../evidence/kernel-30.8.1/captures/time-result-classification-controls/TimeResultPolicyScalar_DM.json), [request](../../evidence/kernel-30.8.1/captures/time-result-classification-controls/request.json), and [artifact](../../evidence/kernel-30.8.1/captures/time-result-classification-controls/artifact.json) hash to `ff6e1cd6fa70ba65448eb9d585eb8b09391e73f2de939ec0ce9dcdb1a3ef3a27`, `d1e25691aed7a16e8e1d527864396952c961ae84773b4e7371fc0883590c515a`, and `4e12056c6d9215a286ff24d9d0430f85a621018d6cee4af5283702d26706a3b5`. Both artifacts reproduced byte-identically, including when replayed from the exact retained model and request bytes.
- `limit`: one `HH:mm:ss` sibling-star axis below one shared repeatable parent, at most two parents, three choices, and two tasks per parent, plus scalar controls for the two ordinary producers. The runtime artifact exposes the source-relative change/application projection, not the complete successful collection or a materialized destination document. Wider formats, zones, source shapes, multi-axis or computed sources, target-local rejection, materialized topology, later validation, pointer rendering, and public protocol remain outside.
- `sync`: accepted under [`SPEC-2026-08-27-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-01--a-sibling-star-computation-may-target-a-repeatable-group-and-stays-parent-local); its topology is independent of the rejected Time-placement premise.

<a id="src-time-repeatable-constant-construction"></a>
#### Repeatable constant `Time(...)` construction result classification, measured locally 2026-08-27

- `revision`: clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, dmtool `0.13.0`, kernel `30.8.1` built and runtime. The sibling worktree was empty before and after static authoring and every runtime replay.
- `route`: the retained whole-second Time model re-expresses its repeatable task computation as `Time("5", "2", "9")` through structured dmtool modification. Kernel-confirmed dry-run and persistence admit the operation on the repeatable target; the deliberately malformed bare-numeric `Time(5, 2, 9)` control reaches the same gate and is refused `MVK_INVALID_ENTITY`, retaining the canonical rule that constructor constants are quoted decimal strings. The retained model and request then use the existing `:adapter:kernelProbe` route on both codegen strategies.
- `runtime-claim`: the changed projection contains VALUE `05:02:09` at the stale, absent, and present-empty rows. Two identical rows under different parents are absent, and a parent with no physical task row contributes nothing. The reviewed ingress-route checkpoint adds a scalar reapplication control with the same no-action result; ingress, not placement, explained the earlier contrast. The artifact does not expose complete successes.
- `generated-rule-claim`: only the stale filled target reports the generated `SelectedTimeComp` mismatch. The identical rows produce neither a change action nor a mismatch, while absent and present-empty targets receive actions without a mismatch. The runtime artifact exposes the source-relative changed projection, not the complete successful collection or application.
- `strategy-claim`: dynamic Groovy and generated Java agree on every message, exact action address, value, and empty formal operand channel.
- `integrity`: retained [model](../../evidence/kernel-30.8.1/captures/repeatable-time-construction/TimeConstructionRows_DM.json), [request](../../evidence/kernel-30.8.1/captures/repeatable-time-construction/request.json), and [artifact](../../evidence/kernel-30.8.1/captures/repeatable-time-construction/artifact.json) hash to `9242e97c804fa948586998532c2de6298857b214dcde37586ffd7b391a75b202`, `3a8071620c84405cc845bf7523892d0760d040705ed60f7caf20dfc4c3af1ada`, and `c753f72a539b785806fd697d2b8c0f8ad56d3b50c1686b35e442d8e0a794d9ad`. The artifact reproduced byte-identically from the exact retained model and request bytes.
- `local-state`: the local semantic account checks a repeatable complete-Time target plus zero-through-three constant components, enumerates physical target rows, retains every exact accepted result, projects changes by ordinary stored-clock equality, and applies only retained actions to a separate same-model destination. Those complete-success and application clauses are internal semantics rather than channels exposed by this artifact. The later [Number-component checkpoint](#src-time-repeatable-number-components) extends the same owner without changing this constant-only denominator.
- `limit`: one `HH:mm:ss` target, one three-constant runtime construction under two repeatable levels, at most two parents and two target rows per parent, no component field, precondition, alternative, no-value or poison branch, materialized topology, later validation execution, pointer rendering, or public protocol.
- `sync`: the equality delta is confirmed but its placement premise rejected under [`SPEC-2026-08-27-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-03--repeatable-constant-time-construction-uses-row-local-source-equality).

<a id="src-time-repeatable-number-components"></a>
#### Repeatable Number-backed `Time(...)` construction across root, enclosing, and leaf scopes, measured locally 2026-08-27

- `revision`: clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, dmtool `0.13.0`, kernel `30.8.1` built and runtime. The sibling worktree was empty before and after structured authoring, the original probe, and the retained replay.
- `route`: one structured dmtool model places Number component fields at the form root, the enclosing `Projects` row, and the repeatable `Tasks` target row, each with the exact Hour, Minute, or Second declaration policy. The persisted task computation is `Time(/TimeProbe/RootHour, ../ParentMinute, RowSecond)` into a complete `HH:mm:ss` Time target. The retained request observes validation plus computation through the existing dual-strategy `:adapter:kernelProbe` route.
- `correlation-claim`: root Hour `5` fans out to every task; project Minutes `2` and `4` bind only their own leaves; task Seconds `9`, `10`, and `9` stay row-local. A source-identical first target is absent from the changed projection, while the second leaf receives exact `05:02:10` and the other parent receives `05:04:09`. The generated comparison messages and referenced paths retain the same exact parent/leaf correlations.
- `missing-and-formal-claim`: an absent enclosing Minute clears its stale leaf target, and an absent row Second clears only that row while a clean sibling computes `05:02:09`. With the root Hour absent, a later malformed row Second is still reached and retained in `formalErrorsInOperands`; both that row and its clean sibling clear their stale targets, while only the malformed row carries the Number diagnostic. A project with no physical task rows produces no computation outcome or message.
- `strategy-claim`: dynamic Groovy and generated Java agree on every message, exact action address, value, clear, and formal operand entry across all four rows.
- `integrity`: retained [model](../../evidence/kernel-30.8.1/captures/repeatable-time-number-components/model.json), [request](../../evidence/kernel-30.8.1/captures/repeatable-time-number-components/request.json), and [artifact](../../evidence/kernel-30.8.1/captures/repeatable-time-number-components/artifact.json) hash to `0a9d8975e879beb1a8498df6447a63671dae844face773f0d56728edda456737`, `3d464318666a54cde2f5d78c5989cdfd298091f41573d24c0d4587d44e1d88ce`, and `497bbfe1a9918086125469e5d331e2b7821c66ec5726b192e6770b51c9bfd4a8`. The artifact reproduced byte-identically from the exact retained model and request bytes.
- `local-state`: the checked addressed constructor resolves each Number field relative to the computation group, certifies its positional declaration gate and target-bound repetition scope, and reads it at that own scope inside each physical target environment.
- `local-state`: the same owner retains exact accepted, incomplete, and poison outcomes, field dependencies, ordinary row-local result classification, and separate-destination application.
- `stored-spelling-claim`: peer `RepeatableTimeComponentScopeDiffTest` measures a maximum-length-2 Number profile with no minimum. Unpadded `5` constructs `05`, while stored `"05"` reports `zahlHatFuehrendeNullen` and clears. The separate scalar `ComputedTimeTargetDiffTest` declares minimum and maximum length 2 and supplies single-digit `1`, which is formally invalid and clears the stale target. This retained scope model uses positional maxima 23/59 and no stored length bounds.
- `separator`: static local locks separate target self-reference, scalar-kind mismatch, unbound sibling scope, position-policy mismatch, and constant/field dependency order. These branches are not claims made by the retained runtime artifact.
- `limit`: one full three-Number prefix, one complete `HH:mm:ss` target below two finite repeatable levels, two parents and at most three leaves, root/enclosing/leaf sources, missing components, one malformed leaf, and no-row execution.
- `limit`: the artifact exposes changed actions and generated validation, not the complete successful collection or materialized destination.
- `limit`: repeatable constant/Number mixing, String and extractor components, world-dependent components, alternatives, materialized topology, later validation execution, pointer rendering, and public protocol remain outside the retained Kernel denominator.
- `sync`: accepted under [`SPEC-2026-08-27-04`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-04--repeatable-number-backed-time-construction-reads-each-component-at-its-own-bound-scope).

<a id="src-time-repeatable-string-components"></a>
#### Repeatable digit-String-backed `Time(...)` construction across root, enclosing, and leaf scopes, measured locally 2026-08-27

- `revision`: clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, dmtool `0.13.0`, kernel `30.8.1` built and runtime. The sibling worktree was empty before and after both probe runs.
- `route`: the retained model changes the Number checkpoint's three component declarations to ordinary String fields with maximum length 2, pattern `[0-9]+`, and one localized pattern message while retaining the root, enclosing `Projects`, and leaf `Tasks` placement and the persisted `Time(/TimeProbe/RootHour, ../ParentMinute, RowSecond)` operation. The request observes validation plus computation through the existing dual-strategy `:adapter:kernelProbe` route.
- `correlation-claim`: leading-zero root Hour `"05"` fans out to every task, project Minutes `"02"` and `"04"` bind only their own leaves, and task Seconds `"09"`, `"10"`, and `"09"` stay row-local. A source-identical first target is absent from the changed projection, while the second leaf receives exact `05:02:10` and the other parent receives `05:04:09`.
- `missing-and-formal-claim`: an absent enclosing Minute and an absent row Second each clear only their stale target while a clean sibling computes `05:02:09`. A reached `"xx"` pattern failure clears its target and remains in `formalErrorsInOperands`; it remains reached when the earlier root Hour is missing, while the clean sibling clears without a formal entry. A project with no physical task rows produces no computation outcome or message.
- `unreal-claim`: pattern-valid row Second `"99"` produces no formal finding and no generated mismatch message, but clock construction yields no value and clears its stale target. Its clean sibling still computes `05:02:09`. This separates the String declaration gate from a Number-like positional maximum and separates impossible-clock no-value from formal poison.
- `strategy-claim`: dynamic Groovy and generated Java agree on every message, exact action address, value, clear, and formal operand entry across all five rows.
- `integrity`: retained [model](../../evidence/kernel-30.8.1/captures/repeatable-time-string-components/model.json), [request](../../evidence/kernel-30.8.1/captures/repeatable-time-string-components/request.json), and [artifact](../../evidence/kernel-30.8.1/captures/repeatable-time-string-components/artifact.json) hash to `7cea7460aab83be03d0076487dc94be6d69590b73bdd7c5ed94f99146e49dba8`, `e83f15d5cca42e5e5e6cf390ff9f7dfa15198ac5ae58a28abc6f38b6b47c19f4`, and `577a138ede2973bbe9ca4180d2fcb50027a50f14fd667735bcc979b59b0cd688`. The artifact reproduced byte-identically from the retained model and request bytes.
- `local-state`: the shared checked addressed constructor retains a distinct String source certificate, reads each field at its own target-bound scope, and converts a checked digit String through the existing placement-neutral classifier. Construction evaluation distinguishes incomplete from unreal before the established addressed Time result intentionally projects both to no-value; that result retains exact accepted, no-value, and poison outcomes for action classification and application.
- `separator`: static local locks separate String from Number source kind, the required digit-String declaration policy from an ordinary unpatterned String, and the String gate's position independence. Runtime locks separate own-scope reads, leading-zero conversion, missing from poison, and formal poison from pattern-valid impossible-clock no-value.
- `limit`: one full three-String prefix using the recognized `[0-9]+` digit pattern, maximum length 2, one complete `HH:mm:ss` target below two finite repeatable levels, two parents and at most three leaves, missing components, one malformed leaf, one pattern-valid impossible Second, and no-row execution.
- `limit`: the artifact exposes changed actions and generated validation, not the complete successful collection or materialized destination. Extensible-enumeration String alternatives, mixed component kinds, other recognized pattern spellings, extractors, world-dependent components, alternatives, later validation, pointer rendering, and public protocol remain outside.
- `sync`: accepted under [`SPEC-2026-08-27-05`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-05--repeatable-digit-string-time-construction-reads-each-component-at-its-own-bound-scope).

<a id="src-time-repeatable-direct-extractor-components"></a>
#### Repeatable direct-extractor `Time(...)` construction across root, enclosing, and leaf scopes, measured locally 2026-08-27

- `revision`: clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, dmtool `0.13.0`, kernel `30.8.1` built and runtime; the sibling stayed empty throughout.
- `route`: the retained model places DateTime `RootStamp`, Time `ParentTime`, and DateTime `RowStamp` at root, enclosing `Projects`, and leaf `Tasks` scopes. It persists `Time(HoursFromTime(/TimeProbe/RootStamp), MinutesFromTime(../ParentTime), SecondsFromTime(RowStamp))` into the repeatable `HH:mm:ss` target and observes validation plus computation through `:adapter:kernelProbe`.
- `static-claim`: dry runs admitted the exact operation. Changing only the first token to `MinutesFromTime` was refused `MVK_INVALID_DATE_OR_TIME_EXTRACT_OP`; changing only its source to String `RootHour` was refused `MVK_NO_DATE`. Persisted read-back remained valid.
- `correlation-claim`: root Hour `5` reaches every task, parent Minutes `2` and `4` stay parent-local, and leaf Seconds `9`, `10`, and `9` stay row-local. The source-identical first target has no change; the other exact changes are `05:02:10` and `05:04:09`, with matching referenced paths.
- `missing-and-formal-claim`: an absent enclosing Time and an absent leaf DateTime each clear only their stale target, while a clean sibling computes `05:02:09`. A malformed leaf DateTime reports `datumFormatFalsch`, remains in `formalErrorsInOperands`, and clears its target. It remains reached when the earlier root DateTime is absent, while its clean sibling clears without a formal entry. With no physical target rows, execution produces no outcome, message, or formal entry.
- `strategy-claim`: dynamic Groovy and generated Java agree on every message, exact action address, value, clear, referenced path, and formal operand entry across all four rows.
- `integrity`: retained [model](../../evidence/kernel-30.8.1/captures/repeatable-time-direct-extractor-components/model.json), [request](../../evidence/kernel-30.8.1/captures/repeatable-time-direct-extractor-components/request.json), and [artifact](../../evidence/kernel-30.8.1/captures/repeatable-time-direct-extractor-components/artifact.json) hash to `69c81e9f1a5e14f8acedf62a9ad53241e88a5afe78ba2dd4f70610c5cace797a`, `21545d2cca68d356fe7d9717ef99e0e079ca87f8415d9338c0ce81cec79b4494`, and `560c25e09b95a388c922734d4e4c48869a689cce10f4a0999675a5eae7f7ac1a`. The artifact reproduced byte-identically from the retained model and request bytes.
- `local-state`: the addressed constructor retains the token and temporal declaration, certifies and reads its target-bound scope, delegates typed projection to the scalar classifier, then reuses the row-local result and application owners.
- `separator`: static local locks separate mismatched extractor token from wrong source kind and retain mixed Number/String/extractor dependency order. Runtime locks separate root, enclosing, and leaf reads, source-identical success from change, missing from poison, later reached formal invalidity, and physical rows from declared capacity.
- `limit`: one full DateTime/Time/DateTime extractor prefix, one complete `HH:mm:ss` target below two finite repeatable levels, two parents and at most three leaves, missing components, one malformed leaf, and no-row execution.
- `limit`: the artifact exposes changed actions and generated validation, not the complete successful collection or materialized destination. Other temporal declaration profiles, mixed component kinds, nested temporal-expression extractors, world-backed components, alternatives, later validation, pointer rendering, and public protocol remain outside.
- `sync`: accepted under [`SPEC-2026-08-27-06`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-27-06--repeatable-direct-extractor-time-construction-reads-each-temporal-source-at-its-own-bound-scope).
