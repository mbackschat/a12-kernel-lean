# Group and iteration source checkpoints

<a id="src-starred-operand-message-polarity"></a>
#### A computation's message type follows what can still grow, measured locally 2026-08-30

- `revision`: three `:adapter:kernelProbe` requests, each clean before and after with `state: CLEAN` in the artifact. The capacity matrix at a12-dmkits `42fdec77fd03f1644c05eb6dcff484441960eff5`, three documents; the isolating witness at `e228345a94505f2736f41009af1f4fa635ac2240`, three documents; the all-filled rows at `568fca72858430b355f46d2faee545b7f99fdbd5`, four documents. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on every row of all three.
- `question`: the [starred group-count checkpoint](#src-starred-group-count-computation) reported `OMISSION_ERROR` on every row, including ones whose computed value was three or four against a stored seed, where the [fixed-operand capture](#src-repeatable-descendant-group-count) had measured the ordinary split. Two captures over two models confound the operand list with the model, so the first job was a single-run witness and the second was finding the boundary of whatever it showed.
- `witness`: one model, one document, one run, four Number computations at the same root group, every target seeded to mismatch. `NumberOfFilledGroups(Flat, Other)`, carrying no wildcard, types **`VALUE_ERROR`**. `NumberOfFilledGroups(Rows*)`, `NumberOfFilledGroups(Flat, Rows*)`, and `Sum(Rows*/RowValue)` all type **`OMISSION_ERROR`**, with `FlatValue`, `OtherValue`, and both `RowValue`s filled. Emptying the two fixed operands flips the wildcard-free target to `OMISSION` in the same matrix, so its `VALUE` reads its own cells rather than being an inert default.
- `claim`: **the type follows whether a legal move can still falsify the fired condition, and nothing about starredness as such.** `/Probe/Rows` is declared `max 5`. At **five** instantiated rows with every cell filled, all four targets type `VALUE_ERROR` — the starred ones included. At **four** rows with every cell filled, the three starred targets type `OMISSION_ERROR` while the wildcard-free one stays `VALUE_ERROR`. One row of remaining capacity is the entire difference.
- `separator`: at five rows with every row **empty**, one document separates two growth channels over the same starred group. `NumberOfFilledGroups(Rows*)` and the mixed list type `VALUE_ERROR`, because capacity is exhausted and a count grows only by rows. `Sum(Rows*/RowValue)` types `OMISSION_ERROR` in that same document, because a row's empty cell can still be filled. So headroom is read per operator against its own quantity, not once per operand.
- `not the discriminator`: the witness **corroborates** the headroom rule and cannot establish it. A wildcard-free operand at `VALUE` beside a starred one at `OMISSION` is exactly what the refuted operand-form account predicts, so every below-capacity row is consistent with both readings. Only the at-capacity empty-row document chooses between them, because it holds operand form constant and the two starred targets still differ. a12-dmkits corrected this project's stronger label for its own witness, against its own contribution rather than for it, and completed the witness on its model at `8b7f602a3`.
- `not the operator`: what the below-capacity rows *do* rule out is a group-count-specific account. `Sum(Rows*/RowValue)` types `OMISSION` there alongside the two counts, and an account naming the operator predicts `VALUE` for it. That is a narrower conclusion than the one first drawn, and it survives.
- `correction`: this refutes the reading the witness alone invited, and which an earlier draft of this record and its outbound entry both stated — *a starred operand types `OMISSION` even when every concrete referenced cell is filled*. It is false at capacity. Every row of the witness, and a12-dmkits' independent two-row measurement of the same split, were taken **below** capacity, where the overreach is invisible; the two measurements are sound and the sentence drawn from them was not. It was caught before the outbound entry was committed, by measuring the discriminator instead of recording it as open.
- `mechanism`: this is `spec/`'s own **directional fillability** rule ([§10](../../spec/10-validation-and-polarity.md)) reaching a carrier it had not been measured on, not a new rule. Counts are grow-only, and `OMISSION` is typed exactly when a legal move can falsify the fired condition. What is new is the boundary — declared capacity ends a starred operand's growth — and the per-operator channel split above. a12-dmkits reached the same reading independently from its field-count fixtures, together with the conclusion this measurement corrects, that a starred operand can never reach all-filled.
- `arms`: measured on the computation arm's implicit self-validation message. The rule-arm account of the same polarity is already established; nothing here extends it.
- `control`: a document whose seed already equals its computed value produces **no message and no computed outcome at all**, which is the established changed-subset projection ([§11](../../spec/09-computations.md)) and confirms every message above is the stored-versus-computed mismatch rather than an independent finding.
- `bytes`: [`starred-operand-message-polarity/`](../../evidence/kernel-30.8.1/captures/starred-operand-message-polarity/). SHA-256 capacity request `57d0ecc66a921271d76b723e8a603ef1e7e6f02c69a0e8d93986f54f934caca1`, capacity artifact `27115e2ecca3b2d91cd6bb51db1860607185185276b6d5af81df43c791cb75e1`.
- `bytes`: witness request `a13b7fb8d24a9c95a277fbe7adb2e440135d23279835824fd0e9e59dfef34133`, witness model `648dbc24a08e29e25f21d01b04a96c47fd73273ec29715495f2b031cbd1f34d6`, witness artifact `5ec223bd5551fe7b398fbfb246f4859b4bb6bebc8000e913b127f2c433649c32`. The capacity rows share this model.
- `bytes`: all-filled request `ca1813c6ad93843a868419734c0b6c3d74787595b2a99c372234a7d5295d278a`, all-filled artifact `160709b7aebea3f68dfe70b24fec5be8bcd882ee29493a130abca343bbc2aefa`, over the model pinned at the [starred group-count checkpoint](#src-starred-group-count-computation). All three are bounded raw calibration captures with no typed consumer: they do not run under `lake test` and establish neither `L` nor `C` coverage.
- `fillToFix`: the message's `fillToFix` channel is **all of `referenced` or none of it**, never a proper subset, on every one of the 30 retained message observations. It tracks the type exactly: empty on each `VALUE_ERROR`, equal to the whole referenced set on each `OMISSION_ERROR`.
- `fillToFix-trap`: it is therefore one decision spelled as a list, not a per-cell analysis. The witness document is the proof — `TallyMixed` types `OMISSION` there with `Probe/Flat/FlatValue` filled, and that filled pointer is in its `fillToFix`. A consumer reading a listed pointer as "this cell is empty" would be wrong on that row. The target's own pointer is listed too.
- `direction`: **the type is directional, measured.** Every row of the three captures above has its computed value below its stored seed, so none of them separates the rule arm's directional reading from a coarser account that types `OMISSION` whenever the quantity can move at all. a12-dmkits measured the separating document at clean revision `531a2c769`, in `NestedGroupFillCountLawsTest.thePolarityIsDIRECTIONALAndNotMerelyWhetherTheQuantityCanStillMove`: a starred count below capacity, every cell filled, three instantiated rows, and only the stored seed varying. Seeded `1`, the computed `3` sits **above** its seed and types `VALUE_ERROR`; seeded `9` the same document types `OMISSION_ERROR`.
- `direction-claim`: so headroom is necessary but not sufficient — `OMISSION` requires that growth move the quantity **toward** the seed. Each conjunct now has its own discriminator: direction by the row above, headroom by the at-capacity row. The coarse account predicts `OMISSION` on both rows of that pair and is refuted. Read here rather than taken on report.
- `direction-history`: both estates had held only below-seed rows, so headroom and direction moved together and neither account was tested on either side. This is the second correction on this operator with that exact shape, after the below-capacity rows that hid the capacity boundary — see [`LF114`](../LEAN-FINDINGS.md#lf114--both-sides-sampling-one-side-of-an-unnamed-boundary-agree-and-re-derivation-cannot-break-the-tie).
- `over-limit-type`: an over-limit row leaves the computed target's message type **unchanged**, read from the [over-limit rows](#src-starred-group-count-computation) of the group-count capture rather than from a new run. Five, six and seven instantiated rows in a `max 5` group all type `VALUE_ERROR` on all four targets, with every row's cell filled and the seed unreachable throughout; the surplus rows raise their own separate `VALUE_ERROR` structural messages and change nothing about the computation's. This is a consequence of two rules `spec/` already carries — declared capacity is the boundary, and the starred count excludes over-limit rows — rather than a rule of its own, so it is recorded here and adds no clause.
- `over-limit-nonseparating`: those rows do **not** separate a physical-row reading of headroom from an in-capacity one, and cannot: once the physical count reaches capacity both readings are closed, and in-capacity can never fall below capacity while physical exceeds it. The reading is separated at the producer's channel value instead, which is where this project locks it.
- `limit`: Number targets, one repetition level, one starred axis, capacity 5, one declaring group, and growth measured only through row count and cell fill. Not measured: a nested or filtered star, a non-Number target, a starred operand inside a precondition rather than an operation, and whether a declared **minimum** interacts.
- `sync`: locally originated and behavioral. `spec/` lists the shapes whose type is computed from the data and carried no starred-operand entry, and its grow-only note did not state the capacity boundary. Opens its own outbound entry.


<a id="src-starred-group-count-computation"></a>
#### The computation arm counts a starred repeatable group's instantiated rows, measured locally 2026-08-30

- `revision`: a12-dmkits `568fca72858430b355f46d2faee545b7f99fdbd5` (clean before and after; the artifact records `state: CLEAN`), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, model version 28.4.0. One `:adapter:kernelProbe` request, seven documents, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on every row, preceded by four `computation add --dry-run` admission rows.
- `question`: [§1](../../spec/02-logic-and-formal-errors.md) already recorded that the starred and mixed spellings are statically admitted in computations, and closed that paragraph with the exact limit this capture answers — *"these are admission facts only; they do not establish computation runtime behavior for a starred operand."* [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) carried the same hole as `starred computation runtime`. Every earlier runtime observation used a **fixed** operand list, where each operand contributes at most one.
- `model`: `StarCount_DM`, authored through structured verbs. Root `Probe` carries the two scale-0 Number targets `Tally` and `TallyMixed`, a flat `/Probe/Flat` with `FlatValue`, and the repeatable `/Probe/Rows` (max 5) declaring `RowValue`. Two computations at `/Probe`: `Tally := NumberOfFilledGroups(Rows*)` and `TallyMixed := NumberOfFilledGroups(Flat, Rows*)`. Both persisted `KERNEL_CONFIRMED`, read back with their operand text intact and `converted: true`, and `model check` reports the model valid. Each target is seeded `9` so a clear would be visible.
- `admission`: a **control, not a finding**. Four spellings, each `verification: KERNEL_CONFIRMED`, and all four reproduce what §1 already recorded: `NumberOfFilledGroups(Rows*)` admitted with one operand, `NumberOfFilledGroups(Flat, Rows*)` admitted, `NumberOfFilledGroups(Rows)` refused `MVK_NO_WILDCARD`, `NumberOfFilledGroups(Flat)` refused `MVK_PARAMSIZE_INVALIDGN`, *"There must be more than one group"*. Their value here is that the runtime rows below stand on a model whose admission was independently re-derived on this project's own fixture rather than assumed from the recorded account. Read together they also restate the recorded gate compactly: the two-operand minimum governs the unstarred form alone, so one operand is legal exactly when it is starred.
- `claim`: **the new fact is runtime.** A starred repeatable operand contributes its **instantiated row count**, not a zero-or-one presence bit. `Tally` reads `0, 1, 1, 2, 3, 2, 0` across no rows, one instantiated empty row, one filled row, two rows with only the second filled, three rows all empty, a two-row document with `Flat` emptied, and the all-absent control.
- `separator`: the one-empty-row row answers `1` and the three-empty-rows row answers `3`. Both kill a filled-row account, and the second also kills any account that saturates at one, which a presence bit and a filled-row count would share.
- `arms`: the computation arm's starred count **agrees** with the validation account §1 already carried for `NumberOfFilledGroups(G*)` — in-capacity instantiated rows, a row counting structurally even when every descendant is empty. That agreement is worth stating because the same operator's *fixed* form has the arms inverted on formal invalidity ([checkpoint](#src-nested-descendant-group-count-invalid)), so agreement here is a measurement rather than the default.
- `mixed`: operand contributions are **independent and unlike each other**. `TallyMixed` reads `1, 2, 2, 3, 4, 2, 0` — exactly `Tally` plus one wherever `FlatValue` is filled. The `flat-empty-two-rows` row is what establishes it: `Tally` and `TallyMixed` both read `2` there, so the fixed operand contributed nothing while the starred one still contributed two.
- `control`: the all-absent document answers `0` on both targets, so neither operand counts unconditionally, and no target was cleared or errored on any row.
- `polarity`: every row reports `OMISSION_ERROR` for both implicit self-validation messages, including the rows whose computed value is `3` or `4` and whose target holds a stored `9`. That is **not** the omission/value split the [fixed-operand capture](#src-repeatable-descendant-group-count) measured, where polarity tracked group content. The two captures differ in the operand list alone, but the mechanism is unmeasured and no account is offered here; recorded as an open discriminator rather than explained.
- `reference`: both messages render the repeatable axis with the wildcard coordinate `/Probe[1]/Rows[0]/RowValue` on every row including the no-row ones, and `TallyMixedComp` additionally lists `Probe/Flat/FlatValue`. It is an authored-shape inventory rather than a reached-cell trace, matching the earlier captures.
- `local-consequence`: this project refuses both admitted shapes, so the refusal is fail-closed rather than wrong, but it is now known to be narrower than the Kernel. The representation this needs is **not** the widened cell projection that [the repeatable-descendant checkpoint](#src-repeatable-descendant-group-count) ruled out: the Kernel gives rows their own spelling rather than making the fixed operand see them, so the starred operand is a second operand form whose **contribution** is a cardinality. The fold itself stays uniform over one result domain, which is what lets a checker keep one traversal.
- `over-limit`: an over-limit row is excluded on the **computation** arm too, measured rather than inherited. At a12-dmkits `ba07ba8b396df85b18a530d2d9eaddd3674ece9e` (clean, both strategies agreeing), a repeatable group declared `max 5` holding five, six, and seven instantiated rows counts `5` on all three, and a starred `Sum` over the same rows likewise stops at the in-capacity five. SHA-256 request `ff94d4566664ebe9a538c1e4512d85e63ae10808bbc6f5eda82a69cd367840b1`, model `648dbc24a08e29e25f21d01b04a96c47fd73273ec29715495f2b031cbd1f34d6`, artifact `9e648b89909318ddfb0d88cfc75962d38820697e3158938a2f11669d9bd98a98`.
- `over-limit-why`: this was measured rather than inherited because the exclusion's only prior evidence was on the **validation** arm, and the two arms are known to diverge on this very operator for a formally invalid operand. Carrying the rule across would have been the same uniformity assumption the [nested invalid-descendant capture](#src-nested-descendant-group-count-invalid) had to replace with an observation.
- `over-limit-second-estate`: a12-dmkits reproduced the same exclusion on its own oracle and fixture at clean revision `856465c37`, locked in `NestedGroupFillCountLawsTest.theCOMPUTEArmExcludesAnOverLimitRowFromBothTheCountAndTheSum` — a `Riders` group declared max 5 counting `4, 5, 5, 5` at four, five, six and seven instantiated rows, with the starred `Sum` reading the same four values. The four-row row is theirs and is the control this project's own capture lacked: without it, `5` everywhere is equally consistent with the quantity being pinned at capacity regardless of content. Read here rather than taken on report; `RuntimeLaws` drives the real Kernel through `com.mgmtp.a12.kernel.md.rt.api.IDocumentRtService`.
- `two-star`: **two starred operands in one list add per authored position, and the same group named twice contributes twice.** Measured on a12-dmkits' kernel oracle at clean revision `856465c37`, `NestedGroupFillCountLawsTest.aRepeatedStarredOperandIsCountedTWICEAndTwoDistinctStarredOperandsAdd`. Distinct groups `(Coverage/Riders*, Bundle/Items*)`: three rider rows beside two item rows count `5`, three beside none count `3`, and no rows count `0`. The same group twice, `(Coverage/Riders*, Coverage/Riders*)`: three rows count `6`, no rows count `0`.
- `two-star-separator`: the duplicate row is the discriminator for the fold's shape. A sum over authored operand **positions** predicts `6`; a fold over the **distinct groups** those positions name predicts `3`. The peer's lock pins the rendered operand text beside the count, so a dedupe inside their own builder would surface as a spelling change rather than as a Kernel fact. The zero-row rows separate both shapes from a constant.
- `two-star-static`: **static admission is measured too**, on a12-dmkits' estate at clean revision `7967f5cb3`, in `EmptyGroupOperandLawsTest.theGROUPCOUNTAdmitsANestedShellAtEveryPositionDepthAndArity`. All three shapes return `ADMITTED` — the same starred group twice, two distinct starred groups, and a fixed group beside two starred ones. Read here rather than taken on report: the route is the Kernel's own `checkConsistency` through `KernelConsistencyOracle` and `KernelModelPreparation`, which imports `com.mgmtp.a12.kernel.md.facade.DocumentModelServiceFactory`.
- `two-star-static-control`: that route needed a positive control, because the peer's helper reports `ADMITTED` whenever no ERROR was raised, which a missing oracle would also produce. Three rows of the same table in the same run return `MVK_GROUP_IS_EMPTY`, `MVK_PARAMSIZE_INVALIDGN`, and `MVK_NO_WILDCARD`; a degraded lane cannot produce an `MVK_*` code, so the gate demonstrably ran.
- `two-star-limit`: this project's own admission rows for the two-star spellings remain uncitable — observed in a window that could not be certified clean — so the static half rests on the peer's estate alone rather than on two. The runtime half rests on the peer's estate as well; both are single-estate observations against this project's independently written fold.
- `limit`: one repetition level, scale-0 Numbers, and — for this project's own rows — one starred operand per list. Not measured: a row holding only formally invalid content, a starred group with a repeatable descendant of its own, static admission of a two-starred or duplicate list, and the starred spelling of a nonrepeatable group.
- `bytes`: [`starred-group-count-computation/`](../../evidence/kernel-30.8.1/captures/starred-group-count-computation/); SHA-256 request `86fe1520d41f6fe351e0444ea4fb1cb4acff34eb2fbba15fc61d49f8a6d88807`, model `c028d6196825fa4e822455cf98f3d627b8e2d0701d1d33d78672b631980bde0a`, artifact `5580e7644feb234eb48f2c44c45da1c0790ddeb88ae67ae2d7d1df4ab4da8026`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: locally originated and behavioral. The admission half is already in `spec/`; what is new is the starred operand's computation-arm contribution and the mixed list's unlike contributions, which replace a recorded explicit non-claim. That is a behavioral extension and opens its own outbound entry.


<a id="src-group-count-row-domains"></a>
#### The half-instantiated shell, a starred group over a repeatable descendant, and why the two row domains cannot be separated, measured locally 2026-08-30

- `revision`: a12-dmkits `89871694469f582d3ec5d0df22dbacb53b0dde63` (clean before and after; the artifact records `state: CLEAN`), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, A12 Tools `2025.06-ext5`. One `:adapter:kernelProbe` request, five documents, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on every row.
- `question`: the [deep checkpoint](#src-deep-repeatable-descendant-group-count) named the **half-instantiated** shape as its own limit, and [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) carried a starred group owning a repeatable descendant as implemented but uncalibrated, plus an open discriminator between two row domains for the fixed operand.
- `model`: the deep checkpoint's `Probe_DM` extended with a third scale-0 Number target `TallyStar := NumberOfFilledGroups(/Probe/ShellDeep/Mid*)`, persisted `KERNEL_CONFIRMED` with `model check` valid. `Mid` is `max 3` and owns the repeatable `Rows` (`max 5`), so the starred operand's group carries a second repetition level beneath it.
- `peer-reproduction`: the half-instantiated row was measured **independently** on a12-dmkits' estate at `1be2d603c` and agrees — its third row is a `Pallets` row with no `Cartons` row beneath it, counting `2` where a leaf-row rule predicts `1`. The peer reached that row from the depth entry alone and its commit still calls the shape this project's own unmeasured reading, which it had ceased to be hours earlier. Two estates, two harnesses, one discriminator, same answer.
- `half-instantiated`: **the outer row alone is content.** With `Mid` holding one row and no inner row beneath it, `TallyTwo` is `2` — the same answer the fully instantiated document gives. Measured on both spellings of that document, an outer row carrying no inner group key at all (`Mid: [{}]`) and one carrying an explicitly empty inner list (`Mid: [{"Rows": []}]`), against the no-row control at `1`. A reading that required a **leaf** row answers `1` on both and is refuted.
- `starred-extent`: the starred operand counts **its own** rows and the level below it does not fold in: no rows counts `0`, one row counts `1`, and two rows each carrying a leaf row count `2` rather than four. This calibrates the shape SG13 carried as implemented-and-pending.
- `over-limit`: four rows in the `max 3` group count `3`, so the starred operand's in-capacity domain holds on a carrier owning a repeatable descendant, rather than being inherited from the flat group where it was first measured. The fixed shell still counts present on that same document, at `TallyTwo` `2`.
- `over-limit-findings`: that row also carries **three** VALUE errors, and they show the over-limit stamp reaching descendants: `zuGrosseZeile` on the row itself (`Probe/ShellDeep/Mid[4]`), and `zuGrosseKontextnummer` on both the descendant group (`Mid[4]/Rows`) and the leaf field (`Mid[4]/Rows/DeepValue`), each naming its own address in `referenced` with an empty `fillToFix`. Representing them belongs to [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration).
- `over-limit-multiplicity`: those three are a **delta**, not a corroboration, and this checkpoint first recorded them as the latter. The standing clause predicts one group-level row finding plus one field-local context-number finding per placed over-capacity cell, which is **two** here; the intermediate `Mid[4]/Rows` finding is on a repeatable **group**, not a placed cell. Every fixture behind the standing count is flat, so the row's cells sit directly in it and the intermediate address cannot arise — an unnamed constant of the [`LF114`](../LEAN-FINDINGS.md#lf114--both-sides-sampling-one-side-of-an-unnamed-boundary-agree-and-re-derivation-cannot-break-the-tie) class, discharged here by a nested carrier.
- `row-domain-unreachable`: **the fixed operand's two candidate row domains cannot be separated by any admissible input**, so this discriminator is closed rather than pending. Index 1 always exists and is always in capacity, so "any instantiated row" and "only in-capacity rows" agree on every document the format can express over every model the Kernel admits.
- `row-domain-basis`: three facts settle that, each from its owning contract rather than by inference. A repeating group is an **array of row objects whose index is the repetition** (a12-dmkits `cli/src/main/resources/schema/document-instance.schema.json`), so an instantiated row set is a dense prefix from index 1 and an unfilled row `{}` is still instantiated. A nonrepeatable group carries `repeatability: 1`, so a repeatable one is at least 2. And `repeatability: 0` is refused by the Kernel — `MVK_MAX_VALUES_INVALID` plus the plain *"Only values greater than 0 are allowed"*, `source: KERNEL`, obtained through the skill's sanctioned measurement exception by hand-writing the value into a **copy** of the model and running `model check`, the copy then deleted.
- `local-consequence`: the half-instantiated branch was locked in [`GroupCount.lean`](../../A12Kernel/Conformance/NumericComputation/GroupCount.lean) as this project's own reading and is now a measured row, with the comment corrected in the same change. [`StarredGroupCount.lean`](../../A12Kernel/Conformance/NumericComputation/StarredGroupCount.lean) gains the starred two-level extent and its over-limit row. The row-domain discriminator recorded in SG13 is retired: `RowAddr.path` is more permissive than the document format, so the sparse row set that would separate the accounts is a Lean-side artefact with no admissible counterpart.
- `limit`: one shell shape, scale-0 Numbers, capacities 3 and 5, and at most four rows. A filtered or nested star over this carrier and any dependence of the stamped set on declaration kind remain unmeasured; the depth, inner level, and multiplicity questions were closed by the [multiplicity checkpoint](over-repetition-probes.md#src-over-limit-finding-multiplicity).
- `bytes`: [`group-count-row-domains/`](../../evidence/kernel-30.8.1/captures/group-count-row-domains/); SHA-256 request `10d3145777ac4ff61ce16372b19d0e05a792b2b60bd0576b4ca8a3123b99589e`, model `6d54d9e0cc229d093d8970087a1cc912c227dec931f5e2adb80b0fb423a7105e`, artifact `1e5e11fada2fc7b8ef5ebb71375d35e6c87a5df987afb808347b035602c608c2`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: two locally originated behavioral deltas, each opening its own outbound entry — the half-instantiated rule, and the over-limit finding multiplicity through a nested repeatable group. Only the starred extent corroborates a clause `spec/` already carries and adds no delta of its own.


<a id="src-deep-repeatable-descendant-group-count"></a>
#### Repetition depth does not bound a group-count operand's row constituent, measured locally 2026-08-30

- `revision`: a12-dmkits `89871694469f582d3ec5d0df22dbacb53b0dde63` (clean before and after; the artifact records `state: CLEAN`), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, A12 Tools `2025.06-ext5`. One `:adapter:kernelProbe` request, four documents, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on every row.
- `question`: the [one-level checkpoint](#src-repeatable-descendant-group-count) established the row constituent and named its own limit — *a repeatable descendant more than one level below the operand* was unmeasured. This project refused that shape, so the refusal claimed nothing while the Kernel's answer was unknown.
- `model`: `Probe_DM`, authored through structured verbs. Root `Probe` carries scale-0 Number targets `Tally` and `TallyTwo`, a flat `/Probe/Flat` with `FlatValue`, a one-level shell `/Probe/ShellOne` whose only content is the repeatable `Rows` (max 5), and a **two-level** shell `/Probe/ShellDeep` whose only content is the repeatable `Mid` (max 3) containing the repeatable `Rows` (max 5) declaring `DeepValue`. `Tally := NumberOfFilledGroups(ShellOne, Flat)` and `TallyTwo := NumberOfFilledGroups(ShellDeep, Flat)` both persisted `KERNEL_CONFIRMED`, read back with the intended structure, and `model check` reports the model valid.
- `static`: both operands are admitted, in one `computation add --dry-run` batch whose third child is the live-gate control — the unstarred repeatable `/Probe/ShellDeep/Mid` refused `MVK_NO_WILDCARD` with `source: KERNEL`. So the two admissions are a verdict rather than a carrier that refuses nothing.
- `claim`: **a descendant two repetition levels below the operand counts exactly as one level does.** With `FlatValue` filled, one `Mid` row holding one `Rows` row whose only cell is empty makes `TallyTwo` `2`. A depth-bounded account answers `1` here, since the shell owns no filled cell anywhere and its rows sit two axes down.
- `control`: removing every row drops `TallyTwo` to `1`, so the deep shell is not counted unconditionally. A filled leaf also gives `2`, so cell content does not move the answer once a row exists — the row constituent alone decides it at depth exactly as it does at one level.
- `cross-operand`: `Tally` reads `1` on all three deep documents and `2` on the one-level document, while `TallyTwo` inverts. Each operand answers from its own subtree, which is what rules out one shell's rows reaching the other's count.
- `local-consequence`: `ResolvedGroupReference.repeatableDescendantShape?` refused a descendant more than one repetition level deep and that refusal is **lifted**. The clause is now stated for any depth rather than for the measured two, because the mechanism is subtree containment rather than an axis count — the validation arm's `rowWithinGroup` already had no depth bound, so matching it is an agreement between two arms rather than an extrapolation from one row.
- `limit`: two repetition levels, one shell shape, scale-0 Numbers, capacities 3 and 5 with at most one row per level. **Not measured: the half-instantiated shape** — an outer row with no inner row beneath it. Every document here carries both levels or neither, and this project's clause counts such a shell as present because it asks whether *any* repeatable descendant has an instantiated row; that branch is locked as its own reading rather than as a measured row. Also unmeasured: three or more levels for *this operand* and any starred spelling of it; over-limit rows at either level were closed by the [multiplicity checkpoint](over-repetition-probes.md#src-over-limit-finding-multiplicity).
- `bytes`: [`deep-repeatable-descendant-group-count/`](../../evidence/kernel-30.8.1/captures/deep-repeatable-descendant-group-count/); SHA-256 request `32f43465d475ffe486a44906ba422d09063d05bb02a1cf61ac2c025023d57a3f`, model `6c89c1f8fa429951fdaeaedaab69f355dcdcee5825cb2aaea3b45f42f46c1e90`, artifact `e35344f573aabe7aa708aa7a021729d15869cc9213f0d27806b28ea7463fa7cb`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `peer-reproduction`: a12-dmkits reproduced the depth reading on its own estate at clean revision `1be2d603c`, `adapter.laws.DeepRepeatableRowConstituentLawsTest`, read here rather than taken on report. A different model (`Pallets` containing `Cartons`) and a different harness — the kernel-backed `RuntimeLaws.compute`, with static admission asserted separately through `consistencyMessages` — give the same rows: empty leaf `2`, filled leaf `2`, no rows `1`. That rules out a fixture-structure confound; it does not widen the shape, since both estates used exactly two repetition levels.
- `sync`: locally originated and behavioral. It extends a clause `spec/` states generally but whose evidence reached only one repetition level, so it opens its own outbound entry. Accepted at the revision above; no new outbound entry follows from the reproduction.


<a id="src-starred-group-operand-extent"></a>
#### A starred group operand answers exactly as the unstarred one, and the malformed pair settles the extent, measured locally 2026-08-31

- `revision`: one `:adapter:kernelProbe` request at a12-dmkits `e235a4b3aa1a32d952069e4a5dc9917900861f04`, clean before and after, `state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on all seven rows.
- `question`: the [fixed-operand sweep](../SOURCES.md#src-group-operand-capacity-consumer-sweep) measured six carriers over an **unstarred** group and explicitly left the starred form open. This estate then split the two forms so it would claim only what was measured, which preserved an *untested* conformance lock asserting that full validation retains the over-limit cell for a starred token group. The split was the claim, not the caution: the probe below settles the form directly.
- `instrument`: a ladder of exact-equality rules per carrier — `== 0/1/2` for the count, `== 5/7/12` for `Sum` — each guarded by `FieldFilled(/Probe/Err)` on its own error field. Exactly one member firing is the value; none firing is `UNAVAILABLE` observed rather than inferred. The idea is the peer's, from the fixed-operand round.
- `model`: `StarredExtent_DM`, a nonrepeatable `/Probe/Shell` owning two sibling repeatable groups at `max 2` — `Notes` with a String `Tag` and `Charges` with a Number `Fee` — plus `/Probe/Guard` and `/Probe/Err` at the root. Operands are `NumberOfFilledFields(/Probe/Shell/Notes*)` and `Sum(/Probe/Shell/Charges*)`, both **starred**. Model SHA-256 `4e12869ccfa2d79958b4e688dd8e52313bdd7a1ea8e05410fe88a92ff7ae7067`.
- `claim`: the starred count answers `0` when the only filled `Tag` sits at `Notes[3]`, and `1` on the control at `Notes[2]`. The two documents differ in the offending index alone.
- `claim`: starred `Sum` answers `5` on a document holding `5` in capacity and `7` over limit, and `12` on the control holding both in capacity.
- `claim`: **the malformed pair holds for the starred form.** With `5` in capacity and a non-numeric `Fee` at `Charges[3]`, `Sum` still answers `5`; with the identical non-numeric value at `Charges[2]`, no candidate fires and `Sum` is `UNAVAILABLE`. This round's ladder had no `== 0` member, so the silence excluded *skipped* but not *zero*; the [computation-arm checkpoint](#src-capacity-projection-computation-arm)'s `claim-confirmed` row re-reads the same shape on a ladder that carries zero and the reading holds. So an over-limit cell is not an excused member of the domain — it is not in the domain being classified — and that is now measured on both group forms rather than transferred across them.
- `local-consequence`: **the fixed/starred split is removed and two untested locks are corrected.** The numeric aggregate resolvers, the token value count, and the Boolean/Confirm value count now narrow every group operand to its declared-capacity extent under full validation. Three conformance cases asserting the opposite — the starred token group count and both Boolean overloads — were untested implementation locks and now carry the measured answer; the dedicated starred token whole-group carrier's two arms no longer disagree.
- `limit`: two carriers, one repetition level, bounded repeatability 2, ordinary String and scale-0 Number fields, `en_US`, full validation. Not measured here: the extrema and `NumberOfDifferentValues` over a *starred* group, partial validation, and nesting below the star.
- `limit-closed`: the extrema and `NumberOfDifferentValues` over a starred group are measured in the [starred-field probe](#src-starred-field-operand-extent) and project like the rest; `full validation` is no longer a limit of the family either, since the [computation-arm probe](#src-capacity-projection-computation-arm) answers the same. Partial validation and nesting below the star remain open.
- `limit-closed`: **the empty in-capacity domain is `0`, and this round could not see it.** `Sum(Charges*)` reads `UNAVAILABLE` here on the three documents that instantiate no `Charges` row only because the ladder carries no `== 0` member; a definite zero and non-evaluability are indistinguishable under that instrument. a12-dmkits ran the missing member at `a8f151784` on two documents — no repeatable row at all, and content present but entirely above capacity — and `Sum` answers `0` in all four cells on both spellings. So the exclusion never makes the aggregate non-evaluable, and the three rows above are a real zero misread as silence. Recorded because the misreading is the instrument's, not the kernel's: a ladder without a zero member cannot distinguish an emptied domain from an unavailable one, which is the same hole as a positive-only rule.
- `bytes`: [`starred-group-operand-extent/`](../../evidence/kernel-30.8.1/captures/starred-group-operand-extent/); SHA-256 request `08941eadcdd00b1a7f3175ed14b6b3ad30145100340b5dc2cbda9db0cf82cbc1`, artifact `0b00d3f0c18462dc07466f853a0f5654a9138a5620b29742aa9dae16bdc80c6b`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: behavioral and outbound as [`SPEC-2026-08-31-10`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-10). It also discharges the correction the [fixed-operand checkpoint](#src-group-operand-over-limit-extent) held: [`spec/02`](../../spec/02-logic-and-formal-errors.md)'s unfiltered tally is right about *classification* and was silent about its *range*, and the malformed pair separates the two.

<a id="src-starred-field-operand-extent"></a>
#### A starred field operand projects to declared capacity under every measured consumer, measured locally 2026-08-31

- `revision`: one `:adapter:kernelProbe` request at a12-dmkits `88fd13243d7b5dd7238fcd190865c4fbdd247a2a`, clean before and after, `state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on all eight rows.
- `question`: the [starred-group probe](#src-starred-group-operand-extent) closed the group carrier and named the remaining hole in its own `limit` line. This estate held a conformance block asserting that a **starred field** operand under `MinValue`, `MaxValue`, `NumberOfDifferentValues`, and the value count retains the complete formal-cell view, with a comment calling them "operators outside the measured slice". That is [`LF124`](../LEAN-FINDINGS.md) a second time in the same family: the limit was honest, and the case carrying it was not.
- `model`: `capfamily` — root `/Probe` with a String `Err`; a repeatable `/Probe/Rows` at `max 2` declaring scale-0 Number `V`; and a nonrepeatable `/Probe/GrpShell` owning a repeatable `GRows` at `max 2` declaring Number `GV`. Twenty-two exact-equality ladder rules across the two carriers. Model SHA-256 `89bcdab325ff45a945aad49d05150d55b664e30c2a7a7b1b1ee1b5995c2b492d`.
- `instrument`: the peer's exact-equality ladder, extended with a `== 0` member per carrier so an emptied domain is separable from an unavailable one, and a `== 7` member so the reversed document is decidable rather than silent.
- `instrument-defect`: **the first run silently dropped one of twenty rule adds, and its missing member read exactly like a kernel answer.** `Sum(/Probe/GrpShell) == 12` never reached the model, so the one document whose fixed-group sum is `12` fired nothing and the fixed spelling appeared to disagree with the starred one on that row alone. Re-adding it returned `KERNEL_CONFIRMED` and the disagreement vanished. A ladder's silence is only evidence once the ladder is confirmed present in the model being run; count the rules in the artifact's model, not in the authoring log.
- `claim`: **a starred field operand's extrema, distinct count, and value count all answer from the in-capacity domain.** With `5` in capacity and `7` over limit, `MinValue`/`MaxValue`/`NumberOfDifferentValues`/`NumberOfValueInFields(7 …)` answer `5`/`5`/`1`/`0`; the reversed document answers `7`/`7`/`1`/`1`, and the in-capacity control holding both values answers `5`/`7`/`2`/`1`. The reversal is what makes `MinValue` discriminate at all.
- `claim`: the same four carriers over a **starred group** project identically — `5`/`5`/`1` against the control's `5`/`7`/`2` — extending the starred-group checkpoint's `Sum` and count rows to the extrema and the distinct count.
- `claim`: a **fixed** group `Sum` and its starred spelling agree on all eight documents, `5`/`7`/`12`/`0`/`0` alike. The star makes no difference to a group operand on this topology either.
- `claim`: on an emptied in-capacity domain **every** operator answers a definite `0` — `Sum`, both extrema, the distinct count, and the value count alike — on two documents, one with no repeatable row at all and one with instantiated but empty rows. It independently corroborates the zero a12-dmkits measured at `a8f151784` and extends it to the other four carriers.
- `claim-corrected`: **an earlier version of the row above said the extrema and the distinct count were `UNAVAILABLE` there, and that was this instrument's silence, not the kernel's answer.** Only the `Sum` and count ladders carried a `== 0` member; the extrema and distinct-count ladders ran `5`/`7` and `1`/`2` and could not express a zero at all. Adding `FMIN0`, `FMAX0`, `FDIF0`, `GMIN0`, `GMAX0`, and `GDIF0` and re-running the same eight documents at `63e5df8e0` answered `0` in every one of those cells. The mistake is the one the `instrument-defect` row above warns about, made in the same session that wrote the warning: the zero member was verified for the two carriers that had it and then assumed for the family ([`LF125`](../LEAN-FINDINGS.md)).
- `local-consequence`: **the falsified block is corrected and the two full-validation resolvers are merged.** `resolvedCheckedDocumentValidationSumSide` and `resolvedCheckedDocumentValidationAggregateSide` had become identical once the star narrowed for every operator, so one resolver now serves `Sum`, the extrema, the distinct count, and the value count; the aggregate evaluator no longer branches on the operator. The two `rfl` restatement laws that distinguished them, and the law equating them on a group operand, collapse into the single remaining restatement.
- `limit`: two carriers, one repetition level, bounded repeatability 2, scale-0 Numbers and one String, `en_US`, full validation. Not measured: a Boolean or token *starred field* carrier, whose value counts still keep the complete formal-cell view in this estate; partial validation; nesting below the star; and the empty-domain split above on any operator other than the four run here.
- `bytes`: [`capacity-projection-family/`](../../evidence/kernel-30.8.1/captures/capacity-projection-family/); SHA-256 request `d3a9800726d49e93c14320b8778e6bdda0b1e70ad0e4f45e66804b0f9dcdcfb6`, model `89bcdab325ff45a945aad49d05150d55b664e30c2a7a7b1b1ee1b5995c2b492d`, artifact `95445086e351d744e6dbf464dabe4ab7814c8fef84005c6fe4f4b6c0fc06d57b`. The zero-member re-run that corrects the empty-domain row is beside them at a12-dmkits `63e5df8e0`: request `0ee4af1606b838e00a5084601e90e15baa978ce5a650fefc8aeea326dd8760f0`, model `cce6bd971a4136b9a05945d229e778e78ca13fc5d08bc081965effa4d5b3cdf9`, artifact `69d49ecf098a2585d6c2c731f98ea4ed7ba7d98f1c0ac6d3e47d01570a2894a1`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: behavioral and outbound as [`SPEC-2026-08-31-11`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-11), together with the computation-arm record below.

<a id="src-capacity-projection-computation-arm"></a>
#### The declared-capacity projection holds on the computation arm, and the over-limit row does not poison, measured locally 2026-08-31

- `revision`: one further `:adapter:kernelProbe` request at the same a12-dmkits `88fd13243d7b5dd7238fcd190865c4fbdd247a2a`, clean before and after, `state: CLEAN`. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` **and** `compute` on both codegen strategies, `enginesAgree: true` on all eight rows.
- `question`: every capacity observation in this family so far ran through `validateFull`, and the arm boundary was crossed without measurement — this estate's computation clauses asserted `unknown .overRepetition` for a fixed group `Sum` and for a starred field under all five operators. Reusing a settled mechanism on a new arm is a claim, not an inheritance, and here the claim was already wrong.
- `model`: the `capfamily` model above with eleven computed scale-0 Number fields added at `/Probe`, each `KERNEL_CONFIRMED` on `computation add`: `Sum`, `MinValue`, `MaxValue`, `NumberOfDifferentValues`, and `NumberOfValueInFields(7 …)` over the starred field and over the starred group, plus `Sum(/Probe/GrpShell)` in its fixed spelling. Model SHA-256 `dbba10fe8ca5cb542a08c5c4376e948a1613c31c582161dcb12e0e02375db083`.
- `instrument`: none needed. `compute` reports each computed field's value directly with `cleared` and `errored` flags, so the ladder that full validation requires has no counterpart here and no missing-member hazard.
- `claim`: **the computation arm answers from the same in-capacity domain as full validation, on all five operators and both carriers.** The eight documents give the identical table under `compute` that the ladder gave under `validateFull` — `5`/`5`/`1`/`0`/`5` on the over-limit document, `7`/`7`/`1`/`1`/`7` reversed, `5`/`7`/`2`/`1`/`12` on the control — with both codegen strategies agreeing on every cell.
- `claim`: **the over-limit row is removed from the domain rather than poisoning the computation.** On the same rows the kernel reports `zuGrosseKontextnummer` for `Probe/Rows[3]/V` in `formalErrorsInOperands` while the computed field is `cleared: false`, `errored: false`, and carries a definite value. A formal error is therefore visible in the operand and still does not reach the result, which is the computation-arm counterpart of the malformed pair that settled the validation arm.
- `local-consequence`: `resolvedCheckedDocumentComputationAggregateSide` now narrows a plain star and a group operand to their declared-capacity extent, and `resolvedCheckedDocumentValueCountComputationSide` — which narrowed for the group alone — is deleted as a duplicate of it. The star's bespoke environment resolution on this arm is replaced by the shared operand core the group arm and the whole validation arm already use; both routes agreed on every retained computation case, so the capacity projection is the only intended behavioural difference.
- `open-closed`: **the apparent arm asymmetry on an emptied domain was not real, and the discriminator was run.** A computation whose operand holds an in-capacity malformed cell returns `outcomes: []` — no outcome entry at all, for any computed field — alongside `zahlHatUngueltigeZeichen` in `formalErrorsInOperands`. So an unavailable computation is *absent*, never a rendered `0`, and the zeros this run read on an emptied domain are real values. The validation arm agrees once its ladder can express zero, so both arms answer `0` under every operator and nothing about the empty domain distinguishes them.
- `claim-confirmed`: **the validation arm's malformed separator is re-read on a ladder that can express zero, and it holds.** The [starred-group round](#src-starred-group-operand-extent) read it as silence on a `5`/`7`/`12` ladder, which excluded *skipped* but not *zero*. On the in-capacity malformed document here **no member fires at all** — not `GSUM0`, not `XSUM0`, not `FCNT0` — while the same cell one index above capacity fires the full in-capacity set. So the aggregate and the count are genuinely unavailable there, and that reading no longer depends on a ladder hole.
- `claim`: **a formal error's reach is what separates the two exclusions.** The same malformed cell one index *above* capacity leaves every computation answering its in-capacity value, while in capacity it removes the outcome entirely. Paired on one model, that is the computation-arm counterpart of the validation arm's malformed separator, and it is why the over-limit row's `zuGrosseKontextnummer` appearing in `formalErrorsInOperands` does not contradict the definite value beside it.
- `limit`: as the record above, plus: computations read at their own scheduling with no dependent chain, no clearing or poison propagation between computed fields, and no partial-validation counterpart.
- `bytes`: [`capacity-projection-family/`](../../evidence/kernel-30.8.1/captures/capacity-projection-family/); SHA-256 request `85808efbff123c0ba7a5ea9e7baffb649dacc39fb4319babf4badbe8a9b395a8`, model `dbba10fe8ca5cb542a08c5c4376e948a1613c31c582161dcb12e0e02375db083`, artifact `f9432662dd21158071ed02652fd6b799e7f025db0bfdcf24ed8b41bb0c06a729`. The discriminator run is beside them at a12-dmkits `63e5df8e0`: request `46ea31fc294dd81f09e77d1d20fe858447b3d270aa746a4be6074548c71401b6`, artifact `b35e3912d57c7fcee4782415fc2752d6acdb94810bd83f68103fd79d68d9550a`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: behavioral and outbound as [`SPEC-2026-08-31-11`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-11).

<a id="src-group-operand-over-limit-extent"></a>
#### A fixed group operand's extent excludes an over-limit row, and the count is zero rather than unknown, measured locally 2026-08-31

- `revision`: one `:adapter:kernelProbe` request at a12-dmkits `7ace4229c4c714f657172f36c643c5adffcb35c9`, clean before and after with `state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on all seven rows.
- `question`: an earlier reading closed this discriminator as **unreachable**, on the ground that separating an all-instantiated row domain from an in-capacity one needs a document whose every row index exceeds capacity, which the dense-array row format forbids. That is true of a *count of rows*; it is false of a **count of cells**. A subtree whose in-capacity rows are instantiated but empty, and whose only filled cell sits in the excess row, separates the two domains on one cell without needing any row set at all.
- `model`: `Deep3Count_DM`, a nonrepeatable `/Probe/Shell` over a three-level `L1`/`L2`/`L3` chain at `max 2` each, plus `/Probe/Other` holding the error field. Four rules on the same operand: `NumberOfFilledFields(Shell) > 0`, `NumberOfFilledFields(Shell) < 1`, `AtLeastOneFieldFilled(Shell)`, and `NoFieldFilled(Shell)`, each guarded by `FieldFilled(Other/W)` so no rule fires vacuously. Model SHA-256 `da5d9df56f590c71c9652e0a12d95d7310077bdad54f094ac730dfb4009ad6a4`.
- `claim`: **an over-limit row's cell is outside the extent, at the outer level and the inner one alike.** A document whose only filled cell sits in `L1[3]`, or in `L2[1,3]`, fires `NoFieldFilled` and does not fire `AtLeastOneFieldFilled`. Its in-capacity control, the same cell one index lower, does the opposite on both.
- `zero-not-unknown`: the `< 1` rule is what makes this a domain result rather than an unavailability one, and it was added for exactly that reason. A retained-but-formally-invalid cell would make the count **unknown**, and an unknown count leaves both the `> 0` and the `< 1` rule silent. The `< 1` rule **fires** on both over-limit documents, so the count is exactly `0`: the cell is not classified as unavailable, it is not there.
- `depth`: the same run isolates depth for this operator, which the neighbouring [nested-star checkpoint](#src-nested-star-bound-outer-level) could not do for group *presence*. A cell three repetition levels below the operand is counted — `AtLeastOneFieldFilled` fires with content only at `L3[1,1,1]` — where presence is explained by the outermost instantiated row under any depth account.
- `constituent-split`: `NumberOfFilledGroups(Shell, Other)` counts `Shell` as **filled** on the very documents where `NumberOfFilledFields(Shell)` is `0`. That is not a contradiction: group presence has a row constituent, and the in-capacity `L1[1]` row satisfies it while carrying no filled cell. It is a clean separation of the two operators' constituents on one document.
- `local-consequence`: **the locus was not a binary after all, and the correction is landed.** This estate already applies the capacity projection per *measured consumer* rather than globally — the starred carrier's `Sum`, `NumberOfFilledFields`, and `NumberOfFilledGroups` opt into `inCapacityAddressedCells` while `MinValue`, `MaxValue`, `NumberOfDifferentValues`, and the value counts deliberately retain the complete formal-cell view for want of a measurement. This round extends the measured slice to the **fixed group operand's** count, which now opts in the same way. The carriers that looked like contradictions are the unmeasured consumers, and they contradict nothing.
- `still-open`: whether the group consumers with no measurement of their own — a value count, a Boolean or Confirm group operand, `MinValue`/`MaxValue` over a group — also project. [`EXP-2026-08-31-02`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-31-02) carries those three observations, each needing its negative partner rule so `unknown` is separable from the empty answer. `AtLeastOneFieldFilled` and `NoFieldFilled` are measured here and agree with the count, but have no checked group carrier in this project to correct.
- `local-consequence-superseded`: the per-consumer split that line describes is **gone**. `MinValue`, `MaxValue`, `NumberOfDifferentValues`, and the numeric value counts no longer retain the complete formal-cell view for want of a measurement — the [starred-field probe](#src-starred-field-operand-extent) measured all four and they project, and the [computation-arm probe](#src-capacity-projection-computation-arm) extends that to the other arm. What remains genuinely unmeasured is narrower: a Boolean or token *starred field* carrier.
- `still-open-closed`: that `still-open` line and the `sync` line below it are **discharged**. All six carriers project over a fixed group operand ([sweep](../SOURCES.md#src-group-operand-capacity-consumer-sweep)) and the starred form answers identically ([probe](#src-starred-group-operand-extent)); the `limit` line's last item — whether a malformed in-capacity cell still blocks the answer while an over-limit one does not — is measured there and is what settles the extent. `AtLeastOneFieldFilled` and `NoFieldFilled` now have their checked group carrier.
- `limit`: three fill quantifiers over one fixed nonrepeatable group operand, two repetition levels exercised out of three declared, bounded repeatabilities of 2, ordinary String fields, `en_US`, full validation. Not measured: a value-count or Boolean group carrier on the same document, an aggregate over a homogeneous Number group, partial validation, and whether a *malformed* in-capacity cell still makes the count unknown while an over-limit one does not.
- `bytes`: [`group-operand-over-limit-extent/`](../../evidence/kernel-30.8.1/captures/group-operand-over-limit-extent/); SHA-256 request `3bf4aee784b50e2b62e2b9b3730325be28dd764793abcfab21ccff57204df143`, artifact `4ce240e23f962f388af2df1149b89a431270e197b8231b03be001af42d5bc12f`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: behavioral, and **no longer held** — the locus is settled and the clause is corrected. [`spec/02`](../../spec/02-logic-and-formal-errors.md)'s unfiltered-validation tally now states that a cell in a row beyond its group's declared repeatability enters none of `t`, `e`, or `u`, and names the malformed cell as the separator that makes this a fact about the range rather than about the classification. This row previously said the correction waits on the entry above; that wait ended with [`SPEC-2026-08-31-10`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-10), which carries it.

<a id="src-nested-star-bound-outer-level"></a>
#### A nested star answers from its own enclosing row, measured locally 2026-08-31

- `revision`: one `:adapter:kernelProbe` request at a12-dmkits `a8bd8e86450b1629c08449e33116f84eef029859`, clean before and after with `state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on all eight rows.
- `question`: a wildcard on the **inner** level of a two-level repeatable chain refused `missingBinding` here for the enclosing level, and that refusal was recorded as a property of the unbound environment supplied rather than of the shape. It left the shape unclassified: nothing said what the operand answers once a rule actually binds the outer level, and a union-over-all-outer-rows reading is the natural wrong account.
- `model`: `Deep3_DM`, a nonrepeatable `/Probe/Shell` over a three-level chain `L1`/`L2`/`L3`, each `max 2` and each owning one String field, plus a nonrepeatable `/Probe/Other`. Model SHA-256 `9ff5cb5c234e4e95e89654301c21921ffedb5740fb9fd463ea2ffe5054472a9e`, `model check` valid.
- `admission`: `NumberOfFilledFields(L2*)` authored on `/Probe/Shell/L1` is **admitted**, so the unstarred outer level is legal exactly because the rule binds it. That is the same wildcard rule [`spec/07`](../../spec/07-repetition-and-iteration.md) states for authored paths, reaching the case where the binding comes from the rule's own iteration.
- `claim`: **the operand answers from its own enclosing row.** A document with two `L1` rows, both carrying `V1`, and inner rows only under the first draws exactly **one** message, on the first outer row. A union reading would answer nonzero for the second row too and fire twice.
- `local-consequence`: the checked starred-group source already answers this way — bound at outer row 1 it counts 1, at outer row 2 it counts 0, and unbound it still refuses — so the measurement classifies the shape rather than correcting it. The case in [`Conformance/FilledFieldGroupCount.lean`](../../A12Kernel/Conformance/FilledFieldGroupCount.lean) locks the separation the second outer row provides.
- `absorption`: the run corroborates the [over-repetition finding set](over-repetition-probes.md#src-over-limit-finding-multiplicity) on a **third** model and at two different levels. An over-limit `L1[3]` and an over-limit `L2[3]` each draw one `zuGrosseZeile` on the row and one `zuGrosseKontextnummer` on the cell that row itself writes, confirming that a cell of the violated row counts and not only a cell of a descendant row.
- `not-separated`: the three-level document confirms that a shell whose content lies three repetition levels down still counts, but **isolates nothing**, and the round is recorded that way rather than as a depth result. Rows are dense from index 1, so instantiating the deepest row instantiates every ancestor row, and the shell's row constituent fires from the outermost one under a depth-bounded account exactly as under an unbounded one. Isolating depth needs a carrier that reads cells rather than rows.
- `measured-since`: this row previously said that design — `FieldFilled(Other/W)` guarding `NumberOfFilledFields(Shell) > 0` over `nover-inner` against `nover-inner-ctrl` — was authored, statically confirmed, and never run, and asked a later session to run it. **It was run**, in the very next round, and is recorded under the neighbouring [group-operand extent checkpoint](#src-group-operand-over-limit-extent) rather than here: `Deep3Count_DM` at a12-dmkits `7ace4229c`, `state: CLEAN`, both engines agreeing on all seven rows. `nover-inner` and `nover-outer` each draw `FZERO` and `NOFILL` beside the two structural codes, while both controls draw `ANYFILL` and `FCOUNT` — so the over-limit row's cell contributes nothing and the count is exactly `0`. The instruction to re-run it stood for one round and is withdrawn; the retained bytes are `group-operand-over-limit-extent/artifact.count.json`.
- `limit`: `NumberOfFilledFields` with a starred group operand, one nesting level below the bound one, bounded repeatabilities of 2, ordinary String fields, `en_US`. Not measured: two unbound levels below the binding, a starred operand naming an ancestor of the binding group, and the filtered form, which the [repeatable-scope checkpoint](#src-group-count-gates-repeatable-scope) shows is refused at authoring anyway.
- `bytes`: [`nested-star-bound-outer-level/`](../../evidence/kernel-30.8.1/captures/nested-star-bound-outer-level/); SHA-256 request `0f4699e7c7f0d46b9b771908f606eb1755adc3ece1c94b6d85063097a431964d`, artifact `8f2b2f61699a570c46cae30d04364646fe8008a5e8620662e86e58294ea48275`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: the admission and the per-row reading both follow rules [`spec/07`](../../spec/07-repetition-and-iteration.md) already states — the wildcard gate reads the authored path, and the operand's extent is enumerated below its own bound depth. Nothing in `spec/` claimed the union reading, so no behavioral change and no outbound entry follows.

<a id="src-repeatable-descendant-group-count"></a>
#### A repeatable descendant makes its shell count structurally, measured locally 2026-08-30

- `revision`: a12-dmkits `04134609513485554fb4a84a67dc6fa0d7add154` (clean before and after; the artifact records `state: CLEAN`), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime. One `:adapter:kernelProbe` request, five documents, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on every row.
- `question`: the [subtree extent](#src-nested-descendant-group-count-runtime) and the [invalid-descendant rule](#src-nested-descendant-group-count-invalid) both measured **nonrepeatable** descendants, where a group's content is its cells. A shell whose only content is a **repeatable** descendant was admitted statically by both projects and counted by neither, so this project refused it and recorded the count as unmeasured on both sides.
- `model`: `RepeatShell_DM`, authored through structured verbs. Root `Probe` carries the scale-0 Number target `Tally`, a flat `/Probe/Flat` with `FlatValue`, and a shell `/Probe/RowShell` whose only content is the repeatable `/Probe/RowShell/Rows` (max 5) declaring `RowValue`. The computation `Tally := NumberOfFilledGroups(Flat, RowShell)` persisted `KERNEL_CONFIRMED` and `model check` reports the model valid, which reproduces the peer's static admission row on this project's own fixture.
- `claim`: **an instantiated row is content on its own.** One row carrying no filled cell at all makes the shell count as filled: with `FlatValue` filled and `Rows` holding a single empty row, `Tally` is `2`. A cell-only account answers `1` here, because the shell owns no filled cell anywhere in its subtree, so the row constituent is established independently of any cell.
- `control`: removing the row drops the count to `1`, so the shell is not counted unconditionally. A filled row also gives `2`, and an empty row followed by a filled one gives `2`, so neither row count nor cell content moves the answer once one row exists. Absent content everywhere gives `0`, so `Flat` still contributes by its cell.
- `mechanism`: content has **two constituents and either is sufficient** — an admitted descendant cell, or an instantiated row in a repeatable descendant. This row establishes the second; the [subtree](#src-nested-descendant-group-count-runtime) and [invalid-descendant](#src-nested-descendant-group-count-invalid) rows establish the first, in subtrees carrying no repeatable group at all. Neither of those shapes separates the constituents on its own, because a cell needs a row wherever rows exist and no row exists where the subtree is repeat-free.
- `kind`: the fill rule is stated **kind-free** — an admitted descendant cell counts, whatever the field declares — and every fixture retained by this project declares `NumberType` and nothing else, so its own rows could never have shown that. The peer's disjunction shape settles it: its cell constituent is a declared `STRING` field, `Bundle/Notes/Text`, and filling it alone makes the group count. Recorded because a kind-free rule measured only on Number fields is an unnamed constant of the same class as the capacity and seed ones, and this one happens to be discharged rather than open.
- `disjunction`: the separating shape is the peer's, read here rather than taken on report. At a12-dmkits `4e1497f57` it reproduced this discriminator and corrected the first reading here, which had called the row constituent a replacement for the cell one rather than its peer; at `b05517e1b` it measured the disjunction directly against **one** operand owning both a nonrepeatable `Notes/Text` and a repeatable `Items`. The cell alone counts, the instantiated empty row alone counts, and the neither-constituent control counts one. That control is load-bearing: without it the pair is indistinguishable from an unconditional count.
- `polarity`: the implicit self-validation mismatch is `VALUE_ERROR` on the three rows carrying an instantiated row and `OMISSION_ERROR` on the two without, which is the established omission/value split and a second channel agreeing that an empty instantiated row counts as present.
- `reference`: the operand inventory renders the repeatable axis with the wildcard coordinate, `/Probe[1]/RowShell[1]/Rows[0]/RowValue`, and does so identically on every row including the ones with no instantiated row at all. It is an authored-shape inventory rather than a reached-cell trace.
- `local-consequence`: the refusal in `ResolvedGroupReference.computationDescendants?` stays and its justification changed from unmeasured to **measured and mechanism-bound**; the addressed group-count route later lifted it by supplying the row topology that query cannot have, while the scalar route keeps refusing. That query projects a group to a list of cell reads, and row instantiation is not expressible as a cell read at any width, so counting this shape needs the document's row topology rather than a wider filter. The exclusion is now a named boundary between two mechanisms rather than an open question.
- `validation-arm`: the other arm **already agreed**, and is now locked on the same discriminator. `ResolvedGroupPresenceInput.derive` takes a group's content from an instantiated row *or* an admitted descendant cell, and `rowWithinGroup` tests subtree containment, so a row in the repeatable descendant already made the shell present. Only the compute arm ever disagreed, and only because its projection has no row dimension. The retained cases add the pair the Kernel row separates plus the boundary beside it: the enclosing shell answers structurally, while the repeatable group's **own** presence fails closed without a bound row.
- `limit`: one repetition level, one shell, scale-0 Numbers, and capacity 5 with at most two rows on this project's own fixture. Not measured here: an over-limit row's effect, and any starred spelling of this operand. A repeatable descendant more than one level below the operand was listed here as unmeasured and now is measured at the [deep checkpoint](#src-deep-repeatable-descendant-group-count) — depth does not bound the row constituent. A shell **mixing** repeatable and nonrepeatable content was listed here as unmeasured and is not — the `disjunction` bullet above measures exactly that on the peer's fixture, and this line was stale relative to it.
- `bytes`: [`repeatable-descendant-group-count/`](../../evidence/kernel-30.8.1/captures/repeatable-descendant-group-count/); SHA-256 request `79f9e77fd39a566f90acd6da23cb5a68f7dc599ea952cd25fadf1aa6983b6c5c`, model `3ebfa078cfd81e615c93763c4f86babbe9439d0718c004916d9bc7a6ae9fbb05`, artifact `c072a06323f9b333864039f75f6a4d47d5951194cf8941cf65bc585a9400f86e`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: locally originated and behavioral. It states a rule `spec/` did not carry for this operand shape, so it opens its own outbound entry.


<a id="src-nested-descendant-group-count-invalid"></a>
#### A formally invalid nested descendant still fills its group, measured locally 2026-08-30

- `revision`: a12-dmkits `c9679c901789585f2f830d02c7d1060efb9012f5` (clean before and after; the artifact records `state: CLEAN`), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, model version 28.4.0. One `:adapter:kernelProbe` request, five documents, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on every row.
- `question`: the widened compute-arm clause counts a group over its whole subtree ([runtime checkpoint](#src-nested-descendant-group-count-runtime)), and the established rule that a formally invalid cell leaves its group *present* was measured only for a **direct** child. Extending it to a nested descendant was uniformity rather than observation, and uniformity is an assumption.
- `model`: `NestedInvalid_DM`, authored entirely through structured verbs. Root `Probe` carries scale-0 Numbers `Tally` and `TallyDirect`, a flat `/Probe/Flat` with `FlatValue`, a flat `/Probe/Direct` with `DirectValue`, and a **shell** `/Probe/Shell` whose only field is `/Probe/Shell/Inner/Deep`. Two computations at `/Probe`: `Tally := NumberOfFilledGroups(Flat, Shell)` and `TallyDirect := NumberOfFilledGroups(Flat, Direct)`. Both persisted `KERNEL_CONFIRMED`, read back structurally with operands rendered relative to the declaring group, and `model check` reports the model valid. Each target is seeded `9` so a clear would be visible.
- `claim`: a nested descendant that is **formally invalid** still makes its group count as filled. With `FlatValue` filled and `Deep` carrying the non-numeric `"bad"`, `Tally` is the ordinary exact `2` — the same value as when both are cleanly filled. The invalid cell is present in `formalErrorsInOperands` as `zahlHatUngueltigeZeichen` at `Probe/Shell/Inner/Deep`, so it was in the checked plan's inventory and still neither made the count unavailable nor cleared the target. The clause's uniformity assumption is now an observation.
- `control`: emptying that same descendant drops `Tally` to `1`, which independently re-derives the whole-subtree rule on this project's own fixture rather than only on the peer's. Absent content everywhere gives `0`. The counts move, so no row is a value the rule never reached.
- `calibration`: `TallyDirect` reproduces the established **direct**-child row in the same model and the same run — a malformed `DirectValue` beside a filled `FlatValue` counts `2` — so the nested result is compared against an independently measured answer rather than standing alone.
- `structural-witness`: a second channel agrees. Every `TallyComp` message's `referenced` inventory lists `Probe/Shell/Inner/Deep`, so the Kernel's own operand expansion for a group reaches the nested descendant, independently of what the count returns.
- `polarity`: the implicit self-validation mismatch is `VALUE_ERROR` on the filled and the malformed rows and `OMISSION_ERROR` on the two rows with absent content, which is the established omission/value split rather than a new rule. It is also a third witness that a malformed cell counts as present here, since the malformed row keeps the filled row's polarity.
- `bytes`: [`nested-descendant-group-count/`](../../evidence/kernel-30.8.1/captures/nested-descendant-group-count/); SHA-256 request `1ffe3efbc0f2d01c6d8cc2b9c59c7acc76a1b1f3c4fbaca45332e864f2462c88`, model `a20b84ee2b5efb12aaeeac6cfdef725e1c1d27b724e5788a047cb9d8b3632169`, artifact `59e96cc906680f1f348910bb87dfdceae6d0b5374a9b2b3eb80e0aceac3ab433`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `arm-divergence`: the peer independently measured the other arm on this shape at a12-dmkits `4e1497f57` — the validation arm is **suppressed** where the computation arm still counts, so the established arm inversion reaches a nested descendant rather than stopping at a direct child. That scopes the peer's own published rule about erroneous group operands to the validation arm, which it has amended in place. The divergence is locked here as an internal separator, since widening the compute arm's extent through the subtree says nothing about the validation arm's availability rule.
- `limit`: one nesting depth for the count, scale-0 Numbers only, and **absent** rather than present-empty descendants in the two zero-and-one rows, so the present-empty variant still rests on the earlier direct-child observation. Not measured: a repeatable descendant's count, a mixed clean-and-invalid subtree, deeper nesting at runtime, and any starred operand.
- `sync`: locally originated, and it narrows no `spec/` distinction — it replaces an explicitly recorded uniformity assumption with the measurement it assumed. The clause changes from "unmeasured on either arm" to measured, which is a behavioral `spec/` extension and carries its own outbound entry.


<a id="src-nested-descendant-group-count-runtime"></a>
#### A group-count operand reads its whole subtree, reconciled inbound 2026-08-30

- `revision`: a12-dmkits `e0bfd1f353acca32b60811bc26f3b2c3d657c1d8` (resolved in the sibling checkout, worktree clean), Kernel `30.8.1`, `dmtool` 0.13.0, A12 Tools `2025.06-ext5`. Inbound handback answering [`SPEC-2026-08-30-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-30-01).
- `route`: the peer's kernel-backed differential harness, `adapter/…/laws/RuntimeLaws` plus `NestedGroupFillCountLawsTest`, read here rather than taken on report. `RuntimeLaws` imports `com.mgmtp.a12.kernel.*` and drives the real kernel's own computation and validation runtime, so the oracle is the engine and not the peer's interpreter. The eleven static rows come from `EmptyGroupOperandLawsTest` through `checkConsistency`.
- `question`: [the admission checkpoint](#src-nested-descendant-group-count-admission) measured that such an operand is legal and left the counting rule open, because admission does not imply contribution — a legal operand can count for nothing, so no static row could settle it.
- `claim`: **the whole subtree.** On a `Catalogue` root carrying a flat `Contact/Email` and a shell `Warranty` whose only field is `Warranty/Terms/Clause` two levels down, `NumberOfFilledGroups(Contact, Warranty)` computes **2** with both filled. The whole-subtree account predicts 2 and the direct-children account predicts 1, since under the latter a shell owns no field and can never be filled.
- `control`: emptying that descendant drops the count to **1**, where both accounts agree. Without it a count stuck at 1 would have satisfied the direct-children prediction for the wrong reason.
- `arms`: the validation arm reaches the same number, measured separately rather than transferred. The rule fires against `= 2` and does not fire against `= 1` on the filled document. The two arms are separately specified and the plain multi-group count is the stricter of them on erroneous operands, so their agreement here is a measurement.
- `static-extension`: eleven `checkConsistency` rows past the admission checkpoint's stated limits. A field **three levels down** (`/Store/Deep/Mid/Leaf/LeafNote`) is admitted, so depth does not bound it; a shell whose only content is a **repeatable descendant** (`/Store/RowShell/Rows`) is admitted; and a shell that is **itself repeatable** follows the operator's ordinary wildcard rule, refused `MVK_NO_WILDCARD` unstarred and admitted starred. The decisive control is the **empty** group on the same carrier in the same table, still refused `MVK_GROUP_IS_EMPTY`, which separates "the count admits nested content" from "this carrier refuses nothing"; the arity and wildcard controls fire there too.
- `local-consequence`: the scalar computation-phase gate is widened. `ResolvedGroupReference.computationDescendants?` now routes through `FlatModel.groupSubtreeFields` like every other group expansion in the theory, which also repairs that query's own documented invariant — it was the one site enumerating a narrower extent than it claimed. Two laws hold the extent: `computationDescendants_admitted_eq_subtreeFields` states it once at the mechanism, and `groupPresentForComputation_of_subtreeMember` states the consumer-visible half that depth is irrelevant to whether a group counts as filled.
- `retained-refusal`: a **repeatable group at or below** the operand still refuses. That is a representational boundary, not the Kernel's: the Kernel admits the `RowShell` shape statically, but a scalar context has no instantiated row to read and what such an operand counts is unmeasured on either side. Refusing claims nothing; counting would claim a value.
- `limit`: one runtime fixture, one nesting depth for the count itself, and one carrier. Not measured on either side: what a repeatable-descendant shell counts, and a shell whose only content is **formally invalid**, which is where the widened clause now extends an established direct-child rule by uniformity rather than by observation. [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) carries the second as an open request the peer has offered to run.
- `fixed-row-domain`: the fixed operand's row constituent counts **any** instantiated row, where the starred count excludes over-limit ones. Separating that needs a `max 5` group holding only row `7`, or a `max 0` group holding row `1`. Neither was authored on either estate: **no witness known as of a12-dmkits `898716944`** (resolved in the sibling checkout, worktree clean). The claim recorded here is the absence of a witness, not the absence of the document.
- `referenced`: the implicit self-validation message's `referenced` channel carries the operator's own **subtree** extent, read from this capture's own message bytes rather than from a later run. `TallyComp` names `Probe/Shell/Inner/Deep` — the shell's only field, two levels below it — beside `Probe/Flat/FlatValue` and the computed target `Probe/Tally`. So a group's depth does not bound what the message reports, exactly as it does not bound what the count reads.
- `referenced-repeatable`: the [repeatable-descendant capture](#src-repeatable-descendant-group-count) extends that through a repeatable descendant, naming `/Probe[1]/RowShell[1]/Rows[0]/RowValue`. Its **local consequence** is that the message extent is wider than this project's admitted evaluation: the scalar route refuses that operand `unsupportedGroupCount` because a cell list cannot express row instantiation, and the inventory must therefore be derived from the authored operand rather than from a successful read. The addressed route reads the rows and counts it, on both group-count atom shapes.
- `referenced-invariance`: a12-dmkits reproduced this at clean revision `7c325e82c` and supplied the row this project's captures could not — `NestedGroupFillCountLawsTest.theReferencedSetFollowsTheAuthoredOperandAndIsUnchangedByWhetherAnyRowExists`. For a starred operand the set is **byte-identical** between a document with no instantiated row and one with four rows whose every cell is filled. This project's two rows each show the set naming *more* than was read, which a partially-read account can still accommodate; invariance under filling cannot be accommodated at all, so the rival is dead rather than disfavoured.
- `referenced-multi-field`: the same lock closes the extent question this project's fixtures could not reach, since every group in them owns exactly one field. The peer's group operand names **both** of its row fields, so the subtree extent is a real enumeration rather than an artefact of single-field groups.
- `referenced-bound`: the extent is the **operand's own reach**, not its enclosing group's. In the same message set a `Sum` over one flattened field names that field alone and not its sibling, so carrying the subtree rule from a group operand to a field one would over-report. Read here rather than taken on report.
- `referenced-coordinate`: the starred axis carries an **unbound** `0`, read off the Kernel pointer's own `repetitionIndexes()` rather than parsed from a rendered path, and it stays at that value whether zero or four rows exist — so it is not a row index. That answers the pointer-domain half for this shape; this project models only the field half, which is an implementation boundary rather than a limit of the observation.
- `referenced-spelling`: one message carries two pointer spellings — a repetition-free address bare (`Probe/Flat/FlatValue`) and one crossing a repeatable group fully coordinated with the starred axis at the wildcard (`/Probe[1]/Rows[0]/RowValue`). Only the **field** half is modelled here; mapping a rendered coordinate onto this project's pointer domain is [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration)'s.
- `referenced-order`: the channel is a set. Every retained observation arrives ordered by rendered spelling, which is the capture's normalization and not a Kernel guarantee, so no order is claimed and the local clause's authored order carries none either.
- `sync`: the count rows are entirely inbound from a committed, reviewed revision, so they open no outbound entry, and they close [`SPEC-2026-08-30-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-30-01). The `referenced` records above are locally originated and behavioral, read from the retained bytes of this and the adjacent captures, and open their own entry.


<a id="src-nested-descendant-group-count-admission"></a>
#### A fixed group-count operand admits nested descendants, measured locally 2026-08-30

- `revision`: a12-dmkits `01bc94279f0aee06fd8d9ccfad2e116354aa409e` (clean before and after), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime. Six `computation add --dry-run` verdicts through a caller-tagged `batch`, all `KERNEL_CONFIRMED` with `source: KERNEL`.
- `question`: this project's scalar computation-phase group read refuses a group that owns any deeper descendant field, deliberately and with its reason stated: no observation covered the shape, and a refusal claims nothing while a count would. The admission half is now measured.
- `model`: `GroupPos_DM` with a Number target `/Probe/Tally`, a flat `/Probe/Flat` and `/Probe/Alpha` holding direct fields, and a **shell** `/Probe/Shell` whose only field sits in the descendant `/Probe/Shell/Inner`.
- `claim`: a fixed group whose fields lie in a **nested descendant** subgroup is an admitted operand of `NumberOfFilledGroups`. Admitted in first position, in second position, and in a three-group list, so neither position nor arity gates it.
- `controls`: the all-flat pair is admitted and a one-group list refuses `MVK_PARAMSIZE_INVALIDGN`, so the arity gate is live in the same model; a list naming a group under a repeatable extent refuses `MVK_NO_WILDCARD`, which is the wildcard gate rather than a nesting one.
- `confound`: a first pass ran against a model that did not declare the shell and drew `MVK_INVALID_ENTITY` on every nested row. The flat control stayed admitted and the arity control still fired, which is what exposed it; a run whose refusals are uniform across the matrix is measuring the fixture.
- `local-consequence`: **none yet, and deliberately so.** Admission is measured; what such an operand *counts* is not. Widening the local gate on admission alone would make the theory return a number it cannot justify, which is worse than the refusal it would replace. The exclusion stays, with its justification narrowed from "no observation covers this shape" to "the counting rule is unmeasured". **Superseded the same day**: the [runtime checkpoint](#src-nested-descendant-group-count-runtime) measured that rule and the gate was widened, so read this row as what admission alone justified, not as the standing local position.
- `limit`: static admission only, one nesting level, list lengths one to three, and a nonrepeatable shell. Not measured: what the count yields over a nested descendant, whether the validation and computation arms agree there, deeper nesting, and a repeatable shell.
- `sync`: locally originated and behavioral. It adds a static admission `spec/` does not state; its outbound entry carries it.


<a id="src-empty-declared-group"></a>
#### An empty declared group is kernel-valid, measured locally 2026-08-29

- `revision`: a12-dmkits `4e78f8254bcf12f6d94fc23c5c3cd5e4906c8d04` (clean before and after), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime. Structured `group add` writes followed by `model check`, the Kernel's own consistency oracle.
- `claim`: a nonrepeatable group carrying **no field anywhere in its subtree** is admitted and the model stays valid. Both a subgroup `/Probe/Hollow` and a whole empty **root** `/Hollow` were added to the two-root `GroupPos_DM` model, and `model check` reported the model valid after each.
- `origin`: this contradicts an unmeasured claim this project carried in `FlatModel.hasGroupPath`, which justified representing groups only through their fields by asserting that the Kernel rejects empty ones. It was never measured, and it is false.
- `consequence`: an empty declared group is visible to the rule-message group position. `$#/Hollow$` on the empty root draws `INVALID_GROUP`, the ordinary containment class, and `$#/Probe/Hollow$` draws it too, against an undeclared `$#/Nope$` drawing `INVALID_ROOT_GROUP` and a declared non-containing `$#/Second$` drawing `INVALID_GROUP` in the same batch. So an empty root is a *declared* root for that gate.
- `local-consequence`: `FlatModel` represents a group only through its fields or its own repeatable declaration, so it cannot express an empty nonrepeatable group at all. Sixteen gates ask `hasGroupPath` and all inherit that limit; none is wrong on the representable domain, and the one that would diverge cannot be reached. Lifting it means giving groups their own declaration list, which [SG9](../SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) carries as an owner decision rather than a capsule.
- `operand-gate`: **legal to declare, illegal to name.** Every operand naming a group whose subtree carries no field is refused, on a code of its own rather than the unknown-group one: `GroupFilled(/Probe/Hollow)` and `FieldsNotCollectivelyFilled(/Probe/Hollow)` draw `MVK_GROUP_IS_EMPTY`, `GroupFilled(/Hollow)` on the empty root draws `MVK_ROOT_GROUP_IS_EMPTY`, and `NumberOfFilledGroups(/Probe/EmptyRows*)` on an empty **repeatable** group draws `MVK_GROUP_IS_EMPTY` too.
- `subtree-not-direct`: the gate is over the whole subtree. A group with **no direct field** but a field in a descendant subgroup is admitted under all three operators, both nonrepeatable (`/Probe/Shell`, populated only through `/Probe/Shell/Inner`) and repeatable (`/Probe/ShellRows`, populated only through `/Probe/ShellRows/Deep`). Populated direct controls are admitted in the same batch.
- `local-consequence`: this half **was** a live defect rather than an unreachable one, and the first pass of this record wrongly said no divergence was reachable. An empty *repeatable* group is expressible in `FlatModel`, because a repeatable declaration names its group directly, and `elaborateStarredGroupSource` admitted a starred operand on one where the Kernel refuses. The certificate now carries a populated-group witness and the elaborator refuses on its own arm, so every construction site is compiler-enforced. The nonrepeatable half remains unreachable for the representational reason above.
- `survey`: the other group-resolving routes were checked rather than assumed. `RepetitionNotUnique` refuses an empty reference group for its own reason, measured, since no key field can lie inside a group with no fields. The fixed-group operand and the scalar computation-descendant query both refuse repeatable groups already, and a present nonrepeatable group is **proved** to carry a subtree field rather than argued to, so neither can reach one. The index-relevance predicate is a document-side pattern rather than an operand admission.
- `limit`: static consistency only, one empty subgroup, one empty root, one empty repeatable group, and no runtime observation. Not measured: what an empty group does at runtime, whether A12 Tools round-trips one, and whether the two empty-group codes differ for anything but a root.
- `sync`: it corrects a Lean docstring rather than a `spec/` clause, so it opens no ledger entry. `spec/` never asserted the rejection.


<a id="src-multi-root-short-name-reach"></a>
#### A model may declare more than one root group, and the short-name tier spans them, measured locally 2026-08-29

- `revision`: a12-dmkits `4e78f8254bcf12f6d94fc23c5c3cd5e4906c8d04` (clean before and after), `dmtool` 0.13.0, Kernel `30.8.1` built and runtime. Every row is one `rule check` verdict through a caller-tagged `batch`, all `KERNEL_CONFIRMED`.
- `reachability`: a model with **two root groups** is authorable and kernel-valid. `group add --parent ""` is accepted, the terminal `apply` gate committed it, and `model check` reports the model valid. Until this row every path measurement ran on a single-root model, so nothing separated "model-wide" from "root-wide" in the short-name tier.
- `model`: `GroupPos_DM` with roots `/Probe` (repeatable `/Probe/Rows`, its subgroup `/Probe/Rows/Deeper`, fixed `/Probe/Store`) and `/Second` (with `/Second/Inner`), `fieldRefByShortNameAllowed` true as `model new` leaves it. `Tag` and `OwnRootOnly` are model-unique; `Shared` is declared once in each root.
- `claim`: the model-wide short-name tier is genuinely **model-wide**. A bare `Tag`, declared only under `/Second/Inner`, resolves from a rule at `/Probe/Rows`, and the symmetric direction resolves too. An absolute cross-root path resolves as well.
- `claim`: the duplicate refusal crosses roots with it. A bare `Shared` from `/Probe/Rows`, which declares neither, draws `MVK_FIELDNAME_NOT_UNIQUE`.
- `separator`: the declaring-group tier still wins over that cross-root ambiguity. The same bare `Shared` from a rule at `/Second/Inner`, which declares one of the two, is **admitted**, so the exact `declaringGroup/name` match runs ahead of the global index exactly as [`LF6`](../LEAN-FINDINGS.md) records, and the ambiguity is a property of the referring group rather than of the name.
- `controls`: a same-root unique name and an own-scope name are admitted in the same batch, and a name declared in no root draws `MVK_INVALID_ENTITY`, so neither the admissions nor the refusals are artifacts of the route.
- `local-consequence`: none. This project's resolver already filtered the whole field list with no root restriction and already tried the declaring group first, so all six rows agreed on the first run. The value is that a newly reachable dimension of the addressing keystone is now locked by cases instead of resting on an untested assumption; [§10](../IMPLEMENTATION-MAP.md#10--paths-and-references) owns the boundary.
- `limit`: two roots, one nesting level below each, one condition language, and the short-name flag enabled. Not measured: the flag disabled across roots, a repeatable group in the second root, a bare name duplicated **within** one root while unique in the other, and whether any other model-wide index shares this reach.
- `reuse`: the same two-root model carried the cross-root computation-placement rows recorded on the [fixed-target star placement checkpoint](../SOURCES.md#src-fixed-target-star-placement), which owns that claim.
- `sync`: confirmatory for addressing. It adds no behavioral correction to `spec/` on its own account, so it opens no ledger entry; the placement rows it enabled strengthen the already pending [`SPEC-2026-08-29-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-29-03).


<a id="src-first-filled-additional-kind-computations"></a>
#### Direct one-star additional-kind `FirstFilledValue` computations

- `revision`: the maintained Boolean, Custom, and DateFragment runtime matrix was introduced at a12-dmkits `feba4552110858b07966660bf7080441d3b90d00` and inspected unchanged at clean HEAD `89aa03957034de620562eb23a095d878f6547dca`.
- `route`: [`FirstFilledValueAdditionalKindDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/FirstFilledValueAdditionalKindDiffTest.kt) executes dynamic Groovy, generated Java, and the interpreter over three fixed targets, each fed by one direct single-level starred source: Boolean, Custom sharing `ReviewCode`, and DateFragment sharing `MM`.
- `claim`: an instantiated row with no source value clears targets seeded with `false`, `SEED`, and `01`; all three engines agree for every carrier.
- `claim`: filled Boolean `true`, Custom `A7`, and DateFragment `06` sources produce `true`, `A7`, and `06`; the supplied Custom validator accepts `A7`, and all three engines agree.
- `policy-admission-revision`: clean a12-dmkits `89aa03957034de620562eb23a095d878f6547dca`, source-prepared dmtool `0.13.0`, and built/runtime Kernel `30.8.1`.
- `policy-admission-route`: guarded structured `computation add --dry-run` checks over one fresh model, followed by one persisted `yyyy` control, exact structural read-back, and a whole-model Kernel check.
- `policy-admission-claim`: matching direct one-star DateFragment computations are Kernel-confirmed for the four project-owned formats `MM`, `yyyy`, `yyyy-MM`, and `MM-dd`.
- `control`: a `yyyy` target fed by an `MM` source is Kernel-confirmed rejected with `MVK_INVALID_COMPARE_TO_DATE`; the persisted `yyyy` control reads back exactly as `FirstFilledValue(Rows*/Year)` and the model remains valid.
- `limit`: only the maintained `MM` matrix establishes runtime clear/value outcomes. The static policy matrix does not establish another format's runtime, formal rejection or poison, or whether output is copied or rendered.
- `limit`: the runtime matrix does not establish Boolean `false` or Custom type mismatch.
- `limit`: the matrix does not establish multiple, mixed, group, or filtered operands, nested repetition, partial validation, validation comparisons, target application topology, or result kinds outside its three carriers.

<a id="src-first-filled-kind-computations"></a>
#### Direct one-star established-kind `FirstFilledValue` computations

- `revision`: a12-dmkits `feba4552110858b07966660bf7080441d3b90d00` widened the maintained kind matrix; its `FirstFilledValueKindDiffTest` blob `c82d63d0d7408ae3e726d96cf397d5b0ced6d242` is unchanged at clean HEAD `89aa03957034de620562eb23a095d878f6547dca`.
- `route`: [`FirstFilledValueKindDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/FirstFilledValueKindDiffTest.kt) compares the dynamic-Groovy Kernel route with the interpreter for fixed targets fed by direct single-level starred sources.
- `route`: at that same clean revision, a12-dmkits' [`KERNEL-SEMANTICS.md`](../../../a12-rulekit/docs/KERNEL-SEMANTICS.md) owns the kind-dependent empty identity.
- `route`: [`ExprEval.kt`](../../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/eval/ExprEval.kt) supplies the independent interpreter's ordered selection, while [`Document.kt`](../../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/eval/Document.kt) supplies declaration-aware temporal and range canonicalization.
- `route`: [`ComputationEngine.kt`](../../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/ComputationEngine.kt) supplies the independent interpreter's target rendering route.
- `admission-revision`: a12-dmkits `e9b3d725bb52bcf6c459b8241710262424def16c`, inspected unchanged at clean HEAD `89aa03957034de620562eb23a095d878f6547dca`.
- `admission-route`: [`FirstFilledValueFamilyLawsTest`](../../../a12-rulekit/src/test/java/io/github/mbackschat/a12/dm/rulekit/validate/laws/FirstFilledValueFamilyLawsTest.java) admits two DateRange operands only when they share one exact legal `(format, separator)` pair, including `dd.MM.yyyy` with `-`; pairing that declaration with `yyyy-MM-dd` and `/`, or with Date, yields `MVK_VARYING_TYPES_NOT_ALLOWED`.
- `rendering-route`: at the same clean HEAD, [`DateRangeComputeDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/DateRangeComputeDiffTest.kt) observes dynamic Groovy, generated Java, and the interpreter render typed ISO input endpoints into a `dd.MM.yyyy`/`-` DateRange target as `01.06.2024-30.06.2024`.
- `target-check-route`: Kernel `cb66e51fa7ab90b650698f861bf670754e2e1e66` [`CalculationController.ts`](../../../a12-kernel/kernel-rt/kernel-core-runtime-ts/src/main/js/internal/core/CalculationController.ts) renders a computed `VkDateRange`, runs the target basic check, retains the attempted text, and marks rejection; [`FormatDefinitionDatumBereich.ts`](../../../a12-kernel/kernel-rt/kernel-core-runtime-ts/src/main/js/internal/formatdef/FormatDefinitionDatumBereich.ts) rejects an end before its start.
- `target-check-route`: the independent a12-dmkits [`ComputationEngine.kt`](../../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/ComputationEngine.kt) and [`FormalError.kt`](../../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/eval/FormalError.kt) rederive the same render-then-target-check mechanism and inverted-range class.
- `full-date-policy-revision`: a12-dmkits `6e482c67fc157dccd5c19226938b166db887543a` owns the format-keyed `FirstFilledValue` discriminator, and `81b11fd9147b80d00614acd1ee19890c57be2f68` owns the exact dotted full-Date target results; both are reachable and inspected at clean HEAD `89aa03957034de620562eb23a095d878f6547dca`.
- `full-date-admission-route`: [`FirstFilledValueFamilyLawsTest`](../../../a12-rulekit/src/test/java/io/github/mbackschat/a12/dm/rulekit/validate/laws/FirstFilledValueFamilyLawsTest.java) authors `dd.MM.yyyy`, rejects it beside `yyyy-MM-dd`, and admits the same-format ISO control while its independent cross-kind control establishes that the gate reads declared format rather than kind.
- `full-date-rendering-route`: [`DateBefore1900DiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/DateBefore1900DiffTest.kt) observes dynamic Groovy and the interpreter retain exact `dd.MM.yyyy` target attempts `31.12.1899` and `01.01.1900`; generated Java reports the exact accepted 1900 value and only outcome polarity for the errored 1899 attempt.
- `claim`: for full Date `yyyy-MM-dd`, Time `HH:mm:ss`, DateTime `yyyy-MM-dd'T'HH:mm:ss`, DateRange `yyyy-MM-dd` with `/`, stored Enumeration, and ordinary String, the maintained test executes named empty and first-row-filled computation inputs and asserts that the dynamic-Groovy Kernel and interpreter signatures agree.
- `claim`: Number has empty-computation and empty-comparison controls only; those rows also assert cross-engine agreement, but the test has no first-row-filled Number case.
- `historical-limit`: that maintained helper compares the two produced signatures but never asserts either literal signature; its test names, messages, and comments therefore did not by themselves establish the described clear/value outcomes.
- `calibration-revision`: clean a12-dmkits `89aa03957034de620562eb23a095d878f6547dca`, source-shipped dmtool `0.13.0`, built/runtime Kernel `30.8.1`.
- `calibration-route`: one dmtool-authored, persisted, structurally read-back, kernel-valid UTC model carries all four exact temporal carriers; `:adapter:kernelProbe` observes `validateFull` plus `compute` on dynamic Groovy and static Java, and dmtool `model compute` supplies the independent interpreter result over the same three canonical documents.
- `calibrated-claim`: with two instantiated empty source rows and four nonempty seeded targets, full Date, Time, DateTime, and DateRange all report CLEARED with no value; with row 1 filled they report respectively `2024-03-20`, `12:34:56`, `2024-03-20T12:34:56`, and `2024-03-20/2024-03-21`.
- `control`: leaving row 1 empty and placing those values in row 2 produces the same four VALUE outcomes, separating the ordered empty-prefix scan from a first-row-only account; both Kernel strategies and the interpreter agree, every Kernel row reports `enginesAgree: true`, and every formal-operand inventory is empty.
- `integrity`: request SHA-256 `a3cbeaa89a1ec3184dd323460416836cb274790d514630bc67b4981a72468dfd`; two unchanged runs produced byte-identical artifacts at SHA-256 `de67145c3e0fabf41bbf2f9d88173ce7ccad9f4300afcaa3426ba8d8dcaa9960`; model SHA-256 `cc8f6cf14d536faba685cf38d6e63159bb5d3c0d51266d6e10afe91e1d637ba4`; document hashes in request order are `682a35c1049d446f517e78e81cd03ddabbf2f0af91173ffb7a93919eac2ebefc`, `63fb2960d5489fc864ca764bb9f6f3b34c37e549a822ec2f5dc60546cb4f9241`, and `d518dbb3ee83453311ef6de2d7b231d9ca5d0078214197d3477e6bb6ac76df7b`.
- `limit`: the calibration establishes exact clear/value outcomes for the four named policies, two instantiated rows, one direct single-level starred source, and the empty-prefix control. It does not establish copy-versus-decode/render mechanism, zero instantiated rows, formal invalidity, alternate formats or separators, optional temporal checks, mixed kinds, multiple/group/filtered operands, nested repetition, partial validation, or wider document application.
- `policy-limit`: this four-policy calibration did not itself execute `FirstFilledValue` from a `dd.MM.yyyy`/`-` DateRange source into the matching target. The later [all-eight profile checkpoint](../SOURCES.md#src-date-range-2026-08-19-reconciliation) executes that exact composition on both Kernel strategies at reviewed a12-dmkits `52ce932dc9b32aefe8c1e424d7e56d7749709540`, superseding the former `external evidence pending` limit. The retained outcomes establish the composition and target text, not whether the implementation copies, decodes, or re-renders internally.
- `target-check-limit`: the source and independent-interpreter routes establish the render-then-check mechanism and inverted-range class, not a retained Kernel outcome for an inverted computed constructor. The Lean target-error outcome is therefore source-rederived and internally executable, not L-closed.
- `full-date-policy-limit`: the checked local composition reuses the format-keyed carrier and the independently calibrated dotted target without claiming an observed combined runtime. No retained external row executes direct starred `FirstFilledValue` from a `dd.MM.yyyy` full-Date source into the matching target, so same-format dotted admission and that exact composition remain external evidence pending; optional pre-1900 policies in this direct composition and every wider Date policy remain outside this route.
- `sync`: the result confirms the existing account and creates no outbound semantic correction.

<a id="src-current-repetition-root-condition"></a>
#### Nonrepeatable-root `CurrentRepetition` condition

- `revision`: a12-dmkits `feba4552110858b07966660bf7080441d3b90d00`, reachable from clean current main `89aa03957034de620562eb23a095d878f6547dca`.
- `route`: [`CurrentRepetitionDiffTest.aNonRepeatableGroupHasTheConstantIndexOneOnAllThreeEngines`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/CurrentRepetitionDiffTest.kt) executes dynamic Groovy, generated Java, and the interpreter.
- `claim`: under a direct filled-field guard, `CurrentRepetition(/Order) == 1` fires and `!= 1` does not; the operand is a nonrepeatable root read by a non-iterating validation condition.
- `limit`: the case does not establish fixed nested groups, repeatable-row or captured-outer lookup, computation use, partial validation, arithmetic wrappers, or other comparison operators.

<a id="src-current-repetition-repeatable-condition"></a>
#### Same-group repeatable `CurrentRepetition` validation

- `revisions`: a12-dmkits `6cef3217875fc8f00bb369745be0ecef6de9bd94` introduced the row-index differential, `a3abca552cf40c5de060733296e710cea40515d8` added VALUE polarity, `13e8ab71a00287783db019a7b1f2d97d86686daf` owns the iterating-versus-noniterating admission split, and `ee70581fae1991d359b7c78f1a6ecfbffdd1a31f` retained the exact row-addressed corpus outcomes; all are reachable from clean current main `89aa03957034de620562eb23a095d878f6547dca`.
- `routes`: maintained [`CurrentRepetitionDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/CurrentRepetitionDiffTest.kt), [`IterationLawsTest.currentRepetitionRequiresTheRuleToIterateOverTheGroup`](../../../a12-rulekit/src/test/java/io/github/mbackschat/a12/dm/rulekit/validate/laws/IterationLawsTest.java), and retained [`current-repetition-fires-beyond-first-row`](../../../a12-rulekit/corpus/cases/current-repetition/current-repetition-fires-beyond-first-row.json) plus its single-row control.
- `claim`: a direct filled field in the named repeatable group supplies the rule's row scope; `CurrentRepetition(group)` reads that group's 1-based selected coordinate. The retained `> 1` corpus is false at row 1 and fires VALUE at rows 2 and 3 with their concrete error addresses. The maintained differential also records cross-engine agreement for `> 2` over two and three rows and for `>= 1` over two rows. A missing iteration over the group is rejected `MVK_NO_WILDCARD`.
- `limit`: the `> 2` and `>= 1` differential rows compare only the first matching message type and carry no independent expected result, so they do not establish exact firing coordinates, cardinality, or even the intended threshold against a shared wrong account; those exact cross-threshold outcomes remain external evidence pending. The malformed rows likewise establish engine agreement, while their UNKNOWN guard result comes from the independently established `FieldFilled` formal-invalidity owner. The measured validation shape has one repeatable level and does not establish fixed descendants, captured `$`, computation use, partial validation, arithmetic wrappers, parsing, or comparison forms beyond `> 1`, `> 2`, and `>= 1`.

<a id="src-current-repetition-computation-dependency"></a>
#### Computation `CurrentRepetition` structural dependency

- `revisions`: the structural-dependency differential is a12-dmkits `43359ae258e1208e4de63ef00f684cd2a5250d7a`; all four two-row runtime calibrations used clean a12-dmkits `cd43c249a88874bf5688a45060e208a0dcd20782` with dmtool-release 0.13.0 and kernel 30.8.1.
- `routes`: [`CurrentRepetitionComputationDependencyDiffTest.structuralGuardAndRealDependentComputeInDependencyOrder`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/CurrentRepetitionComputationDependencyDiffTest.kt) executes dynamic Groovy, generated Java, and the interpreter; [`CurrentRepetitionComputationDependencyTest.structuralGuardDoesNotCycleWithARealDependentComputation`](../../../a12-rulekit/interpreter/src/commonTest/kotlin/io/github/mbackschat/a12/dm/interpreter/CurrentRepetitionComputationDependencyTest.kt) locks the exact `[First, Second]` interpreter order and values; [`AstRefs.kt`](../../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/AstRefs.kt) classifies the function as structural before group dependency expansion.
- `direct-number-artifact`: model, request, and Kernel-artifact SHA-256 values are `68130d55d6d75c7d62b6cf2ad70895b0605d5112c6cab7d302ebdb04e3e824cd`, `d99d6d5b5a6e2ce60203e40660908473d00c2d67a5ca79e64662b326adedc90e`, and `da060e9060ac5eb8009d03595d42209c26e26b405750caad5daa1156a69623b2`.
- `number-to-string-artifact`: model, request, and Kernel-artifact SHA-256 values are `e12e7a8e99abe153fb4e9abe2370900bad5bd93854e19490bc4ea075a93095c2`, `2e4639b03fe5ba9dd15c45af93a81df7caa52db8541dbfd4bbce699a789819bb`, and `f5d0cd3edf8b6884e29427579878e1255c0af722cac6ff96e884209373b90e8f`.
- `string-to-number-artifact`: model, request, and Kernel-artifact SHA-256 values are `0e3497af50be2e4d627ee2de8752fe8b13a2a42b602b845e4b8b9fa832d484cb`, `9c1dd576066e037767aa2930bd469776b4fc2000458eec12c83ccd4cb0044c6a`, and `9654ef1616e08c1d044c2afae17bdabf9fcd1fd60e199f2c380b44fe4a7dc785`.
- `alternating-chain-artifact`: model, request, and Kernel-artifact SHA-256 values are `22cde163c870fa97929c4ccd4132189b15de5d7d3b7f1c2a03d8a6fc959425cf`, `5cc5fd62c192711f2a737a9c243feeae89457e4c83e6f28075c41ed8927f401a`, and `81004442e0bce7fd9fddb2ac21cd7e200bd111a6393959b74029f67e1bd99573`.
- `claim`: in one repeatable `/Shipment/Lines` row, `CurrentRepetition(/Shipment/Lines) > 0` is a structural common precondition for the first Number computation and contributes no contained-field dependency; a second computation may depend on the first, and both produce `7` in dependency order on all three engines.
- `claim`: with two scale-0 rows, the direct-Number cascade replaces stale targets with each row's own `Base`, substitutes zero for an empty first-row source, and clears only the malformed row.
- `claim`: replacing the second direct copy with `FieldValueAsString(First)` produces row-local `"7"` and `"11"`, converts the substituted zero to `"0"`, and clears both typed targets only in the malformed row.
- `claim`: the inverse `FirstString := FieldValueAsString(BaseNumber); SecondNumber := FieldValueAsNumber(FirstString)` produces row-local `"7"`/`7` and `"11"`/`11`; an empty first-row Base clears `FirstString` but produces Number zero; a malformed first-row Base clears both; and Base `-5` retains an errored `FirstString` attempt while clearing `SecondNumber`. The other row completes in every case, and both Kernel strategies and the interpreter agree.
- `claim`: for the fixed alternating chain `FirstNumber := BaseNumber; SecondString := FieldValueAsString(FirstNumber); ThirdNumber := FieldValueAsNumber(SecondString)`, stale first-row targets `70`/`"old1"`/`700` and second-row targets `110`/`"old2"`/`1100` finish as `7`/`"7"`/`7` and `11`/`"11"`/`11`. Empty finishes as `0`/`"0"`/`0`; malformed input clears all three targets in only its row; and a negative Number leaves an errored String attempt before the dependent Number clears. Both Kernel strategies agree.
- `limit`: the runtime artifacts establish row-addressed outcomes and failure locality, not the Kernel's internal scheduling mechanism or cause attribution. The source-backed structural classification supplies the separate mechanism claim. The observations do not establish other comparison operators or bounds, more than two rows, nonzero Number scales externally, String-to-Number profiles beyond exact `[0-9]+` with finite length 15, captured `$`, validation reuse, partial computation, parser lowering, chains longer than three operations, other operations, or a general computation expression surface.

a12-dmkits revision `8094f6643ef3ad01d40dd6d3675356196374e104` answers this project's unstarred-group reference request and is the basis for [`spec/01`](../../spec/01-data-model.md)'s unstarred-group clause. `KF183` and [`UnstarredGroupReferenceDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/UnstarredGroupReferenceDiffTest.kt) own it: an unstarred group operand expands **recursively** into a fired message's `referenced` set with **concrete** coordinates, identically in the presence, entity-list, and count positions, tri-checked dynamic Groovy against generated Java against the interpreter on complete sorted sets, with the starred form re-read on the same model as the wildcarding contrast. Two scope limits travel with it.

The measured model is **nonrepeatable throughout**, so a subtree field below a repeatable level deeper than the rule binds has no witness; and the kernel pointer carries a coordinate per group part, including non-repeatable parts at `[1]`, which this project's normalized field-instance pointer deliberately does not represent — the retained distinctions are set membership and concrete-versus-wildcard, which do agree. The same revision closes the two computed-target scale entries by measuring the complete 24-cell operator × position × operand-kind grid plus twelve suppression rows rather than narrowing the earlier claim, and `KF181` now states that grid.

<a id="src-group-list-rnu-admission-correction"></a>
#### Group-list and RNU admission correction

- `basis-revision`: local dmtool 0.13.0 `rule check` ran 45 `KERNEL_CONFIRMED` rows from clean a12-dmkits `8094f6643ef3ad01d40dd6d3675356196374e104`; its single-root fixture could not separate root identity from overlap, and several rows varied error locus with operand shape.
- `revision`: reviewed a12-dmkits `2d384c59f18cf9a1019e1e8273f2d8e900f741e0`, reachable from the clean current main checkout.
- `route`: real-kernel maintained laws [`GroupListDiagnosticIdentityLawsTest`](../../../a12-rulekit/src/test/java/io/github/mbackschat/a12/dm/rulekit/validate/laws/GroupListDiagnosticIdentityLawsTest.java) and [`RepetitionNotUniqueKeyPathLawsTest`](../../../a12-rulekit/src/test/java/io/github/mbackschat/a12/dm/rulekit/validate/laws/RepetitionNotUniqueKeyPathLawsTest.java), each asserting complete emitted code lists.
- `claim`: a disjoint second root exposes `MVK_ROOT_GROUP_REFERENCED` for the all/not-all family and `MVK_ROOT_GROUP_WITH_OTHER_PARAMETERS` for singleton-admitting/count carriers; an own root/descendant pair still reaches earlier `MVK_DUPLICATE_PARAM2` overlap.
- `claim`: the error field's locus selects unstarred-repeatable admission. Outside the group, sole and paired forms draw `MVK_NO_WILDCARD`; inside, positive quantifiers are admitted, a sole count reaches `MVK_PARAMSIZE_INVALIDGN`, and a paired count reaches `MVK_NEG_CONDITION_IN_ITERATION`.
- `claim`: `MVK_REPEATABLE_GROUP_MISSING` covers a second nonrepeatable-group key, a sole nonrepeatable-group key, and a rule already placed at the repeated group in either path spelling. A parallel repeatable key in the measured index-free fixture instead draws `MVK_NO_WILDCARD` from both loci.
- `limit`: the indexed parallel fixture draws `MVK_NOT_IN_SAME_PATH`, but this evidence does not establish why; the local projector therefore leaves every parallel-repeatable shape unmapped while retaining the exact index-free observation here and in the canonical clause.
- `limit`: error-locus binding is measured for one repeatable level. Local condition elaborators do not receive the error field and therefore keep every unstarred repeatable group refused; the measured static matrix belongs at a rule-owned boundary, while nested binding remains unmeasured.
- `limit`: static admission and diagnostic identity only; no evaluation behavior follows.
- `sync`: the reviewed inbound correction answers and rejects the proposed mechanisms in [`SPEC-2026-08-11-03`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-11-03) and [`SPEC-2026-08-11-04`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-11-04); no feedback entry returns it to its origin.

<a id="src-filled-field-count-nested-capacity"></a>
#### A group `NumberOfFilledFields` moves against declared slot capacity, not declared field count, measured locally 2026-08-31

- `revision`: two `:adapter:kernelProbe` requests at a12-dmkits `1be2d603c90be578fe7ea0de0b8c515cb2a479c4`, both clean before and after with `state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on every row of both.
- `question`: [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) carried an untested clause — the polarity of a fixed group operand present only through an instantiated row of a repeatable descendant — flagged in its own record as following from the contribution rather than from an observation. The Lean clause read movement off `(groupSubtreeFields path).length`, the number of fields **declared** in the subtree. Every fixture behind it declares a flat group, where the field count and the number of cells a document can hold are the same number.
- `model`: `FillCount_DM`, one nonrepeatable `/Probe/Flat` holding `FlatA` and a `max 3` repeatable `/Probe/Flat/Rows` holding `RowR`. So two declarations and four slots. Three rules at `/Probe` on error field `/Probe/Flat/FlatA`: `NumberOfFilledFields(Flat) < 2`, `< 3`, and `< 5`. Model SHA-256 `b1d9562e3f68a8c795a43df01a8983c1341a389aeb18f9f49dda4d8bf35a3cc4`, `model check` valid.
- `separator`: five documents walk the count from 0 to 4. At counts 0, 1, 2, and 3 every fired rule types **`OMISSION_ERROR`**; at count 4 — `FlatA` plus all three rows filled — the one still-fired rule types **`VALUE_ERROR`**. The declaration count is 2, so an extent keyed on it would have typed counts 2 and 3 as `VALUE`. Both are measured as `OMISSION`, on both strategies.
- `mechanism`: the extent is the subtree's **declared slot capacity**: one cell per declared field per row combination its repeatable ancestors below the operand group may reach. Here `1 + 3 × 1 = 4`. This is the same growth rule the [starred-operand polarity checkpoint](#src-starred-operand-message-polarity) established — declared capacity is what ends growth — reaching the fixed-operand carrier, which had been keyed on a different quantity that happens to coincide without a repeatable descendant.
- `prediction`: the mechanism was confirmed on a second model built only to test it, not to derive it. `FillCap_DM` gives `Rows` `max 2` and **two** fields, so capacity is `1 + 2 × 2 = 5` against three declarations. Predicted before measuring: `OMISSION` at four filled cells, `VALUE` at five. Both hold on both strategies, and the declaration-count reading predicts `VALUE` at three. Model SHA-256 `685743b3abb123a3d3016438b5e865ad159992a180d400163365939821f6021d`.
- `local-consequence`: the fixed group operand now takes `FlatModel.groupSubtreeSlotCapacity?`, and an unretained descendant maximum yields no operand rather than an unmeasured movement rule, matching the starred carrier's existing treatment of a missing extent. Two laws record when the cheap reading is safe: capacity collapses to the declaration count exactly on a subtree with no repeatable descendant, and one repeatable descendant already separates them.
- `limit`: one nesting level below the operand group, bounded repeatabilities of 2 and 3, one and two fields per row, ordinary String fields, `en_US`. Not measured: two nesting levels, a partially instantiated row against an uninstantiated one at the same capacity, an operand group that is itself under a repeatable ancestor, and whether `NumberOfFilledGroups` over a *list* of group operands has the same coincidence — its extent is the operand-list length, a different quantity this record says nothing about.
- `bytes`: [`filled-field-count-nested-capacity/`](../../evidence/kernel-30.8.1/captures/filled-field-count-nested-capacity/); SHA-256 request `f0aba98894a3fd1e801343c3077d20ecb06e8212d9dcd9c257b0d7cfbe4a0123`, artifact `0d13375a6e618a610c865c882bc6dbca44d982c949b31503f1440b9ef77380f1`; prediction request `a1a95608a1a827c2f37df754f29a35cd04c094ca278990f69ad1c4252c9dc1de`, artifact `4b2d2c0bb2c06ac61d29217105c2032465a45e0c63b6916c7ca27320e0d30c49`. Bounded raw calibration captures with no typed consumer: they do not run under `lake test` and establish neither `L` nor `C` coverage.
- `sync`: this corrects a Lean clause, not a `spec/` clause. [`spec/10`](../../spec/10-validation-and-polarity.md)'s directional-fillability rule already says `OMISSION` is typed exactly when a legal move can falsify the fired condition, and a document below capacity has such a move; nothing in `spec/` said the extent was the declaration count. No behavioral `spec/` change, so no outbound entry follows.

<a id="src-partial-coverage-group-operands"></a>
#### The count needs every operand field covered, where the presence predicates read what they can see, measured locally 2026-08-31

- `revision`: three `:adapter:kernelProbe` requests at a12-dmkits `8cad1224f86e91c46f0db3573adb4f0fe894a959`, the sibling checkout clean before and after and `producer.source.state: CLEAN` in every artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` **and `validatePart`** on both codegen strategies, `enginesAgree: true` on all twelve rows.
- `route`: this is the first use of the `validatePart` observation, accepted upstream at that revision after this project recorded the coverage leg as unreachable. Each row carries its own `relevant` set and each engine block gains `partialMessages`; `validateFull` is the invariant control and answers identically on every row whatever the coverage.
- `question`: [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) carried two rows here — a call-local partial-coverage error and `noneRelevant` coverage — and had recorded both as unmeasurable because no adapter entry point reached partial validation. That limit is retired, not worked around.
- `model`: `Part_DM`, a nonrepeatable `/Part_DM/G1` holding a String `A` and a Number `B`, a nonrepeatable `/Part_DM/G2` holding a String `C`, plus `W` as every rule's error field and `Z` as an unreferenced field. Rules: a `NumberOfFilledGroups(G1, G2) == n` ladder, `GroupFilled(G1)`, `GroupNotFilled(G2)`, all four admitted group-list quantifiers, and a `FieldFilled(W)` live control. `model check` reports `KERNEL_CONFIRMED`.
- `claim`: **the numeric count and the presence predicates diverge under partial coverage, and the separating coverage is one line.** With `G1` holding a filled `A` and an empty `B`, covering `A` but not `B` fires `GroupFilled`, `AtLeastOneGroupFilled`, and `NotAllGroupsFilled`, and leaves `NumberOfFilledGroups == 1` **silent**. Covering `B` but not `A` silences the presence predicates too. So the predicates decide from the content they can see, while the count needs every field of every operand group in the relevant set.
- `separator`: `validateFull` fires `NFG1` on all six rows of the same document, so the silence is the coverage and not the document; and the same coverage that silences the count still fires three other operators over the same group, so it is not a rule-level gate either.
- `scope`: **the coverage requirement is operand-local, not global.** Leaving the unreferenced field `Z` outside the relevant set changes nothing — `NFG1` still fires — while leaving `G2`'s only field outside silences it. So a reimplementation checks the operand groups' own fields rather than the whole document.
- `none-relevant`: an empty `relevant` array is a request with no coverage, and it produces **no message at all**, the live control included. It is distinct from an absent `relevant`, which means the observation was not asked for.
- `formal-channel`: **partial validation gates the formal-error channel by the same coverage.** A malformed Number in `G1/B` reports `zahlHatUngueltigeZeichen` under `validatePart` when `B` is covered and reports nothing when it is not, while `validateFull` reports it either way. So a consumer must not read a partial run's formal findings as the document's formal state.
- `local-consequence`: **this refuted a shipped case of this project's own.** `numberOfFilledGroups` read the shared `asGroupListPresence`, whose `definitelyFilled` needs only *some* visible content, and a conformance case locked `.value 1` over a partly covered filled group with the comment that it matches `groupFilled`. It does not. The count now reads its own `asFilledGroupCountPresence`, which demands `fullyRelevant` and otherwise answers unavailable; the predicates and quantifiers keep the shared projection, which this batch measured and confirmed. The wrong case had said in its own words that the coverage dimension was carried by uniformity rather than measured ([`LF116`](../LEAN-FINDINGS.md)).
- `limit`: nonrepeatable groups only, one nesting level, at most two fields per group, `en_US`, one document shape per claim. Not measured: partial coverage over a repeatable or starred operand, a coverage naming a group rather than a field, the computation arm, and whether a covered-but-required-empty cell behaves as covered content.
- `bytes`: [`partial-coverage-group-operands/`](../../evidence/kernel-30.8.1/captures/partial-coverage-group-operands/); SHA-256 model `0dc0609f90dde99f5d011d8de3d1915d07723de174ebc3f9e1f270ac3fcb91a4`, request `d272d5d71f2c1cf5cd95a4826cc83984b1b66ea1ff6a0b39434ca55f325a7de8`, artifact `60fdda76528620ee5023437df4cbb35f15196982327cc32174e8c063347df0de`; scope request `264ec048524d1c867cbee1a85c31c0f2d18098517e309e9d98f885124d3f9a00`, scope artifact `f5ae7f7bcb7883cd1324296ffafcb719c11ee71df2f58b6fa564c8e53b679a96`; quantifier request `1b28c936e6c4ca6a53290b79d304d6bb9b309f9dd6f88283129bdc84de985de1`, quantifier artifact `69073511c0ce04ee78334d0b362f4223034a9099cca6f384288c5f4d516f328f`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: behavioral and locally originated. Outbound as [`SPEC-2026-08-31-18`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-18).

<a id="src-starred-group-quantifier-capacity"></a>
#### The threshold quantifiers read the same in-capacity extent the count does, and an instantiated row is filled, measured locally 2026-08-31

- `revision`: two `:adapter:kernelProbe` requests at a12-dmkits `8cad1224f86e91c46f0db3573adb4f0fe894a959`, the sibling checkout clean before and after and `producer.source.state: CLEAN` in both artifacts. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on all nine rows.
- `question`: [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) carried one open item on the declared-capacity dimension — `NoGroupFilled` and `AtLeastOneGroupFilled` at an over-capacity threshold. The three-quantifier half of that row was already deleted as a false gap because those operators reject a starred operand outright.
- `model`: `Fill_DM` with a `max 3` repeatable `/Fill/Shell/Rows` holding one String `A`, a live control field `W`, and eight rules over `Rows*` — the two threshold quantifiers and a `NumberOfFilledGroups == n` ladder for `n` in 0 to 4, each guarded by `FieldFilled(/Fill/W)` so the iteration gate admits them.
- `claim`: **the threshold quantifiers read the in-capacity extent, exactly as the count does.** At four instantiated rows over a cap of three, `AtLeastOneGroupFilled` fires `VALUE_ERROR`, `NoGroupFilled` stays silent, and the ladder's firing member is `== 3` rather than `== 4`, beside `zuGrosseZeile` and `zuGrosseKontextnummer` for the excess row. At zero rows `NoGroupFilled` fires `OMISSION_ERROR` and the ladder answers `== 0`.
- `claim`: **an instantiated row is a filled group whatever its cells hold**, which the four-row document exposed and a second request separated. Three rows with one filled cell and two empty answer `3`; three rows whose every cell is `null` answer `3`; three rows written as bare `{}` objects answer `3`; one filled row answers `1`. So the count is the number of in-capacity instantiated rows and no cell is read.
- `separator`: the one-filled-two-empty document is the discriminating one. A content-reading account predicts `1` there and an instantiation-reading account predicts `3`; every other document in the batch answers alike under both.
- `local-consequence`: **none — this confirms the shipped account.** `ResolvedGroupPresenceInput.hasInstantiatedRow` already includes the concrete group row itself when repeatable, and [`StarGroupElaboration.lean`](../../A12Kernel/Conformance/StarGroupElaboration.lean) already locks a created-but-empty terminal row as structural content with both quantifier outcomes. What changes is assurance: that rule was internally reasoned and is now Kernel-measured on a fresh model at four row counts.
- `limit`: one repetition level, capacity 3, a single String field per row, `en_US`, `validateFull` only. Not measured: a formally invalid cell in an otherwise empty row, a second repetition level, a filtered star, and the same shapes on the computation arm.
- `bytes`: [`starred-group-quantifier-capacity/`](../../evidence/kernel-30.8.1/captures/starred-group-quantifier-capacity/); SHA-256 model `cdbaeec6f4bc0b8195141c8c62d325420b8145f33a37cac54c69c081aa372ab5`, request `c0f72f00c3135c622ca8b2b7de450aa9a2cb34e9234151a607702bfb911ca8e9`, artifact `cfedda51aa75c34f06656b89c77875177b4f319ad082597205591b554cc7ff60`; content request `0ffe45bf4f483408d55d0e6fa0afef53b49a86749e4abdaf71c84bba44d86372`, content artifact `e97a606825d1f1dbd26e8eaaffba9851f8506ff7a5dab04dea35b148b1c0b6cc`. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: behavioral and locally originated. [`spec/02`](../../spec/02-logic-and-formal-errors.md)'s starred-group bullet said *each instantiated row*, which read alone admits an over-limit one; it now says **in-capacity**, measured on these two operators rather than carried from [`SPEC-2026-08-31-10`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-10)'s carriers. Outbound as [`SPEC-2026-08-31-17`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-17).

<a id="src-filled-field-count-deep-capacity"></a>
#### Slot capacity compounds through nested repetition levels and stops at the operand group, measured locally 2026-08-31

- `revision`: two `:adapter:kernelProbe` requests at a12-dmkits `a8bd8e86450b1629c08449e33116f84eef029859`, clean before and after with `state: CLEAN` in both artifacts. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on every row.
- `question`: the [nested-capacity checkpoint](#src-filled-field-count-nested-capacity) measured one nesting level and named two untested shapes at its own `limit` — two nesting levels, and an operand group that is itself under a repeatable ancestor. The Lean clause answers both already, by folding a product over each declaration's repeatable scope after dropping the levels the operand group's own ancestry covers. Both answers were carried across a boundary the measurement had never crossed, which is the shape [`LF116`](../LEAN-FINDINGS.md) is about.
- `oracle`: polarity is a direct extent oracle here and needs no threshold reasoning. A fired count rule types `OMISSION_ERROR` while the operand can still move and `VALUE_ERROR` once it cannot, independently of the literal it is compared against — `< 5` types `OMISSION` at three of four filled cells even though no legal fill can falsify it. `fillToFix` shows the same thing directly: it holds the operand's whole declared extent while any slot is free and is exactly `[]` at capacity.
- `depth`: **capacity compounds.** `FillDeep_DM` puts a `max 2` `Rows` under `/Probe/Flat` and a `max 2` `Inner` under that, one field at each of the three levels, so the compounding reading is `1 + 2 * (1 + 2 * 1) = 7`. Six documents walk the count 0, 3, 4, 5, 6, 7 against `NumberOfFilledFields(Flat) < 8`; only the seven-cell document types `VALUE_ERROR`. Model SHA-256 `7dbc1a1a5fb83b9f7b509bb9ee820cdb98ff4a9afb5cce2352cf448767b80740`.
- `separator`: the walk refutes three rival readings at distinct rows. Summing each level once bounds at five and would type the five- and six-cell documents `VALUE`; the declaration count bounds at three and would type every document from three on; an extent counting **instantiated** rather than declared rows would type every one of these documents `VALUE`, since each is saturated in the rows it instantiates. All five below-capacity documents are measured `OMISSION`.
- `ancestor`: **the extent stops at the operand group and does not multiply by ancestors above it.** `FillAnc_DM` puts `/Probe/Outer/Flat` under a `max 2` repeatable `Outer`, with a `max 2` `Rows` under `Flat`, so the per-enclosing-row reading is `1 + 2 = 3` and an aggregate-over-`Outer` reading would be `6`. An asymmetric document — `Outer[1]` at three cells, `Outer[2]` at one — draws **two messages of different types**, `VALUE` on the first row and `OMISSION` on the second, which no aggregate reading produces. A single saturated `Outer` row alone types `VALUE`, where an aggregate keyed on the declared maximum would still be growing. Model SHA-256 `4f38565984fc4b9e412b76d961b115d72357eb0f2bc8f2374322a077cd3e1d99`.
- `local-consequence`: both answers confirm the shipped clause rather than correcting it, so `declarationSlots?` keeps its product fold and its `drop enclosing`. The depth arm gains its own separating case; the ancestor arm cannot, because this project **refuses that operand shape at elaboration** — see the `narrower` row.
- `narrower`: `FlatModel.resolveFixedGroupReference` refuses any fixed group operand whose repeatable scope is nonempty, `repeatableGroupRequiresAddress`, so the measured `NumberOfFilledFields(Flat)` under an iterating rule group does not elaborate here. The Kernel admits it and binds the operand's ancestry from the rule's own iteration. The gate is **shared** by the group-list, numeric-computation, numeric-validation, and validation-condition carriers, and this round measured exactly one of them, so widening it is a claim about four carriers backed by one observation. Recorded as a gap rather than taken.
- `authoring`: a count-below-threshold condition inside an iteration is refused `MVK_NEG_CONDITION_IN_ITERATION`, because it fires on an unfilled repetition; guarding it with `FieldFilled` on a field of the same row admits it. Declaring the rule group as the **root** does not avoid the gate — the Kernel derives the iteration from the referenced group, and the same condition spelled with an absolute operand path at `/Probe` draws the same refusal. This project already owns the gate and its identity; what is new is that the rule group is not what selects it.
- `coordinates`: **this capture is also the witness [`spec/01`](../../spec/01-data-model.md) recorded as missing, re-read locally 2026-08-31 with no new run.** That clause gave one depth rule for a group operand's expansion — a repeatable level *above* it stays concrete at the firing row, every level *at or below* it is wildcarded — and said the concrete half had no observation behind it. The asymmetric document fires twice and each firing's expansion carries its own ancestor row, `/Probe[1]/Outer[1]/Flat[1]/Rows[0]/RowR` beside `/Probe[1]/Outer[2]/Flat[1]/Rows[0]/RowR`, on both strategies. The `[2]` is what makes it a witness rather than a coincidence: the nonrepeatable `Probe` and `Flat` both carry `[1]`, so only a non-one coordinate separates a tracked row from that constant. The starred form's concrete half stays unwitnessed — one carrier, not the family ([`LF116`](../LEAN-FINDINGS.md)).
- `settler-was-here-already`: this checkpoint read those exact bytes for the `dialects` row and recorded nothing about their coordinates, while the clause elsewhere was recording the coordinate question as unanswerable. [`LF121`](../LEAN-FINDINGS.md) at close range again, and [`LF128`](../LEAN-FINDINGS.md)'s corpus test would have caught it: *no observation places…* quantifies over a finite retained set and was never enumerated.
- `dialects`: one message carries **both** address dialects at once, which the [dialect checkpoint](temporal-and-message-probes.md#src-message-address-dialects) had inferred across separate captures. The ancestor row's `OMISSION` message names its error field `Probe/Outer[2]/Flat/FlatA`, eliding index one on the ancestors it does not vary, while its own `fillToFix` reads `/Probe[1]/Outer[2]/Flat[1]/Rows[0]/RowR`, eliding nothing and marking the iterated level `[0]`. The deep model's `fillToFix` marks **two** levels `[0]` in one path.
- `limit`: two nesting levels, bounded repeatabilities of 2, one field per level, ordinary String fields, `en_US`, one repeatable ancestor above the operand group. Not measured: three nesting levels, unequal repeatabilities across levels, a formally invalid cell inside the deep subtree, and the same ancestor shape on any carrier other than `NumberOfFilledFields`.
- `bytes`: [`filled-field-count-deep-capacity/`](../../evidence/kernel-30.8.1/captures/filled-field-count-deep-capacity/); SHA-256 depth request `d5d3b9d6ad063cfba7d4090812a45721bc4a670f29e197621bb3151dc30132d7`, depth artifact `b0594dbec7885558e493d05d9014ef6060d403fe274fd96c810396e77f63b181`; ancestor request `8b1a33bd542a21814dd3b352bf1d5f4d76c055b2af52be4bf9cb922fd114aa4c`, ancestor artifact `d4cb98114e8bfb2dc97f15ccfbb66cec43491274e27d9847020e390b707a0e6d`. Bounded raw calibration captures with no typed consumer: they do not run under `lake test` and establish neither `L` nor `C` coverage.
- `sync`: the depth arm confirms an existing clause and changes no `spec/` rule. The ancestor arm describes a shape `spec/` does not name — a group operand bound by its rule's own iteration — and its polarity follows [`spec/10`](../../spec/10-validation-and-polarity.md)'s existing directional-fillability rule applied per enclosing row. Neither is behavioral. The later `coordinates` re-read is: it retires a recorded confidence limit in [`spec/01`](../../spec/01-data-model.md), which carries the same obligation as changing a rule, and leaves as [`SPEC-2026-08-31-15`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-15).

<a id="src-group-count-list-extent"></a>
#### A group-count list's extent is its operand list, and a starred member breaks that, measured locally 2026-08-31

- `revision`: two `:adapter:kernelProbe` requests at a12-dmkits `1be2d603c90be578fe7ea0de0b8c515cb2a479c4`, both clean before and after with `state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on every row of both.
- `question`: the [nested-capacity checkpoint](#src-filled-field-count-nested-capacity) corrected the *field* count's extent from declaration count to slot capacity, and named the group count as the untested neighbour. Its extent is the operand-list length, a different quantity. Reusing the capacity mechanism there is a claim, not an inheritance, so the question is whether a listed group can also contribute more than one.
- `confirmed`: **it cannot, and the list length is right.** `GrpFixed_DM` lists two nonrepeatable groups, `NumberOfFilledGroups(G1, G2)`, where `G2` owns a `max 3` repeatable `Sub`. With `G1` filled and `G2` filled **only through one instantiated `Sub` row** — its own direct field `B` empty — the count is 2 and the fired `< 3` rule types **`VALUE_ERROR`**. Three `Sub` rows give the same count and the same type, as does filling `B` directly instead. A listed group therefore contributes at most one however many descendant rows it holds, and it counts as filled through a descendant row. Model SHA-256 `cf40d5a26a3f1eb85a9abd6dcb2a4cb80bfa0f02980de5e74d4f1e2c000b5112`.
- `local-consequence`: the group count keeps `groups.length` and must **not** acquire the field count's slot capacity. The two extents now differ deliberately, which is exactly the kind of neighbouring difference a later cleanup flattens; the site carries a note saying so.
- `gap`: the **mixed** list is a different matter and is authorable. `NumberOfFilledGroups(G1, Rows*)` is admitted by the Kernel — the bare `NumberOfFilledGroups(G1, Rows)` is refused `MVK_NO_WILDCARD`, so the star is mandatory rather than optional — and a starred member counts **per instantiated filled row**: with `Rows` at `max 3`, `G1` filled beside one, two, and three filled rows counts 2, 3, and 4. Every below-capacity firing types `OMISSION_ERROR` and only the count-4 document types `VALUE_ERROR`, so the extent is the sum of each member's own capacity, `1 + 3`. Model SHA-256 `cd7f0ed2b0b87a38fdc66aafe178c236b382cf97347c5320318cf540ea998fc1`.
- `unimplemented`: this project refuses that shape at elaboration with `starredGroupCountOperand`, so the measurement corrects no clause here — it sizes one that does not exist yet and hands a future capsule its extent rule already measured. Record it as a gap rather than reading the refusal as a semantic claim: the Kernel admits the shape.
- `instantiated-not-filled`: a third request separates the two readings the rows above confound, because every document there filled every row it instantiated. A starred member contributes its **instantiated** row count, not its filled one: `G1` filled beside two rows of which only one carries a value counts 3, and `G1` beside a single wholly empty row counts 2. The operator is named `NumberOfFilledGroups` and an empty instantiated row still counts, which is the trap. This is the same quantity the sole-star carrier already computes as `inCapacityRowCount`, so the mixed list needs no new counting clause. Request SHA-256 `84887431e10960a32bfc9dd6ed7b7148de89810dd683e49d5cb518883174b440`, artifact SHA-256 `ddd58d18bba100fe2807591f937bc2a62349d1db1736ab1aa1ebfde2d56a419a`.
- `two-star`: **two stars sum per member and their capacities sum too.** A `max 3` beside a `max 2` counts three at two-plus-one rows and types `VALUE_ERROR` only at five. The **same** star twice double-counts rather than collapsing to one group: two rows count four and the boundary sits at six, so both the fold and the extent are per position. Model SHA-256 `25bd795dee0d17b6eefc6d26f9e5ec2208ecb79e1ede05ce85cfe67cfa1d2793`, request SHA-256 `4e3761431917c4aee1e54820f9fbdd47eb239fe701e82c9e0483deccf94d3221`, artifact SHA-256 `57ec696462e91df311186db0fc59e8cbf43de7eee449590eb183a1501df18326`.
- `arity-and-root`: a fourth request on a **two-root** model closes the remaining static items. Arity is a minimum of two and not a pair: fixed lists of four and five operands and mixed lists of three and four are all admitted. A **root** operand beside a *disjoint* one draws `MVK_ROOT_GROUP_WITH_OTHER_PARAMETERS` whether the other member is starred or fixed, whichever operand carries the root, against a live control — a nonroot group beside another root's star — that is admitted, so the gate is not simply refusing every cross-root list.
- `gate-order`: **containment is checked before rootness, and that order is measured on the mixed carrier rather than carried over from the fixed one.** A root beside its own descendant draws `MVK_DUPLICATE_PARAM2` in either operand order, while the disjoint control keeps the root class. This project had the two gates inverted on the mixed carrier only — the fixed-only carrier already ordered them correctly — so it reported a root the author must remove where the Kernel reports an overlap they must resolve. Corrected in the same change. Model SHA-256 `8f0d6ab99a874f1a5a1bc451f5b2ea807b91f49c69f0d3ab26f2f4e16a9a8188`.
- `duplicate-gate`: the duplicate rule **does** reach a mixed pair, which this project had left ungated on the ground that it was unmeasured. **Strict containment between any two members is refused `MVK_DUPLICATE_PARAM2`**, whichever side carries the star and in whichever order: a fixed group beside a star strictly inside it, and a star beside a star strictly inside it, both refuse. Two **fixed** members naming the same group keep the distinct `MVK_DUPLICATE_PARAM1`. Equal paths where both sides are starred are **admitted**, which is the exception that makes this a containment rule rather than a distinctness rule. Disjoint members admit, mixed or not, and a three-member mixed list admits.
- `limit`: one repeatable member, one nesting level, bounded repeatability 3, one and two direct fields, `en_US`. The starred member owning a further repeatable descendant and an unavailable member's interaction with the sum were open here and are now measured at the [unavailability checkpoint](#src-group-count-unavailability). The computation arm is measured at the [static-gate checkpoint](#src-group-count-static-gates-both-arms) and draws this same table, and an unretained repeatability is closed as unreachable at the [declaration-domain checkpoint](#src-repeatability-declaration-domain).
- `bytes`: [`group-count-list-extent/`](../../evidence/kernel-30.8.1/captures/group-count-list-extent/); SHA-256 fixed request `aa503f0d13c86baeb3ec879e5929c20ec55805c1195d195de916a26b2d09bec4`, fixed artifact `94b1129aa2794bc00b79476add04d198382ef2788dab9ef142549b187fe81346`; mixed request `fe3efc0d1278abbd6e1d68792daf658b77660d441544bb96cb97b380f2f0d612`, mixed artifact `b236da44ecc7aa2951d03c9177b350825d19947b9a2bb7cd4307162343355745`. Bounded raw calibration captures with no typed consumer: they do not run under `lake test` and establish neither `L` nor `C` coverage.
- `sync`: the confirmation changes nothing, and the mixed-list rows describe a shape `spec/` does not yet cover but whose polarity follows [`spec/10`](../../spec/10-validation-and-polarity.md)'s existing directional-fillability rule. No behavioral `spec/` change, so no outbound entry follows.

<a id="src-group-count-unavailability"></a>
#### A group error makes a filled-group count unknown only when that group has no admitted content, measured locally 2026-08-31

- `revision`: three `:adapter:kernelProbe` requests over one model at a12-dmkits `1be2d603c90be578fe7ea0de0b8c515cb2a479c4`, the sibling checkout clean before and after each with `state: CLEAN` in every artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies, `enginesAgree: true` on all twelve rows.
- `question`: this project's `numberOfFilledGroups` made the count unknown on **any** erroneous operand, and `spec/02` said so in as many words. Its neighbour `GroupFilled` was measured long ago to fire positively on a group holding admitted content beside a formal error. Two clauses over one state disagreeing about the same error is a discriminator, so the question is which one the Kernel keeps.
- `confirmed`: **the count agrees with `GroupFilled`, and the old clause was wrong.** `Unavail_DM` declares nonrepeatable `G1` (String `A`, Number `Num`) and `G2` (String `B`, Number `Num2`) beside a `max 3` repeatable `Rows`, with rules `NumberOfFilledGroups(G1, G2) < 1/2/3`. Against `G2` filled: `G1` holding `A="a"` beside a malformed `Num` still counts 2 and fires `F3` `VALUE_ERROR`, exactly as the clean control does; `G1` holding *only* the malformed `Num` fires **nothing at all**, so the count went unknown rather than dropping to 1. An error therefore blocks only the negative answer, and admitted content outranks it.
- `isolation`: the same pair repeats on the **second** operand — a malformed `Num2` beside a filled `B` keeps `F3` firing, a malformed `Num2` alone silences it — so neither operand position nor evaluation order is the cause. A malformed cell in the unreferenced `Rows` leaves `F3` firing, so a document-global error is not the cause either. The three neighbours the account had to beat are each refuted by their own row.
- `mixed-carrier`: the same two legs hold for `NumberOfFilledGroups(G1, Rows*)`. With `G1` filled beside a malformed sibling, the ladder fires unchanged at count 3; with `G1` holding only the malformed cell, the whole ladder goes silent. So the widened carrier inherits the shared rule rather than needing one of its own.
- `star-rows`: a malformed cell **inside** a starred row leaves that row counted and the ladder unchanged, and the unreferenced-error row above independently shows a row whose only non-empty cell is malformed still contributing. A star has no unknown of its own, which this project had recorded as a structural consequence and is now measured.
- `row-domains`: two items the [list-extent checkpoint](#src-group-count-list-extent) listed as unmeasured close here. A starred member owning its own `max 2` repeatable `Inner` counts **identically** to the baseline, so a star's rows are not multiplied by an instantiated repeatable descendant — the opposite of `NumberOfFilledFields`, whose capacity does multiply through. And a starred member's capacity is its own row maximum: three rows count 4 against extent `1 + 3`, typing `VALUE_ERROR` at `< 5` with `< 4` silent.
- `local-consequence`: `numberOfFilledGroups` now reads each operand through `asGroupListPresence`, the decided-presence projection its own group-list predicates already used, instead of re-deriving an unknown condition beside it. The coverage dimension follows that projection rather than a measurement — visible content decides a partly covered group, invisible absence does not — and `validateFull` cannot observe partial coverage, so that leg is stated as a consequence of the shared mechanism and not as a kernel row.
- `structural-error`: **the rule holds for a third kind of error, on its own model.** `StructErr_DM` gives `G1` a `max 2` repeatable `Sub` and instantiates three rows, so the group carries an over-limit *structural* error rather than a cell finding. `NumberOfFilledGroups(G1, G2) < 3` fires `VALUE_ERROR` at count 2 alongside `zuGrosseZeile` and `zuGrosseKontextnummer`, both with `G1`'s direct field filled and with it empty, against an at-capacity control that fires identically. A structural error therefore never makes the fixed count unknown either.
- `structural-error-limit`: those rows do **not** establish that an over-limit row itself supplies content. Rows are dense from index 1, so a `max 2` group holding three rows still has two in-capacity ones, and the content could come from either. Separating them needs a row set whose every index exceeds capacity, which [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) already records as unreachable. The claim is exactly: a structurally erroneous group whose content is admitted still counts.
- `limit`: nonrepeatable operands with one malformed Number descendant, one repeatable member at bounded repeatability 3 with one nesting level, one over-limit margin, `en_US`. Not measured: a call-local partial-coverage error, `noneRelevant` coverage, and the computation arm — which counts a malformed-only group as present and so cannot restate this rule.
- `bytes`: [`group-count-unavailability/`](../../evidence/kernel-30.8.1/captures/group-count-unavailability/), SHA-256 model `20c20034a966fccf4f7fc27dc7311fe1d579106e15ec317c5fd9f355b583665d`. Fixed request `9ed1ecf9fdfebe9921ec6a05d9b2f004c37a3793f8ce5179dec7cab40491ef2a`, fixed artifact `d57049a908dd4256d9a33138ca2efd0bdc45b5884bef9aa090b9410f1b586033`. Mixed request `001df382461f116792bdf3e6de339af86894f7d5a75705278824fb5f8d736789`, mixed artifact `5ebc7d7b3bce3d2baa2542dd0d5e6a2129ca66367e10444540f77c1077731972`.
- `bytes`: isolation request `c93033ef2d507b029583e15dc776e97ec2aa707be0c80d54dc48ed40a8e7f223`, isolation artifact `0179d5a295e2b7a80002f045b23fdb2d123f2ff8ae6020e9c9a1f834ba2db43c`. Structural-error model `69fa1b6e8a25b110fbc777adfd0a1e880dc151b3d9fbe524a7764560dade2fb9`, its request `116c09941d619046eec7a119f94590b05cf14d8f451e898d9def032252236861` and artifact `5179c11aa71acedad424ad02ea9d1598614499b29a8e8b7f5fe03cfe568c6f63`. Bounded raw calibration captures with no typed consumer: they do not run under `lake test` and establish neither `L` nor `C` coverage.
- `sync`: behavioral. [`spec/02`](../../spec/02-logic-and-formal-errors.md) stated the refuted rule in its numeric-count paragraph and again in its checklist, and both are corrected in this change. Outbound entry [`SPEC-2026-08-31-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-01).


<a id="src-group-count-static-gates-both-arms"></a>
#### The group-count static gate table is the same on both arms, measured locally 2026-08-31

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1be2d603c90be578fe7ea0de0b8c515cb2a479c4`, clean before and after, Kernel `30.8.1` built and runtime, catalog verified against 30.8.1. Twenty-six `computation add --dry-run` invocations over one two-root model, each returning either `verification: KERNEL_CONFIRMED` or a single diagnostic code. `--dry-run` writes nothing, so every row sees a computation-free model and no row is pre-empted by `RK_NAME_EXISTS`.
- `question`: the [list-extent checkpoint](#src-group-count-list-extent) measured containment, rootness, and their order on the **validation** arm and named the computation arm as unmeasured. This project's computation arm gated overlap over the fixed operands only and carried no root gate at all, on the recorded ground that a mixed pair's duplicate rule was unmeasured. Reusing the validation arm's gates there is a claim, not an inheritance ([`LF116`](../LEAN-FINDINGS.md#lf116--reusing-a-mechanism-inherits-its-evidences-scope-not-the-mechanisms-generality)), and the arm boundary is known to be real elsewhere — the same operator inverts on formal invalidity — so the boundary had to be crossed by measurement.
- `claim`: **every gate and its order is arm-independent**, and each class below reproduces on this arm exactly as the validation arm measured it.
- `duplicate-class`: equal fixed paths draw `MVK_DUPLICATE_PARAM1`. Strict containment between any two members draws `MVK_DUPLICATE_PARAM2`, whichever side carries the star and in whichever authored order. Equal paths where **both** sides are starred are admitted, which is the exception that makes this a containment rule rather than a distinctness rule.
- `root-class`: a root operand beside a disjoint one draws `MVK_ROOT_GROUP_WITH_OTHER_PARAMETERS` in either order, with the root fixed or starred and the other member fixed or starred. A root beside its **own** descendant draws the containment class instead, so containment is checked first here exactly as there.
- `wildcard-class`: a bare repeatable operand draws `MVK_NO_WILDCARD` and a starred nonrepeatable one draws `MVK_INVALID_WILDCARD`. A lone fixed operand draws `MVK_PARAMSIZE_INVALIDGN`.
- `arity`: a minimum of two rather than a pair, on this arm too. Fixed lists of four and five operands are admitted, as are mixed lists of three and four.
- `falsified`: the ancestor-beside-descendant-star pair, which this project answered at runtime — counting twice, with no overlap gate and no deduplication — **never reaches runtime**. `NumberOfFilledGroups(/Probe/Shell, /Probe/Shell/SRows*)` is refused `MVK_DUPLICATE_PARAM2` in either order, on a pure shell owning no direct field exactly as on a group owning one, against a live disjoint control admitted in the same run. The two conformance cases locking the counting answer were deleted rather than corrected, because the shape they counted is unauthorable.
- `mechanism`: this is one gate pair applied at one place, not two arm-specific rules. The defect was that this project had implemented it twice and diverged: the validation arm reached the shared containment predicate over every member while the computation arm filtered to the fixed ones and skipped rootness entirely. Both arms now call the same predicate in the same order.
- `separator`: every refusing row is paired with an admitted one that differs in exactly the named dimension — the disjoint mixed pair against the containment pair, the nonroot-beside-another-root's-star control against the root pair, the equal-starred pair against the equal-fixed pair. Without those the table would be consistent with a carrier that simply refuses more.
- `limit`: `NumberOfFilledGroups` only, one nesting level for the fixed⊃star pair and two for the star⊃star pair, `en_US`, one declaring group. Not measured here: the same table under a repeatable declaring group and a filtered star. An unretained repeatability is closed as unreachable at the [declaration-domain checkpoint](#src-repeatability-declaration-domain).
- `bytes`: [`group-count-list-extent/CompGate_DM.json`](../../evidence/kernel-30.8.1/captures/group-count-list-extent/CompGate_DM.json), SHA-256 `045acbead5f08f8a1e8f56e9e1336023f09e6e577a9de9cef20823dc9e5399b6`. A static authoring route with no runtime artifact: the rows are `dmtool` outcomes rather than kernel observations, and they establish neither `L` nor `C` coverage.
- `sync`: behavioral. [`spec/02`](../../spec/02-logic-and-formal-errors.md) asserted that duplicate checking skips wildcarded operands, which the containment rows refute, and its static list carried no root class and no arm statement.


<a id="src-group-count-gates-repeatable-scope"></a>
#### The group-count gate table survives a repeatable declaring group, and a filter refuses a group operand, measured locally 2026-08-31

- `revision`: `dmtool` 0.13.0 at a12-dmkits `a8bd8e86450b1629c08449e33116f84eef029859`, clean before and after, Kernel `30.8.1` built and runtime. Twenty-three static invocations over one model — `computation add --dry-run` for the computation arm and `rule check` for the validation arm — each returning `verification: KERNEL_CONFIRMED` or a single diagnostic code. Both verbs write nothing, so every row sees the same rule- and computation-free model.
- `question`: the [static-gate checkpoint](#src-group-count-static-gates-both-arms) measured the whole table under one **nonrepeatable** declaring group and named two untested dimensions at its own `limit` — the same table under a repeatable declaring group, and a filtered star. Relative operand names resolve against the declaring group, so a repeatable one is a different resolution path, not the same one with a flag set.
- `model`: `GateRep_DM`, a `max 3` repeatable `/Probe/Cycle` declaring group holding the Number target `Amount`, a nonrepeatable `G1` owning a `max 2` repeatable `Sub`, a nonrepeatable `G2`, and a nonrepeatable `/Probe/Out` outside the declaring group. Model SHA-256 `b7323ec78514a5c60e795c6b116543ddc17f9364c32392b945c2f78a32c45810`, `model check` valid.
- `claim`: **every class reproduces unchanged under a repeatable declaring group, on both arms.**
- `duplicate-class`: equal fixed paths draw `MVK_DUPLICATE_PARAM1`, and strict containment draws `MVK_DUPLICATE_PARAM2` whichever side carries the star. Equal starred paths stay admitted, so the rule is containment rather than distinctness here too.
- `wildcard-class`: a bare repeatable operand draws `MVK_NO_WILDCARD` and a starred nonrepeatable one draws `MVK_INVALID_WILDCARD`.
- `arity-class`: a lone operand draws `MVK_PARAMSIZE_INVALIDGN` and a three-member list is admitted.
- `rule-group-operand`: `RuleGroup` names the repeatable declaring group and takes the ordinary containment class beside its own descendant, `MVK_DUPLICATE_PARAM2`, on both arms. The declaring group is an ordinary member of the overlap relation rather than a privileged one.
- `outside-operand`: an operand **outside** the declaring group is admitted beside one inside it, on both arms. Nothing requires a group-count member to lie within the iteration; what its value does per row is a runtime question this static round does not reach.
- `filter`: **a `Having` filter refuses a group operand, and the kernel names the filter as the site.** `NumberOfFilledGroups(G1/Sub* Having (…), G2)` draws `MVK_NO_GROUPS_ALLOWED` reading *At 'Having': It is not allowed to use a group*, on both arms and in either operand position. There is no operator-level spelling either: a trailing `Having` after the operand list is a parse error, `MVK_UNEXPECTED_TOKEN`. So the two spellings that could express a filtered group operand are both refused at this revision.
- `filter-closed`: the local `limit` on this row is closed by a12-dmkits `539abf35d`, reviewed: **six** carriers refuse, each against its own unfiltered control, and the post-list spelling is a parse error rather than a second refused site. That round also supplied a **layer** this measurement did not reach — the refusal is drawn at static arbitration and not only at code generation — and the limit that each measured filter reads its own group's field, a different group's field being a separate unmeasured question. Recorded at [`SPEC-2026-08-31-09`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-09).
- `separator`: the filter itself is not what refuses. The same filter over a **field** list, `NumberOfFilledFields(G1/Sub*/S Having (…))`, is admitted on both arms in the same run, and the unfiltered group operand `NumberOfFilledFields(G1/Sub*)` is admitted too. Only the pairing of a group path with a filter site refuses, which is what the kernel's own text says.
- `confound`: three validation rows first drew unrelated gates and were re-run guarded. `MVK_ERROR_FIELD_NOT_REFERENCED` needs an error field the condition actually names, and `MVK_NEG_CONDITION_IN_ITERATION` pre-empts the wildcard classes because the count condition can fire on an unfilled repetition; a `FieldFilled` conjunct on a field of the same row clears it. The computation arm has neither gate, which is why it carries the unguarded table.
- `unimplemented`: this project cannot express any row of this table. `resolveFixedGroupReference` refuses a fixed group operand whose repeatable scope is nonempty, and under a repeatable declaring group every operand inside it has one. The rows therefore correct no clause here; they hand a future capsule the gate table already measured, exactly as the [list-extent checkpoint](#src-group-count-list-extent) did for the mixed list. The filter row is different: a filtered group operand has no surface representation here either, and the measurement says the Kernel refuses it too, so that agreement is now measured rather than assumed.
- `limit`: `NumberOfFilledGroups` and `NumberOfFilledFields` only, one repeatable declaring group at `max 3`, one nesting level below the operand, `en_US`, one root. The **root** class is not measured under a repeatable declaring group: reaching it needs a second root group, which `dmtool model new` does not create. Runtime behaviour of an outside operand per row is not measured.
- `bytes`: [`group-count-gates-repeatable-scope/GateRep_DM.json`](../../evidence/kernel-30.8.1/captures/group-count-gates-repeatable-scope/GateRep_DM.json). A static authoring route with no runtime artifact: the rows are `dmtool` outcomes rather than kernel observations, and they establish neither `L` nor `C` coverage.
- `sync`: the table rows confirm the account [`spec/02`](../../spec/02-logic-and-formal-errors.md) already carries after its last correction, at a declaring-group repeatability it did not name. The filter row states something `spec/` does not: that a group operand is refused at a `Having` site. Outbound as [`SPEC-2026-08-31-09`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-09).

<a id="src-repeatability-declaration-domain"></a>
#### A repeatable group always carries a finite maximum, measured locally 2026-08-31

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1be2d603c90be578fe7ea0de0b8c515cb2a479c4`, clean before and after, Kernel `30.8.1` built and runtime. Four Kernel-gated `model check` runs plus two `computation add --dry-run` runs over one model.
- `question`: three checkpoints and two gaps carried "still not measured: an unretained repeatability" — the `none` branch of this project's `Option Nat` row maximum, which several clauses answer by suppressing an extent rather than inventing a movement rule. Whether any legal model reaches that branch is a **reachability** claim, which reading never discharges ([`CLAUDE.md`](../../CLAUDE.md)); `dmtool` exposes no route to declare it, so the model-check exception was used instead.
- `claim`: **the branch is unreachable in a Kernel-valid model.** `repeatability` present and finite is exactly what makes a group a repetition list. Removing the key leaves a valid model in which the group is **nonrepeatable**: the Kernel then refuses `NumberOfFilledGroups(G1, Rows*)` with `MVK_INVALID_WILDCARD` and admits the bare `NumberOfFilledGroups(G1, Rows)`, which is the reverse of both answers on the same model with the key present.
- `refusals`: an explicitly `null` maximum and a `0` maximum are each refused `MVK_MAX_VALUES_INVALID`, four occurrences per model, naming each affected group by path. Absence means nonrepeatable rather than unbounded.
- `withdrawn`: this row also said `dmtool group add --repeatable` independently requires `≥2`, **and concluded that the declared domain is a finite integer of at least two**. The lower bound is withdrawn: `1` is admitted at both Kernel gates, measured by a12-dmkits at the [inbound domain checkpoint](inbound-group-operand-batches.md#src-repeatability-domain-peer-measurement). The `dmtool` refusal is real and is the instrument's own narrowing, not a Kernel gate; reading a domain off it was the inference recorded as [`LF126`](../LEAN-FINDINGS.md). Nothing else on this checkpoint moves — the two Kernel-measured refusals and the absence reading stand.
- `separator`: the two computation rows are the separator, not the validity verdict. A model that merely stays valid is consistent with the group remaining repeatable-but-unbounded; the wildcard refusal and its bare-form twin are what fix the reading, and both are Kernel diagnostics rather than `dmtool`'s own structural view.
- `local-consequence`: every clause keeping a `none` row maximum stays a total function and gains an annotation rather than a gate: the branch answers, no authorable model reaches it, and no clause may be rewritten to depend on its unreachability.
- `limit`: one model, one nesting level of repeatable groups, `en_US`. The hand-written variants were checked and deleted under the model-check exception, so no artifact is retained and the rows are reproducible only by repeating the procedure on a retained model. Not covered: whether a model arriving through import or type-def inheritance can reach a different spelling.
- `bytes`: none retained by design. The base model is [`group-count-list-extent/CompGate_DM.json`](../../evidence/kernel-30.8.1/captures/group-count-list-extent/CompGate_DM.json), SHA-256 `045acbead5f08f8a1e8f56e9e1336023f09e6e577a9de9cef20823dc9e5399b6`; each variant is that file with one `repeatability` value removed, nulled, or zeroed.
- `sync`: behavioral. [`spec/01`](../../spec/01-data-model.md) implied a present `repeatability` without stating its domain or what absence means.


<a id="src-over-limit-computation-target"></a>
#### An over-limit row receives no computed value, measured locally 2026-08-31

- `revision`: one `:adapter:kernelProbe` request at a12-dmkits `1be2d603c90be578fe7ea0de0b8c515cb2a479c4`, clean before and after, `producer.source.state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on every row.
- `question`: this project's repeatable-computation carriers enumerate their target rows from the checked document's complete physical row list, while the indexed-parallel family's index column skips over-limit rows. The two disagree on exactly that row, and the disagreement is observable: the parallel executor aborts the whole run with a structural `missingTargetIndex` fault on a document that is otherwise valid. Which enumeration is right is a Kernel question, and neither side had been measured.
- `prediction`: the over-repetition family already establishes that an over-limit row's ordinary checks are suppressed across its whole written subtree and that starred numeric domains exclude it, so the row was predicted to receive no computed value. The prediction was recorded before the run and reproduced.
- `claim`: **an over-limit row produces no computed value, and its target is cleared like any other row that produces none.** A repeatable group declared `max 3` carrying three constant computations produces exactly nine outcomes — three rows by three targets — whether the document instantiates three, four, or five rows.
- `correction`: an earlier reading of those rows said the excess row *receives no outcome at all, not a cleared one*. That is false, and it was false because every document behind it left the excess row's target cell empty, where a clear is invisible under the changed-subset projection. **Seed that cell and the Kernel reports it `cleared`** with no value, in the same run where its in-capacity siblings compute a value from a source that is present for both. So the row is excluded from value production, not from the result; a consumer applying the earlier reading would leave a stale value where the Kernel removes one. Caught by measuring the discriminator the record itself listed as open rather than by treating the shorter phrasing as settled ([`LF114`](../LEAN-FINDINGS.md#lf114--both-sides-sampling-one-side-of-an-unnamed-boundary-agree-and-re-derivation-cannot-break-the-tie)).
- `separator`: the at-capacity document is the control that makes the negative readable. It produces the same nine outcomes, so the four- and five-row results cannot be read as a run that stopped early or never reached the target; and the excess rows are demonstrably present, because they draw their own findings in the same run.
- `separator`: for the clear, the control is a **seeded in-capacity** row. It takes the computed value and raises the computation's own mismatch message; the seeded over-limit row takes a clear and raises none. Two seeded rows, one in capacity and one not, in the same model and the same phase.
- `formal-channel`: the excess rows report `zuGrosseZeile` on the row and `zuGrosseKontextnummer` on each of the row's three **written** cells. This model declares exactly those three and every document writes all of them, so the earlier *declared* phrasing was indistinguishable here; the three-level rows of this same capture are the corpus's sharpest separator and are read at the [multiplicity checkpoint](over-repetition-probes.md#src-over-limit-finding-multiplicity)'s `corpus-enumerated` row. Two **sibling** over-limit rows each draw their own `zuGrosseZeile` — eight findings at five rows — which is the disjoint case the absorption rule leaves independent, not a counterexample to it.
- `second-carrier`: **the crossing is discharged on a second carrier and a second topology.** A sibling-star `FirstFilledValue` into a repeatable `Tasks` target under a repeatable `Projects` ancestor, both declared `max 3`, produces three outcomes at three and at four task rows. So the rule is not the constant carrier's, and it holds one repetition level deeper than the first measurement reached.
- `ancestor-row`: **the exclusion reaches through an ancestor.** A fourth `Projects` row carrying a task row of its own adds no outcome, although that task row's own coordinate is in range. The excluded row is therefore decided by the whole environment rather than by the target level's coordinate, which is what a per-level test would get wrong.
- `level-independence`: **a third measurement, on three repetition axes, fixes which level the excess row sits at as irrelevant.** `Milestones`/`Tasks`/`Subtasks` each declared `max 2`, with the computed Date at the deepest level: four documents differing only in whether the outer, middle, or inner level overflows all produce the identical eight-leaf result, the in-capacity two-by-two-by-two cube. The **middle** case is the discriminating one, because the excluded leaf's own coordinate is in range there and only its ancestor's is not.
- `carrier-crossing`: three carriers are measured — a bare constant, a sibling-star `FirstFilledValue`, and a `DateFromDateTime` conversion — across one, two, and three repetition levels. The indexed-parallel plan is now measured on its own at a12-dmkits `7ace4229c` and is no longer part of that set; construction, shift, and enumeration still share the row domain on the **assumption** that the target inventory is one question with one answer. That assumption survives, but weaker than it was: the parallel carrier turned out to be governed by capacity where this record's own reason for excluding it named the execution domain, so the four were not interchangeable after all ([`LF116`](../LEAN-FINDINGS.md#lf116--reusing-a-mechanism-inherits-its-evidences-scope-not-the-mechanisms-generality)).
- `disposition`: the exclusion of the indexed-parallel carrier recorded here was **right about the domain and wrong about the reason**, and [`LF122`](../LEAN-FINDINGS.md#lf122-an-exclusions-stated-reason-is-a-claim-and-a-reason-no-document-could-contradict-is-the-gap-restated) records why the tell was available without measuring: the reason and the outcome it explained agreed on every document this estate could build.
- `local-consequence`: `CheckedDocument.computationRowOutcomes` is the target domain and `actualRowEnvironments` stays the physical one, because validation still reports the excluded row. The domain keeps every physical row in place and splits the treatment: the over-limit row takes the carrier's own no-value outcome, which every application projection turns into the measured clear. `CheckedDocument.inCapacityRowEnvironments` remains only for the indexed-parallel route, whose own index column carries no entry at an over-limit row; that carrier's clear is not measured and is not claimed.
- `two-levels`: two levels overflowing in one document behave as either alone — the same in-capacity cube, with one `zuGrosseZeile` per violated row.
- `limit`: three carriers, one to three repetition levels, bounded repeatabilities of 2 and 3, DATE and DATETIME targets, `en_US`. Not measured: whether the clear reaches a target whose *own* computation is suppressed for a second reason, and whether an over-limit row's clear is emitted by the same mechanism as an in-capacity no-value clear or by a separate path — the observations are identical and do not separate those accounts.
- `bytes`: [`over-limit-computation-target/`](../../evidence/kernel-30.8.1/captures/over-limit-computation-target/); SHA-256 request `69ae07668e769e3863a0f355e4492d187e8976cb7ffeb87e12b882b4290f3bd4`, artifact `d007be9d7b9b0e07d9415b4f6bc9dc22f57eff623f50321bab1f9e82d1cccd5e`.
- `bytes`: seeded request `c205fc9da60f7edc843c1c1921440236cc6e957ee18db66a34f13f39300eee61`, artifact `bc207a797e0aa641ee0353adee843ee5a227959e030b8644a9d75219801e2f83`, over the same three-level model.
- `bytes`: three-level request `5625ab82006c5304d3b88f4496083a059b98df6715c1703a00e7f8b78153da1e`, artifact `6ee7c5af412ebf210f0235d549fc5d267ac7c378e4b875baaef4958e2f7ec464`, model `ThreeLevel_DM.json` `c8ed531d7189096dafca2d07078c80c063b7a57decfdbd6c713e4b64d8a01901` — the retained third-level model with its three capacities lowered to two through `group modify`.
- `bytes`: second carrier request `bee798ab794d7c48e8325a55b0ecb2e335fa1c45a1701683902052ccd2ad8b9c`, artifact `d2dd9a7f306a9a52295c56e220cb5b0517244019a25af3f9ca2b84a61254c8e5`, four inline documents over the retained `DateTimeRows_DM.json` of the [DateTime first-filled capture](../../evidence/kernel-30.8.1/captures/repeatable-datetime-first-filled/).
- `bytes`: documents `d3-capacity.json` `b447d966efaae76e11acd729683cc566469539a11940a4b563c401da3c6a61f3`, `d4-over.json` `f1dcb02e7b0ebb0ac8023beca1d2724baf92fb76bea1c48a6c24c553c5764fe9`, `d5-over.json` `27c5a06aebf59d8d22fe654d571f7f1ef94ef497edaf965bac088adb4d63479e`. The model is the retained `DatConst_DM.json` of the [Date-constant checkpoint](cross-layer-routes.md#src-date-constant-target-formatting).
- `sync`: behavioral. `spec/` stated the over-repetition suppression for the formal channel and for starred numeric domains, but not for a computation's target inventory.

<a id="src-distinct-count-first-operand-class"></a>
#### `NumberOfDifferentValues` classifies by its **first** operand, unlike every sibling gate in the family, measured locally 2026-08-31

- `revision`: `dmtool` 0.13.0 at a12-dmkits `11d9d811657c3562299b463f3dc3ece12142c650`, the sibling checkout clean before the run, Kernel `30.8.1` built and runtime. Thirty-two `rule check` rows on one model, each envelope `verification: KERNEL_CONFIRMED` with `source: KERNEL` and an `MVK_*` code on every refusal. `rule check` persists nothing, so the verdicts are recorded below and the model is the retained byte.
- `question`: [`EXP-2026-08-31-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-31-03)'s document run was blocked on a clean sibling tree, and its model needed a `NumberOfDifferentValues` ladder over a fixed group. That authoring step was refused, which turned a blocked run into a static-legality question the probe route can answer without a document.
- `claim`: **the mismatch code names the class of the operand list's first member, and every later operand must join it.** Number-first draws `MVK_NUMBER_AND_NON_NUMBER`, String-first `MVK_STRING_ENUM_AND_NON_STRING_ENUM`, Date-first `MVK_DATE_AND_NONDATE`. Both orders of every pair are measured — `(Number, String)` against `(String, Number)`, `(Date, String)` against `(String, Date)`, `(Number, Date)` against `(Date, Number)` — and each flips the code while the verdict stays refused.
- `claim`: **position, not offence, selects it.** `(Number, Number, String)`, `(String, String, Number)`, and `(Date, Date, Number)` each report their *first* operand's class rather than the offender's, and `(Number, Date, String)` — two different offenders after an admissible first — still reports the Number class.
- `separator`: **two groups differing in nothing but declaration order flip the code.** A nonrepeatable group declaring a NUMBER then a STRING draws `MVK_NUMBER_AND_NON_NUMBER`; one declaring a STRING then a NUMBER draws `MVK_STRING_ENUM_AND_NON_STRING_ENUM`. So the group operand is expanded in declaration order and the first expanded field is the one that fixes the class — which no homogeneous group and no single mixed group can show.
- `claim`: **a first operand outside the three comparable classes is refused before the homogeneity gate**, with a fourth code naming the admitted set: a BOOLEAN first draws `MVK_ONLY_STRING_ENUM_NUMBER_CMP_DATE_ALLOWED` whether the list is `(Boolean, Boolean)`, `(Boolean, String)`, or `(Boolean, Number)`, while `(Number, Boolean)` draws the ordinary `MVK_NUMBER_AND_NON_NUMBER` — the Boolean is merely "non-number" once an admissible operand leads.
- `contrast`: **the sibling carriers do not behave this way, and the codes are distinguishable.** `FieldValuesNotUnique` refuses every mixed order with one order-independent `MVK_VARYING_TYPES_NOT_ALLOWED` and rejects an all-BOOLEAN list with `MVK_ONLY_STRING_ENUM_NUMBER_DATE_ALLOWED` — the same class-admission code **without** the `CMP` token, so the two gates are separate mechanisms rather than one shared check. `FirstFilledValue` reports the varying-types code on a mixed list like uniqueness does.
- `control`: **the presence quantifiers have no homogeneity gate at all.** `NumberOfFilledFields` and `AllFieldsFilled` admit the mixed group, the explicit mixed list, and the all-BOOLEAN list alike. That is what makes the refusals above a property of reading operand *values* rather than of the operand list, and it is the row that keeps an implementer from gating the whole family.
- `admitted-controls`: the pure-Number group, the pure-String group, `(String, String)`, and `(Date, Date)` are each admitted, so no refusal above is the model failing to load.
- `local-consequence`: **this refuted a Kernel class this project had shipped.** `NumberEntityList.aggregateDiagnostic?` projected `MVK_STRING_ENUM_AND_NON_STRING_ENUM` for a Number entity list whose group expansion contains a String, and its conformance fixture `Probe/A` expands **Number-first**, so the locked class was the wrong one for that very fixture. The claim came from an [inbound batch row](inbound-group-operand-batches.md#src-entity-list-group-gates) measured on a String-first expansion and carried across a boundary nobody had named ([`LF116`](../LEAN-FINDINGS.md)). The projection now claims no class for the distinct count, because the error value carries no first-operand kind and any single class is wrong for some list that reaches it.
- `not-reached`: `FirstFilledValue` over an all-BOOLEAN list reports `MVK_INVALID_COMPARE_TO_YESNO` — the comparison to the String literal refuses first — so that carrier's class-admission gate stays unmeasured.
- `limit`: NUMBER, STRING, DATE, and BOOLEAN operands only, one declared Date format throughout so the format gate never engages, nonrepeatable groups only, `en_US`, static admission with no runtime row.
- `limit`: not measured — ENUM, CUSTOM, CONFIRM, TIME, DATETIME, and DATERANGE as a first operand, a starred group's expansion order, and whether the extrema and `Sum` share the distinct count's positional rule or are genuinely order-free.
- `bytes`: [`distinct-count-first-operand-class/Kind_DM.json`](../../evidence/kernel-30.8.1/captures/distinct-count-first-operand-class/Kind_DM.json), SHA-256 `a31cc2cdaf5bba7a086ddf32b0eaab5134c862f5c2ca208bb7042565d184d233`. No artifact: `rule check` writes none, and the thirty-two verdicts are recorded above.
- `sync`: behavioral and locally originated. [`spec/07`](../../spec/07-repetition-and-iteration.md) stated the String class for a String-containing expansion under this operator without saying the code is positional, and its `NumberOfDifferentValues` clause named the date mismatch code without the rule that selects it. Outbound as [`SPEC-2026-08-31-13`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-13).

<a id="src-capacity-consumer-sweep"></a>
#### The declared-capacity extent reaches the last four operand-stream consumers, measured locally 2026-08-31

- `revision`: one `:adapter:kernelProbe` request at a12-dmkits `5e823edade542fb37336def0bb9f4f736a846b3f`, the sibling checkout clean before and after with `state: CLEAN` in the artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` and `compute` on both codegen strategies, `enginesAgree: true` on all seven rows.
- `question`: [`EXP-2026-08-31-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-31-03) enumerated by call site the consumers of the resolved operand stream still reading the complete formal-cell view, and asked whether the capacity projection is a property of the operand or of each consuming operator.
- `model`: `Cap_DM`, two same-kind shells each over a repeatable group declared `max 2` — `TShell/TRows/Tag` STRING and `NShell/NRows/Amount` scale-0 NUMBER — with a root `W` guarding every rule so none fires vacuously. Eleven rules and three computations: uniqueness over the starred `Tag`, the starred `Amount`, and the fixed `TShell`; a `NumberOfDifferentValues` ladder carrying `0`, `1`, `2`, and `3` over the starred `Tag` **and** over the fixed `TShell`; `FirstFilledValue` over the starred field, the starred group, and the fixed group into three separate targets.
- `claim`: **every consumer is on the extent account, on every operand form.** A duplicate pair spanning rows 1 and 3 leaves both uniqueness carriers and the fixed-group one silent; a `A, A, B` document answers `NumberOfDifferentValues == 1` rather than `2` on the starred field and the fixed group alike; and a document whose only filled `Tag` sits at row 3 produces **no computed outcome at all** for any of the three `FirstFilledValue` forms.
- `separator`: the two in-capacity controls carry the same values one index lower and fire, so each silence is the projection rather than a failure to reach the third row. The distinct-count document is the one that separates value from mechanism: `A, A, B` answers `1` under the extent and `2` under the complete view, where the duplicate document answers the same under both.
- `claim`: the `== 0` ladder member is reached positively — a document with **no rows at all** fires it on both operand forms and produces no first-filled outcome — so an empty answer elsewhere is a reading rather than an unexercised rung ([`LF125`](../LEAN-FINDINGS.md)).
- `interaction`: a malformed in-capacity `Amount` beside a duplicate needing row 3 leaves uniqueness silent and the distinct count definite, so the two exclusions compose as their separate rules predict: the malformed cell is skipped like an empty rather than terminalizing, and the over-limit row supplies nothing.
- `local-consequence`: **only two of the four consumers were changed, and the other two are unfalsifiable here.** The token distinct count and `FirstFilledValue` answered `.unknown .overRepetition` and `.unavailable .overRepetition` on the complete view where the Kernel answers definitely, so both now read the in-capacity projection. Uniqueness reaches the Kernel's verdict either way, because an over-limit cell is formally unavailable and that operator **skips** an unavailable cell exactly as it skips an empty — no document separates the accounts for it, so its call sites are left alone and the [sweep cases](../../A12Kernel/Conformance/CapacityConsumerSweep.lean) say so rather than implying a third measured change.
- `limit`: one repetition level per shell at capacity 2, one over-limit index, `en_US`, no filter, no nesting below the star, no partial validation. Not measured: a filtered star on any of these carriers, a second repetition level, the Boolean and temporal uniqueness carriers, and the token value count's starred-field branch.
- `bytes`: [`capacity-consumer-sweep/`](../../evidence/kernel-30.8.1/captures/capacity-consumer-sweep/); SHA-256 model `c803025f1439fb056ee9fbeed9d3ec8a4288bc1169848bf451cb266a17f67465`, request `86caadba6f9b3ca08497b25f2d68dae2bee746d53b0a61cba16b604063ee7147`, artifact `bc205037d79e102cfda242b86ec635d5fec00666fab4caee926dcd1b67e55b5c`, plus the seven documents. A bounded raw calibration capture with no typed consumer: it does not run under `lake test` and establishes neither `L` nor `C` coverage.
- `sync`: behavioral and locally originated. [`spec/07`](../../spec/07-repetition-and-iteration.md)'s over-repetition evaluation-domain account named the aggregates and the value counts; it did not reach the distinct count's own carrier, `FirstFilledValue`, or the fixed-group forms. Outbound as [`SPEC-2026-08-31-14`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-14).
