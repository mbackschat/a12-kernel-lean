# Evaluation and application source checkpoints

<a id="src-constant-assignment-diagnostic-ladder"></a>
#### A wrong-kind constant assignment's diagnostic is an ordered ladder over the pair, and the complete grid is what makes it legible, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Eighty `computation add --dry-run` children in **one** `batch`, every envelope `KERNEL_CONFIRMED`, five seconds wall clock.
- `retained-bytes`: `Probe3_DM.json` SHA-256 `94f55ee8f6ad3e1af732eeb2cab257f25b8608fb040db109bdb3a86742fb88ca` — the earlier DateRange fixture with seven further targets added to its repeatable `Rows` group so every kind is reachable at one repetition shape. Ops `9252a02fe87de2fe8556d8bb6b4aa354a8373411766253f0d3aacac3ad45632a`; results `6ffa6aca0e8a9f6cad58ca68a84184ff38288f132d36c4e970b4c959aefaff8e`, with the eighty computation specs beside them.
- `question`: five `Repeatable*ConstantComputation` carriers each projected **no class** for a target of the wrong kind, with the Boolean sibling's own partition measured beside them. That is the [`LF152`](../LEAN-FINDINGS.md) cluster shape at its largest: one measured carrier and five neighbours that had inherited its silence by analogy.
- `redundancy-check`: a12-dmkits' `KF222` and `KF239` already settle the **temporal** half — admission reads the target's declared format string rather than its kind, and all three of the Kernel's temporal refusal grounds share `MVK_INVALID_COMPARE_TO_DATE`. Both were found heading-first before the window was spent, and both confirm this project's own `SPEC-2026-08-31-02..04` and `SPEC-2026-09-03-08`. They collapsed roughly half the intended matrix, and what they do **not** cover is a temporal constant at a *non-temporal* target, which is where rung 2 turned out to live.
- `claim`: **the class is decided by an ordered ladder over the (constant family, target kind) pair.** Rung 1, a Boolean target draws `MVK_INVALID_COMPARE_TO_YESNO` and a Confirm target `MVK_INVALID_COMPARE_TO_YES`, whatever the constant is. Rung 2, a temporal constant then draws `MVK_INVALID_COMPARE_TO_DATE` at **every** remaining target — String, Enumeration, Number and DATE_RANGE included. Rung 3, otherwise the target's kind decides: temporal to `MVK_INVALID_COMPARE_TO_DATE`, DATE_RANGE to `MVK_INVALID_COMPARE_TO_DATE_RANGE`, String and Enumeration to `MVK_INVALID_COMPARE_TO_ENUM_OR_STRING`, and Number to `MVK_INVALID_COMPARE_TO_ENUM_OR_STRING` for a string-like constant but `MVK_INCONSISTENT_TYPES_COMPARED` for a Boolean one.
- `claim`: **exactly two cells depend on the pair**, and both are invisible to a single-carrier reading. Rung 2 inverts the whole table for temporal constants, and the Number target is the one rung-3 cell that discriminates the constant. Every other cell is a function of the target alone, which is why "the code names the target" survives a small sample and fails on the grid. This is the concrete cost of reading a table off a measured sibling rather than measuring the carrier — see [`LF153`](../LEAN-FINDINGS.md).
- `claim`: **the format-string rule reproduces on a repeatable target.** One `"05.03.2024"` is admitted into repeatable targets declared `yyyy-MM-dd` and `dd.MM.yyyy` alike, and a clock literal into either draws the date class. `KF222` measured that on nonrepeatable targets, so the transfer across the repeatability boundary is measured here rather than inherited.
- `a-control-that-refused`: the first batch spelled the date constant **bare**, as `05.03.2024`, and its own diagonal control was refused. The tell was the neighbouring cell: bare-into-Number drew `MVK_INVALID_COMPARE_DEC_PLACES`, a *scale* code, so the token had lexed as a Number. The final grid carries that spelling as its own row and it matches the Number row in all ten cells but the scale one. [`spec/09`](../../spec/09-computations.md) already recorded that only the double-quoted spelling is a temporal literal, which is where a reader should have started.
- `new-code`: `MVK_INVALID_COMPARE_TO_YESNO` was absent from this project's diagnostic registry and is added by this capsule. It is distinct from `MVK_INVALID_COMPARE_TO_YES`, which the Confirm neighbour reports in the same ladder position.
- `limit`: static preflight only, one model, `en_US`, one unconditional alternative per spec, every target repeatable. Nothing here runs a computation. The literal-value gates that sit **below** the kind ladder are out of scope except where a cell exposes one: the Enumeration target's stored-token membership and the Number target's decimal scale are named where measured and not otherwise characterised. CUSTOM targets and extensible-enumeration Strings are untouched.
- `sync`: **behavioral** — [`SPEC-2026-09-06-03`](../A12-DMKITS-SPEC-SYNC-LEDGER.md).

<a id="src-value-list-quantifier-kind-gate-partitions-three-ways"></a>
#### The value-list quantifiers' kind gate partitions **three** ways, they admit a sole field operand, and an Enumeration field side is legal against valid tokens, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Twenty-seven `rule check` children across four `batch` runs, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: `Probe4_DM.json` SHA-256 `16271fd62361a7465b87116f9b375e9deb49d53be5a7752543f757c4dc76cebd`; ops `8ef283a83be463254d5b865b0a3b28c914cc36c533b2024d6a80101ac4f87fa3`, `533a1e7d0d5ed13b91c76eea00d59520cf22d54116d244189d86fe7ce02a4419`, `96b3c9cd3ce0a4f765d05422a2b7da417fa1c17125b7b74c34abab70ea3367cf`; results `4fdf5935498bee34b26e2f2f2bd0c589e1fedbb5a25814df2dc78d371dd6906e`, `35f39909abee422b8f0daeccea16d7d1d9014057657179c255b06d8d7f43b3f1`, `c43676754ec4ac88821e121757b19c45e86970b0870261807e4f6d9b9700d80f`. A fourth batch covering the other two quantifiers' kind rows is pinned beside them.
- `question`: `TokenEntityElabError` carried **no** diagnostic projection at all, and the value-list carrier that wraps it dropped the field side's error to `none`. The [`LF152`](../LEAN-FINDINGS.md) sweep reached it; three separate findings came out, only the first of which is a projection fix.
- `claim`: **the kind gate partitions three ways.** String and Enumeration are admitted; a **Number** field draws `MVK_INVALID_TYPES_FOR_COMPARISON`; DATE, BOOLEAN, DATE_RANGE and CONFIRM draw `MVK_ONLY_STRING_ENUM_NUMBER_ALLOWED`. The operator's own message vocabulary is what makes the middle class legible: Number is *inside* the set that message names, so it passes the kind gate and then fails the **literal** comparison, where the other four never reach that stage. Collapsing the two would report the admitted-set class for a kind the Kernel calls admitted.
- `claim`: **a sole unstarred field operand is admitted**, on all three quantifiers — `AtLeastOneFieldValueIncludedInValueList`, `NoFieldValueIncludedInValueList`, `NotAllFieldValuesIncludedInValueList` — while `FieldValuesNotUnique` and `NumberOfDifferentValues` refuse one with `MVK_PARAMSIZE_INVALIDN` on the same fixture. So the arity rule is **per carrier**, and this project's shared shape checker applies the entity-list rule to both. Relaxing it touches `requiredMultiplicity`, a proof field of the shared `CheckedFieldEntityShape`, so it is a representation change rather than a projection fix.
- `claim`: **an Enumeration field side is legal against String literals** when every literal is a valid stored token; a non-token literal draws `MVK_INVALID_STRING_CONSTANT_FOR_ENUMERATION_OR_CATEGORY`. The valid-token row is refused only by the rule's own error-field gate, which is what shows the value check passed. This project refuses the whole shape as `unsupportedFieldsFamily`, a representation limit claiming no class, so closing it is a representation extension.
- `a-mis-designed-row-worth-recording`: the first attempt's "two strings" control paired a String with a **Date** field and drew `MVK_VARYING_TYPES_NOT_ALLOWED`. It was not the control it was labelled, and the single-String row beside it is what carried the admission. Consistent with the operator's own `invalidExample`, which names that same mixing — the catalog would have caught it before the batch.
- `the-family-agrees`: the kind partition is measured on **all three** quantifiers, not one. `NoFieldValueIncludedInValueList` and `NotAllFieldValuesIncludedInValueList` reproduce it cell for cell — Number to the comparison class, DATE and BOOLEAN to the admitted-set class, String admitted — so the gate is the family's rather than `AtLeastOne…`'s, and the two the first batch skipped are no longer read across from it.
- `limit`: static admission only, one model, `en_US`. The Enumeration and DATE_RANGE rows are `AtLeastOne…` only. Nothing here evaluates a membership test.
- `sync`: none yet. The kind partition is this project's own under-projection; the arity and Enumeration findings are recorded as gaps, and enter `spec/` with the capsules that represent them.

<a id="src-temporal-group-operand-follows-its-own-operator"></a>
#### A group operand is admitted at both temporal uniqueness carriers, and its expansion is gated by **that operator's own rule**, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Twelve `rule check` children in one `batch`, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: `Probe4_DM.json` SHA-256 `16271fd62361a7465b87116f9b375e9deb49d53be5a7752543f757c4dc76cebd`, extended with a root fixed group, one mixing spellings at one component set, one mixing component sets, and a pair differing only in **declaration order**. Group-slot ops `05a821ac0d6a66b332e5c2968939dcae9b01674a0a7942d2a66da9e1ece04402` with results `cfbffe04fdd7d7583c21889c5e998f9be6386a42300a69ecb4465fcf29838eea`; order ops `8ad1ce128a791ba8112bbe4eba2787fb395db3958e731b723e2243bbbefba3bf` with results `8d1eee3a795f67baede08eb8b988357e1f39af487b03a5bcb4d7bb73e033d385`. The fixture grew three times and each batch was re-run against the final bytes; the group-slot results came back byte-identical, so the receipt covers every recorded verdict.
- `question`: the temporal distinct count declined every group slot with `groupOperandUnsupported`, on the stated ground that the shared group certificate carries the *neighbouring* operator's format-equality gate and no row measured a group expansion here. Another [`LF152`](../LEAN-FINDINGS.md) arm — and the stated ground turned out to be exactly right about the mechanism while wrong about the verdict.
- `claim`: **a group operand is admitted at both carriers**, in the fixed form at a bindable locus and in the starred form. A fixed group beneath an **unstarred repeatable** ancestor draws `MVK_NO_WILDCARD` instead, which is the ordinary binding rule rather than a group-slot rule — the root-level fixed group is what separates the two, and without it the group slot would look refused.
- `claim`: **the expansion is gated by the operator's own rule, not by one shared group gate.** A group whose fields carry one component set in two spellings — `yyyy-MM-dd` beside `dd.MM.yyyy` — is **admitted** by `NumberOfDifferentValues` and **refused** by `FieldValuesNotUnique` with `MVK_ONLY_STRING_ENUM_NUMBER_DATE_ALLOWED`. A group whose fields differ in component set is refused by the distinct count with `MVK_DATEFORMATS_NOT_COMPATIBLE`. So each operator applies to a group's expansion exactly the gate it applies to scalar operands, and the two gates genuinely differ.
- `the-decline-was-right-about-the-mechanism`: reusing the uniqueness carrier's group certificate at the distinct count would have inherited format equality and **over-refused** the same-set/different-spelling group. The decline named that risk precisely and then paid for it in the opposite direction, refusing everything rather than refusing one shape. Recording it because the reasoning transfers: the fix is a distinct-count-owned certificate carrying a component-set agreement obligation, not the shared one.
- `claim`: **declaration order inside the expansion decides the refusal class**, exactly as the first-operand rule does for a scalar list. Two groups differing in nothing but the order of one DATE and one Number declaration draw `MVK_DATE_AND_NONDATE` and `MVK_NUMBER_AND_NON_NUMBER` respectively, and the scalar controls on the same two kinds agree cell for cell. So the positional rule reaches a group's expansion rather than stopping at the authored slots, and a projection keyed on the *offending* kind alone is right in one order and wrong in the other.
- `limit`: static admission only, one model, `en_US`. Nothing here counts or compares a value, so what a group operand contributes at runtime — and whether an uninstantiated row supplies anything — is untouched. Expansion order is observed here only through the class it selects; that it is declaration order is this project's reading of two rows, not a separately measured ordering.
- `sync`: none yet. The clause states scalar operand gates; the group-expansion rule enters `spec/` with the capsule that implements it.

<a id="src-constant-computation-admits-a-repeatable-target"></a>
#### A constant computation admits a **repeatable** target at every declaring-group choice, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Five `computation add --dry-run` children in one `batch`, the read-only static preflight.
- `retained-bytes`: `Probe4_DM.json` SHA-256 `ddd70e6610bfb3ee7c8d1ae82da90aa89636606c8c0ade5e9f4dd3c321413118`, carrying a Boolean field inside the repeatable group; ops `db8110742dd2db1ce18d56ce2f3c94f4ab90d2b1a1785aeed61b329b6437f3c2`; results `6e68d3f297a0f2db3f0227e9ed39575b77303ee796df2fb9ca23a94d1efbed6a`, with the five specs beside them.
- `question`: `BooleanConstantComputation` refuses a repeatable computed target and projects no class, described as an unmeasured target-placement failure. The [`LF152`](../LEAN-FINDINGS.md) sweep reached it.
- `claim`: **the Kernel admits it**, and the declaring group does not matter — the target inside a repeatable group is admitted with no `group` given, with `group` set to that repeatable group, and with `group` set to the root alike. A constant reads nothing, so no iteration is derived and the placement restriction that governs operand-reading computations has nothing to bind.
- `claim`: **a star inside a precondition is what refuses**, and it refuses with `MVK_NO_WILDCARDS_ALLOWED` — the wildcard gate, not a placement class. That row is what keeps the four admissions from reading as "this carrier has no gate at all": the same repeatable target flips to refused when the alternative's guard reaches a starred operand.
- `local-scope`: this project keeps refusing the shape, now for a stated reason rather than an unmeasured one. Its `executeResult` produces one `(target.id, value)` pair, while a repeatable target writes one cell per instantiated row, so admitting it without a per-row result would return a right value at a **wrong extent** — the silent direction. Refusing is the direction that produces a signal.
- `do-not-read-across`: four sibling carriers — Boolean, String, Custom and temporal first-filled — carry the identical `targetFixed : target.repeatableScope = []` restriction. **None is measured**, and none follows from this row: each reads a starred source where the constant reads nothing, which is precisely the property that makes this one unrestricted. Reading them off this checkpoint is the crossing [`LF116`](../LEAN-FINDINGS.md) names.
- `limit`: static preflight only, one model, `en_US`, one unconditional alternative and one starred precondition. Nothing here runs a computation, so what the Kernel writes into which rows is untouched — and that is exactly the question the local representation would need answered before admitting the shape.
- `sync`: none. No `spec/` clause stated the restriction; it was a local representation boundary described as an unmeasured Kernel gate, and that description is what changed.

<a id="src-pattern-comparison-left-kind-gate-is-total"></a>
#### The pattern comparison's left-kind gate is total: DATE_RANGE draws the same class as the other seven kinds, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Four `rule check` children in one `batch`, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: `Probe4_DM.json` SHA-256 `6f85d83977facea4511f04cb5e04d3149d5f129e294ad5c6150a07f1ec0bbd2e`; ops `bc9a7aceefb78db4c1412347abe24d763d8567e1c9afc75de5f3352ba4253744`; results `f39c2d4d8cac8d036eedfa174b7d0add71e89ef5d69804f3e8e2e1f50b702ded`.
- `question`: the [22-case admission matrix](#src-repeatable-string-application) established `MVK_INVALID_TYPE_FOR_PATTERN_COMPARISON` for seven left kinds — Number, Boolean, Confirm, Enumeration, Date, Time, DateTime — and DATE_RANGE was not among them, so this project's projection fell through a catch-all to `none` for exactly that kind. One more [`LF152`](../LEAN-FINDINGS.md) arm, found by sweeping rather than by a failing case.
- `claim`: **DATE_RANGE draws the same class**, on `PatternMatched` and `PatternViolated` alike, beside a Number control reproducing the earlier matrix's row and an admitted String control. The gate is now total over the left kinds a declaration can carry, so the projection stops needing a catch-all.
- `the-syntax-cost-a-round`: the operator is **infix with a bracketed field** — `[Field] PatternMatched "regex"` — not a call. The first attempt used call syntax and all four rows returned `MVK_UNEXPECTED_TOKEN`, the admitted String control included, which is what exposed it. `dmtool operators PatternMatched` carries `validExample` and `invalidExample` fields holding exactly this; consulting the catalog before authoring a condition is cheaper than a batch, and is the method for any operator whose surface form is not already in a retained record.
- `limit`: static admission only, one model, `en_US`, one regex throughout. The right-operand slot is untouched — the surface makes it a String constant structurally — and nothing here evaluates a match.
- `sync`: none. The class corrects this project's own under-projection; the canonical clause already states one class for every non-String left kind.

<a id="src-custom-validity-operand-refuses-every-kind-with-one-class"></a>
#### The custom-validity operand gate names its **admitted** set: every refused kind draws `MVK_NO_STRING_OR_ENUM_OR_EXT_ENUM`, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Eight `rule check` children in one `batch`, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: `Probe4_DM.json` SHA-256 `6f85d83977facea4511f04cb5e04d3149d5f129e294ad5c6150a07f1ec0bbd2e`; ops `e47b9fe13a22eea19c301a70333e892d6ea35bd316d2d4f7762da8a8eecea167`; results `f81c39c1ca2cd91256e59663b498f3bdf76730db0450228123534b4c3774f581`.
- `question`: `CustomFieldValidity` carried an arm named `unmeasuredKind`, held deliberately separate from `inadmissibleKind` because only the latter had an established class. Four kinds sat behind it — DATE, TIME, DATE_TIME and DATE_RANGE. Another [`LF152`](../LEAN-FINDINGS.md) instance found by sweeping arms that project nothing.
- `claim`: **all four draw `MVK_NO_STRING_OR_ENUM_OR_EXT_ENUM`**, the same class the already-measured Number and Boolean operands draw. The gate names its admitted set and does not partition the refused kinds, so the separate arm had no remaining content and folds.
- `the-control-that-makes-it-a-gate`: an **Enumeration** operand is not refused here at all — it passes the kind gate and is stopped by a later one, reporting `MVK_ERROR_FIELD_NOT_REFERENCED` because the probe rule's error field is unreferenced in that row. Without it, seven refusals would be equally well explained by the operator refusing everything, and the admitted set the message names would be untested.
- `contrast-worth-keeping`: the Boolean constant's computed-target gate partitions the same kinds into [four classes](#src-boolean-constant-target-kind-partitions-into-four-classes). Two kind gates, measured a few minutes apart, with opposite shapes — which is exactly why neither was read off the other, and why "the kinds behave alike here" is a per-gate measurement rather than a family rule.
- `limit`: static admission only, one model, `en_US`, one custom type name throughout. The raw-String operand this project also refuses is untouched and remains a representation boundary rather than an observed Kernel gate.
- `sync`: none. The class corrects this project's own under-projection; no `spec/` clause named it.

<a id="src-temporal-declaration-without-a-format"></a>
#### A temporal field may declare **no** format, and the Kernel resolves the default from its declared kind, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. One `model check` plus nine `rule check` children in one `batch`, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: `Probe4_DM.json` SHA-256 `a0c6dcc2cc2f54bce690fbc72ca6cfafec6f75c50c4d392d8a1cfe0aa99275a2`, declaring a DATE, a TIME and a DATE_TIME each with an **empty** type payload and therefore no `format` key. Ops `b462092c26e771a4a6c6abb0819b87f284528f89944f29cfbd817cf4bb0bc0b5`; results `7c66b842496886f8a1ef6fa87230e652a5dafbcb37f265f554e3688b2deee642`.
- `question`: [SG21](../SEMANTICS-GAPS.md#sg21--the-declared-kindformat-split-across-temporal-carriers) recorded this as **unmeasured on both sides** — whether the Kernel resolves a default format from the declared kind, and therefore whether an undeclared temporal field classifies at all. This project's flat policy gate refuses an empty format string outright, which the gap noted was a property of the local model rather than a measured rule.
- `claim`: **the declaration is authorable.** A model carrying all three format-less temporal fields is kernel-valid. The reachability half of the gap is settled, and settled the only way it can be — by measurement, not by reading.
- `claim`: **the default is resolved from the declared kind**, and the resolved value behaves as a declared one everywhere tested. The format-less DATE is admitted beside `yyyy-MM-dd` and refused beside `yyyy-MM` with `MVK_DATEFORMATS_NOT_COMPATIBLE`, so it carries the complete-date component set; the format-less TIME is admitted beside `HH:mm:ss` and the format-less DATE_TIME beside the full stamp format. The decisive row is the **pair of format-less fields**: a DATE beside a DATE_TIME is refused incompatible, so the two resolve to *different* sets and the resolution reads the kind rather than supplying one universal default.
- `claim`: **the resolved format is a format string, not an absence.** `FieldValuesNotUnique` gates on the declared format *string*, and the format-less DATE is **admitted** beside `yyyy-MM-dd` there, while its control — two declared full dates spelled `yyyy-MM-dd` and `dd.MM.yyyy` — is refused `MVK_ONLY_STRING_ENUM_NUMBER_DATE_ALLOWED` on the same operator. So the format-less field compares equal to the default spelling rather than being exempted from the gate, which is the reading a component-set carrier alone could not separate.
- `an-instrument-note-banked-here`: the first attempt encoded the fields as `{"type":"DateType"}` with **no** payload object. That is not valid DM-JSON — the payload is required — but `dmtool` reports it as `RK_INTERNAL_ERROR` with a Java stack trace and its own `fix` text saying it is a tool defect rather than an input rejection. The correct encoding is an **empty** payload, `{"type":"DateType","DateType":{}}`. Recorded so the next reader does not mistake a malformed encoding for a Kernel refusal.
- `limit`: static admission only, one model, `en_US`. Nothing here reads a stored value, so which text such a field's cells accept, and how the resolved default renders on a computed target, are untouched. The exact default *spelling* is inferred from the compatibility rows rather than read off the Kernel — the DATE row establishes the complete-date component set and equality with `yyyy-MM-dd`'s format string, which together pin it for every gate measured but do not exhibit the literal the Kernel holds.
- `sync`: **behavioral** — [`SPEC-2026-09-06-02`](../A12-DMKITS-SPEC-SYNC-LEDGER.md).

<a id="src-boolean-constant-target-kind-partitions-into-four-classes"></a>
#### The Boolean constant computation's wrong-kind target draws four different classes, not one, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Ten `computation add --dry-run` children across two `batch` runs, the tool's read-only static preflight, which is kernel-gated and writes nothing.
- `route`: the **computation** route, first used here. `dmtool computation add --dry-run <spec.json>` statically checks a computation spec, and `batch` dispatches the `computation` verb exactly as it does `rule`. That opens the computation half of the estate to the same cheap static measurement the rule route already had; the spec shape is `{computedField, alternatives, messages}`, and `dmtool schema computation add` prints it.
- `retained-bytes`: `Probe3_DM.json` SHA-256 `3fb60e71dd1d0aa9221afc12c55d487a36fce4bc76559c74bb1c45b2132a1d9c` — the DateRange fixture extended with Time and DateTime targets, so it differs from the copy pinned beside the [scalar-slot checkpoint](#src-daterange-scalar-slot-rejects-every-nonscalar-form); each capture keeps the exact bytes its own batch ran against. Ops `c295a237a7bf2512e813fe28ccb53ff40c97741ca85627a1bbe7ba6df2d0db24` and `682f70c012f10ad34215361d4029ab6f2c2832d6f2b9d802bac8a03370413551`, results `94cb82c3fb565a6a40411743a722d8cd39995e32676ba1b05750d478638dd163` and `63843075bd42c0e5f975bd2308330f0e358d711f31be094488e201ca08eda2b3`, with the ten computation specs beside them.
- `question`: this estate projected **no class** for a Boolean constant assigned to a target of the wrong kind, while projecting the Confirm asymmetry's `MVK_INVALID_COMPARE_TO_YES` beside it. Another instance of the [`LF152`](../LEAN-FINDINGS.md) shape — one measured sibling and an unmeasured neighbour — found by sweeping the arms that project nothing rather than by a failing case.
- `claim`: **the refusal partitions the target kinds into four classes.** String and Enumeration share `MVK_INVALID_COMPARE_TO_ENUM_OR_STRING`; Number draws `MVK_INCONSISTENT_TYPES_COMPARED`; all three temporal families — DATE, TIME and DATE_TIME — share `MVK_INVALID_COMPARE_TO_DATE`; and DATE_RANGE draws `MVK_INVALID_COMPARE_TO_DATE_RANGE`. A single wrong-type class was the tempting account and is wrong four ways.
- `claim`: the two admitted targets are unchanged and re-confirmed as controls — a Boolean target takes either constant, a Confirm target takes `True`, and `False` into Confirm still draws `MVK_INVALID_COMPARE_TO_YES`. Without those the partition would not distinguish "this kind is refused" from "this constant is refused".
- `new-codes`: `MVK_INVALID_COMPARE_TO_ENUM_OR_STRING` and `MVK_INCONSISTENT_TYPES_COMPARED` were absent from this project's diagnostic registry entirely and are added by this capsule.
- `limit`: static preflight only, one model, `en_US`, one unconditional alternative per spec. Nothing here runs a computation. The repeatable-target refusal that shares this module's error type is untouched and still projects no class.
- `sync`: none. The classes correct this project's own under-projection; no `spec/` clause named them.

<a id="src-daterange-scalar-slot-rejects-every-nonscalar-form"></a>
#### `AtLeastOneDateRangeOverlaps` rejects every non-scalar form in its **scalar** slot with one class, and admits all five in its `In` slot, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Ten `rule check` children in one `batch`, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: a third fixture, `Probe3_DM.json` SHA-256 `cd326fca203adfebdd7b1d7bfebb6ef5b9586ea4cc7ea953e6378b1325b4205c`, adding DateRange declarations in a repeatable group, a homogeneous fixed group, a homogeneous repeatable group, and a nonrepeatable group beneath the latter. Ops `ca6def40c324940d5f6a255748046daece15b488dc40703474e604d31601d177`; results `41593574a3535dbc4f5a03763248c1ad4e9194ac54a4f44d80eea89f457552fe`. The fixture grew twice during the batch and the **whole batch was re-run against the final bytes** each time, so the receipt matches every recorded verdict.
- `question`: this estate refused a filtered star and a group operand in the scalar slot while projecting **no class** for either, with the plain star's class measured beside them. That is the [`LF152`](../LEAN-FINDINGS.md) shape — a refusal inherited by analogy from a measured sibling — and the sweep it prescribes found it at this carrier.
- `redundancy-check`: a12-dmkits' `KF187` holds a per-carrier group-operand table and it records `AtLeastOneDateRangeOverlaps` for the **`In` slot only**, with the scalar slot left `—` on both repetition shapes; their corpus carries no occurrence of this diagnostic code. The gap was real and the check narrowed it to the scalar slot before the window was spent.
- `claim`: **the scalar slot rejects every non-scalar form with `MVK_INVALID_PARAMETER_FOR_DATE_RANGE_COMPARISON`** — plain star, `Having`-filtered star, fixed group, homogeneous starred group, and the presence terminal. Each is paired with the *same form admitted in the `In` slot*, which is what makes the result a property of the **slot** rather than of the form; without the pairing every refusal would be equally well explained by the operator rejecting the shape outright, and the peer's table already measured the opposite on the other side.
- `claim`: **a kind gate reports before the slot gate.** A starred group whose expansion mixes kinds draws `MVK_VARYING_TYPES_NOT_ALLOWED` in the scalar slot instead, which is why the starred-group row uses a homogeneous group — a mixed one measures the earlier gate a second time and says nothing about this one. That confound cost a row before its control exposed it.
- `limit`: static admission only, one model, `en_US`. `DateRangesOverlap`, the plural sibling, is untouched here; `KF187` records it refusing groups outright with a different code, so the two operators are not interchangeable at this gate.
- `sync`: none behavioral. The classes correct this project's own under-projection; `spec/05`'s existing sentence on the scalar slot is narrowed to name the class rather than leave it unmapped.

<a id="src-later-position-class-is-total-and-presence-is-admitted"></a>
#### Every later-position kind draws `MVK_DATE_AND_NONDATE` at the temporal extrema, and a starred group over a nonrepeatable terminal is admitted with its expansion gated, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after every run, Kernel `30.8.1` built and runtime. Fifteen `rule check` children across three `batch` runs, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: a second fixture, `Probe2_DM.json` SHA-256 `d9f70adae2a394a371b318a243ddb5391da135bdccb4ade46292566686bb754d`, carrying the three kinds and the nonrepeatable-subgroup shape the first fixture lacks. Ops `21d1f18ac263bddb844128ce2819e2bce0e2a5ddb1b876888bb81c7a148819cd`, `89c3e58a1075a646d835625486da7d3ffbc96ed411d235292756b0c59033be8e`, `0d808440fda9b1e30143bbe0375448a0fe0cc1ce9e44ef5b40a0f941441e6178`; results `c73561383cc79b8dcc976803aac320cf857079ad8ecbbe5f7b8b5895fc3e554e`, `8b467217900313719b5b5248ebadf7708a1a8479facafb2fc332522726d313c6`, `3e664732d9715f2cf843d7b3309688fcf4d9fd465eaf34b208f4734629c984f5`. The fixture gained a group between the first batch and the later two, so the first batch was **re-run against the pinned bytes** and returned an identical result file; the receipt therefore matches what every recorded verdict actually ran against.
- `question`: [SG23](../SEMANTICS-GAPS.md#sg23--the-temporal-extrema-operand-gate-element-type-and-fold) held three later-position kinds projecting no class, and the starred-group **presence** operand refused with the refusal recorded as unmeasured.
- `claim`: **the later-position class is total.** Enumeration, Confirm and DATE_RANGE each draw `MVK_DATE_AND_NONDATE`, beside a Boolean control on this same model that reproduces the first fixture's row. With Number, String and Boolean already measured, all six kinds share the class, so the Kernel's generalizing message text is the rule and the projection stops branching on kind.
- `claim`: **the presence form is admitted, and its expansion is gated.** A star above a nonrepeatable terminal declaring only dates is admitted; the same path read **without** a star draws `MVK_NO_WILDCARD`, so admission is a property of the star rather than of the group. Against a differing component set it draws `MVK_DATEFORMATS_NOT_COMPATIBLE`; against the same set spelled `dd.MM.yyyy` it is admitted; and with a non-date later operand it draws `MVK_DATE_AND_NONDATE`, so its expansion fixes the leading class like any other operand's.
- `a-confounded-pair-and-the-control-that-caught-it`: the first attempt put the date **beside** the existing Number inside the nonrepeatable subgroup, which made that group's expansion *mixed* — so both rows returned `MVK_NOT_SORTABLE` and measured the mixed-expansion rule a second time instead of the form under test. The numeric control caught it by changing answer between the two fixtures on the identical condition. The fix was a second subgroup declaring only dates. This is the failure mode a12-dmkits named the same day: a control earns its place by failing when the row's *design* is wrong, not only when the Kernel surprises you.
- `limit`: static admission only, two models, `en_US`, arity 2 for every later-position row, and every mixed row places the offender second. Nothing here evaluates a fold; the presence form's row **extent** is not measured and is taken from the shared owner that the Boolean value-count carrier already uses.
- `sync`: none. Both results correct this project's own projections against the Kernel; no clause stated either.

<a id="src-later-position-kinds-and-group-expansion-class"></a>
#### A Boolean in a temporal extremum's later position draws `MVK_DATE_AND_NONDATE`, and a mixed starred-group expansion draws `MVK_NOT_SORTABLE` instead, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `1f2d4512bd92a28eb82091264de8c701004c1076`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Eight `rule check` children in one `batch`, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: `Probe_DM.json` SHA-256 `594680deb6dee9a19663fb86cfd86539bf07f59ef9b64da087cbd3421c21cade`, the same fixture as the two preceding checkpoints; `ops-next4.json` `18b2c6581d1048eed0155e944e6b390c60a009ca3d619ff0f698c1fcccf6e828`; `results-next4.json` `9a25f4b2c8892777b52701f3c2729eadee0b677ffca13da68c05ff334d00f8ee`.
- `question`: [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) carried two open items at the extrema — the four later-position kinds projecting no class, and a **group** operand's leading kind, which the sibling distinct count had measured and the extrema had not.
- `claim`: **Boolean in later position draws `MVK_DATE_AND_NONDATE`**, the same class a Number and a String already drew, and `MinValue` agrees with `MaxValue` on it. Three kinds from three different families now share the class. That is what makes the Kernel's generalizing message text — *"if the first parameter is a date than all parameters have to be dates"* — read as a rule rather than one pair's accident, but the text remains wording and not a row, so Enumeration, Confirm and DATE_RANGE stay unprojected.
- `claim`: **a starred group whose expansion mixes kinds draws `MVK_NOT_SORTABLE`**, not the date/non-date class. This *confirms an account this estate already held* rather than adding one: `firstNotTemporal` deliberately claims no class, on the stated ground that a non-temporal leading operand makes the list the Number family's, whose own list draws exactly `MVK_NOT_SORTABLE`. The group's recursive expansion leads with a Number here, so the row lands where that account predicts. Consulting the estate's own projection before calling a row new is the [`LF147`](../LEAN-FINDINGS.md) tell, applied in time on this batch.
- `the-presence-row-does-not-transfer`: `MaxValue(/Probe/Rows*/Sub) > 0` — a starred group whose terminal is **nonrepeatable**, the presence form — is **admitted**, against an admitted plain starred-group control and an admitted leaf-through-star control. This is the *numeric* extremum: `Sub` declares only a Number, so the row says the presence form is not refused by the wildcard machinery and says nothing about the temporal carrier's component gate. That carrier keeps refusing the form, and keeps recording it unmeasured, because reading admission across a carrier is the crossing [`LF116`](../LEAN-FINDINGS.md) names and this batch cannot close it without a date beneath a nonrepeatable subgroup.
- `limit`: static admission only, one model, `en_US`, arity 2 for every later-position row, and every mixed row places the offender second. The three unmeasured kinds were not attempted because this fixture declares none of them.
- `sync`: none. The Boolean class extends a projection already keyed per kind; the group row confirms an existing account; no clause changes.

<a id="src-filtered-star-temporal-carriers-and-binding-depth"></a>
#### A `Having`-filtered star is admitted wherever a plain star is on all three temporal carriers, the component gate reads through it unchanged, and the operand/target binding relation re-derives independently, measured 2026-09-06

- `revision`: `dmtool` 0.13.0 at a12-dmkits `d7344e6c99928618bd66b3a7afda518825b8ece2`, launcher self-reported `clean`, the sibling checkout clean and unchanged before, between, and after all three runs, Kernel `30.8.1` built and runtime, catalog verified against `30.8.1`. Seventeen `rule check` children across three `batch` runs, every envelope `KERNEL_CONFIRMED`.
- `retained-bytes`: `rule check` persists nothing, so the verdicts are recorded here and the dispatched bytes are what is kept. `Probe_DM.json` SHA-256 `594680deb6dee9a19663fb86cfd86539bf07f59ef9b64da087cbd3421c21cade`, byte-identical to the 2026-09-05 capture — the two checkpoints share one fixture. Ops `13f59051cc72c98fdf7b9069e942e4f31c2a23f4292ec7f90537e2a4eb3cc334`, `eef44d1067fc41ded6561b9c64a3015bb68796978a6022f87668c5ade3ec8278`, `b675b6b27453eb6d0b5446514cf342ea8bd65a53ef757c3fa06685a25c12ddcc`; results `658675eb8cdc53bbd72a36601c17736603a530e6d5dec2b68b77439375e3d830`, `07fb6aa2b7125f47a3953f19a9ff8aa3ddba74fd7ec4c400dfb0acf3de8653da`, `44db0471b8c299477aa8f6a2981ff9b62aaa06b03ac9de2a594607a8c4c25ba8`.
- `question`: this estate refused a filtered star as a temporal extremum operand and recorded the refusal as **claiming no Kernel class**, explicitly gated on a measurement rather than on judgement, because the neighbouring DateRange carrier does refuse starred operands outright and reading admission across that boundary is the crossing [`LF116`](../LEAN-FINDINGS.md) warns about. This is that measurement. It also retires the malformed pair the [previous checkpoint](#src-distinct-count-component-set-and-locus) recorded as having established nothing.
- `claim`: **the filtered star is admitted.** `MaxValue(/Probe/Rows*/RowDate Having [RowVal] > 0) > [FiledOn]` is kernel-valid, beside a plain-star control that is also valid and a *numeric* filtered extremum that is also valid. The three together separate the two readings the decline rested on: the filtered form is not refused at extrema generally, and the temporal carrier does not refuse filtered forms.
- `claim`: **the component gate reads through the filter.** The filtered star against a differing component set — `yyyy-MM-dd` rows beside `yyyy-MM` — draws `MVK_DATEFORMATS_NOT_COMPATIBLE`, identically to the plain star on the same list, and identically again with the filtered star in the *later* operand position. So admission above is the gate passing rather than the gate being skipped for filtered operands.
- `the-separator`: the filtered star beside `dd.MM.yyyy` — the **same** component set spelled differently — is **admitted**. This is the row that makes the gate a component-set gate at this carrier directly, rather than an inference from the sibling aggregate's rows.
- `carrier-sweep`: `FieldValuesNotUnique` and `NumberOfDifferentValues` each admit the same filtered star, against a plain-star control that is also admitted. Measured per carrier rather than inferred from the extremum, because that inference is exactly the boundary crossing this checkpoint exists to stop repeating.
- `binding-depth-re-derivation`: a12-dmkits measured the operand/target binding relation on the rule carrier and reported the transfer holding on every cell, while flagging that their expectations matched every row and inviting an independent check ([their report](../SOURCES.md#inbound-2026-09-06a)). Re-derived here on a different model with different field names: form-level operand into a one-level target, own-level control, form-level into a two-level target, and one level up into a two-level target are all **admitted**; one level below the target, two levels below, and a cross-repeatable sibling without an index declaration all draw `MVK_NO_WILDCARD`. Seven for seven. The depth-2 below-target row is not in their table and extends it.
- `limit`: static admission only — nothing here evaluates a fold, and the filtered extremum's *runtime* reader still declines the form. One model, `en_US`, one filter shape (a self-comparison on the row's own numeric guard), and the filter's own legality is never varied. The starred-group **presence** operand is untouched and remains unmeasured at this carrier.
- `sync`: none. The admission widening corrects this project's own over-refusal against the Kernel; no `spec/` clause stated the decline, which was carried in the Lean arm's docstring as an explicitly unmeasured boundary.

<a id="src-distinct-count-component-set-and-locus"></a>
#### `NumberOfDifferentValues` gates temporal operands by **component set**, not by declared format, and the date-first class is confirmed locally, measured 2026-09-05

- `revision`: `dmtool` 0.13.0 at a12-dmkits `4dc5f8a0ddd93b50976f0ba45f6e54fbdf34eb91`, launcher self-reported `clean`, the sibling checkout clean and unchanged before and after, Kernel `30.8.1` built and runtime. Nine `rule check` children in one `batch`, every envelope `KERNEL_CONFIRMED` with `source: KERNEL`. `rule check` persists nothing, so the verdicts are recorded here and the model plus the dispatched ops are the retained bytes: `Probe_DM.json` SHA-256 `594680deb6dee9a19663fb86cfd86539bf07f59ef9b64da087cbd3421c21cade`, `ops.json` SHA-256 `b7568702ce05585c8ac4c3ae957de99cc9c8b79259cc7c168a73fbaa216c2380`, `window-results.json` SHA-256 `2793e140cd940e42283407ff9817783317632961c5f6c3b6690786ed6354f3cc`.
- `question`: [SG22](../SEMANTICS-GAPS.md#sg22--numberofdifferentvalues-operand-domain-and-its-two-homogeneity-codes) carried the temporal admission gate and the date-first homogeneity class as peer-reported, and [SG9](../SEMANTICS-GAPS.md) wanted one reference row for an unstarred repeatable group-presence operand.
- `claim`: **the gate is the component set and not the declared format.** Two DATE fields whose formats differ in spelling but agree in components — `yyyy-MM-dd` beside `dd.MM.yyyy` — are **admitted**, while `yyyy-MM-dd` beside `yyyy-MM` is refused `MVK_DATEFORMATS_NOT_COMPATIBLE` naming both formats. This re-derives a12-dmkits' reported rows on this project's own fixtures ([their report](../SOURCES.md#inbound-2026-09-05e)), which is independent corroboration rather than agreement: different models, same terminal oracle.
- `claim`: **date-first draws `MVK_DATE_AND_NONDATE`**, whose text states the rule outright — *"if the first parameter is a date than all parameters have to be dates"* — completing the first-operand class matrix locally at arity 2.
- `the-uniqueness-control`: `FieldValuesNotUnique` over that same **admitted** DATE pair is refused, and reports `MVK_ONLY_STRING_ENUM_NUMBER_DATE_ALLOWED` rather than a format code. The code name is misleading rather than the behaviour surprising: a12-dmkits holds the dissolving control — the identical pair spelled `yyyy-MM-dd` twice is **admitted** — so the refusal is the differing format string and the domain code is simply where that failure surfaces. Their source read puts the two operators on different predicates, `hasSameComponents` for the aggregates against `hasSameKnownFormatStringIfExists` for this one, which fits three rows and is a hypothesis rather than a measured cause.
- `this-project-already-predicted-it`: [`TemporalValuesNotUnique.diagnostic?`](../../A12Kernel/Elaboration/TemporalValuesNotUnique.lean) projects that exact code for `mixedDeclaredFormats`, with a comment stating the format rule reports the kind gate's code. The row was recorded as unexplained without consulting the estate's own projection, which is the [`LF147`](../LEAN-FINDINGS.md) tell a third time: ask whether the code already answers before calling an observation new.
- `group-presence`: `GroupFilled(/Probe/Rows/Sub)` from a non-iterating locus is refused `MVK_NO_WILDCARD` naming the unbound ancestor — *"Missing specification of an asterisk for 'Rows' in '/Probe/Rows/Sub'"* — so a nonrepeatable group inherits its ancestors' binding requirement at this carrier too.
- `two-rows-established-nothing-and-the-controls-are-why`: the filtered-star temporal-extremum pair both drew `MVK_INVALID_COMPARE_TO_DATE`, **including the plain-star control**, so the date literal in the condition was malformed and neither row reached the star question. The cross-group pair both drew `MVK_NO_WILDCARD`, but the intended cross-group operand sits in a *repeatable* group read without a star and is therefore also an unstarred-repeatable read; the row cannot separate the two properties it was built to separate. Isolating that gate needs a **nonrepeatable** cross-group operand. Both failures were caught by their own controls rather than by inspection, which is the whole reason each pair carried one.
- `limit`: static admission only, one model, `en_US`, arity 2 throughout, and every mixed row places the offender second. Nothing here evaluates a count.
- `sync`: none. The component-set gate and the date-first class confirm accounts already held; the `FieldValuesNotUnique` kind-domain code is new and unexplained, and is not written into a clause.

<a id="src-distinct-count-operand-domain"></a>
#### `NumberOfDifferentValues` admits Custom as string-like, splits homogeneity into two codes, and counts **expanded** operands for arity, measured locally 2026-09-05

- `revision`: launcher self-reported build a12-dmkits `226b2be175133dff45413b0a0f604ea219ced85b (dirty)`; `dmtool` 0.13.0, Kernel `30.8.1`. Twelve `rule check` children across two `batch` runs, every one `KERNEL_CONFIRMED`.
- `retained`: `raw-distinct-custom.json` SHA-256 `f6584e232630d16169c5f6cb9a21692c4161d9caa69f8a028ad87a1da4e1d8e1`, `raw-distinct-boundary.json` SHA-256 `a0fab81ef51f9222ae2817bb351e8788d30053510decabf96416c966e228046c`, model `Scope_DM.json` SHA-256 `fe91a3c806cbdf0ec5a4547c836383f475e6b8a4db3e53f02296c082db6d96ee`.
- `question`: [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) carried wildcard and group Custom operands for this operator as open. The first batch admitted every row, which by itself establishes nothing about a boundary ([`LF139`](../LEAN-FINDINGS.md)), so a second batch went looking for refusals.
- `kind-domain`: the admitted operand kinds are **string, enumeration, number, and date/time** — a Boolean or Confirm operand is refused `MVK_ONLY_STRING_ENUM_NUMBER_CMP_DATE_ALLOWED`, whose text lists that set.
- `custom-is-string-like-here`: a starred **Custom** operand is admitted alone, and beside a String and beside an Enumeration. Mixing it with a **Number** is refused `MVK_STRING_ENUM_AND_NON_STRING_ENUM` — *"if the first parameter is a string or enumeration then all parameters have to be of type string or enumeration"* — so the Kernel classes a Custom field with the string family for this operator. **That is the opposite of `Valid(Field, "Type")`, which refuses a Custom operand outright** ([gates](#src-validtype-operand-gates)); one field kind, two operators, two answers, which is why neither can be read off the other.
- `homogeneity-is-two-rules`: the list must be homogeneous, and the Kernel reports it from whichever family the **first** operand belongs to. Number first then String draws `MVK_NUMBER_AND_NON_NUMBER`; string-family first then Number draws `MVK_STRING_ENUM_AND_NON_STRING_ENUM`. Same illegal pair, two codes, selected by operand order.
- `arity-confirms-an-existing-clause`: a single **starred** field is admitted alone while a single **fixed** field is refused `MVK_PARAMSIZE_INVALIDN` — *"There must be more than one field or at least one group defined."* [`spec/07`](../../spec/07-repetition-and-iteration.md) already states this and gives the better account of it: a group or flattened operand *denotes a field scope rather than a singleton direct list*, which is why one of them satisfies the arity rule and one fixed field does not. The pair confirms that clause and supplies its diagnostic; it also explains the lone **group** operand the first batch had recorded without knowing the reason.
- `binding-gate-again`: an unstarred repeatable operand draws the ordinary `MVK_NO_WILDCARD`, distinct from every gate above and from the `MVK_NO_WILDCARDS_ALLOWED` the sibling operator draws for a star — the same confusable pair, now at a third operand position.
- `limit`: static admission only; nothing here evaluates a count. One model, `en_US`, validation carrier. Date/time operands are named by the diagnostic but not exercised, and the runtime meaning of a Custom value's distinctness is untouched.
- `sync`: none. The kind domain and homogeneity codes are additions to this project's own account and contradict nothing a12-dmkits ships.

<a id="src-extensible-enumeration-has-no-dm-json-carrier"></a>
#### Extensible Enumeration is a String type's runtime quality with no DM-JSON carrier, so it is not a separable operand arm, inbound 2026-09-05

- `route`: **a12-dmkits source-read, not a probe.** Held with the peer's own stated limit rather than promoted to a measurement: they report no witness of a client `IStringType` carrying the flag, and by their own argument a DM-JSON model could not express one ([`LF134`](../LEAN-FINDINGS.md)).
- `question`: [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) carried extensible Enumeration as a third operand arm of `Valid(Field, "Type")` that no local row had exercised, which read as a hole in the denominator ([gates](#src-validtype-operand-gates)).
- `claim`: **there is no third arm.** "Extensible enumeration" is not a field type but an `IStringType` whose `isExtensibleEnum()` answers true; `FieldUtils.isExtensibleEnum` is `type instanceof IStringType && isExtensibleEnum()`, and `isLikeString` is the union of string, enum, and extensible enum. **No DM-JSON property sets the flag**, and the open values are served at runtime by a `DynamicEnumerationService`. A DM-JSON field carrying the quality is therefore a plain `StringType` with the flag false, and the predicate admits it through the identical `isLikeString` branch. No input separates the two arms, so the String row already covers this one.
- `trap`: `MM_Common`'s `ExtensibleEnumerationType` is **not** that route. It is a model/editor metamodel configuration in the tooling layer beside form-model notions rather than a runtime DM field type, and the peer's own first draft conflated it with the SPI quality — which is what their finding was written to close.
- `not-an-instrument-gap`: the genuinely un-buildable enumeration is the **dynamic / table-backed** one, whose values come from an external table and are not DM-JSON-serializable at all. No new authoring verb would change that, so it is a property of the format rather than a missing `dmtool` capability, and it closed a separate SG8 row that had been waiting for a table-name producer to become reachable.
- `local-consequence`: [`Elaboration/CustomFieldValidity.lean`](../../A12Kernel/Elaboration/CustomFieldValidity.lean) admits extensible Enumeration with ordinary Enumeration and does not separate them, because `FlatModel` carries no extensible variant to separate them with — agreement by construction rather than a modelled decision.
- `sync`: none. Inbound from an already-committed peer reading, recorded as provenance; no clause of this project's changes and no ledger entry follows.

<a id="src-validtype-operand-gates"></a>
#### The custom-type validity operand gates, with their exact codes and the two confusable wildcard refusals, measured locally 2026-09-05

- `revision`: launcher self-reported build a12-dmkits `226b2be175133dff45413b0a0f604ea219ced85b (dirty)`; `dmtool` 0.13.0, Kernel `30.8.1`. Fourteen `rule check` children across two `batch` runs plus two `field add` declarations, every one `KERNEL_CONFIRMED`.
- `retained`: `raw-validtype-operands.json` SHA-256 `983affb5d2e691b5667576647232607d2828c59b137d8122c16af056ad3c5ed2`, `raw-validtype-bound.json` SHA-256 `7927364934be7c42832785cad80e74af9946f7ffac6741229cdf6e1bd264cc39`, model `Scope_DM.json` SHA-256 `fe91a3c806cbdf0ec5a4547c836383f475e6b8a4db3e53f02296c082db6d96ee` — the carrier model plus a String and an Enumeration field.
- `question`: [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) carried the operand field, wildcard, and group gates of `Valid(Field, "Type")` as open. [`spec/05`](../../spec/05-dates-and-time.md) already states the rule — a single non-wildcarded value-validation field operand of String, Enumeration, or extensible-Enumeration type — but gives no diagnostic for any arm, and a consumer reports codes.
- `claim`: **the clause holds on every arm measured, and each arm has its own code.** A String operand and an Enumeration operand are admitted. A Number, Boolean, Confirm, **or Custom** operand is refused `MVK_NO_STRING_OR_ENUM_OR_EXT_ENUM`, whose text names the admitted set. A **group** operand is refused `MVK_NO_GROUPS_ALLOWED`. A **starred** operand is refused `MVK_NO_WILDCARDS_ALLOWED`.
- `custom-is-the-non-obvious-arm`: a field declared with a **CustomFieldType** is *not* a legal operand of the custom-type validity check. The predicate asks whether a String or Enumeration **value** is a valid instance of a named predefined type; it does not accept a field already declared as that shape. Measured rather than inferred from the admitted set, because it is the natural wrong guess.
- `two-wildcard-codes`: **the same operand position carries two refusals whose names differ by one word and whose fixes are opposite.** A **starred** operand draws `MVK_NO_WILDCARDS_ALLOWED` — remove the star. An **unstarred repeatable** operand read from a host that does not bind its level draws `MVK_NO_WILDCARD` — supply the binding, by starring elsewhere or by moving the rule. [`spec/05`](../../spec/05-dates-and-time.md) already records that pair on the `DateRange` endpoint carrier; this confirms it on a second one, which is a carrier crossing rather than an instance ([`LF116`](../LEAN-FINDINGS.md)).
- `confound-found`: the Boolean and Custom arms were first run from a rule at the nonrepeatable `/Rep` while those fields live in the repeatable `/Rep/Rows`, so both came back `MVK_NO_WILDCARD` and measured the binding gate rather than the operand kind. Re-running them from a host that binds the level gives the kind refusal. The error text naming the asterisk is what exposed it.
- `controls`: a plain `FieldFilled` condition and a String operand are admitted through the identical route in both batches, so no refusal belongs to the route or the host.
- `limit`: static admission only; nothing here invokes a validator or observes a verdict. The clause's **value-validation** qualifier on the operand is *not* separated here — the field-add schema exposes `minLength`, `maxLength`, `pattern`, and `patternMessage` for a String and no value-validation switch, so no declaration was available that would make an otherwise legal String operand non-value-validating. Denominator stated rather than the axis called unreachable. The nonempty-type-name arm is also untested.
- `sync`: none. Every arm confirms an existing clause of this project's and contradicts nothing a12-dmkits ships; what is added is the per-arm diagnostic and the Custom row, neither of which corrects an account.

<a id="src-pattern-invalid-string-cell"></a>
#### A pattern or length violation makes the cell formally invalid on the validation arm, measured locally 2026-09-03

- `revision`: `:adapter:kernelProbe`, sibling `git status --short` empty before and after the run. a12-dmkits `acced5d6012b15c0c3145d2a236e61bf2ad74246` on `main` with `source.state: CLEAN`, `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, `validateFull` on both codegen strategies with `enginesAgree: true` on every row. One nonrepeatable String field declared `[0-9]+` with its required pattern message, five rules on one error field, three documents: a violating value `"A"`, a conforming `"7"`, and the field absent. The model passes `model check` valid with zero diagnostics.
- `question`: [`SG7`](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) owns the String pattern surface, and the retained pattern rows cover **static** admission of `PatternMatched`/`PatternViolated` and a *computation* clearing for a pattern-invalid source through the kernel-free interpreter. What a violating cell does to an ordinary **validation** read was unmeasured: does the value remain usable in a comparison, with the violation merely reported alongside, or is the cell formally invalid?
- `claim`: **formally invalid — the value is not usable.** `[Pat] == "A"` against the violating document stays silent while `[Pat] == "7"` fires on the conforming one, so the comparison is not reading `"A"` and losing; it is not reading a value at all. The violation reports its own `stringFalschesMuster` beside that silence, which is what distinguishes "invalid and reported" from "rejected at ingress and absent".
- `presence-on-a-new-carrier`: **both presence polarities are non-true on the violating cell** — neither `FieldFilled(Pat)` nor `FieldNotFilled(Pat)` fires — while the absent document fires `FieldNotFilled` and the conforming one fires `FieldFilled`. That is the same rule already retained for a malformed **Number** cell, now measured on the String/pattern carrier, with absence separated from invalidity by the same three-document set rather than inferred across carriers.
- `unknown-versus-suppressed-stays-free`: `!=` was authored precisely to separate them and does not: `[Pat] != "7"` is silent on the violating document, which both an unknown reading and a suppressed-operand reading predict. Only the *usable-value* reading is refuted, since it makes `!=` fire. So this carrier joins the others where the two accounts collapse; this theory answers unknown.
- `local-consequence`: none. `FlatContext.resolveStringComparisonOperandAt` maps an unavailable observation to `.unknown`, `evalDirectString` yields `.unknown` for either operator, and `observeCell .validation` makes both presence consumers unknown, so every row above is what the existing clauses already produce.
- `length-reads-identically`: **an over-length value behaves exactly as a pattern violation**, which is worth measuring rather than assuming because a length overflow is the constraint a reimplementer would most plausibly treat as soft and reported. On a separate `maxLength 3` declaration holding `"abcd"`, the equality against that same text stays silent, inequality against a different text stays silent, and **both** presence polarities stay silent, while the in-length control fires its equality and its `FieldFilled`. The two classes differ only in the reported code — `stringZuLang` against `stringFalschesMuster` — and not in a single read.
- `three-classes-one-reading`: `minLength` joins them. On a declaration carrying `minLength 2`, `maxLength 3`, and `[0-9]+` together, the value `"1"` satisfies the pattern and the maximum, violates the minimum, reports `stringZuKurz`, and reads exactly as the other two classes do — every comparison and both presence polarities silent, against a clean control that fires. So all three String constraint classes produce one formally invalid cell and differ only in their code.
- `pattern-suppresses-both-length-codes`: on that same declaration `"abcd"` violates the pattern and the maximum, and `"a"` violates the pattern and the minimum; **both report `stringFalschesMuster` alone**. These are suppressions rather than single objecting gates, and the neighbouring rows are what establish that: `"abcd"` against a maximum-only declaration reports `stringZuLang`, `"1"` against this very declaration reports `stringZuKurz` for the same one-character length that `"a"` has, and a letter against a pattern-only declaration reports `stringFalschesMuster`. So each length gate is measured objecting on its own, and only the pattern code survives when both object. The two length bounds cannot co-occur on one value, so this exhausts the pairs.
- `operand-position-decides`: a violating cell behaves differently as an aggregate **member** than as a **filter operand**, contrasted on one document. With a repeatable patterned field holding `1`, `2`, and a violating `A`, **no** spelling of `NumberOfDifferentValues` over it fires — the aggregate is unavailable — while the clean `1, 2, 3` document answers 3 and the duplicate `1, 1, 3` answers 2 and fires uniqueness, so the ladder is live rather than inert. On that same invalid document a count over a *different* field filtered by `FieldFilled` of the violating one answers **2**: the filter drops the offending row and stays available. So one cell makes its own aggregate unavailable and merely excludes a row when read by a filter, which is the operand-position rule already retained for a malformed Number, now on the pattern carrier.
- `uniqueness-cannot-arbitrate`: uniqueness stays silent on the invalid-member document, which both a skip account and an unavailable account predict, so that row is recorded as non-discriminating rather than as evidence for either.
- `limit`: three constraint classes, one or two violating values each, one repeatable and several nonrepeatable declarations, `validateFull` only. The precedence rows say which code is *reported*, not that only one gate ran, and no claim is made about a third constraint kind. Not measured: a violating cell inside a filter or an aggregate operand, and the computation arm on these declarations — the retained interpreter row suggests clearing there but is triangulation rather than kernel runtime.
- `sync`: locally originated, and it **confirms** the existing account rather than correcting it, so no ledger entry.

<a id="src-repeatable-string-application"></a>
#### Finite one- and two-level String separate-destination application, measured locally 2026-08-26

- `revision`: the authorized temporary source-set probe ran at clean a12-dmkits `975a2e78602eaf26126168cd203bd84738885322`; accepted maintained behavioral coverage exists at `25e048ea73e10bd58c6950b1d6104dd5809535df`, whose parent is that exact basis.
- `one-level model`: one finite `/Order/Lines` repetition declares String `Source` and `Label`; the kernel-admitted computation `Label := [Source]` has common precondition `FieldFilled(Source)`, and the later ordinary row rule is `Length(Label) > 0`.
- `one-level topology`: a changed VALUE at row 1 plus a source-classified CLEARED action at row 3 produces the exact three-row prefix with Label states VALUE, absent, and present-empty. Swapping the action classes preserves the same exact coordinates. A destination `Source="KEEP"` sentinel at row 2 stays at row 2.
- `two-level model`: finite `/Order/Outer` and `/Order/Outer/Inner` repetitions have capacities 3 and 4, with nested String `Source` and `Label`, the same direct computation and common precondition, and the same later nonempty-Label rule.
- `two-level topology`: CLEARED at `[1,2]` and VALUE at `[3,4]` produce outer rows 1–3, inner rows 1–2 only below outer 1, no inner row below synthetic outer 2, and inner rows 1–4 below outer 3. Swapping action classes preserves exact coordinates and a nested destination `Source="KEEP"` sentinel survives. Dynamic Groovy, generated Java, and the standalone interpreter agree.
- `error`: an ERRORED action against the absent terminal coordinate leaves the selected extent empty at both depths in all three engines. Existing-target clearing follows the separately source-established type-neutral action boundary and was not remeasured by either probe.
- `later-validation`: one-level changed values at rows 1 and 3 and two-level changed values at `[1,2]` and `[3,4]` produce only their corresponding `LABEL_PRESENT` findings when the returned applied document is validated without recomputation. The three engines agree semantically; the interpreter omits repetition-one brackets in its canonical pointer spelling.
- `limit`: this closes direct finite String application through two levels and the corresponding bounded later row decisions under accepted [`SPEC-2026-08-26-02`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-26-02--one-level-separate-destination-string-application-uses-the-complete-predecessor-prefix) and [`SPEC-2026-08-26-03`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-26-03--two-level-separate-destination-string-application-keeps-inner-prefixes-parent-scoped). Deeper String nesting, other target families, general later-rule execution, document transport reconstruction, and pointer rendering remain outside the claim.

The first same-scope repeatable `FieldValueAsString` route is source- and execution-grounded without claiming a focused retained matrix. At pinned kernel revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, [`CheckFeldOpImpl`](../../../a12-kernel/kernel-tool/kernel-core-parser/src/main/java/com/mgmtp/a12/kernel/core/parser/internal/check/operation/CheckFeldOpImpl.java) admits only a Number operand for `FELD_WERT_ALS_STRING`, [`RuntimeController.feldWertAlsString`](../../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/core/RuntimeController.java) delegates to the iteration-sensitive field read, and each dialect's `CodeGenCalculatedField.st` owns per-environment target execution.

At exact a12-dmkits revision `f6dc22d24cc865c02c7f5b3beec5c0cff0bec140`, the existing [`ComputationModelSynthesizer`](../../../a12-rulekit/adapter/src/main/java/io/github/mbackschat/a12/dm/adapter/profile/ComputationModelSynthesizer.java) emitted same-scope repeatable String targets with `FieldValueAsString` Number operands in the maintained depth-shaped profile; strict kernel model validation accepted the generated model, and the source-owned exact-N/4N semantic preflight completed across dynamic Groovy, generated Java, and the interpreter. That broad profile establishes authorability and runtime route participation.

It does not separately calibrate the Lean capsule's decimal-versus-String storage, empty, poison, and target-policy separators, which remain project-local executable locks grounded in the already measured nonrepeatable formal-read regime.

The inverse same-scope repeatable String-source `FieldValueAsNumber` leaf has a narrower focused probe and a weaker runtime observation channel. With the a12-dmkits checkout at `c876c207bc092912ac5a35bff0db7298b63f92b0`, the source-shipped `dmtool` authored `/Probe/Lines/Amount := FieldValueAsNumber(Code)` where `Lines` repeats, `Code` is an exact `[0-9]+`/max-15 String, and `Amount` is a scale-0 Number; `model check` returned `KERNEL_CONFIRMED`. Replacing the String pattern with `-?[0-9]+` made the same computation fail with `MVK_NO_CONVERT_IN_NUMBER`, which establishes the signed-pattern boundary without generalizing from it to every other String profile.

The unchanged public interpreter `model compute` route returned `0` for both absent and present-empty source cells, `7` for valid text, target error for `123` against maximum `99`, and clearing for pattern-invalid `A`. That public payload repeats the field path without repetition coordinates, so it establishes route participation and value classes but not the Lean capsule's exact two-address identity; dynamic Groovy and generated Java were not separately run for this focused model.

At clean a12-dmkits revision `cd41ea94b470a190f7d766ea6d7adf26b6ba74cf`, dmtool `0.12.1` with built/runtime kernel `30.8.1` measures condition-pattern admission in a 22-case separating matrix over `PatternMatched` and `PatternViolated`. Both operators accept a String field beside a valid String constant. Java-invalid `"("` and Java-valid but kernel-restricted `"(?<=a)b"` each report only `MVK_INVALID_PATTERN`. For either operator, a String field in the right slot reports only `MVK_MISSING_CONST_IN_PATTERN_COMP`, while a Number constant on the right or every represented non-String left kind, Number, Boolean, Confirm, Enumeration, Date, Time, and DateTime, reports only `MVK_INVALID_TYPE_FOR_PATTERN_COMPARISON`. Every row is `KERNEL_CONFIRMED`; these single-defect rows make no diagnostic-precedence claim.

The checked Lean surface represents the direct field plus String-constant form, so it projects the complete measured left-kind and pattern-source classes while the structurally excluded right-field shape remains canonical static knowledge rather than a fabricated local refusal.

At the same clean a12-dmkits revision and version set, a 13-case `rule check` matrix establishes the raw-String `Length` final-rule boundary in both operand orders. Strict whole-rule bounds `5`, `-1`, `2147483647`, and `-2147483648` are admitted. Fractional `0.5`, integral-valued decimal spelling `5.0`, and the one-step overflows `2147483648` and `-2147483649` each report only `MVK_INTERNAL_ERROR`; non-strict and Boolean-composed controls instead report only `MVK_INVALID_LENGTH_OF_RAW_TYPE`; an ordinary String control remains admitted. Every row is `KERNEL_CONFIRMED`. The accepted forward rule persisted to a separate ignored output copy, read back structurally as `Length(/Claim/IncidentNote) > 5`, and survived whole-model checking.

The deterministic observation artifact has SHA-256 `38c9f0ed9d719e1262a8e216542fe82b7a1a9d627422076abb8592d617864841`, the caller-tagged matrix has SHA-256 `591d1b3fea4a51aaef12b7f863fc168467e43b674662d881f633cd8f415fcdf6`, and the dmtool-authored model has SHA-256 `cfc26cc4ba6816522d44ac0f69b69ef3274f206721bebaffea8b1c419fc34656`. A second clean capture reproduced the artifact bytes exactly. Two later direct checks on the same clean route are separate from that hashed artifact: `Length(IncidentNote) > 100` is admitted, which guards an ordinary decimal-point-free trailing-zero token, while `Length(IncidentNote) >= 5.0` reports only `MVK_INVALID_LENGTH_OF_RAW_TYPE`, which establishes shape-before-bound diagnostic precedence. Both are `KERNEL_CONFIRMED`.

This is static admission evidence only: it establishes the accepted literal domain and diagnostic identities, not a causal account of the Kernel's internal-error route.

At the clean a12-dmkits baseline `5c1f04925c4f6d88641eb95bedd411fbbf79b416`, the unchanged source-shipped `dmtool` 0.10.0 route extends that focused placement measurement to stored Enumeration, named category conversion, and `RangeAsNumber`. A four-row `/Order/Lines` model with `StoredAmount := FieldValueAsNumber(Band)`, `CategoryAmount := FieldValueAsNumber(Band -> Numeric)`, and `RangeAmount := RangeAsNumber(Code, 2, 4)` passed the real-kernel `model check` gate as `KERNEL_CONFIRMED`. The public interpreter separated stored token `1` from category token `.5`, produced `-1.25` and `12.50` from the category projection, and separated range selections `012 → 12`, `1XB → 0`, and `123 →` target error against maximum `99`; out-of-domain Enumeration and overlength String rows cleared their respective targets.

The route still omits repetition coordinates and was not a focused dynamic-Groovy/generated-Java run, so it calibrates selected projection, exact range value classes, and outcome classes while exact row identity remains internally locked.

Accepted [`SPEC-2026-07-23-15`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-07-23-15--computation-selects-operand-indices-and-shares-full-default-staging) at a12-dmkits revision `0904542e70b7f0adbb3de4cbab37323059f90ec3` supplies a maintained same-scope repeatable `SkuLength := Length(Sku)` computation. Its dynamic-Groovy/generated-Java route confirms authorability and selected-index formal poison, and the JVM/Node peer retains the cause-blind cleared projection. The focused clean Length value classes were not observed through that result route: UTF-16 code-unit counting after CRLF normalization is separately source-grounded and differentially locked at the shared validation operator, while the exact repeatable zero/value/target-error/address matrix remains internal Lean coverage.

<a id="src-repeatable-number-aggregate-cascade"></a>
#### Repeatable Number producer and aggregate cascade

For the current drill route, the clean a12-dmkits HEAD `cd43c249a88874bf5688a45060e208a0dcd20782` retains [`ComputeStarAggregatePoisonDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ComputeStarAggregatePoisonDiffTest.kt), last modified at reachable revision `27d42be1734fbfdf3217869193e24d9b69f95795`, which tri-checks the fresh two-row `Helper := Price` values through `MaxValue(Pricing*/Helper)` and both `MaxValue` and `Sum` poison transport.

The same clean HEAD retains [`CascadeThroughStarDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/CascadeThroughStarDiffTest.kt), last modified at reachable revision `c1f41dbcb5afd9f1912af96d9ae165936a733820`, which independently separates a downstream `Sum` of freshly computed row targets from the stale stored-empty account on dynamic Groovy and the interpreter. [`ComputeCascadeDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ComputeCascadeDiffTest.kt), last modified at reachable revision `49a4304426cb9793635fac8e50ded5519820816c`, directly asserts on dynamic Groovy, generated Java, and the interpreter that `StockOnHand := Quantity * 2` is visible freshly to the later scalar `BackorderQuantity := StockOnHand + 1`.

[`MultiComputationFieldDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/MultiComputationFieldDiffTest.kt), last modified at reachable revision `8b0faad13d0e87b4cf16d335447dfd9f63da952a`, directly asserts the false-guard fall-through and selected-row termination branches on dynamic Groovy, generated Java, and the interpreter.

[`FieldValueAsStringComputeDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/FieldValueAsStringComputeDiffTest.kt), last modified at reachable revision `8af57d6173bdf32b7eee4f15f3c38b84a96a479e`, and [`ComputePoisonReadDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ComputePoisonReadDiffTest.kt), last modified at reachable revision `b9805f9ef5935e0856d45cc9cdcfe6a0e355cc67`, separately establish the Number-to-String computation primitive and seeded reached dependency poison. The poison matrix seeds every dependent target, asserts its expected cleared outcome directly against dynamic Groovy, and compares generated Java and the interpreter without filtering.

All four component families replay green at the same clean HEAD on all three engines, but none crosses an aggregate plus both scalar families. [`ComputeFieldDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ComputeFieldDiffTest.kt), last modified at reachable revision `19e562ca5fd066a10f291f34ad7715612a2aa8eb`, contains the ordered field-pair product `Amount := Qty * Price`, downstream `Total := Sum(Lines*/Amount)`, and one later scalar consumer over clean, poisoned, and empty operands. It establishes the interpreter's `computeField` versus whole-compute projection invariant, but its kernel comparison filters interpreter results to the pointers the dynamic-Groovy route happened to report.

A missing `Amount`, `Total`, or later target therefore does not fail, generated Java does not run on that model, and none of its three scenarios supplies non-vacuous external correspondence for the field-product cascade or any scalar suffix. The assignment-scale gate remains the canonical source-grounded rule in [`spec/09-computations.md`](../../spec/09-computations.md): the target declaration scale must equal the direct field's derived scale. The direct retained routes expose exact result pointers and public value/CLEARED buckets, but they do not expose the internal overlay representation, rich poison cause, absent/value/target-error matrix, or V2 identity; the field-product extension and exact aggregate-to-scalar compositions remain external evidence pending in full.

The same clean a12-dmkits HEAD retains [`HavingOverComputedDependencyDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/HavingOverComputedDependencyDiffTest.kt), last modified at reachable revision `c24d81ac7697020639a9a95165b647e8e370954d`. Its expected maps are asserted directly against dynamic Groovy before generated Java and the interpreter are compared, so the kernel target pointers cannot disappear vacuously. Stale Boolean flag seeds separate freshly computed selection in both directions: an ended row's cleared flag drops a commission that its stale `true` seed would keep, while an active row's fresh `true` keeps a commission despite stale `false`; a formally invalid flag producer separately clears the downstream filtered sum.

This establishes computed Boolean state transport into an aggregate `Having` filter and the public poison outcome on both kernel strategies. The Lean route specializes the same filter-read seam to a computed Number used only by a Number comparison while aggregating a different clean Number field; that type specialization, its rich cause, and its exact addresses remain internally locked and external correspondence pending.

<a id="src-repeatable-numeric-wrappers-extrema"></a>
#### Repeatable numeric wrappers and extrema

At clean a12-dmkits revision `a955143d6ca4197c27f46078a4c48e2f64bd59f5`, the unchanged source-shipped `dmtool` authored the same-scope repeatable scale-2 computation `/Cart/Lines/Magnitude := Abs([Price])`; its real-kernel terminal gate accepted that exact placement. Re-targeting the expression to the scale-0 `/Cart/Lines/Qty` field was rejected with `MVK_INVALID_COMPARE_DEC_PLACES`, locating the scale-preserving authoring boundary rather than inferring it from nonrepeatable `Abs` coverage. The public interpreter `model compute` route then returned `5.25`, `3.5`, `0`, CLEARED, and target-error `12.34` for negative, positive, absent, formally invalid, and above-maximum source rows.

That runtime route exposes repeated field paths but not row coordinates and does not execute dynamic Groovy or generated Java, so exact runtime addresses remain internal Lean coverage; the static acceptance/rejection result is the real-kernel observation.

The same unchanged a12-dmkits revision and source-shipped route admit all three same-scope repeatable direct-Number rounders: `/Cart/Lines/Down := RoundDown([Source])`, `/Cart/Lines/Up := RoundUp([Source], 1)`, and `/Cart/Lines/Accounting := RoundAccounting([Source])`. Omitted places derive scale 0; explicit places derive that exact target scale. Re-targeting `RoundUp([Source], 1)` to a scale-0 Number is rejected with `MVK_INVALID_COMPARE_DEC_PLACES`. The public interpreter route distinguishes floor, ceiling, and half-up on negative values and ties, and for each mode preserves empty-as-zero, formal-poison clearing, and target rejection.

Its repeated outcome payload still omits row coordinates and executes only the interpreter; the maintained nonrepeatable `RoundDiffTest` independently calibrates the arithmetic modes against dynamic kernel execution but does not cover this exact repeatable computation route.

At the same clean a12-dmkits revision, an unchanged source-shipped probe over an unindexed repeatable group admits `/Probe/Rows/Minimum := Min([A], [B])` and `/Probe/Rows/Maximum := Max([A], [B])` where `A` has scale 0 and `B` scale 2; both target the derived scale 2. Re-targeting the same `Min` to scale 0 is rejected with `MVK_INVALID_COMPARE_DEC_PLACES`. The public interpreter reports `Min(3,5.25)=3`, `Max(3,5.25)=5.25`, lets an empty operand compete as numeric zero on either side, returns zero when both are empty, and clears both targets for malformed left or right operands. The result route omits row coordinates and is interpreter-only, while maintained nonrepeatable differentials independently lock scalar extrema arithmetic and authored order; the exact unindexed repeatable address and source-relative result matrix remains internal Lean coverage.

A second unchanged-source probe at that revision admits `/Probe/Rows/Minimum := Min([A], [B], [C])` and the corresponding `Max`, with source scales 0, 2, and 1 and the target at their derived maximum scale 2. Retargeting the three-source call to scale 1 is rejected with `MVK_INVALID_COMPARE_DEC_PLACES`. The public interpreter confirms that every missing direct Number competes as zero, malformed or declared-constraint invalidity in any reached position clears the target, the third operand can determine either extremum, and a selected value can still fail the target policy. The route remains cause-blind and omits row coordinates; Lean separately locks authored first-poison stopping, exact target addresses, and source-relative result partitioning.

A separate real-kernel `computation add --dry-run` discriminator at the same clean revision gives the third operand the uniquely largest scale: source scales 0, 1, and 3. The scale-3 target is accepted, while an otherwise identical scale-2 target is rejected with `MVK_INVALID_COMPARE_DEC_PLACES` naming derived right-hand scale 3. This isolates later-source participation in the maximum-scale fold; the earlier 0/2/1 runtime matrix establishes third-source value and poison reachability but does not establish that scale mechanism by itself.

A third unchanged-source probe at that revision closes the nonempty-list lower bound: the real-kernel `model check` gate admits repeatable `Min([A])` and `Max([A])` into a scale-2 target and rejects a scale-1 target with `MVK_INVALID_COMPARE_DEC_PLACES`. The separate kernel-free `model compute` route confirms the a12-dmkits interpreter's single-source value identity, empty-as-zero, formal-poison clearing, and ordinary target rejection. That runtime result is interpreter evidence only; it is not a dynamic-Groovy or generated-Java observation.

The immediate-literal addressed extension was measured at the same clean a12-dmkits revision with source-shipped `dmtool` 0.10.1 through its structured workflow: `model new`, one atomic `apply`, `model describe`/`field read`, and a caller-tagged `batch` of `computation add --dry-run` preflights, without inspecting or editing raw model JSON. The real kernel accepts repeatable `Min([A], 1.250)`, `Min(1.250, [A])`, `Max([A], -1.250)`, and `Max([A], [B], -1.250)` into a scale-3 target; it rejects `Min([A], 1.250, 2)` with `MVK_TOO_MANY_CONST_FOR_CALC_OP` and the first call into a scale-2 target with `MVK_INVALID_COMPARE_DEC_PLACES` naming expression scale 3.

Persisting the first accepted case to a separate structured output, reading it back, and running `model check` reconfirmed the exact operation with `KERNEL_CONFIRMED`. The kernel-free `model compute` route corroborated field/literal order, missing Number zero, negative selection, and formal-invalid clearing, while `model eval` located the malformed, above-maximum, and excess-scale source rows exactly. Because each `model compute` item still repeats only the declaration path and omits repetition coordinates, those runtime values remain exploratory interpreter corroboration rather than row-specific retained evidence; exact addressed execution is locked internally in Lean.

The first operation-valued addressed extension was measured through that same structured route and exact clean revision. The real kernel accepts repeatable `Min(Abs([A]), [B])` and `Min([B], Abs([A]))` into a scale-2 target, accepts `Min(Abs([A]), [B], 1.250)` into a scale-3 target, rejects the first call at target scale 1 with `MVK_INVALID_COMPARE_DEC_PLACES`, and rejects `Min(Abs(1.250), [B])` with `MVK_NO_CONST`. Persisting and reading back the first accepted call preserved its exact authored structure, and `model check` returned `KERNEL_CONFIRMED`.

The kernel-free runtime route corroborated negative magnitude, positive identity, missing-Number zero, poison in either field position, post-selection target rejection, and a negative direct-field winner; `model eval` independently located both malformed inputs. The unchanged missing-coordinate limit keeps those runtime rows exploratory, while the static authoring and rejection matrix is real-kernel evidence.

The sibling operand-local rounding probe used that unchanged structured model and exact revision. The real kernel accepts both positions of `RoundDown([A], 1)` beside `[B]`, plus the corresponding nested `RoundUp` and `RoundAccounting`, into a scale-1 target. It accepts a scale-3 literal sibling, rejects the scale-1 expression at target scale 2 with `MVK_INVALID_COMPARE_DEC_PLACES`, rejects `RoundDown(1.250, 1)` with `MVK_NO_CONST`, and rejects places 15 with `MVK_INVALID_DIGIT_FOR_ROUNDING`. Persisted read-back preserved `Min(RoundDown([A], 1), [B])`, and `model check` returned `KERNEL_CONFIRMED`.

The kernel-free floor route corroborated signed ties, missing zero, poison in either position, target rejection, and a negative direct-field winner but retained the same missing-coordinate limit; exact addresses, all three mode distinctions, first-poison cause, and consumer identity remain internal Lean locks.

<a id="src-operation-valued-extrema"></a>
#### Operation-valued extrema

At clean a12-dmkits revision `27c1e583eae27dfba4beabe885f7d1b25f11a460`, dmtool `0.11.1` measures the first arithmetic-valued addressed extremum operand through the repaired structured route. Real-kernel dry-runs accept `Min([A] + [B], [C])`, `Min([C], [A] + [B])`, and `Min([B] + [A], [C])` at derived scale 2, the matching `Max([A] + [B], [C])`, plus `Min([A] + [B], [C], 1.250)` at scale 3 and the independently admitted literal-bearing child `Min([A] + 1.250, [C], 2)`. The scale-2 expression into a scale-1 target is rejected with `MVK_INVALID_COMPARE_DEC_PLACES`; self-reference through `[Target] + [A]` is rejected with `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`. Persisted read-back preserves `Min([A] + [B], [C])`, and `model check` returns `KERNEL_CONFIRMED`.

Row-addressed `DM_INTERPRETER` output separates clean addition selection, a direct-field winner, empty left/right/both, poison in either inner operand, and post-selection target rejection. The result remains cause-blind, so exact inner first-poison ordering is locked internally rather than inferred from `cleared`.

At clean a12-dmkits revision `112169fa4ce71d9b45c0b276e0406e275f70354d`, dmtool `0.11.1` extends the same structured matrix to operand-local subtraction. Real-kernel dry-runs accept `Min([A] - [B], [C])` in both outer positions, reversed inner order, the matching `Max`, a scale-3 literal sibling, and both a literal-bearing and literal-only subtraction child. A scale-2 subtraction expression into a scale-1 target is rejected with `MVK_INVALID_COMPARE_DEC_PLACES`, while self-reference through `[Target] - [A]` is rejected with `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`. Persisted read-back preserves `Min([A] - [B], [C])`, and `model check` returns `KERNEL_CONFIRMED`.

Row-addressed `DM_INTERPRETER` output separates exact subtraction, a direct-field winner, empty left/right/both, poison class in either inner operand, and post-selection target rejection. Independent Lean execution reproduces those addressed values while additionally retaining the inner first-poison cause that the public `cleared` result does not expose.

At clean a12-dmkits revision `b852a2867bc33d621cb4bee0cc692ffa8db5f042`, dmtool `0.11.1` with built/runtime kernel `30.8.1` closes operand-local multiplication through the same structured route. Real-kernel dry-runs accept `Min([A] * [B], [C])` in both outer positions, reversed inner order, and the matching `Max` into the summed scale 3 for a scale-2 and a scale-1 source, plus a scale-4 literal sibling at a scale-4 target, a scale-0 inner source at scale 2, and the separately admitted literal-bearing `Min([A] * 1.5, [C])` and literal-only `Min(1.5 * 2, [C])` children. The same expression is rejected at both a scale-2 and a scale-4 target with `MVK_INVALID_COMPARE_DEC_PLACES` naming derived scale 3, so the product's derived scale is exact rather than a lower bound.

A scale-matched `Min([Target] * [Z], [C])` is rejected with `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`; an unmatched-scale self-reference never reaches that gate. Persisted read-back preserves `Min([A] * [B], [C])` with an empty `errorCodesToSuppress`, and `model check` returns `KERNEL_CONFIRMED`, so exact summed scale needs no warning suppression. Row-addressed `DM_INTERPRETER` output separates the product winning, a direct-field winner, empty left/right/both as zero, a formally invalid operand as cleared, and post-selection target rejection at a capped target, with `model eval` locating the malformed input at its exact instance pointer. The result stays cause-blind, so inner first-poison ordering remains internally locked.

<a id="src-aggregate-repeatable-suffixes"></a>
#### Aggregate-to-repeatable suffixes

At reviewed a12-dmkits revision `975a2e78602eaf26126168cd203bd84738885322`, source-shipped dmtool `0.13.0` authored and kernel-checked the exact one-level three-stage route represented by Lean: `/Order/Lines/Produced := [Candidate]`, `/Order/Total := Sum(Lines*/Produced)`, then unconditional `/Order/Lines/Amount := [../Total]`. The authorized `:adapter:kernelProbe` artifact runs four separating documents through both `kernel-groovy-dynamic` and `kernel-java-static`. Value-valid stale targets prove fresh transport: candidates `2,3` replace row producers `4,6`, aggregate to `5` instead of stored total `9`, and fan out `Amount=5` to both rows instead of stored `8,9`. With one empty candidate, the second producer becomes `0`, `Total=2`, and the unconditional suffix writes `Amount=2` to both instantiated rows.

A malformed second candidate preserves the clean first producer, clears the second producer and root aggregate, and clears both suffix targets; no rows produce only `Total=0`. After inactive inherited branches were removed through dmtool, the minimal model contains only the producer, aggregate, and suffix computations; the unchanged request reproduced the retained artifact byte-for-byte. Both strategies agree on every computation outcome and formal-operand partition.

The exact request, dmtool-authored model, and result artifact are retained under [`aggregate-repeatable-direct-number`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-direct-number/); their SHA-256 values are request `e4d09507cea32b83351338691a94bd2f76c8c458463358d38e2d0846c69e4478`, model `bb7ae8dfc5ea70c466016845a816caf52d7ff4e1ed1230d15dd1594669f143d9`, and artifact `e63a1bad2f725c2c7be1651b7edd69a657dfe9ce1c2f97007fb4ca2c9e40e6f6`. This closes external correspondence for the exact one-level direct Number producer-to-aggregate-to-repeatable-suffix composition only; nested scopes and general scheduling remain unmeasured, and other binary operators remain externally uncalibrated.

The corresponding fixed two-step repeatable Number suffix is retained at the same clean a12-dmkits revision and version set. dmtool extends the exact prefix above with `Amount := [../Total]` guarded by `CurrentRepetition(/Order/Lines) > 0`, followed by unconditional `/Order/Lines/Final := [Amount]`. Value-valid stale `Final` seeds separate fresh row-local dependency transport: the two-value case produces `Amount=5` and `Final=5` at both rows, the value-plus-empty case produces `2` at both targets and both rows after the empty producer becomes `0`, malformed second-row input clears the aggregate and both suffix targets at both rows while preserving the clean first producer, and no rows again produce only `Total=0`.

After the same inactive inherited branches were removed through dmtool, the minimal model contains only the producer, aggregate, and two suffix computations; the unchanged request reproduced the retained artifact byte-for-byte. Both kernel strategies agree on every outcome and formal-operand partition. The exact bundle is retained under [`aggregate-repeatable-fixed-number-chain`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-fixed-number-chain/); SHA-256 values are request `146eeb7ce659cb889e1181b9e847ebe788dd2103e9a183ba69295facf5e2d12b`, model `04688c1c6a489b8d05ab383f95eb0a2c47b34dce00f250441fe80dc638044f75`, and artifact `594bd3d85e4544c3438e8916c81e0159114d667011fa5a504d3a90ea630c6207`.

This closes only the exact one-level fixed two-step Number suffix composition; nested scopes and general scheduling remain unmeasured, and other binary operators remain externally uncalibrated.

The fixed Number-to-String suffix is retained at the same clean a12-dmkits revision and version set. dmtool authors the exact guarded `/Order/Lines/Amount := [../Total]` edge followed by unconditional `/Order/Lines/Label := FieldValueAsString(Amount)`, with no unrelated computation in the retained model. Stale String seeds prove fresh cross-family transport: the two-value case replaces both labels with `"5"`, the value-plus-empty case replaces both with `"2"` after the empty producer becomes numeric `0`, and malformed second-row input clears the second producer, aggregate, both Number targets, and both String targets while preserving the clean first producer.

The malformed computation's formal-operand partition contains only `zahlHatUngueltigeZeichen` on its candidate operand; no rows again produce only `Total=0`. Both kernel strategies agree on every outcome and formal-operand partition. The exact bundle is retained under [`aggregate-repeatable-fixed-number-to-string-chain`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-fixed-number-to-string-chain/); SHA-256 values are request `991e54738d0294c71997cb068c48a55201bedc52e0b8c80783233c1bdf28a0a7`, model `ede690324177075cae9edca994ae7a6720addca95ebca000a28a6d912ce3a3db`, and artifact `03e2bd52b1e8a55a34d8f10b32a5d60ffdf84c2714e9a73666fd768fc1ea750d`.

This closes only the exact one-level fixed Number-to-String suffix composition; nested scopes and general scheduling remain unmeasured, and other binary operators remain externally uncalibrated.

The direct repeatable String suffix is retained at the same clean a12-dmkits revision and version set. Its minimal dmtool-authored model contains only `/Order/Lines/Produced := [Candidate]`, `/Order/Total := Sum(Lines*/Produced)`, and unconditional `/Order/Lines/Label := FieldValueAsString(../Total)`. Stale labels prove fresh direct cross-family fan-out: the two-value case writes `"5"` to both rows, the value-plus-empty case writes `"2"` to both rows after the empty producer becomes numeric `0`, and malformed second-row input clears the second producer, aggregate, and both String targets while preserving the clean first producer. The malformed computation's formal-operand partition again contains only `zahlHatUngueltigeZeichen` on its candidate operand; no rows produce only `Total=0`.

Both kernel strategies agree on every outcome and formal-operand partition. The exact bundle is retained under [`aggregate-repeatable-direct-string`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-direct-string/); SHA-256 values are request `78ec9aa66c0bdf552c829051c03dba2162254ca3a37c067a38d6592c48ff0eac`, model `8ed2398b5f2ae1d517a57de5ebd08fbe533538bc3b9a30b7085739d9a4c5b9a7`, and artifact `fd8fac8911b634b09708a43544fe6ddeb22315168a709f489f4a3f282b06b30a`. This closes only the exact one-level direct repeatable String suffix composition; nested scopes and general scheduling remain unmeasured, and other binary operators remain externally uncalibrated.

The ordered binary Number suffix is retained as a subtraction representative at the same clean a12-dmkits revision and version set. The minimal dmtool-authored model adds the two independent target computations `TotalMinusOffset := [../Total] - [Offset]` and `OffsetMinusTotal := [Offset] - [../Total]` to the shared producer and aggregate prefix. Stale target seeds and asymmetric offsets prove authored operand order: fresh `Total=5` yields `4/-4` at row one and `1/-1` at row two; an empty second producer makes `Total=2` and yields `1/-1` then `-2/2`. A malformed second producer clears the aggregate and all four suffix targets, while a malformed second-row offset leaves the aggregate and both first-row results at `5`, `4`, and `-4` and clears only the two second-row targets.

Those rows separate aggregate dependency poison from row-local operand poison in both orders. Each malformed computation's formal-operand partition contains only the corresponding `zahlHatUngueltigeZeichen` candidate or offset finding; no rows again produce only `Total=0`. Both kernel strategies agree on every outcome and formal-operand partition. The exact bundle is retained under [`aggregate-repeatable-ordered-binary-subtraction`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-ordered-binary-subtraction/); SHA-256 values are request `14c46c2cc48a946862fe339de22727ed915f9c0fd9f658db76bbafff5129cc79`, model `d66822a374e7d2574edb04fa795c01438612e017b2125499e168e65b58230326`, and artifact `03f2af05b72153f8c41e9b43cbf091ba4db97f4aecf9f9e82c4eb80bb40ded96`.

This closes only the exact one-level ordered subtraction suffix composition in both operand orders. Other binary operators, nested scopes, and general scheduling remain unmeasured.

The warning-suppressed division suffix is retained at the same clean a12-dmkits revision and version set. A minimal scale-2 dmtool-authored model carries `TotalOverDivisor := [../Total] / [Divisor]` and `DivisorOverTotal := [Divisor] / [../Total]`, each with `errorCodesToSuppress: ["MVK_INVALID_COMPARE_DEC_PLACES"]`. Fresh candidates `30.00` and `50.00` produce `Total=80.00`: aggregate-first division yields accepted `8.00` and `4.00`, while row-first division yields ERRORED `0.125` at row one through the warning-suppressed no-fit store and accepted `0.25` at row two. With an empty second producer, `Total=30.00`, the aggregate-first results are accepted `3.00` and `1.50`, and both row-first repeating quotients are retained ERRORED.

Aggregate poison clears all four suffix targets; row-local divisor poison clears only the two second-row targets while retaining the first row's accepted `8.00` and ERRORED `0.125`. A zero divisor clears only the aggregate-over-zero target with `berechnungsWertFehler`, while zero-over-aggregate stores `0.00`; the second row remains `4.00` and `0.25`. No rows produce only `Total=0.00`. Both kernel strategies agree on every outcome and formal-operand partition.

The exact bundle is retained under [`aggregate-repeatable-warning-suppressed-division`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-warning-suppressed-division/); SHA-256 values are request `0cd24acdde54230363363240583f4bd34088e7249132f1f6754e58b5949c1ad9`, model `ddcf57b7eaf1e41286d761764b2f4b217cef17f183c7690a1e234c63a44d9ada`, and artifact `cc716511e5b131f16848e5d63c13e339b9c128ebf878455a8423a4cf39672592`. This closes the exact one-level warning-suppressed division suffix representative in both operand orders and completes retained Kernel correspondence for the six bounded aggregate-to-repeatable-suffix forms. Other binary operators, unsuppressed division, nested scopes, and generic scheduling remain unmeasured.

The aggregate-seeded finite mixed String/Number suffix is retained at the same clean a12-dmkits revision and version set. Its minimal dmtool-authored model contains only `/Order/Lines/Produced := [Candidate]`, `/Order/Total := Sum(Lines*/Produced)`, a root String table whose false first alternative would write `"WRONG"` and whose selected second alternative writes `Label := FieldValueAsString(Total)`, and a root Number table guarded by the computed label that writes `Doubled := FieldValueAsNumber(Label) + [Total]`. Value-valid stale seeds prove every stage reads fresh completed state: candidates `2,3` replace row producers `4,6`, aggregate to `5` instead of stored `99`, fall through the false first String guard to replace label `"8"` with `"5"`, and replace doubled `7` with `10`.

With one empty candidate, the second producer becomes `0` and the root chain yields `Total=2`, `Label="2"`, and `Doubled=4`. A malformed second candidate preserves the clean first producer and clears the second producer, aggregate, String suffix, and Number suffix; its formal-operand partition contains only `zahlHatUngueltigeZeichen` on that Candidate. No rows still execute the root suffix after the zero aggregate, yielding `Total=0`, `Label="0"`, and `Doubled=0`. Both kernel strategies agree on every outcome and formal-operand partition.

The exact bundle is retained under [`aggregate-repeatable-finite-mixed-string-number`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-finite-mixed-string-number/); SHA-256 values are request `ada462c166ff6354d32150dc963ca1d1f4ebc13923d5e35393ebc24330b4e68a`, model `bcb7334663e72b0e71b6225af4cb2fe4f54b88f61cfa43776400377a4ba80d1e`, and artifact `557acdff24d5bb924c1e33abec458ee0533708bdac00581cc3c09594012d5e38`. This closes correspondence only for the named one-level aggregate-seeded finite mixed route in its supplied suffix order. Independent mixed steps, other alternative states, wider scalar families, nested scopes, application, and generic scheduling remain unmeasured.

The aggregate-seeded finite Number suffix is retained at the same clean a12-dmkits revision and version set. Its minimal dmtool-authored model contains only `/Order/Lines/Produced := [Candidate]`, `/Order/Total := Sum(Lines*/Produced)`, `/Order/Doubled := [Total] + [Total]`, and `/Order/Tripled := [Doubled] + [Total]`. Stale seeds prove that the supplied Number suffix reads completed state rather than the input document: candidates `2,3` replace row producers `4,6`, aggregate to `5` instead of stored `9`, and replace `Doubled=7` and `Tripled=5` with `10` and `15`. With one empty candidate, the second producer becomes `0` and the chain yields `Total=2`, `Doubled=4`, and `Tripled=6`.

A malformed second candidate preserves the clean first producer and clears the second producer, aggregate, and both prefilled suffix targets; its formal-operand partition contains only `zahlHatUngueltigeZeichen` on that Candidate. No rows still execute the root suffix after the zero aggregate, yielding `Total=0`, `Doubled=0`, and `Tripled=0`. Both kernel strategies agree on every outcome and formal-operand partition.

The exact bundle is retained under [`aggregate-repeatable-finite-number-run`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-finite-number-run/); SHA-256 values are request `6aed2debc80e7ea2478823426ba85268cfeddaf07c9b4240b0463d020ecfbd33`, model `98679733e0e8900f30db7b794e80ce25bab03642fabb14423f4094c2e7af2895`, and artifact `f096ce693f8447262cb8e42d843396830581aaffd651bddcb6deea1427ceb1fd`. This closes correspondence only for the named one-level aggregate-seeded finite Number route and its final source-relative outcomes. It does not establish private kernel phase order, outcome emission order, structural-failure reachability, nested scopes, application, or generic scheduling.

At the same clean a12-dmkits revision and dmtool/kernel version set, the retained one-level model contains `/Order/Total := Sum(Lines*/Candidate, Lines*/Produced)` beside `/Order/Lines/Produced := [Candidate]`. Both kernel strategies agree on the separating runtime matrix. Candidates `2,3` replace stale Produced values `4,6`, so the two aggregate operand streams total `10` rather than the stale-state account `15`, and the supplied Number suffix yields `Doubled=20` and `Tripled=30`. With the second Candidate empty, its fresh Produced value is `0` and the chain yields `Total=4`, `Doubled=8`, and `Tripled=12`. A malformed second Candidate preserves the first Produced value, clears the second Produced value and all three root targets, and reports only `zahlHatUngueltigeZeichen` on that Candidate.

With no rows, the aggregate and both suffix targets are zero. The exact bundle is retained under [`aggregate-repeatable-multi-operand-number-run`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-multi-operand-number-run/); SHA-256 values are request `11f5972872f65eb5c1603f1b551cc31ca660493d68b46bf2d3a33b11dff9e4bc`, model `8f720c5bab7987ca48e5a2df3f331f101408653ce2927d23b553bf2b54eed427`, and artifact `6e20bd41b6d9341a8a2f16f9edc85997b248c3db04e108367ba4b6459a34c636`. This closes correspondence only for the named one-level two-operand plain-star aggregate route with the computed producer in the second authored operand and its final source-relative outcomes.

It does not establish wider arity, mixed filtered/plain operands, cross-group operands, nested scopes, private kernel phase or emission order, structural-failure reachability, application, or generic scheduling.

At the same clean a12-dmkits revision and version set, two retained one-level models place the exact filtered wildcard `Lines*/Produced Having [Lines/Candidate] < [Lines/Produced]` either after or before the plain wildcard `Lines*/Candidate`, beside `/Order/Lines/Produced := [Candidate]`. Both kernel strategies agree in both authored orders. Candidates `2,-3` replace stale Produced values `4,6`; the fresh equality makes the filtered slot select no row, so the plain stream yields `Total=-1`, `Doubled=-2`, and `Tripled=-3` instead of the stale-filter account. With the second Candidate empty, fresh zero remains unselected and the chain yields `2,4,6`.

A malformed second Candidate preserves the clean first producer, clears the second producer and all three root targets, and reports only `zahlHatUngueltigeZeichen` on that Candidate. With no rows, all three root targets are zero. The exact plain-then-filtered bundle is retained under [`aggregate-repeatable-mixed-plain-filtered-number-run`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-mixed-plain-filtered-number-run/); SHA-256 values are request `9b216c01436780b5b9b1a8022db25e459e2106ceabbad7bd065cb792785aacc6`, model `3d7211f6d9646bca3c843cdca0304d5fa1096392eaa9aff4d9e6cacc78949787`, and artifact `13ba18279ded4acccc9068ba0567a23ac9f5f6d55403197becb6ba5474803688`.

The exact filtered-then-plain bundle is retained under [`aggregate-repeatable-mixed-filtered-plain-number-run`](../../evidence/kernel-30.8.1/captures/aggregate-repeatable-mixed-filtered-plain-number-run/); SHA-256 values are request `1f55e9779d38c88712e2e205c3afef5b1a6487c8a98affa0f1bf1204d92b6796`, model `a71af1c1ca98f183f9d07a4098c87b5a896cbe5ce2f0691b2aa929bcf6a85096`, and artifact `728a4c2f6b2aab1a36eb990c99c4be1c721583e5d85751e8ab2973267734863c`. This closes correspondence only for the named direct producer, one plain star, one filtered star with that field-to-field filter, both operand orders, and final source-relative outcomes.

Wider arity, other filter expressions, binary producers, cross-group operands, nested scopes, private kernel phase or emission order, structural-failure reachability, application, and generic scheduling remain unmeasured.

<a id="src-nested-current-repetition-cascades"></a>
#### Nested `CurrentRepetition` cascades

The fixed two-level `CurrentRepetition` Number-to-String chain is retained at the same clean a12-dmkits revision and version set. A minimal dmtool-authored `/Order/Lines/Entries` model contains only `First := [Base]` guarded by `CurrentRepetition(/Order/Lines/Entries) > 0` and `Second := FieldValueAsString(First)`. Two outer rows establish that the terminal coordinate is not the complete address: exact coordinates `[1,1]` and `[2,1]` independently replace stale targets with `7/"7"` and `11/"11"`, while `[1,2]` becomes `8/"8"`.

An empty second leaf under the first outer row becomes only `0/"0"`; a malformed first leaf under the second outer row clears only that exact Number/String pair while the equally numbered leaf under the first outer row remains `7/"7"`, and the formal-operand partition contains only `zahlHatUngueltigeZeichen` at `/Order/Lines[2]/Entries/Base`. An instantiated outer row with no concrete Entries contributes no computation outcome. Both kernel strategies agree on every outcome and formal-operand partition.

The exact bundle is retained under [`nested-current-repetition-number-to-string-chain`](../../evidence/kernel-30.8.1/captures/nested-current-repetition-number-to-string-chain/); SHA-256 values are request `12b471784b824e05e13c635d82087322a680d95533f921be4e04d82cd7f0dd50`, model `c239a63e220dc060d357f970ab5fd2c65d55b3532db4bb76c75ebbb8451a89e0`, and artifact `01d9d510f19cfe690f570d3af87513cba2ad4db434a13adbfdfe7e48d6a8462e`. This closes correspondence only for the named two-level scale-0 Number-to-String fixed chain. The Number-only and String-to-Number nested chains, wider depths and profiles, application, and generic scheduling remain unmeasured.

The fixed two-level three-step alternating `CurrentRepetition` chain is retained at the same clean a12-dmkits revision and version set. The dmtool-authored `/Order/Lines/Entries` model extends the preceding shape with `Third := FieldValueAsNumber(Second)`, using the exact `[0-9]+` String profile needed by that conversion. Exact coordinates `[1,1]` and `[2,1]` share terminal coordinate `1` yet independently replace all three stale targets with `7` and `11`, while `[1,2]` replaces them with `13`. An empty Base at `[2,1]` writes `0`, `"0"`, and `0` only at that leaf. A malformed Base at the same address clears only its three-step chain, preserves the clean `[1,1]` and `[1,2]` chains, and reports only `zahlHatUngueltigeZeichen` at `/Order/Lines[2]/Entries/Base` in the formal-operand partition.

An instantiated outer row without concrete Entries contributes no computation outcome. Both kernel strategies agree on every addressed value and formal-operand partition. The exact bundle is retained under [`nested-current-repetition-alternating-chain`](../../evidence/kernel-30.8.1/captures/nested-current-repetition-alternating-chain/); SHA-256 values are request `5d79d963726c98e16c11a26676ab93c10c9f18a96fd426a8cf0903f07d2c32bf`, model `a5bcf76be319115eb7aacd8b176374e872d794875eb6495a33a2b0968cf0a78a`, and artifact `414782d04e8aa155ec7e110850a61a0fd1e92ade6fae7d424858bf124849188d`. This closes correspondence only for final addressed outcomes of the named two-level scale-0 alternating chain.

It does not establish private kernel phase order or outcome emission order; the Number-only and String-to-Number nested chains, wider depths and profiles, application, and generic scheduling remain unmeasured.

At clean a12-dmkits revision `25e048ea73e10bd58c6950b1d6104dd5809535df`, source-shipped dmtool `0.13.0` extends that exact two-level alternating model with an outer Enumeration computation read back as `FirstFilledValue(DirectChoice, Entries*/Choice Having [Entries/Third] == [$Limit] And [Entries/First] == [$Limit])`. Whole-model checking is `KERNEL_CONFIRMED`.

In the fresh document, stale inner targets `130/"130"/1300`, `70/"70"/700`, and `130/"130"/1300` become `13/"13"/13`, `7/"7"/7`, and `13/"13"/13`. Outer Limits `7` and `13` select `A` and `B` from their respective rows, separating both completed Number overlays from stale reads and the captured outer Limit by exact outer coordinate.

The first outer row deliberately carries an earlier foreign `13/A` candidate while the second carries its own `13/B` candidate. The second Target remains `B`, separating parent-local inner scanning from a global scan that would have selected the earlier `A`.

Both kernel strategies agree on three controls. A malformed Base clears First, Second, Third, and the reached outer Target and reports only `zahlHatUngueltigeZeichen` on Base. Adding direct `B` leaves the same three prefix targets cleared but returns `B` from the lazy final consumer. An absent Base computes `0`, `"0"`, and `0`, then selects `A` under outer Limit `0` without a formal operand error.

The exact bundle is retained under [`nested-current-repetition-alternating-enumeration-having`](../../evidence/kernel-30.8.1/captures/nested-current-repetition-alternating-enumeration-having/); SHA-256 values are request `589866c9e669c3ccb40436bf512d41492790261628616ec016480811532180ea`, model `7a1abca8d05d6a1f3ad64334d37191ff72eda5577a780eedff077ae53bea2604`, and artifact `f15e4afee676d737846403c416ab5e636f145b74dd4fd0bf10cbea95d0cb4312`.

This closes final addressed outcome, parent-local selection, and formal-operand correspondence only for the named four-computation route. It does not establish a middle String target-error branch, unchanged public result partitions, separate-destination application, private phase or emission order, structural-failure scheduling, wider chains, or generic scheduling.

<a id="src-scalar-mixed-consumer-first-dependency-order"></a>
#### Consumer-first mixed scalar dependency order, measured locally 2026-08-25

- `revision`: the model and both computations were authored and read back through source-shipped dmtool `0.13.0` at clean a12-dmkits `975a2e78602eaf26126168cd203bd84738885322`; `model check` returned `KERNEL_CONFIRMED`, built/runtime kernel was `30.8.1`, and the sibling stayed clean.
- `route`: the persisted computation order is `ConsumerFirst` then `ProducerSecond`. The String consumer selects either the independent literal `"SAFE"` or `FieldValueAsString(Producer)`; the Number producer projects `Base`. The existing code-generation source packet above owns the mechanism claim that dependencies order target methods before runtime. The authorized `:adapter:kernelProbe` observes four documents on dynamic Groovy and static Java.
- `runtime-claim`: both strategies agree by target identity. Base `7` replaces stale Producer `99` and stale Consumer `"STALE"` with `7` and `"7"`. A malformed Base with the safe branch selected clears Producer but writes Consumer `"SAFE"`, while the same malformed Base with the producer read reached clears both targets. An absent Base yields numeric `0` and String `"0"`. Each malformed row reports only `zahlHatUngueltigeZeichen` on Base in the formal-operand partition.
- `integrity`: exact files are retained under [`scalar-mixed-consumer-first-dependency-order`](../../evidence/kernel-30.8.1/captures/scalar-mixed-consumer-first-dependency-order/); SHA-256 values are request `fbf7ba287179e1174ebf1386108ef641ea654088b6b0a96ddc8fc0fb2dc83a74`, model `4ace8e04b681eb2e129f0e4dd0c1fbe71775470e49c12a597fccacc8bbe32d09`, and artifact `421edfb3c612b747232b018a9ff90352832099d728f1bad8acc69b9b0ba3002f`.
- `limit`: this closes only the exact two-step nonrepeatable Number-producer/String-consumer representative and its four value/read cases. Artifact collection order is not an execution-schedule contract. Independent targets, mutual cycles, more than two targets, other scalar families, repeatable targets, application, later validation, and generic scheduling remain unmeasured here.

<a id="src-repeatable-diamond-per-row-states"></a>
#### A repeatable dependency diamond keeps all three producer states apart within one call, measured locally 2026-09-03

- `revision`: one `model new`, one `apply` of a repeatable group plus eight fields, four `computation add`, then one `:adapter:kernelProbe` run with `observe: ["validateFull", "compute"]` over two documents. a12-dmkits `acced5d6012b15c0c3145d2a236e61bf2ad74246` on `main` with `source.state: CLEAN`, `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, both codegen strategies with `enginesAgree: true`; `model check` valid with zero diagnostics. Sibling `git status --short` empty before and after.
- `model`: the [flat diamond](#src-diamond-dependency-clear-kinds) moved inside a repeatable group — `Rows` with `max 5`, holding `Base`, `Alt`, `Gate`, `Err` and the same four computed targets `P` (under `FieldFilled(Gate)`), `A = [P]`, `B = [Alt]`, `Final = [A] + [B]`. Every computation is declared in the repeatable group, so the whole diamond runs once per row.
- `question`: the flat round settled ordering and the two clear-kinds on a nonrepeatable root, and named the repeatable crossing as its own residue. A settled mechanism compiles wherever its types fit, so crossing that boundary is a claim rather than an inheritance under the [carrier rule](../../CLAUDE.md) — and this boundary is one where the two arms are already known to differ elsewhere.
- `claim`: **all three producer states coexist in one call, each confined to its own row, and each behaves exactly as it did flat.** One document, three rows: a clean row answers `P = 7, A = 7, B = 5, Final = 12`; a row whose `Base` is malformed clears `P`, `A`, and `Final` while its `B` still answers `5`; and a row whose `Gate` is absent clears `P` silently, has `A` read it as **empty** and answer `0`, and `Final` answer `5`.
- `one-call-is-the-separator`: this is stronger than running the three states as three documents, and the strength is the point. If the plan were scheduled once for all rows rather than per row, the malformed row's poison would have to either reach the clean and precondition rows or become indistinguishable from the precondition row's clear. Neither happens: the clean row computes in full and the two clear-kinds stay apart **inside the same call**, on the same model, at the same instant.
- `disposition-per-row`: with stale values stored in every target, the malformed row's `P`, `A`, and `Final` come back `cleared: true` while the precondition row's `P` alone clears and its `A` still answers `0`. So the clear-then-read-empty order holds per row, and the aborted targets' disposition is again visible only where there was something to clear.
- `addresses-are-row-exact`: `formalErrorsInOperands` names `Rep/Rows[2]/Base` and nothing else, so the eager operand inventory is row-addressed rather than field-addressed, and a consumer can locate the offending instance from it.
- `local-consequence`: none. The account already schedules per instantiated target row and routes each producer state through the same `dependencyObservation` split, so this crossing needed evidence rather than code. No new conformance case: the branches are already represented, and a per-row fixture would exercise the same two functions the flat cases cover.
- `limit`: one repetition level, three rows, Number targets only, one join of arity two, `validateFull` plus `compute` on one document pair. Not measured: two repetition levels, a diamond whose branches sit at different levels, a join reading across rows, and a repeatable diamond under an over-limit row.
- `sync`: locally originated, and it **confirms** the flat account and the existing per-instance confinement rule on a new carrier rather than correcting either, so it opens no ledger entry and dispositions none.

<a id="src-diamond-dependency-clear-kinds"></a>
#### A four-deep dependency diamond orders correctly, and the two clear-kinds part ways at a plain operand read, measured locally 2026-09-03

- `revision`: one `model new`, one `apply` of eight fields, four `computation add` and one `computation modify`, then two `:adapter:kernelProbe` runs with `observe: ["validateFull", "compute"]` over seven documents. a12-dmkits `acced5d6012b15c0c3145d2a236e61bf2ad74246` on `main` with `source.state: CLEAN`, `dmtool` 0.13.0, Kernel `30.8.1` built and runtime, both codegen strategies with `enginesAgree: true` on every row; `model check` valid with zero diagnostics and every authoring child `KERNEL_CONFIRMED`. Sibling `git status --short` empty before and after.
- `model`: a nonrepeatable root with Number `Base`, `Alt`, `Gate`, `Err` and four computed targets — `P = [Base]`, `A = [P]`, `B = [Alt]`, `Final = [A] + [B]`. That is a **diamond** four levels deep: one producer, two independent dependents, one join. The [reverse-authored triple](#src-scalar-mixed-reverse-authored-triple) is a line, and its `limit` names wider plans and generic scheduling as unmeasured; a line cannot show whether a join is ordered after both branches or whether a failure on one branch reaches it.
- `ordering-claim`: **the order is correct at depth four through the join.** On `Base = 7, Alt = 5` the outcomes are `P = 7`, `A = 7`, `B = 5`, `Final = 12`. Two wrong orders are refuted by that single row: evaluating the join before its branches gives `Final` nothing to read, and evaluating the branches before the producer gives `A = 0` and `Final = 5`. The `Final = 12` cell requires the whole chain in order.
- `claim`: **the two clear-kinds are not one state — a silently cleared producer reads EMPTY while an errored one poisons its reader, and the difference is visible only downstream.** With `Base` malformed, `P`, `A`, and `Final` all clear while `B` still computes `5`. With `P` instead cleared by an **unmet precondition** — no malformed cell anywhere in the document — `A` reads it as empty and answers `0`, and `Final` answers `5`. Same topology, same `Alt`, and only the *kind* of the producer's clearing differs.
- `the-artifact-cannot-tell-them-apart`: in both cases the producer's own outcome reads `cleared: true, errored: false`, and the precondition case reports nothing at all in `formalErrorsInOperands`. So the per-outcome flags collapse a distinction the Kernel plainly makes, and the **only** channel that separates them is the dependents' own outcomes. A consumer reading `errored` to predict propagation is wrong on the malformed row. This is a route fidelity limit rather than a semantic finding, and it is instrument feedback for a12-dmkits rather than a ledger entry.
- `poison-crosses-the-join`: `Final` reads one poisoned operand and one clean one and **clears**, rather than substituting empty-zero for the poisoned side. The absent-`Base` row is the control that makes this informative: absence *does* substitute, giving `P = 0`, `A = 0`, `Final = 5`. So empty substitution and poison are both live on this exact join, selected by the producer's state and not by the join's shape.
- `disposition-needs-something-to-clear`: with nothing stored in the targets the aborted ones are simply **absent** from the outcome list; with stale values stored they come back `cleared: true`. Both batches carry that pair, so the earlier [aborted-target row](../sources/having-filter-probes.md#src-nested-correlated-filter-and-poison-scope) is reproduced on a dependency chain rather than a filtered aggregate.
- `bare-constant-under-a-precondition`: the same model closes a separate open item on a third batch. Re-expressing one branch as the bare constant `42` under `FieldFilled(Gate2)` is admitted, and it writes `42` when the precondition is met — so a constant operation honours a precondition exactly as a computed one does. Unmet, the branch clears and the join reads it as **empty**, answering `7` rather than `49`, which reproduces the silent-clear rule on a producer kind the earlier rows did not cover.
- `the-clear-is-visible-within-the-call`: the sharpest row of the three. With `99` **stored** in that branch and the precondition unmet, the join answers `7` — not `106`. So the dependent does not read the stale stored value: the clear takes effect before the read, inside the same call. A reimplementation that cleared at application time and let dependents read pre-call storage produces `106` here, and every fresh-value row of the [reverse-authored triple](#src-scalar-mixed-reverse-authored-triple) still passes for it, because that round propagated new *values* rather than a clear.
- `not-a-call-wide-abort`: `B` computes its value in **every** failing row of both batches. One call therefore produced one value beside three clears, which is what makes the clearing a per-target propagation rather than a plan-level abort.
- `local-consequence`: no clause or code change, and the account was already exactly this. `NumericTargetOutcome.dependencyObservation` maps `.noValue` to `.empty` and `.rejected`/`.invalidNoValue`/`.inheritedPoison` to `.poisoned`, and [`Conformance/NumericDependency.lean`](../../A12Kernel/Conformance/NumericDependency.lean) already asserts every arm of that split. What this round adds is the **external** evidence it lacked, on a channel that had none. It also supplies the missing witness for [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition)'s standing prohibition on using deltas as the dependency overlay: `NumericDelta` deliberately collapses clean no-result, domain invalidity, and inherited poison into one `.cleared`, exactly the collapse the Kernel's dependents refuse.
- `no-retained-bytes`: deliberate, not an omission. The reading is what carries value here and the mechanism it confirms is already locked internally, so a retained artifact would need a Lean projection consumer to earn its place under the [compact pipeline](../SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md) — Lean code for one reading, guarding a branch that already has cases. The model and documents are reproducible from the `model` row above.
- `limit`: one nonrepeatable root, Number targets only, one join of arity two, copy, addition, and one bare constant, `en_US`. Not measured: a join whose two branches poison from *different* producers, a diamond crossing a repeatable level, three or more branches, a join on a non-Number family, and whether any instrument channel exposes the ERRORED-versus-cleared distinction directly.
- `sync`: locally originated. The clear-kind divergence at a plain operand read and the join's poison crossing are additions to [`spec/09`](../../spec/09-computations.md) and go outbound as [`SPEC-2026-09-03-09`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-03-09); the ordering, absent-substitution, and not-a-call-wide-abort rows **confirm** existing clauses and add no request.

<a id="src-scalar-mixed-reverse-authored-triple"></a>
#### Reverse-authored mixed scalar triple, measured locally 2026-09-01

- `revision`: the three computations were persisted and read back through source-shipped dmtool `0.13.0` at clean a12-dmkits `47952f81e13fc63dd72d16f2bbbd24e8bb024654`; `model check` returned `KERNEL_CONFIRMED`, built/runtime kernel was `30.8.1`, and the sibling stayed clean.
- `route`: the persisted authored order is Number `FinalFirst`, String `ConsumerSecond`, Number `ProducerThird`. `FinalFirst` converts `Consumer` through `FieldValueAsNumber`; `ConsumerSecond` selects literal `"5"` when `Gate` is filled and otherwise converts `Producer` through `FieldValueAsString`; `ProducerThird` copies `Base`. The authorized `:adapter:kernelProbe` observes four documents on dynamic Groovy and static Java.
- `runtime-claim`: both strategies agree by target identity. Base `7` replaces stale Producer `90`, Consumer `"90"`, and Final `90` with `7`, `"7"`, and `7`. A malformed Base with the literal branch selected clears Producer while Consumer and Final become `"5"` and `5`; the same malformed Base with the producer read reached clears all three targets. An absent Base produces `0`, `"0"`, and `0`. Each malformed row reports only `zahlHatUngueltigeZeichen` on Base in the formal-operand partition.
- `separator`: the first row separates completed predecessor visibility from stale-target reads across both family crossings. The two malformed rows differ only in whether Consumer reaches Producer, separating dependency invalidity from an eager graph-wide skip; Final then separates propagation through a second reached read. The absent row distinguishes numeric empty substitution from clearing.
- `integrity`: exact files are retained under [`scalar-mixed-reverse-authored-triple`](../../evidence/kernel-30.8.1/captures/scalar-mixed-reverse-authored-triple/); SHA-256 values are request `daa694cfd591f7a2801ba90662a6b0aa3f8375646cd944c82e52f6af731bcc80`, model `f738eae4ba529a3e4631e25816159470a583d5793c7f8d655657d62b3d618e06`, and artifact `2cdb727b352647339487a1583d5aade3c1bea3c60f2147fbe17cbe2696d8e774`.
- `limit`: this closes final computation outcomes and the formal-operand partition only for the exact nonrepeatable Number-to-String-to-Number triple and its four rows. The artifact sorts public result collections by field and therefore establishes no private execution order, authored-stable tie-break between independent targets, or kernel cycle mechanism. Other target families, wider plans, structural failures, application, later validation, and generic scheduling remain unmeasured.

<a id="src-numeric-extremum-call-boundary"></a>
#### Numeric extremum call boundary

- `revision`: a12-dmkits `700095dfb964af69c8a9f7208673415cf91f7e4f`, reachable from clean current main `89aa03957034de620562eb23a095d878f6547dca`.
- `route`: maintained [`OpListMathConstantLawsTest`](../../../a12-rulekit/src/test/java/io/github/mbackschat/a12/dm/rulekit/validate/laws/OpListMathConstantLawsTest.java) invokes the real kernel consistency oracle through the existing typed DSL and exact string control.
- `claim`: each `Min`/`Max` call independently admits at most one immediate or bracketed numeric literal; a nested same- or different-selector call is one complete operand and does not donate its literal to the outer budget; flattening the two calls changes admission.
- `limit`: the maintained row establishes static admission at one nonrepeatable root and the peer's retained tree shape. It does not measure repeatable addressed execution, nested selected values, scale projection, first poison, result addresses, partial execution, division, power, or surrounding arithmetic.

On 2026-08-04, dmtool `0.11.1` with built/runtime kernel `30.8.1` closed the literal-bearing arithmetic child and uncovered the capability-aware target gate. Every static verdict below was reproduced identically by a launcher self-reporting `source: 5c0d5f0395a45d4e0199662de097ecc7fb97b843 (clean)`, so the account is anchored to an exact revision. The original run used an unlabelled working-tree build; a12-dmkits `9d10e682` then added the built-from revision and `CLEAN`/`DIRTY` state to `--version` and `manifest.version`, which is what makes this anchor citable at all.

Real-kernel dry-runs admit an immediate literal at either inner position of the arithmetic child, and the child's derived scale consumes the literal's **authored** scale: `Min([A] * 1.5, [C])` is accepted at declared scale 3 while `Min([A] * 1.50, [C])` requires 4 and is rejected at 3 naming derived scale 4. The child's literal does not consume the outer list's budget — `Min([A] * 1.5, [C], 2)` is accepted while a second outer constant is rejected with `MVK_TOO_MANY_CONST_FOR_CALC_OP` naming `At 'Min'` — and a constant-only child `Min(1.5 * 2, [C])` plus `Min(1.5 * 2, [C], 3)` are both admitted. Persisted read-back preserves `Min([A] * 1.50, [C])` with its authored trailing zero and an empty `errorCodesToSuppress`, and `model check` returns `KERNEL_CONFIRMED`.

Row-addressed `DM_INTERPRETER` output shows an empty field times a literal remaining `0`, a malformed source still clearing, and the replaced field's own target rejection disappearing from the row because the literal removed that dependency. The same route then measured the target gate itself: `[A] * 100` and `Min([A] * 100)` derive scale 0 through integer-literal trailing-zero stripping and are accepted at declared scales 0, 2, and 4, while capable `Min([A] * 1.50)` is rejected at declared scale 0, field-bearing `Min([A] * 100, [C])` is accepted only at its exact derived 2, and `[A] * [B]` and `Min([A], [C])` require exact equality.

That predicate is the correction carried by [`SPEC-2026-08-04-01`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-04-01--the-computed-target-scale-gate-is-the-comparison-predicate-not-equality).

<a id="src-custom-field-type-spi"></a>
#### The custom field type's acceptance arm raises only under a full check, and its codec has one opt-in caller, measured inbound 2026-09-03

- `revision`: a12-dmkits `989f33bae` on `main`, five commits from `6d0d288e9`, resolved here by `git show` and `git ls-tree` rather than from the handback's prose. Locked by `adapter.laws.CustomFieldTypeSpiInvocationLawsTest`; shipped there as `KF232`.
- `question`: [`SPEC-2026-09-02-06`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-02-06) asked which observation stood behind the runtime half of [`spec/06`](../../spec/06-strings-and-enumerations.md)'s unregistered-validator paragraph, and [`SPEC-2026-09-02-05`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-02-05) asked whether the engine invokes either conversion arm. Both were source-read only on this side.
- `answer-to-the-first`: **a12-dmkits carried it unmeasured too** — it lived in a docstring on their bounds fixture with no retained capture and no test exercising it. The entry is what got it measured, which is the outcome a recorded confidence limit is for.
- `claim`: **the raise belongs to the full check.** `checkConsistency` reports nothing for a model declaring a custom type with no validator registered; a filled relevant cell under `validateFull` with a factory answering empty raises, with a validator-missing exception in the cause chain naming the type; the same model with the custom cell **absent** raises nothing and never calls the validator. `FormatDefinitionVordefiniert.validiereFormat` returns valid and never touches the factory when its full-check argument is false, and the raising lookup sits after that return — so the unqualified reading over-claimed.
- `claim`: **the factory and the static registry are alternatives, not a fallback chain.** The registry is consulted only when no factory was supplied at all, so a static registration cannot rescue a factory that answers empty.
- `claim`: **the codec has exactly one engine-side Java caller and it is opt-in.** `FormatDefinitionVordefiniert.convert`, gated on the caller's own presentation information requesting custom-field-type conversion, with XML ingress selecting internal-to-display and everything else display-to-internal. **Neither arm runs on the ordinary `validateFull` path** on either codegen strategy, where acceptance is called once with the stored-value flag false and both declared bounds, the message locale, and the mode recorded from outside.
- `instrument-trap`: **their first conversion instrument was wrong in a way that reads as measured**, and the shape transfers. `ICustomFieldValidator` declares each conversion twice, a one-argument form and a two-argument form taking the configuration map; the engine calls the **two-argument** pair, whose SPI default returns a no-conversion result **without** delegating to the one-argument form. A probe overriding only the one-argument methods therefore records nothing whatever the engine does, and its empty log is indistinguishable from a measured absence. The acceptance arm has the opposite delegation, which is why it was genuinely observed while the codec was not. A cold review caught it; the table above is the re-measured result. [`LF130`](../LEAN-FINDINGS.md) owns the class.
- `denominator`: the caller enumeration covers Java sources in the kernel checkout, excluding build output, delombok output, and the api/impl modules that define or implement the methods. The TypeScript runtime carries its own mirror site and is outside it.
- `limit`: not measured — ordering against the declared length and charset checks, whether the display arm participates in formal checking, and what a conversion's own error message does when acceptance succeeds.
- `do-not-carry`: the neighbouring **predefined**-type surface behaves oppositely and each result stays on its own surface ([route](#src-custom-validity-route)). A source-shaped raise prediction was refuted there, which is why this clause's own surface needed its own measurement rather than an inference from either direction.
- `sync`: inbound from a committed reviewed revision. [`spec/06`](../../spec/06-strings-and-enumerations.md) gains the full-check qualifier, the alternatives correction, and the codec's caller and gate; no new outbound entry.

<a id="src-custom-validity-route"></a>
#### Custom validity source route

At the same launcher revision, the custom-field-type predicates were measured only as far as this route reaches. Their authored keyword is `Valid`/`Invalid`, not the catalog ids `ValidType`/`InvalidType`. Real-kernel `rule check` admits `Valid(Iban, "IBAN")`, `Invalid(Iban, "IBAN")`, the same call inside a larger condition, and — decisively — `Valid(Iban, "NotARegisteredTypeName")`, confirming that the type name is not validated at model-check time; a Number operand is rejected with `MVK_NO_STRING_OR_ENUM_OR_EXT_ENUM`.

The runtime half is **not** reachable here: both predicates delegate to the kernel's client-SPI `IPredefinedType`, a12-dmkits' operator catalog documents that a kernel runtime with no validator for the named type fires **both** polarities so they are not complements, and the kernel-free interpreter instead declines the construct — `model eval --strict-custom` refuses with `unknown predefined type "IBAN"` naming each affected rule. The interpreter is therefore honestly silent rather than contradicting the catalog, and this project has no route to the kernel runtime branch.

<a id="src-field-values-not-unique-route"></a>
#### `FieldValuesNotUnique` source route

At launcher revision `5c0d5f0395a45d4e0199662de097ecc7fb97b843` (clean), dmtool `0.11.1` with kernel `30.8.1` measures the complete `FieldValuesNotUnique` clause, which `spec/07` canonizes but no Lean owner implements. Real-kernel `rule check` admits the plain field list over two and three Number fields of differing scales (`0` and `2`), the starred single-field form `FieldValuesNotUnique(/Probe/Rows*/Key)`, and a mixed direct-plus-starred list

it rejects a single operand with `MVK_PARAMSIZE_INVALIDN`, a mixed Number/String list with `MVK_VARYING_TYPES_NOT_ALLOWED`, and an operand list that omits the error field with the ordinary `MVK_ERROR_FIELD_NOT_REFERENCED`. On the persisted rule, `DM_INTERPRETER` `rule eval` then separates every runtime facet the clause names: scale-0 `5` against scale-2 `5.00` **fires**, so equality is typed across declared scales distinct values pass two empty operands pass, confirming that empties are skipped rather than compared one empty operand passes

and a malformed operand yields `suppressed` with `suppressedBy` naming the field and its formal error code. The runtime half is interpreter triangulation, not kernel runtime evidence. Two stored-Enumeration operands are likewise admitted while any Enumeration/String mix is refused in either order, so the single-declared-kind rule holds across kinds rather than being String-specific. The firing-locus account that this probe left open — the authoring skill claimed per-row iteration while `model eval` fired exactly once with a declaration-shaped pointer — is **resolved inbound**, and the once-per-scope reading was correct see the aggregate-message paragraph below.

a12-dmkits revision `b5aba1ec60ab9ec6b99fd831f91e12e65e3eb0d6` is the reviewed inbound revision behind the 2026-08-04 aggregate-message reconciliation, and its root fix is `bbbbf48ea8480a3f0bfc5274251c148cc7958d54`. Starred `FieldValuesNotUnique` is aggregate-style: for `Count = [7,5,5]` both dynamic Groovy and generated Java emit exactly **one** message at the error field's concrete first-row address `/Order[1]/Items[1]/Count`, with the wildcard reference `/Order[1]/Items[0]/Count`, invariant under repeated-group versus parent placement. A reached `Having` retypes that firing to OMISSION even when every retained value is filled, an unfiltered firing is VALUE, `Having` field operands join the reference set, and `CurrentRepetition` contributes none.

[`FieldValuesNotUniqueDiffTest.starredDuplicateEmitsOneFirstRowMessageWithItsWildcardReferenceInBothPlacements`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/FieldValuesNotUniqueDiffTest.kt) covers both placements, both filter shapes, cardinality, polarity, error pointer, and reference content across both kernel strategies; [`StarOnlyIterationScopeDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/StarOnlyIterationScopeDiffTest.kt) is the independent starred-`Sum` control proving the wildcard reference is an aggregate law rather than a uniqueness special case

and [`GroupStarValueAggregateDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/GroupStarValueAggregateDiffTest.kt) plus [`OuterCorrelationDiffTest`](../../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/OuterCorrelationDiffTest.kt) decide recursive group-star expansion and the concrete-outer/wildcard-inner split.

That reference channel is a **structural projection of the checked condition**, not a read trace. The peer revision is inbound reviewed provenance, so its accepted canonical corrections create no outbound sync-ledger request. Its measured fixture instantiates three of `repeatability` 50 rows, which is what establishes that an uninstantiated declared tail does not escalate the unfiltered VALUE polarity.

At launcher revision `b5aba1ec60ab9ec6b99fd831f91e12e65e3eb0d6` (clean), dmtool `0.11.1` with kernel `30.8.1` independently separates the polarity branch this project implements. `DM_INTERPRETER` `model eval` over four rows keyed `7, 9, 7, empty` emits exactly one unfiltered message typed `VALUE_ERROR` with the wildcard reference `/Probe[1]/Rows[0]/Key`, so neither a skipped empty cell nor the uninstantiated tail of a `repeatability`-8 group escalates it a sibling rule filtered to the first three rows, whose retained `7, 9, 7` are all filled, emits one `OMISSION_ERROR`

and a filter retaining `9, 7, empty` does not fire, so the filter changes the compared set. Real-kernel `rule check` admits both filtered rules, so the filtered starred form is statically legal. The polarity and reference halves are interpreter triangulation against the inbound differential above rather than independent kernel-runtime evidence. The same launcher's `batch --observations` reproduces the admission matrix — two Strings, two stored Enumerations, and two differing-scale Numbers accepted either kind mix rejected `MVK_VARYING_TYPES_NOT_ALLOWED` a single direct operand rejected `MVK_PARAMSIZE_INVALIDN` the starred slot and its filtered form accepted — as a byte-identical artifact across repeated runs, with `rule check` supplying the real-kernel verdicts.

a12-dmkits revision `dd0bcadf33a5df081fa8ac32ac49ba439bc35d3d` is the reviewed inbound revision that answers the three 2026-08-04 outbound entries, all now `accepted`. It reproduced every reported verdict for the `FieldValuesNotUnique` admission matrix and the computed-target scale gate against the real kernel rather than accepting them as reported, and added two controls this project had not required: `NumberOfDifferentValues` over the same String/Enumeration mix is **accepted**, which is what establishes that the two operators' kind gates genuinely differ rather than that the kernel refuses the mix everywhere; and for the scale gate, two derived-scale-4 operations differing only by a field reference flip at one declared scale, which shows the gate reads capability rather than the scales.

Every scale rejection is `MVK_INVALID_COMPARE_DEC_PLACES` alone, asserted as a complete code list. The peer's own computation prose was the error and is corrected; this project's clauses were confirmed accurate. Owners: `KERNEL-SEMANTICS.md` §9 and §11, `KF173`, `KF174`, `FieldValuesNotUniqueAdmissionLawsTest`, `ComputedTargetScaleLawsTest`, and the operator catalog, whose new uniqueness constraints, invalid example, CI-executed rejection probe, and `NumberOfDifferentValues` contrast were verified present in the local launcher.

The same revision settles the custom-field-type `Valid`/`Invalid` observable that [`spec/05`](../../spec/05-dates-and-time.md) already described. Across both kernel strategies, an unregistered type name on a **filled** String field makes both polarities fire as ordinary rule messages typed `VALUE_ERROR` at the concrete error address, with nothing raised; an **empty** field fires neither and raises nothing; and the two distinguishable registration states — no factory supplied, and a factory present that returns nothing for the name — are indistinguishable in every cell. `UnregisteredPredefinedTypeObservableDiffTest` pins that matrix with the non-complement property asserted separately, and `PredefinedTypeValidityDiffTest` now asserts the nature of the observable rather than the presence of a code.

The **causal mechanism is unresolved on both sides** and must not be modeled: kernel runtime source reads as though the double firing were impossible, since the two operator methods share one value-specified gate and then differ only by one comparison against *Valid* while the validator lookup raises on an empty resolution, and the peer confirmed the Groovy lowering reaches those methods and found no covering `catch` or fall-back factory. The named settling probe is to dump the generated program and follow the actual call. This project's local account is therefore deliberately observable-only. Launcher provenance for the local verification: built at `dd0bcadf` from a dirty tree, with the two commits that followed being documentation only.

The same forms' **static** gate has a separate provenance. Kernel `30.8.1` parser check `CheckPredefinedTypeValidityConditionImpl` at pinned kernel revision `cb66e51fa7ab90b650698f861bf670754e2e1e66` requires a field operand, no wildcards, a value validation, a String/Enumeration/extensible-Enumeration declared type, and a nonempty unquoted type name; it never consults the host registry. a12-dmkits' own `MVK-LEDGER.md` records the two resulting rejections independently as kernel-delegated laws — `MVK_NO_EMPTY` for the empty name and `MVK_NO_STRING_OR_ENUM_OR_EXT_ENUM` for the operand kind — so this is a second-implementation corroboration rather than a local-only reading, and it needs no outbound request.

It is **not** a kernel measurement: no `rule check` probe has exercised either rejection from this project, because the local launcher and its sibling checkout were both dirty when the clause was written. The Lean elaboration therefore implements only the nonempty-name gate; [`SG7`](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) owns the unmodelled operand gates.

a12-dmkits revision `ddaf2e13` is the reviewed inbound revision answering the two 2026-08-05 outbound entries, and it carries one correction this project did not ask for and had wrong. **A formally unavailable operand of `FieldValuesNotUnique` is skipped exactly like an empty cell and does not suppress the rule.** Measured across dynamic Groovy, generated Java, and the interpreter at full `field | type | referenced` message identity, on Number and on Date operands: a duplicate before or after an unconvertible cell still fires `VALUE_ERROR`, an unconvertible direct operand before the duplicate still fires, and — the separator — **two equal unconvertible values produce no message at all**, because neither enters the comparison.

That last pair is what distinguishes skipping from suppressing; the peer notes its own published suppression claim had never been separated from evidence either, since the only case behind it has no duplicate once the unconvertible value leaves the comparison, so "does not fire" reads the same under both accounts and the lock asserted only a boolean. The interpreter defect this exposed was the more serious of the two fixed at `ddaf2e13`, because it made a clean-room engine **fail to fire rules the kernel fires**. Also corrected: the positional filter flag follows a value that was *actually compared* rather than one that is merely specified, so a present-but-unconvertible cell in a filtered operand does not escalate the polarity.

The same revision refutes the scope of this project's operand-admission entry. It established BOOLEAN and DATE_RANGE as kind refusals but did not measure CONFIRM; the clean follow-up below adds CONFIRM to that class. TIME, DATETIME, DATE_FRAGMENT, and CUSTOM are admitted. Among temporal operands the gate requires **one identical declared format string** and keys on the format rather than the kind, with a DATE declared `yyyy` beside a DATE_FRAGMENT declared `yyyy` accepted across kinds while two DATEs differing only in format are refused. The mixing gate reads a comparability category: `{String, Custom}`, `{Enumeration}`, `{Number}`, `{Date, Time, DateTime, DateFragment}`. Custom beside String is therefore accepted and "one declared kind" was never the rule.

Gate order is observable as a pre-emption with the pre-empted code asserted **absent**, which is what places Time and DateTime inside the temporal category. Two incidental model-level facts came with it: several plausible declarations are refused by the kernel before any operator gate is reached (`HH:mm` TIME, `dd.MM.yyyy HH:mm:ss` DATETIME, `" - "`-separated DATE_RANGE, `MM.yyyy` DATE_FRAGMENT all draw "The date format is invalid"), and no differing-format TIME or DATETIME witness was known as of `ddaf2e13`, so their same-format rows could not separate format from kind.

Finally, the peer refuted a rival mechanism rather than leaving it standing: this project's source reading that the comparison domain is selected by whether the first operand is a Number field predicts an order-dependent stored-text comparison, and cross-scale equality was measured to hold in both operand orders, so **no order-selected comparison mode is observable**. That reading survives only as an implementation note. Upstream surfaces: `KERNEL-SEMANTICS` §9 rewritten, `KF173` amended and `KF175` added, `IF247` corrected and `IF249` added, a 25-case admission-law tier, a 15-row ordered-scan differential, and an operator-catalog corrective whose trigger probe and advertised repair both execute in CI.

At clean a12-dmkits revision `cd41ea94b470a190f7d766ea6d7adf26b6ba74cf`, dmtool `0.12.1` with built/runtime kernel `30.8.1` rechecks the Number and token `FieldValuesNotUnique` admission gates in a 24-case matrix. Two Numbers of scales 0 and 2, two Strings, and two stored Enumerations are admitted; a direct singleton reports `MVK_PARAMSIZE_INVALIDN`, including BOOLEAN and CONFIRM singletons that prove arity precedes kind; Number beside String, Enumeration, or Date and String beside Enumeration, Number, or Date report `MVK_VARYING_TYPES_NOT_ALLOWED`; and BOOLEAN or CONFIRM report `MVK_ONLY_STRING_ENUM_NUMBER_DATE_ALLOWED` once arity is satisfied, including a same-kind pair.

A three-operand category mismatch beside BOOLEAN reports the kind code with the mixing code absent whether BOOLEAN precedes or follows the mismatching operand. Accepted Number and String representatives were persisted, read back structurally with their exact `FieldValuesNotUnique` conditions, and survived `model check`, all `KERNEL_CONFIRMED`. The CONFIRM rows correct the previous "only BOOLEAN and DATE_RANGE" scope; they make no runtime claim.

<a id="src-fieldless-numeric-computation"></a>
#### Fieldless repeatable numeric computation

A follow-up probe at the same launcher revision `5c0d5f0395a45d4e0199662de097ecc7fb97b843` (clean) establishes that a repeatable Number computation needs **no** field reference at all. `Min(1.5 * 2)`, `Min(1.5 * 2, 3)`, the literal-only `Min(1.25)` and `Max(1.25)`, and even a bare `1.5 * 2` are accepted into a scale-2 target, while `Min(1.25, 2)` is rejected with `MVK_TOO_MANY_CONST_FOR_CALC_OP` because the enclosing call still admits only one immediate constant; the constant-only list derives scale 1 while retaining multiplicative-constant capability, so it is padded up to declared scales 2 and 4 and rejected at 0. Persisted read-back plus `model check` return `KERNEL_CONFIRMED`.

Row-addressed `DM_INTERPRETER` output then computes the fieldless `Min(1.5 * 2)` once per instantiated row — three rows, three `3.00` values, including an entirely empty row — so repetition comes from the computed target's own declaration rather than from any source reference.

<a id="src-addressed-numeric-binary"></a>
#### Addressed numeric binary computation

At clean a12-dmkits revision `27c1e583eae27dfba4beabe885f7d1b25f11a460`, an unchanged source-shipped probe over an unindexed repeatable group admits `/Probe/Rows/Sum := [A] + [B]`, `/Probe/Rows/Difference := [A] - [B]`, and `/Probe/Rows/Product := [A] * [B]` where `A` has scale 1 and `B` scale 2. Addition and subtraction target maximum scale 2; multiplication targets summed scale 3. Re-targeting the product to scale 1 is rejected with `MVK_INVALID_COMPARE_DEC_PLACES`. The `DM_INTERPRETER` route reports exact row-addressed `4.75`, `2.25`, and `4.375` for `3.5` and `1.25`; an empty operand contributes zero in its authored position, both empty operands produce zero, and either malformed operand clears all three targets. The addressed capsule delegates each node to the scalar precision-50 arithmetic owner; this direct-field packet adds no independent precision-rounding separator.

At the same clean a12-dmkits revision, an unindexed repeatable `/Probe/Rows/Quotient := [A] / [B]` into a scale-2 target is rejected without suppression: the kernel reports `MVK_INVALID_COMPARE_DEC_PLACES` with derived right-hand scale `?`. The same generated model is kernel-valid when its computation declares the documented `errorCodesToSuppress: ["MVK_INVALID_COMPARE_DEC_PLACES"]`. The public interpreter then reports `10 / 4` as padded `2.50`, empty divided by `5` as `0.00`, both zero-divisor shapes and either formal-poison position as cleared, `10 / 3` as errored `3.333333333333333` through the warning-suppressed no-fit store, and fitting `10 / 2` as target-rejected `5.00` under an inclusive maximum of `3`.

The public route omits row coordinates and collapses domain invalidity with clearing; the exact addressed Lean matrix keeps domain invalidity, inherited poison, target errors, source-relative clearing, and valueless-error pointers distinct. Maintained `ComputedNumberDigitOverflowDiffTest` separately locks the suppressed scalar quotient across both dynamic kernel strategies, generated Java, and the interpreter.

At the same clean a12-dmkits revision, an unindexed repeatable `/Probe/Rows/Result := [Base] ^ [Exponent]` with a scale-2 base, scale-0 exponent field, and scale-2 target is rejected without suppression with `MVK_INVALID_COMPARE_DEC_PLACES` and accepted when the computation suppresses that code. Changing only the exponent field to scale 1 remains rejected with `MVK_INVALID_NUMBER_FOR_EXP`, proving that result-scale suppression does not bypass exponent admission.

The public interpreter reports `1.50 ^ 2 = 2.25`, missing-base and missing-exponent rows as `0.00` and `1.00`, both missing as `1.00`, `0 ^ -1` and `2 ^ 1001` as cleared, exponent endpoints `±1000` as `1.00`, `1.23 ^ 2` as no-fit errored `1.5129`, `2 ^ 2` as target-rejected `4.00` under maximum `3`, formal invalidity in either operand as cleared, `2 ^ -1` as `0.50`, and `0 ^ 0` as `1.00`. The public route omits row coordinates and collapses domain invalidity with formal poison; the exact addressed Lean matrix retains those causes, target-error classes, source-relative clearing, valueless-error pointers, and exact rows.

Maintained `DecimalPrecisionParityDiffTest` and `PowerDomainFailureComputationDiffTest` separately lock staged positive and reciprocal-first negative power, runtime bounds, domain projection, both kernel strategies, generated Java where applicable, and the interpreter.

At clean a12-dmkits revision `47952f81e13fc63dd72d16f2bbbd24e8bb024654`, maintained `DivisionDomainFailureComputationDiffTest` and `PowerDomainFailureComputationDiffTest` also lock fixed-scope wrapper composition directly. Division by zero and runtime-invalid integral power survive as the first operand of `Min` and the second operand of `Max`, clear the stale Number target without a value, and attach `berechnungsWertFehler`; dynamic Groovy and generated Java agree, and the peer interpreter retains the cleared result. These observations distinguish domain failure from selecting the clean sibling and from inherited operand poison. They do not measure the opposite operand positions, the exact combined repeatable carrier, row addresses, source-relative rich outcomes, or warning-suppression fingerprint; those compose independently established placement and wrapper clauses and remain externally pending as a combined route.

The dmtool-derived numeric observations above were reassessed after the output-contract fixes at clean a12-dmkits revision `27c1e583eae27dfba4beabe885f7d1b25f11a460`, dmtool `0.11.1`, with built/runtime kernel `30.8.1`. Structured current-version dry-runs reproduced the String, Enumeration/category, range, root `Abs`, all root Round modes, direct and literal/wrapper extrema, direct addition/subtraction/multiplication, unsuppressed division/power, and exponent-scale static boundaries with `KERNEL_CONFIRMED`; accepted representatives survived persisted structured read-back and `model check`.

Current `model compute` envelopes identify `DM_INTERPRETER`, distinguish declarations from instances, and carry canonical repeatable instance pointers; row-addressed runs reproduced the conversion, root unary, direct extrema, multiplication, operand-local extrema, and addition value/outcome matrices. Computation first-cause ordering remains outside that cause-blind runtime projection and stays internally locked. At that revision the structured computation add/modify schema had no `errorCodesToSuppress` input, so dmtool could not reconstruct the legal suppressed division/power models; those runtime claims therefore continued to ride the maintained a12-dmkits differentials named above.

a12-dmkits revision `112169fa4ce71d9b45c0b276e0406e275f70354d` closes that structured-authoring gap. On a fresh dmtool-authored model, an unsuppressed scale-2 division still receives kernel-confirmed `MVK_INVALID_COMPARE_DEC_PLACES`, while the same division and a scale-0-field-exponent power pass dry-run and persisted `model check` with `errorCodesToSuppress: ["MVK_INVALID_COMPARE_DEC_PLACES"]`. Structured read-back retains the exact list for both computations; modify dry-run, explain, and model report expose it; an unsupported `MVK_INVALID_DATE_TYPE` is a write-safe `RK_INVALID_VALUE` rejection. `DM_INTERPRETER` then computes the clean representatives `10 / 4 = 2.50` and `10 ^ 2 = 100.00`.

This reopens dmtool for purpose-specific suppressed-computation probes; the maintained a12-dmkits differentials remain the owner of the broader division/power runtime matrices and kernel-strategy agreement.

<a id="src-number-repeatable-first-filled"></a>
#### Sibling-star Number `FirstFilledValue` target admission and ordered runtime, measured locally 2026-08-27

- `revision`: clean a12-dmkits `25e048ea73e10bd58c6950b1d6104dd5809535df`, dmtool `0.13.0`, kernel `30.8.1` built and runtime. The source-matched launcher was prepared in this session, and the sibling worktree was empty before and after the probe.
- `route`: a disposable structured dmtool model declared repeatable `Parents`, with sibling repeatable `Primary`, `Secondary`, and `Targets` groups below each parent. `Candidate`, `Fallback`, and computed `Result` were scale-0 Number fields, and `Result` had maximum `9`. The structured computation writer persisted and read back `FirstFilledValue(../Primary*/Candidate, ../Secondary*/Fallback)` exactly, and terminal `model check` returned `valid:true`, all `KERNEL_CONFIRMED`. The earlier one-source route separately admits `FirstFilledValue(../SourceLines*/Candidate)` and refuses the otherwise identical unstarred spelling with `MVK_NO_WILDCARD`.
- `runtime-claim`: source-shipped `:adapter:kernelProbe` observed `validateFull` and `compute` on dynamic Groovy and generated Java over five documents, with `enginesAgree:true` on every row. A first-extent value `5` reaches both target rows and hides malformed cells later in the same extent and in `Secondary`; an empty first extent falls through to `Secondary=7`, while a second parent selects its own `Primary=3`; a malformed first cell clears despite later values; two empty extents compute `0`; and selected `12` reaches the target policy as `errored:true`, `cleared:false`, value `12`. The stale second target after an empty-before-`5` prefix fires the generated computation message as `OMISSION_ERROR`, retaining the missing-prefix polarity. Exact target addresses distinguish both rows and both parents.
- `triangulation`: read-only `dmtool model compute` reports the same six value/clear instances for the combined parent-local matrix, the same zero for both empty extents, and `outcome:error`, value `12` for the target-limit control under `engine: DM_INTERPRETER`.
- `integrity`: model SHA-256 `515622e811b491939c4690a5a1d00efe44b060e5203d830159635b718360bed7`; request SHA-256 `c3467e441a0942a9054dc23c1931cc91fc9d0c2a1a9b8aa9e6322727012a2221`; two unchanged artifacts are byte-identical at SHA-256 `4974357122718be47de96c6e64cf37bdea1bbde96c71e5438d0cdf1a8532d8ea`; producer source state `CLEAN` before and after.
- `limit`: one scale-0 target and two same-scale unfiltered sibling-star operands, each reopening one axis below one shared repeatable parent. The runtime artifact establishes exact computed outcomes and generated message polarity but not Lean's source-relative result partitions, separate-destination application, materialized topology, pointer-dialect normalization, general scale admission, groups, filters, root or global stars, or structural-failure scheduling.
- `sync`: locally originated and accepted at reviewed a12-dmkits revision `38aeb23b5849b831f8ae6ee09579367e28ce982b` under [`SPEC-2026-08-27-01`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-27-01--a-sibling-star-computation-may-target-a-repeatable-group-and-stays-parent-local).

<a id="src-computed-target-reference"></a>
#### Computed-target reference source route

At clean a12-dmkits revision `93771ee24aeb135a88ce9662ee3d88f39e804f45`, dmtool `0.13.0` with built/runtime kernel `30.8.1` measures the direct Number target-reference overlap through one structured eight-row computation dry-run matrix. The target is always the direct left operand: `[/Probe/Target0] * 2` and `[/Probe/Target0] + [/Probe/Source0]` report only `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`; replacing the right operand by the authored-scale-1 literal `1.5` reports only `MVK_INVALID_COMPARE_DEC_PLACES`; suppressing only that scale code exposes `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD` again. The unrelated `[/Probe/Source0] * 1.5` retains `MVK_INVALID_COMPARE_DEC_PLACES`, while `[/Probe/Source0] * 2` is accepted.

The accepted control was persisted through the structured computation writer, read back exactly, and the whole model remained `KERNEL_CONFIRMED`. Every matrix row carries the complete diagnostic-code list, and the sibling stayed clean at the same revision before and after. Subtraction, another operand position, nested or specialized target reads, and precedence against invalid authoring were not measured.

At the same clean a12-dmkits revision and tool/kernel versions, one caller-tagged three-row String-computation observation batch measures ordinary target self-reference. The direct copy `[/Probe/Target]` and root `RangeAsString(/Probe/Target, 1, 1)` each report exactly one `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`; the other-field copy `[/Probe/Source]` is accepted. That control was persisted, read back structurally as `[Source]`, and survived `model check`, all `KERNEL_CONFIRMED`. The sibling stayed clean before and after. Concatenated, nested, malformed-range, repeatable, and specialized String target reads were not measured.

At the same clean a12-dmkits revision and tool/kernel versions, three ordinary Enumeration-computation observation batches measure plain target self-reference. With closed `A`/`B` domains on every field, direct `[/Probe/Target]` reports exactly one `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`, while `[/Probe/Source]` is accepted. The compatible `FirstFilledValue(/Probe/Target, /Probe/SourceA)` pair reports that same singleton code, while `FirstFilledValue(/Probe/SourceA, /Probe/SourceB)` is accepted. A five-row widening matrix reports the same singleton code for the target second in a pair and first, middle, or last in a triple; the three-source control is accepted.

Each accepted control was persisted separately, read back structurally as `[Source]`, `FirstFilledValue(SourceA, SourceB)`, or `FirstFilledValue(SourceA, SourceB, SourceC)`, and its model remained `KERNEL_CONFIRMED`; the sibling stayed clean before and after.

The retained direct, initial first-filled, and widening artifacts have SHA-256 `f33164ceecd19570f7f07e8bd77f494ed03f04dbcd615f3d3bcf1fc0c4b95e97`, `674d9ad60e1d91105f0d1e2d08bb8d9028ceba6b72fcd63a4e9077a2d0c3a314`, and `b262097df86cd90baf47767a5a94fac7bd11a16e9475ae5040ac395fe52106ba`. dmtool `0.13.0` labelled each kernel-rejected mutation child a batch failure because its summary fold read `ok` alone, so these claims use the child envelope's independent `outcome:rejected`, `valid:false`, `verification:KERNEL_CONFIRMED`, and exact kernel diagnostic plus each accepted control, not the aggregate summary.

A fourth caller-tagged five-row batch at the same clean revision measures compatible category-decorated target reads. `[/Probe/Target -> Choice]` and both `FirstFilledValue(/Probe/Target -> Choice, /Probe/SourceA -> Choice)` operand orders report the complete singleton code `MVK_ERROR_SEMANTIC_INDEX_OR_CATEGORY_FOR_ERRORFIELD`; the ordinary `/Probe/SourceA -> Choice` source and the two-source different-field `FirstFilledValue` are accepted, all `KERNEL_CONFIRMED`. The `FirstFilledValue` control was persisted, read back exactly with both category projections, and survived whole-model checking.

The ordinary accepted category control was also persisted and the model remained valid, but dmtool `0.13.0` read-back returned `[SourceA]` with `converted:true`, dropping `-> Choice`; that control establishes kernel acceptance through its dry-run envelope but is not used as an exact persistence claim. The retained artifact has SHA-256 `02ffb70e71d5357e69ff1598522a0c34fca8eb11ad43e29ae2cb4fb1cd6c4783`. Mixed category/direct lists, category lists longer than two, incompatible categories, stars, `Having`, semantic indices, preconditions, repeatable computation, and scheduling target reads were not measured.

At clean a12-dmkits revision `4ba31a496faed41107ec95e4207cfc6256872053`, dmtool `0.13.0` with built/runtime kernel `30.8.1` closes the operand-scale axis of the computed-Number gate order. On a fresh model carrying a scale-0 target beside scale-0 and scale-1 sources, one caller-tagged five-row batch reports `MVK_INVALID_COMPARE_DEC_PLACES` alone for `[/Probe/Target0] + [/Probe/Source1]`, `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD` alone for that same operation under `errorCodesToSuppress`, the same scale code for the unrelated differing-scale mismatch carrying no self-read, and the self-reference code for the matched-scale self-read; the different-field control is accepted. Every verdict is `KERNEL_CONFIRMED` and every rejected row's list is a complete singleton.

The accepted control was persisted, read back structurally as `[Source0] + 2`, and left the whole model `KERNEL_CONFIRMED`. The retained artifact has SHA-256 `2ac1313991b91af2ee3941a324816533c0da9e4bc7b642d0fa4adb438ad7f94d`, and a re-capture on an unchanged model reproduces byte-identically; the only row that moves after persistence is the accepted control's own dmtool-level `RK_NAME_EXISTS` name collision, which is a naming artifact of that write rather than a verdict change. The sibling stayed clean before and after. Wider expression shapes, other operand kinds, precondition slots, and any causal account of the ordering were not measured.

The computed-target self-read classes are decided in `CalculationService.testRuleStructure`, which asks `AnalyseService.usesSemanticIndexOrCategoriesForErrorField` about the **whole parsed condition** and reports terminally, before delegating to the canonical-form check that owns the plain class. That control flow is the locus of the cross-slot pre-emption and of the projected class covering an index key as well as an indexed read.

It establishes nothing about where the derived-scale gate sits, and no source claim about that position is made here or upstream: the scale-before-self-reference order rests on the measured suppression row alone, because the Kernel's own escape hatch cannot legalize a computation that still reads its target, so what surfaces underneath the suppressed gate is what that gate was reported ahead of. Read the locus to learn why the two self-read classes cannot be decided slot by slot; the measured verdicts remain the evidence.

Repeatable computation additionally uses `CalculatedFieldIterationLimitCreator`, `CalculationCache.getIterationValuesForGroups`, `markFieldsUsingInvalidIndexGroupsAsIncorrectlyCalculated`, `GroupFillCache.getExistingFieldInstances`, and `CodeGenCalculatedField.st`. These establish static route participation, suppression by invalid index columns, exact target-instance overlays, and locus-dependent coarser invalid marking. Hash-backed internal iteration order is not a public result-order contract.
