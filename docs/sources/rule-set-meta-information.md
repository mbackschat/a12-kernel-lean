# Rule-set meta-information source checkpoints

Checkpoints for the Kernel's rule-collection analysis services — derivations over a rule set rather than over a document. Document-level validation and computation routes start at the [`cross-layer-routes.md`](cross-layer-routes.md) overview; [`../SOURCES.md`](../SOURCES.md) remains the anchor registry for both and routes detailed checkpoints to their family shards.

<a id="src-mandatory-information-narrowing"></a>
#### Mandatory-information derivation reads a **32-bit-narrowed** threshold whose `-1` value collides with absence, measured locally through 2026-09-01

- `revision`: thirteen initial `:adapter:kernelProbe` requests at a12-dmkits `c2900fe7c91a55e0debd0124f847ee2de0f06186`, followed by the sentinel matrix at `47952f81e13fc63dd72d16f2bbbd24e8bb024654`; the sibling checkout was clean before and after, with `state: CLEAN` in every artifact. `dmtool` 0.13.0, Kernel `30.8.1` built and runtime. Model-level `mandatoryInformation` observation, no document and no rows — the capability a12-dmkits built for [`EXP-2026-08-30-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-30-01).
- `question`: `FeldOperationUtil.getZahlKonstanteWert` returns `(int) Math.round(Double.parseDouble(value))` for a numeric constant and `-1` for "not a numeric constant", so a rounding **and** a 32-bit narrowing sit in front of one sentinel. The entry asked whether the derived output distinguishes an authored literal from absence.
- `shape-found`: the entry's own `>= z` shape derives **nothing at all**, `mandatoryRootGroups` included, so it cannot discriminate. The shape that reaches the arm is the error-condition spelling of the same requirement, `NumberOfFilledFields(A, B) < z` — which yields the root group and no field. The positive control is a different shape entirely: `FieldNotFilled(A)` derives `/S1_DM/A` into `mandatory` and `mandatoryForRootGroup` and `S1_DM` into `mandatoryRootGroups`, so an empty result is a reading rather than a broken route.
- `claim`: **the derived answer follows the narrowed value and the guard is `> 0`.** Over one two-field list, `< 1` and `< 3` derive the root group; `< 0` and `< -1` do not. Four adjacent literals around `2^32` reproduce that boundary exactly: `< 4294967295` (narrows to `-1`) and `< 4294967296` (to `0`) derive nothing, while `< 4294967297` (to `1`) and `< 4294967299` (to `3`) derive it. `< -0.6` rounds onto `-1` and derives nothing.
- `witness`: **`< 3` against `< 4294967295` is the isolating pair.** Both are trivially true over a two-field count, so satisfiability is held constant and cannot explain the split; the models are identical but for the literal. A reader takes `4294967295` as a *stronger* requirement, and the derivation drops the rule. `< 4294967297` against `< 4294967295` closes it from the other side: adjacent literals, opposite answers, following the narrowed value and nothing else.
- `sentinel`: **the follow-up reaches `EvaluatorVisitor`'s `z != -1` branch and separates an authored `-1` from an ordinary true threshold.** Base `FieldNotFilled(A)` and `FieldNotFilled(B)` rules establish two known mandatory fields. Each tested rule then conjoins a count comparison with `FieldNotFilled(target)`, making target membership the public derived-field discriminator. `count >= 0`, `count > -2`, and `0 <= count` derive their target fields; the adjacent `count >= -1`, `count > -1`, and `-1 <= count` rules do not. All six exact comparisons are true at count two, so comparison truth cannot explain the split.
- `narrowed-sentinel`: the `2^32` pair identifies the same mechanism after conversion: `count >= 4294967297`, narrowing to `1`, derives its target, while `count >= 4294967295`, narrowing to `-1`, does not. This closes both the direct sentinel collision and its reuse after signed-32-bit wrap rather than inferring one carrier from the other.
- `field-sets`: the sentinel matrix yields `/Sentinel/A`, `/Sentinel/B`, `/Sentinel/GeZero`, `/Sentinel/GtMinusTwo`, `/Sentinel/NarrowOne`, and `/Sentinel/ReverseZero` in both `mandatory` and `mandatoryForRootGroup`, omitting the four paired targets whose literal narrows to `-1`; `mandatoryRootGroups` contains `Sentinel`. The earlier standalone count rules still move only the root-group set, so field membership depends on the surrounding recognized conjunction rather than on a count comparison alone.
- `limit`: one nonrepeatable root with two established mandatory inputs and one target per count guard, `en_US`, no repetition and no `Having`. This matrix covers direct strict and inclusive comparisons plus the reversed inclusive form at the `EvaluatorVisitor` site; the [later reversed-strict checkpoint](#src-mandatory-information-reversed-strict-count) closes the fourth spelling with its own signed-32-bit separator. Not measured: repeatable lists, the four `z > 0` collector sites beyond the earlier standalone-root-group shape, non-count rule families, or the complete composition rules for the three returned sets.
- `bytes`: [`mandatory-information-narrowing/`](../../evidence/kernel-30.8.1/captures/mandatory-information-narrowing/), eleven models with their requests and artifacts. The sentinel model, request, and artifact SHA-256 values are `f86a99451ff9c2279ca0b37b041c64bd2079a1bbfdd24f6703490f8420921d3d`, `46547d33b56c8435a1e2bd70b528a1ed0d5e120eec12424d35059c2f867d8699`, and `4523927ab35446f29a2005332867f6946be84594c58f1ceb7bf4313a830932ab`. The earlier isolating pair remains `< 3` against `< 4294967295`, with model hashes `87b9e61ae169ef051a6281d89a4f61ddb1170249e2a241aec81d656e0b50d4fb` and `d030f7d70631db76cfe64d99438fd213a48774c01e84e673283020bcd21f5948` and artifact hashes `ba63424d1cff1452eacaaee17eb1677f0dd244c99a1a679520b404591402afc7` and `2a98fde10a5069ecb78e006b5f5294dac4ec91f2188391e5b66a6b41ec94433f`. These are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) has no section for rule-set meta-information, and opening one remains a scope decision rather than a clause edit; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the broader obligation. [`EXP-2026-08-30-01`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-30-01) is resolved locally against these retained bytes.

<a id="src-mandatory-information-presence-matrix"></a>
#### Mandatory-information derivation separates field contributions, root-only contributions, and ignored rule shapes, measured locally on 2026-09-01

- `revision`: fifteen model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the sibling checkout was clean and unchanged before and after. The source-preparation route returned the exact matching JVM launcher; `dmtool --version` and every artifact report dmtool `0.13.0`, Kernel `30.8.1` built and runtime, and source state `CLEAN`. Every retained model passed persisted structured rule read-back and Kernel-confirmed `model check` before capture. No request contains a document or row.
- `question`: before choosing a checked carrier, which standalone presence rule shapes contribute fields, only their root group, or nothing to `mandatory`, `mandatoryForRootGroup`, and `mandatoryRootGroups`; and do severity or one `Having` filter change that classification?
- `shorthand`: `A`, `B`, and `Note` below name the corresponding absolute fields in each single-purpose model; `root` names that model's root group; `—` is the observed empty set.

| Case | Condition | Severity | `mandatory` | `mandatoryForRootGroup` | `mandatoryRootGroups` |
|---|---|---|---|---|---|
| `field-not-filled` | `FieldNotFilled(A)` | ERROR | A | A | root |
| `field-filled` | `FieldFilled(A)` | ERROR | — | — | — |
| `or-not-filled` | `FieldNotFilled(A) Or FieldNotFilled(B)` | ERROR | A, B | A, B | root |
| `and-not-filled` | `FieldNotFilled(A) And FieldNotFilled(B)` | ERROR | — | — | root |
| `not-all` | `NotAllFieldsFilled(A, B)` | ERROR | A, B | A, B | root |
| `no-field` | `NoFieldFilled(A, B)` | ERROR | — | — | root |
| `not-exactly-one` | `NotExactlyOneFieldFilled(A, B)` | ERROR | — | — | root |
| `not-collectively` | `FieldsNotCollectivelyFilled(A, B)` | ERROR | — | — | — |
| `at-least-one` | `AtLeastOneFieldFilled(A, B)` | ERROR | — | — | — |
| `all-filled` | `AllFieldsFilled(A, B)` | ERROR | — | — | — |
| `group-not-filled` | `GroupNotFilled(root)` | ERROR | — | — | root |
| `group-filled` | `GroupFilled(root)` | ERROR | — | — | — |
| `warning-not-filled` | `FieldNotFilled(A)` | WARNING | — | — | — |
| `having-control` | `FieldNotFilled(Note) Or NoFieldFilled(Rows*/Value)` | ERROR | Note | Note | root |
| `having-filtered` | `FieldNotFilled(Note) Or NoFieldFilled(Rows*/Value Having [Rows/Tag] == "hot")` | ERROR | — | — | — |

- `separators`: the negative and positive singleton field forms separate field contribution from no contribution; `Or` and `And` over the same two negative leaves separate field-set contribution from root-only contribution; the direct negative quantifiers cover all three result classes. ERROR `FieldNotFilled` against the corresponding WARNING form separates the measured severity arm. In the paired repeatable-child models, the filtered shape loses even the independent `Note` contribution retained by its unfiltered counterpart; this finite observation does not by itself identify the internal rejection step.
- `sets`: for all fifteen measured standalone rules, `mandatory` and `mandatoryForRootGroup` are equal. This is a property of the matrix, not a general equivalence claim; the wider service remains open precisely because cross-rule and root-relative composition may separate them.
- `limit`: one nonrepeatable root with two String fields for the flat cases; one repeatable child at maximum two with String `Value` and `Tag` for the filtered pair; `en_US`; ERROR and one WARNING control. Not measured here: multiple-rule interaction or closure, generated required rules, semantic-index operands, parallel iteration, cross-root references, concrete row indices, nested repetition, additional filters, count predicates already owned by the narrowing checkpoint, or every source-recognized rule family. INFO is owned by the later severity checkpoint below.
- `bytes`: [`mandatory-information-presence-matrix/`](../../evidence/kernel-30.8.1/captures/mandatory-information-presence-matrix/) retains fifteen model/request/artifact triples. The hashes below pin every retained input and observation; these are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `all-filled` | `71507e0d761502f22973f9090d14678b031f33ce692cbc99140592316a48ba33` | `4a0d8ec87e6f54d7e75d3b452c185e94fabc461e213e173df2019b989324ab22` | `4a3a876fe9a6e0f954510529d883bd7e7b0766cbacc086883333a23ca3966d43` |
| `and-not-filled` | `231691cacff37e5bf6b1d78cd789cdb61d0c9038099773120a0cec3627326c22` | `58bbc96d1d04acffe2449ecb0661933b5ef573e0162e4809e42c904e3b68acb6` | `f24af7b8fcbb4da2158dbd8de59bb6a24f6c3e7c843b57327b22c3c550701bdd` |
| `at-least-one` | `d0007d237ae579c8dd76d83186c45adf9a7428225611cadd16b5049133b26df2` | `180a2cba6299aea801be51331c03fc1dba9e3c8a88c1879471d0b2675e63562a` | `310ecc7ffa510e3fd2aecb8555c184ef5ab7089d903d4cfbf43fea9bed54e402` |
| `field-filled` | `ecfd9bae615c3f008fa24a264a97b223246a7ae9d910b296c1d13c2b1429a55a` | `525146697759fc36f0aebdd45491faf0b54451041b84b624029fe966f0591391` | `87eb4b578da774e6fa80bdec5014c5f809709088df4b7d32450791101fd1c285` |
| `field-not-filled` | `b72b4fc631c061d6f09bafb278a69050c4ae27b6c94b5b4b09c784aff65fad76` | `870edc95c65630117d0db4424185eb02fae4cc9e6bbb97986385aeac65c3e22f` | `f542b1158ca2d22808dd5b3960c46071d5451d08f49450e05422266217ecd3fc` |
| `group-filled` | `084ae9b224c7a3a12ec82a24c12b3ddb13d95343ed49a064d4ef37e46a854440` | `81d645e2efe4a68d204203a34b35bfa49dbbca0374bd337419fc98e17530f9aa` | `579d9575c1b826c3405f6c9f2a7f852edccb3a1421734a685992b843df90340a` |
| `group-not-filled` | `f9a4b5a1caab829d5fc5e067b53cb78634befd4c008e055bfd6c1d1b7fcd4a58` | `ab0468e29638e20940666b3eb1fa695d116dc0b2f17884e8a0b98d2c90ebe6a0` | `91e17d4014a94965c43b462ba1d2c9f6713b0026bdca155470926c3ad00f4732` |
| `having-control` | `5879a073ba26e4bc5ef240fd2c18afbf311d19ad9481a2e6b7549828f04c979e` | `2ad569678e673150a0df21e57d04774b8671a5ee8ef39149a01ccbc4a1480881` | `0f4782cf70968aceb3b2b956bc7c405d1d72c270869fb4af994149708e9779ae` |
| `having-filtered` | `a195e8f28254bf1b191118bbceb9b7c216d8dbca6ff5a593ec972b4ef2010098` | `1f8155cc2fd034ecb891ebc4b76f8452048d98508e333dd661e82e96cefaa558` | `2691663e4f35489a761c398a8f243237fb91cdfd4c604d1f8f20473c07303885` |
| `no-field` | `7c8f47e6ddf1d6c44a355a59ab184ea4fe6b6b79ceeb107208afdaf8ab9255b1` | `3c9bc86457a383ba0cbfd5c55d80d2f6ed63f272685550e2887b96946ea7627b` | `ff03fd2bb237b3b93dc438cdf9a0296271c65ff4a0907d14bdca6a6cc0eabafd` |
| `not-all` | `09af5c0a2173d386f52e21aa97b9a2940f6d339c74ddd036af09f4d980df72e8` | `4b8e4f342aeacdaf1669326096e503234e82d578a230c760a7d50962c525017e` | `28d99461932852f2a93d2836db02168c752c507c0880753bb90297b5d97b2841` |
| `not-collectively` | `e8a89601fdb7723659e4890b4a54163d12448145e124f68b00b6945d7b617379` | `f70d832879ffc822faa205568766d4a2415a65d6b5f1ad84d1ab2d8587b0da45` | `00ed92e0e3aae4f950767ea0f57aaf7bc132dfd2f30b2248d7d7b351e616015e` |
| `not-exactly-one` | `b1213c448d69b024e0968bd13ecc054fbf52850e47932dfe737f2decaea04f71` | `85c530f1b8d2bffa159d10f1ab15f731ecdf9811af3000d1532b671ceade11d1` | `2b1d299ce0d452110c528aa96757b9207f8403975cd578a15b6beedbd2aca7c5` |
| `or-not-filled` | `e4befb517e13d05664c5ada2c83933ccf93ee2c1a0fb1d3b8560cf5439939a56` | `4c11779349e4a57dbd5b4b6abbc5e6acb30c02a81609cddecf8944a85149d7c9` | `54a9238dc9b9c19cbdf5e0960d62d241269be3e992766f08226253722917b56b` |
| `warning-not-filled` | `c113493fa7537eff5e824355690bbe59889f0e98a16038e59334784b10009694` | `35229ed792d4aa8cbf1913e93a2d648e6488299b875fd03e7bc238aec540cdcb` | `011fba556c7bb92a4a817f941110b81cb49c3796a9ecb68a44a38496b3b996f1` |

- `sync`: **no `spec/` change.** The matrix closes a finite discovery slice but not the full rule-set collector or its composition semantics; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns current checked coverage.

<a id="src-mandatory-information-repeatable-presence"></a>
#### Mandatory-information derivation preserves connective and starred presence-operator identity, measured locally on 2026-09-02

- `revision`: five new model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`, plus the [presence matrix's](#src-mandatory-information-presence-matrix) retained `NoFieldFilled`/`Or` control at that same revision. Every artifact reports dmtool `0.13.0`, Kernel `30.8.1` built and runtime, and source state `CLEAN`; the sibling checkout remained unchanged.
- `preparation`: source-shipped dmtool emitted every new model through structured `model rename` from the retained control and changed only its condition through structured `rule modify`. Persisted structured rule read-back reproduced every exact condition, and `model check` returned Kernel-confirmed valid for all five models. Immediate artifact repeats were byte-identical.
- `question`: when one ordinary `FieldNotFilled(Note)` is composed with a singleton unfiltered starred-field presence quantifier, do `And` and `Or` differ, and may the collector collapse `NoFieldFilled`, `NotAllFieldsFilled`, and `AtLeastOneFieldFilled` because their runtime truth can coincide on a singleton list?

| Starred branch | Connective | `mandatory` and `mandatoryForRootGroup` | `mandatoryRootGroups` |
|---|---|---|---|
| `NoFieldFilled(Rows*/Value)` | `Or` | `Note` | root |
| `NoFieldFilled(Rows*/Value)` | `And` | — | root |
| `NotAllFieldsFilled(Rows*/Value)` | `Or` | `Note`, `Rows/Value` | root |
| `NotAllFieldsFilled(Rows*/Value)` | `And` | — | root |
| `AtLeastOneFieldFilled(Rows*/Value)` | `Or` | `Note` | root |
| `AtLeastOneFieldFilled(Rows*/Value)` | `And` | — | — |

- `claim`: both connective and operator identity are observable in the derived sets. `Or` with `NotAllFieldsFilled` makes the repeatable `Rows/Value` field mandatory, unlike the otherwise identical `NoFieldFilled` and `AtLeastOneFieldFilled` rows. `And` with either negative quantifier contributes only the root, while `And` with the positive quantifier contributes nothing. The collector therefore cannot normalize the singleton starred operators by runtime truth or apply one connective-independent rule.
- `separators`: the three `Or` rows isolate the starred operator while holding connective, ordinary disjunct, fields, root, capacity, and severity fixed; `NotAllFieldsFilled` alone adds `Rows/Value`. The three `And` rows independently separate negative root-only contribution from positive no-contribution. Each operator's `Or`/`And` pair fixes connective polarity without changing either operand.
- `limit`: one nonrepeatable root with fixed String `Note`, one unindexed repeatable child of maximum two with one String `Value`, one ERROR rule, singleton unfiltered starred lists, and `en_US`. Additional operands, nested repetition, filters, semantic indices, concrete indices, independent later rules, other logical compositions, rule execution, and wider topology remain outside.
- `bytes`: [`mandatory-information-repeatable-presence/`](../../evidence/kernel-30.8.1/captures/mandatory-information-repeatable-presence/) retains the five new model/request/artifact triples; the `NoFieldFilled`/`Or` control remains pinned in the preceding presence-matrix table. These are bounded raw captures with no typed replay, so they establish neither L nor C.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `no-field-and` | `a1ad6c786f6007794c65ffe46590346aff85ec31afe7976bafdbe96cb9980da0` | `c0d668a01096a7132f83399848ba9da3fe58146f3a858bffb73e54db473b04ed` | `8c9f868cb7d32b429bee5ddbf03aa4d9c63c10161cdb4eb233981017cb31d541` |
| `not-all-or` | `c564d4614d9570664e6a79b6edfa6aefe448620f0647064310f54adcdbaa75a2` | `dec4d5a9b53b6dca9055267f9bc3526f7bae11e4a1e1fb3cc0c823de28567b00` | `411f2a8a7660175153c79b34e76c1ba7d07f75a691dd97aaf8642c8440177b42` |
| `not-all-and` | `69a0328296970a711557690d5324828c59b8d6d1ca116694949074d2271d2350` | `69775e442cc40ab487456c8b24a0edaca1e57957ef0552aaeee485d8f130ddc9` | `14c6070b0eb7258912472ff3df2da33dc3537c0435c760f46e3281369cf6e953` |
| `at-least-one-or` | `eadadfc9d3f5053a77c79e888cc329eb9f79cf1fe9d9c632ef794ca4340ec527` | `3dd637aa93fdd549c44c7f9b33ac6d8be4241a3e943b0093ac3df3fb5efa7116` | `bd7109668669b2554800d7847f490ff0ecd80d0d11f8422f4e0efd4a490a3522` |
| `at-least-one-and` | `9152cbea78c685654f719d12bbff326f4c994f0bd19705736c24956510359f24` | `09dcff226019c88b29ee2b05b430b49e8a3e251d823d50406d0826628114cfbf` | `8d0e20720335db1c2bfcf10253911951734ff0c7820e08fe1ab993b3a449bbc9` |

- `sync`: **no `spec/` change.** This exact matrix extends SG14's checked carrier but does not define the complete repetition or rule-composition service; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) remains open.

<a id="src-mandatory-information-composition-matrix"></a>
#### Mandatory-information derivation distinguishes root-relative fields and closes finite dependency chains, measured locally on 2026-09-01

- `revision`: seven model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the sibling checkout was clean and unchanged before and after. The already prepared source-matched launcher, every retained structured rule read-back, and every Kernel-confirmed `model check` report dmtool `0.13.0` and Kernel `30.8.1`; every artifact reports the same versions, exact source revision, and source state `CLEAN`. No request contains a document or row.
- `question`: can `mandatoryForRootGroup` differ from global `mandatory`, does a root-mandatory seed promote that conditional field globally, and do field-dependency chains reach the same finite answer when their rule names place dependents before seeds?
- `shorthand`: `A`, `B`, and `C` below name the corresponding absolute fields in each single-purpose model; `root` names that model's root group; `—` is the observed empty set.

| Case | Rules in rule-name order | `mandatory` | `mandatoryForRootGroup` | `mandatoryRootGroups` |
|---|---|---|---|---|
| `field-conditional` | `FieldFilled(A) And FieldNotFilled(B)` | — | — | — |
| `field-chain-forward` | `FieldNotFilled(A)`; `FieldFilled(A) And FieldNotFilled(B)` | A, B | A, B | root |
| `field-chain-reverse` | `FieldFilled(A) And FieldNotFilled(B)`; `FieldNotFilled(A)` | A, B | A, B | root |
| `field-chain-two-hop` | `FieldFilled(B) And FieldNotFilled(C)`; `FieldFilled(A) And FieldNotFilled(B)`; `FieldNotFilled(A)` | A, B, C | A, B, C | root |
| `field-chain-broken` | `FieldFilled(C) And FieldNotFilled(B)`; `FieldNotFilled(A)` | A | A | root |
| `root-conditional` | `GroupFilled(root) And FieldNotFilled(A)` | — | A | — |
| `root-seeded` | `GroupFilled(root) And FieldNotFilled(A)`; `GroupNotFilled(root)` | A | A | root |

- `separators`: `root-conditional` is the first retained case where the two field sets differ, so a carrier must preserve them independently. Adding only the root seed promotes `A` into global `mandatory` and adds the root. The forward and reverse two-rule cases have the same final sets, while the reverse-named two-hop case reaches `C`; the broken-chain control with an unmet `C` guard retains only seed `A`. These are finite closure observations, not a general rule-order theorem.
- `limit`: one nonrepeatable root with two or three String fields; ERROR rules only; direct `FieldFilled`, `FieldNotFilled`, `GroupFilled`, and `GroupNotFilled` conjunctions; at most three rules and two dependency edges. The [later field-cycle checkpoint](#src-mandatory-information-field-cycles) closes one seeded and one unseeded direct two-node cycle. Not measured here: wider cycles, disjunction during closure, repeated or indexed fields or roots, nested groups, filters, semantic indices, parallel iteration, cross-root references, generated required rules, or additional rule families.
- `bytes`: [`mandatory-information-composition-matrix/`](../../evidence/kernel-30.8.1/captures/mandatory-information-composition-matrix/) retains seven model/request/artifact triples. The hashes below pin every retained input and observation; these are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `field-chain-broken` | `7a4ec5ae400bd6d01f2cd8919ae4697dcedd4df9544b4a17bc833942bb333337` | `0d17b6b67d1f1d4d4d36374fdcafaca480e762b0012b1209f877ef3f8079d57c` | `81adf0d1f9074cf04b05ff9a4facd14587c31f6f76879cead7462f329fad6785` |
| `field-chain-forward` | `8515e3a3e7e25d858d8d8246aa5bc3951d3f350f0d2d3a6534fa41696192eb4c` | `832188f2fd1f94bbf8e0747389b5e6ceef2dd4b360f40833347829766787dc46` | `d7a9ecf7fb545fc826deedb0ad6b31fb4f2c0a10b1392e0289db2f138c3c52f2` |
| `field-chain-reverse` | `adf58aa8a804952fbe2548b2633f5ad53fac104fe7389b778f78d020f777167e` | `91683671505fdf90be405db6a66b07066da759a0ec5cb3e7a0f71549190de44a` | `5b84b76e16898554a6ee5c23d6a371333870cfdb07aebc5b3bc216437aba9dfc` |
| `field-chain-two-hop` | `db1c43a883371539477d2906eca0f02154f5c6d8bb3c68dbcd965ebaed78483c` | `f4aa153513c557a1e2254f4aa75295c8826fddef64b01077e88639f5a2c0c0b4` | `53e3e78b020477391f0fd40928e6f3eb1a1defbc4ce22682fdde5b1a311a982e` |
| `field-conditional` | `fda5fb2aa6329e854132c3faf9c3768e961329bc2cb576577219b2b09575cf00` | `ef93f02c222240900bfb2f8fc9f791ea98c35a1b4ab74c88cbcf6a09d774e659` | `07d166890ad7c54d070fa762a53f41a26f8ffea1831fd3ac05ebcdfbf2ee06c9` |
| `root-conditional` | `dc33b50baef087ce835d0a9f831a217a094a3b3222c7f0be33a63b001a4a58d0` | `eb754e5c89ae7d70533ca6665f632dda4e604e209774c521da55ca8f557ce084` | `d9594e7329bd618ea465bb39979a8b50475d528bc91f263b4dd711fe26ef0f9e` |
| `root-seeded` | `f298d154499ffe6a51a5dae248690725189caf47fa10b624ab390a1f8b6923b6` | `da67701eb91dea767c85ff6ccf6434107464bcdfb170230c406746513750b82b` | `aef7d25c8f2f5b037dc061ac7d3e5ef62e06f6bc253d0960ed459f3214a1044a` |

- `sync`: **no `spec/` change.** The matrix settles a flat root-relative and dependency-closure slice, while [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) retains the wider collector obligation and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns current checked coverage.

<a id="src-mandatory-information-field-cycles"></a>
#### Mandatory-information derivation closes a seeded direct field cycle and leaves an unseeded cycle inert, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the source-preparation route, `dmtool --version`, individual typed structured rule read-back, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was created and changed only through structured `dmtool` verbs. The request carries no document or row, and an immediate repeat was byte-identical.
- `question`: does a direct `FieldFilled(premise) And FieldNotFilled(target)` dependency cycle invalidate or suppress the whole mandatory-information result, can a direct seed activate its component, and can an unseeded cycle sustain itself?
- `model`: one nonrepeatable root has disjoint two-node cycles `SeedA ↔ SeedB` and `IdleA ↔ IdleB`. Every guard sorts before the final direct ERROR `FieldNotFilled(SeedA)` rule; the idle component has no direct seed.
- `claim`: both returned field sets contain exactly `SeedA` and `SeedB`, omit `IdleA` and `IdleB`, and `mandatoryRootGroups` contains the root. A direct two-node field cycle therefore does not invalidate the ruleset; the seeded component closes while the unseeded component contributes nothing.
- `separators`: rejecting every dependency cycle returns no checked result instead of these exact sets; treating a cycle as self-supporting adds both idle fields; and one authored-order pass omits `SeedB` because both seeded guards precede the direct seed.
- `limit`: one nonrepeatable root, String fields, ERROR rules, two direct two-node field-guard cycles, one singleton seed, no list guards, repetition, indices, filters, generated rules, wider formulas, cross-root references, or wider topology. The [field-list cycle checkpoint](#src-mandatory-information-field-list-cycles) closes existential and universal list-guard identities in the same two-node topology, and the [longer-cycle checkpoint](#src-mandatory-information-longer-cycles) extends retained correspondence to exact four-node direct and multi-list-edge shapes. The checked carrier applies the already finite monotone closure internally.
- `bytes`: [`mandatory-information-field-cycles/`](../../evidence/kernel-30.8.1/captures/mandatory-information-field-cycles/) retains the model, request, and artifact. Their SHA-256 values are `bf961036cf210df4c7e2f0228b4b192f52ae57c808366dc978572e4ab75d3df5`, `f5c4c9810ab8f7049badfe2961293b40b25ce03c9606fef2758924256422bbc1`, and `e0c9e87298bdf74aa32fbd25709faca1a315e12a408ad18ebe77ee99ac87e455`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-declared-required"></a>
#### Mandatory-information derivation separates unconditional and parent-present field declarations, measured locally on 2026-09-02

- `revision`: two model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the source-matched launcher, structured field read-back, Kernel-confirmed `model check`, and both artifacts report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The models were created and changed only through structured `dmtool` verbs, each request carries no document or row, and immediate repeats were byte-identical.
- `question`: with no authored rule, does a nonrepeatable String field declared `required: true` or `required: ifParentPresent` contribute to the three public mandatory-information sets, and does an otherwise identical optional sibling remain absent?
- `model`: the unconditional model contains `AlwaysRequired` and `OptionalControl`; the parent-present model contains `ParentRequired` and its own `OptionalControl`. Each model has one nonrepeatable root, two String fields, and zero authored rules. Keeping the two declaration modes in separate models prevents the unconditional root contribution from promoting the parent-present field globally.
- `claim`: the unconditional model returns only `AlwaysRequired` in both field sets and its root in `mandatoryRootGroups`. The parent-present model returns an empty global field set, only `ParentRequired` in `mandatoryForRootGroup`, and no mandatory root. Both optional controls remain absent. These values establish declaration-derived requiredness without choosing whether the Kernel internally scans the declaration or materializes an additional rule.
- `separators`: treating both declaration modes as unconditional adds the parent-present field globally and makes its root mandatory; ignoring parent presence removes its root-relative field; treating every declared field as required adds either optional control. Separate models keep these differences observable without relying on cross-rule closure.
- `limit`: one isolated required declaration plus one optional String control per nonrepeatable single-root model, `en_US`, and no authored rules. Coexisting required modes, interaction with authored rules, repeatable or index fields, nested groups, custom required messages, other field kinds, generated computation validation, filters, indices, and wider topology remain outside.
- `bytes`: [`mandatory-information-generated-required/`](../../evidence/kernel-30.8.1/captures/mandatory-information-generated-required/) retains both model/request/artifact triples. These are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `always` | `d37b13ee31357fa1bd99f035141898ade2c12ea51e6aa8f83f3ffb2096c5a31a` | `2680cafb72d77152c7989787173803ff8538c0d1ec93a3f5d1066304b7abfe64` | `fd10b67bb3ec7cb63928ccf85e2a46c29417efde69a9fb030b62ea6db69618ec` |
| `if-parent-present` | `98022574933e42ba48ae6666bb3e256a6f4916b8d6ddd40b5e8c85cc015cf798` | `fe2f0b43c2c6af1a32fff3e896168cfe45583eb1eeb6bfc6ed8e7944c3f43225` | `010e8da336e55110685ba5ee48cefb0317e02b1bccc4ae40a52e2fea2f7c66de` |

- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-declaration-closure"></a>
#### Parent-present field declarations close from declaration, root, and direct-field seeds, measured locally on 2026-09-02

- `revision`: three model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; structured field and rule read-back, Kernel-confirmed `model check`, and every artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The models were created and changed only through structured `dmtool` verbs, each request carries no document or row, and immediate repeats were byte-identical.
- `question`: when a field declared `required: ifParentPresent` coexists with a source that already makes the same root mandatory, does it become globally mandatory through the established root closure, and do unconditional declaration, `GroupNotFilled(root)`, and direct `FieldNotFilled(seed)` sources agree?
- `model`: the declaration-pair model has unconditional `AlwaysRequired`, parent-present `ParentRequired`, an optional control, and no authored rules. The root-seed model has parent-present `ParentRequired`, an optional control, and one authored `GroupNotFilled(root)` rule. The field-seed model has parent-present `ParentRequired`, optional `Seed` and control fields, and one authored `FieldNotFilled(Seed)` rule.
- `claim`: the declaration pair returns `AlwaysRequired` and `ParentRequired` in both field sets plus its root. The root-seed model returns `ParentRequired` in both field sets plus its root. The field-seed model returns `ParentRequired` and `Seed` in both field sets plus its root. Every optional control remains absent. Together with the isolated parent-present checkpoint, these values establish promotion after root availability without choosing an internal generation mechanism.
- `separators`: leaving parent-present requiredness permanently root-relative omits `ParentRequired` from every global set here; ignoring an unconditional declaration omits `AlwaysRequired` and its promotion; ignoring an authored root seed empties the root-seed global result; and failing to derive a root from a direct field requirement omits `ParentRequired` in the field-seed model. The prior isolated case separates promotion from unconditional treatment.
- `limit`: one nonrepeatable String root per model, one parent-present declaration, one unconditional declaration or one authored direct root/field seed, and optional controls. Arbitrary finite combinations are internally executable, but external correspondence is exact only for these three two-source shapes. Wider authored guards, count or list interaction, repeatable or index fields, nested groups, other declaration kinds, and wider topology remain outside.
- `bytes`: [`mandatory-information-declaration-closure/`](../../evidence/kernel-30.8.1/captures/mandatory-information-declaration-closure/) retains all three model/request/artifact triples. These are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `declaration-pair` | `d712b63a8c3146e3cbec6d366a66a9134d71399a4a3a69069ee9ba5fe35b38b8` | `d96a1f2a573d9b7722dbfd53e40fb82d50a279885c91402f2c4b08513ee480e5` | `3e6fb35b6ac6511f728e485bb034b5539ef0759856f8d4fea04c3300fd2082a1` |
| `root-seed` | `c9c0aaf02e10ee183a6a08066d50c791f7d4ee6e4de0783709ce73c31240bdf2` | `86ed93c4a01532f837250319519d02da908898c150bb99bcef1882d89c26e9e8` | `9b73ca86bbde7635eb065d927bc1655e79c7e6b2177c0b811ed1491d6797bc0e` |
| `field-seed` | `ddc4f084ea3aebbdf00cc3beefbf2ad8109e70740e6bc7cc7863700bebb5fcbb` | `25b1aa032e6962a194866a93e803f8eed96431f28f17ffbeced43c02ee4f65a1` | `e442f6235dbcfedc774b145c33422e11e02e9886c886a4996caded408b795019` |

- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-semantic-index"></a>
#### A semantic-indexed operand excludes every referenced field from mandatory-information derivation, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; persisted structured rule read-back, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was created and changed only through structured `dmtool` verbs, the request carries no document or row, and an immediate repeat was byte-identical.
- `question`: when one ERROR rule disjoins an ordinary `FieldNotFilled(Note)` leaf with a keyed `FieldNotFilled(Rows/Value For "k1")` leaf, does mandatory-information derivation retain the ordinary leaf, drop only the indexed leaf, or exclude the whole rule?
- `model`: one nonrepeatable root contains ordinary `Control` and `Note` fields plus repeatable `Rows` at maximum two, indexed by `Key`, with a `Value` field. `AIndexedRule` is the disjunction above and names `Note` as its error field; separate later-sorting `ZControlRule` directly requires `Control`.
- `claim`: both field sets contain exactly `/SemanticIndex/Control`, and the root set is exactly `SemanticIndex`. Neither the ordinary `Note` leaf nor the keyed `Rows/Value` leaf contributes on this semantic-indexed rule shape; because the independent control shares that root, this capture does not separately attribute the root result.
- `separators`: dropping only the keyed leaf retains `Note`; treating the rule as an ordinary disjunction adds both referenced fields; ignoring every rule loses the independent `Control` result.
- `limit`: one literal-key semantic index into one repeatable indexed group, one ordinary sibling leaf in the same disjunction, one independent direct control rule, one nonrepeatable root, and `en_US`. The checked carrier models the excluded identity as contributing nothing, but this same-root capture externally separates only its field contributions. Other index expressions, concrete row indices, nesting, repetition topology, conjunction, filters, parallel iteration, interaction with other recognized rule families, and generated index rules remain outside.
- `bytes`: [`mandatory-information-semantic-index/`](../../evidence/kernel-30.8.1/captures/mandatory-information-semantic-index/) retains the model, request, and artifact. Their SHA-256 values are `fb2ceba45e97fa1cd191dfc16478418fa268bbf5c200e955c2161a578c7cd3c1`, `6f01e593796fe03d6986ce36c0b7c7349bed604df0dea60dbf57ed1425fb7692`, and `a93b172db336b5986a1d86f84e2a14a798192ac2a42289fa694110da97588efb`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-parallel-iteration"></a>
#### A parallel-iterated rule contributes no referenced field to mandatory information, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; persisted structured rule read-back, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was created and changed only through structured `dmtool` verbs, the request carries no document or row, and an immediate repeat was byte-identical.
- `question`: when a Kernel-valid parallel-iteration rule contains an ordinary guarded `FieldNotFilled(Demand/Note)` leaf and a positive read from an index-compatible sibling group, does that ordinary negative leaf contribute to mandatory information, and does an independent non-iterated rule remain visible?
- `model`: one nonrepeatable root contains root field `Control` and sibling repeatable groups `Demand` and `Capacity`, each at maximum two and indexed by a String `Key`. `AParallelRule` reports on `Demand/Note` and conjoins `GroupFilled(Demand)`, `FieldNotFilled(Demand/Note)`, and `FieldFilled(Capacity/Units)`; separate `ZControlRule` directly requires root field `Control`.
- `claim`: both returned field sets contain exactly `/Parallel/Control`, and `mandatoryRootGroups` contains exactly `Parallel`. The ordinary `Demand/Note` leaf and positive `Capacity/Units` reference contribute nothing on this exact parallel-iterated shape, while the independent control field survives; because that control shares the root, this capture does not separately attribute the root result.
- `separators`: extracting the ordinary negative leaf from the parallel rule adds `Demand/Note`; treating every repeatable reference as a mandatory field adds `Capacity/Units`; and ignoring every rule loses `Control`.
- `limit`: two sibling repeatable groups with same-named same-kind index fields, one guarded negative leaf on the error-field side, one positive field read on the joined side, one independent root control, maximum two, and `en_US`. The checked carrier models the excluded identity as contributing nothing, but this same-root capture externally separates only its field contributions. Other parallel formulas, unmatched rows, nested or framed joins, multiple joined groups, aggregate or filter interaction, semantic indices, concrete row indices, and generated rules remain outside.
- `bytes`: [`mandatory-information-parallel-iteration/`](../../evidence/kernel-30.8.1/captures/mandatory-information-parallel-iteration/) retains the model, request, and artifact. Their SHA-256 values are `4e19e13992d286b0093f84b01ec70b4bb82315630e469ea6564932bb8b776925`, `37f76e64f208d3cffd857b81e7ee1696a04ef53e103142950420d11953815fe2`, and `bd0ab29f23aafdce4960fe0b44c75dbf24a4828103387945992633da21d7be76`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-cross-root"></a>
#### A cross-root negative disjunction contributes neither referenced field nor its independent root, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; persisted structured rule read-back, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was emitted through `model rename -o` from the tracked `permit-multi-root-sections` seed at that revision and changed only through structured `dmtool` verbs, the request carries no document or row, and an immediate repeat was byte-identical.
- `question`: when one valid ERROR rule disjoins `FieldNotFilled` leaves from two nonrepeatable root groups, does either leaf or root contribute to mandatory information, and does an independent same-root direct rule remain visible?
- `model`: nonrepeatable roots `Applicant` and `Decision` contain `Note` and `Other` respectively; `Applicant` also contains `Control`. `ACrossRootRule` reports on `Applicant/Note` and disjoins the two roots' negative leaves; separate later-sorting `ZControlRule` directly requires `Applicant/Control`.
- `claim`: both returned field sets contain exactly `/Applicant/Control`, and `mandatoryRootGroups` contains exactly `Applicant`. Neither cross-root leaf nor the independently distinguishable `Decision` root contributes, while the independent `Applicant` control survives; because that control shares the `Applicant` root, this capture does not separately attribute the `Applicant` root result.
- `separators`: analyzing the cross-root disjunction as two ordinary negative leaves adds both fields and both roots; rejecting the entire rule collection loses the valid `Control` result; and retaining only the error-field root adds `Applicant/Note`.
- `limit`: two nonrepeatable roots, one two-leaf negative disjunction spanning them, one independent direct control in the error-field root, String fields, and `en_US`. The checked carrier models both roots as excluded with the identity, while this capture externally separates only `Decision`; `Applicant` remains masked by the control. Other cross-root formulas, guarded dependencies, more roots, repetition, filters, indices, generated rules, cross-rule closure across roots, and wider topology remain outside.
- `bytes`: [`mandatory-information-cross-root/`](../../evidence/kernel-30.8.1/captures/mandatory-information-cross-root/) retains the model, request, and artifact. Their SHA-256 values are `576047c9df788c07c383c618d3ecfccdf62cf195bf05724463fdf1f39dbc15f6`, `177b107dbf3297727469b72fb22fb4efb4e21c2c35ae08f9b86bc36ef08f0ee2`, and `7f3c56b74270e5f787395a64a480cd0057e897ecf109ada4803dfcb79ee465a1`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-generated-index"></a>
#### A generated optional repeatable String index requirement contributes nothing and supplies no global root seed, measured locally on 2026-09-02

- `revision`: three model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the artifact reports dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The index-only and direct-control models were created and changed only through structured `dmtool` verbs. The parent-present model was emitted through structured `model rename -o` from the index-only model, then extended and changed only through structured field verbs. Every model passed Kernel-confirmed `model check`; no request carries a document or row, and immediate repeats were byte-identical.
- `question`: does the model-derived requirement for an optional index field inside a repeatable group add that field to either mandatory field set, add its root, preserve later authored contributions, or seed global promotion of a same-root parent-present declaration?
- `model`: all three models have one nonrepeatable `GeneratedIndex` root with optional `Control` and one `Rows` group at maximum two whose optional String `Key` field is the declared index. The index-only case has no authored rule or other declaration. The direct-control case adds `FieldNotFilled(Control)`. The parent-present case instead adds an optional String `ParentRequired` field with `relativeToParent` requiredness.
- `claim`: the index-only case returns all three sets empty. The direct-control case returns exactly `Control` in both field sets and `GeneratedIndex` in the root set. The parent-present case returns global fields and roots empty and only `ParentRequired` in `mandatoryForRootGroup`. The generated index identity therefore contributes no returned member, leaves a later supported authored rule visible, and does not supply the global root seed that would promote the parent-present declaration.
- `separators`: treating the index field as ordinarily required adds `Rows/Key`; treating its parent dependency as a global root seed promotes `ParentRequired` into `mandatory` and adds the root; rejecting or ignoring the whole collection loses the direct `Control`; and erasing the generated identity from Analyze/Explain cannot distinguish this declared index from a model with no index declaration even though their checked query result agrees.
- `limit`: one optional String index directly inside one repeatable group at maximum two, one nonrepeatable root, no default or S-value, and separate same-root direct-control and parent-present compositions. This establishes returned-set behavior without asserting whether the Kernel internally creates and later excludes a rule. Required index fields, Enumeration indices with or without S-values, multiple or nested index groups, wider repetition, authored rules that mention the index, generated computation-validation rules, filters, joins, and wider topology remain outside.
- `bytes`: [`mandatory-information-generated-index/`](../../evidence/kernel-30.8.1/captures/mandatory-information-generated-index/) retains all three model/request/artifact triples. These are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `index-only` | `a50c793ac92ccf605a238120c06e406b60501fd64009ae4011a7098b9d7168c0` | `e22cc75be3080d0da609e53f021be2b81037584215207a0e59bc9d3b5294b57f` | `56cc88eabc827ac8d5764814ea59515a4004cc8f0318d094378c3c05ff59143f` |
| `direct-control` | `b51067bdbbcec4f0e5b1079bb5465da21f10a71aa40c341683d17c80e628e296` | `b9a0531cfd873980d9bd3ad8b733dc71f7616d2b844544c32d0c2c0afba5491c` | `d4c62cbf1fd0b2e205a55c142e187511f12f7b794de890ef7daba420e42cc8f6` |
| `parent-present` | `c409147f4187a3d1af90b9a3a7408288aa47272479e3cee21da58142f8fbd5e6` | `1db61a3d00a8a93ddd7766f90b1e328f68baf5b0ba301566e2b66fb7e3825403` | `4fe3bec70db395e3f32c947ce015a215e054c1b947b2c0d95995fc2d43563fcb` |

- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-generated-computation-validation"></a>
#### A direct nonrepeatable scale-0 Number copy's generated validation contributes nothing and supplies no global root seed, measured locally on 2026-09-02

- `revision`: three model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; every artifact reports dmtool `0.13.0` and Kernel `30.8.1` built and runtime. Each source model was emitted through structured `model rename -o` from its corresponding generated-index checkpoint control, then extended only through structured field and computation verbs. Persisted computation read-back and Kernel-confirmed `model check` succeeded; no request carries a document or row, and immediate repeats were byte-identical.
- `question`: does the implicit validation generated for one unconditional direct nonrepeatable scale-0 Number copy contribute its source or target to either mandatory field set, add their root, preserve a later authored contribution, or seed global promotion of a same-root parent-present declaration?
- `model`: all three models extend their paired generated-index checkpoint baseline with scale-0 Number `Source` and `Target` fields plus one unconditional `Target := Source` computation and localized self-validation message. The computation-only pair has no authored rule or other declaration; the direct-control pair retains `FieldNotFilled(Control)`; the parent-present pair retains the `relativeToParent` String declaration.
- `claim`: every result is byte-for-byte the same as its paired no-computation baseline. The computation-only case returns all three sets empty. The direct-control case returns exactly `Control` in both field sets and `GeneratedIndex` in the root set. The parent-present case returns global fields and roots empty and only `ParentRequired` in `mandatoryForRootGroup`. The generated validation identity therefore contributes no returned member, leaves a later supported authored rule visible, and does not supply a global root seed.
- `separators`: treating the generated validation's source or target as mandatory adds a field; treating either reference as a global root seed promotes `ParentRequired` into `mandatory` and adds the root; rejecting or ignoring the whole collection loses `Control`; and erasing the generated identity from Analyze/Explain cannot distinguish a computed target from an otherwise identical declaration even though their checked query result agrees.
- `limit`: one unconditional direct copy between same-root nonrepeatable scale-0 Number fields, one generated localized message, and the already measured optional String index baseline, with separate direct-control and parent-present compositions. This establishes only the model-level mandatory-information result, not generated validation execution or its internal condition. Constants, arithmetic, multiple or guarded alternatives, tolerances, other target families, repeatable targets or operands, cross-group placement, filters, indices in the computation, multiple computations, and wider topology remain outside.
- `bytes`: [`mandatory-information-generated-computation-validation/`](../../evidence/kernel-30.8.1/captures/mandatory-information-generated-computation-validation/) retains all three model/request/artifact triples. These are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `computation-only` | `91e883936c5757c99083210d9f414840d113d89388d43f4f58ed3449795aac80` | `a23c271f65f1bf6c27ba86d3cbe3414b568da5138ba9c8c6f990967a6cd354fa` | `ed8554ba5e098566d8e7d1711ba984f54eabf38ce42e79f209f732b44efa0129` |
| `direct-control` | `7d049ee7abea32ad9b965fe579c5bd22e994053a814b6fe385f42f8d9a9f7980` | `262d6dc9cba7298136637f0b4de92102cf5726d964f8bd6d1294aeceb6375315` | `149727d2052f71a9001e71cbdffae6e3204d8bfdae5596636124110c7d9ee0c6` |
| `parent-present` | `62f803331669c89e28cadc0c216af07a42b620ea4bbb57a10775049af55f6316` | `c5edbd2421e6d0a63a2794c2b3f75573ec6930db8a67aaad28c93524dff2c957` | `c1425da78cdf5f991551ce97678823bf3baf979dd54ffca68ae0f362b0dddfbe` |

- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-field-list-cycles"></a>
#### Mandatory-information derivation closes seeded existential and universal field-list cycles without self-support, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the source-preparation route, `dmtool --version`, individual typed structured rule read-back, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was created and changed only through structured `dmtool` verbs. The request carries no document or row, and an immediate repeat was byte-identical.
- `question`: when an `AtLeastOneFieldFilled` or `AllFieldsFilled` guard forms a two-node cycle with a direct field guard, does the cycle close from its available seeds, preserve existential versus universal truth, and leave an unseeded component inert?
- `model`: four disjoint two-node cycles sort before every seed. The existential arm receives one of two listed seeds; the universal-false arm receives one of two; the universal-true arm receives both; and the second existential arm receives neither. Each list-guard target points back to one listed field through a direct field guard.
- `claim`: both returned field sets contain exactly `AnyA`, `AnyB`, `AllFalseA`, `AllTrueA`, `AllTrueSupport`, and `AllTrueB`; they omit every unseeded support field, `AllFalseB`, and the whole idle component, while `mandatoryRootGroups` contains the root. List-guard cycles therefore retain their ordinary existential and universal entailment and do not support themselves.
- `separators`: rejecting every list-guard cycle returns no checked result; collapsing universal to existential adds `AllFalseB`; collapsing existential to universal omits `AnyB`; treating a cycle as self-supporting adds the idle component; and one authored-order pass omits both true targets because every guard precedes every seed.
- `limit`: one nonrepeatable root, String fields, ERROR rules, four two-node cycles with one two-member filled-field list guard and one direct back edge each, exact existential and universal identities, and zero, one, or two direct seeds. The [longer-cycle checkpoint](#src-mandatory-information-longer-cycles) separately covers exact four-node direct and all-list-guard components. Duplicate or empty lists, still-longer or mixed-edge cycles, repetition, indices, filters, generated rules, wider formulas, cross-root references, and wider topology remain outside Kernel correspondence; arbitrary finite admitted field-guard cycles share the existing monotone closure internally.
- `bytes`: [`mandatory-information-field-list-cycles/`](../../evidence/kernel-30.8.1/captures/mandatory-information-field-list-cycles/) retains the model, request, and artifact. Their SHA-256 values are `eb599c472fac015c6b06b2eba3fd923ed82196b1aac2568907b6ab627da3de77`, `6341fe9a58ba7e07576e66f885d110b956838ce060251e05bbf9b2366cd41522`, and `ee14f1d9c244ec45fac2f229a5edce0d2234f5ca18f0de737cc78947161fe08f`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-longer-cycles"></a>
#### Mandatory-information derivation closes exact four-node direct and multi-list-edge cycles without self-support, measured locally on 2026-09-02

- `revision`: two model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; source-shipped dmtool `0.13.0`, both structured model read-backs, both Kernel-confirmed `model check` results, and both artifacts report Kernel `30.8.1` built and runtime. Each model was emitted from its retained two-node predecessor through structured model and group rename plus rule modify, remove, and rename verbs. Neither request carries a document or row, and immediate repeats were byte-identical.
- `question`: do the retained two-node outcomes extend to a seeded four-node direct cycle and a reverse-authored four-node cycle whose every edge is a two-member filled-field-list guard, and can a separate unseeded two-edge universal-list cycle support itself?
- `direct-model`: the four direct guards encode `SeedA → SeedB → IdleB → IdleA → SeedA` in rule-name order before the final singleton `FieldNotFilled(SeedA)` seed. All fields are String members of one nonrepeatable root.
- `list-model`: the seeded component encodes `AnyA → AnyB → IdleB → IdleA → AnyA`; every edge is an `AtLeastOneFieldFilled` guard with one additional unseeded support field, and rule names place the three forward additions in reverse order before the final `FieldNotFilled(AnyA)` seed. A disjoint `AllFalseA ↔ AllFalseB` component uses `AllFieldsFilled` on both edges with an unseeded support on each and has no direct seed.
- `claim`: the direct artifact returns exactly `SeedA`, `SeedB`, `IdleB`, and `IdleA` in both field sets plus its root. The list artifact returns exactly `AnyA`, `AnyB`, `IdleB`, and `IdleA` in both field sets plus its root; it omits every support field and both `AllFalse` targets. These outcomes extend retained correspondence to the two exact authored cycle shapes without identifying the Kernel's private implementation.
- `separators`: one authored-order pass over either model sees only the final seed and omits every dependent field. Stopping the reverse-authored list component after one additional pass yields only `AnyB`; the retained result also contains `IdleB` and `IdleA`. Collapsing its existential guards to universal omits the whole seeded chain because every additional support is unseeded, while self-supporting universal cycles add `AllFalseA` and `AllFalseB` contrary to the retained result.
- `limit`: one nonrepeatable root per model, direct String fields, ERROR rules, one four-node seeded direct cycle, one four-node seeded existential all-list-edge cycle, one unseeded two-edge universal-list cycle, two-member duplicate-free lists, and `en_US`. Longer cycles, mixed direct/list cycles, other list sizes, partial universal seeding, repetition, indices, filters, generated rules, wider formulas, cross-root references, and wider topology remain outside Kernel correspondence.
- `bytes`: [`mandatory-information-longer-cycles/`](../../evidence/kernel-30.8.1/captures/mandatory-information-longer-cycles/) retains both model/request/artifact triples. These are bounded raw captures with no typed replay, so they establish neither L nor C.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `longer-direct` | `26541e96be9352b0cd8e4a4a5092660aa56d1605b808c63a69fa9cb6b5a6bd4e` | `2f10bea6f4e90312e6aeafbda377836ae8d7ec4261fa5044313a52c5b984f2e3` | `16beb1e7808c8d5f7bf25879990b1d29e4faf1ff577e97f234c4295069099a86` |
| `multi-list-edge` | `32c4b1a0b94dbf70aea6f971a864f01a37cc12b0a50c2b46d09d711ebb42d361` | `b9efcfd59dcaa6443e7cafadadca557cd1c95e3b499a2c4590ec578ac958668e` | `be7022954763b6b2bd0f797d759afaac8b6a1d3a7c553098966f0f61b405c1d6` |

- `sync`: **no `spec/` change.** This exact batch closes SG14's longer and multi-list-edge cycle-correspondence item but not the complete rule-set collector; [`spec/`](../../spec/) still has no rule-set meta-information section.

<a id="src-mandatory-information-field-list-guards"></a>
#### Mandatory-information derivation distinguishes existential and universal filled-field guards, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; `dmtool --version`, persisted structured rule read-back, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The request carries no document or row, and an immediate second run was byte-identical.
- `question`: when an ERROR rule conjoins `FieldNotFilled(target)` with `AtLeastOneFieldFilled(A, B)` or `AllFieldsFilled(A, B)`, does the positive list predicate become a guard over fields already derived mandatory, and do the existential and universal forms remain distinct when only one premise is established?
- `model`: one nonrepeatable root contains four disjoint guard cells. Rule names place all four guards before their direct seed rules. The existential-true arm seeds only `A1`; the universal-true arm seeds both `A3` and `B3`; the universal-false arm seeds only `A4`; and the existential-false arm seeds neither `A2` nor `B2`. Every target is named only by its own guard.
- `claim`: both returned field sets contain the four direct seeds plus exactly `TAnyTrue` and `TAllTrue`; they omit `TAnyFalse` and `TAllFalse`, while `mandatoryRootGroups` contains the root. The later seed rules therefore affect earlier-named guards, one established premise settles the existential, and the universal needs both. Standalone positive list predicates remain owned by the earlier presence matrix and contribute nothing by themselves.
- `separators`: collapsing both guards to existential would add `TAllFalse`; collapsing both to universal would omit `TAnyTrue`. Sorting guards before seeds separately rules out one authored-order pass. The two false arms keep an omitted target distinct from a broken observation because the same artifact retains direct seeds and both positive targets.
- `limit`: one nonrepeatable root, String fields, ERROR rules, two-member direct field lists, direct singleton seed rules, no repetition, indices, filters, generated rules, nested formulas, cross-root references, or cycles. Wider closure shapes and every excluded topology remain open.
- `bytes`: [`mandatory-information-field-list-guards/`](../../evidence/kernel-30.8.1/captures/mandatory-information-field-list-guards/) retains the model, request, and artifact. Their SHA-256 values are `ed331b473e98aec028659fcc384ca22b1fa883a9795257e8e83044e2033e71fd`, `43efcfbb4b331bc301a422a5b21695a7da56130808e50da1d694f5f7d0445d83`, and `c6de42804d3613cf1a18edaede9c02305254ccbc516cd1dcb308ffd2fcd26d77`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-reversed-strict-count"></a>
#### Mandatory-information derivation recognizes reversed strict filled-count guards and applies signed-32-bit narrowing, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the source-preparation route, `dmtool --version`, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was created and changed only through structured `dmtool` verbs. `rule read` preserved each literal-left comparison as verbatim canonical text with `converted: false`; that backstop confirms the persisted text but supplies no typed conversion for the literal-left position. The request carries no document or row, and an immediate repeat was byte-identical.
- `question`: does the previously unmeasured reversed strict spelling `z < NumberOfFilledFields(A, B)` participate in mandatory-field closure, and does that carrier read the same direct and signed-32-bit-narrowed `-1` sentinel as the other three comparison spellings?
- `model`: direct ERROR `FieldNotFilled(A)` and `FieldNotFilled(B)` rules sort after four guards. The guard literals are `-2`, `-1`, `4294967295`, and `4294967297`, each paired with its own isolated `FieldNotFilled(target)`.
- `claim`: both field sets contain `A`, `B`, `StrictTrue`, and `WrappedOne`; they omit `DirectSentinel` and `WrappedSentinel`, while `mandatoryRootGroups` contains the root. The reversed strict spelling therefore reaches the same final target result as the count-on-left strict spelling in this exact reverse-authored direct-seed shape. Direct `-1` suppresses target entailment even though `-1 < 2`, and the adjacent wrapped literals split according to signed-32-bit narrowing: `4294967295 → -1` suppresses while `4294967297 → 1` entails.
- `separators`: treating reversed strict comparison as unrecognized omits `StrictTrue`; treating `-1` as an ordinary threshold adds `DirectSentinel`; using exact mathematical magnitude rather than signed-32-bit narrowing omits `WrappedOne`. Sorting every guard before both seeds independently rejects one authored-order pass.
- `limit`: one nonrepeatable root, String fields, ERROR rules, one duplicate-free two-field list, direct singleton seeds, literal-on-left strict comparisons, and isolated targets. The [later false-comparison checkpoint](#src-mandatory-information-false-count-guards) closes the non-sentinel false arm for all four spellings. Absence, other operands, dependencies, repetition, indices, filters, generated rules, wider formulas, cross-root references, and cycles remain outside this observation.
- `bytes`: [`mandatory-information-reversed-strict-count/`](../../evidence/kernel-30.8.1/captures/mandatory-information-reversed-strict-count/) retains the model, request, and artifact. Their SHA-256 values are `073cf8c1e1cf65875d7bf61167877f2a799756e152addbbb7e212f850726107f`, `a08effb226b26aa906549070bde60a993b103488deb0567e2ae87975ba4248a6`, and `fcc6f0cb87efdebebef75ea118e84365acb2a74c79c639327068193d01772977`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-false-count-guards"></a>
#### Mandatory-information derivation admits false filled-count guards without contributing their targets, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the source-preparation route, `dmtool --version`, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was created and changed only through structured `dmtool` verbs. Count-on-left rules returned typed structured read-back; literal-on-left rules returned verbatim canonical text with `converted: false`, so the latter confirms persisted text without claiming typed conversion for that operand position. The request carries no document or row, and an immediate repeat was byte-identical.
- `question`: for each of the four filled-count comparison spellings, is a well-formed direct-seed guard whose comparison is false still a supported mandatory-information input with no target contribution, or does the whole analysis shape disappear?
- `model`: eight isolated guard targets pair true and false comparisons at a derived count of two: `count >= 2` versus `count >= 3`, `count > 1` versus `count > 2`, `2 <= count` versus `3 <= count`, and `1 < count` versus `2 < count`. Every guard sorts before direct ERROR `FieldNotFilled(A)` and `FieldNotFilled(B)` seeds.
- `claim`: both field sets contain exactly `A`, `B`, `GeTrue`, `GtTrue`, `ReverseLeTrue`, and `ReverseLtTrue`; they omit all four paired false targets, while `mandatoryRootGroups` contains the root. Comparison truth therefore controls target contribution rather than admission for these exact direct-seed, isolated-target guards.
- `separators`: treating every admitted guard as entailing adds all four false targets; rejecting or dropping an entire comparison spelling omits its paired true target; and one authored-order pass omits every true target because all guards precede both seeds. The direct seeds keep target omission distinct from an empty or broken observation.
- `limit`: one nonrepeatable root, String fields, ERROR rules, one duplicate-free two-field list per guard, direct singleton seeds, isolated targets, and the exact adjacent true/false thresholds above. The [later seed-cardinality checkpoint](#src-mandatory-information-count-seed-cardinality) separates zero, one, and two direct seeds. Absent and sentinel thresholds retain their earlier checkpoint; dependency-derived operands, target reuse, other operators, repetition, indices, filters, generated rules, wider formulas, cross-root references, and cycles remain outside this observation.
- `bytes`: [`mandatory-information-false-count-guards/`](../../evidence/kernel-30.8.1/captures/mandatory-information-false-count-guards/) retains the model, request, and artifact. Their SHA-256 values are `582e4a93060e6953a38221e90ea7a713542740c1ca7deee0d89d800ad5cb35a6`, `4c39e1d62607388964167295d7333e242439bf5ee9f2e3649694ca839993caa3`, and `b1c388536cb9414a93b0509c36f1e3a7a7eda8533259c656610714d3967cd633`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-count-seed-cardinality"></a>
#### Mandatory-information derivation returns the zero-, one-, and two-seed filled-count results, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; the source-preparation route, `dmtool --version`, individual typed structured rule read-back, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The model was created and changed only through structured `dmtool` verbs. The request carries no document or row, and an immediate repeat was byte-identical.
- `question`: does a duplicate-free `NumberOfFilledFields(A, B) >= 2 And FieldNotFilled(Target)` rule remain an accepted mandatory-information input when zero or one operands have direct singleton `FieldNotFilled` seeds, and does only the two-seed arm entail its target?
- `model`: three disjoint inclusive guards sort before their seed rules. The zero-seed arm has no standalone operand rule, the one-seed arm has only `FieldNotFilled(OneA)`, and the two-seed control has both `FieldNotFilled(TwoA)` and `FieldNotFilled(TwoB)`. Every operand without a seed appears only in its own guard, and every target appears only in its own guard position.
- `claim`: both returned field sets contain exactly `OneA`, `TwoA`, `TwoB`, and `TwoTarget`; they omit every zero-seed field, `OneB`, `ZeroTarget`, and `OneTarget`, while `mandatoryRootGroups` contains the root. The whole ruleset therefore returns normally without manufacturing absent operands or unmet targets, while the two-seed comparison entails its target after reverse-authored closure.
- `separators`: a checked account that rejects the whole ruleset unless every count operand is a direct seed returns no result instead of these exact sets; treating guard operands as mandatory adds `ZeroA`, `ZeroB`, `OneB`, and both unmet targets; and one authored-order pass omits `TwoTarget` because all three guards precede every seed.
- `limit`: one nonrepeatable root, String fields, ERROR rules, duplicate-free two-field lists, count-on-left inclusive `>= 2`, operands either direct singleton seeds or otherwise mentioned only in their own guard, and isolated targets. The zero- and one-seed output does not distinguish evaluating their false comparison from ignoring each guard locally; the checked carrier admits those isolated shapes to reproduce the observable whole-ruleset result without claiming that Kernel-internal mechanism. The [count-dependency checkpoint](#src-mandatory-information-count-dependencies) separately covers one exact dependency-derived operand and downstream target reuse. Other reused operands, target reuse by other rule shapes, other comparisons or operators, absent and sentinel thresholds, repetition, indices, filters, generated rules, wider formulas, cross-root references, and cycles remain outside this observation.
- `bytes`: [`mandatory-information-count-seed-cardinality/`](../../evidence/kernel-30.8.1/captures/mandatory-information-count-seed-cardinality/) retains the model, request, and artifact. Their SHA-256 values are `1f8b28f3a677e94b4bb0bf1c6c03270b92feb46a1a1881e3b9398120d83c19ab`, `a77d0d9aac64b209a09b20629c1485e9f8f68547e44879f7faa55ed3115d2bbb`, and `a1b50aecdcec411b2aa4d6a94f853e79990c6c2bbb8a9d9dae5bdb1bd574104b`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-count-dependencies"></a>
#### Mandatory-information derivation closes through a dependency-derived filled-count operand and reused target, measured locally on 2026-09-02

- `revision`: two model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; source-shipped dmtool `0.13.0`, fully typed structured model read-backs, Kernel-confirmed `model check` results, and both artifacts report Kernel `30.8.1` built and runtime. Both models were emitted from the retained seed-cardinality model through structured model and group rename, rule modify, and rule remove verbs. Neither request carries a document or row, and immediate repeats were byte-identical.
- `question`: when one operand of a reverse-authored `NumberOfFilledFields(OneA, TwoB) >= 2 And FieldNotFilled(ZeroTarget)` rule is itself the target of `FieldFilled(TwoA) And FieldNotFilled(OneA)`, does the count target appear only when the other direct operand is seeded, and may that target in turn guard two further negative fields?
- `models`: both models sort the count guard and the two `ZeroTarget`-guarded negative fields before the `TwoA`-guarded `OneA` dependency and the direct seeds. The true arm ends with `FieldNotFilled(TwoA)` and `FieldNotFilled(TwoB)`; the false arm removes only the `TwoB` seed.
- `claim`: the true artifact returns exactly `OneA`, `OneTarget`, `TwoA`, `TwoB`, `TwoTarget`, and `ZeroTarget` in both field sets plus its root. The false artifact returns exactly `OneA` and `TwoA` in both field sets plus its root; it omits the absent `TwoB`, the count target, and both downstream targets. These exact outcomes establish one dependency-derived filled-count operand and count-target reuse by two ordinary field guards without identifying the Kernel's private implementation.
- `separators`: rejecting every non-direct count operand returns no checked result instead of either exact set. Reading only the initial direct seeds omits `OneA`, while stopping before the count target becomes available omits `ZeroTarget`, `OneTarget`, and `TwoTarget` in the true arm. Treating an absent count operand as available adds those three targets to the false arm, and refusing count-target reuse omits both downstream targets from the true arm.
- `limit`: one nonrepeatable root per model, String fields, ERROR rules, one duplicate-free two-field filled-count list, count-on-left inclusive `>= 2`, one operand produced by a direct-seeded ordinary field guard, one direct operand, and one count target used as the premise of two ordinary field guards. Deeper operand dependencies, operands reused in multiple count rules or other rule roles, count-target reuse by non-field-guard shapes, other target cardinalities, other comparisons or count operators, absence and sentinel thresholds, repetition, indices, filters, generated rules, wider formulas, cross-root references, and cycles remain outside Kernel correspondence.
- `bytes`: [`mandatory-information-count-dependencies/`](../../evidence/kernel-30.8.1/captures/mandatory-information-count-dependencies/) retains both model/request/artifact triples. These are bounded raw captures with no typed replay, so they establish neither L nor C.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `dependency-true` | `1cf63f919ea7ea3e4553a2422f99be735df9e163f95c0d3ee822951c7f3358e9` | `c24f53690b7b8cb5a6ebaa9c12439b09294c58c7c8aad255e4e8d5fa42124247` | `cf9d3e06f44c26c163ac005873526ec0c5b88b0e5fe62024f2de88ff45f90e3a` |
| `dependency-false` | `d3b6bdf692dbdd3644229b5128e0831b60d5f85fa0bf2f450e5189a0b005b3ed` | `fdca52adbee76e4de68260e78d078016530b551b4a0f46c39fdd146a15cfdd6e` | `7e840d4f899e313349425110a18343ad72534885533d5541d5e20d9057b93646` |

- `sync`: **no `spec/` change.** This exact batch closes SG14's first dependency-derived filled-count route but not the complete collector; [`spec/`](../../spec/) still has no rule-set meta-information section.

<a id="src-mandatory-information-count-target-only"></a>
#### Mandatory-information derivation returns an isolated true count target and its root, measured locally on 2026-09-02

- `revision`: two model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; source-shipped dmtool `0.13.0`, fully typed structured model read-backs, Kernel-confirmed `model check` results, and both artifacts report Kernel `30.8.1` built and runtime. The true model was emitted from the retained seed-cardinality model through structured model and group rename, rule modify, and rule remove verbs; the false model was emitted from it by the same structured rename route and one rule modification. Neither request carries a document or row, and immediate repeats were byte-identical.
- `question`: when a ruleset contains only `NumberOfFilledFields(A, B) >= n And FieldNotFilled(Target)`, do isolated operands remain non-mandatory while the adjacent thresholds `n = 0` and `n = 1` separate target and root contribution?
- `models`: each model has one nonrepeatable root, two isolated count operands, one target, and one ERROR rule. The true arm carries the present threshold `0`; the otherwise identical false arm carries `1`. No field has a direct or guarded seed.
- `claim`: the true artifact returns exactly `Target` in both field sets and its root in `mandatoryRootGroups`, omitting both operands. The false artifact returns three empty sets. These exact outcomes close the previously unqualified target-only root separator without identifying the Kernel's private implementation.
- `separators`: treating listed operands as mandatory adds `A` and `B` in both arms. Ignoring an otherwise isolated count guard empties the true arm, omitting its target and root. Deriving fields without their root omits the true arm's root, while ignoring comparison truth adds the target and root to the false arm.
- `limit`: one nonrepeatable root per model, String fields, ERROR severity, one duplicate-free two-field filled-count list, count-on-left inclusive `>=`, isolated operands and target, present scale-0 thresholds `0` and `1`, and `en_US`. Other comparisons, list sizes, count operators, absent and sentinel thresholds, operand or target reuse, repetition, indices, filters, generated rules, wider formulas, cross-root references, and cycles remain outside this observation.
- `bytes`: [`mandatory-information-count-target-only/`](../../evidence/kernel-30.8.1/captures/mandatory-information-count-target-only/) retains both model/request/artifact triples. These are bounded raw captures with no typed replay, so they establish neither L nor C.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `target-only-true` | `811d660af9febc4217c787cf2d8a03efa7dc0836db64e0bd4f956f1aaadf94bb` | `cc8b0fa40d5b72970474ed3ca5e8fdaa86760cca8fcd5596c7c91447cde21776` | `dcfb9c2b7bd9f68e16cf8968f2389324945ee9021231f3db987542004d2cc808` |
| `target-only-false` | `30ebf8d7ef15ba2cfdaaeb22960efdc80949befe65707211707af00cfc2eb580` | `0d40abf41f8e2e56b23807f69449037a1b950ed0a474a85dffe2ce603b800971` | `9afe2995e303cc9bf8ac0901fd84ffe4c2f97baea2525a5ba76d6818c54df7c3` |

- `sync`: **no `spec/` change.** This exact batch closes SG14's target-only root separator but not the complete rule-set collector; [`spec/`](../../spec/) still has no rule-set meta-information section.

<a id="src-mandatory-information-info-severity"></a>
#### Mandatory-information derivation ignores INFO negative field rules, measured locally on 2026-09-02

- `revision`: one model-level `mandatoryInformation` request at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; `dmtool --version`, the individual structured rule read-backs, Kernel-confirmed `model check`, and the artifact report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. The request carries no document or row, and an immediate repeat was byte-identical.
- `question`: does an INFO `FieldNotFilled(InfoField)` rule participate in any mandatory-information set, or does the service retain only the otherwise identical ERROR `FieldNotFilled(ErrorField)` rule?
- `model`: one nonrepeatable root contains two String fields and two rules with identical negative singleton shape. The INFO rule sorts before the ERROR rule; individual persisted read-back confirms both severity values and both fully typed conditions.
- `claim`: `mandatory` and `mandatoryForRootGroup` contain exactly `ErrorField`, omit `InfoField`, and `mandatoryRootGroups` contains the root. Together with the earlier WARNING control, the exact flat singleton negative-field severity family therefore keeps ERROR contribution separate from WARNING and INFO no-contribution identities.
- `separators`: treating INFO as ERROR adds `InfoField` to both field sets; treating all severities as ignored removes the ERROR field and root, so the retained positive control distinguishes an empty collector from severity exclusion.
- `limit`: one INFO and one ERROR rule, direct singleton `FieldNotFilled`, one nonrepeatable root, String fields, and `en_US`. Wider conditions, compositions, generated rules, repetition, indices, filters, other severities on other rule shapes, and wider topology remain open.
- `bytes`: [`mandatory-information-info-severity/`](../../evidence/kernel-30.8.1/captures/mandatory-information-info-severity/) retains the model, request, and artifact. Their SHA-256 values are `66d672821af846eaa685a27d3a9abc7903fb26a99e9838aba75aea5f110b74ac`, `5f9c9ea37770beeaab154ba4b9960920928456ca80993bece731a24bbfec612a`, and `6f672ae0ad3c7e22dc1d851bd09b237088239cfbbc19445161e1d18b0c53d9c5`. This is a bounded raw capture with no typed local replay, so it establishes neither `L` nor `C` coverage.
- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.

<a id="src-mandatory-information-distinct-count"></a>
#### Mandatory-information derivation preserves filled-count and distinct-count operator identity, measured locally on 2026-09-02

- `revision`: two model-level `mandatoryInformation` requests at clean a12-dmkits `b9e7fbdc6b4806e15945bf7f993c04724a83437c`; `dmtool --version`, persisted structured rule read-back, Kernel-confirmed `model check`, and both artifacts report dmtool `0.13.0` and Kernel `30.8.1` built and runtime. Neither request carries a document or row, and immediate repeats were byte-identical.
- `question`: does `NumberOfDifferentValues(A, B) < 1` contribute the same mandatory root as `NumberOfFilledFields(A, B) < 1`, and may a `NumberOfDifferentValues(A, B) >= 2` conjunction enable `FieldNotFilled(DistinctTarget)` after both operands become mandatory as the corresponding filled-count guard does?
- `model`: the isolated control contains only the distinct-count `< 1` rule. The separating model sorts the distinct-count and filled-count `>= 2` guards before the two standalone `< 1` rules and the direct `FieldNotFilled(A)` and `FieldNotFilled(B)` seeds; each target appears only in its own guard.
- `claim`: the isolated distinct-count `< 1` rule yields empty `mandatory` and `mandatoryForRootGroup` sets and the model root in `mandatoryRootGroups`, matching the prior narrowing checkpoint's isolated `NumberOfFilledFields(A, B) < 1` result. The separating model yields `A`, `B`, and `FilledTarget` in both field sets, omits `DistinctTarget`, and yields its root. That exact standalone root contribution therefore agrees across these two operator identities, while mandatory-field entailment remains specific to the exact filled-count `>= 2` guard. The Lean theorem over arbitrary retained thresholds is an internal carrier law only and makes no wider Kernel-correspondence claim.
- `separators`: treating a standalone distinct count as ignored empties the isolated root set; reusing filled-count guard evaluation for the distinct-count operator adds `DistinctTarget`; and a single authored-order pass omits `FilledTarget` because both guards sort before both seeds.
- `limit`: one nonrepeatable root, String fields, ERROR rules, duplicate-free two-field lists, direct singleton seeds, `< 1` standalone rules, and count-on-left inclusive `>= 2` guards. Other comparisons and bounds, duplicates, absent or sentinel thresholds, dependency operands, repetition, indices, filters, generated rules, wider formulas, cross-root references, and cycles remain open.
- `bytes`: [`mandatory-information-distinct-count/`](../../evidence/kernel-30.8.1/captures/mandatory-information-distinct-count/) retains both model/request/artifact triples. These are bounded raw captures with no typed local replay, so they establish neither `L` nor `C` coverage.

| Case | Model SHA-256 | Request SHA-256 | Artifact SHA-256 |
|---|---|---|---|
| `distinct-root-only` | `eaddcfd16a8abdd169412b00aac4a53a9892175d792ee63d57a671d15a175331` | `8c7c6d12a140d907af28a258d6df2780ffc5ad04cd4df93c9bfb6729c2507b41` | `dd0bcd2008bc789b111b6ec548995972795badda62cada2ea76ed8b4169fcaa6` |
| `filled-versus-distinct-guard` | `c4688335bc0d35dc547154b3c5953325b0aa82d9fdfd7b96fbe84a97b4e78b91` | `7ef399227642b6e5bb30ca94abcdefbc054723eb71dccbb05c436101a0f793cd` | `4e3a548c0d2a7d559c7a9b91896bd28c46232e22fe8ace3082cedf38e7b5df54` |

- `sync`: **no `spec/` change.** [`spec/`](../../spec/) still has no rule-set meta-information section; [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation) owns the wider obligation, and the [implementation map](../IMPLEMENTATION-MAP.md#cap-flat-mandatory-information) owns the checked slice.
