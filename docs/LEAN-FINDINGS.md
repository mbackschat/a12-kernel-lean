# Lean formalization findings

This is the durable findings ledger for `a12-kernel-lean`, adapted from a12-dmkits' separation between kernel truth, implementation treatment, and live gaps. It records settled conclusions about how this project should formalize, prove, and empirically anchor the project-owned semantics. Language-neutral behavior belongs in [`../spec/`](../spec/); this ledger owns its Lean consequence and does not become a second semantic specification or backlog.

Each finding has a stable `LF<n>` identifier, date, semantic section, evidence basis, Lean treatment, and explicit limit. Findings are not deleted or renumbered; later corrections amend the record visibly.

## LF1 — differential evidence and proofs answer different questions

> Date: 2026-07-13. Sections: cross-cutting. Basis: project decision; Cedar-style executable-spec/production-engine separation; the local a12-dmkits (`a12-rulekit/`) corpus and differential architecture.

Kernel differential testing is the empirical backbone. Each executable Lean capsule must ultimately receive applicable retained portable observations before it is described as kernel-correspondence complete, but related clauses may be calibrated together and an internal capsule may land honestly as `external evidence pending`. Proofs establish universal consequences of the chosen Lean definitions; they do not establish that the external kernel implements those definitions. Fuzzing broadens the empirical search but proves neither internal laws nor universal correspondence.

The evidence topology has four distinct roles: the real kernel is behavioral authority; current a12-dmkits corpus and differential facilities provide focused kernel-facing observations when their existing shape fits, while a missing observation requires a concrete purpose-specific handback rather than an assumed generic exporter; compact observations remain checked out for ordinary Lean replay while historical raw audit units stay recoverable at pinned Git revisions with project-local archive records; the a12-dmkits interpreter is a clean-room peer for triangulation and divergence discovery, never the oracle. Contrary kernel evidence overrides agreement between Lean and the interpreter.

Lean treatment: every live fragment in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) separates internal assurance from external adequacy. A capsule without retained kernel observations is marked `external evidence pending`, even when it builds, executes, and has trusted proofs.

Limit: finite retained observations cannot prove universal kernel equivalence. A wrong Lean primitive can support flawless theorems, and a finite differential suite can miss a divergence; confidence comes from keeping both forms of evidence connected without collapsing them.

## LF2 — checked elaboration must own model and runtime-policy coherence

> Date: 2026-07-13. Sections: §2, §10. Basis: [`../spec/08-paths-and-references.md`](../spec/08-paths-and-references.md), read-only path differentials, Lean/Lean4Lean parser–elaborator separation, and `do` Unchained's explicit surface-to-core transformation pattern.

The flat evaluator originally accepted resolved IDs, typed field metadata, and already checked cells independently. That was a real semantic admission: a caller could pair one identifier with inconsistent kind/scale metadata or check its raw cell under a different policy.

Lean treatment: [`A12Kernel/Elaboration/Flat.lean`](../A12Kernel/Elaboration/Flat.lean) introduces a parser-independent surface subset and an expanded `FlatModel`. Resolution validates the model first, never uses first-match ambiguity, rejects repeatable scope, and lowers only legal scalar conditions into `CheckedFlatCondition`. The wrapper carries both model-validity and core-field well-formedness proofs. `FlatModel.checkContext` applies the same unique declaration policy to raw cells; unresolved or ambiguous IDs fail closed as malformed.

[`A12Kernel/Proofs/Elaboration.lean`](../A12Kernel/Proofs/Elaboration.lean) proves that an admitted field has one matching non-repeatable declaration, that raw-to-checked context construction uses exactly its `formalCheck` policy, and that lookup failure observes as validation `unknown .malformed`. This closes policy coherence for the checked surface route. The low-level core evaluator remains intentionally usable with an arbitrary `FlatContext` for isolated semantic laws, so the stronger claim does not apply to arbitrary direct calls.

Supported flat path treatment, corrected by LF6: structured absolute paths, plain parent-relative paths, and bare lookup in the kernel order declaring group → flag-gated model-wide unique name. Parent/ancestor lookup requires explicit `..`; it is not a hidden bare-name tier. A later narrow amendment reuses the declaration resolver and parent walker for one group-qualified direct-child star and correlated `Having`: [`A12Kernel/Elaboration/Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean) requires an explicitly declared repeatable group path, exact singleton scope, Number kind, supported comparison, both origins, and operator-specific scale legality before producing `CheckedSingleCorrelatedRule`. This is one closed lowering route, not general star or `$` syntax.

External limit, amended three times on 2026-07-13: [`ShortNameRefDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ShortNameRefDiffTest.kt) and [`NamedAncestorPathDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/NamedAncestorPathDiffTest.kt) establish broad kernel-backed provenance. The separating flat bundle covers explicit parent match/mismatch, absolute lookup, declaring-group precedence, unique model-wide fallback, duplicate rejection, and flag-disabled rejection. The correlation-elaboration bundle adds one exact star path and three static rejection classes plus an accepted mixed-scale ordering control. LF6 records the bare-name correction. Concrete parsing/quoting, named turning points, `RuleGroup`, semantic indices, nested or multiple stars, and general repeatable paths remain outside the admitted fragment rather than untested claims within it.

## LF3 — `$` correlation requires two explicit environments

> Date: 2026-07-13. Sections: §9, §10, §12. Basis: [`../spec/07-repetition-and-iteration.md`](../spec/07-repetition-and-iteration.md), [`../spec/01-data-model.md`](../spec/01-data-model.md), read-only kernel codegen/runtime source, a12-dmkits findings and interpreter treatment from the `a12-rulekit/` checkout, and retained kernel differentials.

The semantic mechanism is an explicit two-environment filter. A rule begins with a complete captured repetition environment `outerEnv`. A starred operand enumerates candidate rows and constructs an `innerEnv` for each candidate. Inside `Having`, ordinary references resolve against `innerEnv`, while `$`-marked references resolve against `outerEnv`; the reference's resolved repeatable level then selects its own coordinate. `CurrentRepetition(G)` and `CurrentRepetition($G)` therefore mean “read level `G` from the candidate or captured environment,” not “read one universal inner or outer row.” `$` is correlation, not positional selection.

The kernel source exposes the same mechanism at its data-flow boundary. [`IterationState.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/util/IterationState.java) carries the complete repetition-index vector plus the captured outer state, [`EbenenIterator.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/util/EbenenIterator.java) pairs that capture with each candidate while evaluating the filter, and [`RtInternalIdentifier.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/util/RtInternalIdentifier.java) first chooses captured or candidate state from the outer marker and then resolves every named path level from that state. [`RuntimeController.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/core/RuntimeController.java) resolves an empty Number to zero with fillability metadata and obtains `CurrentRepetition` from the resolved identifier's last repetition index; the document API states that repetition indices are 1-based in [`PathPart.java`](../../a12-kernel/kernel-md/kernel-md-document-v2/src/main/java/com/mgmtp/a12/kernel/md/document/apiV2/PathPart.java). A formally invalid field is not check-relevant, and [`BedingungsOperatorHelper.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/core/BedingungsOperatorHelper.java) maps a comparison reading such an operand to the kernel's collapsed false-or-unknown result. [`CodeGenFilterCondition.st`](../../a12-kernel/kernel-tool/kernel-core-codegen-condition/src/main/resources/internal/templates/validation/java/conditionsDir/CodeGenFilterCondition.st), [`CodeGenCondition.java`](../../a12-kernel/kernel-tool/kernel-core-codegen-condition/src/main/java/com/mgmtp/a12/kernel/core/codegen/internal/generator/backingbeans/conditions/CodeGenCondition.java), and [`ConditionSuffix.st`](../../a12-kernel/kernel-tool/kernel-core-codegen-condition/src/main/resources/internal/templates/validation/java/conditionsDir/ConditionSuffix.st) show that a generated `Having` predicate keeps only a known true result.

Star binding is inseparable from this mechanism. Bindings are retained only above the first star; a same-group star reopens that group and spans all its instantiated rows rather than silently collapsing to the current row; a multi-star path binds at the first star. The initial Lean reference meaning must be a straightforward candidate-row filter, not the interpreter's equality-bucket/hash-join optimization.

The most important edge cases are already exposed by the read-only evidence estate:

- Self-match is real. `[inner] == [$outer]` includes the outer row unless the authored filter explicitly adds `CurrentRepetition(G) != CurrentRepetition($G)`; the evaluator must not infer self-exclusion. See [`CorrelationLawsTest`](../../a12-rulekit/adapter/src/test/java/io/github/mbackschat/a12/dm/adapter/laws/CorrelationLawsTest.java) and [`OuterCorrelationDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/OuterCorrelationDiffTest.kt).
- Empty and malformed Number keys separate cleanly. Empty uses comparison-local zero at either origin; a malformed inner key drops only that candidate, while a malformed captured outer key drops every candidate whose comparison reads it. The checked neighboring non-law in [`Conformance/Correlation.lean`](../A12Kernel/Conformance/Correlation.lean) is that numeric `notEqual` is not Boolean negation of numeric `equal` on a malformed operand: both yield Lean `K.unknown` and keep neither row.
- Filtering precedes consumed-cell classification. A malformed consumed cell in a dropped row is not read; one in a kept row may make the consumer unknown. See [`SelfCorrelatedOverlapQuantifierDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/SelfCorrelatedOverlapQuantifierDiffTest.kt) and [`MultiStarFilteredCountDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/MultiStarFilteredCountDiffTest.kt).
- Validation and computation have different consequences inside the filter. Validation unknown drops a candidate because the filter is not true; computation reads contribute to dependency and poison behavior. See [`HavingOverComputedDependencyDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/HavingOverComputedDependencyDiffTest.kt).
- Polarity is not a simple consequence of selected values: filtered quantifiers/comparisons conservatively escalate, while filtered counts have an operand-dependent exception. See [`HavingQuantifierPolarityDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/HavingQuantifierPolarityDiffTest.kt), [`HavingStarComparisonPolarityDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/HavingStarComparisonPolarityDiffTest.kt), and [`FilteredCountPolarityDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/FilteredCountPolarityDiffTest.kt).
- Evidence transport must preserve the raw stored condition because the documented kernel formatter defect can lose a `$`-bearing conjunct; see [`KERNEL-FINDINGS.md`](../../a12-rulekit/docs/KERNEL-FINDINGS.md). The one-time raw/compact migration therefore recorded and byte-checked every executed stored condition before accepting the normalized compact requests. Routine replay now pins those requests and the historical source digest; the raw stored conditions remain recoverable through the [validation archive](archived/VALIDATION-RAW-EVIDENCE.md) rather than checked out beside them.

Lean consequence: represent selection as an ordered candidate-row filter with explicit inner and captured-outer origins, keep only known-true filters, and apply the selected consumer afterward. Checked lowering must establish group, scope, origin, kind, operator, and scale legality before constructing that core; an optimized join may be added only with a refinement proof against the straightforward selector. [`ARCHITECTURE.md`](ARCHITECTURE.md) owns the concrete current representation, [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) owns live support and proof status, and [`EVIDENCE.md`](EVIDENCE.md) owns the exact retained witnesses.

The first cross-level risk capsule makes the representation consequence executable. Its caller-established topology contains one starred parent level and one captured descendant; a filter requires the candidate parent row to equal both `CurrentRepetition($Parent)` and `CurrentRepetition($Parent/Child)`. Capture `{Parent = 1, Child = 1}` selects candidate `1`, while `{Parent = 1, Child = 2}` selects none. A scalar outer-row replacement changes the second result. Theorems establish exact named-level resolution and rule out one shared row when the captured coordinates differ. Missing or duplicate level bindings fail closed. The implementation remains source-grounded and `external evidence pending`; [`SPEC-2026-07-19-16`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-16---correlation-captures-every-named-outer-repetition-level) requests a focused existing-harness diagonal/off-diagonal lock rather than new capture machinery.

Limit: the durable mechanism and raw two-level adapter do not imply checked nested paths, captured-row instantiation, multiple stars, joins, cross-group execution, general consumers, computation poison, or filtered-result polarity. They also do not expose the hidden kernel truth value behind an observed suppressed message. Those axes require separate capsules and evidence rather than expansion of this finding's current implementation narrative.

## LF4 — Cedar is the primary Lean engineering precedent; Radix is a scoped secondary check

> Date: 2026-07-13. Sections: cross-cutting. Basis: filesystem audit of Cedar specification revision `3977eb4f017b421b7ac0b31ea4635e1dd36ce3ef` and Radix revision `617b67eb09681ca98e19759b48978866dcafeb17`.

Cedar's actual source tree, not merely its project description, is the best default for this repository's Lean engineering shape. `Cedar/Spec` contains ordinary executable semantic definitions with explicit result/error domains and observable short-circuit order; `Cedar/Validation` is separate from execution; `Cedar/Thm` is a recursively aggregated theorem hierarchy; unit tests, symbolic tests, differential interfaces, CLI/FFI, and protobuf transport are separate libraries or executables. Its Lake linter recursively checks that the theorem root imports every proof file. The differential/FFI boundary may use `unsafe` and timing/IO, while the semantics and theorem layers remain pure. Its style guide favors minimal sorted imports, theorem docstrings, explicit types, `simp only` for stable rewrites, and explicit proof steps.

Lean treatment adopted here: keep `Semantics`, `Elaboration`, `Proofs`, and `Conformance` distinct; maintain one mechanically complete proof root; state residual non-success outcomes rather than claiming accepted input cannot fail; keep portable differential transport outside the trusted semantics; and inspect the complete elaborated trusted environment plus its dependency closure for trust escapes. This repository's explicit axiom and compiled-behavior audit is intentionally stronger than Cedar's import-completeness linter alone; LF11 records the mechanism.

The first iteration capsule applies Cedar's primary pattern concretely: the executable `SingleStar.select` stays in `Semantics`, the ordered `SelectRows` relation and exact two-way bridge stay in `Proofs`, and separating fixtures stay in `Conformance`. The `sumSelected_filter_before_consumer` theorem is the small reachability/noninterference result this slice can honestly support: after selection, consumer-cell agreement is required only on selected rows. This is preferable to inventing a full read-footprint framework before computation or partial validation needs one. Radix remains only a secondary confirmation that an evaluator/relation bridge is useful when the relation exposes independent structure.

Radix is useful when a compact technique needs confirmation. Its `BigStep` relation is genuinely independent from its fuel-based interpreter, and it proves fuel monotonicity plus soundness/completeness for successful terminating runs. Its optimization passes pair transformations with forward semantic-preservation theorems. Its top-level `Slides.lean` imports the real Radix library through Verso, so presentation examples elaborate against live code. These are valuable patterns only with their exact limits: the relation omits failure/divergence outcomes, the optimization results are forward preservation rather than unrestricted equivalence, and the slides dependency tracks an unpinned branch in the audited project.

Decision rule: inspect Cedar first for a new Lean architectural or proof-engineering choice. Use Radix to validate a small relational/evaluator/transformation or checked-exposition pattern when Cedar does not provide a compact example. Do not adopt either project's headline theorem counts, dependency choices, or theorem wording without reading the exact source, result domain, and hypotheses.

## LF5 — empty handling is a layered consuming-clause policy, not a field-kind function

> Date: 2026-07-13; amended 2026-07-15. Sections: §2, §3, §4, §6, §7, §8, §9, §11, §12. Basis: a12-dmkits' kernel-verified operator catalog and structural checks, focused differential tests, retained operator-sensitive capture, and read-only kernel source inspection.

The expert rule “empty input depends only on field type” is a useful baseline for direct comparisons, but `only` is false. The catalog has exactly eight kind defaults: Number is `AS_ZERO`, Confirm is `AS_FALSE`, and Boolean, String, Enumeration, Date, Time, and DateTime are `NOT_EVALUATED`. It also has 31 operators carrying empty-operand deviation blocks: 36 separate claims because five operators distinguish operand positions, backed by 39 executable probes. Every default and deviation claim has at least one probe. [`CATALOG-SPEC.md`](../../a12-rulekit/docs/CATALOG-SPEC.md#4-the-emptyoperand-dimension) defines this as a deviation-only layer and explicitly separates empty operands from invalid/not-check-relevant values.

The decisive same-kind counterexamples are independently visible in tests and source. An empty String suppresses a direct comparison, while `Length(empty String)` produces fillable zero and can fire `0 < 5`; the kernel source explicitly says that `Length` returns zero instead of unknown ([`EmptyValueLawsTest.java`](../../a12-rulekit/adapter/src/test/java/io/github/mbackschat/a12/dm/adapter/laws/EmptyValueLawsTest.java), [`BigDecimalUtils.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/core/BigDecimalUtils.java)). An empty Date suppresses direct comparison, while the date/time extractors and `DifferenceIn*` functions return fillable zero for an empty operand ([`EmptyOperandFiringDiffTest.kt`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/EmptyOperandFiringDiffTest.kt), [`BedingungsOperatorHelper.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/core/BedingungsOperatorHelper.java)). Value-list membership, overlap predicates, and count/aggregate families further distinguish subject versus list-entry positions, `NOT_EVALUATED` versus `SKIPPED` versus `EVALUATES_FALSE`, and per-element treatment versus the identity of an all-empty selection.

Field kind and operator are still insufficient. An empty string-like result can suppress a direct comparison, contribute `""` inside concatenation, and produce `CLEARED` when stored as a standalone computation; [`FieldValueAsStringComputeDiffTest.kt`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/FieldValueAsStringComputeDiffTest.kt) pins the latter two contexts, while [`CalculationController.java`](../../a12-kernel/kernel-rt/kernel-core-runtime/src/main/java/com/mgmtp/a12/kernel/core/rt/_30_8/internal/core/CalculationController.java) rejects a no-value string at the computation store boundary. An index field is not another empty kind: its model role generates requiredness and uniqueness findings, so an empty or duplicate index cell becomes not-check-relevant before an aggregate or lookup consumes it ([`IndexAggregateDiffTest.kt`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/IndexAggregateDiffTest.kt), [`IndexedReadSuppressionDiffTest.kt`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/IndexedReadSuppressionDiffTest.kt)). Phase then controls whether findings expose validation unknown, computation poison, or—in the required-only carve-out—the underlying empty. A clean empty itself is not phase-normalized; it stays empty until a consumer interprets it. Row eligibility is an additional outer gate: the comparison defaults are probed in a content-bearing row because an entirely empty non-repeatable row may not be evaluated at all.

The evidence qualification matters. [`CatalogFacetProbesTest.java`](../../a12-rulekit/adapter/src/test/java/io/github/mbackschat/a12/dm/adapter/laws/CatalogFacetProbesTest.java) executes every catalog empty probe against the real kernel's Groovy-dynamic runtime and checks model acceptance, firing, and optional polarity. It does not run all 39 probes through the static-Java strategy. Separate differential tests tri-check both strategies for selected high-risk mechanisms, so the defensible claim is complete dynamic-runtime probe coverage of the catalog claims, not universal two-strategy coverage of every catalog cell.

Lean treatment: keep `CellObservation.empty` unresolved at the shared read boundary. Never expose a universal `FieldKind → Value` empty normalization and never make `Phase` an empty-substitution table. Each operator family owns named resolution clauses for the relevant operand positions and preserves the provenance its consumer observes: a symmetric clause may need given/substituted, while numeric polarity needs independent grow/shrink directions. Functions may turn empty into a concrete derived value such as fillable zero; otherwise no-value remains explicit so a comparison, concatenation, or store can make its own decision. Aggregates specify two independent clauses: per-element empty treatment and all-empty selection identity. Model roles add staged findings during elaboration/document passes rather than mutating the scalar kind policy.

The current design now demonstrates this boundary rather than merely preparing for it. [`Observation.lean`](../A12Kernel/Semantics/Observation.lean) leaves a clean empty as `.empty` and normalizes a parsed empty String to that same state; [`formalCheck_empty_observes_empty`](../A12Kernel/Proofs/Observation.lean) locks non-normalization at the phase read. [`FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean) then gives direct String equality and `Length` different resolvers: the former is not evaluated, while the latter becomes grow-only zero. [`StringLength.lean`](../A12Kernel/Proofs/StringLength.lean) proves that same-field operator distinction, and the retained six-case capture separates it externally. Direct Number comparison now delegates to directional fillability rather than treating every substituted firing as omission; LF10 owns that refinement. This remains narrow coverage: String presence/requiredness, String inequality, other Length operators, and the rest of the deviation matrix remain rejected or open. Each future deviation still needs a paired witness, a checked false generalization, and retained kernel observation.

Limit: the catalog deliberately leaves some construction and conversion families uncharacterized. The 31-operator inventory is therefore a verified snapshot of recorded deviations for kernel 30.8.1, not a proof that every other operator follows the kind default. Unknown matrix cells remain open evidence obligations rather than inferred defaults.

## LF6 — bare-name resolution is local-or-global, not an ancestor walk

> Date: 2026-07-13. Sections: §10. Basis: kernel parser source, two new kernel-confirmed rejection probes, retained positive local/model-wide cases, and a12-dmkits parser treatment.

The earlier three-tier account—declaring group, then nearest ancestors, then model-wide unique fallback—was falsified. It came from an earlier revision of [`spec/08-paths-and-references.md`](../spec/08-paths-and-references.md) and made the first Lean elaborator internally consistent but externally wrong. The original positive “bare ancestor” witness was underdetermined: its field name was globally unique, so both an ancestor walk and model-wide fallback selected the same field.

The kernel mechanism has two tiers. [`CheckIdNameRefImpl.java`](../../a12-kernel/kernel-tool/kernel-core-parser/src/main/java/com/mgmtp/a12/kernel/core/parser/internal/check/entity/CheckIdNameRefImpl.java) first constructs exactly `declaringGroup/name` and accepts that entity if present. On a miss, a non-absolute reference may use the flag-gated model-wide short-name index: one match resolves, multiple matches raise `MVK_FIELDNAME_NOT_UNIQUE`, and no match raises `MVK_INVALID_ENTITY`. It never walks ancestor groups. The a12-dmkits parser documents and implements the same local-candidate-then-global rule in [`Parser.kt`](../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/parse/Parser.kt).

The separating evidence holds all other relevant structure fixed. A bare `Limit` in a nested group with `Limit` at two ancestor levels is rejected as `MVK_FIELDNAME_NOT_UNIQUE`, instead of choosing the nearest. A second model with one unique ancestor but short-name fallback disabled is rejected as `MVK_INVALID_ENTITY`, instead of walking upward. Conversely, a same-named declaring-group field wins even when an ancestor has that name, and a unique cross-group name resolves when the flag is enabled; both positive cases are retained runtime observations. The current records live in the [compact validation bundle](../evidence/kernel-30.8.1/captures/validation-core-v1/semantic-observations.json), and their complete historical diagnostics remain recoverable through the [validation archive](archived/VALIDATION-RAW-EVIDENCE.md).

Lean treatment: [`FlatModel.resolveBare`](../A12Kernel/Elaboration/Flat.lean) now checks only `declaringGroup/name` before the flag-gated model-wide uniqueness query. The unused ancestor-search helpers were removed. [`Conformance/Elaboration.lean`](../A12Kernel/Conformance/Elaboration.lean) locks the local win, globally unique fallback, duplicate-name rejection, and flag-disabled rejection. Explicit parent-relative resolution remains unchanged and is covered by matching and mismatching retained observations.

This finding overrides LF2's original path-order wording. The corrected two-tier rule is now canonical in [`spec/08-paths-and-references.md` §2](../spec/08-paths-and-references.md#2-bare-name-resolution-order-); kernel 30.8.1 remains authoritative, while this finding retains the falsification and Lean-treatment history.

## LF7 — a malformed `Having` filter drops its row before consumption

> Date: 2026-07-13. Sections: §3, §9. Basis: the `having-malformed-filter-drops` record in the retained [compact validation bundle](../evidence/kernel-30.8.1/captures/validation-core-v1/semantic-observations.json), direct agreement of kernel 30.8.1 Groovy-dynamic and static-Java strategies, and the a12-dmkits triangulation split preserved in the [raw-evidence archive](archived/VALIDATION-RAW-EVIDENCE.md).

For rows `(Weight, Count) = (malformed, 100), (1, 3), (1, 4)`, the rule `Sum(Items*/Count Having Weight == 1) == 7` fires. The malformed Weight still produces its formal-error message, but its row-local filter comparison is not true and the row is dropped before Count is consumed; the remaining selected counts sum to seven. Both kernel strategies produce the authored OMISSION message and the formal error.

The historical a12-dmkits interpreter reported only the formal error for this witness. The exact external split and recovery identity are preserved in the [raw-evidence archive](archived/VALIDATION-RAW-EVIDENCE.md), while routine replay retains the kernel-authored outcome in the compact bundle. This does not weaken the Lean evidence: by LF1 the interpreter is a peer for triangulation, not the oracle, and the two agreeing kernel strategies determine the observation. It is recorded explicitly rather than hidden, dropped from the capsule, or encoded as a kernel-strategy divergence in the compact case.

Lean treatment: `SingleStar.select` evaluates the filter first and `Verdict.keepsHaving` keeps only `.fired`; validation `.unknown` therefore drops the row. `sumSelected_filter_before_consumer` proves the broader internal boundary that dropped-row consumer cells are irrelevant once selection agrees. The neighboring retained pair changes only the malformed consumer row's Weight from `2` to `1`: dropped means the authored rule fires, kept means the sum is unknown and the authored rule is silent.

Limit: the retained external message boundary establishes firing versus silence and the observed OMISSION signature, not the kernel's hidden internal representation of the malformed filter as Lean `Verdict.unknown`. This finding does not diagnose or fix the a12-dmkits interpreter's internal cause, and this repository must not modify that sibling project.

## LF8 — reusable Lean domains and kernel-backed public domains are different boundaries

> Date: 2026-07-13. Sections: §5, §9, cross-cutting protocol/evidence. Basis: cold-handover audit of the checked correlation API, all 12 retained runtime correlation projections, and the first language-neutral candidate suite.

The reusable correlation core deliberately accepts any positive duplicate-free candidate IDs and scans them in list order. That is a useful total semantic abstraction, but it is not harmless generality at the public kernel-correspondence boundary: `CurrentRepetition` compares the numeric `RowIndex` itself, while selection order comes separately from the list. Gapped or reordered IDs can therefore change comparison truth, not merely rename rows. Every retained kernel correlation observation uses actual contiguous one-based repetition indices `[1..n]`; no retained case establishes arbitrary positive IDs as kernel documents.

The same distinction appears in Number transport. The Lean account resolves exact rational values and applies scale-19 `HALF_UP` comparison, and the normalized decoder accepts canonical exact decimals. The runtime correlation projection schema transports integers, and the retained cases exercise only non-negative integer cells. Static evidence establishes selected declared-scale legality, but it does not establish dynamic negative, fractional, or scale-19 rounding behavior inside correlation.

Lean treatment: keep the reusable semantic and checked-elaboration domains general where that aids proofs and later evidence, but narrow the normalized `singleGroupCorrelation.firingRows` operation to non-empty contiguous candidate sequences. Machine-readable support metadata and the implementer kit state that general exact-decimal and signed correlation inputs are Lean-account behavior with external evidence pending. The handover methodology has two conceptual lanes: the finite candidate suite of retained cases and broader Lean differential/property generation outside that suite; it must never relabel the second lane as new external evidence.

This is not an argument to weaken internal types until they match today’s corpus. It is a rule for public support claims: a representation generalization becomes a released kernel-semantic capability only after its observable consequences are researched and retained. A future probe may close negative/fractional values or show that a different document adapter supplies row identity; until then, downstream implementers receive an exact answer and an exact claim class rather than being asked to repeat kernel research.

## LF9 — the bounded process contract depends on pinned Lean runtime details

> Date: 2026-07-15. Sections: cross-cutting process and differential testing. Basis: the pinned Lean 4.31 process API documentation and native runtime implementation, [`A12Kernel/Process/Bounded.lean`](../A12Kernel/Process/Bounded.lean), [`RelayMain.lean`](../A12Kernel/Process/RelayMain.lean), and the adversarial [`ProcessTestMain.lean`](../A12Kernel/ProcessTestMain.lean) gate.

Lean 4.31 documents that `IO.Process.Child.kill` terminates the whole POSIX process group when the child was spawned with `setsid := true`. The pinned native implementation makes that behavior depend on metadata stored in the Lean child handle. Its `Child.takeStdin` path returns a replacement handle without preserving the `setsid` bit, even though the operating-system process remains its session leader. A parent that spawns a candidate with piped stdin, calls `takeStdin`, and later calls `kill` can therefore kill only the candidate PID while leaving descendants alive. The public API shape alone does not reveal this liveness failure.

Lean treatment: the bounded parent starts a project-owned relay with null stdin, piped output, and `setsid := true`, so it retains the original child handle and its group-kill metadata. A capped request is placed in an automatically removed temporary file; the relay starts the candidate in the same process group, feeds its stdin, and streams candidate stdout and stderr without buffering them itself. Only after candidate stdin, exit, and both output streams complete does the relay overwrite that file with a strict completion record containing a magic value and canonical decimal candidate exit code. The parent bounds and parses that record independently, so candidate status and diagnostics cannot be mistaken for relay status.

Cleanup order is a safety invariant, not an implementation detail. Starting `child.wait` too early can reap the relay leader before the process-group kill; the operating system may then reuse its PID as another process-group ID, making a later group kill capable of targeting an unrelated group. The bounded parent instead treats EOF on both relay output pipes as the completion signal, kills the owned group while the unreaped relay leader still reserves its PID, and only then starts the bounded wait. It applies the same kill-before-wait sequence on timeout, stream failure, output overflow, and success, which also prevents a same-group descendant that closed its inherited streams from surviving an otherwise successful invocation.

The result is a finite resource boundary, not hostile-code containment. Input, stdout, stderr, execution, cleanup, poll, aggregate campaign, and result limits are explicit and checked, but the candidate runs with inherited credentials, working directory, and environment. A deliberately hostile child can inspect or modify anything available to that user, create a new session to escape the process group, or otherwise attack the host. The supported contract is cooperative POSIX execution on macOS and Linux; it is not a Windows contract and must not be described as a sandbox.

Toolchain consequence: every change to [`lean-toolchain`](../lean-toolchain), and every proposed extension beyond the declared macOS/Linux profile, must re-audit the process API and native `spawn`, `takeStdin`, `kill`, and wait implementation before the bounded-runner claim is reused. Then rerun `lake exe checkBoundedProcess` on every supported platform, including timeout, blocked input, exact and overflowing outputs, invalid UTF-8, nonzero status separation, and both success- and failure-path descendant cleanup. A compatible type signature or successful ordinary child execution is not enough to preserve this contract.

## LF10 — numeric polarity needs directional fillability, not a given bit

> Date: 2026-07-15. Sections: §2, §5, §7, §12. Basis: [`spec/10-validation-and-polarity.md`](../spec/10-validation-and-polarity.md), read-only kernel and a12-dmkits polarity mechanisms, the retained six-case operator-sensitive capture, red/green conformance, and trusted directional-polarity proofs.

The earlier flat evaluator attached only `given : Bool` to a comparison operand and classified every true comparison over a substituted value as OMISSION. That loses the reason polarity exists: OMISSION means filling could clear the currently true error condition, so the evaluator must know which directions filling can move the value. The bug is visible while holding the substituted value and operator fixed. An empty unsigned Number reads as zero and can grow but cannot shrink: `0 != -1` is therefore VALUE because filling cannot reach `-1`, whereas `0 != 1` is OMISSION because filling could reach `1`. An empty signed Number can move in either direction, so its `0 != -1` is OMISSION. A filled zero is fixed and makes each true inequality VALUE.

The String deviation exposes the same directional mechanism after a different consumer. Direct equality over an empty String is not evaluated, but `Length(empty)` is numeric zero whose future value can only grow. Consequently `Length(empty) < 5` fires as OMISSION, while `Length(empty) >= 0` fires as VALUE. The six retained cases hold the models and three-rule inventories fixed across empty, filled, and empty-row documents; both kernel strategies and a12-dmkits agree on the complete external message lists. This establishes the named observable outcomes, not every internal branch or every String and Number operator.

Lean treatment: [`A12Kernel/Semantics/NumericFillability.lean`](../A12Kernel/Semantics/NumericFillability.lean) owns `NumericFillability {canGrow, canShrink}` separately from exact `Rat` amounts. [`A12Kernel/Semantics/NumericComparison.lean`](../A12Kernel/Semantics/NumericComparison.lean) owns shared scale-19 normalization and computes whether movement on either operand can falsify a firing; `evalFixedRight` specializes the general evaluator with a fixed literal for reduced consumers. Filled scalars use `fixed`; empty unsigned Number and empty `Length` use `growOnly`; empty signed Number uses both directions. `NumericComparisonOp` contains all six direct ordinary operators, while each checked surface admits only its closed subset. The theorem [`growOnlyNotEqualWhenLeftNotBelow_is_value`](../A12Kernel/Proofs/NumericComparison.lean) states the reusable fixed-right law; conformance locks both `-1` and the predicted second instance `-2`, so a special-case patch for one literal would fail.

This finding corrects the universal one-bit Lean modelling note in [`spec/03-empty-and-required.md`](../spec/03-empty-and-required.md). A one-bit not-given flag can serve a simple symmetric consumer—Confirm's substituted false is the current example—but it is not the language-wide provenance account: numeric expressions and numeric aggregates need directional fillability, counts are grow-only, dates and strings use their separate not-given rules, and filters retain explicit escalation clauses in [`spec/10-validation-and-polarity.md`](../spec/10-validation-and-polarity.md). The public flat operation already admitted Number inequality, so the semantic correction changes the result of an accepted request such as empty unsigned Number `!= -1`; no public String/Length surface was added. The current account is 0.3.0 with V2 suites. The superseded 0.2.0 behavior, its OMISSION-versus-VALUE separating result, and the completed Rust capability are preserved only in the [archived historical record](archived/REFERENCE-SEMANTICS-0.2.0-AND-RUST-EXPERIMENT.md), not as current compatibility machinery.

Limit at the time of this finding: the first polarity helper had one fillable left expression and a fixed literal. LF28 and LF29 subsequently closed valid-operand arithmetic propagation, and LF34 now closes the plain two-expression ordinary-comparison route. Power and aggregate fillability, date symmetry, concatenation flags, filtered polarity, partial validation, and the UTF-16 astral boundary externally remain separate consuming-clause and evidence obligations.

## LF11 — trust boundaries must inspect Lean's elaborated environment, not source spellings

> Date: 2026-07-15. Sections: cross-cutting proof and executable trust. Basis: adversarial Lean 4.31 declaration probes, the audited Cedar separation between specification/theorems and compiler/FFI integration, and the project trust-gate review.

A line-oriented ban on `axiom`, `unsafe`, `partial`, or selected attributes is useful only as defense in depth. Lean permits declaration modifiers, same-line and multiline attributes, late `attribute` commands, and command macros. A macro can therefore expand an innocent-looking custom command into an axiom, while `implemented_by` can give compiled execution a different body from the kernel-visible definition and `extern` can route it to foreign code. A theorem-root `#print axioms` list also cannot expose an unused project axiom. These are mechanism-level gaps, not reasons to add another list of spelling exceptions.

Lean treatment: [`A12Kernel/Trust/Environment.lean`](../A12Kernel/Trust/Environment.lean) audits the `ConstantInfo` values owned by every imported trusted project module. It rejects any project `axiomInfo`, `isUnsafe` declaration, unclassified `opaqueInfo`, non-generated partial declaration, `implemented_by` mapping, `extern` entry, or dependency outside `propext`, `Classical.choice`, and `Quot.sound`. The opaque rule deliberately rejects source `partial` wrappers and bodyless inhabited stubs; a future legitimate opaque abstraction needs an explicit policy decision rather than passing accidentally. Lean also creates partial `_unsafe_rec` compiler helpers for total recursive definitions, so the audit admits that exact shape only when its parent is a kernel-visible safe `defnInfo` with the identical type. Treating every `isPartial` constant as authored `partial` would reject ordinary total recursion and would misunderstand Lean's compiled representation; accepting the suffix alone would let a forged helper borrow an unsafe or unrelated parent.

[`scripts/check-lean-trust.sh`](../scripts/check-lean-trust.sh) verifies proof-root imports, the named human-readable theorem registry, and positive source-zone closure. Every current project Lean source is classified; the logical closure admits only logical sources, conformance admits logical plus conformance, and the library root admits only its root plus those two zones. Evidence, binding, registry, reference, process, trust, and IO drivers therefore cannot enter merely because a future path was missing from a blacklist. Its adversarial probes elaborate attributed and macro-generated axioms, unsafe and partial declarations, late compiler/foreign attributes, `sorry`, `native_decide`, and opaque stubs, then require the environment audit to reject each for the predicted reason. An accepted control contains those words only as String data and uses an ordinary non-substituting attribute plus the reviewed logical axioms. Conformance remains outside the logical environment audit because its explicit `native_decide` examples are regression locks whose compiler/runtime trust is already classified.

Limit: this audit establishes the declared Lean project trust policy for the pinned toolchain; it is not a proof of Lean itself or of native compiler correctness. Toolchain upgrades must revalidate the environment APIs, generated-recursion classification, and adversarial probes. Source scanning remains deliberately non-authoritative, and the trust driver itself stays outside the trusted roots it inspects.

## LF12 — String computation has expression, store, and delta boundaries

> Date: 2026-07-15. Sections: §2, §7, §11. Basis: [`spec/09-computations.md`](../spec/09-computations.md), read-only kernel and a12-dmkits computation mechanisms, the retained [compact root-String projection](../evidence/kernel-30.8.1/captures/string-computation-v1/semantic-observations.json) and its [raw recovery record](archived/STRING-COMPUTATION-RAW-EVIDENCE.md), red/green conformance, and trusted evaluator/store/delta proofs.

An empty String does not have one context-free computation result. A bare field copy reads as clean no-value. Concatenation consumes the same missing operand as the empty contribution `""`, so an empty source plus `"-X"` evaluates and stores `"-X"`. But the fact that concatenation evaluated does not make an all-empty final text storable: `[Source] + [Other]` over two empty fields reaches evaluated text `""`, then the root String store treats that final empty text as no calculation. A stale target is therefore `CLEARED`; an absent target remains silent. This expression-versus-root distinction is now canonical in [`spec/09-computations.md` §2](../spec/09-computations.md#2-separate-operations-and-the-three-outcomes); the finding retains its evidence and Lean consequence rather than acting as a correction to that clause.

The 13 retained cases separate direct copy, suffix concatenation, and two-field concatenation while varying filled versus empty source, stale versus absent target, and content-bearing versus independently empty row. Groovy-dynamic and static-Java kernel strategies agree on every complete target-delta list. At the retained 699e-era revision the a12-dmkits interpreter agrees in ten cases but reports `VALUE|` for the three all-empty variants where the kernel clears or stays silent. That exact raw result is mechanically projected to kernel delta granularity and retained as triangulation, not hidden or used as the oracle. The mismatch identifies one reusable implementation rule: final computed empty String must be rejected at the root store boundary even though empty remains a legal intermediate concatenation contribution. a12-dmkits later confirmed and fixed that rule as IF123; the correction strengthens current triangulation but never rewrites the historical capture.

Lean treatment: [`Semantics/StringComputation.lean`](../A12Kernel/Semantics/StringComputation.lean) separates `StringTerm` expression results, `StringStore` root decisions, and `StringDelta` reporting against a typed prior target. `StoredString` makes an empty stored target unrepresentable. Computation-phase field reads preserve clean absence versus poison, concatenation is left-to-right and short-circuits after a left poison, while an empty left contribution still reads a poisoned right operand; root storage alone collapses evaluated `text ""` to no-value. The trusted proof spine connects `CheckedCell` observation to the library evaluator, proves empty contribution on both sides, term-level associativity with poison order, store and delta preservation under a missing contribution, final-empty rejection, typed change reporting, and the nearest stronger non-law: missing is not an identity at the intermediate term layer. This makes root-store or delta preservation—not raw expression equality—the safe boundary for a String refactoring. Term-level associativity alone does not authorize reassociating `StringExpr` trees across effectful reads; that future transformation needs an evaluator-level preservation theorem with explicit context assumptions.

Limit: the external record contains only unconditional, non-repeatable, clean String inputs and unconstrained String targets. It directly establishes final `VALUE`/`CLEARED`/silence deltas, not Lean's intermediate term/store states. Poison and its immediate clear projection are source-grounded internal account behavior but are not distinguished by this record. LF13 subsequently closes the positive one-bound String target-length slice, and LF18 adds exact one-address application with partial external calibration. General target constraints, alternatives, dependency graphs and scheduling, repeatable scope, implicit validation, non-ASCII strings, and general concatenation formatting remain separate capsules.

## LF13 — String target outcome, delta, and application are separate boundaries

> Date: 2026-07-15. Sections: §3, §7, §11. Basis: [`spec/09-computations.md`](../spec/09-computations.md), read-only kernel and a12-dmkits computed-result mechanisms, the retained nine-case [compact root-String projection](../evidence/kernel-30.8.1/captures/string-computation-v1/semantic-observations.json), its [raw recovery and agreement record](archived/STRING-COMPUTATION-RAW-EVIDENCE.md), red/green conformance, and trusted target-check/delta/application proofs.

A nonempty root String write attempt is not yet a successful computation result. The target's reduced computed-result check classifies that attempt as accepted or errored before reporting compares it with the prior target. An accepted value equal to the prior typed value remains a clean accepted outcome but produces no change delta. A target error is different from both clean no-value and computation poison: it retains the attempted stored String and a cause, and `ERRORED` is reported unconditionally whether the prior target was absent, stale, or already equal to the invalid attempt. Equality suppression therefore belongs only to accepted `VALUE`, never to `ERRORED`.

The archived nine-case capture holds the copy expression and target address fixed across positive `maxLength` 4 versus 3 models, with absent, stale, and equal prior-target cases; adds exact positive `minLength` 5 boundary and short controls; and adds a padded `" " + [Source] + " "` result accepted exactly at `maxLength` 5. Groovy-dynamic and Java-static kernel strategies agree on all nine complete rich computation results, change-only deltas, and exact applied-target observations. The a12-dmkits interpreter agrees on all nine projected deltas and stored-value application views. The equal accepted case is still present in the kernel's clean-result collection with `changed = false`, while the equal invalid case remains payloadful `ERRORED`; the padded control establishes for this admitted ASCII result that leading and trailing blanks survive the reduced target check rather than receiving the baseline input-field blank rejection. a12-dmkits remains triangulation, not evidence authority. F11 records the old compact-signature gap; the historical `compute-observation-v1` boundary transported rich per-runner observations with explicit unavailable fidelity instead of pretending the compact route was lossless. That producer estate was later retired without changing the retained observations.

Lean treatment: [`Semantics/StringComputation.lean`](../A12Kernel/Semantics/StringComputation.lean) inserts `StringTargetLengthPolicy`, fail-closed `StringTargetCheckResult`, and `StringTargetOutcome` after `StringStore` and before delta projection. The admitted policy is unconstrained, one positive minimum, or one positive maximum. Produced text containing CR or LF is `unsupported` because this capsule does not yet model target line-break permission; input-ingestion normalization is a separate earlier boundary. Admitted no-line-break text is accepted or errored. `StringTargetOutcome.errored` and `StringDelta.errored` both carry the attempted nonempty `StoredString` and the language-neutral `tooShort`/`tooLong` cause. Accepted results retain the established change-sensitive delta rule, while errors bypass prior-target equality. `appliedValue` deliberately exposes only whether a stored value remains: accepted yields that value, while no-value, poison, and error yield none. The proof spine establishes strict violations, inclusive acceptance on both permitted sides, exact boundaries as corollaries of those inclusive laws, universal no-value and poison bypass, accepted-outcome preservation of the unconstrained delta, attempt/cause preservation, unconditional error reporting, and the nearest stronger non-law: equal value-only application results do not imply equal deltas because no-value can be silent where `ERRORED` remains visible. LF18 later adds the separate exact-state transition rather than changing this lossy projection. The compact record externally exercises exact boundaries and violations but not strict permitted-side acceptance, no-value/poison bypass, or target line-break routing, so those remain internal laws or fail-closed boundaries rather than kernel-correspondence claims.

Limit: the kernel-correspondence record is a positive, one-bound-only String-length capsule over nonempty conservative ASCII values. The internal length theory is broader over nonempty text with no CR or LF, but that broader domain is not externally established; CR/LF fails closed until the earlier line-break clause is modeled. The capsule does not admit a zero bound, simultaneous minimum and maximum, patterns, enumerations, charset/newline checks, the general ordering or interaction of target constraints, `noValueValidation`, other target kinds, target rendering beyond the retained String forms, or the supplementary-plane UTF-16 boundary externally. LF18 now connects the record's absent-versus-present-empty states to a narrow exact one-address Lean transition, but no-value and poison bypass are still not exercised by this target record. Initially present-empty targets, direct poison application, preconditions, alternatives, computation ordering, repeatable targets, implicit validation, checked model/path lowering, and public protocol exposure remain open.

## LF14 — CRLF normalization is an ingestion boundary, not merely a target-length helper

> Date: 2026-07-17. Sections: §2, §7, §11. Basis: [`spec/02-logic-and-formal-errors.md` §B.3](../spec/02-logic-and-formal-errors.md#b3-what-puts-a-cell-in-the-third-state), [`spec/06-strings-and-enumerations.md` Part A](../spec/06-strings-and-enumerations.md#part-a--7-strings-and-patterns), [`spec/13-lean-encoding-guide.md` §§2–3](../spec/13-lean-encoding-guide.md), the a12-dmkits IF124 dual-route probes and IF125 review discriminator, and the current fail-closed Lean String target boundary. The behavior is canonical in `spec/`; no local retained CR/LF packet yet establishes Lean correspondence.

Kernel 30.8.1 normalizes CRLF to LF when an input value enters evaluation, before the normalized value populates the evaluation caches. The effect therefore reaches every later consumer of that evaluated String, including computations and length checks; it is not a local cleanup performed only by one target validator. Lone CR remains unnormalized. The same investigation fixed a distinct clause-order issue in which line-break handling precedes enumeration-domain validation. A separate IF125 discriminator found that a raw-type `Length(nvv) > c` rule can be eliminated during code generation rather than evaluated with an ordinary empty or normalized operand. These are different mechanisms and must not be collapsed into one “String length” branch.

Lean treatment: [`normalizeEvaluatedString`](../A12Kernel/Semantics/String.lean) now performs one non-overlapping CRLF-to-LF pass when [`formalCheck`](../A12Kernel/Semantics/Observation.lean) constructs the checked evaluated String. Both phases, direct comparison, UTF-16 `Length`, and computation reads consume that cached result. Existing LF and lone CR are preserved. The checked counterexample `"\r\r\n"` is essential: one pass yields `"\r\n"` and a second pass would yield `"\n"`, so global idempotence is false and no consumer may normalize independently. This capsule remains `external evidence pending`; no retained portable packet currently exercises the CRLF/LF/lone-CR matrix. The current direct comparison surface is field-to-literal only, so a future external matrix must either use that exact legal shape or separately add and prove field-to-field comparison before replay; one shape must never be silently projected into the other. The existing target-length capsule still rejects produced text containing CR or LF as `unsupported`, because input-ingestion normalization does not decide target line-break permission. Enumeration ordering, forbidden-line-break behavior, raw-type rule elimination, and exact stored-document preservation remain later capsules rather than consequences inferred from this one.

Limit: IF124 and IF125 are confirmed and guarded in current a12-dmkits, but that project remains knowledge, corpus/differential infrastructure, and clean-room triangulation rather than the oracle. Until their complete kernel observations are retained and replayed here through a fit-for-purpose accepted route, they justify the fail-closed design and future scenario matrix, not a new Lean correspondence claim or expansion of the supported String domain.

## LF15 — A dependent computation read has two failure domains, and requiredness is not ordinary poison

> Date: 2026-07-17. Sections: §3, §11. Basis: the direct String-cascade red/green proof study under Lean 4.31.0, the existing phase-indexed observation laws, and the accepted five-case cascade packet's public separating behavior.

Mapping a dependent `readTerm` over `withDependencyOutcome` intentionally produces a nested result. The outer `Except StringDependencyFault` says whether the producer outcome can form a computation dependency; the inner `Except StringComputationFault` says whether the later field read belongs to the admitted String fragment. Flattening the pair, or proving only the successful inner projection, would hide a real boundary.

The proof study exposed the nearest false generalization. A synthetic cell with one ordinary finding observes as computation poison, but the same statement is false for `.required`: computation observation deliberately skips validation-scoped requiredness. The reusable proof law therefore requires `cause ≠ .required`, and `StringDependencyCell.ofOutcome (.poison .required)` rejects the overlay instead of manufacturing absence or poison. Accepted, no-value, target-error, ordinary inherited-poison, and rejected-required branches remain separately stated and checked.

Lean treatment: [`Proofs/Observation.lean`](../A12Kernel/Proofs/Observation.lean) owns the phase-level clean-value and non-required-poison equations. [`Proofs/StringCascade.lean`](../A12Kernel/Proofs/StringCascade.lean) exposes the outer `Except.map` with a reviewed local rewrite set, then uses those observation laws. No global `[simp]` rule or semantic API change was needed. The nontrusted [`StringCascadeProjection.lean`](../A12Kernel/Evidence/StringCascadeProjection.lean) decodes the five producer-certified compact cases and binds them to the existing evaluator at their common clean/error/delta/value-only application boundary.

Limit: the nested-failure statement remains an internal law of the candidate two-step Lean account. The retained compact bundle supplies replayed correspondence for the five named public outcomes, but it does not directly expose the hidden dependency cell or either internal fault tag. LF18 separately models exact one-address application; it does not turn the compact cascade projection into exact-state correspondence. General scheduling, transitive dependency graphs, validation-to-computation inheritance beyond `.required`, and unread poison remain outside the cascade capsule.

## LF16 — certify transport once, then compare typed semantic observations

> Date: 2026-07-17. Sections: cross-cutting evidence architecture. Basis: the archived [direct-cascade raw unit](archived/STRING-DIRECT-CASCADE-RAW-EVIDENCE.md), the accepted [`compact semantic-evidence plan`](SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md), and the red/green `ObservationBundle` and `StringCascadeProjection` consumer study.

The first complete packet proved that the upstream producer could establish its receipts, capabilities, runner fidelity, qualification, closure, and cross-artifact relations, but also showed that reconstructing them in every Lean family is not scalable. The replacement gave ordinary Lean replay one bounded operation-neutral bundle plus a typed family decoder; the accepted raw unit then moved to a named Git recovery revision. The producer checks remain pinned historical assurance recoverable at their a12-dmkits revisions; current upstream `main` later retired the unused implementation.

The completed direct-cascade consumer demonstrates the useful minimum. Its operation-neutral reader and tests total 367 nonblank lines; its family projection is 249 and reuses `StoredString`, `PriorStringTarget`, and `StringTargetError`, while its family tests total 130. The family pins a12-dmkits exporter revision `1b5f463b89adc6cfb81b41121cd6c97855e8cbe3`, compact-bundle SHA-256 `1d8d253e553eba70fa990975666884833748bed9d9b2b6483f472767a9837c7a`, and the two retained receipt identities before it decodes the five inputs, preserves clean/change/error/clear/applied lists with multiplicity in canonical producer-before-consumer order, and compares the real evaluator result directly. It does not parse or hash the raw packet.

The producer-certified compact lane and complete legacy binder agreed once on all five cases. The migration deleted the legacy binder, operation schema/replay/tests, and receipt decoder/tests. Its first scoped comparison measured 666 against a 2,179-line packet-specific target, but that checkpoint excluded 359 nonblank lines of shared artifact/tree production support. The all-in consolidation deleted those helpers and their tests, moved only the live guards into the reader, and settled the complete reader, tests, family, tests, and seven driver lines at 753 nonblank lines—272 fewer than the honest 1,025-line all-in checkpoint—with no permanent dual path. The later raw archive migration removed another 31 files and 123,184 bytes from `HEAD` while retaining the compact bundle and exact recovery identities. A real mutation that bypassed the producer-outcome overlay failed the compact family gate and was restored before the natural replay returned green. Removing the overlay, changing the observed error cause, reversing an observation, or duplicating one therefore remains observable at the small typed boundary.

The historical a12-dmkits one-family exporter itself exceeded its provisional 550-line estimate and was accepted at 778 nonblank production/test/task lines after three independent audits established that the excess was explicit closed projection checks and diagnostics rather than a second capture framework. It was subsequently retired with the rest of portable capture V1. That exception is neither a budget template nor shared logic waiting to be extracted: a future family starts from its concrete observation need, reuses a current corpus/differential route when it fits, or requests the smallest new producer capability. Receipt, runner, qualification, filesystem, and closure machinery must not reappear per family or be resurrected merely because the old framework once existed.

## LF17 — dead error conditions and uninhabitable models are different queries

> Date: 2026-07-18. Sections: §3, §5, §11, §12, cross-cutting analysis. Basis: [`spec/01-data-model.md`](../spec/01-data-model.md), [`spec/10-validation-and-polarity.md`](../spec/10-validation-and-polarity.md), the current `Verdict` and phase-sensitive formal-observation semantics, and the corrected a12-dmkits [G40/SME-TDG contract](../../a12-rulekit/docs/FEATURE-GAPS.md#3-open-backlog) at revision `93e2bd57fc53db49cd36b743d3912e2dc5088df2`.

An authored A12 rule describes an error condition. Therefore an internally contradictory condition such as `[A] > 5 And [A] < 3` is unreachable and never fires; it does not by itself make the model uninhabitable. Whole-model inhabitance instead asks whether some structurally and formally admissible document survives generated checks, computation/application under a fixed profile, and every invalidating authored rule. Multiple individually reachable error rules can exhaust the admissible domain even though no rule condition is internally contradictory.

The distinction cannot be repaired by Boolean polarity alone. A malformed cell can observe as validation `unknown`, so merely asserting that every authored rule is not `fired` would let a solver manufacture false samples through formal invalidity. Repetition shape, requiredness and index checks, computation order, severity, suppression, and the fixed world are also part of the accepted-document relation. Computation-alternative overlap and gap are separate existential queries again.

Lean treatment: keep the relations separately named; illustrative future names include `conditionTruthReachable`, `ruleFiringReachable`, `modelInhabited`, `alternativeOverlap`, and `alternativeGap`. Any analyzer consumes only a checked supported model and states exact bounds and profile inputs. A synthesized SAT witness supports a semantic claim only after it is checked against the exact source relation. Solver UNSAT supports whole-model uninhabitance only when source-to-constraint completeness, constraint rendering, and solver trust or a checked certificate are all established for the stated fragment and bounds; otherwise it remains solver-reported or bounded.

Limit: this finding fixes the analysis vocabulary and proof obligations. It does not define an accepted-document relation, establish an exact SME TDG acceptance profile, adopt a solver capability, or imply an implementation sequence. Current coverage and open adequacy obligations remain owned by [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md).

## LF18 — exact target placement is separate from value and delta

> Date: 2026-07-18. Sections: §1, §11. Basis: [`spec/01-data-model.md`](../spec/01-data-model.md), [`spec/09-computations.md`](../spec/09-computations.md), the retained nine-case [compact root-String projection](../evidence/kernel-30.8.1/captures/string-computation-v1/semantic-observations.json) and its [raw recovery record](archived/STRING-COMPUTATION-RAW-EVIDENCE.md), the archived [direct-cascade raw unit](archived/STRING-DIRECT-CASCADE-RAW-EVIDENCE.md), red/green exact-state conformance, and the executable-to-relational proof bridge.

An empty target value does not answer whether the target cell exists. After a non-value computation, an absent target remains absent, while a present target is cleared in place and remains present-empty. Both states project to the same stored-value view and the same empty delta prior. Conversely, quiet no-value, target rejection, and inherited poison can produce the same exact empty state while retaining different delta, error, and dependency provenance. Exact application, stored value, and report delta are therefore three different observations; none can be reconstructed from either of the others.

Lean treatment: [`Semantics/StringApplication.lean`](../A12Kernel/Semantics/StringApplication.lean) introduces only the exact state of one already-addressed root String target: `absent`, `presentEmpty`, or `presentValue StoredString`. Accepted outcomes create or replace the value. No-value, target error, and poison clear an existing target in place and do not create an absent target. The older [`PriorStringTarget`](../A12Kernel/Semantics/StringComputation.lean) remains the correct smaller input for delta reporting and deliberately collapses absent with present-empty. [`Proofs/StringApplication.lean`](../A12Kernel/Proofs/StringApplication.lean) states the complete transition independently as `StringTargetApplies`, proves soundness and completeness of `applyTo`, determinism, exact clearing and placement preservation for every outcome without an applied value, agreement with the older stored-value view, and both information-loss non-laws. [`Conformance/StringApplication.lean`](../A12Kernel/Conformance/StringApplication.lean) locks all twelve outcome/prior classes plus accepted equality.

Evidence status: the compact root-String projection derives exact state through the live core transition rather than a second local rule. Its nine retained cases actively calibrate accepted writes over absent, stale-filled, and equal-filled targets and target errors over absent, stale-filled, and equal-filled targets. The archived qualified direct-cascade raw unit carries no-value over absent and filled producer targets, and the removed complete binder agreed with that unit before compact migration; ordinary compact replay intentionally projects those observations to value-only state. No retained project-local case starts with a present-empty target. The internal poison tag is also not directly observable in the retained public outputs; downstream clearing after producer failure is only an indirect witness.

Limit: the complete exact-state table is only partially externally calibrated. Accepted, no-value, target-error, and poison over an initially present-empty target remain `external evidence pending`, as do direct public poison application over absent or filled targets. This capsule assumes one already-resolved root target and does not model ancestor creation, general `Document` mutation, raw-text preservation, repeatable addressing, scheduling, multi-target atomicity, alternatives, or implicit validation.

## LF19 — precondition skipping precedes body poison-on-read

> Date: 2026-07-18. Sections: §11. Basis: [`spec/09-computations.md`](../spec/09-computations.md), read-only kernel computation control flow, a12-dmkits IF77/IF85 and its tri-differential precondition/poison controls, red/green conformance, and trusted outcome/application/dependency proofs.

A computation whose common precondition is cleanly not true produces quiet no-value without evaluating its body. A formally invalid body operand therefore cannot poison that computation under that decision. With the same body and input but a holding decision, the operand is read and poison is preserved. Both outcomes can clear the same stale target and produce the same exact present-empty state, yet a later dependent computation observes quiet no-value as clean empty and observes poison as formal invalidity. Delta or target placement alone cannot recover that distinction.

Lean treatment: [`StringComputationStep.evaluateOutcomeWhen`](../A12Kernel/Semantics/StringCascade.lean) consumes the checked [`ComputationConditionResult`](../A12Kernel/Semantics/ComputationCondition.lean). Clean `notTrue` returns `.noValue` before the body evaluator is called, `holds` delegates to the existing outcome evaluator, and `poison cause` preserves that cause without consulting the body. The arbitrary-step equations make the no-read property structural, while the consumed-invalid-field theorem connects a holding decision to the existing poison-on-read account. The separating non-law reuses exact application and the dependency overlay rather than introducing another state, scheduler, relation, or evidence layer.

Limit: the integrated condition AST now contains direct presence and ordered `And`/`Or`; LF21 owns the connective boundary, while LF22 owns ordered alternatives and remaining-alternative skipping. Comparisons, paths, and repeatable contexts remain future semantic clauses. No retained local packet contains a computation precondition, so guard-to-no-value and guard/body-poison correspondence remain `external evidence pending` despite the strong read-only kernel and a12-dmkits differential basis. Any future calibration is a separate approval-gated decision, not part of this semantic capsule.

## LF20 — computation presence reverses only clean truth

> Date: 2026-07-18. Sections: §3, §11. Basis: [`spec/02-logic-and-formal-errors.md`](../spec/02-logic-and-formal-errors.md), [`spec/03-empty-and-required.md`](../spec/03-empty-and-required.md), [`spec/09-computations.md`](../spec/09-computations.md), read-only kernel presence-read behavior, a12-dmkits' direct presence/poison differential controls, red/green conformance, and trusted observation and computation-condition proofs.

Computation `FieldFilled` and `FieldNotFilled` are complements only for clean observations. Empty means not filled, a clean value means filled regardless of its scalar value, and both predicates preserve an ordinary formal invalidity as poison. Validation-scoped requiredness is the deliberate exception at the phase boundary: required-only empty remains empty in computation, whereas requiredness attached beside an ordinary finding does not hide that poison. Therefore neither raw presence nor Boolean negation determines computation presence.

Lean treatment: [`ComputationCondition.eval`](../A12Kernel/Semantics/ComputationCondition.lean) reads one already-resolved checked field through `observeCell .computation` and returns `holds`, clean `notTrue`, or the exact poison cause. [`Proofs/ComputationCondition.lean`](../A12Kernel/Proofs/ComputationCondition.lean) characterizes empty, clean value, required-only, ordinary-invalid, and exact-poison behavior and proves the nearest non-law: both presence predicates agree on poison rather than negating one another. [`Conformance/ComputationCondition.lean`](../A12Kernel/Conformance/ComputationCondition.lean) also locks the second instance of the mechanism: a quietly cleared dependency satisfies downstream `FieldNotFilled`, while a poisoned dependency makes the same predicate poison.

Limit: this is a parser-independent, non-repeatable, already-resolved presence capsule. Ordered connectives and first-match alternatives now compose these leaves as described in LF21 and LF22, but the capsule does not lower DSL paths, evaluate comparisons, enforce model-level alternative legality, schedule computations, expose a public protocol, or claim that the external kernel reports the internal cause identity. Local portable differential evidence remains pending.

## LF21 — computation connectives are ordered control flow, not a commutative truth algebra

> Date: 2026-07-19. Sections: §3, §11. Basis: [`spec/09-computations.md` §3.2](../spec/09-computations.md#32-an-invalidity-clear-poisons), read-only kernel composite-condition control flow, a12-dmkits IF85 and `ComputePoisonReadDiffTest`, red/green conformance, and trusted computation-condition proofs.

Computation `And` and `Or` determine which field reads happen, so they cannot reuse validation's commutative strong-Kleene result tables. Evaluation is left-to-right. `And` evaluates its right operand only after a holding left; a clean not-true left decides not-true. `Or` evaluates its right operand only after a clean not-true left; a holding left decides holds. A poison encountered on the left aborts either connective immediately, and a poison from the right is preserved only when that operand is reached.

The closest Boolean-looking rewrite is therefore unsound in the presence of poison. `notTrue And poison` is clean not-true, while `poison And notTrue` is poison. Likewise, `holds Or poison` holds, while `poison Or holds` poisons. Operand order is an observable part of the computation semantics and must be preserved by an interpreter, refactoring tool, or optimizer unless a proof establishes that the moved operand cannot poison.

Lean treatment: [`ComputationCondition`](../A12Kernel/Semantics/ComputationCondition.lean) now has recursive `and` and `or` constructors whose evaluator matches the left result before recursing into the right. [`Proofs/ComputationCondition.lean`](../A12Kernel/Proofs/ComputationCondition.lean) states all six branch equations and proves the two general operand-order non-laws. [`Conformance/ComputationCondition.lean`](../A12Kernel/Conformance/ComputationCondition.lean) realizes both non-laws with filled, empty, and formally invalid direct-presence fields and feeds the ordered `And` decision through the existing precondition-before-body boundary.

Limit: the current leaf fragment contains direct presence only. It has no concrete clean comparison-unknown clause, quantifier scan, checked path lowering, general read trace, or public protocol. LF22 composes this result into ordered alternatives without widening the leaf language. The exact poison cause is an internal semantic distinction, not an externally reported kernel field. No retained compact bundle exercises computation preconditions or connective order, so this capsule remains `external evidence pending`; the existing a12-dmkits tri-differential is research provenance rather than project-local portable evidence.

## LF22 — computation selection is first-match, but implicit validation is all-alternatives

> Date: 2026-07-19. Sections: §11. Basis: [`spec/01-data-model.md` §3.2](../spec/01-data-model.md#32-computations), [`spec/09-computations.md` §§5–6](../spec/09-computations.md#5-where-a-computation-runs--scope-and-the-parallel-join), kernel computation-table documentation plus the [`ICalculationInfo`](../../a12-kernel/kernel-tool/kernel-core-tool-api/src/main/java/com/mgmtp/a12/kernel/core/tool/a12internal/api/ado/serviceparams/ICalculationInfo.java) selection contract and terminal [`CodeGenCalculationAlternative` template](../../a12-kernel/kernel-tool/kernel-core-codegen/src/main/resources/internal/templates/validation/java/calcDir/CodeGenCalculationAlternative.st), a12-dmkits selection/poison/overlap controls at revision `175d643596f8def7072467bf27a63ad7d63ac31b`, red/green conformance, and trusted selection proofs.

One computation scans its alternatives in declaration order. Clean not-true continues, a holding alternative selects its operation and makes the suffix irrelevant, poison aborts and makes the suffix unread, and exhausting the list yields clean no-match. Selection ends before the operation runs: if the selected operation itself later produces no value or poison, no later alternative is tried. This differs from several separate computations targeting one field, where the first non-empty computation outcome wins.

The same selector cannot implement the computation's generated validation rule. Runtime computation resolves overlapping holding preconditions to the first declaration, but generated validation retains one guarded mismatch disjunct per alternative. With overlapping guards, a stored value can equal the first operation while a later holding operation differs, so computation and validation deliberately take different paths. Agreement therefore needs an explicit mutual-exclusivity assumption or another sufficient condition; it is not true by construction.

Lean treatment: [`ComputationAlternative.selectFirst`](../A12Kernel/Semantics/ComputationCondition.lean) is operation-neutral and returns `noMatch`, `selected operation`, or exact-cause `poison` without evaluating the payload. Its proof spine gives the empty and three head equations, characterizes no-match, and checks declaration-order non-laws for holding/holding and holding/poison pairs. [`StringAlternativeComputation`](../A12Kernel/Semantics/StringAlternatives.lean) now composes that selector once with the existing resolved String step while keeping one target, target policy, and prior state outside the alternatives. Its trusted laws prove exact selected-operation delegation and holding-head suffix irrelevance through operation evaluation, including the payoff case where the selected expression produces no value. The executable non-law distinguishes that terminal no-value from an incorrect scan that would continue to a later holding fallback; target rejection and both selection- and expression-side poison are terminal controls.

Limit: the executable surface represents an already-model-legal guarded String table and totalizes an empty list without claiming it is authorable. The sole unconditional computation remains the existing direct String step; checked lowering still must enforce “one unconditional or multiple all guarded,” table non-emptiness, target legality, and the distinct common precondition. Comparisons, quantifiers, repeatable scope, several computations per target, scheduling, generated implicit validation beyond the separate two-literal Number fragment, and public protocol exposure remain open. Project-local portable evidence does not yet cover alternatives, and selected-operation no-fallthrough is kernel-source-inferred rather than directly observed here, so it remains `external evidence pending`. No evidence, support, protocol, harness, governance, dependency, or general table framework was added.

## LF23 — partial validation has two independent relevance decisions

> Date: 2026-07-19. Sections: §12. Basis: [`spec/10-validation-and-polarity.md` §5](../spec/10-validation-and-polarity.md#5-full-vs-partial-validation), kernel 30.8.1 public partial-validation contracts and runtime routing at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a12-dmkits `PartialValidationDiffTest`, `PartialValidationTest`, and `DmInterpreter.validatePart` at revision `978991836ce298c6382b65e0559d3ccf9af24b3c`, red/green conformance, and trusted flat-relevance proofs.

Partial validation first asks whether the rule instance may run: if its explicit error field is outside the relevant set, the rule is skipped without evaluating its condition. Only for a relevant error field does it evaluate condition leaves, and each referenced field outside the set contributes validation `unknown`. These cannot be collapsed into one relevance-aware read. With clean filled fields `E` and `O`, error field `E`, condition `FieldFilled(E) Or FieldFilled(O)`, and only `O` relevant, read masking alone would yield `unknown Or true` and fire; the real rule gate skips the rule.

Lean treatment: [`FlatCondition.evalSelected`](../A12Kernel/Semantics/FlatValidation.lean) owns the one recursive evaluator, defaults to all fields relevant, and checks an explicitly supplied relevance predicate before reading each leaf. [`FlatCondition.evalPartial`](../A12Kernel/Semantics/PartialValidation.lean) applies the separate error-field gate and returns `FlatPartialResult.skipped | evaluated Verdict`, preserving skipped, evaluated-not-fired, and evaluated-unknown as distinct meanings. Relevance is call-local, so the implementation does not fabricate a `FormalCause`, mutate `CheckedCell`, or create a possible computation poison. `FlatContext.AgreesOn` states equality only at relevant field IDs. Executable locks cover both gate outcomes, representative comparison/presence behavior, all-relevant agreement, and the content-gate non-law. The trusted structural induction covers every comparison, positive/negative presence, and connective constructor: it proves masked-evaluation agreement and the one-sided guarantee that a partial firing survives in every content-bearing completion satisfying `AgreesOn`; the completion's polarity is existential because hidden values can change VALUE/OMISSION precedence.

Checked boundaries: a relevant error field does not imply agreement with full evaluation when another reference is excluded; the direct comparison can be partial-unknown but full-fired. Conversely, a condition may still fire with a non-relevant sibling when a relevant true `Or` branch decides it. A relevant instance also bypasses the full-validation content gate: empty Number equality can fire with omission polarity in partial validation while `evalFull ... false` remains not fired.

Limit: `FlatRelevance = FieldId → Bool` covers only already-resolved nonrepeatable field instances. It deliberately does not model global augmentation, relevant-group descent, wildcard repetitions, aggregate-specific completeness, uniqueness partners, phantom-row construction, autogenerated checks, checked rule/error-field coherence, messages, or whole-document orchestration. The maintained a12-dmkits differential is strong research provenance, but no project-local portable observation currently covers this family; the capsule remains `external evidence pending`. The canonical spec already stated this behavior correctly, so no `spec/` correction or synchronization-ledger entry was needed.

## LF24 — direct Number ordering uses the same directional fixed-right comparison

> Date: 2026-07-19. Sections: §2, §5, §12. Basis: [`spec/03-empty-and-required.md`](../spec/03-empty-and-required.md), [`spec/04-numbers-and-decimals.md`](../spec/04-numbers-and-decimals.md), [`spec/10-validation-and-polarity.md`](../spec/10-validation-and-polarity.md), kernel 30.8.1 comparison and field-policy mechanisms at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a12-dmkits empty/order/polarity/precision controls at revision `0f1bb3601b5765265df0e221c8c1989645d257e1`, red/green conformance, and trusted directional-polarity proofs.

Direct Number field-to-literal `<` and `>=` do not need a second evaluator. They consume the same exact rational operand and comparison-local fillability as equality/inequality, normalize both sides independently to scale 19 with `HALF_UP`, and differ only in the direction that could falsify a true condition. An empty Number substitutes zero; `<` is omission-typed for either signedness because filling can grow the left operand, while `>=` over that zero is omission-typed only for a signed field because only it can shrink. Filled zero is fixed and therefore VALUE when `>= 0` fires. Formal invalidity remains unknown, and the independent full-validation empty-row gate can still suppress the comparison before substitution.

Lean treatment: [`FlatComparison.number`](../A12Kernel/Semantics/FlatValidation.lean) reuses the six-case `NumericComparisonOp`, but checked flat lowering accepts exactly `equal`, `notEqual`, `less`, and `greaterEqual` for its reduced Number-field/literal surface. Boolean and Confirm remain limited to equality and inequality, direct String remains equality-only, and `lessEqual`/`greater` remain outside that checked flat surface even though the general checked numeric-expression consumer admits them. Numeric literals and ordering are statically scale-exempt, while declared field scale still belongs to formal input policy. [`emptyNumberLessFiring_is_omission`](../A12Kernel/Proofs/NumericComparison.lean) and [`emptyNumberGreaterEqualFiring_polarity`](../A12Kernel/Proofs/NumericComparison.lean) state the reusable empty-field consequences.

Limit: this finding's reduced checked-flat route closes only one nonrepeatable Number field against one fixed literal and still does not expose `<=`/`>` or numeric expressions through that flat surface. LF34 subsequently closes an internal checked two-expression arithmetic route with all six operators, while tolerance, public JSON support, and a general order-duality rewrite remain separate. Committed a12-dmkits controls and kernel source inspection establish the research basis, but the local compact evidence bundle has no direct empty-Number ordering observation, so correspondence remains `external evidence pending`. The canonical spec already stated the behavior exactly; it was explicitly audited and required no `spec/` change or [`A12-DMKITS-SPEC-SYNC-LEDGER.md`](A12-DMKITS-SPEC-SYNC-LEDGER.md) queue entry.

## LF25 — authored rounding is a bounded two-stage operation

> Date: 2026-07-19. Sections: §5. Basis: corrected [`spec/04-numbers-and-decimals.md` §§2 and 5](../spec/04-numbers-and-decimals.md#2-rounding), kernel 30.8.1 grammar, checking, scale propagation, runtime rounding, and operand-preservation mechanisms at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a12-dmkits `NumberLawsTest`, `RoundingLawsTest`, `RoundDiffTest`, and `SingleArgRoundDiffTest` at revision `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green conformance, and trusted rounding-bound proofs.

Both rounding syntaxes accept an optional decimal-places argument from zero through fourteen inclusive; omission means exactly zero. The `…Value(field[, n])` forms are not integer-only siblings and do not have different arithmetic. Every form first rescales its operand to scale 19 with decimal `HALF_UP`, then applies the requested target scale with `FLOOR` for `RoundDown`, `CEILING` for `RoundUp`, or `HALF_UP` for `RoundAccounting`. The result's static scale is the requested places count.

That first step changes the strongest valid direction law. Target-scale floor is at or below the scale-19 pre-rounded operand and target-scale ceiling is at or above it, but neither relation is universally true against a raw higher-precision rational. An exact value just below one can pre-round to one before zero-place floor and therefore produce a value greater than its raw input; the dual value just above one can pre-round to one before ceiling and produce less than its raw input. The Lean enum consequently names the mathematical modes `floor`, `ceiling`, and `halfUp` instead of perpetuating the ambiguous surface words “down” and “up.”

Lean treatment: [`Semantics/NumericRounding.lean`](../A12Kernel/Semantics/NumericRounding.lean) makes illegal places unrepresentable with `RoundingPlaces = Fin 15`, reuses one decimal `HALF_UP` primitive for comparison and rounding, and keeps the operation pure over exact `Rat`. [`Proofs/NumericRounding.lean`](../A12Kernel/Proofs/NumericRounding.lean) proves floor/ceiling bounds against the correct pre-rounded input and exact preservation of an unknown cause or directional fillability when a numeric comparison operand is rounded. [`Conformance/NumericRounding.lean`](../A12Kernel/Conformance/NumericRounding.lean) separates both signs, all three modes, the `0`/`14`/`15` authoring boundary, omitted zero, one fractional scale, the pre-round effect, and both nearest false raw-input bounds.

Limit: this is a target-independent fixed-scale semantic seam, not a numeric expression evaluator. It does not add parsing or checked lowering, `MathContext(50)` significant-digit arithmetic, division/power totality, computation target validation, rendering, stored-form equality, public protocol support, or a second rounding meaning for computation metadata. a12-dmkits's kernel differentials provide strong research provenance, but this repository retains no portable rounding observation, so external correspondence remains pending. The corrected spec behavior and the peer project's stale prose/catalog/typed-read surfaces are queued as [`SPEC-2026-07-19-07`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-07--rounding-accepts-zero-places-and-preserves-the-two-stage-mode).

## LF26 — every arithmetic node has a significant-digit boundary

> Date: 2026-07-19. Sections: §5. Basis: corrected [`spec/04-numbers-and-decimals.md` §§1 and 5](../spec/04-numbers-and-decimals.md#5-internal-precision--the-constants-that-must-match-exactly), kernel `VkBigDecimal`, numeric parser checking, and `DivisionTransformer` at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, read-only a12-dmkits arithmetic implementation/prose/tests at revision `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green conformance, and trusted arithmetic laws.

The kernel passes the same decimal `MathContext(50)`, whose default mode is `HALF_UP`, to addition, subtraction, multiplication, division, and power. The former spec and a12-dmkits claim that `+` and `−` stay exact is therefore false. Precision means significant digits, not fractional scale, and the boundary occurs independently at every evaluated arithmetic node. Consequently, exact-rational ring laws cannot be transferred wholesale: addition and multiplication remain commutative, but addition is not associative. With `H = 10^50`, `(H + -H) + 0.6 = 0.6` while `H + (-H + 0.6) = 1`.

The evaluated arithmetic tree can also differ from the authored tree. The exact ordered one-pass rewrite and its current authored/lowered Lean account are recorded in [LF32](#lf32--division-lowering-is-one-ordered-post-order-pass-not-algebraic-normalization). Because each resulting node rounds, directly folding the authored AST is not a faithful default.

Lean treatment: [`Semantics/NumericArithmetic.lean`](../A12Kernel/Semantics/NumericArithmetic.lean) retains exact `Rat` as the value representation but computes the rational's exact decimal order of magnitude from numerator/denominator digit counts, derives a signed target scale, and applies decimal `HALF_UP` at precision 50 after every admitted `add`, `subtract`, `multiply`, or nonzero `divide` node. This handles large integer magnitudes, tiny fractional ties, and nonterminating quotients without floating point or a second decimal representation. Division checks the divisor before using `Rat./` and returns `NumericArithmeticResult.notEvaluated` on zero. [`Proofs/NumericArithmetic.lean`](../A12Kernel/Proofs/NumericArithmetic.lean) proves zero preservation, addition/multiplication commutativity, subtraction-as-add-negation, multiplication by zero, division's exact definedness condition, and zero-dividend behavior. [`Conformance/NumericArithmetic.lean`](../A12Kernel/Conformance/NumericArithmetic.lean) locks above-50 addition/subtraction, a 52-digit product, positive and negative tiny ties, a small exact control, zero-divisor non-evaluation, positive/negative repeating thirds, significant-digit rather than fractional-scale quotient precision, quotient consumption by a following multiplication, and both sides of the non-associativity witness.

The same source audit corrected neighboring authoring facts: checked expressions need known signed scale or unknown plus a multiplicative-constant capability; literals are not globally exempt; and a scale-0 base raised to a simple nonnegative constant is a narrow known-scale power case. The runtime correction and the checked-scale correction are queued separately as [`SPEC-2026-07-19-08`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-08--every-arithmetic-node-uses-precision-50-half_up) and [`SPEC-2026-07-19-09`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-09--numeric-scale-gating-tracks-signed-scale-and-constant-expandability).

Limit: this is a pure known-value seam, not a checked expression evaluator. `notEvaluated` has no checked validation/computation consumer yet, so it does not decide the later consumer-specific domain-failure projection. Power uses the distinct staged algorithm recorded in LF27 rather than being collapsed into exact rational exponentiation. LF28 and LF29 subsequently add known-operand `+`/`−`/`×`/`÷` fillability plus domain-failure absorption, LF31 adds the independent static scale/capability summary, and LF32 supplies the generic authored/lowered expression staging; power fillability, formal-invalid propagation, empty provenance, model-resolved/phase-sensitive integration, rendering, target application, and public protocol support remain open. Kernel source and existing multiplication/division differentials strongly ground the mechanism, but the division precision control does not reach 50 significant digits, a12-dmkits currently disagrees on extreme addition/subtraction, and this repository retains no portable arithmetic observation; correspondence for the new seam remains `external evidence pending`. No evidence or harness machinery was added.

## LF27 — power has two staged precision algorithms, not one algebraic shortcut

> Date: 2026-07-19. Sections: §5. Basis: corrected [`spec/04-numbers-and-decimals.md` §§3 and 5](../spec/04-numbers-and-decimals.md#3-arithmetic-domain-failures-are-consumer-sensitive), kernel `VkBigDecimal.power` and reciprocal handling at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, the OpenJDK 21 `BigDecimal.pow(int, MathContext)` X3.274 contract, read-only a12-dmkits implementation/tests at revision `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green conformance, and trusted domain/staging laws.

Positive power is not exact rational exponentiation followed by one precision-50 round. Java scans the exponent's bits, rounds intermediate squares and accumulator multiplications with `HALF_UP` at working precision `50 + decimalDigits(exponent) + 1`, and only then rounds the result to precision 50. The difference is reachable with a legal 15-digit operand: `0.473471768303411 ^ 7` ends in `…3319`, while exact power followed by a single final round ends in `…3318`.

The kernel does not use Java's negative-exponent branch. It first calculates and precision-50-rounds `1 / base`, negates the exponent, and passes the rounded reciprocal through the positive algorithm. For `3 ^ -3`, that reciprocal-first route ends in `…7036`; raising first and taking the final reciprocal ends in `…7037`. a12-dmkits currently uses the latter order in its JVM value evaluator even though its fillability walk uses reciprocal-first reasoning, and its exact `4 ^ -1` differential cannot expose the defect.

Lean treatment: [`Semantics/NumericArithmetic.lean`](../A12Kernel/Semantics/NumericArithmetic.lean) factors the existing fixed precision through a total significant-digit helper, implements the OpenJDK 21 positive bit algorithm directly over `Rat`, admits only integral runtime exponents in inclusive `-1000..1000`, and routes negative exponents through the existing partial division before positive power. [`Proofs/NumericArithmetic.lean`](../A12Kernel/Proofs/NumericArithmetic.lean) states rejection, zero exponent, zero-base negative quietness, and the reciprocal-first branch equation. [`Conformance/NumericArithmetic.lean`](../A12Kernel/Conformance/NumericArithmetic.lean) locks staged power versus final-round-only power, the exact working-precision guard digit, reciprocal-first versus reciprocal-after-power, `0^0`, `0^-1`, `4^-1`, negative-base parity, fractional rejection, and all four inclusive/exclusive range edges.

Limit: this pins the current X3.274 numeric-value account, which Java documents as an approximation whose implementation may evolve; it is deliberately not a theorem equating power with exact rational exponentiation. Runtime-invalid base values, directional arithmetic fillability, checked authored exponent scale, expression lowering, rendering, and validation/computation consumers remain separate. No portable project-local power observation exists, so correspondence remains `external evidence pending`; the peer-project correction and missing differential matrix are queued as [`SPEC-2026-07-19-10`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-10--power-preserves-staged-java-precision-and-reciprocal-first-negative-order). No evidence or harness machinery was added.

## LF28 — multiplication fillability needs joint movement, not only current signs

> Date: 2026-07-19. Sections: §5, §12. Basis: corrected [`spec/10-validation-and-polarity.md` §4](../spec/10-validation-and-polarity.md#4-the-directional-fill-machinery-behind-the-typing), kernel `VkBigDecimal` arithmetic-fillability methods at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, read-only a12-dmkits `ArithFill`, `ArithmeticFillTest`, and `ArithmeticFillPolarityDiffTest` at revision `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green conformance, and trusted fillability laws.

Arithmetic polarity metadata is a conservative two-direction abstract interpretation over valid current numeric values. Addition ORs corresponding directions; subtraction swaps the subtrahend's directions. Multiplication additionally combines each operand's directions with the other current sign, but that is not enough: grow/grow and shrink/shrink jointly make the product able to grow, while grow/shrink and shrink/grow jointly make it able to shrink. Thus two grow-only operands currently equal to zero produce a grow-only product even though both sign-scaled contributions are false. A sign-only propagation rule would silently type a downstream firing as VALUE instead of OMISSION.

Lean treatment: [`Semantics/NumericFillability.lean`](../A12Kernel/Semantics/NumericFillability.lean) owns `NumericFillability`, derives `NumericSign` from the same exact `Rat` operands consumed by arithmetic, and implements the exact `+`, `−`, and `×` formulas independently of numeric rounding. [`Proofs/NumericFillability.lean`](../A12Kernel/Proofs/NumericFillability.lean) proves all-fixed preservation, addition and multiplication commutativity, subtraction as addition with the right directions swapped, its fixed-operand consequences, and preservation/swap/fixedness under multiplication by fixed positive, negative, and zero factors. [`Conformance/NumericFillability.lean`](../A12Kernel/Conformance/NumericFillability.lean) separately locks the same-direction and mixed-direction zero-sign joint terms and carries positive/negative/zero fixed-factor results into the existing fixed-right comparison polarity evaluator.

Limit: this first capsule accepts valid known operands and deliberately has no expression result that could absorb `NumericArithmeticResult.notEvaluated`. LF29 subsequently adds that smallest combined result and division's reciprocal-direction transformation. Power still uses a much larger conservative branch table and has a surprising zero-base/empty-exponent discriminator that remains observation-gated. Empty provenance remains the responsibility of the operand-producing clause rather than a third fillability bit. No project-local portable arithmetic-fillability observation exists, so correspondence remains `external evidence pending`; the formulas, missing peer controls, and required power probe are queued as [`SPEC-2026-07-19-11`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-11--arithmetic-fillability-uses-joint-terms-and-conservative-power-dispatch). No AST, protocol, target, evidence, or harness machinery was added.

## LF29 — division uses a reciprocal only for fillability and absorbs domain failures

> Date: 2026-07-19. Sections: §5, §11, §12. Basis: [`spec/04-numbers-and-decimals.md` §§3 and 5](../spec/04-numbers-and-decimals.md#3-arithmetic-domain-failures-are-consumer-sensitive), [`spec/10-validation-and-polarity.md` §4](../spec/10-validation-and-polarity.md#4-the-directional-fill-machinery-behind-the-typing), corrected [`spec/09-computations.md` §§3.1–3.2](../spec/09-computations.md#31-a-precondition-clear-cascades-as-empty), kernel `VkBigDecimal.divide`, reciprocal-fillability, rounding, calculation-cache, generated calculation-alternative, and result-projection paths at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, read-only a12-dmkits `ArithFill`, `ExprEval`, `PolarityWalk`, `ComputationEvaluationLawsTest`, and `RuntimeLaws` at revision `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green conformance, and trusted absorption/sign laws.

Division has two deliberately different routes. The numeric value is the direct precision-50 quotient `round50(a / b)`. Only directional metadata rewrites the divisor as a reciprocal and then reuses the multiplication table: reciprocal grow is `Sᵦ ∨ (b < 0 ∧ Gᵦ)`, reciprocal shrink is `Gᵦ ∨ (b > 0 ∧ Sᵦ)`, and its sign is the nonzero divisor's sign. Constructing a rounded reciprocal amount and multiplying would be wrong: direct `2 / 3` rounds its last retained digit up, while `2 × round50(1 / 3)` ends one unit lower at that precision. A current zero divisor is rejected before either metadata transformation or rational division, even when its fillability says it could later move away from zero.

Lean treatment: [`Semantics/NumericFillability.lean`](../A12Kernel/Semantics/NumericFillability.lean) adds `NumericArithmeticOutcome`, whose success constructor keeps the rounded `Rat` and `NumericFillability` inseparable and whose `notEvaluated` constructor absorbs either child before a total arithmetic node or division runs. Division delegates its value to `divideNumeric` and sends only the divisor's transformed flags through `NumericFillability.multiply`. [`Proofs/NumericFillability.lean`](../A12Kernel/Proofs/NumericFillability.lean) proves left/right absorption, exact zero-divisor definedness for known values, fixed/fixed preservation, preservation under a fixed positive divisor, direction swap under a fixed negative divisor, and fixedness of a zero fixed numerator under any nonzero divisor. [`Conformance/NumericFillability.lean`](../A12Kernel/Conformance/NumericFillability.lean) separates all four reciprocal sign/direction branches, zero-numerator joint terms, direct quotient order, zero-gate precedence, enclosing-operation absorption, and the resulting positive/negative-divisor comparison polarities.

The source audit also exposed a computation boundary outside this pure capsule. The pinned a12-dmkits public-API `RoundAccounting(div, 2)` probe establishes no fresh computed-field outcome and stale CLEARED, but its mapper omits `formalErrorsInOperands` and never reads the target from a dependent computation. Kernel source closes the semantic mechanism: rounding preserves the invalid number, the handler marks the target invalid and adds `berechnungsWertFehler`, and the calculation cache throws when a dependent reads it, causing that dependent target to become invalid in turn. The canonical spec therefore classifies division-by-zero computation as poison while [`SPEC-2026-07-19-12`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-12--division-by-zero-computations-poison-despite-a-cleared-delta) requests a portable dual-route lock for the formal-error and dependent-read channels. `NumericArithmeticOutcome.notEvaluated` records the pure domain failure; it deliberately does not choose a validation or computation projection.

Limit: this remains a known-value arithmetic outcome, not checked syntax or a general effect type. LF32 now supplies a generic authored/lowered value tree, but formal-invalid operands and their causes, model-resolved/phase-sensitive integration, two-fillable-side comparison, computation target/store projection, integration of LF31's static summary into exact diagnostics, power fillability, empty-result provenance, rendering, and public protocol support remain separate. The comparison examples make the valid-result polarity and quiet not-fired projection executable but do not create a checked consumer. Project-local portable division-fillability evidence remains pending, and no evidence or harness machinery was added.

## LF30 — tolerance normalizes operands independently and closes gaps directionally

> Date: 2026-07-19. Sections: §5, §11, §12. Basis: pinned kernel 30.8.1 runtime/parser/computation-generation source at `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a read-only audit of a12-dmkits at `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green executable separators, and trusted tolerance laws.

The four fixed tolerance comparisons first normalize each operand independently to scale 19 with `HALF_UP`, then fire only when the absolute normalized gap is strictly greater than `1`, `2`, `5`, or `10`. Rounding the already-computed difference is a different algorithm: with `ε = 49 / 10²¹`, operands `1 + ε` and `−ε` independently normalize to `1` and `0`, so range 1 stays quiet, while rounding their raw difference crosses the boundary.

Polarity reuses the directional inequality mechanism rather than equality's any-movement rule. If normalized left is smaller, only left growth or right shrinkage can repair an exceeded band; if left is larger, only left shrinkage or right growth can. Consequently, the same empty unsigned `0` under range 1 fires VALUE against `−2` but OMISSION against `+2`; the signed negative control is OMISSION. Truth is symmetric under swapping operands, and the direction flags swap with the operand roles.

Lean treatment: [`NumericComparison.lean`](../A12Kernel/Semantics/NumericComparison.lean) owns one shared `numericDifferenceFillCanClose` helper used by ordinary inequality and tolerance. [`NumericTolerance.lean`](../A12Kernel/Semantics/NumericTolerance.lean) owns the closed threshold type, independent normalization, strict truth predicate, two-sided fillability, unknown-preserving verdict, and the closed ordinary-or-tolerance dispatch consumed by checked validation. [`NumericValidation.lean`](../A12Kernel/Elaboration/NumericValidation.lean) reuses one model/path/authoring/lowering/read/failure/row-gate pipeline for both operations; only static scale admission and final primitive verdict dispatch vary. [`Proofs/NumericTolerance.lean`](../A12Kernel/Proofs/NumericTolerance.lean) proves positive thresholds, normalized-gap/truth symmetry, symmetry of the complete verdict under operand/fillability swap, unknown preservation, the exact closed boundary, fixed VALUE firing, and the grow-only closing-versus-widening polarity split. [`Proofs/NumericValidation.lean`](../A12Kernel/Proofs/NumericValidation.lean) proves tolerance's universal scale-gate bypass and direct atom/literal delegation. [`Conformance/NumericTolerance.lean`](../A12Kernel/Conformance/NumericTolerance.lean) retains the pure four-band and normalization/polarity matrix; [`Conformance/NumericValidation.lean`](../A12Kernel/Conformance/NumericValidation.lean) adds checked scale-bypass, one-pass-lowering, row-gate, formal/domain, strict-boundary, directional-polarity, and fail-closed separators without duplicating it.

The audit found two root defects in current a12-dmkits: its evaluator normalizes after subtraction, and its polarity walk uses `anyMove`; existing signed-field tests cannot separate either error. The later `d6a08f5b` handback closed the separate loss of a computation alternative's declared tolerance operator during implicit-validation synthesis. [`SPEC-2026-07-19-13`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-13--tolerance-normalizes-operands-first-and-uses-directional-inequality-polarity) preserves that accepted arm while requesting the two remaining shared corrections and focused existing-harness locks.

Limit: the checked local capsule is parser-independent and admits only the existing same-group, nonrepeatable plain Number-expression subset. It does not lower the four concrete tokens, admit numeric `BaseYear` or other number-like operations, synthesize implicit computation validation, support partial/repeatable evaluation, or expose a public protocol operation. No retained project-local kernel observation exercises tolerance, so correspondence remains `external evidence pending`; no new harness was introduced.

## LF31 — numeric scale is signed static metadata, not runtime decimal representation

> Date: 2026-07-19. Sections: §5. Basis: [`spec/04-numbers-and-decimals.md` §1](../spec/04-numbers-and-decimals.md#1-scale-gates----checked-at-parse-time), pinned kernel parser/type-summary source at `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a read-only a12-dmkits authoring/test audit at `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green conformance, and trusted static-summary laws.

A checked numeric expression carries two independent authoring facts: an exact signed decimal scale or unknown, and whether trailing zeros may expand that scale. The sign matters because an integer literal is stripped before its scale is recorded: `100` has scale `−2`, while authored `100.00` has scale `2`. Every numeric literal is expandable; fields use their declared nonnegative scale and are not. Addition and subtraction take the maximum known scale and AND the capabilities, multiplication adds known scales and ORs the capabilities, division becomes unknown and non-expandable, and explicit rounding fixes the requested scale and clears the capability. Power rejects an unknown or positive exponent scale; its result is known scale zero only for a scale-zero base and a syntactically simple nonnegative constant exponent, and otherwise remains unknown.

The unsuppressed `==`/`!=` gate accepts two known equal scales or a mismatch whose strictly smaller-scale side is expandable. This is value-preserving padding, never scale reduction. Hence a scale-2 field may compare with literal `0`, but a scale-0 field may not compare with authored `0.00`, in either operand order. Expandability also does not imply a known scale: multiplying a division result by a literal yields unknown scale with capability true, and exact comparison still rejects it. Ordering and tolerance bypass this gate; suppression, concrete operator dispatch, and diagnostics remain later checked-surface concerns.

Lean treatment: [`ScaleInfo`](../A12Kernel/Core.lean) was generalized in place from `exact Nat | unknown` to `exact Int | unknown`; no parallel signed-scale type was added. [`NumericScaleSummary`](../A12Kernel/Elaboration/NumericScale.lean) is a pure static abstraction separate from `Rat`, `NumericArithmeticOutcome`, fillability, and stored rendering. Its closed binary enum summarizes `+`, `−`, `×`, and `÷`; `power?` checks the exponent-scale precondition before deriving the narrow result; and `exactNumericScaleComparisonAllowed` owns only the unsuppressed exact gate. [`Proofs/NumericScale.lean`](../A12Kernel/Proofs/NumericScale.lean) proves binary-summary and gate symmetry, exact smaller-side characterization, field-only specialization, same-scale acceptance, and unknown rejection. [`Conformance/NumericScale.lean`](../A12Kernel/Conformance/NumericScale.lean) locks signed literals, both operand orders, the false global-literal-exemption rule, capability propagation, unknown-with-capability independence, rounding reset, and the narrow power cases.

The a12-dmkits audit found that its existing scale-2-field tests against `0`, `0.0`, and `0.00` are all valid but non-separating; prose then generalized them incorrectly to global literal exemption. Its field-to-field precheck remains a correct specialization, and its AST already retains enough field, literal, rounding, and arithmetic structure for one shared static summarizer. [`SPEC-2026-07-19-09`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-09--numeric-scale-gating-tracks-signed-scale-and-constant-expandability) already owns that reconciliation; no duplicate queue entry is warranted.

Limit: the current [`SurfaceLiteral.number`](../A12Kernel/Elaboration/Flat.lean) stores only `Rat` and has irreversibly erased authored scale, so this capsule deliberately does not bolt literal exceptions onto the flat elaborator. LF32's generic authored tree now retains decoded scale, braces/simple-constant classification, and operator form and consumes this algebra, while LF33 checks the plain division/power regions; model resolution, operation-valued wrappers, exact diagnostics, and a checked validation/computation consumer remain later work. Field-only correlation remains unchanged and is covered by the proved specialization. Static authoring acceptance is not runtime power-domain acceptance: a simple `1001` exponent can derive scale zero and still fail the separate runtime `±1000` check. The new clauses are source-grounded but lack the project-local portable separator matrix, so correspondence remains `external evidence pending`; no protocol, evidence, target, parser, or harness machinery was added.

## LF32 — division lowering is one ordered post-order pass, not algebraic normalization

> Date: 2026-07-19. Sections: §5. Basis: corrected [`spec/04-numbers-and-decimals.md` §§4–5](../spec/04-numbers-and-decimals.md#5-internal-precision--the-constants-that-must-match-exactly), pinned kernel grammar/checker, parse-tree traversal, `DivisionTransformer`, composite code generation, and precision-50 runtime source at `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a read-only a12-dmkits audit at `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green structural/value separators, and trusted local lowering equations.

After authoring checks and before code generation, the kernel walks the original operation tree post-order exactly once. At each multiplication it examines only the immediate, already-lowered children whose root is division. Ordinary factors retain their order and precede the extracted numerators; numerators and denominators retain division encounter order; multi-factor products are left-folded. The new products are not visited again. Braces reset the earlier division-count and power-nesting regions, but they do not hide a divided factor from this later pass. Therefore `{b / c} * a` becomes `{a * b} / c`, a nested extracted numerator may leave a direct division inside a newly created product, and a second pass would be a different evaluator.

This staging is numerically observable because each node rounds independently at precision 50. A counterfactual direct fold of `3 * {1 / 3}` yields `1 − 10⁻⁵⁰`; the required lowered tree `{3 * 1} / 3` yields exactly `1`. Multiplying the result by `10³¹` leaves a scale-19-visible gap of `10⁻¹⁹`, so the distinction can change a validation comparison after its own normalization. This also corrected the former pre-round example: `[q] / 3 * 3` becomes exact through division lowering and therefore cannot isolate target rounding's scale-19 pre-round; three independently divided thirds separated by addition do.

Lean treatment: [`NumericExpression.lean`](../A12Kernel/Elaboration/NumericExpression.lean) keeps generic `AuthoredNumericExpr` and `LoweredNumericExpr` types separate. The authored tree stores `DecodedNumericLiteral { value, authoredScale }`, explicit groups, arithmetic, power, and bounded rounding. `summary?` consumes that tree before lowering, so a multiplication can remain unknown-but-expandable even when the lowered root is a non-expandable division. `lowerForEvaluation` performs the exact bottom-up local step; `LoweredNumericExpr.evalValue` reuses the existing partial precision-50 arithmetic, staged power, and two-stage rounding definitions. [`Proofs/NumericExpression.lean`](../A12Kernel/Proofs/NumericExpression.lean) records group transparency for summaries/lowering, group opacity for simple-power classification, all immediate divided-factor equations, the no-second-pass nested case, and the evaluator bridge. [`Conformance/NumericExpression.lean`](../A12Kernel/Conformance/NumericExpression.lean) locks authored-scale preservation, direct versus grouped power classification, factor order, two divided factors, addition/rounding/power boundaries, unknown capability, zero-domain failure, and the rounded non-law.

The a12-dmkits authoring and interpreter trees are useful starting points but currently erase explicit grouping, carry no common static summary, and evaluate the parsed arithmetic tree directly; its Java renderer also cannot faithfully round-trip a right division under multiplication. Existing [`SPEC-2026-07-19-08`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-08--every-arithmetic-node-uses-precision-50-half_up) now states the exact ordered one-pass reconciliation and corrected pre-round discriminator, while SPEC-09 remains the separate static-summary handoff.

Limit: this is a parser-independent known-value expression seam, generic over atoms supplied by a caller. LF33 now checks the plain division/power authoring restrictions over the same tree, but model resolution, operation-valued wrapper legality, exact diagnostics/suppression, `CheckedCell` invalidity or directional-fillability propagation, validation/computation projection, Number rendering/store, and public exposure remain separate. The exact mechanism and separating values are source-grounded but have no retained portable kernel observation, so correspondence remains `external evidence pending`; no protocol, evidence, target, parser, or harness machinery was added.

## LF33 — numeric authoring regions are compositional only over plain arithmetic

> Date: 2026-07-19. Sections: §5. Basis: corrected [`spec/04-numbers-and-decimals.md` §4](../spec/04-numbers-and-decimals.md#4-other-numeric-constraints), pinned kernel grammar and `CheckOperationImpl` at `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a read-only a12-dmkits audit at `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green authoring separators, and trusted structural laws.

For parser-reachable plain arithmetic, division legality has one compositional account. Atoms expose zero divisions. Multiplication combines both open contributions; division combines them and adds one. Addition, subtraction, power, and grouping validate their children as fresh regions but expose zero upward. Any multiplication/division region above one is illegal, so `(a / b) / c` fails, `(a / b) + (c / d)` succeeds, and `{a / b} / c` succeeds without making the grouped quotient opaque to LF32's later evaluation rewrite.

Power legality is a separate syntactic rule: reject exactly a power whose direct left operand is another ungrouped power. The concrete grammar is left-associative, so this catches the reachable `a ^ b ^ c`; grouping the left power or exponent is legal, and separate powers may occur on both sides of multiplication. A generic AST can encode a direct-right ungrouped power that the parser cannot produce; accepting that shape is a checked reminder that grammar reachability remains a distinct elaboration precondition, not evidence for a symmetric kernel rule.

The kernel's operation-valued function behavior is not the same clean recursion. Its legacy listener descends through wrappers such as rounding, but a nested group/add/sub/power boundary aborts the enclosing walk through a control-flow exception. Source therefore predicts order-sensitive cases in which an early nested reset hides later factors; no focused portable probe currently closes that artifact. Treating every function as transparent or as a reset would both overclaim.

Lean treatment: [`NumericExpression.lean`](../A12Kernel/Elaboration/NumericExpression.lean) adds one private structural scan and a closed `NumericAuthoringCheck` with accepted, each independent violation, their combination, and `outsideFragment`. The scan covers atoms, literals, groups, binary arithmetic, and power; any `.round` anywhere returns `outsideFragment`, which is no kernel-legality judgment. [`Proofs/NumericExpression.lean`](../A12Kernel/Proofs/NumericExpression.lean) proves single-division acceptance, additive and grouped separation, preservation of an illegal inner region, exact direct-left power rejection, grouped acceptance, the direct-right parser-shape boundary, and wrapper exclusion. [`Conformance/NumericExpression.lean`](../A12Kernel/Conformance/NumericExpression.lean) locks the wider separating matrix and simultaneous violations.

The a12-dmkits typed tree does not retain grouping and its MVK ledger currently delegates the two checks while summarizing division only as a calculation subtree. [`SPEC-2026-07-19-14`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-14--numeric-authoring-regions-are-structural-and-function-wrappers-remain-unclosed) requests precise prose now and demands existing-harness characterization before any clean-room function-wrapper expansion.

Limit: this capsule does not parse concrete syntax, prove grammar-shape reachability, reproduce source spans/localized diagnostics or their global precedence, model operation-valued functions, resolve fields, check scale/type/domain, or alter runtime evaluation. Its plain structural clauses are source-grounded but have no retained project-local observation, and the wrapper artifact is deliberately unclosed; no evidence or harness machinery was added.

## LF34 — checked numeric validation is an explicit staged consumer

> Date: 2026-07-19. Sections: §2, §3, §5, §10, §12. Basis: [`spec/03-empty-and-required.md` A.1/A.4](../spec/03-empty-and-required.md#part-a--2-non-specified-empty-values), [`spec/04-numbers-and-decimals.md` §§1, 3–5](../spec/04-numbers-and-decimals.md), [`spec/10-validation-and-polarity.md` §4](../spec/10-validation-and-polarity.md#4-the-directional-fill-machinery-behind-the-typing), pinned kernel checker/transformer/runtime source at `cb66e51fa7ab90b650698f861bf670754e2e1e66`, read-only a12-dmkits arithmetic/polarity tests and implementation at `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green integrated conformance, and trusted projection/core-agreement laws.

The checked numeric-expression consumer remains deliberately one validation leaf, not a general expression framework. It accepts two expression operands and a closed dispatch over all six ordinary operators or the four fixed tolerance ranges, with at least one model-resolved Number field across the pair. Every source must be nonrepeatable and in the evaluated row group; cross-group, repeatable, non-Number, constant-only, power, and operation-valued shapes fail closed. Elaboration validates the model and both path sets, retains authored literal scale and grouping, applies the plain arithmetic authoring rule and static summary independently before rewriting, gates ordinary `==`/`!=` by the symmetric exact-scale rule while leaving ordering and tolerance scale-exempt, and then performs exactly one division-lowering pass per operand.

Runtime evaluation reuses the existing validation-phase Number read on both sides. An empty unsigned or signed Number becomes zero with its directional fillability, a clean Number is fixed, and a formal-invalid cell remains an exact-cause error. `Except FormalCause NumericArithmeticOutcome` keeps formal invalidity separate from pure arithmetic `notEvaluated` without introducing another effect hierarchy. The validation projection maps a formal error on either side to `Verdict.unknown`, a pure domain failure to `Verdict.notFired`, and two values to the scale-19 comparison with operand-local movement. The full-validation content gate runs before either expression, so an unreferenced sibling can still change the explicit `hasContent` premise without changing any expression atom.

The key two-sided discriminator compares filled zero on the left with empty unsigned zero on the right under `>=`: the values satisfy the condition, but the right operand can grow and clear it, so the firing is OMISSION; replacing the right operand with filled zero makes the same truth VALUE. Complementary `<` and `!=` cases separate a right operand that can grow away from the boundary from one that can shrink toward it. Both operand positions have independent one-pass-lowering, malformed, division-domain, authoring-region, kind, and scale-gate locks; equality-boundary cases distinguish strict `>` from inclusive `<=`, while pinned source establishes them as direct operator branches. In a content-bearing, row-selected instance, empty `[U] == [U]` is intentionally OMISSION because the runtime reconstructs fillability for each read and the comparator performs no dependency alias analysis; an entirely empty full-validation row remains `notFired`. This alias outcome is source-inferred and remains external evidence pending. A trusted theorem proves direct atom/literal agreement with the shared low-level Number-field evaluator, not equality of the two checked surface languages.

One richer internal choice is deliberately conservative rather than a kernel-correspondence claim: if one operand is formally invalid and the other has an arithmetic domain failure, the Lean projection retains `unknown`. The canonical rule says formal validity is prior, but the kernel runtime collapses both hidden failures to authored-message silence and does not establish the same internal precedence. The conformance locks both operand orders as a project refinement while the mixed kernel state remains open. The two-expression shape, six direct operators, scale rules, and directional polarity were already canonical, so no `spec/` change or new [`A12-DMKITS-SPEC-SYNC-LEDGER.md`](A12-DMKITS-SPEC-SYNC-LEDGER.md) entry was warranted.

Limit: this capsule does not admit power fillability, concrete ordinary/tolerance syntax, `BaseYear`, operation-valued wrappers, partial validation, repeats, cross-group/global evaluation, kernel diagnostic codes, computations or their generated validation, target rendering/application, the public reference protocol, or portable local evidence. Its structured surface assumes a grammar-valid decoder and coherent literal value/authored-scale pairs; it does not prove those preceding parser obligations or reject every generic-AST shape that the concrete grammar cannot produce. Correspondence excludes mixed formal/domain precedence and otherwise remains `external evidence pending`; no evidence, protocol, harness, or governance code was added. The closed operator dispatch is the second demonstrated consumer of the exact checked result domain and replaces a parallel tolerance elaborator; resolution, static checks, lowering, reads, failure projection, and row gating remain single-source.

## LF35 — numeric computation preserves domain failure and read poison separately

> Date: 2026-07-19. Sections: §2, §3, §4, §5, §11. Basis: [`spec/03-empty-and-required.md`](../spec/03-empty-and-required.md), [`spec/04-numbers-and-decimals.md` §3](../spec/04-numbers-and-decimals.md#3-arithmetic-domain-failures-are-consumer-sensitive), [`spec/09-computations.md` §§1–3](../spec/09-computations.md), pinned kernel `VkBigDecimal`, `CompositeOperation`, rounding, calculation-result, and calculation-cache source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, read-only a12-dmkits computation prose and implementation at `2ceee778cbcbd16a63e456fb662d3b61a13c99a8`, red/green conformance, and trusted expression-outcome laws.

Number computation needs a result domain different from both validation and String computation. An empty Number operand—including required-only empty—contributes the real value zero, so the expression has no clean `noValue` arm and no full-validation content gate. Division by zero instead produces arithmetic domain failure, which clean enclosing ordinary arithmetic and explicit rounding preserve until the target consumer classifies it; a later evaluated invalid read can still replace it with poison. A formally invalid field actually read is neither zero nor domain failure: it preserves its exact cause as computation poison. Clean false/no-match belongs to the already separate precondition and alternative-selection boundary and never enters this expression result.

Evaluation order matters even before a scheduler exists. Generated arithmetic is a Java receiver chain: the left numeric result is produced first, then the right argument is evaluated before the enclosing method call. A zero division is represented by the kernel's invalid numeric value rather than an exception, so a later right-side invalid-cell read can still poison; a left poison read throws and prevents the right subtree from replacing its cause. The Lean evaluator exposes these as ordered equations, while portable mixed domain/poison confirmation remains pending.

Lean treatment: [`NumericComputationResult.lean`](../A12Kernel/Semantics/NumericComputationResult.lean) defines `NumericComputationResult = value | domainFailure | poison`, while [`NumericComputation.lean`](../A12Kernel/Elaboration/NumericComputation.lean) retains the already-resolved `FlatFieldDecl` on every atom, refines the shared checked computation context into Number reads, lowers an authored expression exactly once, and evaluates admitted binary arithmetic and rounding. A complete structural preflight rejects every non-Number declaration and power subtree before any data read, preventing empty wrong-kind substitution or unsupported syntax from being hidden by an earlier poison. The runtime order is the lowered-tree order: the checked `(Source / 2) * Later` separator becomes `(Later * Source) / 2`, so distinct poison causes establish that `Later` is reached first. The existing pure expression evaluator and the computation evaluator now share only [`NumericScaleBinaryOp.evalValues`](../A12Kernel/Elaboration/NumericExpression.lean), the exact common two-known-values arithmetic dispatch. They do not share validation fillability/failure projection or a generic String/Number computation result because those meanings differ.

[`Proofs/NumericComputation.lean`](../A12Kernel/Proofs/NumericComputation.lean) proves declaration-certified empty and required-empty zero, exact poison preservation, universal direct division-by-zero domain failure, rounding and clean enclosing-arithmetic preservation on either side, admitted left-poison short-circuiting, the source-grounded right-poison-after-left-domain law, result separation, and context-independent structural rejection. [`Conformance/NumericComputation.lean`](../A12Kernel/Conformance/NumericComputation.lean) locks clean values, all three result classes, requiredness, one-pass numeric lowering, rounding, both domain-failure operand positions, authored-versus-lowered poison order, empty and hidden wrong-kind rejection, and a hidden-power discriminator.

Limit: this is one declaration-resolved pre-target expression consumer, not checked concrete computation syntax or a full computation. It assumes each declared Number atom is paired with a coherent checked cell; the later checked computation route must prove model/declaration/context coherence rather than treating the defensive reached-value kind fault as that certificate. General operation-wrapper authoring legality remains unclosed even though the admitted runtime rounding node is explicit. Power computation is rejected because its invalid-result projection is not audited. LF36 supplies ordinary fit-path target classification, stored rendering, and prior-target delta; LF37 supplies exact one-address final application; LF38 supplies the cause-free outcome-to-dependency observation. Checked concrete target authoring, warning-suppressed no-fit storage, additional Number constraints, downstream context/read integration, precondition/table integration, repeats, scheduling, generated validation, public protocol, and project-local portable evidence remain open. [`spec/09-computations.md` §3.2](../spec/09-computations.md#32-an-invalidity-clear-poisons) records the source-established lowered-tree arithmetic order, and existing [`SPEC-2026-07-19-12`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-12--division-by-zero-computations-poison-despite-a-cleared-delta) carries the peer and dual-route target/dependent-read/order reconciliation rather than duplicating the queue. No evidence or harness machinery was added.

## LF36 — numeric target storage separates decimal form, invalidity, and delta

> Date: 2026-07-19. Sections: §5, §11. Basis: [`spec/09-computations.md` §§2–4](../spec/09-computations.md), pinned kernel `CalculationController`, `FormatDefinitionZahl`, `BigDecimalUtils`, computation-result, and calculation-cache source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, read-only a12-dmkits store implementation and IF34/IF82/IF92 findings at `a00bfc08`, red/green conformance, and trusted target/delta laws.

A numeric expression value and a stored Number are different domains. Expression arithmetic stays an exact `Rat` with explicit rounding at semantic boundaries; target storage needs an exact decimal representation because `7` and `7.00` have equal rational amount but different change behavior. The smallest coherent stored type is therefore `{unscaled : Int, scale : Nat}`. Its plain rendering is derived rather than separately mutable, so coefficient, scale, amount, text, structural equality, and delta equality cannot drift apart.

The ordinary target path applies scale-19 `HALF_UP`, strips only fractional trailing zeros, and pads to at least `minFractionalDigits`. It does not shorten a fitting value to satisfy the universal 15-digit check: the full attempt survives as target rejection. The first admitted policy also checks unsigned negative output after the digit limit. Natural scale above `maxFractionalDigits` is not rounded away; Lean fails closed because the runtime no-fit branch requires the separately authorable `MVK_INVALID_COMPARE_DEC_PLACES` suppression and has its own bounded rendering and unconditional invalidity. [`SPEC-2026-07-19-15`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-15--computed-number-storage-has-distinct-fit-and-warning-suppressed-no-fit-branches) carries that canonical correction and peer reconciliation.

The target result must preserve five meanings: clean no-result, accepted stored decimal, payloadful target rejection, calculation-local invalidity with no attempted decimal, and inherited operand poison. Division domain failure becomes the fourth case and carries the language-neutral `calculationValue` cause behind `berechnungsWertFehler`; it is not an ordinary target rejection and not an inherited source cause. Delta projection deliberately loses some of this information: accepted values compare coefficient and scale; rejection is unconditionally ERRORED; clean no-result, calculation invalidity, and poison are all silent over an empty prior and CLEARED over a filled prior. Trusted laws record both that equality and the non-equivalence it conceals, preparing the later dependent-read separator without inventing a scheduler.

Lean treatment: [`NumericStoredNumber.lean`](../A12Kernel/Semantics/NumericStoredNumber.lean) owns the exact decimal form, normalized dot-decimal rendering, scale-19 coefficient conversion, zero stripping, and minimum-scale padding. Its [proof module](../A12Kernel/Proofs/NumericStoredNumber.lean) proves that stripping and padding each preserve amount and, universally, that complete conversion preserves exactly the scale-19 `HALF_UP` result; it also records the `7` versus `7.00` non-law. Its [conformance module](../A12Kernel/Conformance/NumericStoredNumber.lean) separates whole, leading-fractional-zero, negative, and zero rendering. [`NumericTarget.lean`](../A12Kernel/Semantics/NumericTarget.lean) separately owns the post-static-admission policy, fail-closed no-fit routing, five target outcomes, and placement-erasing delta projection. Its [proofs](../A12Kernel/Proofs/NumericTarget.lean) preserve domain/poison causes, no-fit refusal, digit-attempt retention, signed fit acceptance, exact stored-form delta laws, and semantic constructor distinctions; its [conformance cases](../A12Kernel/Conformance/NumericTarget.lean) distinguish pre-round-before-fit routing, the exact 15-digit boundary, digit-before-signedness precedence, full overflow, no-fit refusal, domain/poison distinction, and delta behavior.

Limit: the policy intentionally cannot represent min/max value, zero prohibition, explicit integer-digit caps, amount traits, leading-zero policy, Number min/max length, custom type checks, or scale-warning suppression. Target rejection causes currently cover only total digit overflow and unsigned negativity. LF37 supplies exact absent/present-empty/present-value final application, and LF38 supplies one cause-free target-outcome dependency observation. Checked target declaration/operation-scale agreement, downstream context/read integration, power invalidity, generated validation, repeatable scope, public protocol, and locally retained portable numeric-target observations remain open. a12-dmkits' multi-route differentials are design knowledge and triangulation, not this repository's retained evidence. No evidence, protocol, harness, governance, or dependency work was added.

## LF37 — numeric application preserves placement but erases cause provenance

> Date: 2026-07-19. Sections: §1, §5, §11. Basis: [`spec/01-data-model.md` §4.3](../spec/01-data-model.md#43-the-compute--apply--validate-flow) and [`spec/09-computations.md` §2](../spec/09-computations.md#2-separate-operations-and-the-three-outcomes), pinned kernel V2 computation-result/application source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a12-dmkits IF126 and its type-neutral String application matrix at `a00bfc08`, red/green conformance, and trusted relational/application laws.

Exact application needs one state dimension that delta deliberately erased. An absent target and a present-empty target both project to the empty delta prior, yet applying clean no-value leaves the first absent and the second present-empty. An accepted result yields `presentValue` with the exact `StoredNumber` coefficient and scale. Every outcome with no applied value—clean no-result, target rejection, calculation invalidity, or inherited poison—empties a present target in place and never creates an absent target solely to clear it.

Final state is not semantic provenance. Quiet no-value and target rejection can both leave a formerly filled target present-empty, while their deltas remain CLEARED versus ERRORED. Quiet no-value and calculation invalidity go further: exact application and delta both agree for every prior state, yet the outcomes remain different because calculation invalidity, unlike quiet no-value, can poison a dependent read. A rejected attempt likewise remains on the full outcome and ERRORED delta but is never stored by application. These information-loss laws prevent a later consumer from reconstructing computation meaning from the document or delta alone.

Lean treatment: [`NumericApplication.lean`](../A12Kernel/Semantics/NumericApplication.lean) defines the Number-specific three-state target and final-state transition without refactoring the separate String estate into a generic framework. Its [proof module](../A12Kernel/Proofs/NumericApplication.lean) gives an independently enumerated relation, evaluator/relation equivalence, determinism, exact stored-value agreement, clearing and presence laws, and the three placement/provenance non-laws. Its [conformance module](../A12Kernel/Conformance/NumericApplication.lean) locks accepted `7.00` over absent, empty, `7`, and equal `7.00` states; all four no-applied outcome classes; absent-versus-present-empty loss; and unconditional ERRORED provenance.

Limit: `NumericTargetState` records only final placement and exact typed decimal. A typed-equal accepted result is not physically rewritten by the kernel because only changed results are applied, but this abstraction has the same final `presentValue` state and deliberately excludes raw lexical preservation. Missing-ancestor creation, document traversal, repeatable addressing, multi-target order, scheduling, downstream context/read integration, and unsupported no-fit results are outside this capsule; LF38 separately owns the pre-consumer dependency observation. The kernel V2 application mechanism is type-neutral and a12-dmkits triangulates its placement table with String targets, but this repository retains no Number-specific application observation; correspondence remains `external evidence pending`. The deprecated mutable V1 application route is not part of the supported account. The canonical clauses already state the application table exactly, so no `spec/` correction or new [`A12-DMKITS-SPEC-SYNC-LEDGER.md`](A12-DMKITS-SPEC-SYNC-LEDGER.md) queue entry was warranted. No evidence, protocol, harness, governance, or dependency work was added.

## LF38 — numeric dependency meaning survives lossy application and delta

> Date: 2026-07-19. Sections: §1, §5, §11. Basis: [`spec/09-computations.md` §§3.1–3.3 and §4](../spec/09-computations.md), pinned kernel `CalculationCommand`, `CalculationController`, `CalculationCache`, and generated calculation-alternative source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, red/green conformance, and trusted dependency-projection laws.

A completed Number target has a dependency meaning that cannot be reconstructed from either its applied document state or reported delta. Clean no-result supplies an ordinary empty dependency; accepted output supplies the exact target-rendered `StoredNumber`; target rejection, calculation-local invalidity, and inherited invalidity all supply a poisoned dependency. The invalid classes do not expose their attempted value or producer-side cause at this boundary: the kernel's calculation cache checks only invalid membership and throws before returning value or presence. Their full provenance remains on `NumericTargetOutcome`.

This makes the division-by-zero separator exact. Over a stale filled target, clean no-result and `invalidNoValue calculationValue` both report CLEARED and both apply as present-empty; over an absent target, both report nothing and preserve absence. Their dependency observations still differ as `empty` versus `poisoned`. The integrated executable witness evaluates the source-established, already-resolved `RoundAccounting([Source] / 0, 2)` shape to domain failure, classifies it as calculation invalidity at the target, and projects poison. It does not claim that Lean currently checks the concrete wrapper's authoring legality or target assignment-scale gate.

Lean treatment: [`NumericDependency.lean`](../A12Kernel/Semantics/NumericDependency.lean) defines only `empty | value StoredNumber | poisoned` and the five-row projection from the existing complete target outcome. Its [proof module](../A12Kernel/Proofs/NumericDependency.lean) characterizes exactly which outcomes yield empty and exact value, locks calculation invalidity as poison, proves that rejected attempts are unreadable, and gives the universal application-plus-delta non-law. Its [conformance module](../A12Kernel/Conformance/NumericDependency.lean) covers all producer classes, exact `7.00`, the complete resolved division route, stale clearing, and fresh silence.

Limit: this is a pre-consumer observation, not `CellObservation`, a synthetic checked cell, a downstream numeric-expression evaluator, a document overlay, or a scheduler. Existing Number read semantics establish that ordinary empty is consumed as zero, but this capsule does not yet inject the observation into `ScalarComputationContext` or prove multi-read order. It does not cover invalid power, precondition/table integration, transitive dependencies, repeats, target paths, message/path provenance, or portable dual-route evidence. The canonical spec already states the behavior; existing [`SPEC-2026-07-19-12`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-12--division-by-zero-computations-poison-despite-a-cleared-delta) remains the single pending peer/evidence reconciliation and its compatibility status is refreshed rather than duplicated. No evidence, protocol, harness, governance, dependency, or generic abstraction was added.

## LF39 — resolved whole-rule messages are a checked post-verdict boundary

> Date: 2026-07-19. Sections: §1, §10, §12, §13. Basis: [`spec/01-data-model.md` §§4.1 and 4.3](../spec/01-data-model.md), [`spec/08-paths-and-references.md`](../spec/08-paths-and-references.md), [`spec/10-validation-and-polarity.md`](../spec/10-validation-and-polarity.md), [`spec/11-messages-and-custom.md`](../spec/11-messages-and-custom.md), red/green conformance, trusted rule-emission laws, and independent source/spec/proof review.

A whole validation rule does not author its VALUE/OMISSION message type. Its condition first yields the complete `Verdict`; only `fired polarity` creates a message, while `notFired` and `unknown` remain distinct internally even though both emit nothing. The emitted message copies an explicit error address, error code, severity, and already-resolved text and derives its message type from that firing polarity. Changing the error field, code, severity, or resolved text therefore cannot change condition truth or polarity. ERROR, WARNING, and INFO all emit when the same condition fires, but only ERROR makes the document invalid.

Lean treatment: [`Semantics/ValidationRule.lean`](../A12Kernel/Semantics/ValidationRule.lean) defines `ResolvedFlatRule`, `FlatRuleOutcome`, and `FlatRuleMessage` as the low-level already-lowered, per-instance emission boundary. [`Elaboration/ValidationRule.lean`](../A12Kernel/Elaboration/ValidationRule.lean) accepts an existing `CheckedFlatCondition`, resolves the explicit error-field ID uniquely in the same `FlatModel`, requires it to be nonrepeatable and referenced by the condition, and evaluates `RawFlatContext` only after that model derives the checked cells. It never infers the error field from the first reference. `hasContent` remains an authoritative caller input because model checking validates cells but does not derive row content or topology. [`Proofs/ValidationRule.lean`](../A12Kernel/Proofs/ValidationRule.lean) proves verdict recovery, exact core and checked message transport, metadata independence, ERROR-only invalidation, assembly coherence, and the equal-silence/non-equal-verdict boundary. [`Conformance/ValidationRule.lean`](../A12Kernel/Conformance/ValidationRule.lean) separates empty OMISSION from filled VALUE, false from malformed silence, the row gate, all severities, an explicit non-first error field, repeatable/unreferenced/unknown field rejection, and token-looking text that remains byte-for-byte opaque.

`ResolvedMessageText` is intentionally not an authored template and this capsule implements no interpolation. Its empty repetition path is valid only because repeatable error fields are rejected. Rule path, referenced-field metadata, fill-to-fix metadata, partial validation, rule collections and ordering, generated checks, labels, localization, custom conditions, concrete syntax, and public protocol exposure remain outside. The one-constructor mandatory severity and message-type types stay as stronger generated-rule refinements until an actual generated-rule integration consumes the shared boundary. Existing retained validation evidence anchors component polarity and selected pointers, not checked rule assembly, code/severity independence, resolved text, or the hidden silent distinction, so the whole capsule remains `external evidence pending`. The generated-computation validation audit exposed the missing code channel before its capsule consumed this boundary; the error code is now transported by the general rule boundary rather than a computation-specific message type. The canonical spec already requires error codes and the existing pending generated-validation reconciliation owns its peer correction, so no new `spec/` change or [`A12-DMKITS-SPEC-SYNC-LEDGER.md`](A12-DMKITS-SPEC-SYNC-LEDGER.md) row was warranted. No support, evidence, harness, governance, protocol, or dependency code was added.

## LF40 — computation selects one alternative while generated validation retains both

> Date: 2026-07-19. Sections: §1, §11, §12. Basis: [`spec/01-data-model.md` §3.2](../spec/01-data-model.md), [`spec/09-computations.md` §§5–6](../spec/09-computations.md), [`spec/10-validation-and-polarity.md`](../spec/10-validation-and-polarity.md), pinned kernel `IComputation`, `ValidationModelConverter`, `ComputationAlternativesJoiner`, normalized-calculation builder, and Java/Groovy/JavaScript alternative templates at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, a12-dmkits read-only triangulation at revision `73f039a9e2954e9c1cf8813668b12ad336f14d23`, red/green conformance, and trusted structural/gating laws.

A computation alternative table has two deliberately different consumers. Computation scans in declaration order and selects the first guard that is known true; a selected operation terminates the scan. The generated validation rule instead retains every alternative as a guarded mismatch. Therefore, with the same holding guard on literal operations `1` then `2`, a stored target `1` is exactly the first selected computation result and still produces a generated error because the later retained mismatch is true. Equal results or a nonholding later guard are silent controls. Mutual exclusivity is author discipline, not a premise supplied by the runtime.

Shared guard syntax does not imply a shared evaluator. Computation observes checked cells in computation phase and preserves a read poison; generated validation is an ordinary validation condition with unknown, strong-Kleene connective behavior, and data-derived VALUE/OMISSION polarity. In the separating case `poisoned FieldFilled Or healthy FieldFilled`, computation aborts at the left read while generated validation can still fire because the healthy validation branch dominates unknown. Conversely, an empty `FieldNotFilled` guard can make the generated ERROR message an OMISSION. Any future agreement theorem therefore needs explicit phase-compatible hypotheses in addition to mutual exclusivity or equal results.

Lean treatment: [`Elaboration/GeneratedComputationValidation.lean`](../A12Kernel/Elaboration/GeneratedComputationValidation.lean) admits exactly two guarded decoded numeric literals for one nonrepeatable Number target. It reuses `ComputationAlternative.selectFirst`, structurally translates each direct-presence/`And`/`Or` guard into `FlatCondition`, retains both strict-mismatch branches below an explicit `FieldFilled(target)` gate, checks each authored literal scale before erasing it to `Rat`, checks guard and target declarations against the same validated model, and reuses `CheckedResolvedFlatRule` for target address, computation name as error code, fixed ERROR severity, resolved text, and verdict-derived message polarity. [`Proofs/GeneratedComputationValidation.lean`](../A12Kernel/Proofs/GeneratedComputationValidation.lean) locks first-row selection, the exact two-branch shape, and universal empty-target suppression for either target-instance content bit. [`Conformance/GeneratedComputationValidation.lean`](../A12Kernel/Conformance/GeneratedComputationValidation.lean) checks the central non-law, equal-result and nonoverlap controls, target gating, phase divergence, omission polarity, a swapped second instance, scale rejection, unsupported String presence, and a non-Number target.

This is the first closed table fragment, not general generated-computation validation. It assumes the source computation has already passed general authoring legality and does not check target self-reference. Singleton/default and more-than-two alternatives, common preconditions, tolerance, expression operations, operation no-value/domain failure, warning-suppressed assignment legality, runtime target checks, repeatable evaluation, anchoring, scheduling, application, partial validation, concrete syntax, protocol, and portable observations remain outside. The source audit confirms the mechanism but supplies no retained black-box observation, so the capsule remains `external evidence pending`. a12-dmkits implements distinct first-match and all-alternatives mechanisms, but its current prose still conflates them, its focused tests do not compose the overlap discriminator, and its loader appears to drop the computation's generated-rule message text; existing pending [`SPEC-2026-07-19-06`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-06--first-match-computation-differs-from-all-alternatives-validation) already owns the mechanism/prose reconciliation, while the message-text transport needs a separate focused peer follow-up rather than a duplicate spec-ledger row. No evidence, protocol, harness, governance, scheduler, general table framework, or dependency work was added.

## LF41 — calendar reality and A12 Date value admission are different types

> Date: 2026-07-19. Sections: §6, §12. Basis: the decoded chronology, calendar-reality, and value-floor clauses of [`spec/05-dates-and-time.md` §§1, 3–4](../spec/05-dates-and-time.md), pinned kernel format, strict civil parser, universal-floor, and direct-comparison source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`, read-only a12-dmkits Date implementation and focused differentials at revision `e346e968e80ebcd596f14660db19b03dcacada54`, red/green conformance including a wrong componentwise-order mutation, and trusted constructor/order laws.

A decoded year/month/day triple, a real calendar day, and an A12-storable Date are three different states. Calendar construction uses positive-era proleptic-Gregorian month lengths and leap years; A12 stored and computed full-Date values additionally require the inclusive universal floor 1583-10-16. The floor is not calendar reality: `1500-01-01` and `1583-10-15` are real days but not admitted values. The optional 1900 field policy is different again, so `1899-12-31` passes the universal floor.

Lean treatment: [`Semantics/FullDate.lean`](../A12Kernel/Semantics/FullDate.lean) represents the stages as raw `DateParts`, proof-carrying `CivilDate`, and floor-certified `FullDate`. Both constructors are partial and fail closed. Strict `before` compares the admitted components lexicographically; it does not encode dates as an epoch count or compare all components conjunctively. Verdict and polarity are deferred until a later operand layer can carry empty/formal/not-given provenance. [`Proofs/FullDate.lean`](../A12Kernel/Proofs/FullDate.lean) exposes the leap-aware reality invariant, characterizes both constructors, carries reality/floor certificates, proves total strict chronology, and connects executable comparison to that order. [`Conformance/FullDate.lean`](../A12Kernel/Conformance/FullDate.lean) locks every ordinary-month length, Gregorian century rules, impossible dates, positive-era bounds, both floor sides, the 1899 separator, year/month/day crossings, equality/reversal, and the componentwise-order non-law.

Limit: this begins after component decoding and deliberately defines an unbounded positive-era Gregorian account plus strict `<`; year zero and negative years are rejected by the Lean constructor, while exact parser range and kernel correspondence for values outside the reachable full-Date fragment remain unclaimed. It does not claim textual formats, optional-1900 policy integration, `Value`/`CheckedCell`/formal-error or empty semantics, polarity, the other five operators, `Date(...)`, fragments/ranges/Base Year, DateTime/Time, zones/DST/`Today`/`World`, arithmetic/differences, checked rule lowering, or protocol support. Pinned source and a12-dmkits tests ground the chosen reachable account; this repository retains no portable Date observation, so correspondence remains `external evidence pending`. The canonical spec already distinguishes these clauses, so no `spec/` correction or new synchronization-ledger row was warranted. No support, evidence, protocol, harness, governance, dependency, or general temporal framework was added.

## LF42 — DateTime arithmetic needs scalar instant identity apart from wall labels

> Date: 2026-07-19; extended 2026-07-20. Sections: §1, §6, §12. Basis: the decoded whole-second, value-floor, UTC, model-zone overlap, and instant-arithmetic clauses of [`spec/05-dates-and-time.md` §§2–5 and 9](../spec/05-dates-and-time.md); pinned kernel canonical-format, strict model-zone parsing, numeric conversion, calendar-add, Date-equality, and instant-difference source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`; read-only a12-dmkits `DateMath`/`ExprEval` triangulation plus maintained `DateTimeArithmeticDiffTest` and `DstTimeZoneDiffTest` at revision `9dd45186609a88857c8f16021b46954b4305a35f`; wrong-unit, wrong-overlap-policy, and floor-division red/green mutations; independent semantic, Lean-proof, and architecture reviews; trusted constructor, resolver-branch, same-date, shift-algebra, and resolved-difference laws.

A local DateTime such as `2024-10-27T02:30:00` is a wall label; arithmetic and comparison operate on an instant. UTC deterministically resolves the label because it has no gaps, folds, or offset, but those concepts must remain different types: under Berlin the autumn label can be rendered from two different instants, and reparsing it chooses the standard-time one. Chained `AddHours` must retain the arithmetic instant rather than round-trip through a local string.

Lean treatment: [`Semantics/DateTime.lean`](../A12Kernel/Semantics/DateTime.lean) reuses floor-certified `FullDate`, adds proof-carrying whole-second `TimeOfDay` and `LocalDateTime`, strengthens the pre-existing `Instant` placeholder into a shared `{ epochSecond : Int }` identity, and resolves UTC labels through an executable Gregorian day coordinate. `CivilDate.next?` advances through the checked civil constructor; `LocalDateTime.Before` orders admitted wall labels before zone resolution. `Instant.shiftHours` adds an already-converted integer amount times 3,600; it is the total instant core after numeric conversion, not the full authored operator. `BerlinAutumn2024.resolveLocal?` is a fixed-date overlap discriminator, not a timezone framework: it accepts only 2024-10-27, resolves before 02:00 with the daylight offset and from 02:00 with the standard offset, and fails closed elsewhere. [`Semantics/DateTimeDifference.lean`](../A12Kernel/Semantics/DateTimeDifference.lean) separately closes the post-resolution `DifferenceInHours/Minutes/Seconds` core: a three-case unit supplies a positive divisor and `Instant.difference` computes `(second − first).tdiv unitSeconds`. Its proofs establish positive units, self-zero, operand-swap negation, exact seconds, exact-unit recovery, and agreement with `shiftHours`; cases lock positive and reverse fractional spans, exact seconds, the selected Berlin fall-back and chained/fresh-instant gaps, and truncation non-additivity. Replacing `tdiv` with floor division failed exactly the two negative fractional controls before restoration.

Limit: this remains a selected post-parse whole-second fragment: general UTC, resolved sub-day instant differences, plus one exact Berlin autumn-transition date. The fixed resolver is intentionally replaced when a general Berlin account lands; it is not retained beside that account as legacy API. The successor and order theorems close the internal coordinate bridge for the modeled positive-era Gregorian dates, but they do not establish parser reachability, external correspondence, or a zone-sensitive order. Numeric decimal-to-integer conversion for additions, empty/formal difference operands and their fillability/polarity, static DateTime-only operator gates, parsing/formatting, local rendering, numeric result admission, `Value`/cell/target integration, comparison against the produced Number, `DifferenceInDays` and calendar differences, other date arithmetic, general model-zone dispatch, ordinary Berlin dates, spring-gap formal errors, other transition years and history, `Today`/`Now`, checked lowering, and protocol support remain open. Maintained Groovy-dynamic kernel differentials directly establish positive and reverse fractional sub-day truncation; selected whole-unit DST cases also agree through generated static Java, but that route does not separately cover the reverse-fraction discriminator. This repository retains no portable DateTime observation, so correspondence remains `external evidence pending`. The canonical spec already states the selected behavior, so no `spec/` correction or synchronization-ledger entry was added. No support, evidence, protocol, harness, new governance mechanism, dependency, or generic temporal framework was added.

## LF43 — message rendering is one pass over display-layer inputs

> Date: 2026-07-19. Sections: §3, §13. Basis: [`spec/02-logic-and-formal-errors.md`](../spec/02-logic-and-formal-errors.md), [`spec/11-messages-and-custom.md`](../spec/11-messages-and-custom.md), pinned kernel rule-message substitution, public display-data, label-resolution, and format-default source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`; read-only a12-dmkits renderer and focused filled/empty interpolation differentials at revision `8b18848de798da0d24158f8438b81c3eb53c3eb2`; red/green conformance and trusted provider/default/opacity/order laws.

The kernel performs rule-message interpolation as one ordered split/substitution/join pass over authored `$...$` text. Lean starts after that syntax has been decoded and field-resolved: it renders structured parts from left to right, treats an escaped dollar as ordinary text, and never parses replacement bytes again. A display value such as `$Other$`, HTML-sensitive text, or CRLF therefore remains opaque once supplied to this renderer. The public validation route obtains that value from caller/display data, not from the normalized evaluation cache. Source inspection consequently predicts that an ordinary String can be CRLF-normalized for conditions and computations while its message display still carries raw CRLF; this separating claim needs a focused external probe before kernel-correspondence closure.

Field display names and empty values are likewise external display-policy inputs, not intrinsic `Value` rendering. The public service first accepts an effective provider result exactly, including an empty string; otherwise it uses a nonempty localized model label and finally the identifier's debug representation. A missing or empty `getValueForDisplay` result selects the field format's exact default. The externally differentiated scale-zero Number and String cases yield `0` and empty respectively, but a Number format may supply `0.00` or a locale-specific equivalent. Present Number, Date, Boolean, Confirm, enumeration, and String display forms remain caller/format results, and the renderer adds no unit or second formatting pass.

Lean treatment: [`Semantics/ValidationRule.lean`](../A12Kernel/Semantics/ValidationRule.lean) adds only `MessageNameInput`, `MessageValueInput`, `MessageRenderPart`, and `MessageRenderPlan` beside the existing `ResolvedMessageText` boundary. The plan has text, field-name, and field-value parts; reference identity has already been consumed by the omitted checked resolver. Its total recursive renderer concatenates exact outputs once. [`Proofs/ValidationRule.lean`](../A12Kernel/Proofs/ValidationRule.lean) locks provider priority including empty output, every model-label/debug fallback branch, missing/empty value defaults, list-append composition, and opacity of every nonempty present display string. [`Conformance/ValidationRule.lean`](../A12Kernel/Conformance/ValidationRule.lean) separates scale-zero and scale-two Number defaults, empty String, provider/model/debug priority, decoded-dollar text, token-looking replacement data, repeated raw CRLF, and ordering. Removing the explicit-empty fallback broke both its Number case and the universal default law; restoration returned both green.

Limit: this is the pure renderer intended for the post-fire boundary, not the checked integration that invokes lookup/display resolution only after a condition fires. It does not parse raw templates, validate field/path/star/non-starred-reference legality, choose locale, invoke a provider, look up a document value, derive a format default, render debug paths, or support repetition/index/category/semantic-index/BaseYear parameters or field-owned formal-error texts. The source audit also exposes three places where current canonical prose is too broad: it folds the actual provider/model-label/debug cascade into a name fallback, states only the common scale-zero Number default, and says message interpolation sees evaluation-cache CRLF normalization. Under the repository rule that `spec/` needs exact user authorization, those clauses and the synchronization ledger remain unchanged; a complete §13 claim is blocked until that correction is authorized and the CRLF discriminator is externally checked. No support, evidence, protocol, harness, new governance mechanism, dependency, or general formatting framework was added.

## LF44 — `No` and `NotAll` value-list quantifiers are not duals

> Date: 2026-07-20. Sections: §3, §5, §8, §12. Basis: [`spec/06-strings-and-enumerations.md` §B.3](../spec/06-strings-and-enumerations.md#b3-the-value-list-quantifiers--per-cell-three-way-classification-), read-only a12-dmkits [`ValueListQuantEval.kt`](../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/eval/ValueListQuantEval.kt) and maintained [`ValueListQuantifierDiffTest.kt`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ValueListQuantifierDiffTest.kt), [`ValueListQuantifierStarDiffTest.kt`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ValueListQuantifierStarDiffTest.kt), and [`ValueListQuantifierHavingDiffTest.kt`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/ValueListQuantifierHavingDiffTest.kt) at revision `ac743157c5ba990183a51bf00950c46703c7a7b5`; red/green separating cases, a fields-UNKNOWN-poisons-`NotAll` mutation, and trusted classification/polarity laws.

Both sides of a value-list quantifier are classified per cell, but the operators consume those classes differently. `AtLeastOne` skips empty and UNKNOWN cells on either side and can never return UNKNOWN. `No` checks for an UNKNOWN field or member before membership, so even a separately known match cannot turn poison into a definite false. `NotAll` first needs one present field; with none it is definitely not fired even if the values side contains UNKNOWN. Once that prerequisite holds, an UNKNOWN member poisons, while an UNKNOWN field is skipped. The smallest shared-input separator is fields `[present "B", unknown]` and members `[present "A"]`: `No` is UNKNOWN, but `NotAll` fires VALUE.

Polarity is independently asymmetric. `No` treats empty cells or an uninstantiated tail on either side as omission potential. `NotAll` considers omission potential only on the values side; fields-side empty and UNKNOWN siblings do not change a clean out-of-set firing. `AtLeastOne` ordinarily fires VALUE. A `Having` marker on either already-filtered side escalates any of the three firings to OMISSION. Empty members contribute omission potential but no value: an empty Number member never matches zero.

Lean treatment: [`Semantics/ValueList.lean`](../A12Kernel/Semantics/ValueList.lean) uses a kind-indexed atom domain so Number/token mixing cannot reach the evaluator; Number equality delegates to the existing scale-19 comparison boundary, while canonical stored tokens compare exactly. `ResolvedValueListSide` retains classified cells, uninstantiated-tail metadata, and whether filtering occurred. The evaluator arms remain separate rather than hiding the precedence differences in a generic quantifier fold. [`Proofs/ValueList.lean`](../A12Kernel/Proofs/ValueList.lean) states the actual UNKNOWN, no-present, empty-member, and `Having` laws. [`Conformance/ValueList.lean`](../A12Kernel/Conformance/ValueList.lean) checks the non-law, both branch precedences, numeric normalization, empty-versus-zero, tail and filter polarity, and the sole can-fire-on-empty operator. A mutation that globally poisoned `NotAll` on a fields-side UNKNOWN failed the central separator and the mixed-sibling case before exact restoration.

Limit: this is the resolved runtime consumer, not full §8 authoring or expansion. It assumes that a checked layer has already admitted only Number or mutually comparable String/Enumeration operands, canonicalized tokens, expanded stars, run `Having` before classification, and derived tail metadata correctly. Scalar value-list syntax, path/scope resolution, actual filter execution, enum domains/text compatibility/categories, row-gate integration, checked lowering, protocol exposure, and project-local portable observations remain open. The maintained external differentials are sufficient to choose and triangulate the account, but correspondence remains `external evidence pending`. No spec correction, synchronization-ledger entry, support mechanism, evidence machinery, protocol, dependency, or general quantifier framework was added.

## LF45 — repetition uniqueness builds one branch-independent duplicate relation before verdict composition

> Date: 2026-07-20. Sections: §3, §5, §9, §12. Basis: [`spec/07-repetition-and-iteration.md` §6](../spec/07-repetition-and-iteration.md#6-repetitionnotunique-precisely-), pinned kernel 30.8.1 `RuntimeController.wiederholungNichtEindeutig`/`ermittleWiederholungenMitDuplikaten`/`getWerteOrNullIfEinFeldNichtPruefungsRelevant` and `ValidierungsErgebnis`, read-only a12-dmkits [`RepetitionNotUniqueEvaluator.kt`](../../a12-rulekit/interpreter/src/commonMain/kotlin/io/github/mbackschat/a12/dm/interpreter/RepetitionNotUniqueEvaluator.kt) plus maintained `RepetitionNotUnique*DiffTest` families at revision `07fb8c03f5519950567c726372568769ad06beb4`; red/green separating cases, an optional-empty equality mutation, and trusted duplicate-membership, firing, cluster, classification, unique-identity, and distinct-peer laws.

`RepetitionNotUnique` is not a guarded search. This capsule consumes already-classified candidate key rows from the complete selected scope, excludes a row when any key cell is formally invalid, skips an all-empty tuple, and groups the remaining rows by exact typed composite key. Each row is identified by its complete repetition `Env`; an inner row numbered 1 beneath one outer row is not the same identity as inner row 1 beneath another. Optional empty components are valid tuple members and match optional empties; they make a duplicate OMISSION-typed. Fully present duplicates are VALUE-typed. Number keys compare through the kernel's scale-19 normalized value boundary, canonical token keys compare exactly, and component position and tuple length matter. Whole-scope evaluation retains each firing row's complete peer cluster in supplied scope order; that order is an internal deterministic representation, not a claim about future pointer order.

Only after that relation exists does the current row's leaf verdict enter the ordinary `And`/`Or` algebra. A guard-false peer still makes another row a duplicate even though the guarded row emits no message; deleting that peer therefore changes the remaining row's RNU truth. This is the central non-law separating RNU from a branch-local query or message-side recognizer.

The internal false/unknown boundary needs one explicit assurance caveat. Kernel 30.8.1 first observes a formally invalid key as three-valued UNKNOWN, then drops that row, and finally represents both “not a duplicate” and “not check-relevant” as one `FALSE_OR_UNKNOWN` result. The RNU leaf result and authored-message projection do not restore which silent verdict it was; separate formal diagnostics are orthogonal. Lean preserves the pre-collapse classification by mapping an invalid current key to `Verdict.unknown`, while a clean unique or all-empty row is `notFired`; a checked conformance case proves the internal distinction and their equal no-message projection. This is a chosen formal refinement, not a claim that external kernel output reveals a hidden UNKNOWN. a12-dmkits currently maps the missing RNU cache entry to leaf false and reconstructs whole-rule suppression separately, which remains observationally compatible.

Lean treatment: [`Semantics/RepetitionNotUnique.lean`](../A12Kernel/Semantics/RepetitionNotUnique.lean) owns typed key components, eligibility, exact tuple equality, the direct ordered duplicate-cluster representation, and per-row results. [`Proofs/RepetitionNotUnique.lean`](../A12Kernel/Proofs/RepetitionNotUnique.lean) characterizes numeric equality, exact cluster membership, the refined UNKNOWN and all-empty boundaries, firing iff cluster cardinality is at least two, complete cluster exposure, uniqueness of complete identities, and a distinct matching peer for every well-formed in-scope firing. [`Conformance/RepetitionNotUnique.lean`](../A12Kernel/Conformance/RepetitionNotUnique.lean) separates three-row clusters, equal local indices beneath different outer rows, optional-empty polarity, all-empty skipping, invalid exclusion, scale-19 Number equality versus token identity, positional tuples, branch-independent composition, and the externally collapsed silence boundary.

Limit: this resolved duplicate-relation capsule assumes a caller-supplied selected scope, topology, common key schema, and already-classified key cells after path expansion. It does not implement checked default/explicit `@From` resolution, partial relevance, one-RNU or negative/iteration/filter/parallel authoring legality, general String/Enumeration lowering, `Document` adaptation, error addresses, `referenced`/`fillToFix` peer pointers, whole-rule message assembly, protocol exposure, or an optimized cache. Pinned source and maintained dual-route differentials are sufficient to choose the relation and externally visible outcomes, but this repository retains no portable RNU observation, and invalid-versus-unique hidden truth is intrinsically collapsed by the kernel API. Correspondence remains `external evidence pending`. No spec correction, synchronization-ledger entry, support mechanism, evidence machinery, protocol, dependency, or generic uniqueness framework was added.

## LF46 — constructed-Date validity needs a reason-bearing non-value

> Date: 2026-07-20. Sections: §3, §6, §12. Basis: the corrected construction and validity clauses of [`spec/05-dates-and-time.md` §3](../spec/05-dates-and-time.md#3-constructing-dates-and-checking-validity); pinned kernel 30.8.1 construction, `Valid`/`Invalid`, strict-calendar, authoring, and empty-row-eligibility source at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66`; read-only a12-dmkits `DateStatus` triangulation and maintained `DateConstructionDiffTest`/`DateValidityLawsTest` controls at revision `883e0e22f87e22e3a7ff4529fd41ab87f331de8f`; red/green conformance plus classification-precedence and verdict-polarity mutations; independent semantic and Lean reviews; trusted availability, verdict-characterization, truth-complement, and non-value-reason laws.

Resolved three-part Date construction has four outcomes, not two. A complete triple that the calendar resolver accepts carries its `DateParts`; a complete rejected triple is unreal; at least one empty component with no malformed component is incomplete; and any formally unavailable component makes the construction unknown. Formal unavailability dominates emptiness. `Valid` fires VALUE only for the real case. `Invalid` does not merely test “not valid”: it stays silent for a real date, fires OMISSION for incomplete input, fires VALUE for a complete unreal date, and is UNKNOWN for formal unavailability.

This corrects one easy-to-miss distinction in the prose. At truth projection, `Valid` and `Invalid` are exact strong-Kleene complements, including the UNKNOWN/UNKNOWN arm. They are not mutually derivable as full A12 verdicts: incomplete and unreal both map to `Valid = notFired`, while `Invalid` must recover different polarities. A bare optional date therefore loses information required by a later authored message.

Lean treatment: [`Semantics/DateConstruction.lean`](../A12Kernel/Semantics/DateConstruction.lean) accepts only checked component availability plus a separate real/unreal decision for an all-present triple. It returns `real DateParts | incomplete | unreal | unknown`; the separate resolver is authoritative for calendar reality, so this layer neither substitutes the zone-free proleptic `CivilDate` predicate nor applies the later 1583 stored/computed-value floor. [`Proofs/DateConstruction.lean`](../A12Kernel/Proofs/DateConstruction.lean) characterizes incomplete and UNKNOWN classification, exact retention of supplied reality, each fired projection, truth complementation, and the nearest non-law: `Valid(incomplete) = Valid(unreal)` while `Invalid(incomplete) ≠ Invalid(unreal)`. [`Conformance/DateConstruction.lean`](../A12Kernel/Conformance/DateConstruction.lean) separates supplied real/unreal outcomes, all three empty positions, all-empty, supplied unreal behind emptiness, and unknown-plus-empty precedence.

Limit: the capsule begins after authoring, kind checking, integer conversion, component availability, and calendar reality resolution. It does not retain exact formal causes or implement raw/checked cells, numeric components, two- or four-part forms, Base Year, constant-only validity rejection, exact component bounds, row orchestration, target admission, checked lowering, or protocol exposure. Kernel source and maintained differentials establish the four classifications and selected outcomes, but only `Invalid` malformed handling is directly differentiated; malformed `Valid` and malformed-plus-empty precedence are source-established. The maintained all-empty differential fills an unrelated time field and therefore does not independently isolate a truly content-empty row; the kernel's explicit eligibility visitor establishes that gate. Concrete calendar resolution stays open because kernel 30.8.1 uses a model-zone-sensitive hybrid `GregorianCalendar`: for example, its mechanism rejects the skipped `Pacific/Apia` date 2011-12-30 and accepts UTC 1500-02-29 under the Julian side of the cutover, while the reusable zone-free proleptic `CivilDate` account gives the opposite classifications. This repository retains no portable Date observation, so correspondence remains `external evidence pending`. The canonical spec's complement wording was corrected under [`SPEC-2026-07-20-06`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-06--constructed-date-validity-complements-only-at-truth-projection), and the distinct concrete-calendar correction is queued under [`SPEC-2026-07-20-07`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-07--constructed-date-reality-uses-the-model-zone-legacy-hybrid-calendar). No support, evidence, protocol, harness, governance mechanism, dependency, or calendar framework was added.
