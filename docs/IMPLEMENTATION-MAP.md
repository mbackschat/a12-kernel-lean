# Lean implementation and evidence map

This is the live join from the read-only [`spec/`](../spec/) taxonomy to executable Lean definitions, trusted theorems and checked counterexamples, external kernel evidence, and exact support boundaries. It follows the map/ledger principle used by a12-dmkits' [`SEMANTICS-MAP.md`](../../a12-rulekit/docs/SEMANTICS-MAP.md), but answers a different question: what has this Lean theory actually captured and justified?

The target behavior version is kernel **30.8.1**. “Implemented” means the named Lean fragment executes. “Proved internally” means the theorem follows from the chosen Lean definitions. Neither means universal correspondence with the external kernel. A fragment remains **external evidence pending** until retained, versioned kernel observations for that fragment are portable and replayable in this repository.

## Taxonomy × Lean surfaces

| Clauses | Current Lean home | Internal assurance | External kernel evidence | Status and exact boundary |
|---|---|---|---|---|
| §1 truth and verdict algebra | [`Core.lean`](../A12Kernel/Core.lean), [`Proofs/Verdict.lean`](../A12Kernel/Proofs/Verdict.lean), [`Proofs/Information.lean`](../A12Kernel/Proofs/Information.lean) | Commutativity, associativity, idempotence, identities, absorbers, absorption, distributivity, and strong-Kleene information monotonicity; checked non-laws retain the exact unknown/polarity limits | Canonical §1 sources; no focused portable observation | **Proved internally; external evidence pending** for the finite `K`/`Verdict` algebras; no generic negation |
| §2 empty scalar comparisons and row gate | [`Semantics/FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`Elaboration/Flat.lean`](../A12Kernel/Elaboration/Flat.lean), [`Conformance/FlatValidation.lean`](../A12Kernel/Conformance/FlatValidation.lean), [`Conformance/Elaboration.lean`](../A12Kernel/Conformance/Elaboration.lean) | Executable Number/Boolean/Confirm empty laws, equality/inequality, polarity, row gates, checked surface lowering, and model-derived evaluation; clean-empty non-normalization in [`Proofs/Observation.lean`](../A12Kernel/Proofs/Observation.lean) | Retained kernel 30.8.1 projections cover empty Number with/without row content, empty Boolean/Confirm, and `FieldNotFilled`; [`EVIDENCE.md`](EVIDENCE.md) records capture and replay | **Implemented internally, partial; focused external observations replayed:** non-repeatable scalar field-to-literal equality/inequality and presence only; this is a comparison-local baseline, not a kind-wide empty law; operator/value matrix remains partial; see [`LF5`](LEAN-FINDINGS.md#lf5--empty-handling-is-a-layered-consuming-clause-policy-not-a-field-kind-function) |
| §3 formal checking and phase observation | [`Semantics/Observation.lean`](../A12Kernel/Semantics/Observation.lean), [`Proofs/Observation.lean`](../A12Kernel/Proofs/Observation.lean), [`Conformance/Observation.lean`](../A12Kernel/Conformance/Observation.lean) | `CheckedCell.WellFormed`, preservation under staged findings, validation-unknown/computation-poison phase laws, and the required-only exception | Retained malformed comparison and branch-combination cases check observable authored-message suppression/firing; the external output cannot distinguish internal `unknown` from `notFired` | **Implemented internally, partial; focused external observations replayed:** starts after scalar text decoding; the internal unknown/poison account still relies on source treatment and internal laws; contextual findings beyond requiredness remain outside the closed base pass |
| §4 required property | [`Semantics/Required.lean`](../A12Kernel/Semantics/Required.lean), [`Proofs/Required.lean`](../A12Kernel/Proofs/Required.lean), [`Conformance/Required.lean`](../A12Kernel/Conformance/Required.lean) | Independently stated source outcome versus generated-rule evaluation; base-before-annotation ordering; computation-observation preservation | Retained kernel projection covers an empty absolute/non-repeatable Number producing `mandatoryField`; [`RequirednessDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/RequirednessDiffTest.kt) remains broader provenance | **Implemented internally, partial; focused external observation replayed:** filled/malformed companions, parent-filled, repeatable-ancestor, index generation, and generated rule identity remain open |
| §5 numbers and decimals | [`Semantics/FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`Elaboration/Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean), [`Conformance/FlatValidation.lean`](../A12Kernel/Conformance/FlatValidation.lean), and [`Conformance/CorrelationElaboration.lean`](../A12Kernel/Conformance/CorrelationElaboration.lean) | Named comparison scale 19, direct positive/negative `HALF_UP` tie locks, literal scale exemption by constructor, and checked field-to-field equality/inequality scale agreement with ordering explicitly exempt | Kernel/interpreter [`PrecisionDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/PrecisionDiffTest.kt); retained static authoring observations separate unequal-scale `==` rejection from acceptance of the same operands under `<`; the legal whole-rule precision witness still requires arithmetic | **Implemented narrowly with focused static scale-legality evidence:** flat runtime remains field-to-literal equality/inequality, while the checked one-star surface admits the evidenced Number field-to-field scale boundary; arithmetic `MathContext(50)`, the legal precision witness, scale derivation, and target rounding are open |
| §6 dates and time | — | — | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §7 strings and patterns | — | — | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §8 enumerations | — | — | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §9 repetition and iteration | [`Semantics/Iteration.lean`](../A12Kernel/Semantics/Iteration.lean), [`Semantics/Correlation.lean`](../A12Kernel/Semantics/Correlation.lean), [`Elaboration/Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean), [`Proofs/Iteration.lean`](../A12Kernel/Proofs/Iteration.lean), [`Proofs/Correlation.lean`](../A12Kernel/Proofs/Correlation.lean), [`Proofs/CorrelationElaboration.lean`](../A12Kernel/Proofs/CorrelationElaboration.lean), [`Conformance/Iteration.lean`](../A12Kernel/Conformance/Iteration.lean), [`Conformance/Correlation.lean`](../A12Kernel/Conformance/Correlation.lean), and [`Conformance/CorrelationElaboration.lean`](../A12Kernel/Conformance/CorrelationElaboration.lean) | Exact ordered selector ↔ relation bridges; filter-before-consumer laws; captured-origin, self-match/exclusion, and observation-footprint results; checked one-star lowering with explicit group paths, path-derived repeatable ancestry, exact singleton scope, operator-specific scale legality, model-derived raw checking, fail-closed runtime references, and pre-evaluation 1-based/unique candidate validation. A theorem connects successful raw validation to the semantic context's `WellFormed` predicate. Checked-wrapper theorems eliminate structural certificates; they are not source-to-core semantic preservation | Seven uncorrelated and twelve captured-outer runtime cases replay through the iteration and correlation projections. Four static cases replay through [`correlation-elaboration-projection.json`](../evidence/kernel-30.8.1/correlation-elaboration-projection.json): all-outer rejection, unequal-scale `==` rejection, unequal-scale `<` acceptance, and sibling-group inner-reference rejection. The three mapped diagnostic classes and accepted control are bound to exact seeded models and captured drafts | **The admitted one-group runtime and one-star checked-lowering slices are implemented and externally separated:** Number `equal`/`notEqual`/`lessThan`, structural repetition comparison, conjunction, direct-child Number guard/value fields, and one correlated star. Nested false-singleton scope metadata is rejected while sibling repeatable groups remain valid. The evidence does not establish a general comparison matrix or universal cross-group rule. `Document` adaptation, filtered-result polarity, nested/multi-star, cross-group execution, general guards/consumers, optimization/refinement, computation, and partial validation remain open |
| §10 paths and references | [`Elaboration/Flat.lean`](../A12Kernel/Elaboration/Flat.lean), [`Elaboration/Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean), [`Proofs/Elaboration.lean`](../A12Kernel/Proofs/Elaboration.lean), [`Proofs/CorrelationElaboration.lean`](../A12Kernel/Proofs/CorrelationElaboration.lean), [`Conformance/Elaboration.lean`](../A12Kernel/Conformance/Elaboration.lean), and [`Conformance/CorrelationElaboration.lean`](../A12Kernel/Conformance/CorrelationElaboration.lean) | Model validation including field/group hierarchy separation and path-derived repeatable-scope coherence; order-independent unique ID/path lookup; shared parent walking; corrected bare declaring-group → flag-gated model-wide unique resolution; explicit repeatable-group path declarations; exact one-star binding; ambiguity, wrong group/scope, nested false-singleton scope metadata, and unsupported surface forms fail closed; unique declaration and raw-policy coherence theorems | The flat path bundle covers parent-relative, absolute, precedence, fallback, and ambiguity. The static correlation bundle retains exact group-qualified drafts for missing inner iteration, equality-scale mismatch, the ordering control, and a sibling-group reference; [`LF6`](LEAN-FINDINGS.md#lf6--bare-name-resolution-is-local-or-global-not-an-ancestor-walk) records the bare-name correction | **Implemented for two narrow structured subsets:** non-repeatable flat paths and one relative group-qualified direct-child star/correlation shape. Named labels, quoting, `RuleGroup`, semantic indices, concrete parser/renderer, nested/multi-star paths, and general repeatable lookup remain excluded; not every excluded or rejected syntax has a retained diagnostic |
| §11 computations | — | Requiredness proofs already preserve computation-phase observation, but no computation evaluator exists | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §12 validation and polarity | [`Core.lean`](../A12Kernel/Core.lean), [`Semantics/FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`Conformance/FlatValidation.lean`](../A12Kernel/Conformance/FlatValidation.lean) | VALUE/OMISSION precedence proved algebraically; malformed-sibling `And`/`Or` outcomes executable | Retained empty-substitution and `healthy Or/And malformed` projections replay focused VALUE/OMISSION or suppression behavior | **Implemented internally for the flat fragment; focused external observations replayed:** directional fillability, filters, iteration, aggregates, and partial relevance remain open |
| §13 message interpolation | — | — | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §14 custom conditions | Explicit pure-oracle boundary in [`Document.lean`](../A12Kernel/Document.lean) design only | No runtime semantics or oracle laws yet | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |

## Empty-handling coverage rule

Empty semantics is not complete at field-kind granularity. Coverage is clause-shaped and must record field kind, operator and operand position, enclosing consumer, selection/all-empty identity, model-derived field role, row eligibility, validation/computation phase, and observable polarity or store outcome. The current §2 capsule covers only direct Number/Boolean/Confirm field-to-literal comparison and presence behavior; its resolvers must not be reused as defaults for functions, aggregates, lookups, or computation. [`LF5`](LEAN-FINDINGS.md#lf5--empty-handling-is-a-layered-consuming-clause-policy-not-a-field-kind-function) records the verified counterexamples and architectural treatment.

## Current external-evidence gate

The retained kernel 30.8.1 bundle and four typed Lean projections now replay 42 focused observations through `lake test`: 36 runtime cases and six static authoring observations. Empty/branch behavior, absolute requiredness, explicit paths, the corrected two-tier bare-name mechanism, uncorrelated 5a selection, and the focused captured-outer runtime shapes retain their earlier evidence. The correlation-elaboration projection adds three exact rejections and one accepted neighboring control: all-outer `Having`, unequal-scale equality, the same unequal-scale operands under ordering, and a sibling-repeatable-group inner reference. It contains structured input but no expected code; the driver derives the complete code list from each external diagnostic artifact, binds an `en_US` conservative identifier projection to unique non-nested repeatable groups and exact Number metadata in the seeded model plus the complete seed/candidate draft and diagnostic provenance/routing, and maps only the three externally established Lean errors. `missingOuter` and every other unclassified result fail closed. The gate does not establish the full authored repeatable language, every operator/scale combination, universal cross-group legality, `Document` adaptation, polarity, nested/multi-star execution, optimization/refinement, computation, partial validation, or general consumers. The legal arithmetic precision witness remains open until arithmetic makes it authorable. New capture may run through the external a12-dmkits adapter harness only while keeping the local `a12-rulekit/` checkout's tracked and visible worktree clean; tracked corpus generation must target an ignored disposable workspace before portable artifacts are copied here. Internal examples remain distinct from differential evidence.

## Trusted theorem surface

[`A12Kernel/Proofs.lean`](../A12Kernel/Proofs.lean) is the trusted theorem root. [`scripts/check-lean-trust.sh`](../scripts/check-lean-trust.sh) verifies that it imports every proof module, that [`A12Kernel/TrustAudit.lean`](../A12Kernel/TrustAudit.lean) inventories every exported theorem, that the transitive semantics/elaboration/proof closure contains no banned escape hatch, and that only the reviewed standard Lean axioms appear.
