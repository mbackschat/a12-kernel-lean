# Lean implementation and evidence map

This is the live join from the read-only [`spec/`](../spec/) taxonomy to executable Lean definitions, trusted theorems and checked counterexamples, external kernel evidence, and exact support boundaries. It follows the map/ledger principle used by a12-dmkits' [`SEMANTICS-MAP.md`](../../a12-rulekit/docs/SEMANTICS-MAP.md), but answers a different question: what has this Lean theory actually captured and justified?

The target behavior version is kernel **30.8.1**. “Implemented” means the named Lean fragment executes. “Proved internally” means the theorem follows from the chosen Lean definitions. Neither means universal correspondence with the external kernel. A fragment remains **external evidence pending** until retained, versioned kernel observations for that fragment are portable and replayable in this repository.

## Taxonomy × Lean surfaces

| Clauses | Current Lean home | Internal assurance | External kernel evidence | Status and exact boundary |
|---|---|---|---|---|
| §1 truth and verdict algebra | [`Core.lean`](../A12Kernel/Core.lean), [`Proofs/Verdict.lean`](../A12Kernel/Proofs/Verdict.lean), [`Proofs/Information.lean`](../A12Kernel/Proofs/Information.lean) | Commutativity, associativity, idempotence, identities, absorbers, absorption, distributivity, and strong-Kleene information monotonicity; checked non-laws retain the exact unknown/polarity limits | Canonical §1 sources; no focused portable observation | **Proved internally; external evidence pending** for the finite `K`/`Verdict` algebras; no generic negation |
| §2 empty scalar comparisons and row gate | [`Semantics/NumericComparison.lean`](../A12Kernel/Semantics/NumericComparison.lean), [`Semantics/FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`Elaboration/Flat.lean`](../A12Kernel/Elaboration/Flat.lean), [`Proofs/NumericComparison.lean`](../A12Kernel/Proofs/NumericComparison.lean), [`Proofs/StringLength.lean`](../A12Kernel/Proofs/StringLength.lean), [`Conformance/FlatValidation.lean`](../A12Kernel/Conformance/FlatValidation.lean), [`Conformance/StringLength.lean`](../A12Kernel/Conformance/StringLength.lean), and [`Conformance/Elaboration.lean`](../A12Kernel/Conformance/Elaboration.lean) | Executable Number/Boolean/Confirm empty laws, direct String equality, `Length <`/`>=`, directional numeric polarity, row gates, checked surface lowering, and model-derived evaluation; parsed-empty String normalization plus operator-distinction and directional-fillability laws | Existing retained projections cover empty Number with/without row content, empty Boolean/Confirm, and `FieldNotFilled`; a separate six-case operator-sensitive capture covers unsigned/signed empty Number inequality, filled-zero controls, direct String equality, `Length`, and the empty-row gate. The frozen [`flat-validation-empty-logic-v1` descriptor](../reference/flat-validation-empty-logic-v1.capability.json) remains the original eight-case 0.2.0 shipment; current [`flat-validation-empty-logic-v2`](../reference/flat-validation-empty-logic-v2.conformance.json) preserves those cases and adds the directional witness | **Implemented internally, partial; focused external observations replayed:** non-repeatable Number/Boolean/Confirm equality/inequality and presence, direct String equality only, and String `Length <`/`>=`. String presence/requiredness and every other String comparison/function are rejected or unimplemented. This is a consuming-clause baseline, not a kind-wide empty law; see [`LF5`](LEAN-FINDINGS.md#lf5--empty-handling-is-a-layered-consuming-clause-policy-not-a-field-kind-function) and [`LF10`](LEAN-FINDINGS.md#lf10--numeric-polarity-needs-directional-fillability-not-a-given-bit) |
| §3 formal checking and phase observation | [`Semantics/Observation.lean`](../A12Kernel/Semantics/Observation.lean), [`Proofs/Observation.lean`](../A12Kernel/Proofs/Observation.lean), [`Conformance/Observation.lean`](../A12Kernel/Conformance/Observation.lean) | `CheckedCell.WellFormed`, preservation under staged findings, validation-unknown/computation-poison phase laws, and the required-only exception | Retained malformed comparison and branch-combination cases check observable authored-message suppression/firing; the external output cannot distinguish internal `unknown` from `notFired` | **Implemented internally, partial; focused external observations replayed:** starts after scalar text decoding; the internal unknown/poison account still relies on source treatment and internal laws; contextual findings beyond requiredness remain outside the closed base pass |
| §4 required property | [`Semantics/Required.lean`](../A12Kernel/Semantics/Required.lean), [`Proofs/Required.lean`](../A12Kernel/Proofs/Required.lean), [`Conformance/Required.lean`](../A12Kernel/Conformance/Required.lean) | Independently stated source outcome versus generated-rule evaluation; base-before-annotation ordering; computation-observation preservation | Retained kernel projection covers an empty absolute/non-repeatable Number producing `mandatoryField`; [`RequirednessDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/RequirednessDiffTest.kt) remains broader provenance | **Implemented internally, partial; focused external observation replayed:** filled/malformed companions, parent-filled, repeatable-ancestor, index generation, and generated rule identity remain open |
| §5 numbers and decimals | [`Semantics/NumericComparison.lean`](../A12Kernel/Semantics/NumericComparison.lean), [`Semantics/FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`Elaboration/Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean), [`Proofs/NumericComparison.lean`](../A12Kernel/Proofs/NumericComparison.lean), [`Conformance/FlatValidation.lean`](../A12Kernel/Conformance/FlatValidation.lean), and [`Conformance/CorrelationElaboration.lean`](../A12Kernel/Conformance/CorrelationElaboration.lean) | Shared comparison scale 19, direct positive/negative `HALF_UP` tie locks, directional `canGrow`/`canShrink` polarity for fixed-right comparison, literal scale exemption by constructor, and checked field-to-field equality/inequality scale agreement with ordering explicitly exempt. The generic grow-only inequality theorem and two distinct negative-literal locks prevent a return to one-bit substituted polarity | The six-case operator-sensitive capture separates unsigned empty `0 != -1` VALUE, unsigned empty `0 != 1` OMISSION, signed empty `0 != -1` OMISSION, and their filled-zero VALUE controls. [`PrecisionDiffTest`](../../a12-rulekit/adapter/src/test/kotlin/io/github/mbackschat/a12/dm/adapter/laws/PrecisionDiffTest.kt) plus retained static authoring observations cover selected rounding and scale-legality boundaries | **Implemented narrowly with focused runtime and static evidence:** the shared `NumericComparisonOp` intentionally admits only `equal`, `notEqual`, `less`, and `greaterEqual`, exactly the operators needed by the closed Number and String-Length consumers. Arithmetic `MathContext(50)`, general ordering surfaces, operand-pair fillability, scale derivation, and target rounding remain open |
| §6 dates and time | — | — | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §7 strings and patterns | [`Semantics/FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`Elaboration/Flat.lean`](../A12Kernel/Elaboration/Flat.lean), [`Proofs/StringLength.lean`](../A12Kernel/Proofs/StringLength.lean), and [`Conformance/StringLength.lean`](../A12Kernel/Conformance/StringLength.lean) | Direct equality and `Length <`/`>=` have separate consuming clauses; parsed empty String normalizes to the same clean-empty observation; `Length` counts UTF-16 code units; the same-empty operator distinction and both directional Length polarities are proved | Four of the six operator-sensitive cases separate empty-content, empty-row, `"ABC"`, and six-character direct-equality/Length outcomes across both kernel strategies and a12-dmkits. The retained ASCII strings do not externally establish the astral UTF-16 boundary | **Implemented narrowly with focused external observations:** direct String equality only; direct inequality, String presence/requiredness, patterns, concatenation, coercion, lists, and every other String function remain rejected or open. `Length` admits only `<` and `>=`; UTF-16 astral behavior is internally locked but externally pending. The public normalized protocol and consumer capability have not been expanded to String |
| §8 enumerations | — | — | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §9 repetition and iteration | [`Semantics/Iteration.lean`](../A12Kernel/Semantics/Iteration.lean), [`Semantics/Correlation.lean`](../A12Kernel/Semantics/Correlation.lean), [`Elaboration/Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean), [`Proofs/Iteration.lean`](../A12Kernel/Proofs/Iteration.lean), [`Proofs/Correlation.lean`](../A12Kernel/Proofs/Correlation.lean), [`Proofs/CorrelationElaboration.lean`](../A12Kernel/Proofs/CorrelationElaboration.lean), [`Conformance/Iteration.lean`](../A12Kernel/Conformance/Iteration.lean), [`Conformance/Correlation.lean`](../A12Kernel/Conformance/Correlation.lean), and [`Conformance/CorrelationElaboration.lean`](../A12Kernel/Conformance/CorrelationElaboration.lean) | Exact ordered selector ↔ relation bridges; filter-before-consumer laws; captured-origin, self-match/exclusion, and observation-footprint results; checked one-star lowering with explicit group paths, path-derived repeatable ancestry, exact singleton scope, operator-specific scale legality, model-derived raw checking, fail-closed runtime references, and pre-evaluation 1-based/unique candidate validation. A theorem connects successful raw validation to the semantic context's `WellFormed` predicate. Checked-wrapper theorems eliminate structural certificates; they are not source-to-core semantic preservation | The retained uncorrelated, captured-outer runtime, and static authoring projections separate selection, origin, malformed/empty filtering, consumer observation order, comparison and scale boundaries, and neighboring rejection classes. The normalized handover indexes every retained case but still uses reviewed manual fixture/evidence association; [`EVIDENCE.md`](EVIDENCE.md) owns the exact inventory and observable support | **The admitted one-group runtime, checked-lowering, and normalized firing-row slices are implemented and externally separated:** Number `equal`/`notEqual`/`lessThan`, structural repetition comparison, conjunction, direct-child Number guard/value fields, and one correlated star. Protocol candidates are narrowed to non-empty contiguous `1..n`; only firing membership is exposed. Retained parsed runtime Number cells are non-negative integers, so general exact decimals and negatives remain externally pending even though the Lean account executes them. `Document` adaptation, parent-relative/bare correlation paths, filtered-result polarity, nested/multi-star, cross-group execution, general guards/consumers, optimization/refinement, computation, and partial validation remain open |
| §10 paths and references | [`Elaboration/Flat.lean`](../A12Kernel/Elaboration/Flat.lean), [`Elaboration/Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean), [`Proofs/Elaboration.lean`](../A12Kernel/Proofs/Elaboration.lean), [`Proofs/CorrelationElaboration.lean`](../A12Kernel/Proofs/CorrelationElaboration.lean), [`Conformance/Elaboration.lean`](../A12Kernel/Conformance/Elaboration.lean), and [`Conformance/CorrelationElaboration.lean`](../A12Kernel/Conformance/CorrelationElaboration.lean) | Model validation including field/group hierarchy separation and path-derived repeatable-scope coherence; order-independent unique ID/path lookup; shared parent walking; corrected bare declaring-group → flag-gated model-wide unique resolution; explicit repeatable-group path declarations; exact one-star binding; ambiguity, wrong group/scope, nested false-singleton scope metadata, and unsupported surface forms fail closed; unique declaration and raw-policy coherence theorems | The flat path bundle covers parent-relative, absolute, precedence, fallback, and ambiguity. The static correlation bundle retains exact group-qualified drafts for missing inner iteration, equality-scale mismatch, the ordering control, and a sibling-group reference; [`LF6`](LEAN-FINDINGS.md#lf6--bare-name-resolution-is-local-or-global-not-an-ancestor-walk) records the bare-name correction | **Implemented for two narrow structured subsets:** non-repeatable flat paths and one absolute-or-direct-child-relative group-qualified star/correlation shape. Parent-relative and bare forms remain outside the public correlation operation. Named labels, quoting, `RuleGroup`, semantic indices, concrete parser/renderer, nested/multi-star paths, and general repeatable lookup remain excluded; not every excluded or rejected syntax has a retained diagnostic |
| §11 computations | — | Requiredness proofs already preserve computation-phase observation, but no computation evaluator exists | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §12 validation and polarity | [`Core.lean`](../A12Kernel/Core.lean), [`Semantics/NumericComparison.lean`](../A12Kernel/Semantics/NumericComparison.lean), [`Semantics/FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`Proofs/NumericComparison.lean`](../A12Kernel/Proofs/NumericComparison.lean), [`Proofs/StringLength.lean`](../A12Kernel/Proofs/StringLength.lean), [`Conformance/FlatValidation.lean`](../A12Kernel/Conformance/FlatValidation.lean), and [`Conformance/StringLength.lean`](../A12Kernel/Conformance/StringLength.lean) | VALUE/OMISSION precedence proved algebraically; malformed-sibling `And`/`Or` outcomes executable; fixed, grow-only, and sign-aware Number/Length fillability determines polarity by the direction that could falsify a firing | Existing retained projections cover empty substitution and `healthy Or/And malformed`; the operator-sensitive capture separates VALUE from OMISSION while holding the substituted zero and comparison family fixed | **Implemented internally for the named flat clauses; focused external observations replayed:** directional fillability is closed for fixed-right `equal`/`notEqual` and String `Length <`/`>=`, not for general expressions, both-fillable operands, dates, strings/concats, filters, iteration, aggregates, or partial relevance |
| §13 message interpolation | — | — | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |
| §14 custom conditions | Explicit pure-oracle boundary in [`Document.lean`](../A12Kernel/Document.lean) design only | No runtime semantics or oracle laws yet | Indexed in [`SOURCES.md`](SOURCES.md) | **Open** |

## Reference-process exposure

The `a12-kernel-reference` executable exposes two disjoint intersections through the normalized [`PROTOCOL.md`](PROTOCOL.md) contract: the public §2/§3/§5/§10/§12 flat slice and the named §9 one-group captured-outer slice. [`Reference/Protocol.lean`](../A12Kernel/Reference/Protocol.lean) decodes bounded operation-specific models, paths, conditions or correlated `Having`, sparse flat or row-addressed cells, and row gates/candidates. [`Reference/Evaluator.lean`](../A12Kernel/Reference/Evaluator.lean) routes every admitted request through the corresponding existing checked elaborator and evaluator. [`Reference/Support.lean`](../A12Kernel/Reference/Support.lean) supplies the finite runtime classifiers and generates the current schema-2 per-operation positive support, external-evidence boundaries, and diagnostic declaration mirrored in [`supported-fragment-v2.json`](../reference/supported-fragment-v2.json). [`Reference/Lineage.lean`](../A12Kernel/Reference/Lineage.lean) preserves historical 0.2.0 and current 0.3.0 identities, the OMISSION-versus-VALUE separating observation, and frozen v1 digests in [`reference-semantics-lineage-v1.json`](../reference/reference-semantics-lineage-v1.json). Requests have no per-request semantics selector; the exact binary's `--manifest` plus a binary/release digest identifies the account. The internal §7 String/Length capsule is deliberately absent from this protocol until a separate public capability closes its transport, diagnostics, and support manifest.

This process surface adds accessibility and fail-closed protocol assurance, not new kernel correspondence. Its accepted semantic clauses inherit the external-evidence statuses in the table above, while [`A12Kernel/ReferenceProcessTestMain.lean`](../A12Kernel/ReferenceProcessTestMain.lean) locks transport behavior, deterministic output, diagnostics, exit codes, sample fixtures, current-manifest agreement, lineage/digest preservation, historical suite structure, and current-suite controls. [`CandidateConformanceMain.lean`](../A12Kernel/CandidateConformanceMain.lean) runs one selected suite but does not query a candidate manifest; neither suite execution nor agreement with Lean transfers proofs or expands evidence. [`Evidence/FlatProtocolBridge.lean`](../A12Kernel/Evidence/FlatProtocolBridge.lean) owns the frozen 0.2.0 typed flat descriptor, derives its finite request/response fixtures from retained replay inputs and an explicit historical verdict table without invoking today's decoder/evaluator, preserves per-case external-support versus Lean-response classification, and generates separate source-maintainer mutation planning. Its `--check` remains active while historical `--write` is rejected. Exact handover material and closure gaps live in the frozen [flat](IMPLEMENTER-KIT-FLAT-EMPTY-LOGIC.md) and [correlation](IMPLEMENTER-KIT-CORRELATION.md) kits. A development descriptor is not release closure, and the protocol's exclusion list does not claim to enumerate every unsupported A12 construct.

The isolated Rust exercise is downstream knowledge-transport history, not added Lean semantic coverage or kernel evidence. The [`flat kit`](IMPLEMENTER-KIT-FLAT-EMPTY-LOGIC.md#cold-test-and-qualification-outcome-2026-07-1415) owns its exact implementation and accepted mutation-qualification outcome and limits. A separate bounded generated-differential lane defines and self-tests 52 non-fixture requests inside the same admitted positive flat fragment; its [reviewed profile](../reference/flat-validation-empty-logic-v1.generated-differential-v1.json) pins source `2cdc37746737d83241f91cd89fa0b56c99c2d47a`, candidate `d213005b3972c2acd8f67e87f523a923d69f6a54`, exact resource bounds, and the finite verdict projection. The retained [result](../qualification/flat-validation-empty-logic-v1-rust-v1/generated-differential-v1.RESULT.json) records 52/52 agreement with identical 14/11/13/14 verdict distributions and no process or integrity failure. It adds no semantic clause, support declaration, proof, kernel evidence, or correctness claim outside the profile.

## Empty-handling coverage rule

Empty semantics is not complete at field-kind granularity. Coverage is clause-shaped and must record field kind, operator and operand position, enclosing consumer, selection/all-empty identity, model-derived field role, row eligibility, validation/computation phase, directional fillability, and observable polarity or store outcome. The current §2/§7 capsule covers direct Number/Boolean/Confirm field-to-literal comparison and presence plus direct String equality and String `Length <`/`>=`; its resolvers must not be reused as defaults for other functions, aggregates, lookups, or computation. A generic given/substituted bit is also not a valid numeric default: [`LF5`](LEAN-FINDINGS.md#lf5--empty-handling-is-a-layered-consuming-clause-policy-not-a-field-kind-function) records the consuming-clause rule, while [`LF10`](LEAN-FINDINGS.md#lf10--numeric-polarity-needs-directional-fillability-not-a-given-bit) records the directional correction.

## Current external-evidence gate

The table above is the sole clause-level classification of which implemented choices are externally replayed, partially anchored, or still pending. [`EVIDENCE.md`](EVIDENCE.md) owns the exact retained inventory, projection mechanics, provenance, observable support, and claim limits; capability kits own their derived finite case classifications. Cross-cutting open obligations remain the manually associated correlation projection-to-protocol bridge, the legal arithmetic precision witness, and every broader fragment excluded in the table. Internal conformance examples remain distinct from differential evidence, and a generated shipment never creates a new kernel observation.

## Trusted theorem surface

[`A12Kernel/Proofs.lean`](../A12Kernel/Proofs.lean) is the trusted theorem root. [`scripts/check-lean-trust.sh`](../scripts/check-lean-trust.sh) verifies that it imports every proof module, preserves the named human-readable theorem registry in [`A12Kernel/TrustAudit.lean`](../A12Kernel/TrustAudit.lean), rejects forbidden dependency directions, and uses [`A12Kernel/Trust/Environment.lean`](../A12Kernel/Trust/Environment.lean) to inspect every elaborated declaration in the trusted project modules. That environment audit rejects project axioms, unsafe or unclassified opaque/partial definitions, compiler/foreign substitutions, and every axiom dependency except `propext`, `Classical.choice`, and `Quot.sound`; conformance remains a separate nontrusted executable-check lane.
