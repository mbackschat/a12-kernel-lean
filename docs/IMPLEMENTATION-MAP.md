# Lean implementation and evidence map

This is the sole live map from the project-owned [`spec/`](../spec/) taxonomy to Lean owners, implemented boundaries, proof/counterexample status, and external assurance. Open obligations are linked from [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) rather than copied here; exact retained observations are owned by [`EVIDENCE.md`](EVIDENCE.md); source provenance is owned by [`SOURCES.md`](SOURCES.md).

The target is kernel **30.8.1**.

## Query contract

Start from the matching bounded record with `rg -n '^<a id="cap-' docs/IMPLEMENTATION-MAP.md`; where no record exists, use the legacy `§n` row or named cross-clause mechanism.

Each record or row answers four questions directly: what exists, where its primary owner lives, how certain the stated slice is, and what remains. A current answer must not require Git archaeology; Git owns chronology, not live state. This map does not narrate capsule history, source-audit chronology, gate counts, or immediate next work.

Coverage uses six independent dimensions:

- **E — executable:** checked definitions and separating executable cases exist.
- **P — proof-closed:** the named boundary has its required proof spine and nearest useful non-law.
- **L — Kernel-locked:** a maintained a12-dmkits test executes the real kernel for the named behavior. `—` means not yet assessed, not “no lock exists.”
- **C — Kernel-calibrated:** retained versioned kernel observations replay through a typed local projection.
- **X — publicly exposed:** the normalized reference process exposes the named behavior.
- **Q — consumer-qualified:** a bounded consumer probe exercised the representation for its named decision. The letter itself never carries the probe class: read the row or record prose for it, and treat the probe as in-context or artifact-only unless that prose names an external cold implementation. `Q` is never cold-consumer qualification, shipment research closure, or release support; [`IMPLEMENTER-GUIDE.md`](IMPLEMENTER-GUIDE.md#consumer-probes-before-shipment) owns those later stages.

`closed` applies only to the boundary stated in that row. `partial` means at least one named slice is implemented while the canonical clause remains open. `none` means no current capability in that dimension.

## Coverage summary

<a id="coverage-dimensions"></a>
| Boundary | E | P | L | C | X | Q |
|---|---|---|---|---|---|---|
| Immutable checked document | closed | closed | — | none | none | closed |
| §1 truth and verdict algebra | closed | closed | — | none | none | none |
| §2 empty scalar comparisons and row gate | partial | partial | partial | partial | partial | none |
| §3 formal checking and phase observation | partial | partial | partial | partial | partial | none |
| §4 required property | partial | partial | partial | partial | none | none |
| §5 numbers and decimals | partial | partial | partial | partial | partial | partial |
| §6 dates and time | partial | partial | partial | none | none | partial |
| §7 strings and patterns | partial | partial | partial | partial | none | partial |
| §8 enumerations and value lists | partial | partial | partial | none | none | partial |
| §9 repetition and iteration | partial | partial | partial | partial | partial | partial |
| §10 paths and references | partial | partial | partial | partial | partial | partial |
| §11 calculations and formal checking | partial | partial | partial | partial | none | partial |
| §12 validation and polarity | partial | partial | partial | partial | partial | partial |
| §13 message interpolation | partial | partial | partial | partial | none | partial |
| §14 custom conditions | partial | partial | — | none | none | partial |

The six columns must not be collapsed into one completion percentage. Internal proof does not establish kernel correspondence; an upstream kernel-executing lock is not locally replayable calibration; finite calibration does not prove the whole clause; public exposure and consumer qualification are separate product decisions.

### Coverage denominator

<a id="the-capability-denominator--mined-from-the-kernel-not-from-spec"></a>
<a id="the-capability-denominator-mined-from-the-kernel-not-from-spec"></a>

The eventual-completeness denominator is mined from the Kernel rather than inferred from the current spec. Runtime observables follow a12-dmkits’ guard-checked [`RT-SEMANTICS-LEDGER`](../../a12-rulekit/docs/RT-SEMANTICS-LEDGER.md). Required static legality follows the Kernel’s `MVK_`-prefixed diagnostic constants: the complete 30.8.1 inventory contains 280 constants across eight enums, while the semantic center’s current in-scope static-legality denominator is approximately 57 after excluding bilingual parser, H-model, and on-the-fly rule-testing scopes. The 57 still includes the code-generation `NoMetaModelCheckErrorType` region that the peer’s model-authoring ledger does not classify. These figures bound inventory work; they are not a completion percentage and must be re-mined on a Kernel-version change.

### Known Kernel locks and deliberate non-locks

<a id="upstream-kernel-locked-inventory-composition-triage-2026-07-25"></a>

This compact index preserves the exact known assurance distinctions that the clause-level `L` column cannot express while its population remains incomplete.

| Seam | Current assurance and receipt |
|---|---|
| Malformed `Having` consumer drop (`LF7`) | Locally calibrated by the retained validation observation; not an upstream lock. |
| Computation precondition skip before poison (`LF19`) | Kernel-locked by `CascadeClearedDependencyDiffTest` and `ComputationPreconditionDiffTest`. |
| Ordered computation connectives (`LF21`) | Kernel-locked by `ComputePoisonReadDiffTest`. |
| Partial-validation filtered-rule skip (`LF23`) | Source-closed by `rulesDir/CodeGenRuleDefinition.st` in all three dialects; the enclosing `CodeGenRules.st` is not the deciding locus. |
| One-pass division lowering (`LF32`) | Kernel-locked by the maintained controls accepted under `SPEC-2026-07-19-08`, `SPEC-2026-07-19-09`, and `SPEC-2026-07-19-14`. |
| Numeric receiver/argument order (`LF35`) | Source-closed by `CompositeOperation.getFeldOperationCodeJava`; no target-language branch exists at that lowering locus. |
| Branch-independent RNU relation (`LF45`) | Kernel-locked by `RepetitionNotUniqueGuardedDiffTest`. |
| Prefix-sensitive `FirstFilledValue` (`LF48`) | Kernel-locked by `FirstFilledValueOmittedTailDiffTest`; this corrected the earlier source-only account. |
| Computation fill-quantifier scan order (`LF50`) | Kernel-locked by `ComputePoisonReadDiffTest`, `BareGroupComputeExactnessDiffTest`, and the separating `ComputationFillQuantifierRestartDiffTest`. |
| Semantic-index and parallel-index route timing (`LF53`) | Kernel-locked on the ordinary route by `IndexedReadSuppressionDiffTest` and `SemanticIndexMatchedInvalidCellDiffTest`, and on the parallel route by `ParallelIterationIndexMarkingDiffTest`. |
| Numeric no-fit prefix order (`LF64`) | Source-confirmed and locally separated, but deliberately not Kernel-locked; this is an assurance absence, not an executable-conformance debt. |
| Traversal order versus cause blindness (`LF69`) | Kernel-locked only for the observable prefix half under accepted `SPEC-2026-07-23-10` and `SPEC-2026-07-23-11`; cause identity is erased publicly. |
| Value-list `AtLeastOne`/`No` values-first behavior | Kernel-locked by `ValueListQuantifierOrderDiffTest` S4/S5 under accepted `SPEC-2026-07-23-16`. |
| Value-list `NotAll` prepass | No authorable input separates the routes, so no Kernel lock is possible or owed; the observable result is locked under accepted `SPEC-2026-07-25-01`. |
| Kept-successor filter lookahead | Kernel-locked by `FirstFilledValueKeptSuccessorDiffTest` S1–S4. |
| Groovy condition-line splitting | Kernel-locked by `ConditionLineSplitDiffTest` at a12-dmkits `29b3ef53`, including proof that the split route engaged. |
| Groovy calculation-closure splitting | Kernel-locked by `CalculationClosureSplitDiffTest` at a12-dmkits `dda6ba01`, including winners in later chunks and no-winner clearing. |

The negative rows are intentionally different: “not yet assessed,” “source-confirmed but not Kernel-locked,” and “no observable discriminator exists” must not be collapsed into one missing-evidence state. Exact retained observation counts and replay identities remain solely in [`EVIDENCE.md`](EVIDENCE.md) and [`TESTING.md`](TESTING.md).

<a id="evidence-snapshot"></a>

## Capability index

<a id="owners"></a>
<a id="implemented"></a>
<a id="coverage"></a>
<a id="evidence"></a>
<a id="excludednext"></a>
<a id="excluded-boundary-and-gap-links"></a>

This is the operational lookup layer. Search the exact capability or type, then read across one row:

- **Exists** states the implemented semantic boundary, not a historical milestone.
- **Owner** is the narrowest primary module or directory; matching proof and conformance owners use the same basename unless named separately.
- **Assurance** reports internal executable/proof status first, then external status using `L`, `C`, `X`, and `Q`. “Source-grounded” and “peer-triangulated” are intentionally not `L` or `C`.
- **Remains** links the live open owner or states that the named slice is closed. It never repeats that gap’s checklist.

For an assurance drill, use the row’s linked owner, then the matching [`Proofs/`](../A12Kernel/Proofs/) and [`Conformance/`](../A12Kernel/Conformance/) family; irregular proof owners are linked in the row. [`Proofs.lean`](../A12Kernel/Proofs.lean) is the theorem inventory, [`Conformance.lean`](../A12Kernel/Conformance.lean) is the executable separator inventory, and [`TrustAudit.lean`](../A12Kernel/TrustAudit.lean) is the exhaustive trusted-theorem registry. Exact retained Kernel observations remain in [`EVIDENCE.md`](EVIDENCE.md), while upstream Kernel locks and source-grounded claims route through [`SOURCES.md`](SOURCES.md).

### Immutable checked-document construction

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Physical document topology | Finite model-relative rows and placed raw cells; contiguous 1-based repetition identity; scope, proof-retained duplicate-placement, raw-classification, and over-repetition checks | [`Document.lean`](../A12Kernel/Document.lean), [checked-document elaboration](../A12Kernel/Elaboration/CheckedDocument.lean), [checked-document proofs](../A12Kernel/Proofs/CheckedDocument.lean) | E/P closed; L/C none; Q Analyze/Explain probe | SG1 closed; family ingestion stays with its clause |
| Checked addressed reads | Absent, present-empty, admitted value, formal finding, and structural address failure remain distinct; prepared field checks are cached | [`CheckedDocument.lean`](../A12Kernel/Elaboration/CheckedDocument.lean), [`Observation.lean`](../A12Kernel/Semantics/Observation.lean) | E/P closed; selected C/X through flat evidence | Wider ingestion: [SG5–SG8](SEMANTICS-GAPS.md) |
| Preliminary index findings | Model-derived required/unique index findings are staged over immutable topology without mutating the document | [`CheckedIndexPreliminary.lean`](../A12Kernel/Elaboration/CheckedIndexPreliminary.lean) | E/P closed; source-grounded; C none | Wider index orchestration: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Required/group projections | Checked absolute requiredness and group-presence input consume the same checked document while retaining separate authored-validation views | [`CheckedRequired.lean`](../A12Kernel/Elaboration/CheckedRequired.lean), [`CheckedGroupPresence.lean`](../A12Kernel/Elaboration/CheckedGroupPresence.lean) | E/P closed for admitted slice; selected C | Wider group terminals: [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |

## Taxonomy by clause

### §1 — truth and verdict algebra

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Three-valued truth | Closed `K` algebra with ordered `and`/`or` and no invented generic negation | [`Core.lean`](../A12Kernel/Core.lean) | E/P exhaustive; L/C/X/Q none | Closed for this algebra |
| Validation verdict | `valid`/`invalid`/`unknown` result algebra, polarity combination, and exact laws | [`Core.lean`](../A12Kernel/Core.lean), [`Verdict.lean`](../A12Kernel/Proofs/Verdict.lean) | E/P exhaustive; L/C/X/Q none | Closed for this algebra |
| Shared condition-tree control flow | Parametric tree shape with family-specific leaves and decisive left-to-right folds | [`Condition.lean`](../A12Kernel/Semantics/Condition.lean) | E/P closed for stated folds; computation ordering has L locks in the inventory above | New leaf families remain clause-owned |

### §2 — empty scalar comparisons and row gate

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Boolean/Confirm/String scalar equality | Nonrepeatable equality/inequality with explicit no-value behavior; stored Boolean/Confirm text admits only the fixed lowercase tokens, ignores declared `@NotInD` display tokens, retains exact formal causes/codes, and is certified against its stored text by checked-document construction | [`ScalarText.lean`](../A12Kernel/Semantics/ScalarText.lean), [`ScalarEquality.lean`](../A12Kernel/Semantics/ScalarEquality.lean), [`CheckedDocument.lean`](../A12Kernel/Elaboration/CheckedDocument.lean), [`Flat/Condition/`](../A12Kernel/Elaboration/Flat/Condition/) | E/P closed for ordinary stored Boolean/Confirm and admitted scalar values; upstream L for token ingestion; selected C/X for Boolean/Confirm | Wider String policy: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) |
| Number comparison and empty polarity | All six comparisons, scale-19 normalization, and directional fillability for empty operands | [`NumericComparison.lean`](../A12Kernel/Semantics/NumericComparison.lean), [`NumericValidation/`](../A12Kernel/Elaboration/NumericValidation/) | E/P closed for admitted routes; selected C/X; upstream L partial | Wider authoring: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) |
| String length comparison | UTF-16 length projected to Number comparison with directional empty behavior | [`String.lean`](../A12Kernel/Semantics/String.lean), [`StringLength.lean`](../A12Kernel/Proofs/StringLength.lean) | E/P closed; source-grounded; C/X none | Wider Unicode/pattern boundary: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) |
| Temporal comparison | Six-way Date/Time/DateTime comparison over admitted literal, field, `Today`, `Now`, and Base-Year sources | [`DateComparison.lean`](../A12Kernel/Semantics/DateComparison.lean), [`TimeComparison.lean`](../A12Kernel/Semantics/TimeComparison.lean), [`DateTimeComparison.lean`](../A12Kernel/Semantics/DateTimeComparison.lean) | E/P closed for stated sources; peer-triangulated; C/X none | Wider operands and zones: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) |
| Presence and row content | Direct presence for admitted scalar kinds plus the structural content-row gate | [`FlatValidation.lean`](../A12Kernel/Semantics/FlatValidation.lean), [`GroupPresence.lean`](../A12Kernel/Semantics/GroupPresence.lean) | E/P closed for admitted slice; selected C/X | Repeatable/group breadth: [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |

### §3 — formal checking and phase observation

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Phase projection | One checked cell projects formal invalidity to validation UNKNOWN and computation poison while preserving exact cause | [`Cell.lean`](../A12Kernel/Cell.lean), [`Observation.lean`](../A12Kernel/Semantics/Observation.lean) | E/P closed; selected C through flat evidence | New causes must extend the shared cell owner |
| Raw String ingestion | Absent/present-empty/decoded String, CRLF normalization, declared pattern hooks, and prepared context | [`RawString.lean`](../A12Kernel/Elaboration/RawString.lean), [`StringContext.lean`](../A12Kernel/Elaboration/StringContext.lean) | E/P closed for admitted profile; selected C; arbitrary patterns injected | Complete String/custom ingestion: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) |
| Group-list diagnostics | Condition-level group-list/count owners retain duplicate, overlap, root, wildcard, multiplicity, and unknown classes. A separate rule-owned static projection uses the error-field declaration, not the rule group, for the exact one-level ordinary-path matrix: scalar `GroupFilled`, sole `AtLeastOneGroupFilled`, authored `AllGroupsFilled(repeatable, fixed)`, and sole/paired `NumberOfFilledGroups(...) > 0`. The first operand must be a non-root ordinary path to the exact repeatable declaration; `RuleGroup`, a repeatable root, a fixed descendant sharing the level, a root peer, or an overlapping fixed peer remains unmapped. Each represented row returns admitted, the exact diagnostic, or explicitly unmapped, and constructs no runtime condition for newly admitted shapes. The shared whole-rule projector maps the already-derived error-field-reference and negative-iteration assembly errors to their exact Kernel classes; the latter retains the directly positive-guarded admission control | [condition assembly](../A12Kernel/Elaboration/ValidationCondition/Assembly.lean), [rule-owned projection](../A12Kernel/Elaboration/ValidationRuleGroupOperand.lean), [whole-rule projection](../A12Kernel/Elaboration/ValidationRule.lean), [group-operand proofs](../A12Kernel/Proofs/ValidationRuleGroupOperand.lean), [whole-rule proofs](../A12Kernel/Proofs/ValidationRule.lean), [group-operand cases](../A12Kernel/Conformance/ValidationRule/GroupOperandDiagnostic.lean), [whole-rule cases](../A12Kernel/Conformance/ValidationRule/OrdinaryAdmission.lean) | E closed for the named static matrix and two whole-rule projections; P closed for the typed decision table and negative-iteration projection; the error-field-reference projection is E closed without a separate theorem; L at a12-dmkits `2d384c59f18cf9a1019e1e8273f2d8e900f741e0`; Q closed for the direct Translate/Explain checked-client decision; C/X none | Nested/different repeatable scopes, other carriers, operators, arities, authored orders, executable newly admitted group-list/count conditions, and other assembly refusals: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| RNU key diagnostics | Repeated/unknown keys and three missing-repeatable shapes; parallel paths stay unmapped. | [elaboration](../A12Kernel/Elaboration/RepetitionNotUnique.lean) | E mapped; L [source](SOURCES.md#src-group-list-rnu-admission-correction); P/X/Q none | Parallel identity and other refusals: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Formal output boundary | Checked values retain formal findings separately from later public message construction | [`CheckedDocument.lean`](../A12Kernel/Elaboration/CheckedDocument.lean), [`ValidationRule.lean`](../A12Kernel/Semantics/ValidationRule.lean) | E/P closed for retained findings; selected C | Codes, locale, and complete output: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |

### §4 — required property

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Requiredness scope | Absolute, nearest-repeatable, and direct-parent modes resolve without erasing the declaration gate | [`Required.lean`](../A12Kernel/Semantics/Required.lean), [`CheckedRequired.lean`](../A12Kernel/Elaboration/CheckedRequired.lean) | E/P closed for resolved scope; selected C on absolute scalar cases | Wider generated orchestration: [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |
| Generated required staging | Full and partial staging traverse model-owned declarations and reuse the same checked certificate | [`CheckedRequired.lean`](../A12Kernel/Elaboration/CheckedRequired.lean), [`PartialValidation.lean`](../A12Kernel/Semantics/PartialValidation.lean) | E/P closed for admitted scalar boundary; selected C | Repeatable/group breadth: [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |
| Group-content gate | Structural row content and group error state remain independent of field values and authored findings | [`GroupPresence.lean`](../A12Kernel/Semantics/GroupPresence.lean), [`CheckedGroupPresence.lean`](../A12Kernel/Elaboration/CheckedGroupPresence.lean) | E/P closed for resolved product; source-grounded | Complete group lists/counts: [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |

### §5 — numbers and decimals

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Arithmetic and scale analysis | Exact `Rat` values, signed scale summaries, constant expandability, precision-50 arithmetic, staged power, and domain failure. Repeatable direct-Number `+`, `−`, `×`, division, and power retain authored order and their exact static/target contracts through addressed execution; division and field-exponent power require explicit result-scale suppression, and power additionally certifies a scale-0 exponent | [`NumericArithmetic.lean`](../A12Kernel/Semantics/NumericArithmetic.lean), [`NumericScale.lean`](../A12Kernel/Elaboration/NumericScale.lean), [`NumericExpression.lean`](../A12Kernel/Elaboration/NumericExpression.lean), [`AddressedNumberBinary.lean`](../A12Kernel/Elaboration/AddressedNumberBinary.lean), [`AddressedNumberDivision.lean`](../A12Kernel/Elaboration/AddressedNumberDivision.lean), [`AddressedNumberPower.lean`](../A12Kernel/Elaboration/AddressedNumberPower.lean) | E/P closed for implemented operators; real-kernel authoring and public interpreter outcomes cover the bounded addressed routes, while staged precision remains owned by the scalar arithmetic separators; C none | Remaining operators/authoring: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) |
| Fillability and comparison | Directional empty provenance, scale-19 comparison, tolerance, and exact six-operator evaluation | [`NumericFillability.lean`](../A12Kernel/Semantics/NumericFillability.lean), [`NumericComparison.lean`](../A12Kernel/Semantics/NumericComparison.lean), [`NumericTolerance.lean`](../A12Kernel/Semantics/NumericTolerance.lean) | E/P closed for stated slice; selected C/X; upstream L partial | Clause-wide calibration: [SG12](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage) |
| Numeric wrappers and extrema | Operation-form `Round`, `Abs`, and expression-valued operand-list `Min`/`Max` with per-call authoring boundaries. Repeatable direct-Number execution is closed for scale-preserving root `Abs`, all three root rounding modes with exact places/result scale, and every nonempty ordered `Min`/`Max` list in which each field-backed operand is direct, carries one operand-local `Abs`/Round, or is one arithmetic node whose two ordered operands are each a direct field or an immediate literal, or is one nested `Min`/`Max` call whose ordered leaves are direct fields or immediate literals, with one further immediate decoded literal admitted anywhere in the outer list. Each nested call retains its selector and leaf order as one outer operand, owns its own one-literal budget, contributes its dependency and leaf-scale union, and evaluates row-locally through the same authored-order selector. No operand has to be field-backed: the checked target certificate owns iteration and the target policy, so a constant-only child, a literal-only list, and any other fieldless list are admitted and still compute once per instantiated row. Extrema store one ordered operand list with no positional literal encoding, so authored order is retained structurally; the shared `NumericArithmeticOp` tag retains operation identity plus both inner operands in authored order without embedding a target-owning binary operation, and the operation owns its derived child scale (maximum for the additive nodes, sum for the product) over field scales and authored literal scales alike; the outer result scale is the maximum transformed operand/literal scale. Target admission is the shared `exactNumericScaleComparisonAllowedWithSuppression` predicate over the list's derived `NumericScaleSummary`, so an all-capable list may be padded up to a larger declared target scale while a capable larger derived scale and every non-capable mismatch remain rejected. | [`NumericRounding.lean`](../A12Kernel/Semantics/NumericRounding.lean), [`AddressedNumberAbs.lean`](../A12Kernel/Elaboration/AddressedNumberAbs.lean), [`AddressedNumberRound.lean`](../A12Kernel/Elaboration/AddressedNumberRound.lean), [`AddressedNumberExtremum.lean`](../A12Kernel/Elaboration/AddressedNumberExtremum.lean), [`NumericValidation/`](../A12Kernel/Elaboration/NumericValidation/) | E/P closed for the named bounded routes. Real-kernel authoring locks cover root wrappers, direct extrema, literals, local wrappers, and field-pair addition, subtraction, and multiplication in either outer/inner order with scale/self-reference controls; the product's summed child scale is exact in both directions at a12-dmkits `b852a286`, and inner literals at either position with authored-scale participation plus the separate outer one-literal budget are measured at `3a63d0d5`; nested same/different selectors and independent per-call literal budgets are statically Kernel-confirmed at a12-dmkits `700095df`, while addressed nested values, scale projection, poison order, and addresses remain internal with external evidence pending. Current `DM_INTERPRETER` output carries exact row pointers for focused value/outcome classes; exact first-poison cause remains internal because the public outcome is cause-blind; C none | Remaining division/power-valued operands, deeper nested or nested wrapper/arithmetic operands, and aggregates: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) |
| Number source conversions | Checked Number fields, `BaseYear`, String `Length`, `RangeAsNumber`, `FieldValueAsNumber`, aggregate, and admitted temporal component/difference sources. Repeatable direct assignment, root wrappers, extrema, binary arithmetic, division, and field-exponent power share certified Number-source placement, which admits an operand at any scope the computed target's scope binds, while retaining operation-specific scale and suppression contracts. Extrema retain direct, local-wrapper, direct-field-arithmetic, and one-level direct/literal nested operands plus at most one outer literal; nested calls keep their own literal budget, selector, ordered dependencies, and scale union; the shared arithmetic tag reuses the ordered pair certificate, contributes both dependencies without a target-owning child, and takes its derived scale from the operation. Conversion retains exact String/Enumeration/category projection; range retains its 1-based inclusive interval; Length consumes normalized UTF-16 text | [`NumericSource.lean`](../A12Kernel/Elaboration/NumericSource.lean), [`AddressedNumericLeaf.lean`](../A12Kernel/Elaboration/AddressedNumericLeaf.lean), [`AddressedNumberField.lean`](../A12Kernel/Elaboration/AddressedNumberField.lean), [`AddressedNumberExtremum.lean`](../A12Kernel/Elaboration/AddressedNumberExtremum.lean), addressed Number family owners | E/P closed for named routes. Real-kernel gates and current row-addressed interpreter observations cover bounded non-suppressed placements. a12-dmkits `112169fa` restores structured suppression authoring and clean division/power representatives; maintained differentials still own the broader runtime matrices and kernel-strategy agreement. Exact cause order and rich addressed partitions remain Lean-locked; C none outside flat Number | Remaining repeatable expressions and ingestion: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) |
| Number aggregates | Direct/plain-star/filtered-star streams; ordered Sum, extrema, counts, and row-aligned products. Under partial validation, the four combiner aggregates use the normalized wildcard-dominance-reduced universal extent gate inside each current iteration subtree; `SumOfProducts` retains its complete-cross-product gate. The unmeasured `NumberOfValueInFields` count keeps its separate one-covering-identifier predicate | [`NumericAggregate.lean`](../A12Kernel/Semantics/NumericAggregate.lean), [`NumericAggregate/`](../A12Kernel/Elaboration/NumericAggregate/) | E/P closed for admitted shapes; upstream L partial; C none | Semantic-root versus physical-root relevance identity; group-scope admission, full-validation extent, and family limits: [Number group aggregates](#cap-number-group-aggregates). Wider aggregate/path shapes: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) and [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Stored Number formal read | Decimal-valued input retains signed coefficient/scale, derives strip-and-minimum-scale plain text, and is checked from that text; String-valued input stays verbatim. The checked document retains exact regime, selected text, and parsed/formal result separately. Nonrepeatable and repeatable addressed `FieldValueAsString` consume that selected text without reconstructing it from `Rat`; the addressed route preserves exact row keys through source-relative result classification and retained-action application | [`NumericInput.lean`](../A12Kernel/Semantics/NumericInput.lean), [`CheckedDocument.lean`](../A12Kernel/Elaboration/CheckedDocument.lean), [`StringComputation.lean`](../A12Kernel/Elaboration/StringComputation.lean), [`AddressedFieldValueAsString.lean`](../A12Kernel/Elaboration/AddressedFieldValueAsString.lean) | E/P closed through the stated computation/result/application paths; upstream L at `43f8e5f4`, `ff1c85eb`, `c4c16399`, plus source-owned addressed authorability and three-engine route preflight at `f6dc22d2`; the exact addressed scale/empty/poison matrix is internally locked but not separately Kernel-calibrated; C none | Host ingestion and remaining addressed expression placements: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) |
| Stored decimal identity | Exact coefficient/scale identity kept separate from expression `Rat`; scale-19 canonical amount and bounded computed rendering | [`NumericStoredNumber.lean`](../A12Kernel/Semantics/NumericStoredNumber.lean) | E/P closed; peer-triangulated; C none | Concrete host decimal ingestion: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) |
| Number target and delta | Target admission, warning-suppressed no-fit branch, exact target checks, source-relative classification, direct one-outcome application, retained CLEARED placement, dependency projection, and checked separate-destination application at scalar or exact repeatable addresses with directly created ancestry | [`NumericTarget.lean`](../A12Kernel/Semantics/NumericTarget.lean), [`NumericComputationResult.lean`](../A12Kernel/Semantics/NumericComputationResult.lean), [`NumericApplication.lean`](../A12Kernel/Semantics/NumericApplication.lean), [`NumericComputation/RunApplication.lean`](../A12Kernel/Elaboration/NumericComputation/RunApplication.lean), [`CheckedDocument.lean`](../A12Kernel/Elaboration/CheckedDocument.lean) | E/P closed for the stated target/action projection; upstream L at `7b2e9f7` and `f6dc22d2`; C none | Target breadth, complete applied-document materialization, unobserved predecessor padding/order, and later validation: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition), [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion), [SG12](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage) |
| Scalar Number computation | Checked alternatives, finite supplied-order run, rich result, relation, generic whole-result application, and the checked separate-destination projection shared with exact repeatable addresses. Bounded repeatable direct Number assignment at a target-bound operand scope, scale-preserving root `Abs(Number)`, all three root Round modes, nonempty `Min`/`Max` over direct/local-wrapper/direct-field-arithmetic operands plus one immediate outer literal and one-level nested `Min`/`Max` operands over direct field/literal leaves, `+`/`−`/`×`, scale-suppressed division, scale-0-field-exponent power, `FieldValueAsNumber`, `RangeAsNumber`, String `Length`, and the numeric Date component of a selected DateRange endpoint specialize the checked target/source placement without widening the scalar expression elaborator. That placement admits an operand exactly when the computed target's repeatable scope **binds** every level the operand crosses, which is the measured Kernel rule and not scope equality, so a form-level constant feeding a row is ordinary; each operand is read at its own path inside the current target row, and a sibling or deeper operand is refused with the shared out-of-scope class that reports `MVK_NO_WILDCARD`. Both halves are Kernel-locked at the [operand-scope checkpoint](SOURCES.md#src-computation-operand-scope), the admission across five operand shapes and the own-path read on both codegen strategies for a filled, an empty, and a formally invalid outer operand, plus a **two-level** target where a middle-level operand reaches exactly the leaves of its own enclosing row and clears exactly those on invalidity. [Nested-placement cases](../A12Kernel/Conformance/AddressedNestedPlacement.lean) own that branch, because the placement rather than any leaf decides it. Every reached target-scale refusal projects exactly to `MVK_INVALID_COMPARE_DEC_PLACES`. `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD` projects across the admitted arithmetic surface: `+`, `-`, or `*` reading the target on exactly one side, against an immediate literal or a different direct Number field of any scale. External rows now cover that surface completely: a12-dmkits `8094f664` measures the whole 24-cell operator × position × operand-kind grid plus twelve suppression rows, so no cell of the admitted arithmetic surface rests on the uniform gate order alone. The gate order rather than the authored shape is the mechanism, so scale mismatch precedes self-reference across that surface, and suppressing only the scale code exposes self-reference. Division, a both-sides target read, nested or specialized reads, invalid authoring, unsupported expressions, and every other target-reading computation retain their immediate structural self-reference refusal, even when a later operand would otherwise fail resolution, and remain unmapped from an exact self-reference class. Fixed multi-group `NumberOfFilledGroups` statically admits at least two distinct disjoint fixed nonrepeatable operands, preserves exact short-arity, duplicate, overlap, and missing-star diagnostic identity, and evaluates the direct-child runtime slice as an ordinary exact `Nat` count over the compute-arm presence projection. Wider runtime shapes and the model-free resolved evaluator refuse structurally in the preflight, ahead of any read | [`NumericComputation/`](../A12Kernel/Elaboration/NumericComputation/), [`AddressedNumericLeaf.lean`](../A12Kernel/Elaboration/AddressedNumericLeaf.lean), [`AddressedNumberField.lean`](../A12Kernel/Elaboration/AddressedNumberField.lean), [`AddressedNumberAbs.lean`](../A12Kernel/Elaboration/AddressedNumberAbs.lean), [`AddressedNumberRound.lean`](../A12Kernel/Elaboration/AddressedNumberRound.lean), [`AddressedNumberExtremum.lean`](../A12Kernel/Elaboration/AddressedNumberExtremum.lean), [`AddressedNumberBinary.lean`](../A12Kernel/Elaboration/AddressedNumberBinary.lean), [`AddressedNumberDivision.lean`](../A12Kernel/Elaboration/AddressedNumberDivision.lean), [`AddressedNumberPower.lean`](../A12Kernel/Elaboration/AddressedNumberPower.lean), [`AddressedFieldValueAsNumber.lean`](../A12Kernel/Elaboration/AddressedFieldValueAsNumber.lean), [`AddressedRangeAsNumber.lean`](../A12Kernel/Elaboration/AddressedRangeAsNumber.lean), [`AddressedStringLength.lean`](../A12Kernel/Elaboration/AddressedStringLength.lean), [`AddressedDateRangeBoundPart.lean`](../A12Kernel/Elaboration/AddressedDateRangeBoundPart.lean) | E/P closed for the named routes, action projection, exact scale projection, and bounded self-reference projection; fixed filled-group computation admission for lists of two and three is Kernel-confirmed at clean a12-dmkits `cd41ea94`, with larger-list correspondence external evidence pending, while its direct-child runtime projection is measured at `677e2eb7`; clean real-kernel target-scale matrices at `5c0d5f03` and accepted peer `dd0bcadf` establish the shared comparison predicate and exact scale class; the bounded target-reference precedence and both exact classes are Kernel-confirmed at clean a12-dmkits `93771ee` and widened across operator and operand position at reviewed inbound `22fa19e7`, with the differing-scale field operand and its suppressed counterpart measured locally at clean a12-dmkits `4ba31a49` under pending [SPEC-2026-08-11-01](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-11-01--the-computed-number-scale-gate-precedes-self-reference-for-a-differing-scale-field-operand-too); real-kernel/static plus row-addressed interpreter observations cover arithmetic field pairs inside extrema at `27c1e583`, `112169fa`, and `b852a286`; static nested-call boundaries are Kernel-confirmed at `700095df`, while addressed nested runtime remains external evidence pending; exact inner first-cause order and rich result partitions remain Lean-locked; C none; bounded Number/String target-diagnostic Q | Heterogeneous scheduling and complete document integration: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition). Starred filled-group computation runtime and remaining repeatable numeric expressions: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) and [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |
| Addressed numeric-operation consumer | Exact bounded footprint distinguishes an ordered nonempty expression-operand list, prior target-state classification read, and target write; the fingerprint retains scope, full target policy, conversion projection/interval, direct versus root `Abs`, root Round mode/authored result scale, and `Min`/`Max` operation plus its derived scale summary — including multiplicative-constant capability, since two lists sharing a derived scale admit different declared target scales when only one is capable — and exact ordered direct-field/operand-local-`Abs`/operand-local-Round/arithmetic-operation/inner-and-outer-literal identity, including a one-level nested selector with its ordered direct field/literal leaves. Fingerprint equality is Analyze-only; exact identity is the sole Transform; `admitsTargetScale`/`admittedTargetScales` decide candidate declared target scales from the retained summary and are proved to agree with each checked operation's own admission at its declared scale | [`AddressedNumericOperationConsumer.lean`](../A12Kernel/Elaboration/AddressedNumericOperationConsumer.lean), [proofs](../A12Kernel/Proofs/AddressedNumericOperationConsumer.lean), [probe](../A12Kernel/Conformance/AddressedNumericOperationConsumer.lean) | E/P/Q closed for the named bounded Analyze/identity-Transform/retarget-legality task; the retarget decision shares the elaborator's gate universally, identity universally preserves the complete Analyze fingerprint as well as rich execution and result views, so no probe case rechecks it, while stored-to-category, changed-endpoint, direct-to-root-`Abs`, same-scale root-Round mode, extrema operation/order/cardinality, literal position/authored scale, direct-to-local-`Abs`, local-Round mode/places, arithmetic operation/inner order/outer position/inner-literal value and authored scale — including a product and a sum that derive the same outer scale — nested selector/ordered-leaf identity, and cross-family fingerprints differ without implying semantic inequivalence; no kernel-evidence claim. Its joint checked-model fixture isolates conversion, interval, wrapper, mode, scale, and ordered-list changes and the universal identity relation covers both rich outcome and result-view boundaries; it adds no harness or intermediate representation | Any nonidentity Transform requires its own semantic relation and consumer hypothesis |

### §6 — dates and time

<a id="resolved-date-range-overlap-truth-and-operator-scans"></a>
<a id="resolved-direct-date-construction-and-base-year-date-sources"></a>
<a id="differenceindays-concrete-profile-closure"></a>

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Date construction and Base Year | Gregorian and legacy-hybrid construction, partial-known admission, Base-Year date source, and numeric component projection | [`DateConstruction.lean`](../A12Kernel/Semantics/DateConstruction.lean), [`BaseYearDateSource.lean`](../A12Kernel/Semantics/BaseYearDateSource.lean), [`DateConstructionNumeric.lean`](../A12Kernel/Semantics/DateConstructionNumeric.lean) | E/P closed for stated constructors; upstream L/peer controls; C none | Remaining constructors and ingestion: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) |
| Model-zone profile | UTC/GMT plus pinned full historical `Europe/Berlin` profile, strict fresh-label resolution, and explicit overlap/gap handling | [`ModelZone.lean`](../A12Kernel/Semantics/ModelZone.lean), [`BerlinLegacyTimeZone.lean`](../A12Kernel/Semantics/BerlinLegacyTimeZone.lean) | E/P closed for three profiles; source-grounded and peer-triangulated; C none | Other legal zones: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) |
| Temporal differences | Date completed periods, exact-instant DateTime elapsed units, model-zone calendar-day differences, checked constructed-Date all-unit execution, field-backed or dynamic-`Now` calendar-day shift/direct DateTime forms, and field-backed or dynamic-`Now` sub-day shift/direct elapsed DateTime forms in either authored position under UTC/GMT and Berlin | [`DateDifference.lean`](../A12Kernel/Semantics/DateDifference.lean), [`DateTimeDifference.lean`](../A12Kernel/Semantics/DateTimeDifference.lean), [`BerlinLegacyCalendarArithmetic.lean`](../A12Kernel/Semantics/BerlinLegacyCalendarArithmetic.lean), [`DateTimeDayDifference.lean`](../A12Kernel/Semantics/DateTimeDayDifference.lean), [`ConstructedDateDifferenceEvaluation.lean`](../A12Kernel/Elaboration/ConstructedDateDifferenceEvaluation.lean), [`DateTimeDayShiftDifferenceEvaluation.lean`](../A12Kernel/Elaboration/DateTimeDayShiftDifferenceEvaluation.lean), [`DateTimeSubdayShiftDifferenceEvaluation.lean`](../A12Kernel/Elaboration/DateTimeSubdayShiftDifferenceEvaluation.lean) | E/P closed for admitted types/units and bounded mixed forms; Berlin midnight/repeated-hour identity, exact-instant ordering and millisecond truncation, per-call world transport, shift-value missing provenance, authored evaluation order/sign, shared period-sign laws, and unit gate locked; upstream L at `976474a4` covers the explicit nested `ValueAsDate` shift, difference, and DateTime placements across both kernel strategies plus JVM/Node; wider upstream L partial; C none | General recursive/wider authored and repeatable use: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) |
| Calendar shifts | Constructed-Date UTC/GMT shifts plus signed Berlin `AddDays`/`AddMonths`/`AddYears` after the pinned floor; direct and two-step field-backed or dynamic-`Now` DateTime `AddDays` under the same profiles; direct, bounded two-day, and both field-backed/dynamic mixed orders reach the shared checked target, result view, and application; field-backed and dynamic-`Now` `AddHours`/`AddMinutes`/`AddSeconds` retain the whole shifted exact instant, expose wall-clock extraction only as a projection, and reach the same checked target/result/application path; either exact sub-day source feeds one further checked sub-day shift and one `AddDays`, while either field-backed or dynamic-`Now` `AddDays` feeds one sub-day shift; bounded continuations feed each inner exact instant directly to the outer operation; day mutation retains source offset and target date, month/year use compute-time resolution, and all retain exact instant and milliseconds except the legacy clock-clear branch; fresh construction and Date-valued shifts share one exact value boundary whose selected instant feeds comparison, shift, and difference consumers without label reconstruction | [`ConstructedDateShift.lean`](../A12Kernel/Semantics/ConstructedDateShift.lean), [`DateShift.lean`](../A12Kernel/Semantics/DateShift.lean), [`BerlinLegacyCalendarArithmetic.lean`](../A12Kernel/Semantics/BerlinLegacyCalendarArithmetic.lean), [`ConstructedDateEvaluation.lean`](../A12Kernel/Elaboration/ConstructedDateEvaluation.lean), [`ConstructedDateShiftEvaluation.lean`](../A12Kernel/Elaboration/ConstructedDateShiftEvaluation.lean), [`ConstructedDateDifferenceEvaluation.lean`](../A12Kernel/Elaboration/ConstructedDateDifferenceEvaluation.lean), [`DateTimeDayShiftEvaluation.lean`](../A12Kernel/Elaboration/DateTimeDayShiftEvaluation.lean), [`DateTimeSubdayThenDayShiftEvaluation.lean`](../A12Kernel/Elaboration/DateTimeSubdayThenDayShiftEvaluation.lean), [`DateTimeDayThenSubdayShiftEvaluation.lean`](../A12Kernel/Elaboration/DateTimeDayThenSubdayShiftEvaluation.lean), [`DateTimeDayShiftComputation.lean`](../A12Kernel/Elaboration/DateTimeDayShiftComputation.lean), [`DateTimeMixedShiftComputation.lean`](../A12Kernel/Elaboration/DateTimeMixedShiftComputation.lean), [`DateTimeSubdayShiftComputation.lean`](../A12Kernel/Elaboration/DateTimeSubdayShiftComputation.lean), [`DateTimeComputation.lean`](../A12Kernel/Elaboration/DateTimeComputation.lean), [`ValueAsDateTimeExtraction.lean`](../A12Kernel/Elaboration/ValueAsDateTimeExtraction.lean) | E/P closed for stated profiles/units and bounded two-step/target/comparison forms; exact-zero identity, signed-32-bit amount narrowing before unit multiplication, long-range Berlin source-offset landing, order-sensitive cross-mechanism gap/overlap landings, repeated-midnight fresh-comparison `false/true/true` separation for day/month/year plus inverse, ordering, and ordinary-date controls, long spring-gap year qualification, sub-day overlap identity and all-unit calendar carry, millisecond retention, explicit per-call world transport, generated inner-before-outer read order, target self-reference, source-relative target actions, and inner-cause short circuit locked; upstream L at `11526d9c` covers shared narrowing and neighboring amount behavior, `5716a964` confirms the source-instant day landing through the separating 369-day composite, `17f95bcc` confirms sign-independent year landing through the dual-kernel 7670 versus pre-fix interpreter 7671 separator, `38b5046` confirms the quoted direct-comparison shapes are authorable, and accepted `41606566` closes retained/derived exact-instant comparison, the repeated-midnight matrix across both kernel strategies plus interpreter/JVM/Node, measured millisecond preservation, and the externally unobservable February-clear source-fidelity branch; C none | General recursion, other profiles, and repeatable placement: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) |
| Component authoring | Checked zero-through-three-component `Time(...)` prefixes over parser-bounded quoted constants, admitted Number, String, direct extractor, and separately checked shifted-extractor sources; checked `Date(...)`, field extraction, `Today`, `Now`, and world-dependent component sources on admitted routes | [`ConstructedDateComponents.lean`](../A12Kernel/Elaboration/ConstructedDateComponents.lean), [`ValueAsDateTimeComponents.lean`](../A12Kernel/Elaboration/ValueAsDateTimeComponents.lean), [`ValueAsDateTimeWorldComponents.lean`](../A12Kernel/Elaboration/ValueAsDateTimeWorldComponents.lean) | E/P closed per named route; four arities, fixed omitted zeroes, effective-zero fractional admission, Java-21 decimal-digit constants, Hour/Minute/Second order, and complete dependency membership locked; accepted upstream revisions `4f211673`, `e75cc6a1`/`40683600`, and `ec9ece23` close prefix arity, serializable Number/String/`yyyy` Date declaration gates, and nested Date/Time extractor provenance across maintained static, dual-kernel, interpreter, JVM, and Node routes; accepted `2f97e4de`/`f29dc2ad` separately close the constant runtime, BMP-versus-supplementary UTF-16 boundary, mixed constant/field construction, and exact positional range/width/empty rejection matrix; the client-SPI extensible-String arm has no DM-JSON representation; C none | Remaining recursive operand breadth: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) |
| Temporal target/computation/application | Checked nonrepeatable zero-through-three-component Time operations cover both document-only prefixes and one explicit-world shifted-`Now` prefix with complete target-reference exclusion, exact `HH:mm:ss` target admission/rendering, rich source-relative result classification, and exact application; checked Date computation admits only a FULL target, while all three partial target modes project exactly to `MVK_INVALID_DATE_TYPE` and partial Date values remain legal source operands; formats, zones, kinds, and policy failures stay unmapped by that projection, and attempted diagnostic suppression has no local authored surface. Direct and shifted dynamic `Now`, field-backed and dynamic-`Now` DateTime `AddDays`, bounded field-backed/dynamic two-day execution, field-backed DateTime sub-day execution, and both field-backed/dynamic mixed orders flow through the same public stages where named; retained CLEARED application is distinct from direct no-value application and creates a present-empty absent destination target | [`TimeComputation.lean`](../A12Kernel/Elaboration/TimeComputation.lean), [`TemporalTargetPolicy.lean`](../A12Kernel/Elaboration/TemporalTargetPolicy.lean), [`TemporalComputationResult.lean`](../A12Kernel/Elaboration/TemporalComputationResult.lean), [`TemporalValueComputationApplication.lean`](../A12Kernel/Elaboration/TemporalValueComputationApplication.lean), [`DateTimeComputation.lean`](../A12Kernel/Elaboration/DateTimeComputation.lean), [`DateTimeDayShiftComputation.lean`](../A12Kernel/Elaboration/DateTimeDayShiftComputation.lean), [`DateTimeMixedShiftComputation.lean`](../A12Kernel/Elaboration/DateTimeMixedShiftComputation.lean), [`DateTimeSubdayShiftComputation.lean`](../A12Kernel/Elaboration/DateTimeSubdayShiftComputation.lean), temporal computation/application owners | E/P closed for the named Time operations, admitted forms, and exact partial computed-Date target diagnostic; per-call world sampling, zero/constant defaults, authored component order, first formal cause, complete static/dynamic target exclusion, incomplete/unreal no-value, clean-Time always-changed classification, direct-versus-retained clearing, poison, residual status, duplicate action targets, and clear-before-write order locked; accepted upstream `0621f088` triangulates computed-Time clock identity, no-value/poison, source-identical changed classification, and separate-destination application, generic retained-result source plus the Number matrix at `f6dc22d2` establishes the action boundary, while `456dca65` locks FULL-only Date target admission, `MVK_INVALID_DATE_TYPE`, rejected suppression, and exact pre-1900 target rejection; C none | Alternatives, wider expressions, suppression authoring, and scheduling: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion), [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) |

<a id="cap-full-date-stored-classifier"></a>
#### Bounded full-Date stored classifier

- `state`: implemented for full-precision `yyyy-MM-dd` and `dd.MM.yyyy` declarations under UTC, GMT, and the pinned Europe/Berlin profile.
- `boundary`: exact-width declared-format parsing and default-cutover legacy-calendar reality produce `datumFormatFalsch`; the universal 1583-10-16 floor and enabled pre-1900 check produce `datumFalsch`; present-empty remains physically present and value-free.
- `boundary`: admitted input becomes one `DateValue` carrying its model-zone midnight instant, decoded parts, and stored-Gregorian basis; equal UTC and Berlin labels retain equal parts but distinct instants.
- `route`: [`FullDateInput.lean`](../A12Kernel/Elaboration/FullDateInput.lean) owns certification, parsing, and resolution; integration with the single checked-document input route remains open.
- `reuse`: exact DateRange endpoints delegate to the same scalar parser and stored-Date resolver; no second document route or DateRange-specific date parser remains.
- `assurance`: E closed for the separating classifier matrix, including the legacy cutover gap and Julian-side leap day; P closes decoded-part and calendar-basis preservation in [`TemporalValue.lean`](../A12Kernel/Proofs/TemporalValue.lean); L directly locks both exact formats and malformed-calendar cause, while legacy-calendar parsing, the floor, and pre-1900 transfer are source-grounded and exact UTC/Berlin instants remain external evidence pending under the [stored full-Date checkpoint](SOURCES.md#src-full-date-stored-input); C/X/Q none.
- `remains`: checked-document integration, partial Date input, wider `SimpleDateFormat` syntax, other legal zones, DateTime/Time ingestion, and wider checked consumers remain under [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-temporal-value-identity"></a>
#### Universal temporal value identity

- `state`: implemented for scalar Date, Time, DateTime, and the DateRange value core.
- `boundary`: scalar Date and both ordered DateRange endpoints share `DateValue`, retaining exact instant, decoded parts, and Gregorian versus legacy-hybrid provenance; Time and DateTime retain their established closed payloads.
- `bridge`: `DateRangeValue.toResolvedDateRange?` projects both decoded endpoints through full-Date reality and floor admission; failure of either endpoint returns `none`, while the richer source range remains available so equal resolved intervals do not imply equal exact values.
- `boundary`: the yearless owner admits a direct stored field, a plain or filtered starred field, or a fixed/starred group carrier whose complete recursive expansion is nonempty and entirely yearless, sharing one cell projection so a star's empty row becomes a skipped slot in canonical address order. A filter makes a firing that reaches its operand an omission, reusing the generic scan's provenance rather than a yearless-specific rule. A carrier contributes every declaration in its expansion at every instantiated row; an expansion that is empty or carries any non-yearless declaration is refused, as is every non-stored read form. One plural entry point serves every admitted list shape, so the scalar keeps its single direct-field restriction in one place.
- `boundary`: the yearless owner exposes both scans. Its any-pair and scalar-versus-list entry points share one generic first-match and one generic accumulate scan with the resolved domain, parameterized by the overlap relation, so the no-UNKNOWN law is proved once and specialized rather than restated per operator and domain.
- `bridge`: `YearlessInterval` is the separate unconfigured-yearless domain, keyed to what a declaration denotes rather than to a completed date: a month-only pair spans day 1 through its finish month's yearless maximum last day, February reaching 29, while a day-bearing pair keeps its authored days. Its overlap relation reuses the resolved closed-interval and inverted guards but is **not** a specialization of the resolved one, because February's yearless maximum disagrees with any non-leap completion.
- `route`: [`YearlessDateRangeOverlap.lean`](../A12Kernel/Elaboration/YearlessDateRangeOverlap.lean) owns unconfigured admission and evaluation as its own module, so the Base-Year-completed route keeps its exact domain. Admission requires every operand to be a direct stored yearless field in a model with no Base Year; a configured model, an exact declaration, and therefore any mixed pair are refused, because no retained observation covers a mixed pair and guessing would put a manufactured year in a public result.
- `owner`: the any-pair scan in [`DateRangeOverlapOperators.lean`](../A12Kernel/Semantics/DateRangeOverlapOperators.lean) is parameterized by the interval domain's own relation, so occurrence order and sticky reached-filter polarity are stated once; `ResolvedDateRangeSlot` and its siblings remain abbreviations of that generic shape.
- `owner`: [`Core.lean`](../A12Kernel/Core.lean) owns the universal payloads; [`DateRangeOverlap.lean`](../A12Kernel/Semantics/DateRangeOverlap.lean) owns the resolved bridge; [proofs](../A12Kernel/Proofs/TemporalValue.lean), [payload cases](../A12Kernel/Conformance/TemporalValue.lean), and [bridge cases](../A12Kernel/Conformance/DateRangeOverlap.lean) own projection assurance.
- `assurance`: E/P closed for the represented payloads, the resolved bridge, and the yearless interval, whose relation carries commutativity and inverted-guard laws. L for the starred and filtered-star operands is exact against the [starred checkpoint](SOURCES.md#src-date-range-starred-yearless-overlap), whose sole-star and filtered rows this project reproduces end to end including the omission polarity, and for group carriers against the [carrier checkpoint](SOURCES.md#src-date-range-yearless-group-carrier); its per-row scalar-plus-star rows stay measured-but-unmodeled. L for the yearless relation is exact against the [yearless-overlap checkpoint](SOURCES.md#src-date-range-yearless-overlap): closed intervals, whole-month spans at both edges, and the February 29 maximum. The resolved range representation remains internally justified and adds no kernel-correspondence claim. The unconfigured route's own cases run stored text through checked cells to the verdict for every measured row, including the halved spelling and the February maximum.
- `remains`: wider raw-input and target policies plus authored operator consumers beyond the implemented DateRange routes: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-date-range-checked-declaration"></a>
#### Checked DateRange declaration and admission

- `state`: flat declaration validation decides the exact Kernel-measured eight-pair `(format, separator)` allowlist, and every admitted pair selects exactly one stored-input profile through the existing checked-document route. [`Proofs/DateRangeInput.lean`](../A12Kernel/Proofs/DateRangeInput.lean) owns that totality law, so the `unsupportedPolicy` refusal stays fail-closed for callers that bypass declaration validation without being claimed unreachable.
- `boundary`: a `DateRangeInputFormat` constructor names one stored presentation, not one component shape: two constructors retain a month pair and two retain a month/day pair, because the declared separator and endpoint spelling change the stored text while the retained components agree. The order rule is shared, so an inverted range is invalid under every presentation.
- `boundary`: the two lexical variants execute stored input and the direct single-level starred `FirstFilledValue` route, storing `0609` and `01.06-30.09` through their own target spellings. Direct bound and singular overlap admit them on the same terms as the slash-separated spelling of their component set.
- `boundary`: a yearless target consumes a result only from a profile retaining the same components, selected by the spelling tables in [`DateRangeTargetPresentation.lean`](../A12Kernel/Elaboration/DateRangeTargetPresentation.lean); one order rule serves every profile sharing a shape, and a profile retaining different components poisons the target rather than inventing a spelling.
- `boundary`: direct-star source and target admission compares declared component sets, not carriers. `TemporalFirstFilledStarCarrier.acceptsSource` admits every DateRange crossing within one component set and requires identity for every scalar temporal carrier, where no crossing is measured. The target's declared spelling decides the stored text on a crossing.
- `boundary`: under an empty separator the stored token is halved, so the missing-separator cause cannot arise and an odd length is a shape failure; the dotted profile keeps that cause because its separator is present.
- `boundary`: a refused declaration keeps `emptyFormat` or `emptySeparator` where a source is absent and `unsupportedPair` otherwise; no Kernel diagnostic class is claimed for any of the three, and no format other than month-only admits an empty separator.
- `boundary`: `DateRangeFormat` remains shared by exact input and target presentation. Exact and year-bearing fragment inputs construct stored-Gregorian `DateValue` endpoints with model-zone midnight instants; shorter fragments retain either that exact range after Base-Year completion or an explicit yearless ordered component pair without a synthetic year or instant.
- `boundary`: present-empty, missing separator, malformed split or endpoint, inverted order, and the applicable universal-floor cause remain distinct. For every supported pair, `CheckedDocument` rejects caller-supplied raw state that disagrees with canonical classification; no DateRange-specific document or second read route exists.
- `boundary`: unsupported policy is explicit `DateRangeInputError`. Exact and year-bearing fragment profiles require a supported zone; configured shorter fragments require one after successful parsing; fragments without Base Year need no zone.
- `boundary`: direct bounds admit exact full-year, both year-bearing fragment profiles, and every Base-Year-completed yearless profile including the two lexical variants; without Base Year each yearless profile is refused. Singular overlap gates its whole operand list by year class through `mixesDateRangeYearInclusion`: a declaration is year-bearing on its own or through the model's Base Year, a list mixing the two classes is refused `MVK_DATE_WITH_AND_WITHOUT_YEAR`, and a configured model therefore admits every list including one crossing component sets. Within one class the operator applies no component-set gate. A starred or filtered yearless operand and the plural yearless route stay refused. Direct `FirstFilledValue` and the bounded constructor target admit all eight declaration pairs.
- `boundary`: `interpretationOfYear` is an optional declaration key that takes no part in admission and decides only whether a **wrapping** yearless value is formally valid: absent refuses it `dateRangeInvalid`, `FROM` anchors the start in the Base Year and moves the finish into the following year, and `TO` anchors the finish and moves the start into the preceding year. An ordered value is unaffected by every reading, and without a Base Year there is no anchor year, so a wrapping value keeps the refusal. The month-only finish takes the last day of its own placed year, so a February finish follows that year's leap rule. `resolveYearlessForModel` is the single consumer of the key and `completionYears` the single placement rule.
- `boundary`: both overlap operators **refuse** an operand whose declaration carries the key, on either side, under a yearless or a year-bearing exact profile, and with or without a Base Year, reporting the Kernel's `MVK_INVALID_DATE_RANGE_FORMAT`. `refuseDateRangeOverlapYearInterpretation` is the one gate and the three overlap certification routes are its only callers; bound extraction and equality comparison deliberately do not call it, because the same declaration is admitted there. The three certification dispatchers now share one fallback rule: only an unsupported canonical policy opens the fragment route, so a fragment-specific refusal is reported as itself instead of being replaced by the policy cause.
- `route`: [`DateRangeInput.lean`](../A12Kernel/Elaboration/DateRangeInput.lean) owns stored classification; [`CheckedDocument.lean`](../A12Kernel/Elaboration/CheckedDocument.lean) owns canonical coherence and phase reads; declaration and generic presence/required/index routes remain unchanged.
- `owner`: [`Core.lean`](../A12Kernel/Core.lean) owns exact versus yearless cell identity; [`DateRangeInput.lean`](../A12Kernel/Elaboration/DateRangeInput.lean) owns stored classification.
- `owner`: [`TemporalFormat.lean`](../A12Kernel/Semantics/TemporalFormat.lean) owns declaration policy and exact presentation; [`TemporalTarget.lean`](../A12Kernel/Semantics/TemporalTarget.lean) owns exact rendering; [`DateRangeTargetPresentation.lean`](../A12Kernel/Elaboration/DateRangeTargetPresentation.lean) owns checked fragment rendering; [`Observation.lean`](../A12Kernel/Semantics/Observation.lean) owns cause projection.
- `assurance`: E internally closed for the complete 8x4 candidate declaration grid, all eight input pairs, empty, separator and format failures, calendar reality, order, applicable floor, UTC/Berlin exact identity, yearless component identity, configured-year leap completion, unsupported capability, and checked-document anti-spoofing. P retains the admitted-format law, the month-only-empty-separator law, and the admitted-pair input-profile totality law; the former both-sources-nonempty conjunct was refuted by the allowlist. The comparison capability owns the remaining payoff-selected P boundary.
- `assurance`: L is exact for the two added stored texts `0609` and `01.06-30.09`, for their direct-star computation route, and for every crossing within a component set in both directions including the two full-Date spellings; the [crossing checkpoint](SOURCES.md#src-date-range-component-set-crossing) owns those admission verdicts, runtime outputs, the different-component refusal control, and the integrity hashes. The peer's six empty-separator formal rows are Kernel/interpreter parity rows without pinned codes, so malformed and inverted precedence for the two variants is internal reuse of the family's established order. Base-Year completion of either variant has no retained observation. The [bound checkpoint](SOURCES.md#src-date-range-bound-base-year) adds the Base-Year bound-admission matrix, the component-extraction gate, and the pinned completion of both variants with two silent negative controls.
- `assurance`: L is exact for the year-class gate at the [mixed-year checkpoint](SOURCES.md#src-date-range-mixed-year-overlap): ten of fifteen conditions refused unconfigured, eight mixed pairs and two mixed three-operand lists, with all fifteen admitted under Base Year 2024; both year-bearing fragment profiles appear on the mixing side, the five uniform-class conditions establish the absent component-set gate, and fifteen runtime rows agree on both codegen strategies. Those runtime rows also pin that a completed operand occupies the Base Year alone, so an exact operand naming another year is disjoint. The two gate laws state when the gate cannot fire; neither claims it is the only refusal a list can meet. Pending [SPEC-2026-08-22-01](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-22-01--date-range-overlap-admits-by-year-class-and-a-base-year-lifts-the-gate) carries the clause correction.
- `assurance`: L is exact for the overlap refusal, the endpoint years, and the placed-year leap check at the [interpretation-overlap checkpoint](SOURCES.md#src-date-range-interpretation-overlap-refusal): 10 refused and 5 admitted overlap conditions across a configured and an unconfigured model, 5 admitted bound and equality conditions on the same declarations, and 5 documents observed on both codegen strategies with every row agreeing. The endpoint years are therefore measured rather than peer-pinned. Pending [SPEC-2026-08-23-01](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-01--an-interpretationofyear-declaration-is-refused-as-an-overlap-operand) carries both clause additions.
- `assurance`: L is exact for the interpretation's formal-validity flip at the [interpretation checkpoint](SOURCES.md#src-date-range-year-interpretation): one Base-Year-2020 `dd.MM`+dash model, six documents, both codegen strategies agreeing, showing `datumBereichNichtGueltig` with no interpretation and nothing under either `FROM` or `TO`. The endpoint years themselves stay peer-pinned at the [reviewed reconciliation checkpoint](SOURCES.md#src-date-range-2026-08-19-reconciliation), whose retained rows carry no fixed expected outcome for the leap-day or `MM` cases. An interpretation-bearing unconfigured model, an interpretation on a slash-separated yearless profile, and any comparison, overlap, bound, or target row under an interpretation have no retained observation and stay internal.
- `assurance`: P owns the interpretation's two consequences at their extracted step: adding, changing, or removing the key cannot move an ordered range's endpoints, and the standard reading refuses every wrapping value under either Base Year state. Both wrapping placements are proved to span exactly one year boundary. The overlap refusal is **E-locked only**: no certificate records the absent key, so a proof-carrying invariant would have to add a field to the shared canonical operand carrier that bounds and comparison also use, which is beyond this gate's payoff. Five separating cases cover both operators, both sides, both profiles, the unconfigured route, and the diagnostic identity instead.
- `assurance`: L is exact for the declaration allowlist itself: the [reviewed 2026-08-19 checkpoint](SOURCES.md#src-date-range-2026-08-19-reconciliation) retains the complete 8x4 grid against the real Kernel. The refusal cause of a rejected pair is local classification with no measured Kernel counterpart.
- `assurance`: L is exact for the measured dotted cause matrix, `yyyy` and `yyyy-MM` completion and verdicts at their [year](SOURCES.md#src-date-range-year-fragment-construction) and [year-month](SOURCES.md#src-date-range-year-month-fragment-construction) checkpoints, and valid configured/yearless `MM` and `MM-dd` values at [the fragment checkpoint](SOURCES.md#src-date-range-base-year-fragment-construction). The [construction-target](SOURCES.md#src-date-range-construction-target-crossing), [year-fragment-target](SOURCES.md#src-date-range-year-fragment-constructor-target), and [exact-and-configured](SOURCES.md#src-date-range-exact-and-configured-constructor-targets) checkpoints add the rendered constructor output for **every** admitted target: all four yearless spellings, both year-bearing profiles, the ISO exact target, and both Base-Year-completed fragment targets, each with its inverted attempt and the yearless ones with their equal-endpoint row. What stays internally composed on this family is the empty, formal, and clearing branch of each target.
- `assurance`: the [reviewed reconciliation checkpoint](SOURCES.md#src-date-range-2026-08-19-reconciliation) pins the complete declaration grid and the empty-separator split. Formal negatives and exact endpoint instants remain internally composed.
- `remains`: other legal zones, full message construction, partial-validation overlap evaluation, and local target/computation breadth beyond the six implemented policies: [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) and [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration).

<a id="cap-date-range-construction-computation"></a>
#### Direct DateRange construction computation

- `state`: implemented for one checked direct nonrepeatable `DateRange(start, finish)` construction into a direct same-group nonrepeatable DateRange target under either exact policy or a matching `yyyy`, `yyyy-MM`, `MM`, or `MM-dd` profile, with or without Base Year where the profile permits it.
- `boundary`: admission reuses the checked two-endpoint construction and direct DateRange declaration. The target certificate retains the target's **own declared profile** and admits it whenever its component set equals the construction's, so both spellings of each set are reachable — a `MM` construction stores into a `MM`-with-empty-separator target and a `MM-dd` construction into a `dd.MM` target — while cross-component pairs, repeatable targets, wrong-group targets, and wrong-kind targets stay refused. The former enumeration named one spelling per set and could not represent the other two at all.
- `boundary`: execution reads both endpoints once through the existing immutable checked document in computation phase. Endpoint observations become one typed exact or yearless `DateRangeCellValue`; formal unavailability dominates emptiness through the established construction observation, and empty becomes no value.
- `boundary`: all targets delegate to the shared `DateRangeInputFormat.evaluateComputationResult`. Exact and configured fragment results render resolved endpoints; unconfigured `MM` and `MM-dd` render retained components; every profile shares ordered acceptance, inverted attempted-value retention, and existing DateRange application.
- `boundary`: each operand refusal now carries the Kernel diagnostic measured at the [operand-grid checkpoint](SOURCES.md#src-date-range-construction-operand-grid). Wrong kind, an unsupported partial profile, and a component mismatch all report `MVK_WRONG_DATE_FORMAT_FOR_OP`, so the operand kind is not recoverable from the code; a repeatable reference reports `MVK_NO_WILDCARD`. A **starred** operand draws `MVK_NO_WILDCARDS_ALLOWED` upstream but is not expressible at this boundary, which resolves a field id rather than an authored path, so that class stays unrepresented rather than guessed.
- `owner`: [`DateRangeConstructionComparison.lean`](../A12Kernel/Elaboration/DateRangeConstructionComparison.lean) owns endpoint certification and its refusal diagnostics; [`DateRangeConstructionComputation.lean`](../A12Kernel/Elaboration/DateRangeConstructionComputation.lean) owns checked assembly, the component-matched target certificate, and rich execution; [`DateRangeTargetPresentation.lean`](../A12Kernel/Elaboration/DateRangeTargetPresentation.lean) owns the shared exact-or-yearless target evaluator used by construction and `FirstFilledValue`.
- `owner`: [`TemporalTarget.lean`](../A12Kernel/Semantics/TemporalTarget.lean) owns exact-target evaluation; [`TemporalApplication.lean`](../A12Kernel/Semantics/TemporalApplication.lean) owns application.
- `case`: [constructor-target cases](../A12Kernel/Conformance/DateRangeConstructionComputation.lean) separate six local policies, unconfigured `MM` and `MM-dd`, cross-component directions, every other named static refusal, leap-aware completion, all six rendered outputs, inverted target rejection, empty, formal-before-empty, value application, and clearing.
- `proof`: [constructor-target laws](../A12Kernel/Proofs/DateRangeConstructionComputation.lean) preserve group plus profile ownership, exact and yearless endpoint projection, supported-format ordered and inverted target outcomes, fail-closed exact-target mismatch, and single construction evaluation.
- `assurance`: E/P closed for all six matching target profiles, including component-only `MM` and `MM-dd` without Base Year; C/X none.
- `assurance`: L is exact for filled ISO full-Date source endpoints rendered into dotted/dash at the [established-kind computation checkpoint](SOURCES.md#src-first-filled-kind-computations).
- `assurance`: L is exact for no-Base-Year `MM` and `MM-dd` admission plus accepted, inverted, empty, and formal outcomes at the [yearless constructor-target checkpoint](SOURCES.md#src-date-range-yearless-construction-target). ISO/slash, `yyyy`, `yyyy-MM`, and configured `MM`/`MM-dd` constructor outputs remain internal composition; application remains internal.
- `assurance`: Q closed for bounded public Execute/Analyze/Explain/Transform probes that recover all six target profiles, target and endpoint identities, model Base Year where applicable, exact or yearless endpoint observations, accepted or errored stored attempts, and applied or cleared values without renewed source research.
- `remains`: the two policies currently measured only on direct-star admission/runtime, alternatives, repetition, scheduling, overlap, bounds, wider application, shipment, and public compatibility remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-indexed-date-range-construction-computation"></a>
#### String-keyed DateRange construction computation

- `state`: implemented for two exact full-Date fields selected by literal exact-text keys or direct nonrepeatable evaluated String fields in one direct one-level String-indexed group and rendered into one direct same-declaring-group exact DateRange target.
- `boundary`: checked admission derives the indexed group and its declared String index from each repeated Date endpoint, requires exact full-Date endpoint policies under one supported zone, certifies each key as a literal or an exact model-owned direct evaluated String field, requires both endpoints to share the indexed group, and reuses direct DateRange target ownership and presentation certification.
- `boundary`: execution resolves one generated preliminary index column, reads dynamic keys once through the immutable checked document, projects each selected target through the shared semantic-index computation policy, retains literal identity or the checked source-field observation plus selected physical address, completes each Date at its endpoint position, and delegates the resulting typed range to the existing target renderer.
- `boundary`: clean no-match and selected empty become no value; any unavailable index column poisons before lookup, duplicate participants expose no selected address, and keyed reversal changes physical endpoint rows without changing authored endpoint position.
- `owner`: [`IndexedDateRangeConstructionComputation.lean`](../A12Kernel/Elaboration/IndexedDateRangeConstructionComputation.lean) owns bounded indexed admission, rich selection, and target assembly; [`CheckedIndexColumn.lean`](../A12Kernel/Elaboration/CheckedIndexColumn.lean), [`SemanticIndex.lean`](../A12Kernel/Semantics/SemanticIndex.lean), and [`DateRangeConstructionComparison.lean`](../A12Kernel/Elaboration/DateRangeConstructionComparison.lean) retain shared column, lookup, and endpoint-completion ownership.
- `case`: [indexed constructor-target cases](../A12Kernel/Conformance/IndexedDateRangeConstructionComputation.lean) separate literal and checked-field key identity, physical row selection, runtime key reversal, clean no-match, empty or formally unavailable selector fields, selected empty endpoints, duplicate-column poison, evaluated direct String-key admission, nested-scope refusal, and cross-group refusal.
- `proof`: [`checkedIndexedDateRangeConstructionComputation_admitted`](../A12Kernel/Proofs/IndexedDateRangeConstructionComputation.lean) exposes both key-admission, same-group, direct-target ownership, and target-profile certificates.
- `assurance`: E/P closed for the named literal- and direct-field-key profiles; L pins static admission, filled output, and duplicate-index clearing for both key forms, selected-empty endpoint clearing for literal keys, and clean unmatched or empty-selector clearing for field keys at the [String-index checkpoint](SOURCES.md#src-indexed-date-range-construction-target); C/X none.
- `assurance`: Q closed for one bounded internal Execute/Analyze/Explain probe. An isolated consumer reconstructed literal-versus-field key identity, exact-text selection, address/no-selection/selected-empty distinctions, selector-versus-column poison, authored endpoint position, and inverted attempted-target rejection from the canonical DateRange, path, and computation clauses plus this bounded record; no shipment or external correspondence for reversed field keys was claimed.
- `consumer`: Execute and Analyze can distinguish authored literals from checked selector-field observations and recover selected concrete addresses, endpoint observations, target policy, and output without re-running index selection; Explain can distinguish no selection from selected empty, selector formal poison, and column poison.
- `remains`: fragments, Number or Enumeration indices, nested or cross-group selection, comparisons, partial validation, alternatives, scheduling, application, shipment, and public compatibility remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-checked-date-range-bound"></a>
#### Checked direct DateRange bound extraction, fixed-Date comparison, and components

- `state`: implemented for direct nonrepeatable DateRange fields under the two canonical full-Date policies, exact-valued `yyyy`/`/` and `yyyy-MM`/`/`, plus `MM`/`/` and `MM-dd`/`/` when the checked model supplies Base Year, including comparison with one fixed full Date in either authored position and all four numeric Date components.
- `boundary`: `DateRangeBound` selects start or finish directly from `DateRangeValue`; the checked operation reads the field once through `CheckedDocument`, returns the exact `DateValue`, and preserves empty, validation-unknown, computation-poison, and exact cause identity without label reconstruction.
- `boundary`: the checked comparison retains field, bound, operand position, operator, fixed peer, exact selected observation, and established full-Date verdict in one result; it projects decoded calendar identity only at the existing comparison seam and reports an impossible non-full endpoint explicitly.
- `boundary`: the checked component consumer retains the same exact selected observation beside the existing `NumericOperand`; empty becomes symmetric fillable zero and formal unavailability preserves its cause without a second document read.
- `owner`: [`Core.lean`](../A12Kernel/Core.lean) owns endpoint selection, [`DateRangeBound.lean`](../A12Kernel/Elaboration/DateRangeBound.lean) owns direct-field admission, the sole checked read, and both compositions, while [`DateComparison.lean`](../A12Kernel/Semantics/DateComparison.lean) and [`DateNumeric.lean`](../A12Kernel/Semantics/DateNumeric.lean) remain the comparison and component semantic owners.
- `case`: [canonical input cases](../A12Kernel/Conformance/DateRangeInput.lean) and [bound, comparison, and component cases](../A12Kernel/Conformance/DateRangeBound.lean) distinguish all six local policy/configuration pairs, unconfigured `MM` and `MM-dd`, unsupported policy, repeatable placement, and wrong kind.
- `case`: runtime cases distinguish both endpoints, leap/ordinary month completion, exact instant/parts/basis identity, operand position, all four components, all three bound non-value observations, comparison/component empty and unknown, and impossible full-Date projection.
- `proof`: [bound, comparison, and component laws](../A12Kernel/Proofs/DateRangeBound.lean) preserve selected value, empty, validation cause, and computation cause through the checked read, retain exact selected identity, and delegate value, empty, and unavailable behavior to the existing consumers.
- `assurance`: E/P internally closed for the named direct-field, fixed-Date comparison, and component fragment.
- `assurance`: Q closed for bounded internal Execute/Analyze/Explain probes: isolated consumers recovered all six local policy/configuration pairs, source profile, model Base Year, and exact endpoint identity. Canonical queries cover both comparison positions, all four components, non-value behavior, and named exclusions; fragment queries cover their checked comparison/component compositions and leap-aware completion.
- `assurance`: L observes dotted-range start year/month and finish day components and pins direct field admission at [the source checkpoint](SOURCES.md#src-date-range-bound-extraction). `yyyy`, `yyyy-MM`, configured `MM`, and configured `MM-dd` bound extraction are internal compositions over checked completion plus the source-established generic bound mechanism, not retained Kernel observations.
- `assurance`: the retained rows do not directly compare a bound with a full Date or cover quarter, exact direction independently of component choice, instant/basis transport, phase failures, the ISO policy, or any fragment policy.
- `remains`: constructed ranges, wider comparison operands, overlap and wider computation-target assembly, nested consumers, and repeatable addressing remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).
- `remains`: research-closed shipment and public compatibility remain unclaimed; current Q is only the bounded internal probe defined by the [consumer maturation path](IMPLEMENTER-GUIDE.md#consumer-probes-before-shipment).

<a id="cap-stored-date-range-equality"></a>
#### Checked stored-versus-stored DateRange equality

- `state`: implemented for two direct nonrepeatable stored DATE_RANGE fields under `==` or `!=`, across all eight admitted declaration pairs, with or without a model Base Year, using the declaration-level component-set gate and the verdict over the shared exact-or-yearless identity domain.
- `boundary`: admission reads the declared **component set** and nothing else, projected by `DateRangeInputFormat.components` into the existing `TemporalComponents`; the model's Base Year does not enter it, and neither does a declared year interpretation. The eight declarations therefore fall into five classes — full Date, year, year-month, month, month-day — and comparability is exactly equality of that class. Each class's lexical spellings cross freely; every cross-class pair is refused `MVK_INVALID_COMPARE_TO_DATE_RANGE`, symmetrically in the authored order and identically for both operators.
- `boundary`: the verdict reuses `DateRangeCellValue.sameIdentity` and the shared symmetric equality scan, so an admitted crossing compares resolved endpoint instants for an exact class and retained labels for a yearless one, never stored text. An empty operand on either side leaves **both** directions unfired, and formal unavailability on either side is UNKNOWN.
- `route`: [`DateRangeStoredComparison.lean`](../A12Kernel/Elaboration/DateRangeStoredComparison.lean) owns certification and the one-read evaluation; both operands reuse `elaborateDirectDateRange` and `CheckedDirectDateRange.evaluate` from the bound owner, so no second direct-DateRange read or comparison representation exists.
- `owner`: [`DateRangeInput.lean`](../A12Kernel/Elaboration/DateRangeInput.lean) owns the component-set projection beside the profile it reads; [`DateRangeComparison.lean`](../A12Kernel/Semantics/DateRangeComparison.lean) owns the identity relation; [`ScalarEquality.lean`](../A12Kernel/Semantics/ScalarEquality.lean) owns the empty and unknown rules and already proves them once.
- `assurance`: E internally closed for the complete same-component and cross-component relation over the eight declarations, both authored orders on every crossing, both operators on one mismatch, all three crossings at runtime with a discriminating negative each, and the empty and all-empty rows. P adds nothing local: order symmetry and empty suppression are already proved at the shared symmetric scan, and the component gate is `Eq` on a `DecidableEq` record rather than a table needing its own reflexivity and symmetry laws.
- `assurance`: L is exact at the [stored-comparison checkpoint](SOURCES.md#src-date-range-stored-comparison): 24 conditions decided through `rule check` on both grid batches (9 admitted, 15 refused) and four documents observed on both codegen strategies with every row agreeing. The [interpretation-comparison checkpoint](SOURCES.md#src-date-range-interpretation-comparison) adds that a declared year interpretation does not enter the gate and that identical wrapping stored text under `FROM` and `TO` is unequal, so this route admits declarations both overlap operators refuse. The clause addition instantiates the already accepted [SPEC-2026-08-19-01](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-19-01--date-range-equality-requires-matching-component-sets-across-both-operands) and creates no new outbound entry.
- `boundary`: a **semantic-index-selected** operand is admitted on this carrier beside a direct one, in either authored slot. The gate is this record's own declaration-level component set unchanged, applied to the keyed target's certified profile; the verdict is this record's own, taken over the two retained identities. The keyed operand is therefore not a second equality: it supplies one observation to the existing one. The authored order is retained rather than normalized, so an Explain consumer recovers which side was keyed. The direct operand standing beside it keeps its own resolution class, which is what distinguishes an unbound repeatable level from an incomparable profile. [`SemanticIndexDateRange.lean`](../A12Kernel/Elaboration/SemanticIndexDateRange.lean) owns that pairing and the [keyed cases](../A12Kernel/Conformance/SemanticIndexDateRange.lean) lock it; the keyed source itself is described at [the endpoint-component record](#cap-date-range-bound-component-operand).
- `remains`: starred, group, and repeatable operands; a keyed operand on **both** sides, which no measurement covers; a construction on either side, which keeps its own owner; and formally invalid operand rows. All remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-unconfigured-yearless-date-range-bound"></a>
#### Unconfigured yearless DateRange bound extraction and components

- `state`: implemented for direct nonrepeatable yearless DateRange fields in a model with **no** Base Year, both endpoint positions, all four lexical spellings of the two yearless component sets, and the four numeric Date components gated by the declaration's own set.
- `boundary`: extraction is unconditional; the gate is on the consumer. A selected endpoint carries exactly the components its declaration retains, so `MonthFromDate` and `QuarterFromDate` are admitted on a month-only or month-and-day declaration, `DayFromDate` only on the latter, and `YearFromDate` on neither. The refusal is the Kernel's `MVK_WRONG_DATE_FORMAT_FOR_OP` at the extractor.
- `boundary`: the gate is the existing direct extractor admission `DateNumericPart.admittedBy` over the new `DateRangeInputFormat.components` projection, with no Base Year to supplement the year. No second admission table exists, and the projection is shared with stored-versus-stored equality, which reads the same component set for a different purpose.
- `boundary`: a selected yearless endpoint is a retained label, never a manufactured date. Its absent halves are the literal zeroes a partially known Date uses, and the static gate keeps them unobservable. Empty input keeps the shared symmetric-zero projection and formal unavailability keeps its exact cause, both through the one direct DateRange read.
- `route`: [`YearlessDateRangeBound.lean`](../A12Kernel/Elaboration/YearlessDateRangeBound.lean) owns this route and reuses `elaborateDirectDateRange` and `CheckedDirectDateRange.evaluate` from the exact owner, so no second direct-DateRange read exists. A year-bearing declaration or a configured model is refused here as a routing fact and belongs to [the exact bound owner](#cap-checked-date-range-bound); neither refusal carries a Kernel diagnostic, because the Kernel admits both there.
- `owner`: [`DateNumeric.lean`](../A12Kernel/Semantics/DateNumeric.lean) owns extractor admission and the numeric projection; [`DateRangeInput.lean`](../A12Kernel/Elaboration/DateRangeInput.lean) owns the component-set projection.
- `assurance`: E internally closed for the admitted and refused extractor grid over both yearless sets and both spellings, both endpoint positions, the two routing refusals, the diagnostic identity, the filled component values, and the empty and formally invalid projections. P adds nothing local: the empty and unknown projections are already proved at their shared owner and the gate is a reuse of the settled admission predicate.
- `assurance`: L is exact for admission and the filled values at the [unconfigured-bound checkpoint](SOURCES.md#src-date-range-unconfigured-bound). The empty and formally invalid rows are internal compositions with no observation on this route. Pending [SPEC-2026-08-23-02](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-02--an-extracted-daterange-bound-carries-its-declarations-component-set) carries the clause correction.
- `remains`: bound-versus-bound comparison with its measured positional completion, the `MVK_INVALID_COMPARE_TO_DATE` refusal of a full-Date peer, and every repeatable, starred, or group source remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-date-range-bound-pair-comparison"></a>
#### Checked DateRange endpoint-pair comparison

- `state`: implemented for two extracted DateRange endpoints from direct nonrepeatable fields, either endpoint position on each side, all six temporal comparison operators, and both admitted runtime domains: two exact endpoints and two yearless ones.
- `boundary`: admission is the ordinary direct temporal rule `TemporalComparisonOp.admitsFormats` over the two declarations' component sets, so it reads year presence and date class rather than requiring identical sets. Two yearless endpoints therefore cross their component sets, two year-bearing ones do too, and a yearless endpoint against a year-bearing one is refused `MVK_INVALID_COMPARE_TO_DATE` unless the model's Base Year supplies the missing year. Because that gate admits only homogeneous pairs, the runtime never meets a mixed one and keeps it as a defensive fault.
- `boundary`: a yearless endpoint is completed by its authored position before comparison — a month-only start to the first day and a month-only finish to the greatest day that month can ever have, February reaching 29 with no year to decide leapness. The completion reuses `YearlessInterval.ofMonthPair`, so the bound and overlap families cannot disagree about a yearless label's extent. Two exact endpoints compare as resolved full Dates through the established relation.
- `boundary`: the six operators share one ordered relation. `TemporalComparisonOp.holdsOrdered` derives the four order forms from a strict `before` alone, and both the full-Date and the yearless-label relations are specializations, so no domain restates the closed forms and none can disagree about them.
- `route`: [`DateRangeBoundComparison.lean`](../A12Kernel/Elaboration/DateRangeBoundComparison.lean) owns the pair across both bound owners and reuses each owner's own certification and read. Its operand certification prefers the exact owner and falls back to the yearless one only on an unsupported canonical policy, so a genuine source refusal is reported as itself.
- `owner`: [`DateComparison.lean`](../A12Kernel/Semantics/DateComparison.lean) owns the ordered relation and its two specializations; [`DateRangeOverlap.lean`](../A12Kernel/Semantics/DateRangeOverlap.lean) owns the yearless completion; [`TemporalFormat.lean`](../A12Kernel/Semantics/TemporalFormat.lean) owns the admission rule.
- `assurance`: E internally closed for the admitted yearless and year-bearing pairs across operators and positions, the cross-class refusal with its diagnostic, the day-1 start completion with a discriminating negative, the month-end finish completion, the February-29 pair, the empty projection, and the fixed-Date gate in both directions. P owns the coincidence law behind the reused certificate; the ordered relation's laws and the empty suppression are already proved at their shared owners.
- `assurance`: L is exact for admission and the completion rows at the [unconfigured-bound checkpoint](SOURCES.md#src-date-range-unconfigured-bound), including the February pair that separates day 29 from day 28 on otherwise identical documents. Exact-versus-exact runtime and the empty rows are internal compositions with no observation. Pending [SPEC-2026-08-23-02](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-02--an-extracted-daterange-bound-carries-its-declarations-component-set) carries the clause correction.
- `boundary`: comparing an endpoint with a fixed complete date runs through the same admission rule with a full-Date peer, so a source with no year and no Base Year is refused `MVK_INVALID_COMPARE_TO_DATE` at the comparison instead of as a policy insufficiency at extraction. The exact owner's certificate is reused unchanged, and a proved law states why that is sound: direct-bound support and comparability with a complete date are the same condition on a declaration, so no second precondition can drift from the gate.
- `boundary`: a **semantic-index-selected** endpoint is admitted on this carrier beside a direct one, in either authored slot, and it is the one keyed carrier that needs a runtime **domain** decision. The two direct owners' gates are complementary — proved, not assumed — so the keyed operand's domain is a total two-way read of its retained profile rather than a preference, taken from the certificate instead of from which owner certified the operand. This record's admission rule, both completions, and both verdicts are reused unchanged; a domain disagreement between the keyed profile and the direct operand's owner is the same defensive fault two direct endpoints keep, and it fails closed rather than manufacturing a domain. The authored order reaches the fault vocabulary too, so a consumer learns which authored slot failed rather than which operand shape it was. [`SemanticIndexDateRange.lean`](../A12Kernel/Elaboration/SemanticIndexDateRange.lean) owns that pairing; the keyed source is described at [the endpoint-component record](#cap-date-range-bound-component-operand).
- `remains`: repeatable, starred, and group sources, plus a keyed operand on **both** sides, remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-date-range-bound-component-operand"></a>
#### The numeric Date component of a selected DateRange endpoint

- `state`: implemented as one numeric operand atom on three surfaces — validation at both loci, the scalar computation operation, and the repeatable addressed computation placement that computes a row target per instantiated row. `DayFromDate`, `MonthFromDate`, `QuarterFromDate`, and `YearFromDate` accept a DateRange endpoint selection as their source, so the atom carries the field path, the endpoint, and the component together and feeds an ordinary numeric comparison or a Number computation operation.
- `boundary`: two independent gates, and they are kept apart because the family's neighbouring consumers differ. The **locus** gate is the shared unstarred-repeatable-reference rule, so a row operand needs a rule that iterates the row and no wildcard form of this operand exists. The **component** gate is the declared profile's own component set with the model's Base Year supplementing the year alone, which is `DateNumericPart.admittedBy` unchanged from a direct Date field — deliberately *not* the exact-value gate a comparable endpoint carries, so an unconfigured yearless month range exposes month and quarter here while a comparison against a complete date refuses it.
- `boundary`: the runtime read selects from **every** carrier. An exact range yields a complete Date and a yearless one yields exactly the labels its declaration retains, and both project into one calendar-part view, so the component read reuses the established direct-component projection rather than adding an account of emptiness, unavailability, or fillability. Absent substitutes symmetric zero, formal unavailability keeps its exact cause, and only a payload whose kind is not a DateRange stays malformed. The earlier design selected through the exact-value-only carrier, which reported an admitted `MonthFromDate` on an `MM` range as malformed; that divergence is recorded at the checkpoint.
- `owner`: [`DateRangeBoundComponent.lean`](../A12Kernel/Elaboration/DateRangeBoundComponent.lean) owns the cross-carrier endpoint selection, its calendar-part view, the component-exposure rule, and the flat validation read; [`DateRangeBound.lean`](../A12Kernel/Elaboration/DateRangeBound.lean) owns the shared range observation and the nonrepeatable-source fact; [`YearlessDateRangeBound.lean`](../A12Kernel/Elaboration/YearlessDateRangeBound.lean) owns the retained-label carrier the yearless arm projects.
- `owner`: the numeric surfaces carry the atom through [`NumericSource.lean`](../A12Kernel/Elaboration/NumericSource.lean) (surface and resolved constructors, data dependency, scale), [`NumericValidation/Core.lean`](../A12Kernel/Elaboration/NumericValidation/Core.lean) (scope admission, reference, relevance, the exposure refusal and its diagnostic), [`NumericValidation/Resolution.lean`](../A12Kernel/Elaboration/NumericValidation/Resolution.lean) (both scalar and addressed resolution), [`NumericValidation/Evaluation.lean`](../A12Kernel/Elaboration/NumericValidation/Evaluation.lean), [`NumericValidation/Ordered.lean`](../A12Kernel/Elaboration/NumericValidation/Ordered.lean) (addressed relevance field ids), [`ValidationCondition/StaticIteration.lean`](../A12Kernel/Elaboration/ValidationCondition/StaticIteration.lean) (derived iteration scope), and the computation side through [`NumericComputation/Core.lean`](../A12Kernel/Elaboration/NumericComputation/Core.lean), [`NumericComputation/Evaluation.lean`](../A12Kernel/Elaboration/NumericComputation/Evaluation.lean), and [`GeneratedComputationValidation.lean`](../A12Kernel/Elaboration/GeneratedComputationValidation.lean).
- `case`: [endpoint-component cases](../A12Kernel/Conformance/DateRangeBoundComponent.lean) separate the two gates on one model — the complete exposure matrix over all six admitted profiles and all four extractors, the Base-Year supplement with the `DayFromDate` control that stays refused, the locus refusal reported before the exposure gate with its kernel class, both endpoint selections of one exact range, the yearless carrier reaching its retained label at either end, absent and formally unavailable reads, and the computation surface admitting the scalar source while refusing the row one.
- `assurance`: E closed for the static matrix and the runtime projection on both carriers. P states the one non-definitional claim the read depends on: the cross-carrier selection and the yearless owner's own selection cannot disagree about which end an endpoint is, plus the read's factoring into observation then projection and the wrong-kind refusal; the empty, present, and unavailable cases are now generic laws at the component owner and are consumed by specialization. L is exact for every static row at the [endpoint-component checkpoint](SOURCES.md#src-date-range-bound-component), including the computation dry runs and their negative controls, and for the computed values at the [runtime checkpoint](SOURCES.md#src-date-range-bound-component-runtime), where both codegen strategies agreed on every channel: the selected endpoint's component, the unconfigured yearless carrier's retained label, an unplaced range's real zero, and an invalid range's cleared target beside a clean sibling. What stays uncalibrated is the validation-surface runtime read and the day and year component values. Pending [SPEC-2026-08-23-05](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-05--a-declared-base-year-supplements-the-year-component-of-a-selected-daterange-endpoint) carries the clause correction.
- `owner`: the addressed computation placement is [`AddressedDateRangeBoundPart.lean`](../A12Kernel/Elaboration/AddressedDateRangeBoundPart.lean), which adds only the target half — a repeatable Number target at the source's own scope and the measured scale-0 derived-scale gate — over the shared [`AddressedNumericLeaf.lean`](../A12Kernel/Elaboration/AddressedNumericLeaf.lean) placement. It carries no second read: the shared placement routes the atom through the same scalar computation evaluator, so the row read and the component projection are the operand owner's.
- `case`: [addressed endpoint-component cases](../A12Kernel/Conformance/AddressedDateRangeBoundPart.lean) separate the three gates this placement adds and replay the measured runtime rows with exact addresses — both endpoint selections over the same two ranges, the unconfigured yearless carrier reaching its retained label on all three measured pairs, an unplaced row's real zero, an invalid operand reaching the `cleared` channel rather than the error channel while the clean rows still change, the fractional-target refusal, and the nonrepeatable-target and scope refusals.
- `boundary`: the placement admits its source at any scope the target's own scope binds, so a scalar endpoint computing a row Number is ordinary. That widening belongs to the shared placement and landed across every addressed one-source leaf at once; [§5](#5--numbers-and-decimals) owns it and the [operand-scope checkpoint](SOURCES.md#src-computation-operand-scope) owns its measurement.
- `owner`: the **semantic-index-selected** source is [`SemanticIndexDateRange.lean`](../A12Kernel/Elaboration/SemanticIndexDateRange.lean), which adds only the target half over the shared semantic-index certificate — the selected declaration's own certified DateRange profile, plus this component read's exposure witness over it — and reads through that certificate's own generated-preliminary route. The profile is admitted through the family's scope-free `FlatModel.dateRangeSourceProfile`, which every addressed carrier now reaches through its own reading-scope gate: a keyed row arrives from the index column rather than from the reading environment, and that is the whole difference between the two admissions. Both read routes now compose the same `DateNumericPart.fromDateRangeBoundObservation` clause, so keyed and direct reads are structurally unable to disagree about a projected observation; only about which cell they reach.
- `boundary`: the keyed read's result domain is the ordinary phase observation with no new outcome class. A no-match row and a matched-but-empty cell both read empty and reach the same fillable zero, while a duplicated key and a formally invalid matched cell both reach UNKNOWN carrying their own distinct causes. An unresolvable lookup keeps its own error class rather than collapsing, because a consumer must distinguish it from a resolved column that selected nothing; a selected cell of the wrong kind collapses to malformed exactly as a direct read does.
- `case`: [keyed endpoint-component cases](../A12Kernel/Conformance/SemanticIndexDateRange.lean) lock both key forms with a String index over a DateRange target, the exposure gate on an unconfigured yearless target, the wrong-kind target refusal with its deliberately unmapped diagnostic, and the four-state runtime domain where each case perturbs exactly one cell of one baseline.
- `assurance`: E/P closed for the keyed source and all four wired carriers. L is exact for the admission and the four-state result domain at the [endpoint-shape checkpoint](SOURCES.md#src-date-range-endpoint-shapes), which measured the keyed operand accepted on both of them at a nonrepeatable locus, at an iterating one, and when the keyed group is itself iterated. Every local refusal class — a non-DateRange selected target, an unexposed component, and a comparability mismatch — stays **unmapped to a Kernel diagnostic** and is external evidence pending: that batch established which shapes are accepted, not which code a keyed rejection carries.
- `boundary`: the same keyed source serves **stored range equality**, the **endpoint-pair comparison**, and the **overlap predicate**, whose gates and verdicts stay with their own records. All four measured carriers now consume one keyed read, which is what justified promoting the source's certified profile onto its certificate and extracting three shared clauses the direct routes now use too: the yearless endpoint projection, the overlap year rule, and the overlap slot projection.
- `remains`: no measured keyed carrier is open. What stays unrepresented is a keyed slot inside the overlap operator's own list beside a starred, filtered, or group slot, and a keyed operand on **both** sides of any carrier; neither is measured. Starred and group endpoint sources remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-iterated-date-range-conditions"></a>
#### DateRange conditions read at the rule's iterating row

- `state`: implemented for nine carriers at **either** locus — stored-versus-stored equality, one selected endpoint against a fixed complete date, a pair of selected endpoints in either value domain, both overlap operators in **both** value domains, and a constructed range against either a stored range or another construction — in both authored slots and at every comparison polarity each carrier admits.
- `boundary`: admission is the locus rule stated positively. The reading scope is the rule group's own repeatable scope, and an operand is accepted exactly when that scope binds every repeatable level it crosses, so the nonrepeatable read is the `[]` instance of one predicate rather than a separate rule. An operand crossing an unbound level is still reported as a repeatable reference, which is the class that reports `MVK_NO_WILDCARD`, and a condition with no repeatable operand is refused because it belongs to the scalar carriers.
- `boundary`: the nine carriers share one leaf family and one read shape, so the checked condition tree carries a single arm per dispatch and a further carrier lands as a member rather than as another set of arms. Each member keeps its own comparison: the equality's component gate and identity comparison, the fixed-date member's authored-side rule, the pair's ordinary `admitsFormats` comparability, each overlap member's whole-list gates including the group refusal, the uniform-year rule, and the plural operator's own scalar-shape and cross-side rules, and the construction member's endpoint kind, profile, and cross-operand component gates. The cell read is a parameter of the shared verdict function, because its failure channel belongs to the enclosing leaf; the overlap member additionally receives the document, because a starred operand's extent is a stream of rows rather than one cell. Every locus refusal is reported as the same resolution failure across members, so a consumer reads one class rather than one per carrier.
- `boundary`: what the leaf owns is the row read, and every member performs it through its own carrier's read rather than a leaf-local one. Four shared mechanisms were widened by a reading-scope or environment parameter whose empty instance is the previous behaviour exactly: the entity-list shape resolver, which admits an unstarred operand the scope binds; the direct entity-operand read; the temporal target policy, which also retains its declaration so a row-reading consumer can see the scope it must satisfy; and the DateRange source read itself, which now addresses at an environment while the root read stays a plain root read sharing one payload projection with it. Absent and formally unavailable operands keep their carriers' behaviour; a payload of the wrong kind collapses to UNKNOWN or to a skipped overlap slot rather than claiming a fault channel, and the certificates' kind, policy, and exact-value gates make those collapses unreachable.
- `boundary`: the leaf contributes its operands' declaration scopes to the derived iteration scope through the shared merge, requires the addressed evaluator, returns UNKNOWN on the scalar route rather than reading row one, and projects its operands as message pointers in authored order. Its iteration guard is positive at every polarity — measured, not assumed: the same model refuses `FieldNotFilled` on the same operand with `MVK_NEG_CONDITION_IN_ITERATION` and admits its guarded form, so the gate's reach is presence-shaped rather than polarity-shaped.
- `owner`: [`IteratedDateRangeCondition.lean`](../A12Kernel/Elaboration/IteratedDateRangeCondition.lean) owns the shared member set, its refusal classes, and the read-parameterized verdict; [`DateRangeBound.lean`](../A12Kernel/Elaboration/DateRangeBound.lean) owns the scope-bound admission projection, the iterated source and endpoint certificates, and the authored-side comparison now shared with the scalar carrier; [`DateRangeStoredComparison.lean`](../A12Kernel/Elaboration/DateRangeStoredComparison.lean) owns the equality pair; [`Flat/Model.lean`](../A12Kernel/Elaboration/Flat/Model.lean) owns the scope-binding predicate and its resolver.
- `owner`: the condition tree carries the leaf across [`Core.lean`](../A12Kernel/Elaboration/ValidationCondition/Core.lean) (construction and static admission), [`Evaluation.lean`](../A12Kernel/Elaboration/ValidationCondition/Evaluation.lean) (the row read), [`Iteration.lean`](../A12Kernel/Elaboration/ValidationCondition/Iteration.lean) and [`StaticIteration.lean`](../A12Kernel/Elaboration/ValidationCondition/StaticIteration.lean) (guard and derived scope), [`Reference.lean`](../A12Kernel/Elaboration/ValidationCondition/Reference.lean) (message pointers), and [`Assembly.lean`](../A12Kernel/Elaboration/ValidationCondition/Assembly.lean) (one locus-checked step behind three member front ends).
- `case`: the locks are split by value domain: [resolved-date cases](../A12Kernel/Conformance/IteratedDateRangeCondition.lean) and [retained-label cases](../A12Kernel/Conformance/IteratedYearlessDateRangeCondition.lean). Together they separate both loci on every carrier, every member at a non-iterating locus with its gates intact, both slots, both authored construction positions, every admitted polarity, the scalar-only refusal, the per-row construction from each row's own endpoints, the component and comparability gates with an admitted lexical crossing, the overlap member's unchanged multiplicity/duplicate/group gates, the star-only refusal that keeps one owner per condition, the derived scope with the addressed-route requirement, the Explain-facing row pointer, per-row verdicts over a two-row document, both endpoint selections of one row, the mixed bare-plus-star self-pair, and the empty-operand rows.
- `assurance`: E closed for the nine admitted shapes at both loci, including the unconfigured yearless endpoint pair and both label-domain overlap operators whose labels stay retained rather than completed; P states the one law the row read needed — a root-read endpoint's address does not depend on its environment, which is what lets the four endpoint laws rewrite past the address computation. L is exact for the static rows at the [rule-locus checkpoint](SOURCES.md#src-date-range-rule-locus), including the endpoint and overlap shapes, the mixed bare-plus-star admission, the polarity matrix, and its live negative control; every per-row runtime row is an internal composition of two separately calibrated mechanisms with no observation of its own, and the mixed shape's self-pair verdict follows the canonical clause rather than an observation.
- `boundary`: the family has one source certificate. `CheckedDateRangeSource` carries the reading scope, the declaration, and the checked profile; the nonrepeatable `CheckedDirectDateRange` is that certificate refined by a proof that the scope is empty, so a scalar carrier coerces into the general one and an iterated consumer accepts a scalar operand without a second certificate. Both admit through the one shared projection, so no two carriers can disagree about a declaration's profile.
- `boundary`: the locus may iterate **two** repeatable levels, and then it admits an operand at the root, at the enclosing row, or at the leaf, each read at its own path inside the current leaf. The correlating middle-level operand reaches its own enclosing row's cell, which the [nested-locus cases](../A12Kernel/Conformance/NestedIteratedDateRangeCondition.lean) separate from a borrowed row-one read by inverting the verdict pattern rather than shifting it; the [nested-locus checkpoint](SOURCES.md#src-nested-rule-locus) Kernel-locks both the admission matrix and that runtime pattern on either codegen strategy, including the `referenced` channel naming the second row's own operand. The mechanism needed no change: the leaf already carried its locus scope as a parameter and read every operand at its own declared scope.
- `boundary`: this leaf is the DateRange condition family's **only** rule-level owner. Before it, every overlap and comparison source was a component-level capability with conformance and laws but no condition-tree carrier, so no assembled rule could reach one. It therefore admits an all-scalar condition as well, deriving no iteration there, rather than refusing the scalar shape into unreachability. Neither unconfigured yearless overlap operator had a whole-source elaborator at all; both now share the same shape gates as their exact counterparts, with the plural one keeping the operator's own scalar-shape rule.
- `boundary`: an overlap member reports rather than drops an impossible payload. The family's evaluation carries three payload classes and an incoherent-extent class that no checked certificate can produce, and they now project into one shared addressing class instead of collapsing to a skipped slot, so the standalone and leaf routes give one account of one certificate. The earlier collapse was the divergence this replaced.
- `boundary`: the family's certificate chain is one line, not two. A DateRange source carries its reading scope; the nonrepeatable source is that certificate refined by its declaration crossing no repeatable level; the general endpoint extends the source; and the root-reading endpoint is that endpoint refined the same way. Each refinement's read is a plain root read, so no consumer pays for an environment it does not have, while every scope-carrying consumer reuses the same selection and comparison.
- `consolidation`: one duplication remains. The mixed construction-versus-stored carrier is still paired locally by the iterated member rather than reused, because its stored half is typed as the scalar refinement; retyping it to the shared source certificate and reading at an environment would delete the local pairing. That is an ordinary follow-on unit, not a blocker.
- `remains`: the numeric-component extraction of a row-read endpoint, nested repeatable chains, filtered star operands beside a row read, and partial-view evaluation for this leaf remain under [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-date-range-repeatable-operand-locus"></a>
#### Repeatable-operand rule locus across the DateRange carriers

- `state`: implemented for the refused half only. An unstarred repeatable DateRange operand read from a non-iterating rule locus is refused and now reports `MVK_NO_WILDCARD` on every carrier: the overlap predicate, stored range equality, exact and yearless endpoint extraction, the endpoint pair, and the two-endpoint construction.
- `boundary`: the class is owned once by `ResolveError.diagnostic?` at the resolver's own error type and inherited by the shared entity-list checker, the direct DateRange owner, and the construction endpoint, so a new carrier acquires it by delegating rather than by restating the row. Every other resolution failure still projects `none`, which keeps uncovered classes visibly uncovered.
- `boundary`: the **admitted** inside-locus half is not represented. These carriers are checked condition owners outside the iterating validation-condition tree, whose leaf families are the only ones that derive a rule iteration scope, so admitting the operand at elaboration would produce a condition the evaluator cannot read at a row. Widening the shared resolver without that evaluation path is therefore refused rather than staged.
- `owner`: [`Flat/Types.lean`](../A12Kernel/Elaboration/Flat/Types.lean) owns the projection; [`FieldEntityList.lean`](../A12Kernel/Elaboration/FieldEntityList.lean), [`DateRangeBound.lean`](../A12Kernel/Elaboration/DateRangeBound.lean), and [`DateRangeConstructionComparison.lean`](../A12Kernel/Elaboration/DateRangeConstructionComparison.lean) delegate to it.
- `case`: [rule-locus cases](../A12Kernel/Conformance/DateRangeRepeatableOperand.lean) pin the reported class on the overlap predicate in either slot, stored equality from either side, an exact endpoint, a yearless endpoint whose format and Base-Year gates would otherwise refuse the same field, and the endpoint pair, each against an admitted scalar control; the [construction-operand cases](../A12Kernel/Conformance/DateRangeConstructionOperand.lean) keep the construction row with their own grid.
- `assurance`: E closed for the refused half across all carriers; P none, since the projection is a total case split with no law worth stating separately. L is exact for the ten measured rows and the gate-order separator at the [rule-locus checkpoint](SOURCES.md#src-date-range-rule-locus); the admitted inside-locus rows are measured but unrepresented. Pending [SPEC-2026-08-23-04](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-04--the-operand-locus-gate-decides-before-the-error-field-gate) carries the gate order and the carrier breadth outbound.
- `remains`: admitting the operand from an iterating locus remains under [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion), with the embeddability blocker recorded there.

<a id="cap-temporal-comparison-and-aggregates"></a>
#### Temporal comparison and aggregates

- `state`: implemented for the named admitted families.
- `boundary`: Date, Time, and DateTime own six-way scalar comparison; admitted temporal aggregate and DateRange overlap owners retain their existing value domains.
- `boundary`: checked DateRange construction equality compares either two admitted constructions or one construction with one matching direct stored range under `==` or `!=`; the mixed pair additionally preserves whether the construction was authored left or right.
- `boundary`: checked construction-pair admission accepts the two nonrepeatable full-Date profiles, exact `yyyy` and `yyyy-MM` DateFragment profiles, and same-profile `MM` or `MM-dd` pairs with or without a model Base Year. It requires matching components within each construction and across compared constructions.
- `boundary`: construction execution completes `yyyy` to January 1 or December 31, `yyyy-MM` and Base-Year-resolved `MM` to day 1 or the leap-aware month end, and Base-Year-resolved `MM-dd` to its stated day at either position. It resolves completed labels under the checked model-zone profile rather than carrying incoming source years or instants.
- `boundary`: without Base Year, `MM` retains only each valid authored month and `MM-dd` retains only each valid authored month/day pair. No year or instant is synthesized; impossible decoded component payloads remain structural endpoint faults.
- `boundary`: the checked result retains all four typed endpoint observations. A formal endpoint makes the verdict UNKNOWN before empty suppression; otherwise an empty endpoint is not evaluated, and two present ranges delegate their exact-or-yearless identities to the shared symmetric equality seam.
- `boundary`: checked construction-versus-stored admission accepts full Date against an exact stored range and every DateFragment profile against its matching stored profile, with or without Base Year where applicable. Year-bearing and configured fragments complete both operands to exact values; no-Base-Year fragments retain component-only identity.
- `boundary`: the shared direct source retains declaration policy plus input profile; bound consumers refine it to an exact-valued profile.
- `boundary`: mixed execution reads both operands once from the same immutable document in authored order and retains the construction observation, stored `DateRangeCellValue`, authored side, and verdict.
- `boundary`: the total `DateRangeCellValue` equality seam compares exact ranges by both endpoint instants, yearless `MM` and `MM-dd` ranges by both ordered component pairs, and distinct profiles as unequal. Checked consumers own same-profile admission.
- `boundary`: checked singular `DateRangesOverlap` source admission reuses the shared entity-list shape and certifies canonical full-year policies for every direct or starred operand. Direct fields additionally admit exact-valued `yyyy`/`yyyy-MM` and Base-Year-completed `MM`/`MM-dd` profiles; fragment stars and every plural fragment position remain refused. A measured full-year/yearless direct pair projects `MVK_DATE_WITH_AND_WITHOUT_YEAR`.
- `boundary`: a **semantic-index-selected** operand overlaps a direct one, in either authored order, through [`SemanticIndexDateRange.lean`](../A12Kernel/Elaboration/SemanticIndexDateRange.lean). It is a **pair**, not a keyed slot in the operator's list: the measured authoring is one keyed operand against one direct range, and a slot inside the list would have to thread an index preliminary through the shared addressed resolution that starred, filtered, and group slots use. The pair reuses this operator's two declaration gates, its uniform-year rule, its slot projection, and its pure any-pair scan, so it adds an operand shape rather than a predicate; the year rule is now one shared profile-level clause consumed by both the list gate and this pair. Absence, no match, formal invalidity, and a duplicated key each skip their own slot, so the predicate stays two-valued while the column reports its own unavailability. The authored order is retained for Explain but proved not to change this verdict, unlike the directional endpoint comparison. Every refusal reached through the shared certifier stays unmapped here, because those codes are measured for a direct operand rather than a keyed one.
- `boundary`: checked plural `AtLeastOneDateRangeOverlaps` admission validates the one direct scalar shape before reading its nonempty list, then resolves both sides through one shared entity-list shape so exact duplicate and overlap gates span the boundary without making the scalar a compared list member. The scalar and each direct list operand admit the exact canonical policy or one Base-Year-resolved fragment profile, reusing the singular route's certificate; the uniform-year gate spans scalar and list together, so its two laws are stated once at the shared predicate rather than restated per operator. Starred, filtered, group, and unconfigured yearless list operands stay canonical-only.
- `boundary`: the plural list admits direct, plain-star, filtered-star, fixed-group, and starred-group operands; a group retains its authored source plus a complete nonempty recursively certified DateRange expansion, and mixed expansions fail rather than being filtered.
- `boundary`: the exact measured starred-scalar reversal projects to `MVK_INVALID_PARAMETER_FOR_DATE_RANGE_COMPARISON`, the measured direct Number/Number singleton pair projects to `MVK_NO_DATE_RANGE`, shared shape classes retain their projections, and isolated kind mismatches or other unmeasured plural refusals remain unmapped.
- `boundary`: singular full-validation assembly resolves every authored slot through one immutable checked document, retains concrete addresses and topology beside ordered skipped/kept occurrences, and derives its verdict through the existing reached-filter any-pair scan.
- `boundary`: plural full-validation resolves the scalar first; a skipped scalar leaves every list operand unread, while a kept scalar resolves authored list operands only through the first overlap. The result retains the complete checked source separately from the reached prefix.
- `boundary`: each reached plural list operand retains its field/group boundary, filter provenance, concrete addresses, and complete recursive group extent. Group cells use the project-normalized declaration-major then canonical-row order, and verdict delegates to the existing scalar-versus-list scan.
- `owner`: scalar temporal owners plus [`DateRangeComparison.lean`](../A12Kernel/Semantics/DateRangeComparison.lean) own comparison; [`DateRangeConstructionComparison.lean`](../A12Kernel/Elaboration/DateRangeConstructionComparison.lean) owns checked construction-pair and mixed admission, label resolution, rich results, and verdict assembly.
- `owner`: [`DateRangeBound.lean`](../A12Kernel/Elaboration/DateRangeBound.lean) owns the shared canonical direct-DateRange read; [`DateRangeOverlap.lean`](../A12Kernel/Elaboration/DateRangeOverlap.lean) owns singular overlap assembly plus the certification steps and uniform-year gate its plural sibling reuses; [`AtLeastOneDateRangeOverlap.lean`](../A12Kernel/Elaboration/AtLeastOneDateRangeOverlap.lean) owns the plural scalar-versus-list family.
- `case`: [DateRange construction equality cases](../A12Kernel/Conformance/DateRangeComparison.lean), [exact-or-yearless value identity cases](../A12Kernel/Conformance/DateRangeCellEquality.lean), [general checked overlap cases](../A12Kernel/Conformance/DateRangeOverlapOperators.lean), and [direct fragment-overlap cases](../A12Kernel/Conformance/DateRangeFragmentOverlap.lean).
- `proof`: [DateRange construction equality laws](../A12Kernel/Proofs/DateRangeComparison.lean), [direct-read and exact-bound laws](../A12Kernel/Proofs/DateRangeBound.lean), [checked policy plus resolved overlap laws](../A12Kernel/Proofs/DateRangeOverlapOperators.lean), and [overlap year-class admission laws](../A12Kernel/Proofs/DateRangeOverlapAdmission.lean).
- `assurance`: E/P closed for the shared exact-or-yearless DateRange identity and checked construction-pair execution across full Date plus all four DateFragment profiles. E is closed for matching full-Date and DateFragment mixed execution with or without Base Year; value comparison delegates to the proved shared identity seam, and direct read preserves every admitted cell profile.
- `assurance`: the named scalar, aggregate, overlap, static overlap-source, and full-validation overlap boundaries remain closed. L is exact for the plural year-class gate, its fragment operands on either side, and the scalar-first termination rule at the [plural checkpoint](SOURCES.md#src-date-range-plural-year-class); its authored lists are one element long, so no retained row separates scalar-versus-list from any-pair or exercises matched-container polarity.
- `assurance`: construction cases separate `yyyy` start/finish completion, leap-aware `yyyy-MM` completion, configured 2024/2023 `MM` and `MM-dd` labels, configured-year and yearless component invalidity, Base-Year override of incoming year/instant, component-only yearless identity, start-only and finish-only changes, empty/formal precedence, cross-profile refusal, exact observations, and authored side.
- `assurance`: the construction law proves symmetry, while the generic scalar law owns formal dominance.
- `assurance`: L for the exact dotted/dash filled checked construction-versus-stored equality and inequality rows is pinned by [its source checkpoint](SOURCES.md#src-date-range-construction-equality); ISO/slash mixed execution is internal composition over the separately checked stored-input policy.
- `assurance`: full-Date construction-pair reachability and all four direct DateFragment profiles are Kernel-confirmed. The `yyyy` and `yyyy-MM` rows pin asymmetric completion and mixed verdicts; configured 2024/2023 `MM` and `MM-dd` rows pin earliest/latest completion and mixed comparison; no-Base-Year rows pin pair and mixed comparison over a distinct yearless boundary.
- `assurance`: reviewed a12-dmkits `df637d7db9f918e132ccbcc2faf1e36cd4c45bb6` closes the former year-bearing and equal-`MM` mixed divergences through the shared finish-position completion route.
- `assurance`: full-Date construction-pair runtime rows and mixed empty/formal rows remain external evidence pending, and C remains none.
- `assurance`: the singular group refusal and plural starred-group admission are pinned by the [group-carrier sweep](SOURCES.md#src-group-carrier-admission-sweep), while plural fixed-group admission and both group execution forms are pinned by the [focused checkpoint](SOURCES.md#src-date-range-plural-overlap).
- `assurance`: singular direct `yyyy-MM` overlap is L-closed for leap/nonleap latest-day and next-day polarity at the [year-month checkpoint](SOURCES.md#src-date-range-year-month-fragment-construction). Configured `MM`/`MM-dd` boundaries and the without-Base-Year diagnostic are L-closed at the [Base-Year checkpoint](SOURCES.md#src-date-range-base-year-fragment-construction). Direct `yyyy` overlap is internally closed but remains external evidence pending.
- `assurance`: plural runtime L covers the exact full-validation dotted/dash policies and recursive group extent; ISO/slash runtime and declaration-major group trace order remain internal composition. Mixed-presentation admission beyond the measured fragment rows remains externally unverified, and C/X remain none.
- `assurance`: Q closed for bounded internal Execute/Analyze/Explain probes. Singular probes recovered authored order, topology, skipped/kept identities, duplicate addresses, local filter provenance, `.year`/`.yearMonth`/configured yearless source classification, exact cached fragment identity, the concrete Base Year, static diagnostic code, and the any-pair verdict.
- `assurance`: construction-pair and mixed Execute/Analyze/Explain queries recover typed endpoint observations, stored identity, authored side, and verdict for admitted fragment profiles. They distinguish endpoint-position completion and Base-Year substitution from incoming source identity, retain yearless components without synthesizing an instant, and keep component-mismatched profiles at static elaboration.
- `assurance`: the mixed Execute/Analyze/Explain probes reconstruct authored order and side, exact and component-only stored identity, both construction endpoints, formal-before-empty precedence, leap-aware year-month completion, and same-profile static admission from canonical records plus the checked operation and rich result.
- `assurance`: they reject fixed-day or one-endpoint comparison and cross-profile widening without renewed source archaeology; wider external research, shipment, and public compatibility remain unclaimed.
- `assurance`: the plural probe recovered complete checked source versus reached prefix, scalar-first unread-list termination, first-match polarity, field/group identities, concrete group extent, and the explicit project-normalized trace limit without external A12 research.
- `remains`: exact construction-operand diagnostics and rule-locus admission, partial-validation and rule/message integration, wider computation targets, the per-row scalar-plus-star overlap shape, non-stored operand read forms, and wider repeatable overlap execution remain in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).
- `remains`: research-closed shipment and public compatibility remain unclaimed; current Q is only the bounded internal probe defined by the [consumer maturation path](IMPLEMENTER-GUIDE.md#consumer-probes-before-shipment).

### §7 — strings and patterns

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| String value and length | Decoded/evaluated text, equality, CRLF normalization, and UTF-16 length. The flat `Length` surface retains each numeric literal's exact rational value and authored scale in either operand order; ordinary ordering lowering intentionally projects only the value. The raw final-rule elaborator recognizes the two strict whole-rule forms, admits exactly decimal-point-free signed-32-bit bounds including trailing-zero integer tokens, eliminates them to maximum-length metadata, and projects bound-domain versus shape refusal to the two exact measured Kernel classes without mapping unrelated elaboration failures | [`String.lean`](../A12Kernel/Semantics/String.lean), [`Flat/Types.lean`](../A12Kernel/Elaboration/Flat/Types.lean), [`Flat/Condition/Surface.lean`](../A12Kernel/Elaboration/Flat/Condition/Surface.lean), [`RawString.lean`](../A12Kernel/Elaboration/RawString.lean), [`StaticDiagnostic.lean`](../A12Kernel/Elaboration/StaticDiagnostic.lean) | E/P closed for the exact raw final-rule bound, diagnostic projection, decoded-literal identity, and ordinary String control; clean dmtool `0.12.1` at a12-dmkits `cd41ea94` Kernel-confirms the 13-case boundary plus direct trailing-zero and diagnostic-precedence controls; selected C | Complete ingestion/Unicode: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion). Wider static-diagnostic coverage: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Pattern and charset admission | Finite built-in Java-pattern profiles, injected model-complete matchers, legal-character membership, and exact condition-pattern diagnostics for every represented non-String left kind plus both pattern-admission stages. Unmeasured local refusals project to `none`; the canonical right-slot field diagnostic has no local projection because the checked surface requires a String constant structurally | [`StringPattern.lean`](../A12Kernel/Semantics/StringPattern.lean), [`StringPattern.lean`](../A12Kernel/Elaboration/StringPattern.lean), [`LegalCharset.lean`](../A12Kernel/Semantics/LegalCharset.lean) | E/P closed for stated profiles; clean dmtool `0.12.1` at a12-dmkits `cd41ea94` Kernel-confirms the 22-case accepted, invalid-source, complete left-kind, and wrong-right-slot matrix for both operators; runtime L partial; C none | Arbitrary pattern/custom integration and repeatable pattern operands: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) |
| String target policy | Declaration-owned line-break, pattern, minimum/maximum length, attempted payload, and source-relative delta | [`StringFieldPolicy.lean`](../A12Kernel/Semantics/StringFieldPolicy.lean), [`StringComputation.lean`](../A12Kernel/Semantics/StringComputation.lean) | E/P closed for admitted target; root subset C | Wider target/ingestion: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) |
| String aggregates/value lists | Direct/plain-star/filtered-star String entity lists, membership, quantifiers, token counts, and mixed first-filled selection | [`ValueList.lean`](../A12Kernel/Semantics/ValueList.lean), [`TokenEntityValueList.lean`](../A12Kernel/Elaboration/TokenEntityValueList.lean), token count/first-filled owners | E/P closed for named shapes; upstream L partial; C none | Remaining list shapes: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion), [SG8](SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion) |
| String computation execution | Checked expression/table, same-target encounter-order flattening, finite supplied-order run, overlay, relation, rich result, direct-versus-retained application, direct-cascade specialization, typed nonrepeatable Number `FieldValueAsString` reads, checked String `FieldValueAsNumber` reads, both bounded completed Number/String overlays through the immutable checked document, one repeatable `FieldValueAsString` placement over exact checked addresses, and exact target diagnostic projection for measured root direct-copy and `RangeAsString` self-reference | [`StringComputation.lean`](../A12Kernel/Elaboration/StringComputation.lean), [`StringComputationTable.lean`](../A12Kernel/Elaboration/StringComputationTable.lean), [`StringComputationRunPlan.lean`](../A12Kernel/Elaboration/StringComputationRunPlan.lean), [`StringComputationRun.lean`](../A12Kernel/Elaboration/StringComputationRun.lean), [`NumberToStringComputationRun.lean`](../A12Kernel/Elaboration/NumberToStringComputationRun.lean), [`StringToNumberComputationRun.lean`](../A12Kernel/Elaboration/StringToNumberComputationRun.lean), [`AddressedFieldValueAsString.lean`](../A12Kernel/Elaboration/AddressedFieldValueAsString.lean), result/application owners | E/P closed for the named nonrepeatable String surface, both bounded Number/String directions, and the addressed repeatable leaf. Clean dmtool `0.13.0` at a12-dmkits `93771ee2` Kernel-confirms the direct-copy and root-range target diagnostic plus a persisted accepted other-field control; wider target reads retain local refusal and no external projection. The addressed matrix separates two row keys, decimal-versus-String Number storage, absence/present-empty, local formal poison, target rejection, source-relative clearing, and application into an absent destination; a12-dmkits `f6dc22d2` supplies broad source-owned authorability and three-engine route execution, not a focused lock for that exact matrix. Root/direct-cascade C; bounded Number/String target-diagnostic Q; composed cross-family execution C none | Wider cross-family execution: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition). Remaining repeatable String expressions and conversions: [SG5](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) and [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) |

### §8 — enumerations and value lists

<a id="resolved-firstfilledvalue"></a>

<a id="cap-first-filled-value"></a>
#### Resolved `FirstFilledValue`

- `boundary`: ordered mixed String/stored-Enumeration selection retains omitted-tail and kept-successor behavior.
- `boundary`: fixed Boolean or Custom targets consume one direct single-level starred field of the same carrier. DateFragment admits matching target/source declarations for exactly `MM`, `yyyy`, `yyyy-MM`, or `MM-dd`; its checked-token path excludes the opt-in pre-1900 check.
- `boundary`: the full-Date computation admits matching fixed `yyyy-MM-dd` or `dd.MM.yyyy` target/source declarations over one direct single-level star, selects typed instant identity, and delegates model-zone rendering and target checks to the existing checked full-Date policy; crossed formats, an optional pre-1900 policy on either declaration, and source-token copying remain excluded.
- `boundary`: the fixed `HH:mm:ss` Time computation selects clock identity from one direct single-level star, discards the runtime transport instant, then delegates rendering to the existing checked Time target; it does not claim source-token copying.
- `boundary`: the fixed `yyyy-MM-dd'T'HH:mm:ss` DateTime computation selects exact-instant identity from one direct single-level star, ignores source text and cached wall-label components, then delegates model-zone rendering to the existing checked DateTime target; that target admits dotted and ISO whole-second formats.
- `boundary`: the DateRange computation admits all eight declaration pairs as target and direct single-level starred source, and accepts any source whose declared component set equals the target's, so both full-Date spellings, both month-only spellings, and both day-and-month spellings cross in either direction. It retains exact `DateRangeValue` or unconfigured yearless identity through declaration-owned rendering.
- `boundary`: the DateRange computation also admits a finite list of at least two direct nonrepeatable same-group sources. Two gates apply in order, and they are not the same gate: every source must repeat the **identical** declared format, refused `varyingProfiles` with the Kernel's `MVK_VARYING_TYPES_NOT_ALLOWED` even where the component sets agree, and only then must the one shared source format expose the **target's** component set, refused `targetProfileNotComparable` with `MVK_INVALID_COMPARE_TO_DATE_RANGE`. The lazy recursive scan retains exact or yearless component identity, and the **target's** own format renders the selected value, so an admitted crossing stores the target spelling.
- `boundary`: all seven bounded computation families project no-value on exhaustion and poison on reached formal invalidity. Boolean retains its typed result; Custom and DateFragment retain exact tokens; full Date and DateTime retain an instant; Time retains a `TimeOfDay`; DateRange retains exact endpoints or yearless components until target rendering.
- `owner`: [`FirstFilledValue.lean`](../A12Kernel/Semantics/FirstFilledValue.lean) owns the ordered core; [`FirstFilledStarSource.lean`](../A12Kernel/Elaboration/FirstFilledStarSource.lean) and [`TemporalFirstFilledStarComputation.lean`](../A12Kernel/Elaboration/TemporalFirstFilledStarComputation.lean) own the direct-star and typed temporal-carrier certificates; [`FieldEntityList.lean`](../A12Kernel/Elaboration/FieldEntityList.lean) owns the finite direct-list cardinality, duplicate, and overlap shape.
- `owner`: [`DateRangeTargetPresentation.lean`](../A12Kernel/Elaboration/DateRangeTargetPresentation.lean) owns shared exact and fragment DateRange presentation for the constructor and direct-star computation paths.
- `owner`: the bounded computation adapters are [Boolean](../A12Kernel/Elaboration/BooleanFirstFilledComputation.lean), [Custom](../A12Kernel/Elaboration/CustomFirstFilledComputation.lean), [DateFragment](../A12Kernel/Elaboration/DateFragmentFirstFilledComputation.lean), [full Date](../A12Kernel/Elaboration/FullDateFirstFilledComputation.lean), [Time](../A12Kernel/Elaboration/TimeFirstFilledComputation.lean), [DateTime](../A12Kernel/Elaboration/DateTimeFirstFilledComputation.lean), and [DateRange](../A12Kernel/Elaboration/DateRangeFirstFilledComputation.lean).
- `case`: [Boolean](../A12Kernel/Conformance/BooleanFirstFilledComputation.lean), [Custom](../A12Kernel/Conformance/CustomFirstFilledComputation.lean), and [DateFragment](../A12Kernel/Conformance/DateFragmentFirstFilledComputation.lean) separate carrier admission, no-value/value, and formal-order branches.
- `case`: [full Date](../A12Kernel/Conformance/FullDateFirstFilledComputation.lean), [Time](../A12Kernel/Conformance/TimeFirstFilledComputation.lean), [DateTime](../A12Kernel/Conformance/DateTimeFirstFilledComputation.lean), and [DateRange](../A12Kernel/Conformance/DateRangeFirstFilledComputation.lean) separate typed payloads, format policy, no-value/value, and formal-order branches.
- `proof`: [Boolean](../A12Kernel/Proofs/BooleanFirstFilledComputation.lean) specializes the shared scan; [Custom](../A12Kernel/Proofs/CustomFirstFilledComputation.lean) preserves accepted bytes and registered rejection; [DateFragment](../A12Kernel/Proofs/DateFragmentFirstFilledComputation.lean) preserves the checked output token and formal cause.
- `proof`: [full Date](../A12Kernel/Proofs/FullDateFirstFilledComputation.lean) preserves exact instant/cause identity and proves that a present or poisoned head hides every suffix. Existing Number/token proofs retain the general prefix and lookahead laws.
- `proof`: [Time](../A12Kernel/Proofs/TimeFirstFilledComputation.lean) preserves exact clock and cause identity; its executable cases lock empty-prefix continuation and present/poison prefix termination without adding operator-named restatements of the shared scan law.
- `proof`: [DateTime](../A12Kernel/Proofs/DateTimeFirstFilledComputation.lean) preserves exact instant and cause identity; its executable cases distinguish source text, cached wall label, model-zone rendering, and ordered scan terminals.
- `proof`: [DateRange](../A12Kernel/Proofs/DateRangeFirstFilledComputation.lean) preserves exact-or-yearless cell and cause identity and proves finite-list exhaustion, empty-head recursion, and value/formal prefix termination for the lazy direct-source scan.
- `assurance`: E/P closed for the named mixed selection and bounded Boolean/Custom computations; a12-dmkits `feba4552` locks their empty/value outcomes through all three engines.
- `assurance`: E/P internally closed for all four exact DateFragment formats, including exact-token selection, crossed-format and unsupported-policy refusal, and the shared clear/poison branches. Static Kernel admission covers all four formats; direct runtime calibration remains exact only for `MM`.
- `assurance`: E/P internally closed for both exact full-Date formats, fixed `HH:mm:ss` Time, and fixed `yyyy-MM-dd'T'HH:mm:ss` DateTime, including typed selection, target rendering/checking, exact value/clear application, crossed full-Date formats, optional-check exclusion, and the remaining named admission refusals. External full-Date `FirstFilledValue` runtime calibration remains exact only for `yyyy-MM-dd`; dotted target rendering is independently tri-engine locked while its direct composition remains external evidence pending.
- `assurance`: E/P internally closed for all six same-policy direct-star and finite direct-field-list DateRange inputs, including exact-or-yearless checked-cell selection, lazy recursive empty/value/formal order, declaration-owned rendering, exact value/clear application, cross-component and target-self-reference refusal, and independent unsupported format/separator refusal.
- `assurance`: external static admission and Kernel/interpreter runtime calibration cover all eight direct-star DateRange declaration pairs at a12-dmkits `52ce932d`, including the full-Date lexical cross, and Lean executes all eight with every within-set crossing. Neither direct-list nor constructor-target evidence widens with this result.
- `assurance`: the exact full-Date lexical cross is externally admitted and locally represented on both the starred and the direct-list route, the latter pinned at the [list-crossing checkpoint](SOURCES.md#src-date-range-list-crossing) together with its distinct source gate and its rendered output. Universal resolved-endpoint refusal remains owned by the value bridge rather than an authorable checked-input case.
- `assurance`: L covers the maintained dynamic-Kernel/interpreter agreement rows and exact four-carrier clear/value calibration.
- `assurance`: the [fragment DateRange checkpoint](SOURCES.md#src-date-range-fragment-first-filled) and [reviewed reconciliation checkpoint](SOURCES.md#src-date-range-2026-08-19-reconciliation) lock all-eight direct-star admission and outputs through both Kernel strategies, plus dotted-to-ISO full-Date admission and target rendering. Direct-list and constructor-target denominators remain narrower; empty/formal fragment outcomes remain external evidence pending; C none.
- `assurance`: the [direct-field-list DateRange checkpoint](SOURCES.md#src-date-range-direct-first-filled) locks dotted/dash admission at lengths two and three; later exact-policy and target-self-reference refusal; recursive empty continuation, terminal-prefix suffix hiding, reached-formal termination, and eager formal-input reporting through both Kernel strategies.
- `assurance`: the same checkpoint locks three-source admission and third-source output for every fragment profile, including configured and yearless `MM`/`MM-dd`, through both Kernel strategies. ISO/slash list execution, fragment-list formal suffixes, and lengths greater than three remain external evidence pending; C none.
- `assurance`: Boolean false and all seven formal-failure branches are internally checked with external evidence pending. Existing prefix/lookahead seams remain upstream locked; C none.
- `assurance`: Q Execute closed for the exact fixed UTC four-carrier profile: a fresh implementation-blind reader reconstructed typed selection, ordered empty-prefix scanning, target rendering, clearing/application, and all twelve calibrated outcomes from the canonical clauses plus the calibrated source record. It kept copy-versus-decode/render, alternate policies, formal poison calibration, shipment, and public compatibility outside the claim.
- `assurance`: Q Execute/Analyze internally closes the direct DateRange list decision: the checked operation exposes authored source order and checked declarations, while its result distinguishes recursive third-source selection, middle value/formal termination, exact versus yearless identity, and two-slot truncation. ISO/slash list correspondence and external authorability beyond length three remain explicit insufficient information rather than guessed compatibility claims.
- `source`: [direct one-star additional-kind computations](SOURCES.md#src-first-filled-additional-kind-computations).
- `source`: [direct one-star established-kind computations](SOURCES.md#src-first-filled-kind-computations).
- `source`: [fragment DateRange computations](SOURCES.md#src-date-range-fragment-first-filled).
- `source`: [direct-field-list DateRange computation](SOURCES.md#src-date-range-direct-first-filled).
- `remains`: full `entitySpec` expansion, external calibration beyond three sources, mixed/group/filtered operands, nested repetition, and temporal or DateRange policies beyond the named checked profiles remain under [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) and [SG8](SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion).
- `remains`: validation-rule use, wider application, and focused external false/poison/rejection calibration remain under [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) and [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition).

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Enumeration identity and categories | Stored-token identity, positional category projection, and checked category validity | [`Enumeration.lean`](../A12Kernel/Semantics/Enumeration.lean), [`CheckedEnumeration.lean`](../A12Kernel/Semantics/CheckedEnumeration.lean), [`Enumeration.lean`](../A12Kernel/Elaboration/Enumeration.lean) | E/P closed for flat declarations; peer-triangulated; C none | Dynamic/table/host breadth: [SG8](SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion) |
| Direct comparability | Separate checked relation for String/Enumeration and Enumeration/category equality/inequality | [`EnumerationComparability.lean`](../A12Kernel/Elaboration/EnumerationComparability.lean), [`EnumerationValueList.lean`](../A12Kernel/Semantics/EnumerationValueList.lean) | E/P closed for admitted families; source-grounded; C none | Wider paths/categories: [SG8](SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion) |
| Typed value lists | Scalar, multi-field, and direct/plain/filtered-star String/Enumeration/Number membership and quantifiers with typed operation-specific payloads. Partial starred entries filter concrete cells before reading and use their own existential covering-identifier extent gate per current iteration row, independent of the aggregate family's universal gate | [`ValueList.lean`](../A12Kernel/Semantics/ValueList.lean), entity-list/value-list elaboration owners | E/P closed for named shapes; multiple upstream L locks; C none | Remaining shapes and operators: [SG8](SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion) |
| Enumeration RNU and computation | Admitted Enumeration/category RNU keys plus checked filtered Enumeration target computation. An ordinary stored-direct source that reads its target and every target position in a compatible stored-direct `FirstFilledValue` list of length two or three project `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`. A compatible category-decorated target source, either target position in a compatible two-category `FirstFilledValue`, and either operand order of a compatible two-entry list whose category-projected entry names the target — beside another projection, a plain read of a different field, or a plain read of the target itself — project the distinct `MVK_ERROR_SEMANTIC_INDEX_OR_CATEGORY_FOR_ERRORFIELD`, because the class follows the **reading mode** of the self-read rather than the authored list shape: one projected self-read pre-empts the plain class. Operand identity carries the reading form, so a stored read and a category projection of one field are two operands that reach that fold, while a same-form repeat stays a repeated-operand shape refusal. A plain self-read standing beside a *different* field's category read is outside the measured plain-mode bound and projects no class. Accepted different-field forms and unrelated longer, incompatible-category, star, filter, domain, display, kind, and resolution refusals remain separate and unmapped | [`EnumerationRepetitionNotUnique.lean`](../A12Kernel/Semantics/EnumerationRepetitionNotUnique.lean), [`EnumerationComputation.lean`](../A12Kernel/Elaboration/EnumerationComputation.lean), [`EnumerationFirstFilledComputation.lean`](../A12Kernel/Elaboration/EnumerationFirstFilledComputation.lean) | E/P closed for named routes and both bounded target-diagnostic classes; clean dmtool `0.13.0` at a12-dmkits `93771ee2` Kernel-confirms the direct sources, every target position in two/three-field stored-direct lists, the ordinary compatible target-category source, and both positions in a two-category list beside accepted controls; reviewed inbound a12-dmkits `22fa19e7` and `4ba31a49` (`ComputedTargetProjectedReadLawsTest` over the real-kernel consistency oracle) establish the reading-mode fold itself and every two-entry pairing projected here, including the other-field plain entry measured after this project first reached it by composition and the same-field plain-beside-projected pairing that this project had been refusing as a repeated operand. Stored-direct controls and the category-list control persisted and read back exactly; the accepted ordinary category control is dry-run calibrated because dmtool `0.13.0` erased its category on converted read-back, so an exact persistence claim for that shape needs a re-measure on a later dmtool; upstream L partial; C none | Wider repeatable/composition, mixed and wider category lists, semantic-index target reads, and lists longer than three: [SG8](SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion), [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) |

### §9 — repetition and iteration

<a id="reopened-star-structural-completeness-and-addressing"></a>

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Named environments and addresses | One-group/nested environments, physical-row enumeration, recursive validation-only implicit child row 1 below existing parents, ancestor projection, checked `CellAddr`, and a validation addressed view that reads an implicit leaf as absent without materializing topology | [`Iteration.lean`](../A12Kernel/Semantics/Iteration.lean), [`StarAddressing.lean`](../A12Kernel/Semantics/StarAddressing.lean), [`CheckedDocument.lean`](../A12Kernel/Elaboration/CheckedDocument.lean), [`CheckedStarDocument.lean`](../A12Kernel/Elaboration/CheckedStarDocument.lean) | E/P closed for admitted scopes; implicit validation row membership and pointers kernel-locked at a12-dmkits `75205407`; relative concrete/implicit emission order is a deterministic Lean account with external order unverified; selected C/X/Q | Wider path grammar: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Star selection and correlation | Reopened-star extent, direct/plain/filtered selection, captured outer correlation, and one-star checked lowering | [`StarCompleteness.lean`](../A12Kernel/Semantics/StarCompleteness.lean), [`Correlation.lean`](../A12Kernel/Semantics/Correlation.lean), [`Correlation.lean`](../A12Kernel/Elaboration/Correlation.lean) | E/P closed for bounded shapes; selected C/X/Q, the only Q carrying an external cold implementation result ([fresh Rust runtime probe](IMPLEMENTER-KIT-CORRELATION.md#fresh-rust-runtime-consumer-probe)); upstream L partial | Multi-level/wider correlation: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Field-values-not-unique | Number, String/stored-Enumeration, and temporal overloads of `FieldValuesNotUnique` over the shared checked entity-list shape. `FieldEntityList` owns the four comparability categories and whole-list kind/category scans: Number selects the Number category, the token overload selects one String or Enumeration category, and temporal selects its format-keyed category. Two Strings or two stored Enumerations are admitted while any mix of those categories is refused in either order. The temporal overload's gate is one identical declared **format** string across every operand, keyed on the format rather than the kind, so a DATE `yyyy` beside a DATE_FRAGMENT `yyyy` is admitted across two kinds while two DATE fields differing only in format are refused; kind and category failures retain the offending path and actual kind, while temporal declarations without a coherent format and temporal format mismatches remain distinct; every refusal projects to its **Kernel diagnostic class** or honestly to `none`. The kind gate is applied over the whole operand list before any category decision, because gate order is observable and a fail-fast walk in authored order reports the mixing class where the Kernel reports the kind class. Its compared identity is the operand's **exact stored text**, which reuses the canonical token atom exactly rather than rendering a decoded value, and is deliberately a different reading from the decoded date `NumberOfDifferentValues` uses over the same entity lists. Common to all three: one **ordered fused scan** decides truth and polarity together, because the engine answers at the first duplicate it finds; membership stays on the shared comparable primitives with the scale-19 boundary for Number and exact identity for tokens; and empty *and formally unavailable* cells are skipped alike, so an unavailable cell neither suppresses nor compares. The operator's own result therefore carries no UNKNOWN at all. Every consumer therefore collects its operands through the non-suppressing `collectTaggedValueListCells`, never the aggregate families' availability-gated operand scan; the skip is locked at that checked-document boundary rather than only at the pure verdict, and one mechanism law specialized per overload proves that no such route can answer UNKNOWN. All three overloads return a polarity-bearing verdict whose filter escalation is **positional**: the scan walks operands in authored order, marks a filter only when it collects a present value from a filtered operand, and answers at the duplicate, so a filter authored after the duplicate never retypes it and one contributing only empties never marks anything. A firing the scan reached through a filter is omission-typed because the filter selects which values are compared; every other firing stays value-typed, and no missing potential in the selected cells escalates it | [`NumberValuesNotUnique.lean`](../A12Kernel/Elaboration/NumberValuesNotUnique.lean), [`TokenValuesNotUnique.lean`](../A12Kernel/Elaboration/TokenValuesNotUnique.lean), [`TemporalValuesNotUnique.lean`](../A12Kernel/Elaboration/TemporalValuesNotUnique.lean), [`NumericAggregate.lean`](../A12Kernel/Semantics/NumericAggregate.lean), [`ValueList.lean`](../A12Kernel/Semantics/ValueList.lean), [comparison proofs](../A12Kernel/Proofs/NumericAggregate.lean), [boundary proofs](../A12Kernel/Proofs/ValuesNotUnique.lean), [cases](../A12Kernel/Conformance/NumberValuesNotUnique.lean), [token cases](../A12Kernel/Conformance/TokenValuesNotUnique.lean), [temporal cases](../A12Kernel/Conformance/TemporalValuesNotUnique.lean) | E/P closed for the Number, String/stored-Enumeration, and temporal overloads; clean dmtool `0.12.1` at a12-dmkits `cd41ea94` Kernel-confirms exact arity, category, BOOLEAN/CONFIRM kind, and two-order pre-emption diagnostics for Number and token lists, with accepted representatives persisted and read back; real-kernel authoring admits the two- and three-field list over differing scales, the starred single-field form, a mixed direct-plus-starred list, and the filtered starred form, and rejects a single operand plus a mixed-kind list; `DM_INTERPRETER` separates typed cross-scale firing and empty skipping at `5c0d5f03`, and one-message unfiltered VALUE versus filtered OMISSION at `b5aba1ec`; that route's apparent formal *suppression* was disproved at `ddaf2e13`, where the kernel skips an unavailable cell instead; Kernel-locked for cardinality, first-row anchoring, placement invariance, and single-operand filter polarity by `FieldValuesNotUniqueDiffTest` at a12-dmkits `bbbbf48e`; both the **positional** polarity rule and the unavailable-cell skip are Kernel-locked at `ddaf2e13` across dynamic Groovy, generated Java, and the interpreter under accepted [`SPEC-2026-08-05-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-05-01--fieldvaluesnotunique-filter-escalation-is-positional-not-static), including the filtered operand that contributes only unconvertible cells and the two-equal-unconvertible pair; the temporal overload's format-keyed admission gate is Kernel-locked by the same revision's 25-case admission tier, including the cross-kind same-format row and the differing-format refusal, though the local matrix asserts that gate rather than replaying retained observations; its stored-text identity is kernel-source grounded with one unmeasured residual under pending [`SPEC-2026-08-05-03`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-05-03--the-temporal-fieldvaluesnotunique-identity-is-stored-text-and-its-agreement-with-decoded-values-is-an-assumption-rather-than-a-measured-equivalence); C none | The Custom category, the stored-text residual, and the format-versus-kind-family annotation: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion). The message reference and first-row anchoring channels this clause now specifies remain unmodeled: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration). Group-scope admission, family retention, full-validation extent, reference projection, source assurance, and live remainders are owned by [shared admission](#cap-shared-entity-list-group-admission), [Number](#cap-number-group-aggregates), [token](#cap-token-group-expansion), [temporal](#cap-temporal-group-uniqueness), and [reference projection](#cap-group-operand-reference-projection). Group-form message emission remains under [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration). |
| Repetition-not-unique | Branch-independent clusters, mixed admitted key kinds, validation-domain implicit descendants, deepest-key-parent row selection independent of authored component order, and the admitted `@From` reference boundary | [`RepetitionNotUnique.lean`](../A12Kernel/Semantics/RepetitionNotUnique.lean), [`RepetitionNotUnique.lean`](../A12Kernel/Elaboration/RepetitionNotUnique.lean) | E/P closed for admitted keys; upstream L locked for guarded composition, implicit optional-empty duplicates, mixed-depth key order, and admitted `@From`; C none | Remaining key/path forms: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Semantic index | Checked normalized key column with ordered occurrences, duplicate/unavailable state, shared duplicate exclusion for semantic and parallel selection, and one ordinary read route. The checked source carries the declared index **declaration** rather than a Number field, so any index kind is admitted and one dispatch chooses between the numeric-value and exact-text identities; a literal key's identity domain must be that index's own. The reduced raw-context route keeps a Number index and Number target as its own stated refinement, because it rebuilds the column from a one-group scan instead of projecting the shared one. A **DateRange** selected target is closed on the endpoint-component carrier through [`SemanticIndexDateRange.lean`](../A12Kernel/Elaboration/SemanticIndexDateRange.lean), which refines the same certificate and projects the keyed read through the direct route's own payload rule. A field-valued key read from **inside the indexed group itself** carries its own measured placement class, `MVK_SEMANTIC_INDEX_CONTAINED_IN_INDEX`, rather than the generic repeatable-reference refusal, and a repeatable key outside that group still reaches the ordinary nonrepeatable boundary. The Kernel's declaration-format check on an authored literal, `MVK_INDEX_VALUE_INVALID`, is an explicit exclusion: this project performs no lexing, so a literal reaches the surface already decoded. A duplicated side is unmatched when a clean far side supplies the join key and contributes no key when both sides duplicate it | [`SemanticIndex.lean`](../A12Kernel/Semantics/SemanticIndex.lean), [`SemanticIndex.lean`](../A12Kernel/Elaboration/SemanticIndex.lean), [`CheckedIndexColumn.lean`](../A12Kernel/Elaboration/CheckedIndexColumn.lean) | E/P closed for the named selection boundary, with the general delegation law and its Number specialization proved so the two routes cannot describe two policies; upstream L locked for ordinary match/column timing and full parallel duplicate exclusion, and index-kind independence plus the four-state read are locally Kernel-locked at the [kind-independence](SOURCES.md#src-semantic-index-kind-independence) and [endpoint-shape](SOURCES.md#src-date-range-endpoint-shapes) checkpoints; C none | Remaining non-Number selected targets, the literal format check, and wider keys: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Repeatable validation | Ordinary full/partial rule loops over concrete outer rows plus recursive implicit nested child row 1; partial rule admission projects error-path relevance onto the levels bound by the checked reference-derived iteration plan and ignores deeper coordinates, while admitted ordinary field and Number-aggregate reads consume the call-local generated-preliminary view, including requiredness, relevance-scoped duplicate-index findings, and cause-free default suppression; parallel presence retains concrete indexed topology | [`ValidationRule.lean`](../A12Kernel/Elaboration/ValidationRule.lean), [`ValidationCondition/Iteration.lean`](../A12Kernel/Elaboration/ValidationCondition/Iteration.lean), [`ParallelPresenceRule.lean`](../A12Kernel/Elaboration/ParallelPresenceRule.lean), [partial-preliminary projection law](../A12Kernel/Proofs/CheckedIndexPreliminary.lean) | E/P closed for admitted loops, ordinary field leaves, and unfiltered Number aggregates, including three-level projection, empty-as-zero full/partial execution, no-outer control, nonmaterialization, row-exact admission, relevance-scoped duplicate suppression, requiredness, and a zero-bound once plan admitted by another concrete error row; upstream L locked at a12-dmkits `75205407` for the row-domain and iteration-bound gate splits and `cb75eb3c` for duplicate-partner relevance; selected C/X | Wider leaves/parallel shapes: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Repeatable Number computation | Checked route tables, invalid-index suppression, target-specific clearing, exact-address overlay, and finite supplied-order execution | [`ParallelNumericAlternativeTable.lean`](../A12Kernel/Elaboration/ParallelNumericAlternativeTable.lean), [`ParallelNumericRun.lean`](../A12Kernel/Elaboration/ParallelNumericRun.lean) | E/P closed for finite Number plan, including universal operation/table target exclusion, table/whole-fold target-field ownership, and exact successful-output address uniqueness; upstream L partial; C none | Cross-family/repeatable breadth: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) |
| Group-list/count terminals | Terminal repeatable groups count structural rows; nonrepeatable terminals below stars evaluate once per selected ancestor environment | [`StarGroup.lean`](../A12Kernel/Elaboration/StarGroup.lean), [`ValidationCondition/`](../A12Kernel/Elaboration/ValidationCondition/) | E/P closed for bounded terminals, including repeated-star occurrence preservation, zero-row polarity, bound-prefix admission, and nonrepeatable-terminal composition; upstream L at `b6fa6eb8`, `b9ade32d`, and `8cab6ece`; C none | Complete group-list/count surface: [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |

### §10 — paths and references

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Flat resolution | Absolute, parent-relative, and bare flat names plus quote-aware structured names | [`Flat/`](../A12Kernel/Elaboration/Flat/) | E/P closed for admitted grammar; selected C/X | Full concrete grammar/renderer: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Star/correlation paths | Bounded group-qualified star and correlation paths lower to named scopes/environments | [`StarPath.lean`](../A12Kernel/Elaboration/StarPath.lean), [`StarAddressing.lean`](../A12Kernel/Semantics/StarAddressing.lean) | E/P closed for admitted shapes; selected C/X/Q | Wider nesting/legality: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| `RuleGroup` and index routes | Declaring-group resolution, checked semantic-index construction, and duplicate-excluding index-group routes consumed by bounded full parallel validation/computation | [`ValidationContext.lean`](../A12Kernel/Elaboration/ValidationContext.lean), [`SemanticIndex.lean`](../A12Kernel/Elaboration/SemanticIndex.lean), [`CheckedIndexColumn.lean`](../A12Kernel/Elaboration/CheckedIndexColumn.lean) | E/P closed for named routes; upstream L partial; selected C | Partial parallel relevance and wider static diagnostics: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Guarded nonrepeatable-root `CurrentRepetition` | One indivisible validation leaf retains a direct same-root filled-field guard, an ordinary nonrepeatable root, and the closed `== 1` / `!= 1` tag. It reuses the shared filled-presence, numeric-verdict, connective, and reference owners; only the guard contributes a field pointer, and partial validation is structurally unsupported | [`CurrentRepetition.lean`](../A12Kernel/Elaboration/CurrentRepetition.lean), [`Core.lean`](../A12Kernel/Elaboration/ValidationCondition/Core.lean), [`Assembly.lean`](../A12Kernel/Elaboration/ValidationCondition/Assembly.lean), [proofs](../A12Kernel/Proofs/ValidationCondition/CurrentRepetition.lean), [cases](../A12Kernel/Conformance/ValidationCondition/CurrentRepetition.lean) | E/P closed for the exact shape; dynamic Groovy, generated Java, and interpreter L at a12-dmkits `feba4552110858b07966660bf7080441d3b90d00`; C/X/Q none | Captured row lookup, fixed nested groups, wider root comparisons, computation, filters, partial validation, arithmetic wrappers, and parsing: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Guarded same-group repeatable `CurrentRepetition` | One indivisible validation leaf retains a direct filled guard, the shared model-owned repeatable coordinate source, and closed `> 1`, `> 2`, or `>= 1` tags. Ordinary iteration supplies the complete selected environment; missing, duplicate, or zero named bindings remain structural errors; the shared numeric owner compares the 1-based coordinate; every firing is VALUE and carries the guard's concrete row pointer. The guard alone supplies iteration scope, legality, field dependency, and reference projection; partial validation is structurally unsupported | [`CurrentRepetition.lean`](../A12Kernel/Elaboration/CurrentRepetition.lean), [`Core.lean`](../A12Kernel/Elaboration/ValidationCondition/Core.lean), [`Assembly.lean`](../A12Kernel/Elaboration/ValidationCondition/Assembly.lean), [proofs](../A12Kernel/Proofs/ValidationCondition/CurrentRepetition.lean), [cases](../A12Kernel/Conformance/ValidationCondition/CurrentRepetition.lean) | E/P closed for the checked same-group shape; exact one-level `> 1` row/value/pointer L at a12-dmkits `ee70581fae1991d359b7c78f1a6ecfbffdd1a31f`; `> 2` and `>= 1` have cross-engine agreement but exact cross-threshold coordinates remain external evidence pending; admission L at `13e8ab71a00287783db019a7b1f2d97d86686daf`; deeper model-owned levels remain external evidence pending; C/X/Q none | Captured `$`, fixed nonrepeatable descendants, other validation comparisons, computation beyond the fixed one-row `> 0` cascade, filters outside the established `Having` owner, partial validation, arithmetic wrappers, and parsing: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |

### §11 — calculations and formal checking

<a id="11--computations"></a>
<a id="11-computations"></a>

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Computation conditions | Ordered presence/fill leaves with not-true/holds/poison, decisive short-circuiting, and common-precondition distribution | [`ComputationCondition.lean`](../A12Kernel/Semantics/ComputationCondition.lean), [`ComputationCondition.lean`](../A12Kernel/Elaboration/ComputationCondition.lean) | E/P closed for admitted leaves; upstream L locked for poison order; C partial through cascade | Wider leaves follow family gaps |
| First-selected tables | Guarded rows select once; selected empty, poison, and runtime failure end the table; supplied same-target String and Number computations flatten into their family table at the first target position | String/Number/Enumeration table owners, especially [`StringComputationTable.lean`](../A12Kernel/Elaboration/StringComputationTable.lean), [`StringComputationRunPlan.lean`](../A12Kernel/Elaboration/StringComputationRunPlan.lean), [`NumericComputation/Table.lean`](../A12Kernel/Elaboration/NumericComputation/Table.lean), and [`NumericComputation/RunPlan.lean`](../A12Kernel/Elaboration/NumericComputation/RunPlan.lean) | E/P closed for admitted tables and nonrepeatable String/Number same-target assembly; source/template-grounded; upstream L at `4ebe84fc` separates selected-empty String termination from false-guard fall-through on both kernel strategies and JVM/Node | Repeatable same-target assembly remains in [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) |
| Scalar execution plans | Finite supplied-order String and Number plans with same-target consolidation, both bounded Number/String producer-consumer directions, and one finite mixed run over preconsolidated family tables; the mixed state retains typed completions and projects them only through the reached consumer context | [`StringComputationRunPlan.lean`](../A12Kernel/Elaboration/StringComputationRunPlan.lean), [`StringComputationRun.lean`](../A12Kernel/Elaboration/StringComputationRun.lean), [`NumericComputation/RunPlan.lean`](../A12Kernel/Elaboration/NumericComputation/RunPlan.lean), [`NumericComputation/Run.lean`](../A12Kernel/Elaboration/NumericComputation/Run.lean), [`NumberToStringComputationRun.lean`](../A12Kernel/Elaboration/NumberToStringComputationRun.lean), [`StringToNumberComputationRun.lean`](../A12Kernel/Elaboration/StringToNumberComputationRun.lean), and [`ScalarComputationRun.lean`](../A12Kernel/Elaboration/ScalarComputationRun.lean) | E/P closed for homogeneous nonrepeatable plans, both named two-family directions, and finite mixed activation/execution. A structural Number run fault carries the failing table's own target on both constructors: the atomic law fixes it against the evaluated table, the public law confines it to the run's own checked targets, and a retained two-table case separates the failing table's label from the run's first target in both supplied orders. The `targetCheck` arm stays unwitnessed under [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition). String root/cascade C and upstream same-target L; Number and composed cross-family C none | Application and repeatable activation: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) |
| Repeatable Number plan | Finite supplied-order target tables, target-specific route inventory, exact-address completion, index suppression, and classification | [`ParallelNumericRun.lean`](../A12Kernel/Elaboration/ParallelNumericRun.lean) | E/P closed for finite Number plan; every checked operation/table excludes its own target, every successful table emits only its checked target field, and the append fold preserves target-field ownership and exact output-address uniqueness; upstream L partial; C none | Cross-family and multi-computation repeatable work: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) |
| One-row structural-guard Number cascade | One checked one-level `CurrentRepetition(group) > 0` source plus two same-row direct Number copies. Analyze keeps the structural group separate from `First ← Base` and `Second ← First`; Execute requires exactly one instantiated row, evaluates the checked coordinate, overlays only the first exact rich outcome for the second read, and otherwise reuses the direct-copy target/result owners. Multiple rows are explicit insufficient information; no condition tree, graph, sort, or scheduler is added | [`CurrentRepetition.lean`](../A12Kernel/Elaboration/CurrentRepetition.lean), [`CurrentRepetitionComputation.lean`](../A12Kernel/Elaboration/CurrentRepetitionComputation.lean), [proofs](../A12Kernel/Proofs/CurrentRepetitionComputation.lean), [cases](../A12Kernel/Conformance/CurrentRepetitionComputation.lean) | E/P closed for the exact one-row, scale-0 fixture; dynamic Groovy, generated Java, and interpreter L at a12-dmkits `43359ae258e1208e4de63ef00f684cd2a5250d7a`; the peer's independent AST reference walk and ordered interpreter test discharge structural-versus-field dependency identity; Q closed for the in-context Execute/Analyze checked-client decision; C/X none | Other bounds/operators, multiple rows, captured `$`, partial computation, parser lowering, generic computation conditions, other operations, clearing/error branches, and wider scheduling: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition), [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Result and application | Source-relative unchanged/changed/rejected/cleared projections, cause-blind dependency cells, direct-outcome versus retained-action application, caller-destination application without validation, both bounded heterogeneous directions, and the finite mixed run retaining the Number and String family views separately | Family result/application owners, [`NumericComputation/RunResult.lean`](../A12Kernel/Elaboration/NumericComputation/RunResult.lean), [`NumberToStringComputationRun.lean`](../A12Kernel/Elaboration/NumberToStringComputationRun.lean), [`StringToNumberComputationRun.lean`](../A12Kernel/Elaboration/StringToNumberComputationRun.lean), and [`ScalarComputationRunResult.lean`](../A12Kernel/Elaboration/ScalarComputationRunResult.lean) | E/P closed for admitted String/Number/temporal routes, both bounded family-preserving results, and finite mixed family projection; upstream L at `7b2e9f7` for Number V2 source identity, `351bf9f2` for cause-blind dependency erasure, and `f6dc22d2` for separate-destination retained action application; C String only | Complete materialized document application, unobserved repeatable topology, and compatibility certificate: [SG4](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) |
| Computation message partition | Shared tagged message-pointer identity with concrete/wildcard/unknown coordinates, exact computed-instance association, residual channel, value-less target error, and payload-independent partition | [`MessagePointer.lean`](../A12Kernel/Semantics/MessagePointer.lean), [`ComputationMessage.lean`](../A12Kernel/Semantics/ComputationMessage.lean), [`NumericComputation/RunResult.lean`](../A12Kernel/Elaboration/NumericComputation/RunResult.lean) | E/P closed for the normalized pointer and existing partition; upstream pointer-domain L at `61705844`; C none | Raw pointer-factory boundary, other message collections, error-code mapping, and localized payload construction: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| Generated computation validation | Every relevant alternative is retained for later validation; execution first-selection is not reused | [`GeneratedComputationValidation.lean`](../A12Kernel/Elaboration/GeneratedComputationValidation.lean) | E/P closed for admitted String/Number/repeatable routes; source-grounded; C none | Wider target families follow SG5–SG8 |

### §12 — validation and polarity

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Whole-rule semantics | Rule condition states the error condition; unified verdict; fired-only addressed message emission | [`ValidationRule.lean`](../A12Kernel/Semantics/ValidationRule.lean), [`ValidationRule.lean`](../A12Kernel/Elaboration/ValidationRule.lean) | E/P closed for admitted rules; selected C/X | Complete message providers: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| Ordinary/once/repeatable execution | Once execution plus ordinary validation-row loops over checked flat, numeric, temporal, group, and admitted repeatable leaves; nested implicit contexts are separate from concrete stored topology | [`ValidationCondition/`](../A12Kernel/Elaboration/ValidationCondition/), [`ValidationRule.lean`](../A12Kernel/Elaboration/ValidationRule.lean) | E/P closed per admitted leaf; selected C/X; upstream L partial | Wider leaves/paths: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion) |
| Partial validation | Rule-wide filter skip, per-cell relevance masking, and one-sided soundness without an unrestricted full/partial `iff` | [`PartialValidation.lean`](../A12Kernel/Semantics/PartialValidation.lean), partial rule owners | E/P closed for admitted rules; LF23 source-closed; selected C/X | Wider addressed/custom leaves: [SG9](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion), [SG11](SEMANTICS-GAPS.md#sg11--custom-condition-checked-orchestration) |
| Fill/group quantifiers | Validation fill quantifiers plus resolved group predicates/counts over checked topology | [`ValidationFillQuantifier.lean`](../A12Kernel/Semantics/ValidationFillQuantifier.lean), [`GroupPresence.lean`](../A12Kernel/Semantics/GroupPresence.lean) | E/P closed for admitted shapes; upstream L partial; C none | Group-list completeness: [SG13](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) |
| Presence contradiction analyzer | Proof-bearing exact-shape analysis that detects a bounded contradictory presence pattern without claiming general satisfiability | [`Flat/PresenceContradiction.lean`](../A12Kernel/Elaboration/Flat/PresenceContradiction.lean) | E/P/Q closed for the named Analyze task; no kernel-evidence claim | Wider analysis requires a new consumer hypothesis |

### §13 — message interpolation

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Whole-rule authoring and rendering | Checked nonempty printable-ASCII flat rule templates admit text, `$$`, `$Field$`, and `$Field.value$`. A parameter's entity spec is the **shared path grammar** the condition parser uses, decoded into the one existing `SurfaceFieldPath` and resolved by the one existing field resolver rather than a second lookup: a bare name, a group-qualified relative path, an absolute path, and a parent walk with an optional explicit turning point all reach a nonrepeatable field, which must be referenced by the condition before direct lowering to the one-pass post-fire renderer. The value suffix is taken at the end of the whole spec, which the grammar itself settles rather than this fragment choosing: the value terminal is not one of the four a name may end with, so a trailing one is always the suffix. A name colliding with a terminal is written in the grammar's single-quote escape, erased before lookup so an unnecessary quote is transparent, and the collision is checked at any path level naming the offending segment; this producer's quoting requirement is deliberately narrower than the condition language's, because a caller-supplied set of terminals is historically accepted unquoted inside an entity name. A field reference may instead carry an Enumeration **category** suffix, whose three gates are the Kernel's own in its order — a missing name, a non-Enumeration field, and an undeclared category name reached through the one existing Enumeration projection boundary — and which carries neither of the value suffix's extra gates. What a category access *renders* is deliberately unclaimed: the checked part hands the caller an opaque text run with no fallback policy, unlike the name and value inputs. One **non-field** form is admitted: the Base Year terminal with an optional signed offset, gated only on the model declaring a Base Year and resolved at authoring because it depends on the model alone. The selected language's spellings are data rather than built in, since the parameter grammar is bilingual and only five terminals differ. Every malformed spec is the Kernel's single parse class rather than a family of shape classes. The certificate retains the authored spelling beside its decoded path, so a diagnostic and an Explain consumer can quote what was written | [`ValidationMessageAuthoring.lean`](../A12Kernel/Elaboration/ValidationMessageAuthoring.lean), [`ValidationRule.lean`](../A12Kernel/Semantics/ValidationRule.lean) | E/P closed for the bounded authoring and admitted flat/mixed rendering boundary; **L is exact for the whole static parameter surface** at the [parameter-grammar checkpoint](SOURCES.md#src-rule-message-parameter-grammar) — every path form including the turning point and its live wrong-name control, the quote escape beside the exemption's own separator, all four category gates, and the Base Year form on both a configured and an unconfigured model — measured through `rule add --dry-run`, because `rule check` takes no message and cannot reach this surface; the one class divergence is the doubled arrow, which both refuse. What stays uncalibrated is everything **rendered**: no runtime observation of any parameter's text exists, so the render plan is internally composed. Pending [SPEC-2026-08-24-01](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-24-01) carries the clause additions; selected C; X none | The remaining non-field parameter forms, the semantic-index suffix, a category access's rendering policy, the grammar's accented name letters that the bounded-ASCII template gate refuses first, locale, providers, and repeatable pointers: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| String-pattern field messages | Checked bounded-ASCII `en_US` templates admit only fixed lowercase `$field$` and `$field.value$`, reject the empty `$$` parameter, lower already-selected owning-field bytes once, and attach resolved text without reclassifying the established pattern error | [`ValidationMessageAuthoring.lean`](../A12Kernel/Elaboration/ValidationMessageAuthoring.lean) | E/P closed for the measured String-pattern producer; upstream L at `ed0b0cd9`; C/X/Q none | Requiredness grammar, other locales, provider invocation, empty-value fallback, and other field-message producers: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| Structural message references | The validation rule message's `referenced` channel as a structural projection of the checked condition at one firing environment. It takes no document, context, or verdict, so it cannot degenerate into a read trace. **One shared coordinate rule** reads the concrete prefix out of the *referenced field's own* repeatable scope and wildcards from the first reopened level down, which is what lets a starred field and a starred group's deeper descendant share it without either re-deriving scope; a nonrepeatable reference carries no coordinates at all. Both connectives contribute every branch, and the deduplicated result's order carries no kernel claim because the engine's own collection is a hash set. Classified over the `orderedNumeric` leaf's complete delegated scalar atom family, every shape the scalar fragment admits except the refused fixed group count, including UTF-16 `Length`, range, category-projected conversion, and the three temporal differences, whose declarations may be repeatable under addressed admission and are then concrete at the bound row, over its `FirstFilledValue`, Number value-count, and aggregate atoms with unfiltered direct and starred operands, and over its remaining checked sources, so that leaf is now classified for **every** atom constructor with no catch-all: the token value count projects each declaring field instance; group-bearing entity-list, Boolean/Confirm count, group-list, and group-presence leaves delegate their recursive membership and coordinates to [group-operand reference projection](#cap-group-operand-reference-projection); stored/category and canonical-token projections remain invisible; filtered stars refuse through their optional filter field; and a row-paired `SumOfProducts` projects both value fields under one shared-path certificate, over the ordinary non-starred `repeatableFieldPresence` leaf, so a standard iteration-guard rule projects end to end with the guard's reference merging into the guarded leaf's by deduplication, and over the `flat` and non-model-indexed `numeric` leaves. Membership uses one of two strategies chosen by whether the family can carry a starred operand: explicit traversal where it can, or sieving the model's declarations through the family's own exhaustive `referencesField` where it cannot, which inherits exactly that predicate's coverage. Both sieved families qualify structurally rather than by convention: neither the flat leaf constructors nor the scalar numeric atoms admit a star, and each family's predicate covers every constructor with no catch-all. The fixed group count's arm of that predicate reports the whole counted subtree, which is now the measured relation rather than an admission-gate meaning transported into an output channel, so it is sieved like every other atom and the per-atom sievability gate that briefly refused it is deleted. Its remaining boundary is that the kernel account is measured on a nonrepeatable model: a subtree field below a repeatable level deeper than the rule binds fails closed at that exact level rather than choosing between a wildcard and a pinned first repetition. Only `RepetitionNotUnique` and the filtered-star operand still **refuse the whole projection** rather than return a partial set that would read as complete | [`Reference.lean`](../A12Kernel/Elaboration/ValidationCondition/Reference.lean), [proofs](../A12Kernel/Proofs/ValidationCondition/Reference.lean), [cases](../A12Kernel/Conformance/ValidationRule/OrdinaryReference.lean) | E/P closed for that fragment: exactness and arity are proved once at the shared rule, so a reopened pointer provably never collapses to a `CellAddr` and always carries one coordinate per repeatable level, and the starred-field laws are specializations rather than restatements; the two strategies' pointer shapes are proved complementary, since every sieved pointer provably recovers an exact address while every reopened one provably cannot; connective blindness is proved. Twelve non-group mutations each fail a retained case: wildcard the bound prefix, never wildcard, off-by-one on `firstStar`, use a fixed single wildcard, sieve a numeric leaf whose fixed group count would have answered, classify only one comparison side, let a context-free atom contribute or refuse, treat a delegated scalar atom as coordinate-free, project one operand of a two-field atom, report projection-specific rather than declaring identity, admit a filtered star through its optional filter, or project one side of a row-paired product. Group-expansion and unstarred-coordinate separators are owned by [the group record](#cap-group-operand-reference-projection). Upstream L for the projection rule at a12-dmkits `bbbbf48e`; C/X/Q none | Filtered-star operand coordinates in all three families that refuse them, the `RepetitionNotUnique` leaf, `fillToFix`, the message-record channel, and the per-part pointer-shape exclusion: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| Shared message pointer | One resolved field identity plus concrete/wildcard/unknown coordinates, distinct from exact `CellAddr`, reused by every implemented formal-message and callback error channel | [`MessagePointer.lean`](../A12Kernel/Semantics/MessagePointer.lean), [`MessagePointer.lean`](../A12Kernel/Proofs/MessagePointer.lean), [`MessagePointer.lean`](../A12Kernel/Conformance/MessagePointer.lean) | E/P closed for the normalized field-instance domain; upstream pointer-domain L at `61705844`; C/X/Q none | Raw name/index factory asymmetries, root, accessors, and conversions: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| Computation formal messages | Shared partial-pointer identity and payload-independent computed/residual partition | [`MessagePointer.lean`](../A12Kernel/Semantics/MessagePointer.lean), [`ComputationMessage.lean`](../A12Kernel/Semantics/ComputationMessage.lean) | E/P closed for the current partition; upstream pointer-domain L at `61705844`; C/X/Q none | Error-code mapping and localized payload construction: [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| Custom-field formal messages | Shared partial pointer, code, severity/type, and resolved text for the admitted custom-field route | [`MessagePointer.lean`](../A12Kernel/Semantics/MessagePointer.lean), [`CustomFieldFormalMessage.lean`](../A12Kernel/Semantics/CustomFieldFormalMessage.lean) | E/P closed for the local field-instance route; upstream pointer-domain L at `61705844`; C/X none | Complete formal-message orchestration under [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |

### §14 — custom conditions

| Capability | Exists | Owner | Assurance | Remains |
|---|---|---|---|---|
| Custom-condition callback | One reached pure total callback with explicit data/relevance/formal channels and the shared partial error pointer; no hidden host registry in `World` | [`CustomCondition.lean`](../A12Kernel/Semantics/CustomCondition.lean), [`MessagePointer.lean`](../A12Kernel/Semantics/MessagePointer.lean) | E/P/Q closed for the reached representation boundary; pointer-domain L at `61705844`; C/X none | Name grammar, registration, host failure, rule orchestration: [SG11](SEMANTICS-GAPS.md#sg11--custom-condition-checked-orchestration) |
| Custom-field validation | Checked registered custom field, bounds, stored-value mode, cause, validity, and formal-message projection | [`CustomField.lean`](../A12Kernel/Elaboration/CustomField.lean), custom-field semantic owners | E/P/Q closed for admitted route; upstream L now locks the **per-cell** observation cardinality at a12-dmkits `73bc4a04` across both kernel strategies and the interpreter, which the pure-oracle account satisfies by construction because only an impure validator could observe the count; C/X none | Complete integration: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion), [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration) |
| Explicit validity predicates | Two-argument `Valid(field, "Name")` and `Invalid(field, "Name")` over a **partial** host registry. Any nonempty authored name is admitted and carries whatever the world resolved for it, the fixed German no-bounds stored-value context samples a resolved validator, and the value-specified gate answers UNKNOWN for an absent or present-empty operand before any registry contact. A resolved name makes the two polarities exact complements; an unresolved name is degenerate and makes **both** fire VALUE on the same value, with the two indistinguishable no-usable-validator kernel states collapsed deliberately | [`CustomFieldValidity.lean`](../A12Kernel/Semantics/CustomFieldValidity.lean), [proofs](../A12Kernel/Proofs/CustomFieldValidity.lean), [cases](../A12Kernel/Conformance/CustomFieldValidity.lean) | E/P closed over the registration × fill × polarity matrix, including the proved non-law that complementation needs its resolved hypothesis; Kernel-locked for the unregistered observable by `UnregisteredPredefinedTypeObservableDiffTest` and the strengthened `PredefinedTypeValidityDiffTest` at a12-dmkits `dd0bcadf`; C/X/Q none | Enumeration and extensible-Enumeration operands, the operand field/wildcard/value-validation static gates, and validation-condition-leaf integration: [SG7](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion) |

## Cross-clause owners

<a id="cross-clause-implementation-notes"></a>
<a id="resolved-number-aggregates"></a>
<a id="stringenumeration-aggregate-counts"></a>
<a id="stringenumeration-distinct-count"></a>
<a id="resolved-validation-group-presence"></a>
<a id="checked-group-star-terminals"></a>

These stable names help when a query crosses numbered clauses. Their capability status is already in the rows above.

| Shared mechanism | Sole owner and reuse boundary |
|---|---|
| Kernel static-diagnostic classes | [`StaticDiagnostic.lean`](../A12Kernel/Elaboration/StaticDiagnostic.lean) owns the established `MVK_` vocabulary and exact identifiers; [`laws`](../A12Kernel/Proofs/StaticDiagnostic.lean) guard enumeration distinctness and completeness. Each family projects its local refusals and returns `none` where no class is established. The bounded [Translate/Explain probe](../A12Kernel/Conformance/ComputationTargetDiagnosticConsumer.lean) preserves accepted, mapped, and family-local unmapped outcomes. This is distinct from the public process's [`Reference.Support.DiagnosticCode`](../A12Kernel/Reference/Support/Diagnostics.lean). Coverage includes all three `FieldValuesNotUnique` overloads, the [shared group-admission gates](#cap-shared-entity-list-group-admission), the [repeatable-operand rule locus](#cap-date-range-repeatable-operand-locus) projected once at the resolver's own error type, singular DateRange overlap group refusal, the bounded token `FirstFilledValue` kind pair, fixed filled-group computation admission, computed Number, ordinary String and Enumeration targets, partial computed-Date targets, condition-pattern admission, and raw-String `Length`; the hardcoded pairings in [`ValidationProjection.lean`](../A12Kernel/Evidence/ValidationProjection.lean) remain consolidation candidates when this vocabulary reaches them |
| Resolved `FirstFilledValue` | [`FirstFilledValue.lean`](../A12Kernel/Semantics/FirstFilledValue.lean) and checked elaboration own ordered mixed String/stored-Enumeration selection; computation specializes the same selection rather than reimplementing it. |
| Resolved Number aggregates | [`NumericAggregate.lean`](../A12Kernel/Semantics/NumericAggregate.lean) and [`NumericAggregate/`](../A12Kernel/Elaboration/NumericAggregate/) own checked direct/plain-star/filtered-star streams, ordered precision-50 folds, validation/computation projections without a second iterator, and the combiner family's normalized universal partial-extent gate per current iteration row. [`StarPath.lean`](../A12Kernel/Elaboration/StarPath.lean) keeps that gate separate from the starred value-list existential extent gate. |
| String/Enumeration aggregate counts | [`TokenDistinctCount.lean`](../A12Kernel/Elaboration/TokenDistinctCount.lean) and [`TokenValueCount.lean`](../A12Kernel/Elaboration/TokenValueCount.lean) reuse checked entity-list topology while retaining token/category identity and family polarity. Their group-scope retention and per-cell projection are owned by [token group expansion](#cap-token-group-expansion). |
| Boolean/Confirm value count | [`BooleanValueCount.lean`](../A12Kernel/Elaboration/BooleanValueCount.lean) applies the distinct `True`/`False` field-kind matrix and derives fixed canonical tokens from checked direct/plain-star/filtered-star values. Existing scalar and addressed computation plus generated mismatch validation retain fixed scale 0, exact source/filter references, omitted-tail movement, and reached formal poison. Empty Confirm remains unfilled, every direct or starred `False` Confirm source is rejected statically, and scalar plans reject repeatable sources explicitly. Group-scope retention, assurance, reference projection, and runtime refusal are owned by [Boolean/Confirm group value count](#cap-boolean-confirm-group-value-count). |
| Reopened-star structural completeness and addressing | [`StarCompleteness.lean`](../A12Kernel/Semantics/StarCompleteness.lean), [`StarAddressing.lean`](../A12Kernel/Semantics/StarAddressing.lean), and checked document/path owners keep topology independent of filled cells. |
| Resolved group presence, both arms | [`GroupPresence.lean`](../A12Kernel/Semantics/GroupPresence.lean) owns one classified group-product state consumed through distinct predicate, count, requiredness, full-validation, and partial-validation projections, plus the separate computation-arm presence projection and its `Nat` count. The two must not be merged behind a phase flag: over the same descendant cells a formally invalid one is unavailable in validation and *counts* in computation. [`laws`](../A12Kernel/Proofs/GroupPresence.lean) confine that disagreement to cells carrying a finding — monotonicity is unconditional in the measured direction, agreement holds in the clean region — and prove nothing about the row, structural, or relevance dimensions the computation projection does not consume. |
| Checked group-star terminals | [`StarGroup.lean`](../A12Kernel/Elaboration/StarGroup.lean) distinguishes terminal repeatable-row counts from one evaluation per selected starred ancestor for a nonrepeatable terminal. |
| Resolved Date-range overlap truth, admission, and scans | [`DateRangeOverlap.lean`](../A12Kernel/Semantics/DateRangeOverlap.lean) owns primitive truth, [`DateRangeOverlapOperators.lean`](../A12Kernel/Semantics/DateRangeOverlapOperators.lean) owns ordered scans, and [`DateRangeOverlap.lean`](../A12Kernel/Elaboration/DateRangeOverlap.lean) owns checked singular/plural admission and full-validation assembly. |
| Resolved direct Date construction and Base-Year sources | [`DateConstruction.lean`](../A12Kernel/Semantics/DateConstruction.lean), [`BaseYearDateSource.lean`](../A12Kernel/Semantics/BaseYearDateSource.lean), and checked component owners share typed Date construction without collapsing calendar provenance. |
| Berlin legacy calendar arithmetic | [`BerlinLegacyCalendarArithmetic.lean`](../A12Kernel/Semantics/BerlinLegacyCalendarArithmetic.lean) owns source-offset day mutation, compute-time month/year mutation, and completed month/year qualification over decoded labels plus exact instants. [`DateTimeDayDifference.lean`](../A12Kernel/Semantics/DateTimeDayDifference.lean) alone owns the stateful day-count algorithm and concrete-profile dispatch; it is not elapsed-duration division. |
| Addressed numeric placement and execution seam | [`AddressedNumericLeaf.lean`](../A12Kernel/Elaboration/AddressedNumericLeaf.lean) owns `CheckedAddressedNumericTarget`, which carries actual-environment selection, iteration, and the target policy for a repeatable Number target, and admits an operand at any scope that target's own scope binds rather than requiring equality. Five completed consumers: `FieldValueAsNumber`, `RangeAsNumber`, String `Length`, direct Number assignment, and a DateRange endpoint's numeric component. A further consumer joins by supplying a certified source and reusing the existing result/application owners; it does not add a second placement representation. |
| Certified direct-Number source certificate | Direct Number assignment owns the certificate carrying a Number source with exact source/target scale. `Abs`, the three `Round` modes, and `Min`/`Max` operand lists wrap that same certificate rather than introducing another source representation, so each adds only its own node semantics and result-scale contract. |
| Ordered Number-pair certificate | Shared by [`AddressedNumberField.lean`](../A12Kernel/Elaboration/AddressedNumberField.lean) for two-operand `+`/`−`/`×`, [`AddressedNumberDivision.lean`](../A12Kernel/Elaboration/AddressedNumberDivision.lean), and [`AddressedNumberPower.lean`](../A12Kernel/Elaboration/AddressedNumberPower.lean). Three completed consumers, each retaining its own derived-scale rule and warning-suppression selection while delegating arithmetic to the already-separated precision-50 scalar owner. |
| Derived numeric scale admission | [`NumericScale.lean`](../A12Kernel/Elaboration/NumericScale.lean) owns `NumericScaleSummary` and the `==`/`!=` comparison predicate that admits a declared computed-target scale rather than requiring equality. Consumed by `Min`/`Max` operand-list admission and by the comparison gate itself; a capable list may pad up to a larger declared scale while a capable larger derived scale is rejected. |
| Normalized `MessagePointer` | [`MessagePointer.lean`](../A12Kernel/Semantics/MessagePointer.lean) owns resolved field identity plus concrete/wildcard/unknown repetition coordinates. Four completed consumers: validation, computation, registered custom-field, and reached custom-condition error channels. Exact `CellAddr` embeds losslessly but remains a separate type, so wildcard and unknown never project as exact cells. |

### SG5 group-operand capabilities

<a id="cap-shared-entity-list-group-admission"></a>
#### Shared entity-list group admission

- `state`: implemented for the represented group slot and measured gates.
- `boundary`: the shared checker resolves an authored group in both repetition shapes, satisfies arity from the slot, and reads kind and category from one recursive declaration expansion.
- `boundary`: a starred path may terminate at a nonrepeatable group through the existing terminal-presence certificate; the authored group and its equivalent explicit fields are admitted, retain the reopened star, and publish the same wildcarded expansion references.
- `boundary`: star and indirect ancestor/descendant refusals are shared; an unstarred repeatable level below an earlier star projects `MVK_NO_WILDCARD`, while expansion-kind diagnostics remain operator-specific and unmapped when no class is established.
- `boundary`: exact non-wildcard field and fixed-group identities share one authored encounter scan; the duplicate whose second occurrence arrives first supplies `MVK_DUPLICATE_PARAM1`, starred group occurrences remain distinct, strict overlap follows as `MVK_DUPLICATE_PARAM2`, and singleton direct-field cardinality stays structurally separate.
- `boundary`: the plural String-literal value-list form projects `MVK_ONLY_STRING_ENUM_NUMBER_ALLOWED` only for a homogeneous Date-group expansion; its String-group control is retained, while Number and heterogeneous group refusals stay unmapped.
- `boundary`: token `FirstFilledValue` projects `MVK_NO_BOOLY_ALLOWED` for the exact two-Confirm fixed-group or two-direct-stored-field expansion and `MVK_VARYING_TYPES_NOT_ALLOWED` for the same carriers in ordinary-evaluated-String-then-Number declaration order; raw or custom String policies, reverse order, starred or filtered carriers, wider kind combinations, and wider widths remain unmapped.
- `owner`: [`FieldEntityList.lean`](../A12Kernel/Elaboration/FieldEntityList.lean), [`StarGroup.lean`](../A12Kernel/Elaboration/StarGroup.lean), [`TokenEntityValueList.lean`](../A12Kernel/Elaboration/TokenEntityValueList.lean), [`TokenFirstFilledValue.lean`](../A12Kernel/Elaboration/TokenFirstFilledValue.lean), [`StaticDiagnostic.lean`](../A12Kernel/Elaboration/StaticDiagnostic.lean), [`ValidationCondition/Reference.lean`](../A12Kernel/Elaboration/ValidationCondition/Reference.lean), and [`FlatModel.groupSubtreeFields`](../A12Kernel/Elaboration/Flat/Model.lean).
- `case`: [shared group admission cases](../A12Kernel/Conformance/FieldEntityGroupOperand.lean), [token group cases](../A12Kernel/Conformance/TokenEntityGroupOperand.lean), and [carrier-boundary and precedence cases](../A12Kernel/Conformance/TokenFirstFilledValue.lean).
- `assurance`: E closed for the represented admission boundary and diagnostic matrix; P partial through family completeness certificates plus diagnostic enumeration/code uniqueness; L locks the nonrepeatable Date/String value-list pair, fixed-group kind matrix, below-star terminal/refusal pair, and nine complete-code duplicate/overlap rows with both repair witnesses at a12-dmkits `2d384c59`; starred Date value-list remains `external evidence pending`; C/X/Q none.
- `source`: [entity-list group gates](SOURCES.md#src-entity-list-group-gates), [group-carrier admission sweep](SOURCES.md#src-group-carrier-admission-sweep), [group-carrier static admission](SOURCES.md#src-group-carrier-static-admission), and [group-carrier duplicate precedence](SOURCES.md#src-group-carrier-duplicate-precedence).
<a id="cap-number-group-aggregates"></a>
#### Number group aggregates

- `state`: partial.
- `boundary`: the Number carrier retains the authored group and a certified complete recursive Number expansion with uniform signedness.
- `boundary`: full validation resolves the operand-bounded `(row × field)` extent from model repeatability, binding only levels above the operand from the outer environment.
- `boundary`: `FieldValuesNotUnique`, `Sum`, extrema, and distinct count share that resolver; expansion-kind diagnostics remain keyed by operator.
- `limit`: non-Number, empty, and mixed-signedness expansions fail locally without inventing a Kernel class.
- `boundary`: the resolver emits cells declaration-major in stable model declaration order, with each declaration's canonical row order inside it.
- `limit`: Kernel correspondence for that order remains open on Number consumers; [token group expansion](#cap-token-group-expansion) owns the separately calibrated fixed and direct-single-star `FirstFilledValue` fragments.
- `limit`: only instantiated rows are enumerated and `hasUninstantiatedTail` is false; correspondence for a declared but uninstantiated tail is open.
- `owner`: [`NumberEntityList.lean`](../A12Kernel/Elaboration/NumberEntityList.lean) and [`CheckedStarDocument.lean`](../A12Kernel/Elaboration/CheckedStarDocument.lean).
- `case`: [group extent and admission cases](../A12Kernel/Conformance/FieldEntityGroupOperand.lean).
- `proof`: [`checkedNumberEntityGroup_expansion_complete`](../A12Kernel/Proofs/NumberEntityList.lean).
- `assurance`: E/P closed for retention and full-validation extent; L partial; C none; X none; Q none.
- `source`: [group runtime and reference correction](SOURCES.md#src-group-runtime-and-reference) and [group runtime differential](SOURCES.md#src-field-values-not-unique-group-runtime).
- `remains`: [Number and token group runtime](SEMANTICS-GAPS.md#gap-sg5-number-token-group-runtime).

<a id="cap-token-group-expansion"></a>
#### Token group expansion

- `state`: partial.
- `boundary`: the token carrier retains the authored group and a certified recursive expansion, pairing every reached cell with the declaration and operand that read it.
- `boundary`: a group authors no read form, so every member is read stored and homogeneity applies across the entire expansion rather than only between authored slots.
- `boundary`: full-validation `FieldValuesNotUnique` and token aggregate routes consume the shared operand-bounded extent.
- `boundary`: full-validation token `FirstFilledValue` consumes the shared checked extent for exactly one fixed group whose authored terminal is nonrepeatable, including recursive repeatable descendants, or for one terminal single-level starred group whose expansion contains only direct token declarations; the measured starred form may precede one nonrepeatable direct fallback.
- `boundary`: group cells remain declaration-major with canonical row order inside each declaration; the shared stop-at-first evaluator preserves selected token and reached-empty-prefix missingness, including a no-row group star before the direct fallback, while ignoring every suffix after selection.
- `extent`: [Number group aggregates](#cap-number-group-aggregates) owns the shared resolver's ordering and uninstantiated-tail limits.
- `limit`: `NumberOfValueInFields` currently admits a group literal only when every expanded declaration admits it; this is a local representation choice, not a Kernel claim.
- `limit`: a fixed operand whose authored terminal is itself repeatable, multi-level or nonterminal starred groups, starred groups with nested declarations, mixed lists other than the one measured direct fallback, computation, partial validation, and legacy raw-document routes remain explicitly unsupported by this first-filled path.
- `owner`: [`TokenEntityGroup.lean`](../A12Kernel/Elaboration/TokenEntityGroup.lean), [`TokenEntityValueList.lean`](../A12Kernel/Elaboration/TokenEntityValueList.lean), and [`TokenFirstFilledValue.lean`](../A12Kernel/Elaboration/TokenFirstFilledValue.lean).
- `case`: [token group cases](../A12Kernel/Conformance/TokenEntityGroupOperand.lean).
- `proof`: [`checkedTokenEntityGroup_expansion_complete`](../A12Kernel/Proofs/TokenEntityValueList.lean), [`checkedTokenEntityGroup_projections_stored`](../A12Kernel/Proofs/TokenEntityValueList.lean), and the generic [token first-filled scan and projection laws](../A12Kernel/Proofs/TokenFirstFilledValue.lean).
- `assurance`: E/P closed for retention, per-cell projection, recursive fixed-group full-validation extent, and the direct-single-star specialization through existing generic laws; L locks the unstarred nested-row/direct-before-nested matrix plus the four fixed-order and six starred-order/polarity rows at a12-dmkits `08115206`; C/X/Q none.
- `source`: [group-operand admission](SOURCES.md#src-field-values-not-unique-group-admission), [group runtime differential](SOURCES.md#src-field-values-not-unique-group-runtime), [fixed-group first-filled order](SOURCES.md#src-group-first-filled-runtime-order), and [starred-group first-filled order](SOURCES.md#src-star-group-first-filled-runtime-order).
- `remains`: [Number and token group runtime](SEMANTICS-GAPS.md#gap-sg5-number-token-group-runtime).

<a id="cap-temporal-group-uniqueness"></a>
#### Temporal group uniqueness

- `state`: partial.
- `boundary`: temporal uniqueness retains one recursively complete group expansion with one exact declared format.
- `owner`: [`TemporalValuesNotUnique.lean`](../A12Kernel/Elaboration/TemporalValuesNotUnique.lean).
- `case`: [temporal group cases](../A12Kernel/Conformance/TemporalEntityGroupOperand.lean).
- `proof`: [`checkedTemporalUniquenessGroup_expansion_complete`](../A12Kernel/Proofs/ValuesNotUnique.lean).
- `assurance`: E/P closed for retention and expansion completeness; L none; C none; X none; Q none.
- `source`: [group-carrier static admission](SOURCES.md#src-group-carrier-static-admission).
- `remains`: [temporal group runtime](SEMANTICS-GAPS.md#gap-sg5-temporal-group-runtime).

<a id="cap-boolean-confirm-group-value-count"></a>
#### Boolean/Confirm group value count

- `state`: partial.
- `boundary`: Boolean/Confirm value count retains the authored group and every descendant declaration; the constant-specific kind gate applies to each descendant.
- `boundary`: the structural reference projection expands the retained group, while every runtime route remains refused.
- `owner`: [`BooleanValueCount.lean`](../A12Kernel/Elaboration/BooleanValueCount.lean).
- `case`: [Boolean/Confirm group cases](../A12Kernel/Conformance/BooleanEntityGroupOperand.lean).
- `proof`: [`checkedBooleanValueCountGroup_expansion_complete`](../A12Kernel/Proofs/TokenValueCount.lean).
- `assurance`: E/P closed for retention and expansion completeness; L none; C none; X none; Q none.
- `source`: [group-carrier static admission](SOURCES.md#src-group-carrier-static-admission).
- `remains`: [Boolean/Confirm group runtime](SEMANTICS-GAPS.md#gap-sg5-boolean-group-runtime).

<a id="cap-group-operand-reference-projection"></a>
#### Group-operand reference projection

- `state`: implemented for admitted entity-list, group-list, nonrepeatable group-presence, and Boolean/Confirm count operands.
- `boundary`: a group contributes recursive descendant field pointers and never a group pointer; starred and unstarred forms differ by the shared coordinate rule rather than by expansion membership.
- `boundary`: one depth rule uses the referenced field's repeatable scope, preserves the bound concrete prefix, and wildcards every reopened level, including repeatable descendants of an unstarred group.
- `owner`: [`Reference.lean`](../A12Kernel/Elaboration/ValidationCondition/Reference.lean) and [`FlatModel.groupSubtreeFields`](../A12Kernel/Elaboration/Flat/Model.lean).
- `case`: [ordinary reference cases](../A12Kernel/Conformance/ValidationRule/OrdinaryReference.lean).
- `proof`: [reference projection laws](../A12Kernel/Proofs/ValidationCondition/Reference.lean).
- `separator`: retained cases fail if expansion stops at direct children, an unstarred group is wildcarded like a starred one, a fixed group is expanded only one level, or an unbound descendant is dropped or pinned.
- `assurance`: E/P closed for the named projection; L partial; C none; X none; Q none.
- `source`: [group runtime and reference correction](SOURCES.md#src-group-runtime-and-reference).
- `consumer`: Analyze and Explain must distinguish exact-reference membership from broader reachability; a field reachable only through expansion has no direct referrer.
- `limit`: an admitted unstarred repeatable group-presence operand still fails closed when its own level is unbound because no witness selects a concrete or wildcard coordinate.
- `remains`: [unstarred repeatable group-presence coordinates](SEMANTICS-GAPS.md#gap-sg5-unstarred-repeatable-group-presence-reference); unbound deeper descendants on other carriers, filtered-star operands, `RepetitionNotUnique`, and the message-record channel remain under [SG10](SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration).

## Reference-process exposure

[`Reference/Support.lean`](../A12Kernel/Reference/Support.lean) owns the current finite normalized support declaration; [`Reference/Protocol.lean`](../A12Kernel/Reference/Protocol.lean) and [`Reference/Evaluator.lean`](../A12Kernel/Reference/Evaluator.lean) decode and dispatch only the declared flat and one-group correlation operations. [`PROTOCOL.md`](PROTOCOL.md) owns the wire contract, [`ARTIFACTS.md`](ARTIFACTS.md) the shipped files, and the current capability kits their self-contained handovers. Reference exposure never upgrades internal or external evidence status.

## Current external-evidence gate

<a id="empty-handling-coverage-rule"></a>

[`EVIDENCE.md`](EVIDENCE.md) is the sole exact inventory of retained bundles, case identities, digests, replay commands, and finite claim limits. This map records only whether a semantic boundary has local calibration.

An internally closed capsule may remain `external evidence pending`. Before claiming kernel correspondence, select a coherent family, identify realistic wrong accounts, and retain the smallest source-maintained observation shape that separates them. Source reading, peer agreement, and theorem proof remain distinct from local calibration. The open cross-family calibration obligation is [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage).

## Trusted theorem surface

[`A12Kernel/Proofs.lean`](../A12Kernel/Proofs.lean) is the trusted theorem root and [`A12Kernel/TrustAudit.lean`](../A12Kernel/TrustAudit.lean) is its exhaustive audit registry. Current counts belong only in [`PLAN.md`](PLAN.md) as a verified resumption checkpoint; this map records coverage, not repository size.

## Maintenance rule

Update the affected clause or shared-owner entry when an implemented boundary, proof/non-law status, external-assurance dimension, public exposure, or consumer qualification changes. Replace the old sentence; do not append a capsule narrative. Do not copy open-work details from [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), source facts from [`SOURCES.md`](SOURCES.md), evidence inventories from [`EVIDENCE.md`](EVIDENCE.md), or current sequencing from [`PLAN.md`](PLAN.md).
