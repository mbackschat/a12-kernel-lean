# Numeric capabilities

Record keys and the `L`/`C`/`X`/`Q` assurance letters are defined once in the [hub's record-key legend](../IMPLEMENTATION-MAP.md#record-key-legend).

### §5 — numbers and decimals

<a id="cap-arithmetic-and-scale-analysis"></a>
#### Arithmetic and scale analysis

- `boundary`: Exact `Rat` values, signed scale summaries, constant expandability, precision-50 arithmetic, staged power, and domain failure. Repeatable direct-Number `+`, `−`, `×`, division, and power retain authored order and their exact static/target contracts through addressed execution; division and field-exponent power require explicit result-scale suppression, and power additionally certifies a scale-0 exponent
- `owner`: [`NumericArithmetic.lean`](../../A12Kernel/Semantics/NumericArithmetic.lean), [`NumericScale.lean`](../../A12Kernel/Elaboration/NumericScale.lean), [`NumericExpression.lean`](../../A12Kernel/Elaboration/NumericExpression.lean), [`AddressedNumberBinary.lean`](../../A12Kernel/Elaboration/AddressedNumberBinary.lean), [`AddressedNumberDivision.lean`](../../A12Kernel/Elaboration/AddressedNumberDivision.lean), [`AddressedNumberPower.lean`](../../A12Kernel/Elaboration/AddressedNumberPower.lean)
- `assurance`: E/P closed for implemented operators; real-kernel authoring and public interpreter outcomes cover the bounded addressed routes, while staged precision remains owned by the scalar arithmetic separators; C none
- `assurance`: **operator composition is Kernel-measured and locked** on the scalar carrier, in the admitting direction only ([checkpoint](../sources/computation-placement-and-constant-probes.md#src-aggregate-operand-list-takes-paths-only), [cases](../../A12Kernel/Conformance/NumericCompositionAdmission.lean)). A wrapper nests to the measured depth three and an aggregate is admitted inside a wrapper and in either operand position of an extremum; the estate carries no depth bound, which the rows record as their own behavior because the checkpoint searched for none. The refused direction needs no gate: an aggregate's source is a field-path list, so an expression there is unrepresentable rather than rejected. Static admission only — every retained row is a `--dry-run`, so evaluation, scale, and the target comparison are untouched
- `remains`: Remaining operators/authoring: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion)

<a id="cap-fillability-and-comparison"></a>
#### Fillability and comparison

- `boundary`: Directional empty provenance, scale-19 comparison, tolerance, and exact six-operator evaluation
- `owner`: [`NumericFillability.lean`](../../A12Kernel/Semantics/NumericFillability.lean), [`NumericComparison.lean`](../../A12Kernel/Semantics/NumericComparison.lean), [`NumericTolerance.lean`](../../A12Kernel/Semantics/NumericTolerance.lean)
- `assurance`: E/P closed for stated slice; selected C/X; upstream L partial
- `remains`: Clause-wide calibration: [SG12](../SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage)

<a id="cap-numeric-wrappers-and-extrema"></a>
#### Numeric wrappers and extrema

- `boundary`: Operation-form `Round`, `Abs`, and expression-valued operand-list `Min`/`Max` with per-call authoring boundaries. Repeatable direct-Number execution is closed for scale-preserving root `Abs`, all three root rounding modes with exact places/result scale, and every nonempty ordered `Min`/`Max` list in which each operand is a direct field, carries one operand-local `Abs`/Round, is one ordinary arithmetic, division, or power node whose two ordered operands are each a direct field or an immediate literal, or is one nested `Min`/`Max` call whose ordered leaves are direct fields, carry one operand-local `Abs`/Round, carry one ordinary arithmetic, division, or power child over direct fields or immediate literals, or are immediate literals, with one further immediate decoded literal admitted anywhere in the outer list.
- `boundary`: Each nested call retains its selector and leaf order as one outer operand, owns its own one-literal budget, contributes its dependency and leaf-scale union, and evaluates row-locally through the same authored-order selector. No operand has to be field-backed: the checked target certificate owns iteration and the target policy, so a constant-only child, a literal-only list, and any other fieldless list are admitted and still compute once per instantiated row.
- `boundary`: Extrema store one ordered operand list with no positional literal encoding, so authored order is retained structurally. Ordinary arithmetic retains its `NumericArithmeticOp` plus both inner operands without embedding a target-owning binary operation; division and power are separate variants over the same ordered checked child. Division derives unknown scale and therefore needs explicit warning suppression, while power separately certifies the exponent-scale gate and retains its narrow derived summary. The outer result scale is the maximum transformed operand/literal scale, and the checked operation retains whether the authored computation suppressed the exact-scale warning. Target admission is the shared `exactNumericScaleComparisonAllowedWithSuppression` predicate over those retained facts.
- `owner`: [`NumericRounding.lean`](../../A12Kernel/Semantics/NumericRounding.lean), [`AddressedNumberAbs.lean`](../../A12Kernel/Elaboration/AddressedNumberAbs.lean), [`AddressedNumberRound.lean`](../../A12Kernel/Elaboration/AddressedNumberRound.lean), [`AddressedNumberExtremum.lean`](../../A12Kernel/Elaboration/AddressedNumberExtremum.lean), [`NumericValidation/`](../../A12Kernel/Elaboration/NumericValidation/)
- `assurance`: E/P closed for the named bounded routes. Real-kernel authoring locks cover root wrappers, direct extrema, literals, local wrappers, ordinary field/literal arithmetic, and nested selector/literal-budget boundaries at the a12-dmkits [source registry](../SOURCES.md). Maintained kernel differentials additionally establish that division-by-zero and runtime-invalid integral power survive as the first operand of fixed-scope `Min` and the second operand of fixed-scope `Max`. Current `DM_INTERPRETER` output separately carries exact row pointers for focused addressed value/outcome classes; the opposite fixed-scope positions, addressed nested-wrapper/arithmetic runtime, and the exact combination of repeatable placement with outer or nested division/power-valued extrema, addressed poison causes, and addresses remain external evidence pending. C none.
- `assurance`: the **suppressed** power placement is Kernel-measured — a field or aggregate exponent is admitted into a scale-0 target under `errorCodesToSuppress` and refused without it ([checkpoint](../sources/computation-placement-and-constant-probes.md#src-power-derived-scale-and-operand-shapes)) — which supplies the evidence for this fragment's standing `scaleSuppressionRequired` certificate rather than leaving it source-derived.
- `remains`: Deeper nested calls, aggregate-valued operands, and power beyond the bounded field-or-literal child: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion)

<a id="cap-number-source-conversions"></a>
#### Number source conversions

- `boundary`: Checked Number fields, `BaseYear`, String `Length`, `RangeAsNumber`, `FieldValueAsNumber`, aggregate, and admitted temporal component/difference sources. Repeatable direct assignment, root wrappers, extrema, binary arithmetic, division, and field-exponent power share certified Number-source placement, which admits an operand at any scope the computed target's scope binds, while retaining operation-specific scale and suppression contracts.
- `boundary`: Extrema retain direct, local-wrapper, bounded field/literal arithmetic, division, power, and one-level nested operands over direct/local-wrapper/bounded-arithmetic/division/power/literal leaves plus at most one outer literal. Each binary variant reuses the ordered pair certificate and contributes both dependencies without a target-owning child; division retains unknown scale and warning suppression, while power retains its certified exponent gate and derived summary. Nested calls keep their own literal budget, selector, ordered dependencies, and scale union. Conversion retains exact String/Enumeration/category projection; range retains its 1-based inclusive interval; Length consumes normalized UTF-16 text
- `boundary`: direct temporal-component validation now preserves whether a source declaration is non-temporal or temporal without the requested component. `YearFromDate` over the flat model's exact seven authorable non-temporal kinds projects to `MVK_NO_DATE`; full-Date `HoursFromTime` projects to `MVK_WRONG_DATE_FORMAT_FOR_OP`. Adjacent extractor/format pairs and the separate numeric-computation error surface remain unmapped unless measured, so a consumer can return insufficient information instead of inheriting either diagnostic.
- `boundary`: the same split governs a temporal **operand**, measured separately rather than inherited: extractor and completed-period-difference admission reads the component set alone, so `HoursFromTime` takes a DATE-declared `HH:mm:ss` field, `YearFromDate` takes a TIME-declared `yyyy-MM-dd` one, `DifferenceInYears` mixes that TIME declaration with an ordinary DATE, and a declared Base Year admits `YearFromDate` on a field with no date component at all ([checkpoint](../SOURCES.md#src-temporal-operand-family-is-the-formats-not-the-kinds))
- `boundary`: seven shipped conjuncts fell to that row set — the six `dateDifference` admission sites and `TemporalNumericPart.admittedBy` — each having refused operands the Kernel admits, and the diagnostic projection stopped keying on the kind because both complete-date declarations draw the same code. `CalendarDayDifference.admitsKind` and the `dateTimeDifference` conjunct stay kind-keyed: their witness is a DATE declaration at a full date-and-time format, which no row here carries.
- `owner`: [`NumericSource.lean`](../../A12Kernel/Elaboration/NumericSource.lean), [`NumericValidation/Core.lean`](../../A12Kernel/Elaboration/NumericValidation/Core.lean), [`NumericValidation/Resolution.lean`](../../A12Kernel/Elaboration/NumericValidation/Resolution.lean), [`AddressedNumericLeaf.lean`](../../A12Kernel/Elaboration/AddressedNumericLeaf.lean), [`AddressedNumberField.lean`](../../A12Kernel/Elaboration/AddressedNumberField.lean), [`AddressedNumberExtremum.lean`](../../A12Kernel/Elaboration/AddressedNumberExtremum.lean), addressed Number family owners
- `assurance`: E/P closed for named routes. Real-kernel gates and current row-addressed interpreter observations cover bounded non-suppressed placements. The exact direct-extractor diagnostic controls are locked in [`Conformance/NumericValidation/Temporal.lean`](../../A12Kernel/Conformance/NumericValidation/Temporal.lean) against the reviewed [PR2 extraction checkpoint](../SOURCES.md#src-pr2-date-extraction-diagnostics), including negative controls that keep unmeasured pairs unmapped. a12-dmkits [source registry](../SOURCES.md) restores structured suppression authoring and clean division/power representatives, while maintained differentials own fixed-scope extremum-wrapper preservation and broader kernel-strategy agreement. Exact repeatable composition, cause order, and rich addressed partitions remain Lean-locked; C none outside flat Number
- `remains`: Remaining repeatable expressions and ingestion: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion)

<a id="cap-number-aggregates"></a>
#### Number aggregates

- `boundary`: Direct/plain-star/filtered-star streams; ordered Sum, extrema, counts, and row-aligned products. A `SumOfProducts` pair requires the same immediate owning group, an unfiltered star only at the lowest repeatable level, every unstarred repeatable ancestor already bound by the declaring scope, and identical topology; that owning group may be fixed below the starred ancestor. Its measured pair-dependent refusals project all five exact Kernel classes. Under partial validation, the four combiner aggregates use the normalized wildcard-dominance-reduced universal extent gate inside each current iteration subtree; `SumOfProducts` retains its complete-cross-product gate; numeric `NumberOfValueInFields` uses the existential value-list extent. The obsolete uncalibrated one-covering predicate is removed
- `owner`: [`NumericAggregate.lean`](../../A12Kernel/Semantics/NumericAggregate.lean), [`NumericAggregate/`](../../A12Kernel/Elaboration/NumericAggregate/)
- `assurance`: E/P closed for admitted shapes; upstream L is exact for the complete bounded `SumOfProducts` admission/refusal matrix at its [static checkpoint](../SOURCES.md#src-sum-of-products-owning-group) and for the represented normalized direct one-level count extent at the [count partial-extent checkpoint](../SOURCES.md#src-count-partial-extent), otherwise partial; C none
- `remains`: Semantic-root versus physical-root relevance identity; group-scope admission, checked-document runtime extent, carrier-specific Kernel calibration of the internally admitted nested lowest-star path whose outer repeatable ancestor is already bound by the declaring scope, and family limits: [Number group aggregates](../IMPLEMENTATION-MAP.md#cap-number-group-aggregates). Wider aggregate/path shapes: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) and [SG9](../SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion)

<a id="cap-stored-number-formal-read"></a>
#### Stored Number formal read

- `boundary`: Decimal-valued input retains signed coefficient/scale, derives strip-and-minimum-scale plain text, and is checked from that text; String-valued input stays verbatim. The checked document retains exact regime, selected text, and parsed/formal result separately. Nonrepeatable and repeatable addressed `FieldValueAsString` consume that selected text without reconstructing it from `Rat`; the addressed route preserves exact row keys through source-relative result classification and retained-action application
- `owner`: [`NumericInput.lean`](../../A12Kernel/Semantics/NumericInput.lean), [`CheckedDocument.lean`](../../A12Kernel/Elaboration/CheckedDocument.lean), [`StringComputation.lean`](../../A12Kernel/Elaboration/StringComputation.lean), [`AddressedFieldValueAsString.lean`](../../A12Kernel/Elaboration/AddressedFieldValueAsString.lean)
- `assurance`: E/P closed through the stated computation/result/application paths; upstream L at [source registry](../SOURCES.md), [source registry](../SOURCES.md), [source registry](../SOURCES.md), plus source-owned addressed authorability and three-engine route preflight at [source registry](../SOURCES.md); the exact addressed scale/empty/poison matrix is internally locked but not separately Kernel-calibrated; C none
- `remains`: Host ingestion and remaining addressed expression placements: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion)

<a id="cap-stored-decimal-identity"></a>
#### Stored decimal identity

- `boundary`: Exact coefficient/scale identity kept separate from expression `Rat`; scale-19 canonical amount and bounded computed rendering
- `owner`: [`NumericStoredNumber.lean`](../../A12Kernel/Semantics/NumericStoredNumber.lean)
- `assurance`: E/P closed; peer-triangulated; C none
- `remains`: Concrete host decimal ingestion: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion)

<a id="cap-number-target-and-delta"></a>
#### Number target and delta

- `boundary`: Target admission, warning-suppressed no-fit branch, exact target checks, source-relative classification, direct one-outcome application, retained CLEARED placement, dependency projection, checked separate-destination application at scalar or exact repeatable addresses, and finite direct one- and two-level projections with normalized parent-scoped predecessor rows, preserved destination cells, exact action coordinates, and explicit later `Number > 0` validation addresses
- `owner`: [`NumericTarget.lean`](../../A12Kernel/Semantics/NumericTarget.lean), [`NumericComputationResult.lean`](../../A12Kernel/Semantics/NumericComputationResult.lean), [`NumericApplication.lean`](../../A12Kernel/Semantics/NumericApplication.lean), [`NumericComputation/RunApplication.lean`](../../A12Kernel/Elaboration/NumericComputation/RunApplication.lean), [`CheckedDocument.lean`](../../A12Kernel/Elaboration/CheckedDocument.lean)
- `assurance`: E/P closed for the stated target/action and bounded one- and two-level applied-document projections; upstream L at [source registry](../SOURCES.md), [source registry](../SOURCES.md), and reviewed a12-dmkits [source registry](../SOURCES.md) for the complete one-level topology and later-validation address set, plus a locally executed three-engine two-level extension at that clean revision for parent-scoped inner padding and exact later-validation addresses; Q closed for the bounded Execute/Analyze query; C none
- `remains`: Number application beyond two direct finite levels, non-Number application, general later-rule execution and pointer rendering, complete transport materialization, and compatibility certification: [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition), [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion), [SG12](../SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage)

<a id="cap-scalar-number-computation"></a>
#### Scalar Number computation

- `boundary`: Checked alternatives, finite supplied-order run, rich result, relation, generic whole-result application, and the checked separate-destination projection shared with exact repeatable addresses. Bounded repeatable direct Number assignment at a target-bound operand scope, scale-preserving root `Abs(Number)`, all three root Round modes, nonempty `Min`/`Max` over direct/local-wrapper/bounded field-or-literal arithmetic, division, or power operands plus one immediate outer literal and one-level nested `Min`/`Max` operands over direct/local-wrapper/bounded-arithmetic/division/power/literal leaves, `+`/`−`/`×`, scale-suppressed division, scale-0-field-exponent power, `FieldValueAsNumber`, `RangeAsNumber`, String `Length`, and the numeric Date component of a selected DateRange endpoint specialize the checked target/source placement without widening the scalar expression elaborator.
- `boundary`: Every checked scalar operation and assembled table row retains its computation declaration group independently of the fixed target that owns runtime scope; only target-owned evaluation ignores that Analyze/Transform identity.
- `boundary`: That placement admits an operand exactly when the computed target's repeatable scope **binds** every level the operand crosses, which is the measured Kernel rule and not scope equality, so a form-level constant feeding a row is ordinary; each operand is read at its own path inside the current target row, and a deeper operand is refused with the shared out-of-scope class that reports `MVK_NO_WILDCARD`. A sibling operand instead selects the parallel-iteration route, which admits matching index-field names, reports `MVK_ERROR_FIELD_INVALID_INDEX` for different names, and reports `MVK_NO_WILDCARD` when no index exists.
- `boundary`: Both halves are Kernel-locked at the [operand-scope checkpoint](../SOURCES.md#src-computation-operand-scope), the admission across five operand shapes and the own-path read on both codegen strategies for a filled, an empty, and a formally invalid outer operand, plus a **two-level** target where a middle-level operand reaches exactly the leaves of its own enclosing row and clears exactly those on invalidity. [Nested-placement cases](../../A12Kernel/Conformance/AddressedNestedPlacement.lean) own that branch, because the placement rather than any leaf decides it. Every reached target-scale refusal projects exactly to `MVK_INVALID_COMPARE_DEC_PLACES`.
- `boundary`: `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD` projects across the admitted arithmetic surface: `+`, `-`, or `*` reading the target on exactly one side, against an immediate literal or a different direct Number field of any scale. External rows now cover that surface completely: a12-dmkits [source registry](../SOURCES.md) measures the whole 24-cell operator × position × operand-kind grid plus twelve suppression rows, so no cell of the admitted arithmetic surface rests on the uniform gate order alone. The gate order rather than the authored shape is the mechanism, so scale mismatch precedes self-reference across that surface, and suppressing only the scale code exposes self-reference.
- `boundary`: Division, a both-sides target read, nested or specialized reads, invalid authoring, unsupported expressions, and every other target-reading computation retain their immediate structural self-reference refusal, even when a later operand would otherwise fail resolution, and remain unmapped from an exact self-reference class. Fixed multi-group `NumberOfFilledGroups` statically admits at least two distinct disjoint fixed nonrepeatable operands, preserves exact short-arity, duplicate, overlap, and missing-star diagnostic identity, and counts each operand over its **whole subtree** at any depth as an ordinary exact `Nat` count over the compute-arm presence projection. A repeatable group at or below an operand is refused **on the scalar route only**, on a measured boundary: content there has a second constituent, an instantiated row, which a cell-read projection expresses at no width ([checkpoint](../SOURCES.md#src-repeatable-descendant-group-count)).
- `boundary`: the addressed route carries the document and answers that shape, deciding a fixed operand by the scalar cells outside its repeatable descendants **or** an instantiated row in one. That disjunction is locked inside a single operand on a shell owning both constituents, with a neither-constituent control; a descendant more than one repetition level below the operand is refused rather than composed, since nothing measures it.
- `boundary`: the **starred** group-count spelling is now **statically admitted**, alone or beside a fixed operand, and the arity minimum is applied to the unstarred form only — one operand is a complete list exactly when it is starred. `SurfaceGroupCountOperand` carries the two surface forms, a list containing a star lowers to `CheckedGroupCountOperand` over the existing starred-group certificate, and a fixed-only list keeps the established resolved atom untouched. A starred path navigating by parent count, and a starred **nonrepeatable** terminal, are both refused as unmeasured on this carrier.
- `boundary`: the **addressed** route evaluates a mixed list, reading each operand into a `GroupCountOperandReading` and folding: a fixed operand keeps the same cell projection the fixed-only form uses, so the two forms cannot disagree about a group they both reach, and a starred operand contributes `CheckedStarredGroupSource.inCapacityRowCount` over the context's document. The **scalar** route refuses with the repeatable-context fault a repeatable aggregate already gets, which is correct rather than incomplete: its whole input is `FieldId → CheckedCell`.
- `boundary`: excluding an over-limit row is **measured on this arm**, not inherited from the validation carrier. A group declared `max 5` holding five, six, or seven instantiated rows counts five, on both codegen strategies ([checkpoint](../SOURCES.md#src-starred-group-count-computation)). The measurement was taken because the exclusion's only prior evidence was rule-arm, and these two arms diverge on this same operator for a formally invalid operand, so the agreement could not be assumed.
- `boundary`: an operand list admits **any number** of starred operands, including the same group twice: cardinalities add once per authored **position** and nothing deduplicates them, which is why the fixed operands' overlap gate is deliberately not extended over starred ones. The runtime fold is measured on a12-dmkits' kernel oracle — three rows named twice count six, where a fold over distinct groups counts three ([checkpoint](../SOURCES.md#src-starred-group-count-computation)). Static admission is measured too, on the peer's estate through the Kernel's own `checkConsistency` with refusing rows in the same run as the live-gate control, so the elaborator's admission now rests on an observation rather than on the recorded wildcard-duplicate gate alone.
- `boundary`: the validation carrier refuses the starred operand on this shared atom by design, because this project's validation account owns the starred group count in its own operator; narrowing one here would mint a second representation of it.
- `boundary`: Wider runtime shapes and the model-free resolved evaluator refuse structurally in the preflight, ahead of any read, and the two causes are now **separate classes**: a group outside the admitted scalar boundary names the offending operand's path so an Explain or Qualify consumer can point at it, while the model-free evaluator's refusal says only that no model was supplied, since no single operand is at fault there
- `owner`: [`NumericComputation/`](../../A12Kernel/Elaboration/NumericComputation/), [`AddressedNumericLeaf.lean`](../../A12Kernel/Elaboration/AddressedNumericLeaf.lean), [`AddressedNumberField.lean`](../../A12Kernel/Elaboration/AddressedNumberField.lean), [`AddressedNumberAbs.lean`](../../A12Kernel/Elaboration/AddressedNumberAbs.lean), [`AddressedNumberRound.lean`](../../A12Kernel/Elaboration/AddressedNumberRound.lean), [`AddressedNumberExtremum.lean`](../../A12Kernel/Elaboration/AddressedNumberExtremum.lean), [`AddressedNumberBinary.lean`](../../A12Kernel/Elaboration/AddressedNumberBinary.lean), [`AddressedNumberDivision.lean`](../../A12Kernel/Elaboration/AddressedNumberDivision.lean), [`AddressedNumberPower.lean`](../../A12Kernel/Elaboration/AddressedNumberPower.lean)
- `owner`: [`AddressedFieldValueAsNumber.lean`](../../A12Kernel/Elaboration/AddressedFieldValueAsNumber.lean), [`AddressedRangeAsNumber.lean`](../../A12Kernel/Elaboration/AddressedRangeAsNumber.lean), [`AddressedStringLength.lean`](../../A12Kernel/Elaboration/AddressedStringLength.lean), [`AddressedDateRangeBoundPart.lean`](../../A12Kernel/Elaboration/AddressedDateRangeBoundPart.lean)
- `assurance`: E/P closed for the named routes, action projection, exact scale projection, and bounded self-reference projection. Fixed filled-group computation admission for lists of two and three is Kernel-confirmed at clean a12-dmkits [source registry](../SOURCES.md), with larger-list correspondence external evidence pending, while its subtree runtime projection is measured on both arms at [source registry](../SOURCES.md), held by two extent laws, and extended to a formally invalid nested descendant by one local both-strategy probe at [source registry](../SOURCES.md), clean real-kernel target-scale matrices at [source registry](../SOURCES.md) and accepted peer [source registry](../SOURCES.md) establish the shared comparison predicate and exact scale class.
- `assurance`: Exact declaration-group retention is E/P closed. The cross-group constant separator retains `/Rules` definition identity beside the `/Store/StockLevel` target and the unchanged `-1` result; P locks checked-group validity and same-target group preservation through table assembly. Its target-owned fixed execution is Kernel-confirmed at the [fixed target-scope checkpoint](../SOURCES.md#src-fixed-computation-target-scope) and again on both strategies at the [declaring-group gate checkpoint](../SOURCES.md#src-computation-declaring-group-gate), which additionally measures that a repeatable declaring group adds no repetition of its own. Q closes the bounded artifact-only [Analyze placement inventory](../consumer-probes/ANALYZE.md#fixed-scalar-placement-probe): a cold consumer separated declaration from execution group and kept the two-row same-target table in authored order without reconstructing placement from operand-free literals.
- `assurance`: the bounded target-reference precedence and both exact classes are Kernel-confirmed at clean a12-dmkits [source registry](../SOURCES.md) and widened across operator and operand position at reviewed inbound [source registry](../SOURCES.md), with the differing-scale field operand and its suppressed counterpart measured locally at clean a12-dmkits [source registry](../SOURCES.md) under pending [SPEC-2026-08-11-01](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-11-01--the-computed-number-scale-gate-precedes-self-reference-for-a-differing-scale-field-operand-too), real-kernel/static plus row-addressed interpreter observations cover arithmetic field pairs inside extrema at [source registry](../SOURCES.md), [source registry](../SOURCES.md), and [source registry](../SOURCES.md)
- `assurance`: static nested-call boundaries are Kernel-confirmed at [source registry](../SOURCES.md), while addressed nested runtime, including local-wrapper and bounded-arithmetic/division/power leaves, remains external evidence pending
- `assurance`: exact inner first-cause order and rich result partitions remain Lean-locked, C none, bounded Number/String target-diagnostic Q
- `remains`: Heterogeneous scheduling and complete document integration: [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition). Starred filled-group computation runtime and remaining repeatable numeric expressions: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) and [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion)

<a id="cap-addressed-numeric-operation-consumer"></a>
#### Addressed numeric-operation consumer

- `boundary`: Exact bounded footprint distinguishes an ordered nonempty expression-operand list, prior target-state classification read, and target write; the fingerprint retains scope, full target policy, conversion projection/interval, direct versus root `Abs`, root Round mode/authored result scale, and `Min`/`Max` operation plus its derived scale summary and warning-suppression bit. It also retains exact ordered direct-field/operand-local-`Abs`/operand-local-Round/arithmetic/division/power/inner-and-outer-literal identity, including a one-level nested selector with its ordered direct/local-wrapper/bounded-arithmetic/division/power/literal leaves.
- `boundary`: Fingerprint equality is Analyze-only; exact identity is the sole Transform; `admitsTargetScale`/`admittedTargetScales` decide candidate declared target scales from the retained summary and suppression bit through the elaborator's gate and are proved to agree with each checked operation's own admission at its declared scale
- `owner`: [`AddressedNumericOperationConsumer.lean`](../../A12Kernel/Elaboration/AddressedNumericOperationConsumer.lean), [proofs](../../A12Kernel/Proofs/AddressedNumericOperationConsumer.lean), [probe](../../A12Kernel/Conformance/AddressedNumericOperationConsumer.lean)
- `assurance`: E/P/Q closed for the named bounded Analyze/identity-Transform/retarget-legality task; the retarget decision shares the elaborator's gate universally, identity universally preserves the complete Analyze fingerprint as well as rich execution and result views, so no probe case rechecks it, while stored-to-category, changed-endpoint, direct-to-root-`Abs`, same-scale root-Round mode, extrema operation/order/cardinality, literal position/authored scale, direct-to-local-`Abs`, local-Round mode/places, arithmetic operation/inner order/outer position/inner-literal value and authored scale — including a product and a sum that derive the same outer scale — nested selector/leaf-wrapper/arithmetic/division/power/order identity, and cross-family fingerprints differ without implying semantic inequivalence; no kernel-evidence claim.
- `assurance`: Its joint checked-model fixture isolates conversion, interval, wrapper, mode, scale, and ordered-list changes and the universal identity relation covers both rich outcome and result-view boundaries; it adds no harness or intermediate representation
- `remains`: Any nonidentity Transform requires its own semantic relation and consumer hypothesis
