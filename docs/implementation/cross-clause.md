# Cross-clause capabilities and gates

## Cross-clause owners

<a id="cross-clause-implementation-notes"></a>
<a id="resolved-number-aggregates"></a>
<a id="stringenumeration-aggregate-counts"></a>
<a id="stringenumeration-distinct-count"></a>
<a id="resolved-validation-group-presence"></a>
<a id="checked-group-star-terminals"></a>

These stable names help when a query crosses numbered clauses. Their capability status is already in the rows above.

<a id="cap-kernel-static-diagnostic-classes"></a>
#### Kernel static-diagnostic classes

- `owner-and-boundary`: [`StaticDiagnostic.lean`](../../A12Kernel/Elaboration/StaticDiagnostic.lean) owns the established `MVK_` vocabulary and exact identifiers; [`laws`](../../A12Kernel/Proofs/StaticDiagnostic.lean) guard enumeration distinctness and completeness. Each family projects its local refusals and returns `none` where no class is established. The bounded [Translate/Explain probe](../../A12Kernel/Conformance/ComputationTargetDiagnosticConsumer.lean) preserves accepted, mapped, and family-local unmapped outcomes. This is distinct from the public process's [`Reference.Support.DiagnosticCode`](../../A12Kernel/Reference/Support/Diagnostics.lean).
- `owner-and-boundary`: Coverage includes all three `FieldValuesNotUnique` overloads, the [shared group-admission gates](../IMPLEMENTATION-MAP.md#cap-shared-entity-list-group-admission), the [bounded `SumOfProducts` pair matrix](../IMPLEMENTATION-MAP.md#cap-number-aggregates), the [`RuleGroup` suffix, indexed `RepetitionNotUnique` refusal, and starred semantic-index refusal](repetition-and-paths.md#cap-semantic-index), and the [indexed filled-group-count refusal](../IMPLEMENTATION-MAP.md#cap-group-list-diagnostics).
- `owner-and-boundary`: Coverage also includes the [repeatable-operand rule locus](../IMPLEMENTATION-MAP.md#cap-date-range-repeatable-operand-locus) projected once at the resolver's own error type, singular DateRange overlap group refusal, the bounded token `FirstFilledValue` kind pair, [Boolean/Confirm constant computation target admission](../IMPLEMENTATION-MAP.md#cap-boolean-confirm-constant-computation-target-admission), fixed filled-group computation admission, computed Number, ordinary String and Enumeration targets, partial computed-Date targets, condition-pattern admission, and raw-String `Length`; the hardcoded pairings in [`ValidationProjection.lean`](../../A12Kernel/Evidence/ValidationProjection.lean) remain consolidation candidates when this vocabulary reaches them.

<a id="cap-resolved-firstfilledvalue"></a>
#### Resolved `FirstFilledValue`

- `owner-and-boundary`: [`FirstFilledValue.lean`](../../A12Kernel/Semantics/FirstFilledValue.lean) and checked elaboration own ordered mixed String/stored-Enumeration selection; computation specializes the same selection rather than reimplementing it.

<a id="cap-resolved-number-aggregates"></a>
#### Resolved Number aggregates

- `owner-and-boundary`: [`NumericAggregate.lean`](../../A12Kernel/Semantics/NumericAggregate.lean) and [`NumericAggregate/`](../../A12Kernel/Elaboration/NumericAggregate/) own checked direct/plain-star/filtered-star streams, ordered precision-50 folds, validation/computation projections without a second iterator, the combiner family's normalized universal partial-extent gate, and numeric value count's existential extent per current iteration row. Full-validation `Sum`, the extrema, `NumberOfDifferentValues`, and the Number value count select the in-capacity cell projection for plain and filtered stars plus group operands; direct fields keep the complete formal-cell view, and the immutable checked document retains every over-limit formal cell either way. The filtered Number branch is internally selected with external evidence pending; the other named operand forms carry their own retained measurements.
- `owner-and-boundary`: [`StarPath.lean`](../../A12Kernel/Elaboration/StarPath.lean) owns the separate universal and existential predicates.

<a id="cap-string-enumeration-aggregate-counts"></a>
#### String/Enumeration aggregate counts

- `owner-and-boundary`: [`TokenDistinctCount.lean`](../../A12Kernel/Elaboration/TokenDistinctCount.lean) and [`TokenValueCount.lean`](../../A12Kernel/Elaboration/TokenValueCount.lean) reuse checked entity-list topology while retaining token/category identity and family polarity. Token value count uses the same existential partial extent internally as the numeric overload; Kernel source shares the loop, but the token partial runtime matrix remains externally uncalibrated. Checked full validation selects the shared in-capacity projection for a plain or filtered starred String field, while direct and Enumeration/category operands retain the complete projection. The value-count-specific starred-group surface admits the measured sole terminal-repeatable String group through common stored-projection group certification, serves full validation and checked computation from a dedicated whole-group carrier, and selects an in-capacity projection before computation classifies content.
- `assurance`: E/P close the plain and filtered starred String full-validation extent with the over-limit-only separators and a resolver-restatement theorem. The retained [plain](../SOURCES.md#src-token-starred-field-capacity) and [filtered](../SOURCES.md#src-filtered-starred-field-capacity) Kernel checkpoints record their exact three-row matrices as raw evidence; they establish neither `L` nor `C`. Filtered Number Kernel correspondence, Enumeration/category, partial, and computation carriers remain open. Q closes both the [plain-](../consumer-probes/EXPLAIN.md#plain-star-capacity-probe) and [filtered-star capacity](../consumer-probes/EXPLAIN.md#filtered-star-capacity-probe) Explain decisions; the latter's repaired-basis rerun recovered every decision independently after its amber predecessor exposed the sibling movement conflict.
- `owner-and-boundary`: direct nonrepeatable Custom declarations join ordinary String and stored Enumeration in the distinct-count token family. Runtime consumes a caller-prepared checked Custom String through the existing token scan and exact identity fold, so this operator does not resample or assume a registered validator.
- `assurance`: E locks direct Custom admission beside String and internal exact-token execution in [`Conformance/TokenDistinctCount.lean`](../../A12Kernel/Conformance/TokenDistinctCount.lean); a direct-Custom rejection mutant fails that case. L is exact only for static family placement at the reviewed [PR2 kind-family checkpoint](../SOURCES.md#src-pr2-distinct-count-kind-families). Custom runtime identity, registered-validator integration, and wildcard or group Custom operands remain unmeasured.
- `owner-and-boundary`: The fixed nonrepeatable path group remains a separate full-validation-only surface. Group-scope retention, assurance, and per-cell projection are owned by [token group expansion](../IMPLEMENTATION-MAP.md#cap-token-group-expansion).

<a id="cap-boolean-confirm-value-count"></a>
#### Boolean/Confirm value count

- `owner-and-boundary`: [`BooleanValueCount.lean`](../../A12Kernel/Elaboration/BooleanValueCount.lean) applies the distinct `True`/`False` field-kind matrix and derives fixed canonical tokens from checked direct/plain-star/filtered-star values. Existing scalar and addressed computation plus generated mismatch validation retain fixed scale 0, exact source/filter references, omitted-tail movement, and reached formal poison. Empty Confirm remains unfilled, every direct or starred `False` Confirm source is rejected statically, and scalar plans reject repeatable sources explicitly. Group-scope retention, assurance, reference projection, checked-document full validation, and fieldwise single-axis starred-group computation are owned by [Boolean/Confirm group value count](../IMPLEMENTATION-MAP.md#cap-boolean-confirm-group-value-count).
- `boundary`: checked full validation selects only in-capacity cells for plain or filtered starred Boolean fields under both constants and Confirm fields under `True`. On a plain Confirm star, empty in-capacity cells retain grow-only movement while a fully confirmed in-capacity extent is fixed. A reached filter instead permits growth on every available count and permits shrinkage exactly when it selected a currently counted match. Direct operands retain the complete checked-cell projection.
- `assurance`: E closes the plain Boolean/Confirm ladders plus the exact structural-filter ladders in [`Conformance/TokenValueCount.lean`](../../A12Kernel/Conformance/TokenValueCount.lean); P states the exact projection for every checked Boolean/Confirm star in [`Proofs/TokenValueCount.lean`](../../A12Kernel/Proofs/TokenValueCount.lean). The retained [Boolean](../SOURCES.md#src-boolean-starred-field-capacity), [Confirm](../SOURCES.md#src-confirm-starred-field-capacity), and exact structural-filtered String/Boolean-`True`/Confirm-`True` [capacity checkpoint](../SOURCES.md#src-filtered-starred-field-capacity) are bounded raw external evidence rather than `L` or `C` coverage; filtered Boolean `False`, wider filters, partial, computation, and nested carriers remain open externally.
- `assurance`: Q closes both the [plain-](../consumer-probes/EXPLAIN.md#plain-star-capacity-probe) and [filtered-star capacity](../consumer-probes/EXPLAIN.md#filtered-star-capacity-probe) Explain decisions; the repaired-basis filtered rerun recovered every decision independently after its amber predecessor exposed the former boundary conflict; X none.

<a id="cap-reopened-star-structural-completeness-and-addressing"></a>
#### Reopened-star structural completeness and addressing

- `owner-and-boundary`: [`StarCompleteness.lean`](../../A12Kernel/Semantics/StarCompleteness.lean), [`StarAddressing.lean`](../../A12Kernel/Semantics/StarAddressing.lean), and checked document/path owners keep topology independent of filled cells.

<a id="cap-resolved-group-presence-both-arms"></a>
#### Resolved group presence, both arms

- `owner-and-boundary`: [`GroupPresence.lean`](../../A12Kernel/Semantics/GroupPresence.lean) owns one classified group-product state consumed through distinct predicate, count, requiredness, full-validation, and partial-validation projections, plus the separate computation-arm presence projection and its `Nat` count. The two arms must not be merged behind a phase flag: over the same descendant cells a group whose *only* content is formally invalid is unavailable in validation and *counts* in computation. [`laws`](../../A12Kernel/Proofs/GroupPresence.lean) confine that disagreement to cells carrying a finding — monotonicity is unconditional in the measured direction, agreement holds in the clean region — and prove nothing about the row, structural, or relevance dimensions the computation projection does not consume.
- `boundary`: within the validation arm the tally and count consumers read one decided-presence projection, `asGroupListPresence`. **A formal error blocks only the negative presence answer**, so a group with admitted content is filled whatever else its descendants carry, and only an undecided group makes a count unavailable. `numberOfFilledGroups` once re-derived the tally condition beside the projection and disagreed with it; the count is now defined through that projection. `filledGroupCount_indifferentToErrorOnDecidedGroup` and `undecidedHead_makesFilledGroupCountUnknown` state that boundary directly.
- `boundary`: the scalar `GroupNotFilled` leaf tests the same positive `definitelyFilled` projection before the raw error flag: admitted content plus an independent error is `.notFired`, while a content-free error remains `.unknown`. `groupNotFilled_notFired_of_definitelyFilled` states that boundary without an error hypothesis.
- `assurance`: E for the error dimension at the [unavailability checkpoint](../SOURCES.md#src-group-count-unavailability) on both codegen strategies — the fires-with-a-filled-sibling row against the silent malformed-only row, repeated on the second operand and controlled by an unreferenced-group error. P for the tally and count laws above. The structural leg is measured too: a group carrying an over-limit error still counts when its content is admitted, so `definitelyFilled` ignoring `erroneous` is right for both error kinds rather than only the cell one.
- `assurance`: the scalar leaf's `.notFired` rather than `.unknown` result is internally selected and externally uncalibrated. P through `groupNotFilled_notFired_of_definitelyFilled`; `ConditionTree.evalVerdict_collapseUnknown` proves that the pure tree evaluator's collapsed message projection is identical. It does not equate raw verdicts or cover structural-error evaluation.
- `assurance`: the **coverage** dimension is a consequence of the shared projection and not a measured row: `validateFull` cannot observe partial coverage, so visible content deciding a partly covered group is stated as mechanism. Not measured: a call-local partial-coverage error, and `noneRelevant` coverage.

<a id="cap-checked-group-star-terminals"></a>
#### Checked group-star terminals

- `owner-and-boundary`: [`StarGroup.lean`](../../A12Kernel/Elaboration/StarGroup.lean) distinguishes terminal repeatable-row counts from one evaluation per selected starred ancestor for a nonrepeatable terminal. Both threshold predicates and the numeric starred-group count select the shared instantiated in-capacity extent before tallying; the physical topology still retains over-limit rows.
- `boundary`: Full validation covers terminal repeatable stars and nonrepeatable terminals below starred ancestry. Partial validation covers a sole terminal-repeatable star: a fully covered extent counts every in-capacity row, a concrete relevant in-capacity row counts structurally whatever its cells hold, and a selection confined to over-limit rows is unavailable rather than an empty operand. It also covers a sole nonrepeatable terminal below starred ancestry by selecting in-capacity ancestor environments first and resolving each terminal through the relevance-aware group product: visible content can decide a partly covered group as filled, while clean absence requires complete descendant coverage. The checked-rule addressed partial entry preserves structural failures outside semantic `UNKNOWN` on both routes. Mixed or repeated starred lists remain unsupported.
- `assurance`: E closes the named full boundary, the one-level `/Order/Lines*` partial carrier at the [starred-group capacity checkpoint](../SOURCES.md#src-starred-group-quantifier-capacity), and the internally derived nonrepeatable-terminal route with separators for partly covered content, complete empty coverage, an unavailable peer, and an over-limit populated ancestor. P states the generic in-capacity selection and relevance-aware resolver delegation exactly. Kernel correspondence for the nonrepeatable-terminal route and multi-level partial execution remains pending rather than inheriting the one-level terminal-repeatable observation.
- `remains`: Kernel calibration of partial nonrepeatable terminals and multi-level terminal stars, wider partial behavior, and mixed or repeated starred lists remains with [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion).

<a id="cap-flat-mandatory-information"></a>
#### Flat mandatory-information derivation

- `boundary`: [`MandatoryInformation.lean`](../../A12Kernel/Semantics/MandatoryInformation.lean) models the measured flat, nonrepeatable rule-set fragment as authored-shape-preserving rules and derives `mandatory`, `mandatoryForRootGroup`, and `mandatoryRootGroups` independently. The executable closure covers direct negative field and root requirements, the measured root-only field-list forms, field-guarded and root-guarded targets, reverse-authored finite dependency chains, finite direct and field-list guard cycles, and the measured no-contribution categories. Retained two-node cycles separate seeded closure from inert unseeded components and existential from universal list truth; exact four-node direct and reverse-authored existential all-list-edge cycles now carry retained Kernel correspondence, with a disjoint unseeded universal-list component remaining inert.
- `boundary`: Singleton `FieldNotFilled` rules retain severity identity. ERROR contributes its field and root, while the exact measured WARNING and INFO forms remain visible as checked no-contribution rules; this does not generalize the severity result to wider conditions or compositions.
- `boundary`: Declaration-derived field-required modes retain their origin without claiming the Kernel's internal implementation. `always` contributes its field globally and root-relatively plus its root; isolated `ifParentPresent` contributes only the root-relative field. Parent-present requiredness joins the existing root closure with unconditional declarations and authored direct field or root seeds; wider authored interactions fail closed.
- `boundary`: The exact unfiltered singleton-field matrix over one direct unindexed repeatable child of a nonrepeatable root retains one ordinary `FieldNotFilled(Note)` beside `NoFieldFilled`, `NotAllFieldsFilled`, or `AtLeastOneFieldFilled` over `Rows*/Value` under `And` and `Or`. `Or` preserves `Note`, with `NotAllFieldsFilled` additionally contributing `Rows/Value`; `And` with either negative quantifier contributes only the root, while the positive conjunction contributes nothing. The checked identity retains that measured scope separately and rejects every wider scope. The filtered twin remains a whole-rule exclusion; other repeatable presence shapes remain outside.
- `boundary`: The model-derived requirement identity for one optional String index directly inside a repeatable group remains visible but contributes no returned member. It leaves a same-root direct requirement visible and does not promote a parent-present declaration globally; the field's root remains part of static single-root admission without becoming a global result seed.
- `boundary`: The generated-validation identity for one unconditional direct copy between same-root nonrepeatable scale-0 Number fields retains source and target but contributes no returned member. It likewise leaves a same-root direct requirement visible and does not promote a parent-present declaration globally. Both generated identities participate in static single-root topology but their explanation references do not change another rule's count-shape isolation; generated validation execution remains owned elsewhere.
- `boundary`: Filled-field list guards retain `AtLeastOneFieldFilled` and `AllFieldsFilled` as distinct authored identities. In the flat checked closure the existential exposes its target after any listed field is mandatory, while the universal requires every listed field; guard rules authored before their seeds reach the same fixed point even when the target points back into the premise list. Empty or duplicate lists and cross-root shapes retain the existing fail-closed boundary.
- `boundary`: The bounded count slice retains each decoded authored literal in a privately constructed threshold beside its shared Java-rounded signed-32-bit value. Standalone filled-count `< z` rules contribute only the root when that narrowed value is positive; the exact calibrated distinct-count `< 1` rule shares that root-only result, while arbitrary-threshold equality remains an internal carrier law rather than a Kernel claim. No numeric bound remains structurally distinct while sharing the sentinel outcome.
- `boundary`: The four measured filled-count guard spellings contribute their target only when the available admitted inputs establish the measured count, the narrowed value is not the `-1` sentinel, and the comparison holds. Inputs may be direct singleton seeds, isolated to that guard, or the exact target of one direct-seeded ordinary field guard. A zero- or one-seed guard with otherwise isolated operands remains accepted and returns only its actual seeds without an unmet target; the exact isolated `>= 0` target-only rule contributes its target to both field sets plus its root, while the adjacent `>= 1` rule returns three empty sets. An admitted filled-count target may be isolated, feed ordinary field guards, or become the first operand of the exact reverse-named two-stage scale-0 `>= 2` to `>= 1`/`>= 2` chain whose five field roles remain distinct.
- `boundary`: Distinct-count guards retain no target contribution for the exact seeded `>= 2` shape and the measured isolated adjacent pair under all four comparison spellings, while preserving authored comparison and scale-zero threshold identity.
- `boundary`: [`deriveCheckedMandatoryInformation`](../../A12Kernel/Semantics/MandatoryInformation.lean) is the consumer entry. It rejects empty multi-field forms, contributing multiple-root rule sets, unclassified cross-root guards, duplicate count operands, filled-count operand or target reuse outside the exact admitted roles, and distinct-count shapes outside the exact authored-scale-zero seeded and isolated branches; comparison truth decides filled-count contribution rather than admission.
- `boundary`: The retained filtered, exact literal-key semantic-index, exact two-group parallel-iteration, and exact two-root disjunction categories are modeled as whole-rule no-contribution identities. The three captures externally separate every referenced field and the cross-root capture additionally separates the `Decision` root, while same-root control contributions mask the other root attributions. The cross-root identity is excluded before contributing-root admission. Filter and iteration internals, wider indexed shapes, and contributing cross-root forms remain outside the carrier.
- `assurance`: E separates field contribution, all six operator/connective cells in the unfiltered repeatable presence matrix, singleton negative-field severity, declaration-derived unconditional versus parent-present requiredness, promotion from declaration/root/direct-field seeds, generated-index and generated direct nonrepeatable scale-0 Number-copy identities versus returned contribution, global root promotion, and count-shape isolation, dependency-derived count operands, downstream count-target reuse, root-only contribution, no contribution, dependency direction, seeded versus unseeded direct and list-guard cycles, existential versus universal cycle truth, four-node direct and multi-list-edge closure, unmet guards, global versus root-relative fields, root-seed promotion, and reverse-authored guard closure.
- `assurance`: E also separates semantic-indexed, parallel-iterated, and cross-root field exclusion versus ordinary negative-leaf extraction, the internally modeled whole-rule exclusion, filled-count versus distinct-count seeded and isolated target entailment, zero/one/two direct-seed cardinality, threshold wrapping around `2^32`, the authored-`-1` collision, true versus false arms for all four filled-count guard spellings, absent bounds, and fail-closed unsupported shapes.
- `assurance`: P reduces every exact unfiltered repeatable presence cell to its established flat result without erasing the retained starred identity, and proves the existing flat laws, isolated declaration-mode bridges, root-seeded parent-present closure, generated-identity inertness beside controls, INFO exclusion, whole-rule exclusions, reverse-authored field-list closure, count-root operator independence, reversed-strict equivalence, and checked threshold pairing.
- `assurance`: X records green isolated Analyze+Explain readbacks of the flat threshold, filled-field guard, count-identity, singleton-severity, validation-cycle, whole-rule-exclusion, exact generated-identity, and direct-repeatable-child presence slices. The exclusion reader retained all four distinct ignored identities and referenced-field inventories, recovered every exact checked result and refusal, rejected all eight wrong accounts, and made no semantic guess. Its cold review narrowed external assurance to what the same-root controls separate: every referenced field plus the independent cross-root `Decision` root, not attribution of the masked roots.
- `assurance`: The latest [generated-identity reader](../consumer-probes/ANALYZE.md#flat-mandatory-information-probe) retained both exact generated identities and references, recovered seven three-set compositions plus two count-isolation controls, refused a wider form, and rejected all ten wrong accounts. Cold review first exposed the overbroad copy identity and count-admission coupling; the corrected clean basis passed build-free convergence and the isolated rerun. The reversed-strict, false-comparison, and seed-cardinality specializations have no separate isolated run.
- `assurance`: The latest [repeatable-presence reader](../consumer-probes/ANALYZE.md#flat-mandatory-information-probe) retained exact scope, connective, quantifier, and both references across all six results, refused cross-root and wider normalized scopes, kept a wider two-field syntax insufficient, and rejected all ten wrong accounts in 81 seconds without a semantic guess. Cold review first exposed omitted topology and nested-scope admission; red/green correction made scope explicit, then build-free D1–D7 convergence and a new isolated rerun found no remaining issue.
- `assurance`: The latest [count-dependency reader](../consumer-probes/ANALYZE.md#flat-mandatory-information-probe) recovered the exact admitted true, false-with-fields, ordinary-downstream, and empty triples; refused deeper, disjoined-target, and directly seeded target reuse; preserved every count and dependency role; and rejected all ten wrong accounts in 341 seconds without a semantic guess. It kept the unrequested true count target with no independently mandatory field or root insufficient from the frozen packet; the subsequent target-only Kernel checkpoint closes that separator without changing the checked consumer decision. Triggered cold review passed D1–D7 before the clean probe basis was committed.
- `assurance`: The latest [distinct target-only reader](../consumer-probes/ANALYZE.md#flat-mandatory-information-probe) recovered the filled-count and distinct-count target-only pairs, the seeded distinct-count control, and seven exact refusals while preserving operator, comparison, operand order, target, and authored-scale/narrowed threshold identity. It separated accepted empty from refusal and rejected all ten wrong accounts in 101 seconds without a semantic guess. Triggered cold review first exposed narrowed-threshold aliasing, an overbroad seeded branch, and imprecise raw-model prose; red/green correction and the amended D1–D7 review converged before the clean probe basis was committed.
- `assurance`: The latest [distinct comparison reader](../consumer-probes/ANALYZE.md#flat-mandatory-information-probe) recovered the accepted-empty adjacent pair for all four authored comparison spellings, the seeded distinct control, three true filled-count separators, and six exact refusals. It preserved operator, comparison, operand order, target, and authored-scale/narrowed threshold identity; separated accepted empty from refusal; and rejected all ten wrong accounts in 133 seconds without a semantic guess. Triggered cold review exposed non-count operand-reuse over-admission and an untriggered Plan edit; red/green correction kept the measured count-family reuse, refused the extra role, removed the Plan edit, and converged D1–D7 before the clean probe basis was committed.
- `assurance`: The reverse-named [count-chain checkpoint](../sources/rule-set-meta-information.md#src-mandatory-information-count-chain) externally separates both-target closure from downstream-only and seed-only suffixes; the checked self-target and forward-order controls keep every wider topology fail-closed.
- `assurance`: The latest [count-chain reader](../consumer-probes/ANALYZE.md#flat-mandatory-information-probe) recovered all three exact suffixes, both pre-existing dependency controls, and ten order, alias, seed, operator, or topology refusals while preserving the least closure and authored identities. It rejected all ten wrong accounts in 123 seconds without a semantic guess. Triggered cold review exposed the unmeasured count/seed order and both hygiene overruns; red/green correction admitted the retained order, extracted the narrow conformance owner, split the oversized record, and converged D1–D7 before the clean probe basis was committed.
- `assurance`: The twenty-five [mandatory-information checkpoints](../sources/rule-set-meta-information.md) are bounded raw Kernel observations with no typed replay, so they establish neither L nor C.
- `remains`: Other count collector sites and longer, branching, differently ordered, or otherwise reused count dependencies, wider Boolean formulas, still-longer or mixed-edge cycle correspondence, wider repetition and parallel-iteration shapes, concrete indices and wider semantic-index shapes, filter internals, declaration interaction beyond unconditional/direct field/root seeds, other generated index and computation-validation shapes, contributing cross-root shapes, and wider root topology remain with [SG14](../SEMANTICS-GAPS.md#sg14--mandatory-information-derivation).

<a id="cap-fixed-group-bound-repeatable-ancestry"></a>
#### Fixed groups under bound repeatable ancestry

- `state`: partial.
- `boundary`: An ordinary nonrepeatable terminal group below repeatable ancestry is a fixed operand on the four measured carriers exactly when its repeatable scope equals the declaring rule group's scope. Their explicit rule-bound resolver retains that exact scope, while the established default resolver remains scalar-only for every unmeasured carrier. A nonrepeatable operand outside every repeatable scope remains scalar, while a strict-prefix caller, a deeper unbound scope, and a terminal repeatable group still refuse through `repeatableGroupRequiresAddress`.
- `boundary`: The checked scope drives all three validation-side consumers. A fixed `NumberOfFilledFields` group binds its expansion to the current enclosing row, a fixed group-list operand contributes the same ordinary iteration scope and requires addressed evaluation, and fixed `NumberOfFilledGroups` uses the addressed ordered-numeric route with the same row-local products. The ordered numeric carrier refuses partial addressed evaluation structurally rather than manufacturing an `UNKNOWN` result from unavailable scalar group state.
- `boundary`: Group-list and numeric-count reference projection traverse the retained fixed groups directly. Both keep the bound ancestor concrete at the firing row and reopen only repeatable descendant levels as wildcards.
- `boundary`: The computation representative is a separate exact-address carrier rather than a widening of the scalar Number operation. It accepts at least two disjoint nonroot fixed groups and an unconstrained scale-0 repeatable Number target only when every operand, target, and declaring scope is the same nonempty scope, counts computation-phase group presence per physical target row, clears over-limit target rows through the shared row walker, and returns exact `CellAddr` outcomes. Other target profiles and wider expressions remain outside the measured boundary.
- `owner`: shared [`SingleGroup.lean`](../../A12Kernel/Elaboration/SingleGroup.lean), [`FieldEntityList.lean`](../../A12Kernel/Elaboration/FieldEntityList.lean), validation [`Core`](../../A12Kernel/Elaboration/ValidationCondition/Core.lean) and [`StaticIteration`](../../A12Kernel/Elaboration/ValidationCondition/StaticIteration.lean), numeric-validation [`Core`](../../A12Kernel/Elaboration/NumericValidation/Core.lean), [`Ordered`](../../A12Kernel/Elaboration/NumericValidation/Ordered.lean), and [`Evaluation`](../../A12Kernel/Elaboration/NumericValidation/Evaluation.lean), plus [`RepeatableNumberFixedGroupCountComputation.lean`](../../A12Kernel/Elaboration/RepeatableNumberFixedGroupCountComputation.lean).
- `case`: [`FixedGroupRepeatableAncestorCarriers.lean`](../../A12Kernel/Conformance/FixedGroupRepeatableAncestorCarriers.lean), with the rule-bound resolver red/green lock retained in [`ValidationCondition.lean`](../../A12Kernel/Conformance/ValidationCondition.lean).
- `assurance`: E closes exact-scope admission on all four consumers, default-resolver refusal for unmeasured carriers, scalar fixed-group preservation, and the strict-prefix, deeper-unbound, repeatable-terminal, and scalar-computation refusals.
- `assurance`: E also closes addressed group-list dispatch, structural partial-numeric refusal, both row-local validation verdicts, fixed-group filled-field counts, computation values and exact addresses, nested-descendant presence, and matching group-list/numeric pointer coordinates.
- `assurance`: P is partial through the established group-presence/count, pointer, and checked-address laws; no theorem claims Kernel correspondence.
- `assurance`: The [four-carrier checkpoint](../SOURCES.md#src-fixed-group-repeatable-ancestor-carriers) retains both Kernel strategies and the cross-row-union separator. Its raw capture has no typed replay consumer and therefore establishes neither L nor C.
- `assurance`: X none. Q is bounded by the isolated [two-document Execute+Explain probe](../consumer-probes/EXECUTE.md#fixed-group-bound-repeatable-ancestry-probe), whose 17 tests reject eight wrong accounts while leaving the stated wider routes excluded.
- `source`: [fixed child groups below a repeatable ancestor](../SOURCES.md#src-fixed-group-repeatable-ancestor-carriers) and the earlier [filled-field-count ancestor carrier](../SOURCES.md#src-filled-field-count-deep-capacity).
- `remains`: unequal or outside operand scopes, constrained or nonzero-scale computation targets, partial validation, formal and over-limit interactions, wider count expressions, and other repeatable group shapes remain with [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion); the exact implemented boundary is closed.

<a id="cap-mixed-validation-filled-group-count"></a>
#### Mixed validation `NumberOfFilledGroups`

- `boundary`: One validation filled-group count whose operand list carries at least one star, compared against a literal threshold. The Kernel admits this shape and refuses the unstarred spelling of a repeatable member with `MVK_NO_WILDCARD`, so the star is mandatory rather than optional. The fixed-only list keeps the established scalar atom untouched and is refused here, so widening the operator costs the already-measured form nothing.
- `boundary`: Contributions add. A fixed member gives its filled indicator, and a starred member gives its **in-capacity instantiated row count** — an instantiated but wholly empty row still counts, under an operator named `NumberOfFilledGroups`. That is the same quantity the sole-star carrier computes, so the two cannot disagree. A fixed member whose presence is undecided makes the whole count unknown, through the shared decided-presence projection rather than a second copy of its condition; both legs of that rule are measured on this carrier. A star contributes no unknown of its own, and that is now measured too: a malformed cell inside a starred row leaves the row counted and the ladder unchanged.
- `boundary`: The static gates run in a **measured order**: containment before rootness. A root beside its own descendant draws the overlap class in either operand order, and the root class is reserved for a root beside a disjoint operand. This carrier had them inverted — the gates were reused from the fixed-only list but their order was silently re-derived, which is [`LF116`](../LEAN-FINDINGS.md) at the level of an ordering rather than a mechanism. Arity is a minimum of two rather than a pair, and admission does not depend on where the star sits.
- `boundary`: Movement runs against the sum of each member's own capacity — one per fixed group, the declared row maximum per star — never against the operand-list length, which the neighbouring [field count](numeric-and-temporal.md#cap-filled-field-count) got wrong in its own currency. An unretained row maximum leaves no finite extent and yields unavailability rather than an invented rule, matching the starred carrier; that branch is a total function with no authorable model reaching it ([declaration-domain checkpoint](../SOURCES.md#src-repeatability-declaration-domain)). The arm resolves only on the **addressed** route, because a star's row count needs document topology the scalar context does not carry.
- `boundary`: The duplicate gate reaches every member, not only the fixed ones. Strict containment between any two members refuses with the Kernel's containment class whichever side carries the star; two fixed members naming the same group keep the distinct equal-operand class; and two **stars** naming the same group are admitted, counting that group once per position with its capacity likewise counted twice. That exception is what makes this a containment rule rather than a distinctness rule, and it is measured rather than inherited from the fixed-only carrier.
- `owner`: [`GroupPresence.lean`](../../A12Kernel/Semantics/GroupPresence.lean) owns the fold, [`StarGroup.lean`](../../A12Kernel/Elaboration/StarGroup.lean) the operand certificate shared with the computation arm, [`Resolution.lean`](../../A12Kernel/Elaboration/NumericValidation/Resolution.lean) the elaborator, [`Evaluation.lean`](../../A12Kernel/Elaboration/NumericValidation/Evaluation.lean) the addressed resolver, with [proofs](../../A12Kernel/Proofs/MixedFilledGroupCount.lean) and [cases](../../A12Kernel/Conformance/MixedFilledGroupCount.lean).
- `assurance`: E closes the count ladder one through four against a `max 3` star, with the capacity boundary as the polarity separator. Instantiated-versus-filled rows count two whether both, one, or neither row carries a value, and emptying the fixed member drops the total by exactly one.
- `assurance`: E also closes the two-star sum and the same-star-twice doubling of both count and boundary, the containment refusals in both orders and both spellings against their admitted star-star and disjoint controls, and the fixed-only, single-operand, and root-group refusals.
- `assurance`: P closes the two laws that keep the widening honest: on a fixed-only list the count is exactly the established one, unknown arm included, so no existing consumer changes meaning by being routed through the new carrier; and the declared extent is the list length exactly on a fixed-only list, so a star is precisely what makes the two quantities part company.
- `assurance`: L is the [list-extent checkpoint](../SOURCES.md#src-group-count-list-extent) on both codegen strategies — the count ladder, the capacity boundary's `VALUE_ERROR` against three `OMISSION_ERROR` rows below it, the instantiated-but-empty separator, the two-star sum with summed capacities, the same star twice double-counting to a doubled boundary, and the duplicate decision table. The two items it left open are closed at the [unavailability checkpoint](../SOURCES.md#src-group-count-unavailability): an unavailable member's interaction with the sum, and a starred member owning a further repeatable descendant, whose rows are **not** multiplied by it. An unretained repeatability is closed as unreachable at the [declaration-domain checkpoint](../SOURCES.md#src-repeatability-declaration-domain). C/X/Q none.
- `assurance`: L now covers **both arms**. The whole gate table — the two duplicate classes, the equal-star exception, the root class in either order with either side starred, and containment before rootness — reproduces on the computation arm at the [static-gate checkpoint](../SOURCES.md#src-group-count-static-gates-both-arms), so the two arms share one containment predicate and one root gate rather than two derivations.
- `assurance`: that measurement also fixed a defect on the other arm. The computation arm had gated overlap over its fixed operands alone and carried no root gate at all, corrected in [`NumericComputation/Core.lean`](../../A12Kernel/Elaboration/NumericComputation/Core.lean).
- `remains`: partial addressed evaluation is structurally unsupported for this atom, and wider operand shapes stay with [SG13](../SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion).

<a id="cap-resolved-date-range-overlap-truth-admission-and-scans"></a>
#### Resolved Date-range overlap truth, admission, and scans

- `owner-and-boundary`: [`DateRangeOverlap.lean`](../../A12Kernel/Semantics/DateRangeOverlap.lean) owns primitive truth, [`DateRangeOverlapOperators.lean`](../../A12Kernel/Semantics/DateRangeOverlapOperators.lean) owns ordered scans, and [`DateRangeOverlap.lean`](../../A12Kernel/Elaboration/DateRangeOverlap.lean) owns checked singular/plural admission and full-validation assembly.

<a id="cap-resolved-direct-date-construction-and-base-year-sources"></a>
#### Resolved direct Date construction and Base-Year sources

- `owner-and-boundary`: [`DateConstruction.lean`](../../A12Kernel/Semantics/DateConstruction.lean), [`BaseYearDateSource.lean`](../../A12Kernel/Semantics/BaseYearDateSource.lean), and checked component owners share typed Date construction without collapsing calendar provenance.

<a id="cap-berlin-legacy-calendar-arithmetic"></a>
#### Berlin legacy calendar arithmetic

- `owner-and-boundary`: [`BerlinLegacyCalendarArithmetic.lean`](../../A12Kernel/Semantics/BerlinLegacyCalendarArithmetic.lean) owns source-offset day mutation, compute-time month/year mutation, and completed month/year qualification over decoded labels plus exact instants. [`DateTimeDayDifference.lean`](../../A12Kernel/Semantics/DateTimeDayDifference.lean) alone owns the stateful day-count algorithm and concrete-profile dispatch; it is not elapsed-duration division.

<a id="cap-addressed-numeric-placement-and-execution-seam"></a>
#### Addressed numeric placement and execution seam

- `owner-and-boundary`: [`AddressedNumericLeaf.lean`](../../A12Kernel/Elaboration/AddressedNumericLeaf.lean) owns `CheckedAddressedNumericTarget`, which carries actual-environment selection, iteration, and the target policy for a repeatable Number target, and admits an operand at any scope that target's own scope binds rather than requiring equality. Five completed consumers: `FieldValueAsNumber`, `RangeAsNumber`, String `Length`, direct Number assignment, and a DateRange endpoint's numeric component. A further consumer joins by supplying a certified source and reusing the existing result/application owners; it does not add a second placement representation.

- `boundary`: Placement against the declaring group is **containment**, not equality: the target is admitted from its own group and from any ancestor, and refused only from a group it does not lie at or below. A declaring-group validity check runs first, because `[]` is a prefix of every path and would otherwise admit every placement vacuously. The same rule holds in the shared [`AddressedRepeatableTarget.lean`](../../A12Kernel/Elaboration/AddressedRepeatableTarget.lean) certificate and in the Enumeration, `FieldValueAsString`, `DateFromDateTime`, and `TimeFromDateTime` copies. L is the [declaring-group gate checkpoint](../SOURCES.md#src-computation-declaring-group-gate), which owns that measurement and its limits.

<a id="cap-certified-direct-number-source-certificate"></a>
#### Certified direct-Number source certificate

- `owner-and-boundary`: Direct Number assignment owns the certificate carrying a Number source with exact source/target scale. `Abs`, the three `Round` modes, and `Min`/`Max` operand lists wrap that same certificate rather than introducing another source representation, so each adds only its own node semantics and result-scale contract.

<a id="cap-ordered-number-pair-certificate"></a>
#### Ordered Number-pair certificate

- `owner-and-boundary`: Shared by [`AddressedNumberField.lean`](../../A12Kernel/Elaboration/AddressedNumberField.lean) for two-operand `+`/`−`/`×`, [`AddressedNumberDivision.lean`](../../A12Kernel/Elaboration/AddressedNumberDivision.lean), and [`AddressedNumberPower.lean`](../../A12Kernel/Elaboration/AddressedNumberPower.lean). Three completed consumers, each retaining its own derived-scale rule and warning-suppression selection while delegating arithmetic to the already-separated precision-50 scalar owner.

<a id="cap-derived-numeric-scale-admission"></a>
#### Derived numeric scale admission

- `owner-and-boundary`: [`NumericScale.lean`](../../A12Kernel/Elaboration/NumericScale.lean) owns `NumericScaleSummary` and the `==`/`!=` comparison predicate that admits a declared computed-target scale rather than requiring equality. Consumed by `Min`/`Max` operand-list admission and by the comparison gate itself; a capable list may pad up to a larger declared scale while a capable larger derived scale is rejected.

<a id="cap-normalized-messagepointer"></a>
#### Normalized `MessagePointer`

- `owner-and-boundary`: [`MessagePointer.lean`](../../A12Kernel/Semantics/MessagePointer.lean) owns resolved field identity plus concrete/wildcard/unknown repetition coordinates. Four completed consumers: validation, computation, registered custom-field, and reached custom-condition error channels. Exact `CellAddr` embeds losslessly but remains a separate type; a bound pointer recovers it, while every successfully reopened pointer contains a wildcard and provably cannot. This exact-versus-partial shape is the measured address-kind distinction, and deliberately does not retain whether an authored star or group expansion produced it.

<a id="cap-having-correlated-direct-field-identity"></a>
#### Correlated direct-field identity inside `Having`

- `state`: implemented for the direct field-list source nested inside a filter.
- `boundary`: candidate and captured references resolve against their own available repetition scope, then exact identity retains origin, resolved declaration, and read form through one authored encounter scan. `($P, P)` therefore survives with both operands in order while `(P, P)` reaches the shared `MVK_DUPLICATE_PARAM1` projection.
- `owner`: [`FieldEntityList.lean`](../../A12Kernel/Elaboration/FieldEntityList.lean) owns the surface, checked source, and shared duplicate scan; [`Correlation.lean`](../../A12Kernel/Elaboration/Correlation.lean) owns the candidate/captured scope choice.
- `case`: [`CorrelationElaboration.lean`](../../A12Kernel/Conformance/CorrelationElaboration.lean) separates origin erasure, identity erasure, and slot-order dependence.
- `proof`: [`havingDirectField_inner_outer_pair_unique`](../../A12Kernel/Proofs/CorrelationElaboration.lean) and its reverse-order sibling establish origin-sensitive uniqueness for every resolved declaration and read form.
- `assurance`: E/P close the checked direct-source identity. Kernel L covers `($P, P)` and `(P, P)` in that authored order under `Sum`, `NumberOfFilledFields`, and `NumberOfDifferentValues` at the [reviewed source checkpoint](../SOURCES.md#src-pr2-correlated-operand-identity). The reverse pair and `($P, $P)` have internal typed locks only; carrier kind gates, embedding in the complete filter grammar, runtime aggregation, and wider nested expressions remain open; C/X/Q none.


### SG5 group-operand capabilities

<a id="cap-shared-entity-list-group-admission"></a>
#### Shared entity-list group admission

- `state`: implemented for the represented group slot and measured gates.
- `boundary`: the shared checker resolves an authored group in both repetition shapes, satisfies arity from the slot, and reads kind and category from one recursive declaration expansion.
- `boundary`: a starred path may terminate at a nonrepeatable group through the existing terminal-presence certificate; the authored group and its equivalent explicit fields are admitted, retain the reopened star, and publish the same wildcarded expansion references.
- `boundary`: star and indirect ancestor/descendant refusals are shared; an unstarred repeatable level below an earlier star projects `MVK_NO_WILDCARD`, while expansion-kind diagnostics remain operator-specific and unmapped when no class is established.
- `boundary`: exact non-wildcard field and fixed-group identities share one authored encounter scan; the duplicate whose second occurrence arrives first supplies `MVK_DUPLICATE_PARAM1`, starred group occurrences remain distinct, strict overlap follows as `MVK_DUPLICATE_PARAM2`, and singleton direct-field cardinality stays structurally separate.
- `boundary`: the plural String-literal value-list form projects `MVK_ONLY_STRING_ENUM_NUMBER_ALLOWED` only for a homogeneous Date-group expansion; its String-group control is retained, while Number and heterogeneous group refusals stay unmapped.
- `boundary`: token `FirstFilledValue` projects `MVK_NO_BOOLY_ALLOWED` for the exact two-Confirm fixed-group or two-direct-stored-field expansion and `MVK_VARYING_TYPES_NOT_ALLOWED` for the same carriers in ordinary-evaluated-String-then-Number declaration order; raw or custom String policies, reverse order, starred or filtered carriers, wider kind combinations, and wider widths remain unmapped.
- `owner`: [`FieldEntityList.lean`](../../A12Kernel/Elaboration/FieldEntityList.lean), [`StarGroup.lean`](../../A12Kernel/Elaboration/StarGroup.lean), [`TokenEntityValueList.lean`](../../A12Kernel/Elaboration/TokenEntityValueList.lean), [`TokenFirstFilledValue.lean`](../../A12Kernel/Elaboration/TokenFirstFilledValue.lean), [`StaticDiagnostic.lean`](../../A12Kernel/Elaboration/StaticDiagnostic.lean), [`ValidationCondition/Reference.lean`](../../A12Kernel/Elaboration/ValidationCondition/Reference.lean), and [`FlatModel.groupSubtreeFields`](../../A12Kernel/Elaboration/Flat/Model.lean).
- `case`: [shared group admission cases](../../A12Kernel/Conformance/FieldEntityGroupOperand.lean), [token group cases](../../A12Kernel/Conformance/TokenEntityGroupOperand.lean), and [carrier-boundary and precedence cases](../../A12Kernel/Conformance/TokenFirstFilledValue.lean).
- `assurance`: E closed for the represented admission boundary and diagnostic matrix; P partial through family completeness certificates plus diagnostic enumeration/code uniqueness; L locks the nonrepeatable Date/String value-list pair, fixed-group kind matrix, below-star terminal/refusal pair, and nine complete-code duplicate/overlap rows with both repair witnesses at a12-dmkits [source registry](../SOURCES.md); starred Date value-list remains `external evidence pending`; C/X/Q none.
- `source`: [entity-list group gates](../SOURCES.md#src-entity-list-group-gates), [group-carrier admission sweep](../SOURCES.md#src-group-carrier-admission-sweep), [group-carrier static admission](../SOURCES.md#src-group-carrier-static-admission), and [group-carrier duplicate precedence](../SOURCES.md#src-group-carrier-duplicate-precedence).
<a id="cap-number-group-aggregates"></a>
#### Number group aggregates

- `state`: partial.
- `boundary`: the Number carrier retains the authored group and a certified complete recursive Number expansion with uniform signedness.
- `boundary`: full validation resolves the operand-bounded `(row × field)` extent from model repeatability, binding only levels above the operand from the outer environment.
- `boundary`: checked-document computation resolves one shared rich group operand. Starred `Sum`, extrema, and distinct count retain its complete checked-cell projection, while Number value count selects its in-capacity projection before computation-phase content classification.
- `boundary`: **on the validation arm every consumer the two accounts separate now reads the in-capacity projection**, the token distinct count and `FirstFilledValue` included, measured on a starred field, a starred group, and a fixed group ([checkpoint](../sources/group-list-and-capacity-probes.md#src-capacity-consumer-sweep), outbound [`SPEC-2026-08-31-14`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-14)). `FieldValuesNotUnique` deliberately keeps the complete view on both carriers: it skips a formally unavailable operand exactly as it skips an empty, so the two projections give the same verdict on every document and the retention claims no Kernel distinction. `resolvedCheckedEntityOperandCore_inCapacity_sublist` states the selection once at its owning mechanism: the extent is a sublist of the complete view and contains exactly the cells carrying no over-repetition finding, so every consumer reading it inherits both halves without a carrier-specific restatement.
- `boundary`: `FieldValuesNotUnique`, `Sum`, extrema, distinct count, and Number value count share the checked operand resolver; ordinary aggregates and value count deliberately select different projections, and expansion-kind diagnostics remain keyed by operator.
- `boundary`: **the distinct count is the one member whose expansion-kind class is not keyed by the operator at all, so it claims none.** `NumberOfDifferentValues` reports the class of its operand list's *first* member, and two groups differing only in declaration order flip the code, so a projection keyed by operator alone is wrong for one of them — as the shipped one was, having inherited a String class measured on a String-first expansion and applied it to a Number-first fixture ([checkpoint](../sources/group-list-and-capacity-probes.md#src-distinct-count-first-operand-class), outbound [`SPEC-2026-08-31-13`](../A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-13)). Projecting the right code needs the first operand's kind carried in the refusal, which no consumer has yet asked for.
- `boundary`: a group operand **and a plain starred field operand** narrow to their declared-capacity extent for `Sum`, both extrema, `NumberOfDifferentValues`, and the Number value count, on the full-validation **and the computation** arm alike. A row beyond its group's declared repeatability is not in the domain the operand denotes, so its cell is neither aggregated nor able to make the aggregate unavailable ([fixed](../sources/inbound-group-operand-batches.md#src-group-operand-capacity-consumer-sweep), [starred group](../sources/group-and-iteration-probes.md#src-starred-group-operand-extent), [starred field](../sources/group-and-iteration-probes.md#src-starred-field-operand-extent), [computation arm](../sources/group-and-iteration-probes.md#src-capacity-projection-computation-arm)).
- `boundary`: on full validation a filtered Number star joins the same in-capacity projection for all five operators. The over-limit-only fixture fixes `15`, `5`, `5`, `1`, and `0` for `Sum`, `MinValue`, `MaxValue`, `NumberOfDifferentValues`, and `NumberOfValueInFields(7 …)` respectively. The first four are both-directionally fillable after reaching the filter; value count is grow-only at zero and both-directionally fillable after an in-capacity match. Direct fields and the computation and partial filtered-star routes are unchanged.
- `boundary-history`: three narrower accounts were split off here in turn — fixed against starred, `Sum` against the other operators, validation against computation — and each split's unmeasured side was pinned by a conformance case rather than left unexercised. All three fell to measurement, so one resolver now serves each arm and the aggregate evaluator does not branch on the operator ([`LF124`](../LEAN-FINDINGS.md)).
- `boundary`: the four aggregate operators carry their own measurement for the plain-star projection; the Number value count shares that resolver and has none of its own. The filtered Number projection is internal for all five and has no retained Kernel row, while the filtered String, Boolean, and Confirm sibling counts are measured. `FieldValuesNotUnique` uses a different resolver by design and is unmeasured on this dimension, so it keeps the complete formal-cell view.
- `boundary`: a well-formed over-limit cell cannot establish that narrowing, because *excluded from the extent* and *admitted and classified unavailable* both predict the same definite answer. The separating observation is a **malformed** cell: it poisons the aggregate one index below capacity and is ignored one index above it. The immutable checked document still reports the over-limit cell, so the exclusion is the operand's extent and not a document-level erasure.
- `limit`: non-Number, empty, and mixed-signedness expansions fail locally without inventing a Kernel class.
- `boundary`: the resolver emits cells declaration-major in stable model declaration order, with each declaration's canonical row order inside it.
- `limit`: Kernel correspondence for that order remains open on Number consumers; [token group expansion](../IMPLEMENTATION-MAP.md#cap-token-group-expansion) owns the separately calibrated fixed and direct-single-star `FirstFilledValue` fragments.
- `limit`: only instantiated rows are enumerated and `hasUninstantiatedTail` is false; correspondence for a declared but uninstantiated tail is open.
- `limit`: fixed-group computation, Number value-count validation, and Number group routes outside the measured checked computation remain `external evidence pending`; partial validation and raw-`Document` group routes remain refused.
- `owner`: [`NumberEntityList.lean`](../../A12Kernel/Elaboration/NumberEntityList.lean), [`CheckedStarDocument.lean`](../../A12Kernel/Elaboration/CheckedStarDocument.lean), and [`NumericAggregate/Entities.lean`](../../A12Kernel/Elaboration/NumericAggregate/Entities.lean).
- `case`: [group extent and admission cases](../../A12Kernel/Conformance/FieldEntityGroupOperand.lean), [checked computation cases](../../A12Kernel/Conformance/NumberEntityGroupComputation.lean), [declared-capacity extent cases](../../A12Kernel/Conformance/GroupOperandCapacity.lean), and [filtered Number full-validation cases](../../A12Kernel/Conformance/NumericAggregateElaboration/CheckedDocument.lean).
- `proof`: [`checkedNumberEntityGroup_expansion_complete`](../../A12Kernel/Proofs/NumberEntityList.lean), [`checkedNumberEntityGroup_checkedComputationAggregate_usesRichProjection`](../../A12Kernel/Proofs/NumericAggregateElaboration.lean), [`checkedNumberEntityGroup_checkedComputationValueCount_usesCapacityProjection`](../../A12Kernel/Proofs/NumericAggregateElaboration.lean), and [`checkedNumberEntityFilteredStar_checkedValidationAggregate_usesCapacityProjection`](../../A12Kernel/Proofs/NumericAggregateElaboration.lean).
- `proof`: [`checkedNumberEntityGroup_validationSum_agreesWithAggregate`](../../A12Kernel/Proofs/NumericAggregateElaboration.lean) equates the two full-validation resolvers on a group operand, so the six measured carriers cannot drift onto different extents and a later selected narrowing of one resolver alone fails rather than passing silently.
- `assurance`: E/P closed for retention, full-validation extent including the filtered Number star, checked-computation resolver delegation, ordinary rich projection, and value-count capacity projection. The filtered Number branch is `external evidence pending`; L Kernel-locks starred combiner computation and starred group value-count values, poison, empty identity, and over-capacity exclusion while the wider routes remain partial. Q is green at the [filtered Number capacity probe](../consumer-probes/EXPLAIN.md#filtered-number-star-capacity-probe): after the first reader exposed an incomplete movement allowlist, a new reader recovered all seven exact results and movement states independently from the repaired committed handover; C/X none.
- `source`: [group runtime and reference correction](../SOURCES.md#src-group-runtime-and-reference), [group runtime differential](../SOURCES.md#src-field-values-not-unique-group-runtime), [Number group computation runtime](../SOURCES.md#src-number-group-computation-runtime), [Number group value-count capacity](../SOURCES.md#src-number-group-value-count-computation-capacity), and [fixed-group over-limit extent](../SOURCES.md#src-group-operand-capacity-consumer-sweep).
- `remains`: [group runtime](../SEMANTICS-GAPS.md#gap-sg5-number-token-group-runtime).

<a id="cap-token-group-expansion"></a>
#### Token group expansion

- `state`: partial.
- `boundary`: the token carrier retains the authored group and a certified recursive expansion, pairing every reached cell with the declaration and operand that read it.
- `boundary`: a group authors no read form, so every member is read stored and homogeneity applies across the entire expansion rather than only between authored slots.
- `boundary`: full-validation `FieldValuesNotUnique` and token aggregate routes consume the shared operand-bounded extent.
- `boundary`: the `NumberOfValueInFields` group surface admits one sole terminal-repeatable starred group as an internally already-many stored-projection slot and delegates its certification to the common token mechanisms. Its dedicated checked whole-group carrier selects the in-capacity extent on both arms, leaving independent over-repetition findings on the immutable checked document.
- `boundary`: its separate fixed-group surface admits one sole nonrepeatable ordinary path group through the common token entity-list gates. `RuleGroup` is unrepresented, and a group inside repeatable scope remains refused rather than being assigned an implicit row.
- `boundary`: every group surface narrows to the operand's declared-capacity extent under full validation, so a matching cell beyond its group's declared repeatability neither counts nor makes the count unavailable; kernel 30.8.1 answers `0` there and `1` on the control one index lower, on the fixed form ([checkpoint](../sources/inbound-group-operand-batches.md#src-group-operand-capacity-consumer-sweep)) and on the starred one ([probe](../sources/group-and-iteration-probes.md#src-starred-group-operand-extent)). The dedicated starred whole-group carrier's validation arm counted the complete extent until that probe; its two arms now agree.
- `boundary`: checked-document partial token value-list evaluation filters reached group cells per expanded declaration and records incomplete extent on the one authored group slot unless every expanded declaration has a covering wildcard identifier across the levels that slot reopens.
- `limit`: Kernel calibration for that partial route establishes a starred String group's equivalence to its explicit expansion under wildcard and concrete-row relevance and establishes that those shapes disagree. The exact group-fixture orientation and equal reached-cell selection remain externally unverified; fixed or nested groups, Enumeration projection, the values side, and the other quantifiers are internally executable but remain `external evidence pending`.
- `boundary`: full-validation token `FirstFilledValue` consumes the shared checked extent for exactly one fixed group whose authored terminal is nonrepeatable, including recursive repeatable descendants, or for one terminal single-level starred group whose expansion contains only direct token declarations; the measured starred form may precede one nonrepeatable direct fallback.
- `boundary`: group cells remain declaration-major with canonical row order inside each declaration; the shared stop-at-first evaluator preserves selected token and reached-empty-prefix missingness, including a no-row group star before the direct fallback, while ignoring every suffix after selection.
- `extent`: [Number group aggregates](../IMPLEMENTATION-MAP.md#cap-number-group-aggregates) owns the shared resolver's ordering and uninstantiated-tail limits.
- `limit`: Kernel calibration establishes starred String-group admission and a two-row cross-declaration full-validation count reaching the measured threshold, plus exact checked-computation counts, in-capacity malformed poison, and over-capacity content exclusion. Fixed nonrepeatable String-group static admission is measured; fixed-group runtime and stored-Enumeration group expansion with its all-expanded-declarations literal gate remain internally executable with `external evidence pending`; the surface has no `RuleGroup` constructor.
- `limit`: a fixed operand whose authored terminal is itself repeatable, multi-level or nonterminal starred groups, starred groups with nested declarations, mixed lists other than the one measured direct fallback, computation, partial validation, and legacy raw-document routes remain explicitly unsupported by this first-filled path.
- `owner`: [`TokenEntityGroup.lean`](../../A12Kernel/Elaboration/TokenEntityGroup.lean), [`TokenEntityList.lean`](../../A12Kernel/Elaboration/TokenEntityList.lean), [`TokenValueCount.lean`](../../A12Kernel/Elaboration/TokenValueCount.lean), [`TokenEntityValueList.lean`](../../A12Kernel/Elaboration/TokenEntityValueList.lean), and [`TokenFirstFilledValue.lean`](../../A12Kernel/Elaboration/TokenFirstFilledValue.lean).
- `case`: [token group cases](../../A12Kernel/Conformance/TokenEntityGroupOperand.lean), [token group value-count cases](../../A12Kernel/Conformance/TokenGroupValueCount.lean), and [declared-capacity extent cases](../../A12Kernel/Conformance/GroupOperandCapacity.lean).
- `proof`: [`checkedTokenEntityGroup_expansion_complete`](../../A12Kernel/Proofs/TokenEntityValueList.lean), [`checkedTokenEntityGroup_projections_stored`](../../A12Kernel/Proofs/TokenEntityValueList.lean), [`checkedTokenEntityGroup_partialExtentRelevant_iff`](../../A12Kernel/Proofs/TokenEntityValueList.lean), [`checkedTokenValueCountGroup_checkedComputation_usesCapacityProjection`](../../A12Kernel/Proofs/TokenValueCount.lean), and the generic [token first-filled scan and projection laws](../../A12Kernel/Proofs/TokenFirstFilledValue.lean).
- `assurance`: E/P closed for retention, per-cell projection, recursive fixed-group full-validation extent, checked partial value-list relevance, starred and fixed-group value-count authoring through shared group certification, the starred String-group checked-computation capacity projection, and the direct-single-star specialization through existing generic laws
- `assurance`: L locks starred group-versus-explicit partial `No` equivalence within each relevance shape and disagreement between shapes, sole starred String-group full-validation admission and cross-row threshold, and fixed nonrepeatable String-group value-count admission at clean a12-dmkits [source registry](../SOURCES.md), plus the checked-computation exact values, in-capacity malformed clear, and over-capacity match/malformed exclusions at clean [source registry](../SOURCES.md), and the unstarred nested-row/direct-before-nested matrix and the four fixed-order and six starred-order/polarity rows at [source registry](../SOURCES.md), C/X/Q none.
- `source`: [group-operand admission](../SOURCES.md#src-field-values-not-unique-group-admission), [group runtime differential](../SOURCES.md#src-field-values-not-unique-group-runtime), [token group partial runtime](../SOURCES.md#src-token-group-partial-runtime), [token starred-group value-count full-validation runtime](../SOURCES.md#src-token-value-count-group-runtime), [token starred-group value-count computation capacity](../SOURCES.md#src-token-group-value-count-computation-capacity), [token fixed-group value-count admission](../SOURCES.md#src-token-value-count-fixed-group-admission), [fixed-group first-filled order](../SOURCES.md#src-group-first-filled-runtime-order), and [starred-group first-filled order](../SOURCES.md#src-star-group-first-filled-runtime-order).
- `remains`: [group runtime](../SEMANTICS-GAPS.md#gap-sg5-number-token-group-runtime).

<a id="cap-temporal-group-uniqueness"></a>
#### Temporal group uniqueness

- `state`: implemented for checked-document full validation.
- `boundary`: temporal uniqueness retains one recursively complete group expansion with one exact declared format, resolves every declaration at every instantiated descendant row below the operand's own bound depth, and compares the resulting combined stream by exact stored text.
- `owner`: [`TemporalValuesNotUnique.lean`](../../A12Kernel/Elaboration/TemporalValuesNotUnique.lean).
- `case`: [temporal group cases](../../A12Kernel/Conformance/TemporalEntityGroupOperand.lean).
- `proof`: [`checkedTemporalUniquenessGroup_expansion_complete`](../../A12Kernel/Proofs/ValuesNotUnique.lean) and [`checkedTemporalUniquenessGroup_resolveValidationCore`](../../A12Kernel/Proofs/ValuesNotUnique.lean).
- `assurance`: E/P closed for retention, complete expansion, shared-resolver delegation, recursive full-validation extent, explicit-field agreement, and distinct/empty controls; L locks the exact two-row group-versus-explicit matrix at clean a12-dmkits [source registry](../SOURCES.md); C/X/Q none.
- `source`: [group-carrier static admission](../SOURCES.md#src-group-carrier-static-admission) and [temporal group runtime](../SOURCES.md#src-temporal-field-values-not-unique-group-runtime).
- `remains`: group-form message emission remains under [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration); external calibration beyond the exact unstarred two-row checkpoint remains pending.

<a id="cap-boolean-confirm-group-value-count"></a>
#### Boolean/Confirm group value count

- `state`: partial.
- `boundary`: Boolean/Confirm value count retains the authored group and every descendant declaration; the constant-specific kind gate applies to each descendant.
- `boundary`: the structural reference projection expands the retained group; checked-document full validation reads the operand-bounded recursive `(row × field)` extent and separately preserves hierarchical declared-but-uninstantiated capacity for count fillability.
- `boundary`: under full validation a group operand narrows to its declared-capacity extent, starred or not, so a `true` cell beyond its group's declared repeatability neither counts nor makes the count unavailable; kernel 30.8.1 answers `0` there and `1` on the control one index lower ([checkpoint](../sources/inbound-group-operand-batches.md#src-group-operand-capacity-consumer-sweep), [starred probe](../sources/group-and-iteration-probes.md#src-starred-group-operand-extent)). Both overloads' validation cases asserted the complete view until that probe and now carry the measured answer.
- `boundary`: one constant-indexed checked computation carrier admits a sole terminal-repeatable starred group with one repeatable axis and no selected declaration below a deeper repeatable group, delegates the constant-specific fieldwise kind gate to the existing Boolean/Confirm group carrier, and selects only in-capacity cells before computation-phase token classification. `True` therefore admits Boolean and Confirm declarations in any order while `False` admits only Boolean declarations. Full validation now selects the same in-capacity projection, and the over-repetition findings stay independently on the immutable checked document.
- `boundary`: a separate constant-indexed fixed computation carrier admits exactly two direct declarations in one nonrepeatable group, delegates the common group certificate, requires the measured declaration-kind sequence, and selects the complete addressed extent through the shared checked group scan. The measured `True` form contains Boolean then Confirm, and the measured `False` form contains two Booleans.
- `limit`: other fixed declaration kinds or orders, recursive or wider fixed groups, groups containing declarations below a deeper repeatable level, partial validation, and legacy raw-`Document` group routes remain refused.
- `owner`: [`BooleanValueCount.lean`](../../A12Kernel/Elaboration/BooleanValueCount.lean).
- `case`: [Boolean/Confirm group cases](../../A12Kernel/Conformance/BooleanEntityGroupOperand.lean) and [declared-capacity extent cases](../../A12Kernel/Conformance/GroupOperandCapacity.lean).
- `proof`: [`checkedBooleanValueCountGroup_expansion_complete`](../../A12Kernel/Proofs/TokenValueCount.lean), [`checkedBooleanValueCountGroup_resolvedCheckedValidationSide`](../../A12Kernel/Proofs/TokenValueCount.lean), [`checkedBooleanValueCountStarredGroup_checkedComputation_usesCapacityProjection`](../../A12Kernel/Proofs/TokenValueCount.lean), [`checkedBooleanValueCountFixedGroup_checkedComputation_usesCompleteProjection`](../../A12Kernel/Proofs/TokenValueCount.lean), and the generic [`valueCount_singleton_match_tail`](../../A12Kernel/Proofs/NumericAggregate.lean).
- `assurance`: E/P closed for retention, complete expansion, shared concrete-extent plus hierarchical-tail composition, second-row agreement with the explicit expansion, open-tail growability, full-capacity fixedness, the all-false control, the fieldwise one-axis computation carrier and capacity projection, the two-direct-field fixed computation carrier and complete projection, and fixed-group validation movement.
- `assurance`: L locks the exact finite one-level full-validation matrix and `False` mixed-group refusal at clean a12-dmkits [source registry](../SOURCES.md), the starred `True` and `False` capacity and shape matrices at clean [source registry](../SOURCES.md), and the fixed-group exact `2`, `1`, `0`, malformed-clear, distinct Boolean/Confirm formal causes, and full-extent VALUE polarity at reviewed [source registry](../SOURCES.md), which accepts [`SPEC-2026-08-24-04`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-24-04--numberofvalueinfields-group-computation-ignores-over-capacity-cell-content-for-measured-number-string-and-booleanconfirm-overloads) and [`SPEC-2026-08-24-05`](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-24-05--fixed-boolean-group-numberofvalueinfields-computation-expands-its-direct-fields); C/X/Q none.
- `source`: [group-carrier static admission](../SOURCES.md#src-group-carrier-static-admission), [Boolean value-count group runtime](../SOURCES.md#src-boolean-value-count-group-runtime), [Boolean/Confirm starred-group value-count computation capacity](../SOURCES.md#src-boolean-group-value-count-computation-capacity), [False Boolean-group value-count computation capacity](../SOURCES.md#src-false-boolean-group-value-count-computation-capacity), [Boolean-group value-count computation shape matrix](../SOURCES.md#src-boolean-group-value-count-computation-shape-matrix), and [Boolean fixed-group value-count computation](../SOURCES.md#src-boolean-fixed-group-value-count-computation).
- `remains`: wider fixed, nested-repeatable, partial-validation, and raw-document group routes remain under [SG5 group runtime](../SEMANTICS-GAPS.md#gap-sg5-number-token-group-runtime).

<a id="cap-group-operand-reference-projection"></a>
#### Group-operand reference projection

- `state`: implemented for admitted entity-list, group-list, nonrepeatable group-presence, and Boolean/Confirm count operands.
- `boundary`: a group contributes recursive descendant field pointers and never a group pointer; starred and unstarred forms differ by the shared coordinate rule rather than by expansion membership.
- `boundary`: one depth rule uses the referenced field's repeatable scope, preserves the bound concrete prefix, and wildcards every reopened level, including repeatable descendants of an unstarred group.
- `owner`: [`Reference.lean`](../../A12Kernel/Elaboration/ValidationCondition/Reference.lean) and [`FlatModel.groupSubtreeFields`](../../A12Kernel/Elaboration/Flat/Model.lean).
- `case`: [ordinary reference cases](../../A12Kernel/Conformance/ValidationRule/OrdinaryReference.lean).
- `proof`: [reference projection laws](../../A12Kernel/Proofs/ValidationCondition/Reference.lean).
- `separator`: retained cases fail if expansion stops at direct children, an unstarred group is wildcarded like a starred one, a fixed group is expanded only one level, or an unbound descendant is dropped or pinned.
- `assurance`: E/P closed for the named projection; L partial; C none; X none; Q none.
- `source`: [group runtime and reference correction](../SOURCES.md#src-group-runtime-and-reference).
- `consumer`: Analyze and Explain must distinguish exact-reference membership from broader reachability; a field reachable only through expansion has no direct referrer.
- `limit`: an admitted unstarred repeatable group-presence operand still fails closed when its own level is unbound because no witness selects a concrete or wildcard coordinate.
- `remains`: [unstarred repeatable group-presence coordinates](../SEMANTICS-GAPS.md#gap-sg5-unstarred-repeatable-group-presence-reference); unbound deeper descendants on other carriers, the unwitnessed Number and Boolean/Confirm filtered-star reference carriers, `RepetitionNotUnique`, and the message-record channel remain under [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration).

## Reference-process exposure

[`Reference/Support.lean`](../../A12Kernel/Reference/Support.lean) owns the current finite normalized support declaration; [`Reference/Protocol.lean`](../../A12Kernel/Reference/Protocol.lean) and [`Reference/Evaluator.lean`](../../A12Kernel/Reference/Evaluator.lean) decode and dispatch only the declared flat and one-group correlation operations. [`PROTOCOL.md`](../PROTOCOL.md) owns the wire contract, [`ARTIFACTS.md`](../ARTIFACTS.md) the shipped files, and the current capability kits their self-contained handovers. Reference exposure never upgrades internal or external evidence status.

## Current external-evidence gate

<a id="empty-handling-coverage-rule"></a>

[`EVIDENCE.md`](../EVIDENCE.md) is the sole exact inventory of retained bundles, case identities, digests, replay commands, and finite claim limits. This map records only whether a semantic boundary has local calibration.

An internally closed capsule may remain `external evidence pending`. Before claiming kernel correspondence, select a coherent family, identify realistic wrong accounts, and retain the smallest source-maintained observation shape that separates them. Source reading, peer agreement, and theorem proof remain distinct from local calibration. The open cross-family calibration obligation is [`SG12`](../SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage).

## Trusted theorem surface

[`A12Kernel/Proofs.lean`](../../A12Kernel/Proofs.lean) is the trusted theorem root and [`A12Kernel/TrustAudit.lean`](../../A12Kernel/TrustAudit.lean) is its exhaustive audit registry. Current counts belong only in [`PLAN.md`](../PLAN.md) as a verified resumption checkpoint; this map records coverage, not repository size.

## Maintenance rule

Update the affected clause or shared-owner entry when an implemented boundary, proof/non-law status, external-assurance dimension, public exposure, or consumer qualification changes. Replace the old sentence; do not append a capsule narrative. Do not copy open-work details from [`SEMANTICS-GAPS.md`](../SEMANTICS-GAPS.md), source facts from [`SOURCES.md`](../SOURCES.md), evidence inventories from [`EVIDENCE.md`](../EVIDENCE.md), or current sequencing from [`PLAN.md`](../PLAN.md).
