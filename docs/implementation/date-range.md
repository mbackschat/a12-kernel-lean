# DateRange capabilities

Record keys and the `L`/`C`/`X`/`Q` assurance letters are defined once in the [hub's record-key legend](../IMPLEMENTATION-MAP.md#record-key-legend).

The remaining §6 temporal capabilities are owned by [`temporal.md`](temporal.md).

### §6 — dates and time

<a id="resolved-date-range-overlap-truth-and-operator-scans"></a>

<a id="cap-date-range-checked-declaration"></a>
#### Checked DateRange declaration and admission

- `state`: flat declaration validation decides the exact Kernel-measured eight-pair `(format, separator)` allowlist, and every admitted pair selects exactly one stored-input profile through the existing checked-document route. [`Proofs/DateRangeInput.lean`](../../A12Kernel/Proofs/DateRangeInput.lean) owns that totality law, so the `unsupportedPolicy` refusal stays fail-closed for callers that bypass declaration validation without being claimed unreachable.
- `boundary`: a `DateRangeInputFormat` constructor names one stored presentation, not one component shape: two constructors retain a month pair and two retain a month/day pair, because the declared separator and endpoint spelling change the stored text while the retained components agree. The ordinary order rule is shared inside the field-local fragment. Configured interpretation-bearing declarations are the explicit wrapping exception; the Kernel's unconfigured interpretation branch is declaration-order dependent and remains outside this representation's correspondence claim.
- `boundary`: the two lexical variants execute stored input and the direct single-level starred `FirstFilledValue` route, storing `0609` and `01.06-30.09` through their own target spellings. Direct bound and singular overlap admit them on the same terms as the slash-separated spelling of their component set.
- `boundary`: a yearless target consumes a result only from a profile retaining the same components, selected by the spelling tables in [`DateRangeTargetPresentation.lean`](../../A12Kernel/Elaboration/DateRangeTargetPresentation.lean); one order rule serves every represented profile sharing a shape, and a profile retaining different components poisons the target rather than inventing a spelling. The withdrawn unconfigured interpreted dotted-carrier specialization depended on model declaration order that this target boundary does not retain.
- `boundary`: direct-star source and target admission compares declared component sets, not carriers. `TemporalFirstFilledStarCarrier.acceptsSource` admits every DateRange crossing within one component set and requires identity for every scalar temporal carrier, where no crossing is measured. The target's declared spelling decides the stored text inside the represented field-local fragment. The Kernel's unconfigured interpretation route may instead use a format contributed by another declaration, so no target-local account of that branch is claimed.
- `boundary`: under an empty separator the stored token is halved, so the missing-separator cause cannot arise and an odd length is a shape failure; the dotted profile keeps that cause because its separator is present.
- `boundary`: a refused declaration keeps `emptyFormat` or `emptySeparator` where a source is absent and `unsupportedPair` otherwise; no Kernel diagnostic class is claimed for any of the three, and no format other than month-only admits an empty separator.
- `boundary`: `DateRangeFormat` remains shared by exact input and target presentation. Exact and year-bearing fragment inputs construct stored-Gregorian `DateValue` endpoints with model-zone midnight instants; shorter fragments retain either that exact range after Base-Year completion or an explicit yearless ordered component pair without a synthetic year or instant.
- `boundary`: present-empty, missing separator, malformed split or endpoint, inverted order, and the applicable universal-floor cause remain distinct. For every supported pair, `CheckedDocument` rejects caller-supplied raw state that disagrees with canonical classification; no DateRange-specific document or second read route exists.
- `boundary`: unsupported policy is explicit `DateRangeInputError`. Exact and year-bearing fragment profiles require a supported zone; configured shorter fragments require one after successful parsing; fragments without Base Year need no zone.
- `boundary`: direct bounds admit exact full-year, both year-bearing fragment profiles, and every Base-Year-completed yearless profile including the two lexical variants; without Base Year each yearless profile is refused. Both overlap operators gate their whole operand list by year class through `mixesDateRangeYearInclusion`: a declaration is year-bearing on its own or through the model's Base Year, an unconfigured list mixing the two classes is refused `MVK_DATE_WITH_AND_WITHOUT_YEAR`, and a configured model therefore admits every list including one crossing component sets. Within one class neither operator applies a component-set gate.
- `boundary`: The separate unconfigured yearless route admits direct, plain-starred, and filtered-starred operands, plus fixed and starred groups in the plural list slot; the plural scalar remains direct and the singular group refusal remains intact. Direct `FirstFilledValue` and the bounded constructor target admit all eight declaration pairs.
- `boundary`: `interpretationOfYear` is an optional declaration key that takes no part in admission. With a Base Year, an absent key refuses a wrapping yearless value, `FROM` anchors the start and moves the finish into the following year, and `TO` anchors the finish and moves the start into the preceding year. An ordered value is unaffected, and the month-only finish takes the last day of its own placed year, so a February finish follows that year's leap rule. Without a Base Year, the Kernel's validity and presentation depend on model declaration order in the measured shared-format pair. `resolveYearlessForModel` deliberately keeps that unrepresented wrapping branch fail-closed; `completionYears` remains the owner of configured placement.
- `boundary`: both overlap operators **refuse** an operand whose declaration carries the key, on either side, under a yearless or a year-bearing exact profile, and with or without a Base Year, reporting the Kernel's `MVK_INVALID_DATE_RANGE_FORMAT`. `refuseDateRangeOverlapYearInterpretation` is the one gate and the three overlap certification routes are its only callers; bound extraction and equality comparison deliberately do not call it, because the same declaration is admitted there. The three certification dispatchers now share one fallback rule: only an unsupported canonical policy opens the fragment route, so a fragment-specific refusal is reported as itself instead of being replaced by the policy cause.
- `route`: [`DateRangeInput.lean`](../../A12Kernel/Elaboration/DateRangeInput.lean) owns stored classification; [`CheckedDocument.lean`](../../A12Kernel/Elaboration/CheckedDocument.lean) owns canonical coherence and phase reads; declaration and generic presence/required/index routes remain unchanged.
- `owner`: [`Core.lean`](../../A12Kernel/Core.lean) owns exact versus yearless cell identity; [`DateRangeInput.lean`](../../A12Kernel/Elaboration/DateRangeInput.lean) owns stored classification.
- `owner`: [`TemporalFormat.lean`](../../A12Kernel/Semantics/TemporalFormat.lean) owns declaration policy and exact presentation; [`TemporalTarget.lean`](../../A12Kernel/Semantics/TemporalTarget.lean) owns exact rendering; [`DateRangeTargetPresentation.lean`](../../A12Kernel/Elaboration/DateRangeTargetPresentation.lean) owns checked fragment rendering; [`Observation.lean`](../../A12Kernel/Semantics/Observation.lean) owns cause projection.
- `assurance`: E internally closed for the complete 8x4 candidate declaration grid, all eight input pairs, empty, separator and format failures, calendar reality, order, applicable floor, UTC/Berlin exact identity, yearless component identity, configured-year leap completion, unsupported capability, and checked-document anti-spoofing. P retains the admitted-format law, the month-only-empty-separator law, and the admitted-pair input-profile totality law without requiring both sources to be nonempty. The comparison capability owns the remaining payoff-selected P boundary.
- `assurance`: L is exact for the two added stored texts `0609` and `01.06-30.09`, for their direct-star computation route, and for every crossing within a component set in both directions including the two full-Date spellings; the [crossing checkpoint](../SOURCES.md#src-date-range-component-set-crossing) owns those admission verdicts, runtime outputs, the different-component refusal control, and the integrity hashes. The peer's six empty-separator formal rows are Kernel/interpreter parity rows without pinned codes, so malformed and inverted precedence for the two variants is internal reuse of the family's established order. Base-Year completion of either variant has no retained observation.
- `assurance`: The [bound checkpoint](../SOURCES.md#src-date-range-bound-base-year) adds the Base-Year bound-admission matrix, the component-extraction gate, and the pinned completion of both variants with two silent negative controls.
- `assurance`: L is exact for the year-class gate at the [mixed-year checkpoint](../SOURCES.md#src-date-range-mixed-year-overlap): ten of fifteen conditions refused unconfigured, eight mixed pairs and two mixed three-operand lists, with all fifteen admitted under Base Year 2024; both year-bearing fragment profiles appear on the mixing side, the five uniform-class conditions establish the absent component-set gate, and fifteen runtime rows agree on both codegen strategies. Those runtime rows also pin that a completed operand occupies the Base Year alone, so an exact operand naming another year is disjoint. The two gate laws state when the gate cannot fire; neither claims it is the only refusal a list can meet.
- `assurance`: L is exact for the overlap refusal, the endpoint years, and the placed-year leap check at the [interpretation-overlap checkpoint](../SOURCES.md#src-date-range-interpretation-overlap-refusal): 10 refused and 5 admitted overlap conditions across a configured and an unconfigured model, 5 admitted bound and equality conditions on the same declarations, and 5 documents observed on both codegen strategies with every row agreeing. The endpoint years are therefore measured rather than peer-pinned. Accepted [SPEC-2026-08-23-01](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-23-01--an-interpretationofyear-declaration-is-refused-as-an-overlap-operand) carries both clause additions.
- `assurance`: L is exact for the configured interpretation's formal-validity flip at the [interpretation checkpoint](../SOURCES.md#src-date-range-year-interpretation): one Base-Year-2020 `dd.MM`+dash model, six documents, both codegen strategies agreeing, showing `datumBereichNichtGueltig` with no interpretation and nothing under either `FROM` or `TO`.
- `assurance`: The endpoint years are locally measured at the [interpretation-overlap checkpoint](../SOURCES.md#src-date-range-interpretation-overlap-refusal), the [interpretation-comparison checkpoint](../SOURCES.md#src-date-range-interpretation-comparison) pins both the resolved-identity inequality of identical wrapping text under `FROM` and `TO` and the equality of identical ordered text, and the [interpretation first-filled checkpoint](../SOURCES.md#src-date-range-interpretation-first-filled) pins direct-list computation under the same configured profile. The corrected [unconfigured checkpoint](../SOURCES.md#src-date-range-unconfigured-interpretation-first-filled-star) establishes declaration-order dependence and therefore withdraws, rather than closes, that correspondence branch.
- `assurance`: P owns only the interpretation consequences preserved by the supported representation: changing the key cannot move an ordered range's configured endpoints, the absent interpretation refuses a configured wrapping value, and both configured wrapping placements span exactly one year boundary. The removed unconfigured admission and dotted-carrier laws were consequences of the falsified field-local account, not Kernel evidence. The overlap refusal is **E-locked only**: no certificate records the absent key, so a proof-carrying invariant would have to add a field to the shared canonical operand carrier that bounds and comparison also use, which is beyond this gate's payoff. Five separating cases cover both operators, both sides, both profiles, the unconfigured overlap route, and the diagnostic identity instead.
- `assurance`: L is exact for the declaration allowlist itself: the [reviewed 2026-08-19 checkpoint](../SOURCES.md#src-date-range-2026-08-19-reconciliation) retains the complete 8x4 grid against the real Kernel. The refusal cause of a rejected pair is local classification with no measured Kernel counterpart.
- `assurance`: L is exact for the measured dotted cause matrix, `yyyy` and `yyyy-MM` completion and verdicts at their [year](../SOURCES.md#src-date-range-year-fragment-construction) and [year-month](../SOURCES.md#src-date-range-year-month-fragment-construction) checkpoints, and valid configured/yearless `MM` and `MM-dd` values at [the fragment checkpoint](../SOURCES.md#src-date-range-base-year-fragment-construction).
- `assurance`: The [construction-target](../SOURCES.md#src-date-range-construction-target-crossing), [year-fragment-target](../SOURCES.md#src-date-range-year-fragment-constructor-target), and [exact-and-configured](../SOURCES.md#src-date-range-exact-and-configured-constructor-targets) checkpoints add the rendered constructor output for **every** admitted target: all four yearless spellings, both year-bearing profiles, the ISO exact target, and both Base-Year-completed fragment targets, each with its inverted attempt and the yearless ones with their equal-endpoint row. What stays internally composed on this family is the empty, formal, and clearing branch of each target.
- `assurance`: the [reviewed reconciliation checkpoint](../SOURCES.md#src-date-range-2026-08-19-reconciliation) pins the complete declaration grid and the empty-separator split. Formal negatives and exact endpoint instants remain internally composed.
- `remains`: other legal zones, full message construction, partial-validation overlap evaluation, and remaining local target/computation carriers; all eight legal DateRange `(format, separator)` pairs themselves are implemented: [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) and [SG10](../SEMANTICS-GAPS.md#sg10--message-construction-and-formal-output-integration).

<a id="cap-date-range-construction-computation"></a>
#### Direct DateRange construction computation

- `state`: implemented for one checked direct nonrepeatable `DateRange(start, finish)` construction into a direct nonrepeatable DateRange target under either exact policy or a matching `yyyy`, `yyyy-MM`, `MM`, or `MM-dd` profile, with or without Base Year where the profile permits it. The target need not share the declaring group: nonrepeatable endpoints derive no iteration, so the Kernel imposes no placement gate here ([checkpoint](../SOURCES.md#src-fixed-target-star-placement)).
- `boundary`: admission reuses the checked two-endpoint construction and direct DateRange declaration. The target certificate retains the target's **own declared profile** and admits it whenever its component set equals the construction's, so both spellings of each set are reachable — a `MM` construction stores into a `MM`-with-empty-separator target and a `MM-dd` construction into a `dd.MM` target — while cross-component pairs, repeatable targets, and wrong-kind targets stay refused; a target outside the declaring group is admitted, and only an unrepresentable declaring group is refused. The former enumeration named one spelling per set and could not represent the other two at all.
- `boundary`: execution reads both endpoints once through the existing immutable checked document in computation phase. Endpoint observations become one typed exact or yearless `DateRangeCellValue`; formal unavailability dominates emptiness through the established construction observation, and empty becomes no value.
- `boundary`: all targets delegate to the shared `DateRangeInputFormat.evaluateComputationResult`. Exact and configured fragment results render resolved endpoints; unconfigured `MM` and `MM-dd` render retained components; every profile shares ordered acceptance, inverted attempted-value retention, and existing DateRange application.
- `boundary`: every rich constructor outcome composes with the shared [nonrepeatable DateRange result](../IMPLEMENTATION-MAP.md#11--calculations-and-formal-checking) boundary. The result projection classifies changes and clearing against the immutable source target, retains computed errors and residual messages, and applies only retained actions to a separately supplied checked destination; this constructor adds no second result or application representation.
- `boundary`: each operand refusal now carries the Kernel diagnostic measured at the [operand-grid checkpoint](../SOURCES.md#src-date-range-construction-operand-grid). Wrong kind, an unsupported partial profile, and a component mismatch all report `MVK_WRONG_DATE_FORMAT_FOR_OP`, so the operand kind is not recoverable from the code; a repeatable reference reports `MVK_NO_WILDCARD`. A **starred** operand draws `MVK_NO_WILDCARDS_ALLOWED` upstream but is not expressible at this boundary, which resolves a field id rather than an authored path, so that class stays unrepresented rather than guessed.
- `owner`: [`DateRangeConstructionComparison.lean`](../../A12Kernel/Elaboration/DateRangeConstructionComparison.lean) owns endpoint certification and its refusal diagnostics; [`DateRangeConstructionComputation.lean`](../../A12Kernel/Elaboration/DateRangeConstructionComputation.lean) owns checked assembly, the component-matched target certificate, and rich execution; [`DateRangeTargetPresentation.lean`](../../A12Kernel/Elaboration/DateRangeTargetPresentation.lean) owns the shared exact-or-yearless target evaluator used by construction and `FirstFilledValue`.
- `owner`: [`TemporalTarget.lean`](../../A12Kernel/Semantics/TemporalTarget.lean) owns exact-target evaluation; [`TemporalComputationResult.lean`](../../A12Kernel/Elaboration/TemporalComputationResult.lean) and [`TemporalErroredComputationApplication.lean`](../../A12Kernel/Elaboration/TemporalErroredComputationApplication.lean) own the shared DateRange result and separate-destination application.
- `case`: [constructor-target cases](../../A12Kernel/Conformance/DateRangeConstructionComputation.lean) separate six local policies, unconfigured `MM` and `MM-dd`, cross-component directions, every other named static refusal, leap-aware completion, all six rendered outputs, inverted target rejection, empty, formal-before-empty, source-relative changed and unchanged results, computed errors, clearing, and separate-destination application with unrelated-state preservation.
- `proof`: [constructor-target laws](../../A12Kernel/Proofs/DateRangeConstructionComputation.lean) preserve declaring-group validity plus profile ownership, exact and yearless endpoint projection, supported-format ordered and inverted target outcomes, fail-closed exact-target mismatch, single construction evaluation, and exact delegation to the shared five-channel result projection.
- `assurance`: E/P closed for all six matching target profiles, including component-only `MM` and `MM-dd` without Base Year; C/X none.
- `assurance`: L is exact for filled ISO full-Date source endpoints rendered into dotted/dash at the [established-kind computation checkpoint](../SOURCES.md#src-first-filled-kind-computations).
- `assurance`: L is exact for no-Base-Year `MM` and `MM-dd` admission plus accepted, inverted, empty, and formal outcomes at the [yearless constructor-target checkpoint](../SOURCES.md#src-date-range-yearless-construction-target). ISO/slash, `yyyy`, `yyyy-MM`, and configured `MM`/`MM-dd` constructor outputs remain internal composition. The shared result row owns its separate external evidence limit; this constructor's result projection and application remain internal composition.
- `assurance`: Q closed for bounded public Execute/Analyze/Explain/Transform probes that recover all six target profiles, target and endpoint identities, model Base Year where applicable, exact or yearless endpoint observations, accepted or errored stored attempts, and applied or cleared values without renewed source research.
- `remains`: the two policies currently measured only on direct-star admission/runtime, alternatives, repetition, scheduling, overlap, bounds, wider application, shipment, and public compatibility remain in [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-indexed-date-range-construction-computation"></a>
#### String-keyed DateRange construction computation

- `state`: implemented for two exact full-Date fields selected by literal exact-text keys or direct nonrepeatable evaluated String fields in one direct one-level String-indexed group and rendered into one direct exact DateRange target, which need not share the declaring group.
- `boundary`: checked admission derives the indexed group and its declared String index from each repeated Date endpoint, requires exact full-Date endpoint policies under one supported zone, certifies each key as a literal or an exact model-owned direct evaluated String field, requires both endpoints to share the indexed group, and reuses direct DateRange target ownership and presentation certification.
- `boundary`: execution resolves one generated preliminary index column, reads dynamic keys once through the immutable checked document, projects each selected target through the shared semantic-index computation policy, retains literal identity or the checked source-field observation plus selected physical address, completes each Date at its endpoint position, and delegates the resulting typed range to the existing target renderer.
- `boundary`: the field-key whole-call entry derives endpoint, dynamic-key, and shared-index fields through the [selected computation-preliminary owner](computations.md#cap-selected-computation-preliminary), prepares that view once, and reuses the existing five-channel DateRange result with the eager findings in its residual channel.
- `boundary`: clean no-match and selected empty become no value; any unavailable index column poisons before lookup, duplicate participants expose no selected address, and keyed reversal changes physical endpoint rows without changing authored endpoint position.
- `boundary`: the rich target outcome also projects through the shared nonrepeatable DateRange result carrier against the immutable checked document underlying the preliminary index. Source-relative changed/clear/error actions apply to a separately supplied checked destination while the richer keyed endpoint observations remain available from execution.
- `owner`: [`IndexedDateRangeConstructionComputation.lean`](../../A12Kernel/Elaboration/IndexedDateRangeConstructionComputation.lean) owns bounded indexed admission, rich selection, target assembly, and result projection; [`CheckedIndexColumn.lean`](../../A12Kernel/Elaboration/CheckedIndexColumn.lean), [`SemanticIndex.lean`](../../A12Kernel/Semantics/SemanticIndex.lean), and [`DateRangeConstructionComparison.lean`](../../A12Kernel/Elaboration/DateRangeConstructionComparison.lean) retain shared column, lookup, and endpoint-completion ownership; [`TemporalComputationResult.lean`](../../A12Kernel/Elaboration/TemporalComputationResult.lean) and [`TemporalErroredComputationApplication.lean`](../../A12Kernel/Elaboration/TemporalErroredComputationApplication.lean) own the shared result and separate-destination application.
- `case`: [indexed constructor-target cases](../../A12Kernel/Conformance/IndexedDateRangeConstructionComputation.lean) separate literal and checked-field key identity, physical row selection, runtime key reversal, clean no-match, empty or formally unavailable selector fields, selected empty endpoints, duplicate-column poison, evaluated direct String-key admission, nested-scope refusal, cross-group refusal, source-relative changed and unchanged results, computed errors, clearing, and unrelated-state-preserving separate-destination application.
- `proof`: [`checked indexed-construction laws`](../../A12Kernel/Proofs/IndexedDateRangeConstructionComputation.lean) expose both key-admission, matching endpoint group, model-owned direct target, target-profile certificates, and exact delegation to the shared five-channel result projection. Target ownership no longer carries a declaring-group conjunct, because placement is unconstrained for this shape.
- `assurance`: E/P closed for the named literal- and direct-field-key profiles, including the field-key whole-call formal-input projection owned by the linked computation-input capability; L pins static admission, filled output, and duplicate-index clearing for both key forms, selected-empty endpoint clearing for literal keys, and clean unmatched or empty-selector clearing for field keys at the [String-index checkpoint](../SOURCES.md#src-indexed-date-range-construction-target); result projection, application, and formal-input composition are internal under the shared DateRange carrier's evidence limit; C/X none.
- `assurance`: Q closed for one bounded internal Execute/Analyze/Explain probe. An isolated consumer reconstructed literal-versus-field key identity, exact-text selection, address/no-selection/selected-empty distinctions, selector-versus-column poison, authored endpoint position, and inverted attempted-target rejection from the canonical DateRange, path, and computation clauses plus this bounded record; no shipment or external correspondence for reversed field keys was claimed.
- `consumer`: Execute and Analyze can distinguish authored literals from checked selector-field observations and recover selected concrete addresses, endpoint observations, target policy, and output without re-running index selection; Explain can distinguish no selection from selected empty, selector formal poison, and column poison.
- `remains`: fragments, Number or Enumeration indices, nested or cross-group selection, comparisons, partial validation, alternatives, scheduling, materialized-document reconstruction, shipment, and public compatibility remain in [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-checked-date-range-bound"></a>
#### Checked direct DateRange bound extraction, fixed-Date comparison, and components

- `state`: implemented for direct nonrepeatable DateRange fields under the two canonical full-Date policies, exact-valued `yyyy`/`/` and `yyyy-MM`/`/`, plus `MM`/`/` and `MM-dd`/`/` when the checked model supplies Base Year, including comparison with one fixed full Date in either authored position and all four numeric Date components.
- `boundary`: `DateRangeBound` selects start or finish directly from `DateRangeValue`; the checked operation reads the field once through `CheckedDocument`, returns the exact `DateValue`, and preserves empty, validation-unknown, computation-poison, and exact cause identity without label reconstruction.
- `boundary`: the checked comparison retains field, bound, operand position, operator, fixed peer, exact selected observation, and established full-Date verdict in one result; it projects decoded calendar identity only at the existing comparison seam and reports an impossible non-full endpoint explicitly.
- `boundary`: the checked component consumer retains the same exact selected observation beside the existing `NumericOperand`; empty becomes symmetric fillable zero and formal unavailability preserves its cause without a second document read.
- `owner`: [`Core.lean`](../../A12Kernel/Core.lean) owns endpoint selection, [`DateRangeBound.lean`](../../A12Kernel/Elaboration/DateRangeBound.lean) owns direct-field admission, the sole checked read, and both compositions, while [`DateComparison.lean`](../../A12Kernel/Semantics/DateComparison.lean) and [`DateNumeric.lean`](../../A12Kernel/Semantics/DateNumeric.lean) remain the comparison and component semantic owners.
- `case`: [canonical input cases](../../A12Kernel/Conformance/DateRangeInput.lean) and [bound, comparison, and component cases](../../A12Kernel/Conformance/DateRangeBound.lean) distinguish all six local policy/configuration pairs, unconfigured `MM` and `MM-dd`, unsupported policy, repeatable placement, and wrong kind.
- `case`: runtime cases distinguish both endpoints, leap/ordinary month completion, exact instant/parts/basis identity, operand position, all four components, all three bound non-value observations, comparison/component empty and unknown, and impossible full-Date projection.
- `proof`: [bound, comparison, and component laws](../../A12Kernel/Proofs/DateRangeBound.lean) preserve selected value, empty, validation cause, and computation cause through the checked read, retain exact selected identity, and delegate value, empty, and unavailable behavior to the existing consumers.
- `assurance`: E/P internally closed for the named direct-field, fixed-Date comparison, and component fragment.
- `assurance`: Q closed for bounded internal Execute/Analyze/Explain probes: isolated consumers recovered all six local policy/configuration pairs, source profile, model Base Year, and exact endpoint identity. Canonical queries cover both comparison positions, all four components, non-value behavior, and named exclusions; fragment queries cover their checked comparison/component compositions and leap-aware completion.
- `assurance`: L observes dotted-range start year/month and finish day components and pins direct field admission at [the source checkpoint](../SOURCES.md#src-date-range-bound-extraction). `yyyy`, `yyyy-MM`, configured `MM`, and configured `MM-dd` bound extraction are internal compositions over checked completion plus the source-established generic bound mechanism, not retained Kernel observations.
- `assurance`: the retained rows do not directly compare a bound with a full Date or cover quarter, exact direction independently of component choice, instant/basis transport, phase failures, the ISO policy, or any fragment policy.
- `remains`: constructed ranges, wider comparison operands, overlap and wider computation-target assembly, nested consumers, and repeatable addressing remain in [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).
- `remains`: research-closed shipment and public compatibility remain unclaimed; current Q is only the bounded internal probe defined by the [consumer maturation path](../IMPLEMENTER-GUIDE.md#consumer-probes-before-shipment).

<a id="cap-stored-date-range-equality"></a>
#### Checked stored-versus-stored DateRange equality

- `state`: implemented for two direct nonrepeatable stored DATE_RANGE fields under `==` or `!=`, across all eight admitted declaration pairs, with or without a model Base Year, using the declaration-level component-set gate and the verdict over the shared exact-or-yearless identity domain.
- `boundary`: admission reads the declared **component set** and nothing else, projected by `DateRangeInputFormat.components` into the existing `TemporalComponents`; the model's Base Year does not enter it, and neither does a declared year interpretation. The eight declarations therefore fall into five classes — full Date, year, year-month, month, month-day — and comparability is exactly equality of that class. Each class's lexical spellings cross freely; every cross-class pair is refused `MVK_INVALID_COMPARE_TO_DATE_RANGE`, symmetrically in the authored order and identically for both operators.
- `boundary`: the verdict reuses `DateRangeCellValue.sameIdentity` and the shared symmetric equality scan, so an admitted crossing compares resolved endpoint instants for an exact class and retained labels for a yearless one, never stored text. An empty operand on either side leaves **both** directions unfired, and formal unavailability on either side is UNKNOWN.
- `route`: [`DateRangeStoredComparison.lean`](../../A12Kernel/Elaboration/DateRangeStoredComparison.lean) owns certification and the one-read evaluation; both operands reuse `elaborateDirectDateRange` and `CheckedDirectDateRange.evaluate` from the bound owner, so no second direct-DateRange read or comparison representation exists.
- `owner`: [`DateRangeInput.lean`](../../A12Kernel/Elaboration/DateRangeInput.lean) owns the component-set projection beside the profile it reads; [`DateRangeComparison.lean`](../../A12Kernel/Semantics/DateRangeComparison.lean) owns the identity relation; [`ScalarEquality.lean`](../../A12Kernel/Semantics/ScalarEquality.lean) owns the empty and unknown rules and already proves them once.
- `assurance`: E internally closed for the complete same-component and cross-component relation over the eight declarations, both authored orders on every crossing, both operators on one mismatch, all three crossings at runtime with a discriminating negative each, and the empty and all-empty rows. P adds nothing local: order symmetry and empty suppression are already proved at the shared symmetric scan, and the component gate is `Eq` on a `DecidableEq` record rather than a table needing its own reflexivity and symmetry laws.
- `assurance`: L is exact at the [stored-comparison checkpoint](../SOURCES.md#src-date-range-stored-comparison): 24 conditions decided through `rule check` on both grid batches (9 admitted, 15 refused) and four documents observed on both codegen strategies with every row agreeing. The [interpretation-comparison checkpoint](../SOURCES.md#src-date-range-interpretation-comparison) adds that a declared year interpretation does not enter the gate and that identical wrapping stored text under `FROM` and `TO` is unequal, so this route admits declarations both overlap operators refuse. The clause addition instantiates the already accepted [SPEC-2026-08-19-01](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-19-01--date-range-equality-requires-matching-component-sets-across-both-operands) and creates no new outbound entry.
- `boundary`: a **semantic-index-selected** operand is admitted on this carrier beside a direct one, in either authored slot. The gate is this record's own declaration-level component set unchanged, applied to the keyed target's certified profile; the verdict is this record's own, taken over the two retained identities. The keyed operand is therefore not a second equality: it supplies one observation to the existing one. The authored order is retained rather than normalized, so an Explain consumer recovers which side was keyed. The direct operand standing beside it keeps its own resolution class, which is what distinguishes an unbound repeatable level from an incomparable profile.
- `boundary`: [`SemanticIndexDateRange.lean`](../../A12Kernel/Elaboration/SemanticIndexDateRange.lean) owns that pairing and the [keyed cases](../../A12Kernel/Conformance/SemanticIndexDateRange.lean) lock it; the keyed source itself is described at [the endpoint-component record](../IMPLEMENTATION-MAP.md#cap-date-range-bound-component-operand).
- `remains`: starred, group, and repeatable operands; a keyed operand on **both** sides, measured **admitted** with the duplicate identity keyed on the (field, key) pair ([checkpoint](../sources/static-admission-and-class-probes.md#src-keyed-date-range-overlap-admits-both-sides)) and now **implemented for the overlap carrier** as its own certificate, `CheckedSemanticIndexKeyedPairOverlap`, which carries the duplicate gate the keyed/direct pairing structurally cannot state; this carrier still models its own pairing as `keyedFirst : Bool` and the measurement did not reach it; a construction on either side, which keeps its own owner; and formally invalid operand rows. All remain in [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-unconfigured-yearless-date-range-bound"></a>
#### Unconfigured yearless DateRange bound extraction and components

- `state`: implemented for direct nonrepeatable yearless DateRange fields in a model with **no** Base Year, both endpoint positions, all four lexical spellings of the two yearless component sets, and the four numeric Date components gated by the declaration's own set.
- `boundary`: extraction is unconditional; the gate is on the consumer. A selected endpoint carries exactly the components its declaration retains, so `MonthFromDate` and `QuarterFromDate` are admitted on a month-only or month-and-day declaration, `DayFromDate` only on the latter, and `YearFromDate` on neither. The refusal is the Kernel's `MVK_WRONG_DATE_FORMAT_FOR_OP` at the extractor.
- `boundary`: the gate is the existing direct extractor admission `DateNumericPart.admittedBy` over the new `DateRangeInputFormat.components` projection, with no Base Year to supplement the year. No second admission table exists, and the projection is shared with stored-versus-stored equality, which reads the same component set for a different purpose.
- `boundary`: a selected yearless endpoint is a retained label, never a manufactured date. Its absent halves are the literal zeroes a partially known Date uses, and the static gate keeps them unobservable. Empty input keeps the shared symmetric-zero projection and formal unavailability keeps its exact cause, both through the one direct DateRange read.
- `route`: [`YearlessDateRangeBound.lean`](../../A12Kernel/Elaboration/YearlessDateRangeBound.lean) owns this route and reuses `elaborateDirectDateRange` and `CheckedDirectDateRange.evaluate` from the exact owner, so no second direct-DateRange read exists. A year-bearing declaration or a configured model is refused here as a routing fact and belongs to [the exact bound owner](../IMPLEMENTATION-MAP.md#cap-checked-date-range-bound); neither refusal carries a Kernel diagnostic, because the Kernel admits both there.
- `owner`: [`DateNumeric.lean`](../../A12Kernel/Semantics/DateNumeric.lean) owns extractor admission and the numeric projection; [`DateRangeInput.lean`](../../A12Kernel/Elaboration/DateRangeInput.lean) owns the component-set projection.
- `assurance`: E internally closed for the admitted and refused extractor grid over both yearless sets and both spellings, both endpoint positions, the two routing refusals, the diagnostic identity, the filled component values, and the empty and formally invalid projections. P adds nothing local: the empty and unknown projections are already proved at their shared owner and the gate is a reuse of the settled admission predicate.
- `assurance`: L is exact for admission and the filled values at the [unconfigured-bound checkpoint](../SOURCES.md#src-date-range-unconfigured-bound). The empty and formally invalid rows are internal compositions with no observation on this route.
- `remains`: bound-versus-bound comparison with its measured positional completion, the `MVK_INVALID_COMPARE_TO_DATE` refusal of a full-Date peer, and every repeatable, starred, or group source remain in [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-date-range-bound-pair-comparison"></a>
#### Checked DateRange endpoint-pair comparison

- `state`: implemented for two extracted DateRange endpoints from direct nonrepeatable fields, either endpoint position on each side, all six temporal comparison operators, and both admitted runtime domains: two exact endpoints and two yearless ones.
- `boundary`: admission is the ordinary direct temporal rule `TemporalComparisonOp.admitsFormats` over the two declarations' component sets, so it reads year presence and date class rather than requiring identical sets. Two yearless endpoints therefore cross their component sets, two year-bearing ones do too, and a yearless endpoint against a year-bearing one is refused `MVK_INVALID_COMPARE_TO_DATE` unless the model's Base Year supplies the missing year. Because that gate admits only homogeneous pairs, the runtime never meets a mixed one and keeps it as a defensive fault.
- `boundary`: a yearless endpoint is completed by its authored position before comparison — a month-only start to the first day and a month-only finish to the greatest day that month can ever have, February reaching 29 with no year to decide leapness. The completion reuses `YearlessInterval.ofMonthPair`, so the bound and overlap families cannot disagree about a yearless label's extent. Two exact endpoints compare as resolved full Dates through the established relation.
- `boundary`: the six operators share one ordered relation. `TemporalComparisonOp.holdsOrdered` derives the four order forms from a strict `before` alone, and both the full-Date and the yearless-label relations are specializations, so no domain restates the closed forms and none can disagree about them.
- `route`: [`DateRangeBoundComparison.lean`](../../A12Kernel/Elaboration/DateRangeBoundComparison.lean) owns the pair across both bound owners and reuses each owner's own certification and read. Its operand certification prefers the exact owner and falls back to the yearless one only on an unsupported canonical policy, so a genuine source refusal is reported as itself.
- `owner`: [`DateComparison.lean`](../../A12Kernel/Semantics/DateComparison.lean) owns the ordered relation and its two specializations; [`DateRangeOverlap.lean`](../../A12Kernel/Semantics/DateRangeOverlap.lean) owns the yearless completion; [`TemporalFormat.lean`](../../A12Kernel/Semantics/TemporalFormat.lean) owns the admission rule.
- `assurance`: E internally closed for the admitted yearless and year-bearing pairs across operators and positions, the cross-class refusal with its diagnostic, the day-1 start completion with a discriminating negative, the month-end finish completion, the February-29 pair, the empty projection, and the fixed-Date gate in both directions. P owns the coincidence law behind the reused certificate; the ordered relation's laws and the empty suppression are already proved at their shared owners.
- `assurance`: L is exact for admission and the completion rows at the [unconfigured-bound checkpoint](../SOURCES.md#src-date-range-unconfigured-bound), including the February pair that separates day 29 from day 28 on otherwise identical documents. Exact-versus-exact runtime and the empty rows are internal compositions with no observation.
- `boundary`: comparing an endpoint with a fixed complete date runs through the same admission rule with a full-Date peer, so a source with no year and no Base Year is refused `MVK_INVALID_COMPARE_TO_DATE` at the comparison instead of as a policy insufficiency at extraction. The exact owner's certificate is reused unchanged, and a proved law states why that is sound: direct-bound support and comparability with a complete date are the same condition on a declaration, so no second precondition can drift from the gate.
- `boundary`: a **semantic-index-selected** endpoint is admitted on this carrier beside a direct one, in either authored slot, and it is the one keyed carrier that needs a runtime **domain** decision. The two direct owners' gates are complementary — proved, not assumed — so the keyed operand's domain is a total two-way read of its retained profile rather than a preference, taken from the certificate instead of from which owner certified the operand. This record's admission rule, both completions, and both verdicts are reused unchanged; a domain disagreement between the keyed profile and the direct operand's owner is the same defensive fault two direct endpoints keep, and it fails closed rather than manufacturing a domain.
- `boundary`: The authored order reaches the fault vocabulary too, so a consumer learns which authored slot failed rather than which operand shape it was. [`SemanticIndexDateRange.lean`](../../A12Kernel/Elaboration/SemanticIndexDateRange.lean) owns that pairing; the keyed source is described at [the endpoint-component record](../IMPLEMENTATION-MAP.md#cap-date-range-bound-component-operand).
- `remains`: repeatable, starred, and group sources, plus a keyed operand on **both** sides — measured **admitted** ([checkpoint](../sources/static-admission-and-class-probes.md#src-keyed-date-range-overlap-admits-both-sides)), implemented for the overlap carrier alone and still blocked here by the `keyedFirst : Bool` pairing — remain in [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-date-range-bound-component-operand"></a>
#### The numeric Date component of a selected DateRange endpoint

- `state`: implemented as one numeric operand atom on three surfaces — validation at both loci, the scalar computation operation, and the repeatable addressed computation placement that computes a row target per instantiated row. `DayFromDate`, `MonthFromDate`, `QuarterFromDate`, and `YearFromDate` accept a DateRange endpoint selection as their source, so the atom carries the field path, the endpoint, and the component together and feeds an ordinary numeric comparison or a Number computation operation.
- `boundary`: two independent gates, and they are kept apart because the family's neighbouring consumers differ. The **locus** gate is the shared unstarred-repeatable-reference rule, so a row operand needs a rule that iterates the row and no wildcard form of this operand exists. The **component** gate is the declared profile's own component set with the model's Base Year supplementing the year alone, which is `DateNumericPart.admittedBy` unchanged from a direct Date field — deliberately *not* the exact-value gate a comparable endpoint carries, so an unconfigured yearless month range exposes month and quarter here while a comparison against a complete date refuses it.
- `boundary`: the runtime read selects from **every** carrier. An exact range yields a complete Date and a yearless one yields exactly the labels its declaration retains, and both project into one calendar-part view, so the component read reuses the established direct-component projection rather than adding an account of emptiness, unavailability, or fillability. Absent substitutes symmetric zero, formal unavailability keeps its exact cause, and only a payload whose kind is not a DateRange stays malformed.
- `owner`: [`DateRangeBoundComponent.lean`](../../A12Kernel/Elaboration/DateRangeBoundComponent.lean) owns the cross-carrier endpoint selection, its calendar-part view, the component-exposure rule, and the flat validation read; [`DateRangeBound.lean`](../../A12Kernel/Elaboration/DateRangeBound.lean) owns the shared range observation and the nonrepeatable-source fact; [`YearlessDateRangeBound.lean`](../../A12Kernel/Elaboration/YearlessDateRangeBound.lean) owns the retained-label carrier the yearless arm projects.
- `owner`: the numeric surfaces carry the atom through [`NumericSource.lean`](../../A12Kernel/Elaboration/NumericSource.lean) (surface and resolved constructors, data dependency, scale), [`NumericValidation/Core.lean`](../../A12Kernel/Elaboration/NumericValidation/Core.lean) (scope admission, reference, relevance, the exposure refusal and its diagnostic), [`NumericValidation/Resolution.lean`](../../A12Kernel/Elaboration/NumericValidation/Resolution.lean) (both scalar and addressed resolution), [`NumericValidation/Evaluation.lean`](../../A12Kernel/Elaboration/NumericValidation/Evaluation.lean), [`NumericValidation/Ordered.lean`](../../A12Kernel/Elaboration/NumericValidation/Ordered.lean) (addressed relevance field ids)
- `owner`: [`ValidationCondition/StaticIteration.lean`](../../A12Kernel/Elaboration/ValidationCondition/StaticIteration.lean) (derived iteration scope), and the computation side through [`NumericComputation/Core.lean`](../../A12Kernel/Elaboration/NumericComputation/Core.lean), [`NumericComputation/Evaluation.lean`](../../A12Kernel/Elaboration/NumericComputation/Evaluation.lean), and [`GeneratedComputationValidation.lean`](../../A12Kernel/Elaboration/GeneratedComputationValidation.lean).
- `case`: [endpoint-component cases](../../A12Kernel/Conformance/DateRangeBoundComponent.lean) separate the two gates on one model — the complete exposure matrix over all six admitted profiles and all four extractors, the Base-Year supplement with the `DayFromDate` control that stays refused, the locus refusal reported before the exposure gate with its kernel class, both endpoint selections of one exact range, the yearless carrier reaching its retained label at either end, absent and formally unavailable reads, and the computation surface admitting the scalar source while refusing the row one.
- `assurance`: E closed for the static matrix and the runtime projection on both carriers. P states the one non-definitional claim the read depends on: the cross-carrier selection and the yearless owner's own selection cannot disagree about which end an endpoint is, plus the read's factoring into observation then projection and the wrong-kind refusal; the empty, present, and unavailable cases are now generic laws at the component owner and are consumed by specialization.
- `assurance`: L is exact for every static row at the [endpoint-component checkpoint](../SOURCES.md#src-date-range-bound-component), including the computation dry runs and their negative controls, and for the computed values at the [runtime checkpoint](../SOURCES.md#src-date-range-bound-component-runtime), where both codegen strategies agreed on every channel: the selected endpoint's component, the unconfigured yearless carrier's retained label, an unplaced range's real zero, and an invalid range's cleared target beside a clean sibling. What stays uncalibrated is the validation-surface runtime read and the day and year component values.
- `owner`: the addressed computation placement is [`AddressedDateRangeBoundPart.lean`](../../A12Kernel/Elaboration/AddressedDateRangeBoundPart.lean), with [caller-read proofs](../../A12Kernel/Proofs/AddressedDateRangeBoundPart.lean). [`AddressedDateRangeBoundPartFormalInput.lean`](../../A12Kernel/Elaboration/AddressedDateRangeBoundPartFormalInput.lean) composes the selected source preparation with the typed Number result, and its [proof owner](../../A12Kernel/Proofs/AddressedDateRangeBoundPartFormalInput.lean) preserves eager findings on success and later failure. The placement adds only the target half over the shared [`AddressedNumericLeaf.lean`](../../A12Kernel/Elaboration/AddressedNumericLeaf.lean) placement.
- `case`: [addressed endpoint-component cases](../../A12Kernel/Conformance/AddressedDateRangeBoundPart.lean) separate the placement gates and replay the measured runtime rows with exact addresses. A transient-view separator replaces two immutable ranges with a different value and one invalid range while immutable target-row enumeration still contributes the absent third-row zero. [Formal-input cases](../../A12Kernel/Conformance/AddressedDateRangeBoundPartFormalInput.lean) retain the source/target/selected roles, both duplicate-index findings, prepared row poison, empty typed-message channel, and exact Number clears.
- `assurance`: E/P additionally close caller-read execution and result projection as exact generalizations of the immutable addressed endpoint-component route. The transient matrix retains exact target addresses, produces month `12`, row-local DateRange poison, and third-row zero, and rejects an immutable-read mutant. This is an internal source-view seam with no new Kernel correspondence, scheduling, or document-materialization claim.
- `assurance`: E/P additionally close the selected whole call. The direct DateRange source remains the sole raw input, duplicate-index findings remain eager, the prepared read poisons both reached rows, both source-filled Number targets clear, and the typed Number message channel stays empty. Success and post-preparation failure retain the exact raw inventory; an immutable-read mutant fails. Kernel correspondence remains pending.
- `assurance`: Q closes bounded cold Execute transport of that caller-read seam. An isolated kind-dispatched consumer retained the DateRange source and Number target families, immutable three-row target topology, caller-supplied reached cells, start-month `12`, row-local invalid-range poison, and absent-source zero; an independent checker rejected immutable reads, finish selection, topology replacement, global poison, and untyped-result accounts. This is finite Lean-account transport, not Kernel correspondence, scheduling, or shipment closure.
- `boundary`: the placement admits its source at any scope the target's own scope binds, so a scalar endpoint computing a row Number is ordinary. That widening belongs to the shared placement and landed across every addressed one-source leaf at once; [§5](../IMPLEMENTATION-MAP.md#5--numbers-and-decimals) owns it and the [operand-scope checkpoint](../SOURCES.md#src-computation-operand-scope) owns its measurement.
- `owner`: the **semantic-index-selected** source is [`SemanticIndexDateRange.lean`](../../A12Kernel/Elaboration/SemanticIndexDateRange.lean), which adds only the target half over the shared semantic-index certificate — the selected declaration's own certified DateRange profile, plus this component read's exposure witness over it — and reads through that certificate's own generated-preliminary route. The profile is admitted through the family's scope-free `FlatModel.dateRangeSourceProfile`, which every addressed carrier now reaches through its own reading-scope gate: a keyed row arrives from the index column rather than from the reading environment, and that is the whole difference between the two admissions.
- `owner`: Both read routes now compose the same `DateNumericPart.fromDateRangeBoundObservation` clause, so keyed and direct reads are structurally unable to disagree about a projected observation; only about which cell they reach.
- `boundary`: the keyed read's result domain is the ordinary phase observation with no new outcome class. A no-match row and a matched-but-empty cell both read empty and reach the same fillable zero, while a duplicated key and a formally invalid matched cell both reach UNKNOWN carrying their own distinct causes. An unresolvable lookup keeps its own error class rather than collapsing, because a consumer must distinguish it from a resolved column that selected nothing; a selected cell of the wrong kind collapses to malformed exactly as a direct read does.
- `case`: [keyed endpoint-component cases](../../A12Kernel/Conformance/SemanticIndexDateRange.lean) lock both key forms with a String index over a DateRange target, the exposure gate on an unconfigured yearless target, the wrong-kind target refusal with its deliberately unmapped diagnostic, and the four-state runtime domain where each case perturbs exactly one cell of one baseline.
- `assurance`: E/P closed for the keyed source and all four wired carriers. L is exact for the admission and the four-state result domain at the [endpoint-shape checkpoint](../SOURCES.md#src-date-range-endpoint-shapes), which measured the keyed operand accepted on both of them at a nonrepeatable locus, at an iterating one, and when the keyed group is itself iterated. Every local refusal class — a non-DateRange selected target, an unexposed component, and a comparability mismatch — stays **unmapped to a Kernel diagnostic** and is external evidence pending: that batch established which shapes are accepted, not which code a keyed rejection carries.
- `boundary`: the same keyed source serves **stored range equality**, the **endpoint-pair comparison**, and the **overlap predicate**, whose gates and verdicts stay with their own records. All four measured carriers now consume one keyed read, which is what justified promoting the source's certified profile onto its certificate and extracting three shared clauses the direct routes now use too: the yearless endpoint projection, the overlap year rule, and the overlap slot projection.
- `remains`: no measured keyed carrier is open. What stays unrepresented is a keyed slot inside the overlap operator's own list beside a starred, filtered, or group slot, and a keyed operand on **both** sides of any carrier — the latter now **measured admitted** on either overlap operator, with the duplicate identity keyed on the (field, key) pair ([checkpoint](../sources/static-admission-and-class-probes.md#src-keyed-date-range-overlap-admits-both-sides)), so only the mixed-slot shape stays unmeasured. The overlap carrier now implements the keyed pair with its own duplicate gate; the numeric-component, stored-equality and endpoint-comparison carriers keep the single-keyed-side pairing, which the measurement did not reach. Starred and group endpoint sources remain in [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-iterated-date-range-conditions"></a>
#### DateRange conditions read at the rule's iterating row

- `state`: implemented for nine carriers at **either** locus — stored-versus-stored equality, one selected endpoint against a fixed complete date, a pair of selected endpoints in either value domain, both overlap operators in **both** value domains, and a constructed range against either a stored range or another construction — in both authored slots and at every comparison polarity each carrier admits.
- `boundary`: admission is the locus rule stated positively. The reading scope is the rule group's own repeatable scope, and an operand is accepted exactly when that scope binds every repeatable level it crosses, so the nonrepeatable read is the `[]` instance of one predicate rather than a separate rule. An operand crossing an unbound level is still reported as a repeatable reference, which is the class that reports `MVK_NO_WILDCARD`, and a condition with no repeatable operand is refused because it belongs to the scalar carriers.
- `boundary`: the nine carriers share one leaf family and one read shape, so the checked condition tree carries a single arm per dispatch and a further carrier lands as a member rather than as another set of arms. Each member keeps its own comparison: the equality's component gate and identity comparison, the fixed-date member's authored-side rule, the pair's ordinary `admitsFormats` comparability, each overlap member's whole-list gates including the group refusal, the uniform-year rule, and the plural operator's own scalar-shape and cross-side rules, and the construction member's endpoint kind, profile, and cross-operand component gates.
- `boundary`: The cell read is a parameter of the shared verdict function, because its failure channel belongs to the enclosing leaf; the overlap member additionally receives the document, because a starred operand's extent is a stream of rows rather than one cell. Every locus refusal is reported as the same resolution failure across members, so a consumer reads one class rather than one per carrier.
- `boundary`: what the leaf owns is the row read, and every member performs it through its own carrier's read rather than a leaf-local one. Four shared mechanisms were widened by a reading-scope or environment parameter whose empty instance is the previous behaviour exactly: the entity-list shape resolver, which admits an unstarred operand the scope binds; the direct entity-operand read; the temporal target policy, which also retains its declaration so a row-reading consumer can see the scope it must satisfy; and the DateRange source read itself, which now addresses at an environment while the root read stays a plain root read sharing one payload projection with it.
- `boundary`: Absent and formally unavailable operands keep their carriers' behaviour; a payload of the wrong kind collapses to UNKNOWN or to a skipped overlap slot rather than claiming a fault channel, and the certificates' kind, policy, and exact-value gates make those collapses unreachable.
- `boundary`: the leaf contributes its operands' declaration scopes to the derived iteration scope through the shared merge, requires the addressed evaluator, returns UNKNOWN on the scalar route rather than reading row one, and projects its operands as message pointers in authored order. Its iteration guard is positive at every polarity — measured, not assumed: the same model refuses `FieldNotFilled` on the same operand with `MVK_NEG_CONDITION_IN_ITERATION` and admits its guarded form, so the gate's reach is presence-shaped rather than polarity-shaped.
- `owner`: [`IteratedDateRangeCondition.lean`](../../A12Kernel/Elaboration/IteratedDateRangeCondition.lean) owns the shared member set, its refusal classes, and the read-parameterized verdict; [`DateRangeBound.lean`](../../A12Kernel/Elaboration/DateRangeBound.lean) owns the scope-bound admission projection, the iterated source and endpoint certificates, and the authored-side comparison now shared with the scalar carrier; [`DateRangeStoredComparison.lean`](../../A12Kernel/Elaboration/DateRangeStoredComparison.lean) owns the equality pair; [`Flat/Model.lean`](../../A12Kernel/Elaboration/Flat/Model.lean) owns the scope-binding predicate and its resolver.
- `owner`: the condition tree carries the leaf across [`Core.lean`](../../A12Kernel/Elaboration/ValidationCondition/Core.lean) (construction and static admission), [`Evaluation.lean`](../../A12Kernel/Elaboration/ValidationCondition/Evaluation.lean) (the row read), [`Iteration.lean`](../../A12Kernel/Elaboration/ValidationCondition/Iteration.lean) and [`StaticIteration.lean`](../../A12Kernel/Elaboration/ValidationCondition/StaticIteration.lean) (guard and derived scope), [`Reference.lean`](../../A12Kernel/Elaboration/ValidationCondition/Reference.lean) (message pointers), and [`Assembly.lean`](../../A12Kernel/Elaboration/ValidationCondition/Assembly.lean) (one locus-checked step behind three member front ends).
- `case`: the locks are split by value domain: [resolved-date cases](../../A12Kernel/Conformance/IteratedDateRangeCondition.lean) and [retained-label cases](../../A12Kernel/Conformance/IteratedYearlessDateRangeCondition.lean).
- `case`: Together they separate both loci on every carrier, every member at a non-iterating locus with its gates intact, both slots, both authored construction positions, every admitted polarity, the scalar-only refusal, the per-row construction from each row's own endpoints, the component and comparability gates with an admitted lexical crossing, the overlap member's unchanged multiplicity/duplicate/group gates, the star-only refusal that keeps one owner per condition, the derived scope with the addressed-route requirement, the Explain-facing row pointer, per-row verdicts over a two-row document, both endpoint selections of one row, the mixed bare-plus-star self-pair, and the empty-operand rows.
- `assurance`: E closed for the nine admitted shapes at both loci, including the unconfigured yearless endpoint pair and both label-domain overlap operators whose labels stay retained rather than completed; P states the one law the row read needed — a root-read endpoint's address does not depend on its environment, which is what lets the four endpoint laws rewrite past the address computation. L is exact for the static rows at the [rule-locus checkpoint](../SOURCES.md#src-date-range-rule-locus), including the endpoint and overlap shapes, the mixed bare-plus-star admission, the polarity matrix, and its live negative control; every per-row runtime row is an internal composition of two separately calibrated mechanisms with no observation of its own, and the mixed shape's self-pair verdict follows the canonical clause rather than an observation.
- `boundary`: the family has one source certificate. `CheckedDateRangeSource` carries the reading scope, the declaration, and the checked profile; the nonrepeatable `CheckedDirectDateRange` is that certificate refined by a proof that the scope is empty, so a scalar carrier coerces into the general one and an iterated consumer accepts a scalar operand without a second certificate. Both admit through the one shared projection, so no two carriers can disagree about a declaration's profile.
- `boundary`: the locus may iterate **two** repeatable levels, and then it admits an operand at the root, at the enclosing row, or at the leaf, each read at its own path inside the current leaf. The correlating middle-level operand reaches its own enclosing row's cell, which the [nested-locus cases](../../A12Kernel/Conformance/NestedIteratedDateRangeCondition.lean) separate from a borrowed row-one read by inverting the verdict pattern rather than shifting it; the [nested-locus checkpoint](../SOURCES.md#src-nested-rule-locus) Kernel-locks both the admission matrix and that runtime pattern on either codegen strategy, including the `referenced` channel naming the second row's own operand. The mechanism needed no change: the leaf already carried its locus scope as a parameter and read every operand at its own declared scope.
- `boundary`: this leaf is the DateRange condition family's **only** rule-level owner. Before it, every overlap and comparison source was a component-level capability with conformance and laws but no condition-tree carrier, so no assembled rule could reach one. It therefore admits an all-scalar condition as well, deriving no iteration there, rather than refusing the scalar shape into unreachability. Neither unconfigured yearless overlap operator had a whole-source elaborator at all; both now share the same shape gates as their exact counterparts, with the plural one keeping the operator's own scalar-shape rule.
- `boundary`: an overlap member reports rather than drops an impossible payload. The family's evaluation carries three payload classes and an incoherent-extent class that no checked certificate can produce, and they project into one shared addressing class instead of collapsing to a skipped slot, so the standalone and leaf routes give one account of one certificate.
- `boundary`: the family's certificate chain is one line, not two. A DateRange source carries its reading scope; the nonrepeatable source is that certificate refined by its declaration crossing no repeatable level; the general endpoint extends the source; and the root-reading endpoint is that endpoint refined the same way. Each refinement's read is a plain root read, so no consumer pays for an environment it does not have, while every scope-carrying consumer reuses the same selection and comparison.
- `consolidation`: one duplication remains. The mixed construction-versus-stored carrier is still paired locally by the iterated member rather than reused, because its stored half is typed as the scalar refinement; retyping it to the shared source certificate and reading at an environment would delete the local pairing. That is an ordinary follow-on unit, not a blocker.
- `remains`: mixed construction-versus-stored carrier consolidation, filtered-star operands beside a row read, keyed operands, and partial-view evaluation for this leaf remain under [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).

<a id="cap-date-range-repeatable-operand-locus"></a>
#### Repeatable-operand rule locus across the DateRange carriers

- `state`: implemented in both directions. An unstarred repeatable DateRange operand read from a non-iterating rule locus is refused with `MVK_NO_WILDCARD`; the same operand is admitted when the rule locus binds its repeatable level. The checked DateRange condition leaf carries that scope through elaboration and addressed evaluation for the overlap predicates, stored equality, exact and yearless endpoint extraction, endpoint pairs, numeric endpoint components, and two-endpoint construction.
- `boundary`: the class is owned once by `ResolveError.diagnostic?` at the resolver's own error type and inherited by the shared entity-list checker, the direct DateRange owner, and the construction endpoint, so a new carrier acquires it by delegating rather than by restating the row. Every other resolution failure still projects `none`, which keeps uncovered classes visibly uncovered.
- `boundary`: the admitted half is represented by the family's sole rule-level leaf, which derives the reading scope from the rule locus and reads each operand at its own path inside the selected row. The same leaf admits all-scalar conditions without inventing iteration and supports two nested repeatable levels, correlating a root, enclosing-row, or leaf operand to the selected leaf environment.
- `owner`: [`Flat/Types.lean`](../../A12Kernel/Elaboration/Flat/Types.lean) owns the projection; [`FieldEntityList.lean`](../../A12Kernel/Elaboration/FieldEntityList.lean), [`DateRangeBound.lean`](../../A12Kernel/Elaboration/DateRangeBound.lean), and [`DateRangeConstructionComparison.lean`](../../A12Kernel/Elaboration/DateRangeConstructionComparison.lean) delegate to it.
- `case`: [rule-locus refusal cases](../../A12Kernel/Conformance/DateRangeRepeatableOperand.lean) pin the reported class on the overlap predicate in either slot, stored equality from either side, an exact endpoint, a yearless endpoint whose format and Base-Year gates would otherwise refuse the same field, and the endpoint pair, each against an admitted scalar control; [iterated cases](../../A12Kernel/Conformance/IteratedDateRangeCondition.lean), [iterated yearless cases](../../A12Kernel/Conformance/IteratedYearlessDateRangeCondition.lean), and [nested cases](../../A12Kernel/Conformance/NestedIteratedDateRangeCondition.lean) pin admitted execution.
- `assurance`: E is closed for refused outside-locus and admitted inside-locus execution in the exact and unconfigured yearless domains, including two-level correlation. P none, since the shared locus projection is a total case split and the payoff lies in executable addressing separators. L is exact for the ten refusal/admission rows and gate-order separator at the [rule-locus checkpoint](../SOURCES.md#src-date-range-rule-locus), the yearless directions at the [yearless-locus checkpoint](../SOURCES.md#src-date-range-yearless-locus), and nested correlation at the [nested-locus checkpoint](../SOURCES.md#src-nested-rule-locus). Accepted [SPEC-2026-08-23-04](../archived/A12-DMKITS-SPEC-SYNC-LEDGER-THROUGH-2026-08-28.md#spec-2026-08-23-04--the-operand-locus-gate-decides-before-the-error-field-gate) carries the gate order and carrier breadth outbound.
- `remains`: filtered-star operands beside a row read, keyed operands, and partial-view execution remain under [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).
