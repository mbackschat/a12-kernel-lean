# String and enumeration capabilities

### §7 — strings and patterns

<a id="cap-string-value-and-length"></a>
#### String value and length

- `boundary`: Decoded/evaluated text, equality, CRLF normalization, and UTF-16 length. The flat `Length` surface retains each numeric literal's exact rational value and authored scale in either operand order; ordinary ordering lowering intentionally projects only the value. The raw final-rule elaborator recognizes the two strict whole-rule forms, admits exactly decimal-point-free signed-32-bit bounds including trailing-zero integer tokens, eliminates them to maximum-length metadata, and projects bound-domain versus shape refusal to the two exact measured Kernel classes without mapping unrelated elaboration failures
- `owner`: [`String.lean`](../../A12Kernel/Semantics/String.lean), [`Flat/Types.lean`](../../A12Kernel/Elaboration/Flat/Types.lean), [`Flat/Condition/Surface.lean`](../../A12Kernel/Elaboration/Flat/Condition/Surface.lean), [`RawString.lean`](../../A12Kernel/Elaboration/RawString.lean), [`StaticDiagnostic.lean`](../../A12Kernel/Elaboration/StaticDiagnostic.lean)
- `assurance`: E/P closed for the exact raw final-rule bound, diagnostic projection, decoded-literal identity, and ordinary String control; clean dmtool `0.12.1` at a12-dmkits [source registry](../SOURCES.md) Kernel-confirms the 13-case boundary plus direct trailing-zero and diagnostic-precedence controls; selected C
- `remains`: Complete ingestion/Unicode: [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion). Wider static-diagnostic coverage: [SG9](../SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion)

<a id="cap-pattern-and-charset-admission"></a>
#### Pattern and charset admission

- `boundary`: Finite built-in Java-pattern profiles, injected model-complete matchers, legal-character membership, and exact condition-pattern diagnostics for every represented non-String left kind plus both pattern-admission stages. Unmeasured local refusals project to `none`; the canonical right-slot field diagnostic has no local projection because the checked surface requires a String constant structurally
- `owner`: [`StringPattern.lean`](../../A12Kernel/Semantics/StringPattern.lean), [`StringPattern.lean`](../../A12Kernel/Elaboration/StringPattern.lean), [`LegalCharset.lean`](../../A12Kernel/Semantics/LegalCharset.lean)
- `assurance`: E/P closed for stated profiles; clean dmtool `0.12.1` at a12-dmkits [source registry](../SOURCES.md) Kernel-confirms the 22-case accepted, invalid-source, complete left-kind, and wrong-right-slot matrix for both operators; runtime L partial; C none
- `remains`: Arbitrary pattern/custom integration and repeatable pattern operands: [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion)

<a id="cap-string-target-policy"></a>
#### String target policy

- `boundary`: Declaration-owned line-break, pattern, minimum/maximum length, attempted payload, and source-relative delta
- `owner`: [`StringFieldPolicy.lean`](../../A12Kernel/Semantics/StringFieldPolicy.lean), [`StringComputation.lean`](../../A12Kernel/Semantics/StringComputation.lean)
- `assurance`: E/P closed for admitted target; root subset C
- `remains`: Wider target/ingestion: [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion)

<a id="cap-string-aggregates-value-lists"></a>
#### String aggregates/value lists

- `boundary`: Direct/plain-star/filtered-star String entity lists, membership, quantifiers, token counts, and mixed first-filled selection
- `owner`: [`ValueList.lean`](../../A12Kernel/Semantics/ValueList.lean), [`TokenEntityValueList.lean`](../../A12Kernel/Elaboration/TokenEntityValueList.lean), token count/first-filled owners
- `assurance`: E/P closed for named shapes; upstream L partial; C none
- `remains`: Remaining list shapes: [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion), [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion)

<a id="cap-string-computation-execution"></a>
#### String computation execution

- `boundary`: Checked expression/table, same-target encounter-order flattening, finite supplied-order run, overlay, relation, rich result, direct-versus-retained application, direct-cascade specialization, typed nonrepeatable Number `FieldValueAsString` reads, checked String `FieldValueAsNumber` reads, both bounded completed Number/String overlays through the immutable checked document, one repeatable `FieldValueAsString` placement over exact checked addresses, finite direct one- and two-level separate-destination projections with normalized parent-scoped predecessor rows and explicit later String-row selection, and exact target diagnostic projection for measured root direct-copy and `RangeAsString` self-reference
- `owner`: [`StringComputation.lean`](../../A12Kernel/Elaboration/StringComputation.lean), [`StringComputationTable.lean`](../../A12Kernel/Elaboration/StringComputationTable.lean), [`StringComputationRunPlan.lean`](../../A12Kernel/Elaboration/StringComputationRunPlan.lean), [`StringComputationRun.lean`](../../A12Kernel/Elaboration/StringComputationRun.lean), [`StringComputationRunApplication.lean`](../../A12Kernel/Elaboration/StringComputationRunApplication.lean), [`NumberToStringComputationRun.lean`](../../A12Kernel/Elaboration/NumberToStringComputationRun.lean), [`StringToNumberComputationRun.lean`](../../A12Kernel/Elaboration/StringToNumberComputationRun.lean), [`AddressedFieldValueAsString.lean`](../../A12Kernel/Elaboration/AddressedFieldValueAsString.lean)
- `owner`: [one-level application cases](../../A12Kernel/Conformance/StringComputationRepeatableApplication.lean), and [two-level application cases](../../A12Kernel/Conformance/StringComputationNestedApplication.lean)
- `assurance`: E/P closed for the named nonrepeatable String surface, both bounded Number/String directions, the addressed repeatable leaf, and direct one- and two-level String application. Clean dmtool `0.13.0` at a12-dmkits [source registry](../SOURCES.md) Kernel-confirms the direct-copy and root-range target diagnostic plus a persisted accepted other-field control; wider target reads retain local refusal and no external projection. The addressed matrix separates two row keys, decimal-versus-String Number storage, absence/present-empty, local formal poison, target rejection, and source-relative clearing.
- `assurance`: L closes one-level String predecessor topology and two-level parent-scoped inner topology, including exact VALUE/CLEARED coordinates, destination preservation, non-vacuous ERRORED non-creation, and later `Length(Label) > 0` addresses. The exact receipts remain in the [source registry](../SOURCES.md).
- `assurance`: Root/direct-cascade C; bounded application and Number/String target-diagnostic Q; composed cross-family execution C none
- `remains`: Wider cross-family execution: [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition). String application beyond two direct finite levels and remaining repeatable String expressions and conversions: [SG5](../SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion) and [SG7](../SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion)


### §8 — enumerations and value lists

<a id="resolved-firstfilledvalue"></a>

<a id="cap-first-filled-value"></a>
#### Resolved `FirstFilledValue`

- `boundary`: ordered mixed String/stored-Enumeration selection retains omitted-tail and kept-successor behavior.
- `boundary`: fixed Boolean or Custom targets consume one direct single-level starred field of the same carrier. Their addressed siblings admit one repeatable target and a same-carrier source with exactly one reopened star axis and a nonempty outer binding prefix; each physical target row supplies that prefix, so a nested sibling source scan remains correlated to the enclosing target row. Boolean retains typed truth identity. Custom requires exact declaration equality and consumes already prepared registered-validator findings without resampling the validator.
- `boundary`: DateFragment admits matching target/source declarations for exactly `MM`, `yyyy`, `yyyy-MM`, or `MM-dd`; its checked-token path excludes the opt-in pre-1900 check, classifies the exact selected token against immutable source placement, and applies retained actions to a separate same-model text-state projection without claiming whether the Kernel copies or rerenders that token.
- `boundary`: the full-Date computation admits matching fixed `yyyy-MM-dd` or `dd.MM.yyyy` target/source declarations over one direct single-level star, selects typed instant identity, delegates model-zone rendering and target checks to the existing checked full-Date policy, classifies that outcome against the immutable source target, and applies retained source-relative actions to a separate same-model checked destination; crossed formats, an optional pre-1900 policy on either declaration, source-token copying, and materialized-document reconstruction remain excluded.
- `boundary`: the fixed `HH:mm:ss` Time computation selects clock identity from one direct single-level star, discards the runtime transport instant, delegates rendering to the existing checked Time target, classifies that outcome against the immutable source target under Time's measured always-changed rule, and applies retained actions to a separate same-model checked destination; it does not claim source-token copying or materialized-document reconstruction.
- `boundary`: the fixed `yyyy-MM-dd'T'HH:mm:ss` DateTime computation selects exact-instant identity from one direct single-level star, ignores source text and cached wall-label components, delegates model-zone rendering to the existing checked DateTime target, classifies that outcome against the immutable source target, and applies retained source-relative actions to a separate same-model checked destination; the currently admitted computation target uses the ISO whole-second profile, and materialized-document reconstruction remains outside.
- `boundary`: the DateRange computation admits all eight declaration pairs as target and direct single-level starred source, and accepts any source whose declared component set equals the target's, so both full-Date spellings, both month-only spellings, both day-and-month spellings, and configured opposite `FROM`/`TO` interpretations cross in either direction. It retains exact `DateRangeValue` or interpretation-free unconfigured yearless identity through declaration-owned rendering, classifies that direct-star outcome against the immutable source target, and applies retained source-relative actions to a separate same-model checked destination. The order-dependent unconfigured interpretation branch and materialized-document reconstruction are excluded from this field-local account.
- `boundary`: the DateRange computation also admits a finite list of at least two direct nonrepeatable same-group sources. Two gates apply in order, and they are not the same gate: every source must repeat the **identical** declared format, refused `varyingProfiles` with the Kernel's `MVK_VARYING_TYPES_NOT_ALLOWED` even where the component sets agree, and only then must the one shared source format expose the **target's** component set, refused `targetProfileNotComparable` with `MVK_INVALID_COMPARE_TO_DATE_RANGE`. A declared `interpretationOfYear` changes neither gate.
- `boundary`: The lazy recursive scan retains exact or yearless component identity, and the **target's** own format renders the selected value, so an admitted crossing stores the target spelling even when source and target interpret a wrapping label under different endpoint years. The result is classified against the immutable source target and retained source-relative actions apply to a separate same-model checked destination without materialized-document reconstruction.
- `boundary`: all seven bounded computation families project no-value on exhaustion and poison on reached formal invalidity. Boolean retains its typed result Custom and DateFragment retain exact tokens full Date and DateTime retain an instant Time retains a `TimeOfDay` DateRange retains exact endpoints or yearless components until target rendering. Every carrier additionally classifies its family-owned outcome against the immutable source target and applies retained actions to a separate same-model checked destination Boolean keeps typed identity and formally invalid nonempty placement distinct, Custom does so without resampling the registered validator or running implicit validation, DateFragment keeps copy-versus-render outside its correspondence claim, and Time retains its measured source-identical changed-action rule.
- `owner`: [`FirstFilledValue.lean`](../../A12Kernel/Semantics/FirstFilledValue.lean) owns the ordered core; [`ExactTokenComputationResult.lean`](../../A12Kernel/Elaboration/ExactTokenComputationResult.lean) owns the shared target projection after a family has certified exact-token acceptance; [`FirstFilledStarSource.lean`](../../A12Kernel/Elaboration/FirstFilledStarSource.lean) and [`TemporalFirstFilledStarComputation.lean`](../../A12Kernel/Elaboration/TemporalFirstFilledStarComputation.lean) own the direct-star and typed temporal-carrier certificates; [`FieldEntityList.lean`](../../A12Kernel/Elaboration/FieldEntityList.lean) owns the finite direct-list cardinality, duplicate, and overlap shape.
- `owner`: [`DateRangeTargetPresentation.lean`](../../A12Kernel/Elaboration/DateRangeTargetPresentation.lean) owns shared exact and fragment DateRange presentation for the constructor and direct-star computation paths.
- `owner`: [`BooleanApplication.lean`](../../A12Kernel/Semantics/BooleanApplication.lean) owns target-polymorphic typed Boolean state and retained value/clear transitions.
- `owner`: The bounded computation adapters are fixed [Boolean](../../A12Kernel/Elaboration/BooleanFirstFilledComputation.lean), [addressed Boolean](../../A12Kernel/Elaboration/AddressedBooleanFirstFilledComputation.lean), fixed [Custom](../../A12Kernel/Elaboration/CustomFirstFilledComputation.lean), [addressed Custom](../../A12Kernel/Elaboration/AddressedCustomFirstFilledComputation.lean), [DateFragment](../../A12Kernel/Elaboration/DateFragmentFirstFilledComputation.lean), [full Date](../../A12Kernel/Elaboration/FullDateFirstFilledComputation.lean), [Time](../../A12Kernel/Elaboration/TimeFirstFilledComputation.lean), [DateTime](../../A12Kernel/Elaboration/DateTimeFirstFilledComputation.lean), and [DateRange](../../A12Kernel/Elaboration/DateRangeFirstFilledComputation.lean). [`AddressedFirstFilledStar.lean`](../../A12Kernel/Elaboration/AddressedFirstFilledStar.lean) owns the shared addressed target and source-placement certificates.
- `case`: fixed [Boolean](../../A12Kernel/Conformance/BooleanFirstFilledComputation.lean), [addressed Boolean](../../A12Kernel/Conformance/AddressedBooleanFirstFilledComputation.lean), fixed [Custom](../../A12Kernel/Conformance/CustomFirstFilledComputation.lean), [addressed Custom](../../A12Kernel/Conformance/AddressedCustomFirstFilledComputation.lean), and [DateFragment](../../A12Kernel/Conformance/DateFragmentFirstFilledComputation.lean) separate carrier admission, no-value/value, and formal-order branches. The fixed Boolean cases additionally separate typed source-relative unchanged behavior against a different destination, changed application with unrelated-state preservation, filled-source clear materialization, absent-source no-action, cause-blind poison clearing, and residual-error retention.
- `case`: The addressed Boolean matrix separates parent-correlated sibling scans from the wrong global-scan account, explicitly refuses an unbound root/global star, and distinguishes false from no-value, leaf-exact formal poison, immutable-source changed classification, exact clear placement, and unrelated destination preservation across seven target rows. The addressed Custom matrix additionally separates unknown target lookup, exact declaration policy rather than name identity, partially bound and unbound source scope, noncanonical physical target order, registered rejection, absent sibling no-action, and source-filled exact clearing.
- `case`: DateFragment separates exact-token changed and unchanged classification, residual retention, exhausted and poisoned clearing, absence-to-present-empty application, target-error exclusion, and unrelated destination preservation.
- `case`: [full Date](../../A12Kernel/Conformance/FullDateFirstFilledComputation.lean), [Time](../../A12Kernel/Conformance/TimeFirstFilledComputation.lean), [DateTime](../../A12Kernel/Conformance/DateTimeFirstFilledComputation.lean), and the DateRange [direct-star](../../A12Kernel/Conformance/DateRangeFirstFilledComputation.lean) and [direct-list](../../A12Kernel/Conformance/DateRangeFirstFilledDirectComputation.lean) owners separate typed payloads, format policy, no-value/value, and formal-order branches. The full-Date, DateTime, and both DateRange cases additionally separate changed application with unrelated-state preservation, source-relative unchanged behavior against a different destination, and retained clear materialization for both exhaustion and reached formal poison.
- `case`: The direct-list changed control also crosses source and target lexical formats before classification. The Time cases separate its source-identical changed action, changed application with unrelated-state preservation, and the same two clear terminals.
- `proof`: fixed [Boolean](../../A12Kernel/Proofs/BooleanFirstFilledComputation.lean) specializes the shared scan and proves checked execution delegates its exact outcome, certified target, and residual channel to the Boolean result classifier. [Addressed Boolean](../../A12Kernel/Proofs/AddressedBooleanFirstFilledComputation.lean) retains the one-axis and bound-prefix source certificates, proves exact-row result projection, and delegates exact-address application to the same typed Boolean fold.
- `proof`: [Fixed Custom](../../A12Kernel/Proofs/CustomFirstFilledComputation.lean) preserves accepted bytes and registered rejection, retains the exact checked operation, excludes target-error results, owns every action at that operation's target, and delegates application to the established String-shaped fold. [Addressed Custom](../../A12Kernel/Proofs/AddressedCustomFirstFilledComputation.lean) retains exact Custom declaration equality plus the shared placement certificates, proves exact-address token projection and target-error exclusion, and delegates application to that same String-shaped fold.
- `proof`: [DateFragment](../../A12Kernel/Proofs/DateFragmentFirstFilledComputation.lean) preserves the checked output token and formal cause, retains the exact checked operation, excludes target-error results, owns every action at that operation's target, and delegates application to the same established fold.
- `proof`: [full Date](../../A12Kernel/Proofs/FullDateFirstFilledComputation.lean) preserves exact instant/cause identity, proves that a present or poisoned head hides every suffix, and proves that result construction retains the operation-owned target while delegating to the established full-Date projection. Existing Number/token proofs retain the general prefix and lookahead laws.
- `proof`: [Time](../../A12Kernel/Proofs/TimeFirstFilledComputation.lean) preserves exact clock and cause identity and proves that result construction retains the operation-owned target while delegating to the established Time projection; its executable cases lock empty-prefix continuation and present/poison prefix termination without adding operator-named restatements of the shared scan law.
- `proof`: [DateTime](../../A12Kernel/Proofs/DateTimeFirstFilledComputation.lean) preserves exact instant and cause identity and proves that result construction retains the operation-owned target while delegating to the established DateTime projection; its executable cases distinguish source text, cached wall label, model-zone rendering, and ordered scan terminals.
- `proof`: [DateRange](../../A12Kernel/Proofs/DateRangeFirstFilledComputation.lean) preserves exact-or-yearless cell and cause identity, proves both checked carriers retain their operation-owned target while delegating result construction to the established DateRange projection, and proves finite-list exhaustion, empty-head recursion, and value/formal prefix termination for the lazy direct-source scan.
- `assurance`: E/P closed for the named mixed selection and bounded Boolean/Custom computations. Addressed Boolean and Custom share one carrier-neutral target and source-placement certificate; addressed Custom and Enumeration share the exact-address token outcome, source-state projection, and no-target-error law. The addressed carriers close physical target-row execution, one nonempty bound outer prefix above one reopened star axis, exact target keys, parent-local sibling selection, source-relative actions, and same-model exact-cell application. The unbound root/global-star shape remains outside this fragment without a Kernel-legality claim. a12-dmkits [source registry](../SOURCES.md) locks fixed Boolean/Custom empty and value outcomes through all three engines; both addressed carriers and their result/application integrations remain external evidence pending beyond the general starred-binding and retained-action clauses.
- `assurance`: E/P internally closed for all four exact DateFragment formats, including exact-token selection, crossed-format and unsupported-policy refusal, the shared clear/poison branches, source-relative result construction, and same-model checked-destination text-state application. Static Kernel admission covers all four formats; direct runtime calibration remains exact only for `MM`, does not distinguish token copying from target rendering, and does not establish the result/application composition.
- `assurance`: E/P internally closed for both exact full-Date formats, fixed `HH:mm:ss` Time, and fixed `yyyy-MM-dd'T'HH:mm:ss` DateTime, including typed selection, target rendering/checking, crossed full-Date formats, optional-check exclusion, and the remaining named admission refusals. The full-Date and DateTime routes additionally close source-relative result construction and exact checked-destination value/clear application. The Time route closes result construction, its always-changed accepted path, and exact checked-destination value/clear application. External full-Date `FirstFilledValue` runtime calibration remains exact only for `yyyy-MM-dd`; dotted target rendering is independently tri-engine locked while its direct composition remains external evidence pending.
- `assurance`: E/P internally closed for all eight direct-star DateRange inputs and the represented finite direct-field-list profiles, including exact-or-yearless checked-cell selection, lazy recursive empty/value/formal order, declaration-owned rendering, cross-component and target-self-reference refusal, and independent unsupported format/separator refusal. Both carriers additionally close source-relative result construction and exact checked-destination value/clear application.
- `assurance`: external static admission and Kernel/interpreter runtime calibration cover all eight direct-star DateRange declaration pairs at a12-dmkits [source registry](../SOURCES.md), including the full-Date lexical cross, and Lean executes all eight with every within-set crossing. Neither direct-list nor constructor-target evidence widens with this result.
- `assurance`: the exact full-Date lexical cross is externally admitted and locally represented on both the starred and the direct-list route, the latter pinned at the [list-crossing checkpoint](../SOURCES.md#src-date-range-list-crossing) together with its distinct source gate and its rendered output. Universal resolved-endpoint refusal remains owned by the value bridge rather than an authorable checked-input case.
- `assurance`: the [interpretation direct-list](../SOURCES.md#src-date-range-interpretation-first-filled) and [configured direct-star](../SOURCES.md#src-date-range-interpretation-first-filled-star) checkpoints pin both admission directions between `FROM` and `TO`, wrapping and ordered target output, later-source or later-row fallback, distinct source identity, and seeded-target clearing on both Kernel strategies. The corrected [unconfigured direct-star checkpoint](../SOURCES.md#src-date-range-unconfigured-interpretation-first-filled-star) covers one shared-format equivalence pair under both declaration orders and proves that a field-local source or target rule is insufficient. Unconfigured direct-list execution, the wider generated-format denominator, a dependent target read, another computation family, and configured interpreted profiles beyond `dd.MM` remain external evidence pending.
- `assurance`: L covers the maintained dynamic-Kernel/interpreter agreement rows and exact four-carrier clear/value calibration.
- `assurance`: the [fragment DateRange checkpoint](../SOURCES.md#src-date-range-fragment-first-filled) and [reviewed reconciliation checkpoint](../SOURCES.md#src-date-range-2026-08-19-reconciliation) lock all-eight direct-star admission and outputs through both Kernel strategies, plus dotted-to-ISO full-Date admission and target rendering. Direct-list and constructor-target denominators remain narrower; empty/formal fragment outcomes remain external evidence pending; C none.
- `assurance`: the [direct-field-list DateRange checkpoint](../SOURCES.md#src-date-range-direct-first-filled) locks dotted/dash admission at lengths two and three; later exact-policy and target-self-reference refusal; recursive empty continuation, terminal-prefix suffix hiding, reached-formal termination, and eager formal-input reporting through both Kernel strategies.
- `assurance`: the same checkpoint locks three-source admission and third-source output for every fragment profile, including configured and yearless `MM`/`MM-dd`, through both Kernel strategies. ISO/slash list execution, fragment-list formal suffixes, and lengths greater than three remain external evidence pending; C none.
- `assurance`: Boolean false and all seven formal-failure branches are internally checked with external evidence pending. Existing prefix/lookahead seams remain upstream locked; C none.
- `assurance`: Q Execute closed for the exact fixed UTC four-carrier profile: a fresh implementation-blind reader reconstructed typed selection, ordered empty-prefix scanning, target rendering, clearing/application, and all twelve calibrated outcomes from the canonical clauses plus the calibrated source record. It kept copy-versus-decode/render, alternate policies, formal poison calibration, shipment, and public compatibility outside the claim.
- `assurance`: Q Execute/Analyze internally closes the direct DateRange list decision: the checked operation exposes authored source order and checked declarations, while its result distinguishes recursive third-source selection, middle value/formal termination, exact versus yearless identity, and two-slot truncation. ISO/slash list correspondence and external authorability beyond length three remain explicit insufficient information rather than guessed compatibility claims.
- `source`: [direct one-star additional-kind computations](../SOURCES.md#src-first-filled-additional-kind-computations).
- `source`: [direct one-star established-kind computations](../SOURCES.md#src-first-filled-kind-computations).
- `source`: [fragment DateRange computations](../SOURCES.md#src-date-range-fragment-first-filled).
- `source`: [direct-field-list DateRange computation](../SOURCES.md#src-date-range-direct-first-filled).
- `remains`: full `entitySpec` expansion, external calibration beyond three sources, mixed/group/filtered operands, nested repetition, and temporal or DateRange policies beyond the named checked profiles remain under [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) and [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion).
- `remains`: validation-rule use, repeatable or wider-policy temporal application beyond the closed fixed and exact-address Custom, DateFragment, full-Date, Time, DateTime, and DateRange roots, and focused external false/poison/rejection calibration remain under [SG6](../SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) and [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition).

<a id="cap-enumeration-identity-and-categories"></a>
#### Enumeration identity and categories

- `boundary`: Stored-token identity, positional category projection, and checked category validity
- `owner`: [`Enumeration.lean`](../../A12Kernel/Semantics/Enumeration.lean), [`CheckedEnumeration.lean`](../../A12Kernel/Semantics/CheckedEnumeration.lean), [`Enumeration.lean`](../../A12Kernel/Elaboration/Enumeration.lean)
- `assurance`: E/P closed for flat declarations; peer-triangulated; C none
- `remains`: Dynamic/table/host breadth: [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion)

<a id="cap-direct-comparability"></a>
#### Direct comparability

- `boundary`: Separate checked relation for String/Enumeration and Enumeration/category equality/inequality
- `owner`: [`EnumerationComparability.lean`](../../A12Kernel/Elaboration/EnumerationComparability.lean), [`EnumerationValueList.lean`](../../A12Kernel/Semantics/EnumerationValueList.lean)
- `assurance`: E/P closed for admitted families; source-grounded; C none
- `remains`: Wider paths/categories: [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion)

<a id="cap-typed-value-lists"></a>
#### Typed value lists

- `boundary`: Scalar, multi-field, and direct/plain/filtered-star String/Enumeration/Number membership and quantifiers with typed operation-specific payloads. Partial starred entries filter concrete cells before reading and use their own existential covering-identifier extent gate per current iteration row, independent of the aggregate family's universal gate
- `owner`: [`ValueList.lean`](../../A12Kernel/Semantics/ValueList.lean), entity-list/value-list elaboration owners
- `assurance`: E/P closed for named shapes; multiple upstream L locks; C none
- `remains`: Remaining shapes and operators: [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion)

<a id="cap-enumeration-rnu-and-computation"></a>
#### Enumeration RNU and computation

- `boundary`: Admitted Enumeration/category RNU keys plus checked filtered Enumeration target computation. An ordinary stored-direct source that reads its target and every target position in a compatible stored-direct `FirstFilledValue` list of length two or three project `MVK_ERROR_REFERENCE_TO_CALCULATED_FIELD`.
- `boundary`: A compatible category-decorated target source, either target position in a compatible two-category `FirstFilledValue`, and either operand order of a compatible two-entry list whose category-projected entry names the target — beside another projection, a plain read of a different field, or a plain read of the target itself — project the distinct `MVK_ERROR_SEMANTIC_INDEX_OR_CATEGORY_FOR_ERRORFIELD`, because the class follows the **reading mode** of the self-read rather than the authored list shape: one projected self-read pre-empts the plain class. Operand identity carries the reading form, so a stored read and a category projection of one field are two operands that reach that fold, while a same-form repeat stays a repeated-operand shape refusal.
- `boundary`: A plain self-read standing beside a *different* field's category read is outside the measured plain-mode bound and projects no class. Accepted different-field forms and unrelated longer, incompatible-category, star, filter, domain, display, kind, and resolution refusals remain separate and unmapped
- `owner`: [`EnumerationRepetitionNotUnique.lean`](../../A12Kernel/Semantics/EnumerationRepetitionNotUnique.lean), [`EnumerationComputation.lean`](../../A12Kernel/Elaboration/EnumerationComputation.lean), [`EnumerationFirstFilledComputation.lean`](../../A12Kernel/Elaboration/EnumerationFirstFilledComputation.lean)
- `assurance`: E/P closed for named routes and both bounded target-diagnostic classes; clean dmtool `0.13.0` at a12-dmkits [source registry](../SOURCES.md) Kernel-confirms the direct sources, every target position in two/three-field stored-direct lists, the ordinary compatible target-category source, and both positions in a two-category list beside accepted controls; reviewed inbound a12-dmkits [source registry](../SOURCES.md) and [source registry](../SOURCES.md) (`ComputedTargetProjectedReadLawsTest` over the real-kernel consistency oracle) establish the reading-mode fold itself and every two-entry pairing projected here, including the other-field plain entry measured after this project first reached it by composition and the same-field plain-beside-projected pairing that this project had been refusing as a repeated operand.
- `assurance`: Stored-direct controls and the category-list control persisted and read back exactly; the accepted ordinary category control is dry-run calibrated because dmtool `0.13.0` erased its category on converted read-back, so an exact persistence claim for that shape needs a re-measure on a later dmtool; upstream L partial; C none
- `remains`: Wider repeatable/composition, mixed and wider category lists, semantic-index target reads, and lists longer than three: [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion), [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition)

<a id="cap-enumeration-result-and-application"></a>
#### Enumeration result and application

- `boundary`: Checked literal, direct stored/category, and `FirstFilledValue` computations classify their exact token against the immutable source target through the common String-shaped result. The ordinary literal/direct/category surface admits one repeatable target whose scope binds the source: execution walks physical target rows, reads a field source at its own exact path or fans out an enclosing source or literal, retains exact target addresses, and applies only source-relative actions to a separate same-model destination cell-state projection.
- `boundary`: Repeatable `FirstFilledValue` now composes that same target/result boundary with the established Enumeration/category first-filled source surface: each physical target row supplies the outer environment for its own lazy checked-document scan, including exact same- and enclosing-scope direct reads, nested stars, and resolving inner/outer filters; structural addressing failure stays outside token poison. The addressed first-filled source is certified at the target's bound repeatable scope, while the scalar `checkedEnumerationOperand?` wrapper remains restricted to nonrepeatable declarations. Each checked result retains its producing operation so an unrelated target/action pair cannot inhabit the route.
- `boundary`: Static domain compatibility makes target rejection impossible at runtime, while no-value and source poison retain ordinary clearing
- `owner`: [`EnumerationComputationResult.lean`](../../A12Kernel/Elaboration/EnumerationComputationResult.lean), [`AddressedEnumerationComputation.lean`](../../A12Kernel/Elaboration/AddressedEnumerationComputation.lean), [`EnumerationFirstFilledComputation.lean`](../../A12Kernel/Elaboration/EnumerationFirstFilledComputation.lean), [`AddressedEnumerationFirstFilledComputation.lean`](../../A12Kernel/Elaboration/AddressedEnumerationFirstFilledComputation.lean), [root result proofs](../../A12Kernel/Proofs/EnumerationComputationResult.lean), [addressed proofs](../../A12Kernel/Proofs/AddressedEnumerationComputation.lean), [addressed `FirstFilledValue` proofs](../../A12Kernel/Proofs/AddressedEnumerationFirstFilledComputation.lean), [`FirstFilledValue` proofs](../../A12Kernel/Proofs/EnumerationFirstFilledComputation.lean)
- `owner`: [root cases](../../A12Kernel/Conformance/EnumerationComputationResult.lean), [addressed cases](../../A12Kernel/Conformance/AddressedEnumerationComputation.lean), [addressed `FirstFilledValue` cases](../../A12Kernel/Conformance/AddressedEnumerationFirstFilledComputation.lean), and [`FirstFilledValue` cases](../../A12Kernel/Conformance/EnumerationFirstFilledComputation.lean)
- `assurance`: E/P closes root and exact-address changed and unchanged tokens, literal and enclosing-scope fan-out, row-local category projection, same-row and enclosing-scope direct ordered selection, exact two-level target/application addresses, per-parent nested-star selection, empty-prefix fall-through, row-local poison, resolving inner/outer filter selection, residual preservation, exhausted and poisoned clearing, absence-to-present-empty retained clearing, unrelated destination preservation, certified target retention, rejection-channel exclusion, exact `FirstFilledValue` delegation after successful selection, and delegation to the established String application fold.
- `assurance`: The ordinary source semantics are source-grounded, while repeatable placement and every model-indexed result/application composition remain external evidence pending beyond the established bound-scope, ordered-scan, and type-neutral retained-action mechanisms. Q closes the bounded root and exact-cell Execute/Transform decisions; C/X none
- `remains`: Computed-source composition includes the fixed three-stage serial cascade, two-producer ordered `FirstFilledValue` join, computed Number filter dependency, and [combined computed Enumeration plus Number filter route](#cap-computed-enumeration-number-having-join).
- `remains`: Chains beyond three stages, wider sibling-parallel routes, materialized topology, later validation, and wider scheduling remain with [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) and [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion).

<a id="cap-exact-row-enumeration-dependency-cascade"></a>
#### Exact-row Enumeration dependency cascade

- `boundary`: One checked addressed Enumeration producer completes every physical target row before one checked stored or category-projected consumer whose target scope binds the producer source. Same-scope reads stay row-local; an enclosing producer row fans out only to its descendant consumer rows through exact-address transient Enumeration cells. Rich VALUE and clean no-value transport unchanged; producer poison becomes the cause-blind `computedDependency` marker.
- `boundary`: The two phase outcome lists remain distinct from immutable input and their phase-separated public result views. One combined fold applies retained actions to a separately supplied same-model cell-state projection without reconstructing rows.
- `boundary`: The fixed three-stage serial extension certifies three distinct targets, exact adjacent dependencies, and the nonadjacent reverse-edge exclusion. It accumulates the first two exact-address overlays before the terminal read while retaining a complete target/dependency Analyze view and three source-relative result phases. One fold applies the phases to a separate same-model destination.
- `owner`: [`AddressedEnumerationCascade.lean`](../../A12Kernel/Elaboration/AddressedEnumerationCascade.lean), [addressed result owner](../../A12Kernel/Elaboration/AddressedEnumerationComputation.lean), [proofs](../../A12Kernel/Proofs/AddressedEnumerationCascade.lean), and [cases](../../A12Kernel/Conformance/AddressedEnumerationCascade.lean)
- `assurance`: E closes the directional producer edge through stored and non-identity category reads, reverse-edge and unrelated-source refusal, checked source-scope binding, complete producer-before-consumer phases, exact row keys, same-scope and two-level enclosing correlation, stale-input exclusion, VALUE, clean no-value, and cause-blind poison transport.
- `assurance`: Two-stage result projection classifies both outcome lists against immutable source-target state without recomputation. The producer retains its changed value and source-filled clears; the non-identity category consumer retains its computed value without inventing clears from absent source targets.
- `assurance`: Two-stage application folds producer actions before consumer actions through the established String owner. Changed values apply at exact keys, retained producer clears materialize absent states, and inert consumer plus unrelated source cells survive.
- `assurance`: P proves cause-blind poison conversion, distinct two-stage targets, target-rejection exclusion, and application delegation. The route adds no generic schedule, document materialization, or validation. Kernel correspondence remains pending. Q closes the bounded two-stage Execute/Analyze decision; C/X none.
- `assurance`: E closes the fixed three-stage plan's first adjacent missing/reverse edges, terminal missing edge, nonadjacent duplicate/reverse edges, exact Analyze inventory, and same-row VALUE, clean no-value, and transitive cause-blind poison matrix. Contradictory stored target cells at both later edges separate accumulated overlays from stale reads.
- `assurance`: Three-stage result projection retains independent value, changed, cleared, and residual-message partitions against immutable source state. A source-identical terminal success stays inert against a differing destination, while retained changes and clears apply at exact addresses.
- `assurance`: P proves pairwise target distinction, exact two-overlay execution delegation, projection from one execution, target-rejection exclusion in all phases, and application delegation. Kernel correspondence remains pending; no generic scheduler or document state was added.
- `assurance`: Q closes the artifact-only Execute, Analyze, and Transform decision. A fresh restricted reader reconstructed the exact static edges, three-row execution matrix, all three source-relative result partitions, separate residual channels, and named separate-destination application.
- `assurance`: The reader kept phase order limited to delegation because the targets are distinct and refused generic scheduling, wider source shapes, destination reconstruction beyond the named cells, validation, protocol, revision binding, and Kernel correspondence.
- `remains`: The separately owned [two-producer join](#cap-two-producer-enumeration-firstfilledvalue-join), [computed Number filter dependency](#cap-number-dependency-in-enumeration-having), and [computed Enumeration plus Number filter join](#cap-computed-enumeration-number-having-join) cover sibling and mixed filtered compositions.
- `remains`: Chains beyond three stages, more than two sibling producers, and wider computed-source forms remain with [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) and [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion).
- `remains`: Materialized topology, later validation, and wider scheduling remain with [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition).

<a id="cap-two-producer-enumeration-firstfilledvalue-join"></a>
#### Two-producer Enumeration `FirstFilledValue` join

- `boundary`: Two independent checked addressed Enumeration producers complete against one immutable input before one checked repeatable `FirstFilledValue` consumer reads their exact two target fields in authored order. Their completed exact-address cells form one transient overlay. The first computed value stops before a later producer poison, first no-value reaches a non-identity category projection of the second computed value or its cause-blind dependency poison, and double no-value exhausts. The three outcome and public result phases remain distinct, while one fold applies both producer action sets and then the consumer actions to a separately supplied same-model destination without reconstructing rows.
- `owner`: [`AddressedEnumerationFirstFilledJoin.lean`](../../A12Kernel/Elaboration/AddressedEnumerationFirstFilledJoin.lean), [shared dependency projection](../../A12Kernel/Elaboration/AddressedEnumerationCascade.lean), [overlay-aware first-filled evaluation](../../A12Kernel/Elaboration/EnumerationFirstFilledComputation.lean), [proofs](../../A12Kernel/Proofs/AddressedEnumerationFirstFilledJoin.lean), and [cases](../../A12Kernel/Conformance/AddressedEnumerationFirstFilledJoin.lean)
- `assurance`: E closes distinct producer targets, both producer cross-edge refusals, both consumer back-edge refusals, exact two-source identity and order, stale-input exclusion, lazy hiding of an unreached second poison, category projection of a reached computed token, reached cause-blind poison, exhausted no-value, three phase-separated result views, exact retained clearing, and unrelated destination preservation. P retains producer target distinction and exact consumer source order, excludes target-rejection entries from all three views, and proves application delegation. Overlay-bypass and reversed-source-order mutants fail the separating matrix. The route adds no generic scheduler, graph, document materialization, validation, or public protocol. Kernel correspondence remains external evidence pending.
- `assurance`: Q closes the artifact-only Execute/Analyze decision. An isolated reader reconstructed all four row outcomes, runtime reachability, source-relative actions, static dependency limits, and both global-poison and stale-read separators from the language-neutral clauses and capability record while keeping research closure, shipment readiness, Kernel correspondence, and public exposure explicitly open; C/X none.
- `remains`: The separately owned [computed Number filter dependency](#cap-number-dependency-in-enumeration-having) and [computed Enumeration plus Number filter join](#cap-computed-enumeration-number-having-join) cover the filtered sibling compositions after this join.
- `remains`: Chains beyond three stages, more than two sibling producers, wider computed-source forms, materialized topology, later validation, and wider scheduling remain with [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) and [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion).

<a id="cap-number-dependency-in-enumeration-having"></a>
#### Computed Number dependency inside Enumeration `Having`

- `boundary`: One checked addressed Number-result producer completes before one checked repeatable Enumeration `FirstFilledValue` consumer whose exact source is a direct Enumeration field followed by a filtered Enumeration star. The producer may be any existing member of the shared direct, arithmetic, wrapper, String, Enumeration, or DateRange-to-Number row-producer union.
- `boundary`: Analyze retains the producer operation kind, target, ordered source fields, and the consumer's complete checked source inventory. Completed Number outcomes form an exact-address transient overlay, while lazy first-filled and filtered-star traversal still determine which dependency reads are reached.
- `owner`: [`AddressedNumberEnumerationHavingCascade.lean`](../../A12Kernel/Elaboration/AddressedNumberEnumerationHavingCascade.lean), [shared producer union](../../A12Kernel/Elaboration/RepeatableNumberAggregateProducer.lean), [shared Number dependency overlay](../../A12Kernel/Elaboration/AddressedNumberField.lean), [Enumeration source inventory and traversal](../../A12Kernel/Elaboration/EnumerationFirstFilledComputation.lean), [model-certified Enumeration result and application](../../A12Kernel/Elaboration/AddressedEnumerationFirstFilledComputation.lean), [proofs](../../A12Kernel/Proofs/AddressedNumberEnumerationHavingCascade.lean), and [cases](../../A12Kernel/Conformance/AddressedNumberEnumerationHavingCascade.lean)
- `assurance`: E closes exact source-shape and filter-edge certification, complete Analyze inventories, fresh row-local Number values over reversed stale seeds, a poisoned producer hidden by the deciding direct token, and the same cause-blind poison when the filtered star is reached. The two already-sourced phases project through their existing family result owners and apply independently to a compatible destination. The matrix retains fresh Number VALUE/CLEARED partitions and Enumeration VALUE partitions, verifies source-relative changes, and shows that source-identical success stays inert against different destination state. The prior Number aggregate consumer delegates to the same exact-address overlay, giving that extraction two completed semantic users. The overlay-bypass mutant fails both fresh-state and reached-poison separators.
- `assurance`: P retains the exact filter-only producer edge, exact Analyze projection, two-phase execution delegation, and delegation of result projection to the completed Number and model-certified Enumeration family owners. Existing maintained evidence establishes computed state inside aggregate `Having` and the component Enumeration traversal, but not this exact cross-family composition, so Kernel correspondence remains `external evidence pending`; mixed-document reconstruction, later validation, public protocol, and generic scheduling are excluded.
- `assurance`: The wider producer certificate is generic over the existing union. Its String `Length` separator retains UTF-16 operation identity and the String source edge, replaces reversed stale Number seeds, converts reached malformed String state to cause-blind dependency poison, and lets the deciding direct Enumeration prefix hide that same completed poison. Other union members inherit the checked dispatch and exact projection but remain compositionally uncalibrated.
- `assurance`: Q closes the artifact-only Execute/Analyze decision. A restricted independent reader reconstructed all four Number observations, both lazy Scenario M Target values, the reached cause-blind poison control, exact static inventories, runtime reachability, and the global-pre-skip and stale-cell separators. It correctly left result/application, later validation, Kernel correspondence, protocol, research closure, and shipment readiness insufficient or excluded; C/X none.
- `assurance`: Q also closes the artifact-only result/application decision. A fresh restricted reader reconstructed the exact Number and Enumeration successful, changed, cleared, error, and message partitions, applied both family results independently to a different compatible destination, and identified source-identical Number and Enumeration successes as two separators against destination-relative reclassification. It correctly refused a merged cross-family document, action interleaving order, materialized topology, later validation, protocol, research closure, shipment readiness, and exact Kernel correspondence.
- `assurance`: Q closes Execute, Analyze, and the independent Transform boundary for the String `Length` representative. A fresh artifact-only reader reconstructed UTF-16 operation identity, ordered edges, fresh exact-address overlay values over reversed stale seeds, reached cause-blind poison, hidden completed poison, and separate family result phases.
- `assurance`: The reader left destination compatibility, Target delta classification, a merged document, action interleaving, other producer-union arms, wider shapes, validation, protocol, version binding, shipment, and Kernel correspondence insufficient or excluded.
- `remains`: The [combined computed Enumeration value plus Number filter route](#cap-computed-enumeration-number-having-join) closes the exact three-phase successor including its result/application boundary. Producer kinds beyond the existing addressed Number-result union, wider source shapes, mixed-document reconstruction, later validation, and wider scheduling remain with [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) and [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion).

<a id="cap-computed-enumeration-number-having-join"></a>
#### Computed Enumeration value plus Number `Having` join

- `boundary`: One checked repeatable direct Number producer and one independent direct-field Enumeration producer complete against the same immutable input before one repeatable Enumeration `FirstFilledValue` consumer. Its source is one direct Enumeration field followed by one filtered star whose selected field is the completed Enumeration target and whose `Having` inventory contains the completed Number target. The two exact-address overlays combine only for that lazy consumer read.
- `boundary`: One execution projects all three source-relative result phases without recomputation and retains separate Number, Enumeration-producer, and Enumeration-consumer message channels. Number applies independently to a same-model destination. The two Enumeration phases fold in producer-then-consumer order without defining a mixed cross-family document.
- `boundary`: The serial successor instead completes the direct Enumeration producer first, converts that exact produced target through selected-projection `FieldValueAsNumber`, and exposes both overlays to the same lazy consumer. Its Analyze view retains the conversion projection and all three ordered target/source inventories. One execution projects the three source-relative phases without recomputation; Number applies independently and the two Enumeration phases fold in producer-then-consumer order against a separately supplied same-model destination.
- `owner`: [`AddressedEnumerationNumberHavingJoin.lean`](../../A12Kernel/Elaboration/AddressedEnumerationNumberHavingJoin.lean), [`AddressedFieldValueAsNumber.lean`](../../A12Kernel/Elaboration/AddressedFieldValueAsNumber.lean), [shared Enumeration overlay](../../A12Kernel/Elaboration/AddressedEnumerationCascade.lean), [shared Number overlay and producer](../../A12Kernel/Elaboration/AddressedNumberField.lean), and [filtered source-shape owner](../../A12Kernel/Elaboration/AddressedNumberEnumerationHavingCascade.lean)
- `owner`: [proofs](../../A12Kernel/Proofs/AddressedEnumerationNumberHavingJoin.lean) and [cases](../../A12Kernel/Conformance/AddressedEnumerationNumberHavingJoin.lean)
- `assurance`: E closes direct Enumeration producer admission, reverse Enumeration edge refusal, exact selected-value and filter edges, complete three-target Analyze inventories, and five outer discriminator rows. A deciding direct value hides both producer poisons; fresh Number and Enumeration values override reversed stale seeds; a false fresh filter hides a selected Enumeration poison; and either reached poison crosses the consumer boundary as cause-blind `computedDependency`.
- `assurance`: The Enumeration-overlay bypass mutant fails the fresh-value and reached-poison separators. The same five outer rows plus one second inner address retain exact Number and both Enumeration successful, changed, and cleared partitions, including distinct producer poison and clean no-value clears. The two Enumeration residual-message channels remain separate and target errors remain excluded.
- `assurance`: Independent application preserves source-identical Number and Enumeration successes against a different destination, applies retained Number changes and clears, and folds both Enumeration action sets without reconstructing a mixed document. A consumer/producer projection-confusion mutant fails these partitions and destination controls.
- `assurance`: P retains the direct Enumeration source certificate, selected-value edge, Number-filter edge, exact Analyze projection, and execution delegation. Result projection observes the three already-executed child views, both Enumeration target-error partitions are universally empty, and Enumeration application delegates in phase order. This is an internally checked Execute/Analyze/Transform composition only. Kernel correspondence remains `external evidence pending`; C/X none.
- `assurance`: Q closes the artifact-only Execute/Analyze decision. A clean isolated reader constructed the same five mutually incompatible row classes with one nested item each, recovered fresh overlay reads, short-circuiting, both reached poison paths, the complete static computed-edge set, and narrower runtime reachability.
- `assurance`: The Execute/Analyze reader left raw formal-code ingestion, serialized inventory order, result/application, document reconstruction, later validation, generic scheduling, public protocol, revision binding, and Kernel correspondence insufficient or excluded.
- `assurance`: Q also closes the artifact-only Transform decision. A second clean isolated reader reconstructed the three independent source-relative partitions, separate Enumeration residual channels, independent Number application, the ordered Enumeration fold, and three source-identical successes against differing destination state.
- `assurance`: The Transform reader treated raw diagnostic construction as insufficient and refused public collection order, a merged cross-family document, topology, validation, protocol, generic scheduling, revision binding, and Kernel correspondence.
- `assurance`: E closes the serial successor's exact Enumeration-to-Number source edge, reverse-edge refusal, selected-value and Number-filter edges, conversion identity, and three-row matrix. Fresh category conversion and selected Enumeration values replace both stale targets, reached poison crosses both boundaries cause-blind, and the direct prefix hides both completed poisons. The same rows retain every produced value, source-relative change, and source-filled clear across all three phases, retain separate Enumeration residual channels, and apply the Number phase independently plus both Enumeration phases in order. A source-identical terminal success stays inert against a conflicting destination value.
- `assurance`: P retains the exact Enumeration-to-Number edge, complete Analyze view, and delegation through Enumeration projection, caller-read Number conversion, and the combined final overlay. Result projection observes the three already-executed child views, both Enumeration target-error partitions are universally empty, and Enumeration application delegates in phase order. This is an internally checked Execute/Analyze/Transform composition only. Kernel schedule correspondence remains `external evidence pending`; C/X none.
- `remains`: Chains beyond three stages, wider producers and source shapes, other computed filter types, materialized document topology, later validation, and wider scheduling remain with [SG4](../SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) and [SG8](../SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion).
