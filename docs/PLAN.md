# Active implementation plan

This is the volatile checkpoint for resuming current work. Stable purpose, method, and coverage horizons belong in [`PROJECT-DESIGN.md`](PROJECT-DESIGN.md); exact implementation and proof coverage in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md); retained correspondence in [`EVIDENCE.md`](EVIDENCE.md); and durable design conclusions in [`ARCHITECTURE.md`](ARCHITECTURE.md) and [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md). Git and those owners retain completed history.

## Verified checkpoint

The repository has executable Lean semantics for admitted flat validation, one-group captured-outer correlation, and a narrow non-repeatable String-computation family, with checked lowering, payoff-selected laws and counterexamples, compact retained kernel observations, and versioned public/candidate process surfaces. Reference semantics 0.3.0 and the V2 suites are current; 0.2.0/V1 and its historical Rust experiment are archived. A fresh current-version Rust runtime probe is accepted at the narrower boundary owned by the [`correlation kit`](IMPLEMENTER-KIT-CORRELATION.md#fresh-rust-runtime-consumer-probe).

The infrastructure simplification is complete and settled. Three compact evidence bundles remain; retired raw readers, packet binders, generators, qualification campaigns, and capture V1 machinery stay retired. [`SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md`](SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md) owns the scaling boundary, and the [simplification archive](archived/REFERENCE-AND-EVIDENCE-SIMPLIFICATION-PROPOSAL.md) owns measurements and recovery history.

Direct computation presence, ordered left-to-right `And`/`Or`, and operation-neutral first-match alternatives are internally complete over the admitted direct-presence leaves in one resolved checked context. The branch laws and declaration-order counterexamples preserve clean not-true/no-match versus poison. Selection returns an operation payload before that operation is evaluated, so no operation result feeds back into the scan; a conformance separator instantiates the payload with a String expression without putting target, policy, or prior-target state inside each alternative. A shared checked computation-table representation, model-level alternative legality, selected-operation integration, and implicit generated validation remain separate. Local portable computation-control evidence remains pending.

The first partial-validation capsule is internally complete for one explicit nonrepeatable error field and the existing flat condition fragment. It keeps the error-field gate separate from out-of-set reference masking, preserves `skipped` versus evaluated verdicts, reuses one connective evaluator, locks the relevant-instance content-gate override, and proves one-sided firing soundness for content-bearing completions agreeing on relevant fields. Group/global/wildcard relevance, repeats, aggregates, uniqueness, autogenerated checks, integration with the separately closed resolved whole-rule message boundary, and orchestration remain open. Local portable partial-validation evidence remains pending.

Direct nonrepeatable Number field-to-literal `<` and `>=` are internally complete through the existing flat comparison and checked-lowering path. The same scale-19 fixed-right comparator now covers all four admitted Number operations; executable separators preserve empty unsigned/signed polarity, filled controls, malformed suppression, and the independent empty-row gate. The proof spine states the universal empty-Number `<` and `>=` polarity laws. The public flat JSON operation remains deliberately narrower at equality/inequality, and project-local portable direct-ordering evidence remains pending.

The first pure decimal-rounding capsule is internally complete. Authored places are statically bounded to `0..14`, omission is zero, expression and field-value forms share scale-19 `HALF_UP` pre-rounding followed by mathematical `FLOOR`, `CEILING`, or `HALF_UP`, and the requested places determine static result scale. Universal bounds use the pre-rounded operand; checked counterexamples block both false raw-input bounds. Numeric unknown causes and directional fillability survive rounding unchanged. No expression AST, public protocol, target application, or evidence machinery was added, and project-local portable rounding evidence remains pending.

The first significant-digit arithmetic capsule is internally complete for pure `+`, `−`, `×`, `÷`, and `^` nodes. Exact rational binary arithmetic is followed immediately by precision-50 decimal `HALF_UP`, including signed target scales for values above and below one; division checks zero first and returns explicit `notEvaluated` rather than exposing Lean's total rational zero. Power pins Java's staged X3.274 positive algorithm, the kernel's reciprocal-first negative order, integral `-1000..1000`, `0^0`, and domain invalidity. Universal surviving arithmetic/domain/staging laws and checked 51/52-digit, tiny-tie, quotient, non-associativity, staged-power, reciprocal-order, and domain-edge cases prevent exact-, fixed-scale-, final-round-only, rational-division-by-zero, or reciprocal-after-power substitutes. The kernel audit corrected the former spec and a12-dmkits arithmetic account; accepted revision `71775c9905b057831253348c31ce39e321e61889` now implements and locks reciprocal-first negative power and the staged precision matrix. No checked power consumer, public protocol, target application, or project-local portable arithmetic evidence exists yet. The generic pure evaluator's fractional-exponent branch is defensive: kernel authoring rejects that shape, current Lean checked consumers reject all power, and the authored summary records the scale rule for a future checked power consumer.

The resolved arithmetic-fillability capsule is internally complete for known operands of `+`, `−`, `×`, `÷`, and power. One shared pure seam carries `canGrow`/`canShrink`, derives current signs from the actual rational operands, preserves multiplication's joint-direction terms, transforms a nonzero divisor's directions as a reciprocal before reusing the same product rule, and couples staged power values to the kernel's conservative fixedness/base-region/parity table. `NumericArithmeticOutcome` keeps the rounded amount and fillability inseparable and makes `notEvaluated` absorb either child before an enclosing arithmetic node runs; direct division remains the value algorithm, while power's existing staged value evaluator is the sole domain gate. Negative power transforms the precision-50 reciprocal base and swaps exponent directions before reusing the nonnegative table. Universal fixedness, sign, zero-divisor, absorption, power-domain-coherence, zero-exponent, and zero-base separation laws plus the complete branch/polarity matrix guard the account. Checked power consumers, result-empty provenance, protocol, target projection, and retained project-local arithmetic-fillability evidence remain open; no new result type, expression AST, support/evidence mechanism, framework, or dependency was added.

The computation audit corrected one unsupported clean-empty claim. A pinned a12-dmkits public-API probe originally exposed only fresh no-field-output and stale CLEARED, while kernel source established invalid target and dependent-poison behavior. Accepted [`SPEC-2026-07-19-12`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-12--division-by-zero-computations-poison-despite-a-cleared-delta) at revision `71775c9905b057831253348c31ce39e321e61889` now locks domain preservation through numeric `Abs`/`Min`/`Max`/rounding, Number-target invalidity and dependent poison, the distinct valueless Date-shift projection, and lowered-tree order through both kernel routes plus peer triangulation. This repository still retains no portable observation for that family. The pure arithmetic result remains consumer-neutral, while the first Number-computation consumer maps it into an explicit pre-target `value | domainFailure | poison` result. Empty and required-only Number read as zero; rounding and clean enclosing admitted arithmetic preserve domain failure, while a later reached invalid read can still poison; the complete lowered tree rejects non-Number declarations and unaudited power before reads; and distinct poison causes lock authored-versus-lowered order. Lean does not yet model the `Abs`/`Min`/`Max` wrappers or the Date-shift consumer projection. Trusted laws and executable separators close the existing expression boundary; the target, exact application, and cause-free dependency observation are now closed narrowly by the following paragraphs, while downstream context/read integration and portable confirmation remain open. The expression capsule adds 426 nonblank Lean lines, above the default 250-line decomposition signal: the executable outcome/declaration/read-order mechanism, its universal proof spine, and its separating conformance matrix are already split into their three owning modules, and dropping one would leave this same public boundary semantically unsound or unclosed; it adds no support or evidence code.

The first numeric target delivery is internally complete for one nonrepeatable, already-resolved, ordinary scale-compatible target with signedness and proved-consistent minimum/maximum fractional digits. Review decomposed it into two semantic responsibilities. The 235-nonblank-line stored-decimal capsule owns `{unscaled, scale}`, normalized dot-decimal rendering, scale-19 `HALF_UP` conversion, and universal amount coherence across zero stripping, minimum-scale padding, and both signs. The 315-line target capsule starts after separate static assignment-scale admission, checks the universal 15-digit limit before signedness, fails closed on the warning-suppressed no-fit branch, preserves five distinct target outcomes, and projects exact-form-sensitive change delta. Cases distinguish pre-round-before-fit routing, the 15/16-digit boundary, digit-before-signedness precedence, signed-negative and unsigned-nonnegative acceptance, fresh/equal/changed prior targets, rendering branches, `7` versus `7.00`, division-domain invalidity, exact-cause poison, and no-fit refusal. [`SPEC-2026-07-19-15`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-15--computed-number-storage-has-distinct-fit-and-warning-suppressed-no-fit-branches) corrects the former one-path spec summary. Exact application and cause-free dependency observation are now closed by the next two paragraphs; checked target authoring, downstream context/read integration, extra Number constraints, the suppressed no-fit implementation, and portable local target evidence remain open. The complete integrated delivery adds 596 gross and 586 net nonblank Lean lines, including the extracted operation-neutral result type, shared rounding coefficient seam, root imports, and theorem registry. Stored conversion is below the default 250-line signal; target classification/delta is 65 lines above it but remains one closed outcome boundary, while application and dependency behavior are excluded. It adds no support or evidence code. At that checkpoint, the canonical all-in count including IO/test drivers was 9,624 theory lines, 5,449 support lines, and 1,528 evidence lines.

Exact numeric target application is internally complete for one already-addressed cell. The 206-nonblank-line Number-specific capsule distinguishes absent, present-empty, and exact `presentValue {unscaled, scale}`; accepted output yields that exact final value state, while clean no-result, rejection, calculation invalidity, and inherited poison all empty a present cell in place and preserve absence. An independently enumerated relation agrees exactly with the executable transition and proves determinism, exact stored-value agreement, and presence preservation. Three non-laws show why the layers remain separate: delta prior loses placement; equal final empty states can retain CLEARED-versus-ERRORED delta; and even equal final state plus equal delta cannot identify clean no-result versus calculation invalidity. Typed-equal accepted output is extensionally represented without claiming the kernel physically rewrites it or preserves a modeled raw lexical form. The capsule adds 218 gross/net nonblank Lean lines including roots and theorem registry, stays below the 250-line signal, and adds no support or evidence code. The current canonical boundary is 9,832 theory lines, 5,449 support lines (55.4% of theory), and 1,528 evidence lines (15.5%); Number-specific application remains `external evidence pending`.

The clean-clear-versus-poison dependency spike is internally complete as one pre-consumer projection, not a cascade evaluator. `NumericTargetOutcome` now projects to exactly `empty | value StoredNumber | poisoned`: clean no-result becomes empty, accepted output retains its exact stored decimal, and rejection, calculation invalidity, and inherited invalidity become cause-free poison because the downstream cache gate exposes invalid membership rather than producer details. The integrated runtime witness evaluates the source-established, already-resolved `RoundAccounting([Source] / 0, 2)` shape to domain failure, classifies target invalidity, and projects poison; checked wrapper/target authoring remains outside the claim. Universal laws prove the empty/value characterizations and that application plus delta cannot recover dependency meaning; stale CLEARED/present-empty and fresh silent/absent cases provide independent separators. The capsule is 147 nonblank semantics/proof/conformance lines and 155 gross/net Lean additions including roots and trust registrations, below the 250-line signal. The current canonical boundary is 9,981 theory lines, 5,449 support lines (54.6% of theory), and 1,528 evidence lines (15.3%); no support/evidence code was added, and the numeric dependency family remains `external evidence pending`.

An artifact-only evaluator consumer probe then tested whether this numeric chain actually transports knowledge. An isolated reader reconstructed the one-pass lowering, structural preflight, ordered reads and failure precedence, target storage/fit, delta, exact application, and dependency projection—including the clean-empty-versus-poison separator—without kernel or a12-dmkits research. It also correctly refused an end-to-end or shipment-ready claim: checked target authoring, assignment-scale admission, downstream context injection and scheduling, portable numeric evidence, and a capability/protocol boundary remain open. This establishes a useful distinction for future probes: an internally understandable semantic dependency cut can be complete while shipment research closure is not. No external implementation, protocol, qualification, or harness was added.

The first checked resolved whole-rule capsule is internally complete for one nonrepeatable flat rule over the existing checked condition fragment. The condition verdict is evaluated once; not-fired and unknown remain distinct silent outcomes; only a firing attaches the explicit nonrepeatable error address with an empty repetition path, error code, severity, verdict-derived VALUE/OMISSION type, and opaque already-resolved text. Checked assembly reuses `CheckedFlatCondition`, resolves a unique nonrepeatable referenced error field in the same model, and derives checked cells from raw input through that model. `hasContent` remains an authoritative caller input; this capsule does not derive row content or topology. Laws prove exact verdict/message transport, metadata independence, ERROR-only invalidation, checked address coherence, and the silence non-law; cases separate empty/fill polarity, false/malformed silence, row gating, exact code/severity transport, explicit non-first error field, text opacity, and all assembly rejections. The generated-validation audit caught and corrected the initially omitted error-code channel before that consumer was implemented. Rule path, referenced-field metadata, and fill-to-fix metadata remain explicit exclusions. The two semantic responsibilities—resolved emission and checked model assembly—occupy 329 nonblank lines across their four owning semantics/elaboration/proof/conformance modules. This exceeds the default 250-line decomposition signal but is already honestly split; compressing the laws/cases or creating more files solely for the metric would weaken readability without reducing responsibility. Authored templates/interpolation, repeatable addresses, row-content derivation, partial-rule integration, rule lists/order, generated checks, orchestration, and portable whole-rule evidence remain open.

The first generated-computation validation risk capsule is internally complete for exactly two guarded decoded numeric literals and one nonrepeatable Number target. It reuses the closed first-match selector for computation but structurally lowers the same guard syntax to ordinary validation conditions, retaining both guarded strict mismatches under `FieldFilled(target)`. The central case selects literal `1` and still fires against stored `1` because a later simultaneously holding literal `2` disagrees; equal-result, nonholding-later, `And`-translation, empty-target, phase-divergence, omission-polarity, swapped-result, and fail-closed repeatable-field controls isolate the mechanism. Authored literal scales remain present until the unsuppressed exact-comparison gate, guard/target IDs are checked against one validated model, unsupported String presence and non-Number targets fail closed, and the existing whole-rule boundary supplies the target address, computation name as code, ERROR severity, resolved text, and derived polarity. Trusted laws cover first-row selection, the exact retained two-branch shape, and empty-target suppression for either authoritative target-instance content bit. The three owning modules contain 358 nonblank lines—120 checked desugaring, 41 proofs, and 197 branch-sensitive conformance—plus 11 net nonblank lines for the reused checked-condition seam, roots, and trust registry. This exceeds the default 250-line decomposition signal, but the implementation is already split by semantic responsibility; shortening the independent scale, repeatability, phase, polarity, connective-translation, and overlap guards would hide an unprotected branch rather than simplify a mechanism. The capsule adds no new evaluator, verdict, message, table framework, scheduler, protocol, evidence, or dependency. The current repository ratio is 10,711 executable-theory lines to the unchanged 5,449 support lines; evidence remains 1,528 lines, so support is 50.9% and evidence 14.3% of theory. General computation authoring—including target self-reference—remains an explicit input assumption; warning-suppressed assignment legality, runtime target checks, singleton/default and larger tables, common preconditions, tolerance, expressions, no-value/domain outcomes, repeatable evaluation, anchoring, application, and portable evidence remain open. The source mechanism is confirmed; project-local correspondence is `external evidence pending`.

The first post-simplification cross-category consumer probe is complete over that capsule. An isolated evaluator reader reconstructed the selector, exact generated shape, key outcomes, exclusions, and error precedence without external A12 research, but could not build a standalone exact evaluator without chasing imported Lean definitions for model validation, phase observation, numeric scale admission, flat verdict/polarity, and rule assembly. An isolated refactoring reader recovered the unsafe shadowed-row deletion, overlap reordering, phase-sensitive guard rewrite, and fixed-severity/flexible-polarity boundaries, then correctly refused a releasable-preservation claim. The remaining task-profile checks found that a JSON importer has no justified mapping until a pinned source construct and satisfaction relation exist, that a table-consistency analysis must distinguish benign overlap from overlapping different results, and that this direct-presence query should begin solver-free rather than motivate an SMT dependency. [`USE-CASES.md`](USE-CASES.md#current-probe-record-generated-computation-alternatives) records all ten category outcomes. No implementation, protocol, suite, solver, evidence route, qualification campaign, or harness was added.

The cross-level correlation risk spike is internally complete as a source-grounded representation correction. The shared filter core now carries complete candidate and captured repetition environments, structural repetition references retain their named level, and exact lookup fails closed on missing or duplicate bindings; the checked and public one-group surfaces remain adapters over that core. One caller-established raw topology reopens a parent candidate level while retaining parent and descendant coordinates in the captured environment. A diagonal capture `{parent = 1, child = 1}` selects parent candidate `1`; an off-diagonal capture `{parent = 1, child = 2}` selects none, while deliberately collapsing both coordinates to one scalar row changes truth. Trusted laws establish shared evaluator/relation agreement, captured-reference stability, exact two-level resolution, and the impossibility of one shared row when the captured coordinates differ. A coordinate-collapse mutation fails the focused conformance module. The 285-net-nonblank-line Lean delivery consists of 149 lines in its semantics/proof/conformance capsule and 136 lines migrating the established core, one-group adapters, elaboration locks, roots, and trust registry: 280 executable-theory lines plus five trust-registry lines. It exceeds the default 250-line review signal because this is a one-time representation correction rather than an isolated new clause; review removed a duplicated cross-level consumer/footprint layer and an unused topology predicate, then found the remaining responsibilities cleanly split. The canonical boundary is now 10,991 executable-theory lines against unchanged 5,449 support lines (49.6% of theory) and 1,528 evidence lines (13.9%). Checked nested hierarchy/row instantiation, concrete cross-level lowering, multiple stars, joins, general consumers, computation, protocol exposure, and project-local portable evidence remain open. Accepted [`SPEC-2026-07-19-16`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-16---correlation-captures-every-named-outer-repetition-level) records the diagonal/off-diagonal lock at revision `71775c9905b057831253348c31ce39e321e61889`; this repository still retains no portable observation. No harness, protocol, support, evidence, or dependency code was added here.

The second post-simplification cross-category consumer probe is complete over the checked nonrepeatable whole-rule/message capsule and raw cross-level captured-environment slice. Across three isolated readers, the probe reconstructed both procedures, the scalar-collapse separator, the whole-rule metadata/validity distinction, exact exclusions, and the missing composition boundary without sibling, web, history, or conversational context. The evaluator and transformation probes correctly refused to combine cross-level row selection with whole-rule message emission: the former ends in ordered rows under three-valued filter truth, while the latter starts from a nonrepeatable polarity-bearing condition verdict. Translate likewise refused to invent a JSON Schema mapping without source semantics, and Analyze recovered two useful solver-free questions. [`USE-CASES.md`](USE-CASES.md#current-probe-record-whole-rule-messages-and-cross-level-capture) records all ten category outcomes. No implementation, protocol, suite, solver, evidence route, qualification campaign, or harness was added.

The historical Rust repository is not the base for another implementation. A future Rust experiment starts from a fresh repository and current capability material; the retired 0.2.0/V1 source and fixtures remain an immutable control consulted only after the new natural result is frozen. Cross-level correlation and the second consumer-probe pass are now complete, but the probe confirms that the combined capability is not yet internally closed and that either separate slice still lacks a sufficiently self-contained language-neutral dependency projection. Once one exact task reaches that boundary, the user chooses its capability and timing, starts or authorizes the isolated downstream session from this project's prepared prompt and allowlist, and ferries its report back. Until that owner gate, no Rust repository, shipment machinery, protocol expansion, dependency, or qualification campaign is created.

The fixed numeric-tolerance capsule is internally complete through the checked same-group, nonrepeatable plain Number-expression leaf. One closed ordinary-or-tolerance dispatch reuses the existing model/path/authoring/one-pass-lowering/read/failure/row-gate pipeline; ordinary equality/inequality retain their exact-scale gate, ordering remains scale-exempt, and every fixed tolerance range bypasses the gate before delegating two values to the independently normalized strict-band seam. Universal scale-bypass and atom/literal delegation laws plus checked scale, strict-boundary, one-pass-lowering, directional-polarity, malformed/domain, row-gate, and fail-closed separators guard the integration. The audit corrected the spec's former equality-like claim; accepted [`SPEC-2026-07-19-13`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-13--tolerance-normalizes-operands-first-and-uses-directional-inequality-polarity) records the completed a12-dmkits operand-normalization, directional-polarity, metadata, and separating-matrix work at revision `7b39728eafdb53b0a69fcafd34817980bb900632`. Concrete syntax, `BaseYear`, implicit validation in Lean, public exposure, and project-local portable tolerance evidence remain open.

The numeric static-summary capsule is internally complete before expression integration. `ScaleInfo` now represents an exact signed scale or unknown, while `NumericScaleSummary` separately carries constant expandability and exhaustively summarizes `+`, `−`, `×`, `÷`, explicit rounding, exponent-scale acceptance, the narrow scale-zero power case, and the unsuppressed `==`/`!=` gate. Proofs establish summary and gate symmetry, smaller-side characterization, the field-only specialization, same-scale acceptance, and unknown rejection; separators retain negative stripped-literal scale, false global literal exemption, capability AND/OR propagation, and unknown-with-capability independence. The current flat `Rat` literal has erased authored scale and therefore remains intentionally disconnected. Accepted [`SPEC-2026-07-19-09`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-09--numeric-scale-gating-tracks-signed-scale-and-constant-expandability) records the peer correction at revision `71775c9905b057831253348c31ce39e321e61889`; no duplicate ledger entry or new harness was added.

The first structured numeric-expression capsule is internally complete for generic already-resolved atoms. Its authored tree preserves decoded literal scale and braces, computes the static summary before lowering, and remains distinct from the evaluation tree. One exact order-sensitive post-order pass rewrites immediate divided multiplication factors, keeps ordinary factors before extracted numerators, preserves encounter order, and does not revisit newly created products; the lowered evaluator reuses precision-50 arithmetic, staged power, and bounded two-stage rounding. Structural equations and executable factor-order/no-second-pass/boundary cases lock the algorithm; `3 * {1 / 3}` proves rounded-value non-preservation, with a `10³¹` amplifier retaining the difference through scale-19 comparison. The source audit corrected `spec/04` and extended existing [`SPEC-2026-07-19-08`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-08--every-arithmetic-node-uses-precision-50-half_up); no new ledger row or harness was added.

The plain numeric-authoring capsule is internally complete over the same generic tree. One structural scan enforces the exact open division contribution across multiplication/division regions, resets contribution at grouping/addition/subtraction/power, checks only a direct-left ungrouped nested power, retains simultaneous violations, and makes parser-shape reachability explicit. Any rounding wrapper returns `outsideFragment`: source shows that the kernel's legacy walk through operation-valued functions is ordered and non-compositional, and no focused observation closes it. Trusted laws and executable separators cover each boundary. The canonical spec and accepted [`SPEC-2026-07-19-14`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-19-14--numeric-authoring-regions-are-structural-and-function-wrappers-remain-unclosed) record the exact rule, grouping-preserving peer rendering, and the still-fail-closed wrapper boundary at revision `71775c9905b057831253348c31ce39e321e61889`. Lean retains the structured tree but has no concrete arithmetic renderer. No harness was added.

The checked numeric-expression consumer is internally complete for one full-validation, same-group, nonrepeatable ordinary comparison leaf. It accepts two plain `+`/`−`/`×`/`÷` expressions with at least one Number field across them and all six direct comparison operators; checks both sides for paths, kinds, group, authoring legality, and authored scale before lowering each exactly once; then reuses validation-phase empty/formal reads, arithmetic value/fillability, independent scale-19 normalization, two-sided directional polarity, and the row gate. Formal invalidity remains `unknown`; pure arithmetic domain failure becomes `notFired`; mixed formal/domain precedence is an explicit conservative Lean refinement excluded from the kernel-correspondence claim. Proofs cover fixed-right specialization, equality/inequality symmetry, right-direction polarity, both checked evaluations being reachable, direct atom/literal agreement with the reduced flat evaluator, and empty-row suppression. The mutation-sensitive two-sided/operator/rejection matrix remains `external evidence pending`, including same-field aliasing; no spec correction, peer-sync row, protocol, evidence, target, harness, or governance mechanism was added.

The first Date baseline is internally complete at the post-parse value boundary. `DateParts`, proof-carrying `CivilDate`, and floor-admitted `FullDate` prevent decoded components, Gregorian calendar reality, and the universal 1583-10-16 A12 value floor from being conflated. Partial construction checks an unbounded positive-era Gregorian account before the floor; 1899 remains admitted because the optional 1900 field policy is deliberately separate, while exact parser reachability is not claimed. Strict `before` is lexicographic over admitted components; verdict and polarity wait for a later operand layer that can represent empty, formal-invalid, and not-given provenance. Trusted laws expose reality, characterize construction, establish total chronology and the inclusive-floor interpretation, and connect the executable comparison to that order; cases lock every ordinary month length, century leaps, impossible dates, positive-era bounds, exact floor boundaries, year/month/day crossings, and the componentwise-order non-law. The three owning modules contain exactly 250 nonblank Lean lines—100 semantics, 92 proofs, and 58 conformance—plus three root imports and the trust-registry entries; no parser, cell/value integration, DateTime, zone, protocol, evidence, dependency, or harness was added. The canonical boundary is now 11,244 executable-theory lines against unchanged 5,449 support lines (48.5% of theory) and 1,528 evidence lines (13.6%). Pinned source and a12-dmkits differential tests ground the reachable account, but there is no project-local portable Date observation, so correspondence remains `external evidence pending`.

The selected UTC DateTime baseline is internally complete after decoded full-Date and time admission and after authored numeric-to-hour conversion. `TimeOfDay` carries exact whole-second component bounds, `LocalDateTime` retains the wall label, and the shared `Instant` used by `World.now` carries scalar epoch-second identity. UTC resolution is executable and deterministic; the whole-hour shift core changes only the instant and is total even below the later value floor. Trusted laws characterize both constructors, bound seconds since midnight, expose the UTC and same-date-difference equations, and prove zero/composition/inverse shift algebra. Cases lock the Unix origin, exact floor reuse, invalid dates/times, 30/31-day, ordinary leap, non-leap century, leap century, year, and negative rollover, UTC autumn controls, the same-date-wrap non-law, and below-floor shifting. A wrong-unit mutation failed every shift/carry/floor control. Global ordinal injectivity/monotonicity/successor laws, decimal offset truncation and runtime bounds, cells/polarity/operator gates, rendering/result admission, and non-UTC behavior remain open. The three owning modules contain 238 nonblank Lean lines; including the three root imports and the one shared-`Instant` refactor, the delivery adds 242 net executable-theory lines and no support/evidence code. The canonical boundary is now 11,486 executable-theory lines against unchanged 5,449 support lines (47.4% of theory) and 1,528 evidence lines (13.3%). Pinned source and a12-dmkits triangulation ground the reduced account, but no project-local portable DateTime observation exists, so correspondence remains `external evidence pending`.

The selected Berlin autumn-overlap risk spike is internally complete without a timezone framework. `BerlinAutumn2024.resolveLocal?` accepts only 2024-10-27, resolves before 02:00 with the daylight offset and from 02:00 with the standard offset, and returns `none` for every other date. Exact cases lock stored `01:30`, the daylight-side first `02:30`, fresh standard `02:30`, the first-shift inequality, the second-shift equality, the branch boundary, and closed adjacent/spring/other-year inputs. Universal laws characterize the exact domain and both offset branches. Moving the daylight branch through the ambiguous hour failed exactly the fresh-standard, first-shift, second-shift, and 02:00 controls. The slice adds 100 nonblank executable-theory lines—20 semantics, 23 proofs, and 57 conformance—plus three entries in the existing trust registry; it adds no support, evidence, protocol, dependency, or framework code. The canonical boundary is now 11,586 executable-theory lines against unchanged 5,449 support lines (47.0% of theory) and 1,528 evidence lines (13.2%). Existing real-kernel probes directly establish fresh ambiguous `02:30` as standard and the first-shift distinction; the second-shift equality is source-derived and internally checked but remains direct external evidence pending. A future general Berlin resolver replaces this fixed slice rather than retaining parallel legacy behavior.

The first structured message-rendering capsule is internally complete at its deliberately low boundary. `MessageRenderPlan` performs one ordered pass over already-decoded text, resolved display-name input, and resolved display-value input; replacement bytes are opaque, provider/model/debug priority is explicit, and missing or empty display uses the exact format-supplied default rather than a universal Number formatter. Universal laws cover every provider/model/debug branch, missing/empty defaults, append composition, and nonempty replacement opacity; cases separate scale-zero and scale-two Number defaults, empty String, provider/model/debug outcomes, decoded-dollar text, token-looking replacement data, and repeated raw CRLF. Removing the explicit-empty fallback broke both the Number case and its universal law before restoration. The slice adds 158 nonblank semantics/proof/conformance lines plus six entries in the existing trust registry, with no new module, support, evidence, protocol, dependency, parser, or formatting framework. The canonical boundary is now 11,718 executable-theory lines against unchanged 5,449 support lines (46.5% of theory) and 1,528 evidence lines (13.0%). Source audit exposes three canonical refinements—the exact provider/model-label/debug cascade, format-specific rather than universally literal `0` Number defaults, and a display-data CRLF path distinct from evaluation-cache normalization. The pure renderer is source-grounded and internally closed, but checked invocation only after firing, raw-template legality, those canonical corrections, and the CRLF discriminator remain open; no complete §13 or kernel-correspondence claim is made.

The civil-coordinate bridge is internally complete for the selected positive-era Gregorian account. `CivilDate.next?` stays on the checked constructor boundary and is proved total through its mapped coordinate: every real date advances exactly one day across ordinary, month-end, leap, non-leap-century, and year-end branches. Strict civil chronology raises the same coordinate, and strict `LocalDateTime` wall-label chronology is preserved by UTC resolution. A month-end mutation failed the expected leap, century, month-end, and coordinate cases before restoration. The capsule adds 398 net nonblank executable-theory lines—22 semantics, 325 proofs, and 51 conformance—plus three trust-registry entries. The proof size exceeds the 250-line review signal because it closes checked successor construction, fold-derived month-prefix arithmetic, year-prefix arithmetic, same-year order, and the UTC lift in one proof-owning module; independent review replaced finite month-pair enumeration with one recurrence and one monotonicity induction, consolidated the successor branches, and reduced the proof from its first 352-line working form without adding a second mechanism. The canonical boundary is 12,116 executable-theory lines against unchanged 5,449 support lines (45.0% of theory) and 1,528 evidence lines (12.6%). Parser reachability, authored DateTime operators, zones, cells, result admission, protocol exposure, and portable correspondence remain open.

The first value-list quantifier capsule is internally complete at the already-expanded, already-filtered runtime boundary. A kind index admits only Number or canonical stored-token atoms and makes mixed runtime lists unrepresentable; per-cell present/empty/UNKNOWN classification remains separate from uninstantiated-tail and `Having` metadata. Separate `AtLeastOne`, `No`, and `NotAll` evaluators preserve their distinct precedence and polarity clauses: `No` is poisoned by UNKNOWN on either side before membership, while `NotAll` first requires a present field, ignores field-side UNKNOWN, and is poisoned only by an UNKNOWN member. Trusted laws cover exact scale-19 numeric membership, empty-member non-substitution, exact UNKNOWN boundaries, field-UNKNOWN invariance, and `Having` escalation. Cases retain the shared-input `No = unknown` / `NotAll = fired VALUE` non-law, no-present-before-member-poison precedence, empty-versus-zero, tail and filter polarity, and can-fire-on-empty classification. A global field-UNKNOWN poison mutation failed exactly the central and mixed-sibling `NotAll` controls before restoration. The three owning modules add 314 nonblank executable-theory lines—123 semantics, 83 proofs, and 108 conformance—plus three root imports and ten trust-registry entries; no support, evidence, protocol, dependency, path, expansion, filtering, or generic quantifier framework was added. This exceeds the default 250-line review signal, but it is already split into one semantic owner, one proof owner, and one conformance owner; the three operation-specific clauses and branch-sensitive proof/conformance guards form the indivisible closed boundary, so compression or another file split would not reduce responsibility. The canonical boundary is now 12,430 executable-theory lines against unchanged 5,449 support lines (43.8% of theory) and 1,528 evidence lines (12.3%). Checked comparability/lowering, star/path expansion, actual `Having` execution, row-gate integration, scalar membership, enumeration domains/text/categories, protocol exposure, and project-local portable evidence remain open.

The first resolved `RepetitionNotUnique` capsule is internally complete over a caller-supplied selected scope and topology with a common key schema and already-classified validation-phase key cells after path expansion. It defines branch-independent duplicate membership over explicit ordered rows identified by complete repetition environments; the direct evaluator recomputes that extensional cluster per target rather than adding a cache. It excludes keys containing formal UNKNOWN, skips all-empty keys, retains optional empties as matching tuple components, and reuses scale-19 Number equality. Whole-scope evaluation returns complete peer clusters in its deterministic scope order, which is not a future pointer-order claim. Per-row projection fires VALUE for fully present duplicates and OMISSION for duplicates containing a valid empty; ordinary verdict composition happens afterward, so a guard-false peer still changes another row's duplicate status. Lean maps an invalid current key to `unknown` and a clean unique/all-empty row to `notFired`; pinned source establishes that the pre-collapse invalid key is UNKNOWN, but kernel 30.8.1 merges both outcomes into `FALSE_OR_UNKNOWN`, so the finer Lean distinction is explicitly internal and external evidence can establish only authored-message suppression. Trusted laws characterize exact membership, refined UNKNOWN/all-empty/firing boundaries, complete cluster exposure, unique complete identities, and the existence of a distinct same-key peer under the resolved scope obligations. Cases separate nested rows with equal local indices, composite keys, invalid exclusion, typed equality, branch independence, polarity, and the collapsed external view. Against the final matrix, changing optional-empty equality to false failed exactly the optional-empty duplicate and invalid-peer controls before restoration. The three owning modules add 434 nonblank executable-theory lines—113 semantics, 204 proofs, and 117 conformance—plus three root imports and eight trust-registry entries. Independent review removed a redundant verdict-algebra case, merged the complete-cluster and nested-identity separators, and simplified the distinct-peer conclusion through the existing membership theorem. The result remains above the default 250-line review signal because the semantic relation, universal proof spine, and branch-sensitive separating matrix are distinct responsibilities; further compression or a generic uniqueness layer would reduce clarity rather than mechanism count. The measured canonical boundary is 12,868 executable-theory lines against unchanged 5,449 support lines (42.3% of theory) and 1,528 evidence lines (11.9%). Checked default/explicit `@From`, topology and key-schema construction, partial relevance, one-RNU and negative/iteration/filter/parallel authoring gates, peer-pointer/message projection, whole-rule integration, protocol support, and project-local portable RNU evidence remain open. The capsule adds no support, evidence, protocol, dependency, cache, or uniqueness framework.

The resolved String-alternative integration is internally complete over an already-model-legal guarded table, one shared nonrepeatable target policy, and one shared prior target. Its 40-line semantics composes the existing operation-neutral selector exactly once with the existing String expression/target/delta step; no outcome can reopen the remaining alternatives. Trusted laws prove selected delegation, holding-head suffix irrelevance, terminal selected no-value, and final delta projection. Cases separate clean preselection fallthrough and nonempty clean exhaustion from the kernel-specific terminal cases: selected clean no-value clears rather than using a later literal, target rejection reports rather than retrying, and both selection-side and expression-side poison stop the scan. An outcome-driven-fallback mutation failed exactly the central no-value separator before restoration. The three owning modules add 220 nonblank executable-theory lines—40 semantics, 76 proofs, and 104 conformance—plus three root imports and seven trust-registry entries. The measured canonical boundary is 13,091 executable-theory lines against unchanged 5,449 support lines (41.6% of theory) and 1,528 evidence lines (11.7%). Checked authoring legality, common-precondition lowering, comparisons/quantifiers in guards, repeatable evaluation, several computations per target, scheduling, generated validation beyond the separate Number fragment, protocol support, and project-local portable evidence remain open. The capsule adds no support, evidence, protocol, harness, governance, dependency, or table framework.

The resolved sub-day DateTime-difference capsule is internally complete after both operands have become exact instants. A closed three-unit enum supplies only positive divisors, and `Instant.difference` computes `(second − first).tdiv unitSeconds`, preserving authored order and truncating negative fractions toward zero. Trusted laws cover positive divisors, self-zero, swap/negation, exact seconds, exact-unit recovery, and agreement with whole-hour shifting. Cases lock `5 h 30 m 15 s` in all three units, both reverse fractional separators, the resolver-composed three-hour Berlin fall-back span, fresh `01:30 → 02:30 = 120` minutes, the 60-minute chained-versus-fresh ambiguous-hour gap, and truncation non-additivity. Replacing `tdiv` with floor division failed exactly the two negative fractional cases before restoration. The three owning modules add 147 nonblank executable-theory lines—26 semantics, 52 proofs, and 69 conformance—plus three root imports and six trust-registry entries. The measured canonical boundary is 13,241 executable-theory lines against unchanged 5,449 support lines (41.2% of theory) and 1,528 evidence lines (11.5%). Parsing/zone resolution, empty/formal operands and polarity, static operator gates, numeric result admission/consumption, `DifferenceInDays` and calendar differences, other DateTime arithmetic, checked lowering, protocol support, and project-local portable evidence remain open. The capsule adds no support, evidence, protocol, harness, governance, dependency, or temporal framework.

The resolved three-part Date reason-and-verdict capsule is internally complete after component availability and all-present calendar reality have been supplied. One reason-bearing result retains real `DateParts`, incomplete, present-but-unreal, and formally unavailable outcomes; formal unavailability dominates emptiness, and the 1583 stored/computed-value floor remains downstream. Trusted laws characterize incomplete/UNKNOWN precedence, exact retention of supplied reality, both fired polarities, exact truth complementation, and the central full-verdict non-law: `Valid` forgets whether a non-value was incomplete or unreal, while `Invalid` preserves OMISSION versus VALUE. Cases lock supplied real/unreal outcomes, each empty position, all-empty, unreal hidden by emptiness, and unknown-plus-empty precedence. Collapsing incomplete polarity to VALUE and checking emptiness before formal unavailability each failed the intended laws/cases before restoration. The three owning modules add 201 nonblank executable-theory lines—55 semantics, 82 proofs, and 64 conformance—plus three root imports and eleven trust-registry entries. The measured canonical boundary is 13,442 executable-theory lines against unchanged 5,449 support lines (40.5% of theory) and 1,528 evidence lines (11.4%). Exact causes/cells, numeric component lowering and authoring bounds, concrete model-zone/cutover calendar resolution, two-/four-part forms and Base Year, row orchestration, stored target admission, checked lowering, protocol support, and project-local portable evidence remain open. The first reviewed design was narrowed before landing when independent review demonstrated that zone-free proleptic resolution disagrees with the kernel's zone-aware hybrid calendar; no callable false resolver remains. Accepted [`SPEC-2026-07-20-06`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-06--constructed-date-validity-complements-only-at-truth-projection) and [`SPEC-2026-07-20-07`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-07--constructed-date-consumers-retain-reason-and-legacy-calendar-identity) record a12-dmkits alignment at revision `71775c9905b057831253348c31ce39e321e61889`. Compositional temporal reason state, date differences, computation consumption, and the concrete legacy-calendar resolver remain open. The capsule adds no support, evidence, protocol, harness, governance, dependency, or Date/calendar framework.

A resolved Number `FirstFilledValue` capsule is internally complete for one operand after ordered expansion, filtering, partial-relevance classification, and tail derivation. Its dedicated scan stops at the first present or unavailable cell; an empty prefix is sticky, while every suffix after selection is unread. The result retains amount plus not-given classification or exact internal unavailability cause, then projects separately to validation fillability and computation value/poison. Explicit-empty, marked-uninstantiated, and filtered-empty Number selections yield fillable zero, and `Having` escalates even a selected value. Trusted laws establish both terminal heads, empty-prefix stickiness/idempotence, all three empty routes, both projections, and the central non-law that computation forgets an empty prefix while validation distinguishes it. Making an immediate invalid suffix poison, discarding the empty-prefix marker, replacing empty-selection zero with unavailable, and dropping `Having` only at exhausted selection each failed the predicted laws/cases before restoration. Independent review exposed and closed the input-boundary risk: the one global filter bit belongs to the sole admitted operand because a later authored operand's filter is not encountered after an earlier value terminates the kernel scan. Multiple operand slots therefore remain excluded rather than being flattened unsoundly. Checked authored lowering must mark a no-row star as uninstantiated; the total unmarked empty side remains fixed zero and is explicitly locked but not claimed authored-reachable. The three owning modules add 249 nonblank executable-theory lines—39 semantics, 106 proofs, and 104 conformance—plus three root imports and ten trust-registry entries, staying one line below the default 250-line review signal. The measured canonical boundary is 13,693 executable-theory lines against unchanged 5,449 support lines (39.8% of theory) and 1,528 evidence lines (11.2%). Exact cause carriage is an internal refinement; correspondence for the observable order, value, polarity, and poison boundaries remains `external evidence pending`. Non-Number kinds, multiple operand slots, paths/stars, actual `Having`, checked authoring/lowering, row orchestration, target application, protocol support, and local portable evidence remain open. Accepted [`SPEC-2026-07-20-09`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-09--firstfilledvalue-observes-only-filters-before-termination) records per-slot filter encounter and unreachable-suffix behavior at revision `71775c9905b057831253348c31ce39e321e61889`. The capsule adds no support, evidence, protocol, harness, governance, dependency, or aggregate framework.

The bounded artifact-only `FirstFilledValue` consumer probe is complete. An isolated evaluator/refactoring reader reconstructed the exact five-branch scan/projection decision table without kernel or a12-dmkits research, identified terminal-suffix removal and repeated-empty-prefix collapse as resolved-boundary optimizations, and correctly rejected reordering, eager whole-list poisoning, filter removal, no-row-marker deletion, and computation-only equivalence as full-observation rule rewrites. The scan itself is unambiguous; authored lowering, paths/stars, filter execution, partial relevance, portable task types/fixtures, a public operation, external evidence, and a source-level preservation relation remain the shipment and integration gaps. [`USE-CASES.md`](USE-CASES.md#current-probe-record-resolved-firstfilledvalue) owns the user-facing result. No protocol, runner, external implementation, qualification, or harness was added.

The resolved Date-range overlap family is internally complete from admitted full-Date endpoints through both ordered operator shapes. The first capsule keeps inversion explicit, defines closed symmetric overlap, and preserves duplicate occurrences in flat any-pair truth. The consuming capsule retains operand boundaries as ordered skipped/kept slots with filter presence, then implements two deliberately separate scans: any-pair uses sticky reached-filter provenance updated before the current overlap check, while scalar-versus-list exits for a skipped scalar and uses only the first matched list operand's filter. Trusted laws cover the primitive relation, occurrence behavior, non-UNKNOWN results, skipped-filter inertness, smallest-shape delegation, first-match behavior, both polarity mechanisms, and two truth-forgetting bridges: any-pair firing is equivalent to the flat occurrence-preserving relation, while scalar-versus-list firing is equivalent to one flattened primitive match. The general any-pair bridge intentionally pays for the opposite prefix-versus-suffix scan directions because it gives interpreter and refactoring consumers a universal truth-preservation boundary without adding runtime machinery. Cases lock touching/adjacency, inversion, later internal and duplicate pairs, filtered skipped input, disjoint reached filters, a filtered current match, unreachable later filters, reordering-sensitive polarity, skipped/disjoint scalars, and matched-container polarity. Seven mechanism mutations—primitive inversion removal, half-open endpoints, scalar-vs-list substitution for flat truth, loss of sticky any-pair state, skipped-scalar internal-pair rescue, cumulative scalar-list filter taint, and updating the any-pair filter marker after rather than before the overlap check—failed the predicted proofs or cases before restoration. The operator trio contains 459 nonblank lines—79 semantics, 261 proofs, and 119 conformance—and therefore exceeds the default 250-line review signal. The 261-line proof portion is the largest responsibility because the universal consumer-facing bridge needs a seen/tail invariant plus symmetry; extracting or compressing it solely for the metric would not reduce the responsibility. The capsule's exact subject remains the non-interchangeability of two source-distinct scans sharing one operand model, while a generic scan abstraction would erase that semantic difference. The measured canonical boundary is 14,331 executable-theory lines against unchanged 5,449 support lines (38.0% of theory) and 1,528 evidence lines (10.7%). Raw cell classification, actual filter evaluation, paths/stars, `$`, row gates, checked lowering, messages, protocol support, and project-local portable evidence remain open. [`LF49`](LEAN-FINDINGS.md#lf49--date-range-overlap-is-closed-occurrence-based-and-shape-sensitive) owns the boundary, and [`SPEC-2026-07-20-10`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-10--date-range-overlap-polarity-follows-the-reached-scan) owns the canonical correction and peer reconciliation. The family adds no support, evidence, protocol, harness, governance, dependency, or overlap framework.

The matching artifact-only consumer probe is complete. An isolated interpreter/refactoring reader reconstructed both resolved scans and their first-match polarity rules without external A12 research, then correctly separated flat truth-preserving transformations from full-verdict-preserving transformations. It identified skipped-slot inertness and the proved truth projections, while rejecting deduplication, endpoint normalization, half-open conversion, shape conflation, and filter-sensitive reordering. [`USE-CASES.md`](USE-CASES.md#current-probe-record-resolved-date-range-overlap) owns the user-facing result. Authored lowering and shipment remain open; no external implementation or infrastructure was added.

A resolved computation field-fill quantifier capsule is internally complete for all seven predicates over one caller-supplied, already-expanded slot stream. `ComputationFillSlot` preserves instantiated filled, instantiated empty, declared-but-uninstantiated absence, and exact-cause poison; the evaluator uses separate first-filled, first-declared-empty, and second-filled scans rather than a count or global invalidity pass. `AllFieldsFilled`/`NotAllFieldsFilled` use the declared range, the four ordinary count predicates use instantiated slots, and the two composite predicates restart their source-level second stage only after finding a filled witness. Trusted laws establish universal poison-head preservation, representative deciding-prefix suffix equations, the range fork, zero/one/two classification, mixed-range success, selected reached-poison separators, and order observability. Cases lock all seven full-decision-before-poison controls, exact internal predecision causes, the uninstantiated matrix, both mixed-order separators, and the threshold cases. Five mutations failed in their predicted neighborhoods: eager suffix poisoning (eight cases), dropping declared omissions (three), stopping at the first fill (four), collapsing `NotExactlyOne` to `MoreThanOne` (two), and collapsing the mixed predicate to `NotAll` (one). Independent semantic and Lean-design audits approved the four-state boundary; the canonical spec now qualifies validation's neither-bucket rule and states the exact computation scan table under the accepted successor [`SPEC-2026-07-20-12`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-12--computation-fill-quantifiers-retain-their-exact-staged-scans). The three owning modules contain 276 nonblank Lean lines, 26 above the default review signal; the excess is the explicit seven-arm decision surface plus its payoff proof/case spine, already split by semantics/proofs/conformance, and no generic framework or compression would reduce the responsibility. The measured canonical boundary is 14,609 executable-theory lines against unchanged 5,449 support lines (37.3% of theory) and 1,528 evidence lines (10.5%). The isolated consumer readback recovered an exact bounded evaluator and unsafe rewrite boundaries without external A12 research; [`USE-CASES.md`](USE-CASES.md#current-probe-record-computation-field-fill-quantifiers) owns that result. Authored expansion and stream construction, checked-cell classification, AST/connective integration, validation polarity, protocol support, and project-local portable evidence remain open. The capsule adds no support, evidence, protocol, harness, governance mechanism, dependency, or quantifier framework.

The resolved validation field-fill companion is internally complete for the same seven operator names over one caller-supplied unfiltered tally. The phase evaluators share only the closed operator vocabulary: validation retains instantiated filled, instantiated empty, instantiated unavailable, and declared-but-uninstantiated counts, then returns fired VALUE/OMISSION or the exact kernel-visible collapsed `FALSE_OR_UNKNOWN`; computation remains the separate ordered poison-preserving scan. The seven clauses preserve the declared/instantiated/mixed range fork, known witnesses despite unavailable siblings, the zero-versus-at-least-two polarity change of `NotExactlyOneFieldFilled`, and the all-empty separator between `NotAllFieldsFilled` and `FieldsNotCollectivelyFilled`. Trusted laws add universal clean/unknown and instantiated-range invariance results plus the phase non-law. Twelve mutations were killed at the unknown, range, composite, polarity, and decisive-witness boundaries. Independent semantic and Lean reviews found and removed one reverse import from validation to computation, then approved the corrected phase split and strengthened locks. The three validation modules contain 257 nonblank lines—58 semantics, 130 proofs, and 69 conformance—seven above the default review signal. The excess is the payoff-bearing universal range/unknown proof spine added after review; compressing it for the metric would weaken the exact second-consumer boundary. The 15-line shared enum replaces the former computation-local vocabulary instead of creating an evaluator framework. The canonical executable-theory estate grows by 265 counted lines to 14,874 against unchanged 5,449 support lines (36.6% of theory) and 1,528 evidence lines (10.3%). An isolated consumer recovered the complete tally evaluator, validation-only permutation/count saturation, unsafe negative-operator collapses, and the prohibition on reusing validation's extensional account for poison-sensitive computation without external A12 research; [`USE-CASES.md`](USE-CASES.md#current-probe-record-validation-field-fill-quantifiers) owns the readback. [`LF51`](LEAN-FINDINGS.md#lf51--validation-field-fill-quantifiers-are-extensional-and-observably-collapsed) and accepted [`SPEC-2026-07-20-13`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-13--validation-fill-quantifiers-expose-an-extensional-collapsed-result) own the durable account. Authored field/group/star expansion, checked tally construction, `Having`, row eligibility, partial validation, connective integration, physical read traces, hidden false/unknown refinement, protocol support, and project-local portable evidence remain open. The capsule adds no support, evidence, protocol, harness, governance mechanism, dependency, or generic quantifier framework.

The follow-up constructed-Date numeric capsule is internally complete for direct day/month/year extraction from the reason-bearing construction result. Its cause-free result retains `value amount notGiven | unavailable`: real parts are fixed, incomplete is symmetric not-given zero, unreal is fixed zero, and formal unavailability remains UNKNOWN without a fabricated `FormalCause`. Trusted laws establish exact selection, same-zero/distinct-provenance, universal unavailable suppression, and VALUE/OMISSION separation for every true fixed-literal comparison; executable cases additionally lock one false comparison, equality polarity, `< 1`, and `>= 0`, distinguishing symmetric Date missingness from unsigned grow-only fillability and preventing the fixed-literal consumer from ignoring truth. Five manual mechanism mutations—misrouting day to month, making incomplete fixed, making unreal fillable, turning unavailable into zero, and bypassing comparison truth—failed their predicted proof/case neighborhoods before exact restoration. The three owning modules contain 161 nonblank Lean lines—47 semantics, 48 proofs, and 66 conformance—plus three root imports and five trust-registry entries, below the 250-line review signal. The measured canonical boundary is 15,037 executable-theory lines against unchanged 5,449 support lines (36.2% of theory) and 1,528 evidence lines (10.2%). Correspondence remains `external evidence pending`; the capsule adds no support, evidence, protocol, harness, governance mechanism, dependency, or calendar framework.

A subsequent `DifferenceInDays` risk spike was removed before landing. Kernel source counts signed model-zone `Calendar.DAY_OF_MONTH` steps; a proleptic wall-second quotient agrees on the existing named probes but fails both on a Berlin special-hour landing and across a skipped whole date. The Berlin and `Pacific/Apia` separators make the representation choice material, so no Lean evaluator or reusable wall-day coordinate remains. Accepted [`SPEC-2026-07-20-08`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-20-08--differenceindays-counts-model-zone-calendar-steps) records both kernel-route separators and peer calendar-step reconciliation at revision `71775c9905b057831253348c31ce39e321e61889`; [`LF47`](LEAN-FINDINGS.md#lf47--differenceindays-needs-a-model-zone-calendar-step-account) owns the remaining Lean consequence. Resume only with a profile-aware calendar-step account or a proved and enforced narrower domain; do not repeat the zone-free spike.

The project now owns [`../spec/`](../spec/) as its language-neutral semantic bridge. [`A12-DMKITS-SPEC-SYNC-LEDGER.md`](A12-DMKITS-SPEC-SYNC-LEDGER.md) is the compact reconciliation ledger. Reviewed a12-dmkits revision `71775c9905b057831253348c31ce39e321e61889` closes the prior outbound queue through `SPEC-2026-07-20-11`; later entries retain their own independent receipt histories.

## Current spiral

The project now follows the controlled-interleaving and risk-first method in [`PROJECT-DESIGN.md`](PROJECT-DESIGN.md#controlled-interleaving-and-coverage-horizons). This is interleaving between completed vertical capsules, not parallel partial implementation, and baseline/risk pairs are a default heuristic rather than a quota. Reuse an existing baseline, keep a selected pair together unless blocked, and reassess frontier order after the pair. Every retained semantic unit closes and commits independently.

1. **Fresh Rust consumer probe — Phase 1 accepted, then stop:** the fresh dependency-free implementation recovered the current resolved `single-group-correlation-v2` runtime from the language-neutral kit and twelve retained fixtures without Lean, kernel, a12-dmkits, old Rust, or other semantic research. The [`correlation kit`](IMPLEMENTER-KIT-CORRELATION.md#fresh-rust-runtime-consumer-probe) owns the accepted result and exact limits. Do not begin optional JSON/lowering/candidate-runner Phase 2 by momentum: it is separate protocol/qualification work, may require a Rust JSON dependency, and needs an explicit owner decision. Return now to A12 semantics.
2. **§8 value-list quantifiers — resolved runtime closed:** the separate Number/canonical-token runtime, payoff laws, and branch-sensitive cases are complete. [`LF44`](LEAN-FINDINGS.md#lf44--no-and-notall-value-list-quantifiers-are-not-duals) owns the durable asymmetry; [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) owns the exact unsupported authoring, expansion, filter, comparability, and evidence boundaries. Do not deepen §8 by momentum; select the next capsule under the controlled-interleaving rule.
3. **§9 repetition uniqueness — resolved duplicate relation closed:** the direct two-phase `RepetitionNotUnique` account, proof spine, and branch-sensitive cases are complete. [`LF45`](LEAN-FINDINGS.md#lf45--repetition-uniqueness-builds-one-branch-independent-duplicate-relation-before-verdict-composition) owns the relation, invalid-key refinement, and evidence limit. Do not add checked scope/topology, pointer projection, a cache, or a uniqueness framework by momentum; select the next ready capsule after closing this unit.
4. **§11 String alternatives — resolved operation integration closed:** one guarded table now selects exactly once and delegates to the existing String target step. [`LF22`](LEAN-FINDINGS.md#lf22--computation-selection-is-first-match-but-implicit-validation-is-all-alternatives) owns the terminal no-value/rejection/poison rule and its distinction from all-alternatives generated validation. Do not add a general table framework, scheduler, or protocol by momentum.
5. **Date and DateTime sequence — construction verdicts and sub-day difference closed:** the pure full-Date boundary, resolved three-part construction classification with `Valid`/`Invalid` verdicts after an externally supplied calendar decision, direct day/month/year numeric projection with incomplete symmetric not-given zero versus unreal fixed zero, whole-second UTC local-label/instant/whole-hour-shift baseline, calendar-coordinate successor/strict-monotonicity/UTC-order bridge, fixed Berlin 2024 autumn-overlap discriminator, and exact resolved `DifferenceInHours/Minutes/Seconds` core are closed. Numeric component authoring/lowering and computation consumption, exact model-zone/cutover calendar construction, two-/four-part forms and Base Year, general model-zone dispatch, spring-gap formal errors, other transition years/history, operand/result integration, `DifferenceInDays`, calendar differences, and other authored operators remain later separate capsules; replace the fixed overlap slice when the general resolver lands. A future Date/DateTime consumer may read this theory, but executable qualification waits for a deliberately selected task boundary rather than a new ad hoc process operation.
6. **§8/§11 Number `FirstFilledValue` — one resolved operand scan and consumer probe closed:** the dedicated prefix-sensitive runtime, payoff laws, validation/computation separator, mutation-sensitive cases, and artifact-only evaluator/refactoring readback are complete. [`LF48`](LEAN-FINDINGS.md#lf48--firstfilledvalue-is-a-prefix-sensitive-scan-not-a-list-fold) owns the semantic boundary; [`USE-CASES.md`](USE-CASES.md#current-probe-record-resolved-firstfilledvalue) owns the probe result. No shipment or external implementation follows by momentum.
7. **§6 Date-range overlap — resolved truth, shape, and polarity closed:** the closed primitive, occurrence-preserving truth scan, ordered skipped/kept operand model, and both source-distinct consuming scans are complete. Do not continue DateRange breadth by momentum and do not factor the two scans through a generic overlap fold. Rotate to the next non-DateRange risk frontier; raw cells, actual filters, paths/stars, checked authoring, messages, protocol, and evidence remain separate future integration work.
8. **§1/§3/§11 computation field-fill quantifiers — resolved scan and consumer probe closed:** all seven predicates now preserve the declared/instantiated fork, staged composite decisions, exact internal reached poison, and suffixes unread after the full predicate's final decision over a caller-supplied resolved slot stream. [`LF50`](LEAN-FINDINGS.md#lf50--computation-field-fill-quantifiers-are-ordered-phase-specific-scans) owns the durable boundary. Do not add path/group/star/filter expansion, AST integration, protocol support, or a generic quantifier framework by momentum.
9. **Message and whole-rule language boundary — risk checkpoint parked cleanly:** the structured one-pass renderer is closed over already-resolved display inputs. Do not start parser-independent authored whole-rule/model lowering or a syntax-shape discriminator until the provider/default/CRLF canonical refinements are authorized in `spec/` and the raw-CRLF message discriminator is externally checked; neither unknown may be hidden in lowering. Checked post-fire integration should then reuse the closed condition and resolved-message boundaries rather than introduce another evaluator. Do not build a general parser, generic lowering framework, publication surface, or custom-oracle runtime.
10. **§1/§3 validation field-fill quantifiers — resolved phase companion closed:** the exact unfiltered tally, collapsed result, seven polarities, declared/instantiated/mixed ranges, unavailable-as-neither treatment, phase separator, mutation locks, canonical clarification, and bounded consumer readback are complete. The shared enum is vocabulary only; validation remains extensional and computation ordered. [`LF51`](LEAN-FINDINGS.md#lf51--validation-field-fill-quantifiers-are-extensional-and-observably-collapsed) owns the boundary. Do not add authored expansion, AST integration, path/group/filter machinery, row gates, protocol support, or a generic quantifier framework by momentum.
11. **§6 constructed-Date direct numeric components — resolved validation consumer closed:** the closed day/month/year selector consumes the existing reason-bearing construction result, reads supplied real parts without re-running calendar resolution, maps incomplete and unreal to equal numeric zero with symmetric not-given versus fixed provenance, and leaves formal unavailability cause-free and UNKNOWN. Universal laws prove exact selection, equal-amount/distinct-provenance, all-operator fixed-right true-comparison polarity, and unavailable suppression; executable cases lock real selection, every non-value state, a false comparison, equality polarity, and both breaking directions. Computation consumption, exact causes, DateTime/reason composition, differences, calendar identity, authored lowering, protocol, and project-local portable evidence remain open. No support, evidence, protocol, harness, governance, dependency, or calendar framework was added.

12. **§5 resolved power fillability — pure risk capsule closed:** `NumericArithmeticOutcome.power` now reuses the staged value/domain evaluator and implements the complete conservative nonnegative direction table plus reciprocal/base-direction and exponent-direction transformations for negative powers. Laws lock child absorption, exact domain agreement with `powerNumeric`, rejected exponents, fixed zero exponent, and the equal-amount zero-base direction separator; branch-sensitive cases and seven mutations cover the full table and its comparison consequence. The three existing owning modules add 187 nonblank executable-theory lines—81 semantics, 34 proofs, and 72 conformance—plus six trust-registry lines, below the 250-line review signal. The canonical boundary is now 15,230 executable-theory lines against unchanged 5,449 support lines (35.8% of theory) and 1,528 evidence lines (10.0%). [`LF52`](LEAN-FINDINGS.md#lf52--power-fillability-is-a-conservative-branch-table-not-parity-inference) owns the durable account. Do not widen into checked authored validation/computation, result-empty provenance, target projection, protocol, or evidence machinery by momentum.

13. **§10 resolved literal-key semantic-index value-read risk — closed:** one already-checked nonempty literal and normalized canonical-token index column now contains only unique clean entries plus an unavailable-key marker from the preceding mandatory/uniqueness/formal-check stage. Validation finds a clean match before consulting that marker; computation consults it before any match. Clean no-match equals matched empty, selected invalid targets retain the ordinary phase split, unselected invalid targets are irrelevant, and required key versus required target stays distinct. The shared direct-Number projection preserves signedness-aware no-match polarity; the canonical no-match/matched-empty `== 0` cases directly lock OMISSION. Eight mutations and trusted gate/locality/empty/polarity laws close the 246-net-nonblank-line unit: 228 lines in the standard semantics/proof/conformance trio, a seven-line net semantic reuse refactor plus three root imports and eight trust registrations. The canonical boundary is now 15,476 executable-theory lines against unchanged 5,449 support lines (35.2% of theory) and 1,528 evidence lines (9.9%). [`LF53`](LEAN-FINDINGS.md#lf53--semantic-index-lookup-has-match-first-validation-and-column-first-computation) owns the durable account. Do not add key checking, presence/fill consumers, paths, field-keyed lookup, nested repeats, a cache, checked lowering, protocol, or evidence machinery by momentum.

14. **§8 resolved Enumeration category baseline — closed:** one already-observed ordinary closed Enumeration now compares either its exact stored token or one selected positional category token with a pre-admitted nonempty literal. The category retains parallel declaration-order vectors; lookup advances them in lockstep, and duplicate category tokens remain legal many-to-one mappings. Empty is not evaluated under either equality operator, formal unavailability remains UNKNOWN, wrong kinds and incoherent projection input fail closed, and every clean firing is VALUE rather than OMISSION. Exact stored projection, positional lookup/skip, many-to-one equivalence, empty/unavailable behavior, and VALUE-only firing are trusted laws; cases separate head and non-head vector positions, equality from inequality, wrong kind, and a defensive incomplete mapping. Two explicitly out-of-domain total-runtime controls distinguish stored from display and category tokens without claiming authored correspondence. Ten mechanism mutations failed at their predicted cases before byte-exact restoration. Extracting the pre-existing 31-line scalar equality seam removes 25 lines from flat validation; the complete Lean change adds 247 net nonblank lines, below the 250-line review signal, while the repository ratio count grows by 237 lines because it excludes the umbrella library and trust-report files. The count-method boundary is now 15,698 executable-theory lines against unchanged 5,449 support lines (34.7% of theory) and 1,528 evidence lines (9.7%). [`LF54`](LEAN-FINDINGS.md#lf54--enumeration-categories-are-positional-runtime-projections-not-display-lookups) owns the durable account. Correspondence remains `external evidence pending`: the maintained focused matrix has the Groovy-dynamic kernel anchor plus peer triangulation, while generated-Java currently contributes only a broader catalog smoke case and this repository retains no portable §8 observation. Do not add authoring checks, display-label transport, field-to-field comparability, open/dynamic enums, value-list/category integration, paths, checked lowering, protocol, or evidence machinery by momentum.

15. **Next risk checkpoint — §8 Enumeration field comparability and display identity:** use the closed runtime baseline to audit the separate static rule for direct Enumeration/String and Enumeration/Enumeration field comparison. First settle the source-level distinction between no display labels, labels identical to stored tokens, and genuinely different display labels; state the smallest checked surface and rejection classes before writing Lean. If existing source and maintained authoring locks cannot decide the identical-label edge, park it with one exact discriminator and rotate to the next evidence-ready non-Date frontier. Do not add locale/display transport, a parser, runtime comparison duplication, open/dynamic enums, value lists, paths, protocol, or evidence machinery.

Before writing Lean for an ambiguous risk, require enough source or retained-observation support to choose the account. If the discriminator is unavailable, record the competing accounts, smallest required observation, and downstream consequence once, add no code, and continue with the next ready frontier. If two consecutive risks park this way, choose one evidence-ready ordinary capsule or propose one batched evidence request; any new harness or governance mechanism still requires an explanation of need, gap, payoff, cost, and alternatives plus explicit approval.

Consumer probes are capability-specific, may fail usefully, and never imply support from overall Lean coverage or implementation language. A named task is internally usable only when every required semantic branch is closed or rejected; shipment-ready only when the task profile is research-closed with its decision procedure or preservation relation, examples, evidence limits, laws/non-laws, support declaration, and applicable task interface aligned; and independently qualified only after a cold consumer completes the task without kernel, a12-dmkits, or undocumented context. Product and release support remain later gates. The historical Rust result validates this transport shape for one finite retired evaluator capability, not current or general interpreter support.

At each probe checkpoint, consult the full catalog in [`USE-CASES.md`](USE-CASES.md) and select only the one or two task profiles whose dependency cuts were materially improved; do not turn ten categories into parallel workstreams or a quota. Execute/refactoring are named above. An SMT-backed Analyze/Synthesize probe remains readiness-only until a closed reachability query and first-class numeric domains exist; its first implementation should be solver-free, and any external solver is a separately approved dependency. Translate, Compile, Verify, Qualify, Explain, Govern, and newly discovered potentials receive bounded probes when their exact prerequisites and task questions become ready.

## Guardrails and stop conditions

- Work only in this repository. Sibling repositories remain read-only and visibly unchanged.
- Put verified language-neutral behavior in [`../spec/`](../spec/) and update [`A12-DMKITS-SPEC-SYNC-LEDGER.md`](A12-DMKITS-SPEC-SYNC-LEDGER.md) in the same change. Keep Lean design, implementation status, evidence status, and plans in their `docs/` owners.
- Reuse a completed baseline; never recreate equivalent semantics, lowering, examples, or proofs merely to satisfy the spiral.
- Keep one active capsule or risk spike. A spike must end as a closed capsule, a precise unresolved boundary with experimental code removed, or a representation correction whose affected gates are re-closed. Do not leave parallel half-built semantic branches, and do not generalize a mechanism until a second completed consumer demonstrates the same meaning, result domain, and law.
- After a representation correction, audit the affected theorem statements, hypotheses, directions, result domains, and nearest non-laws rather than accepting compilation alone. Source-grounded clauses may propagate `external evidence pending`; representation-affecting unresolved alternatives block dependent work.
- Use red/green TDD, the Tier 1 gate in [`TESTING.md`](TESTING.md), payoff-selected laws/non-laws, and honest external-evidence status for each capsule.
- Add no dependency and change no linkage, hashing, or distribution policy without explicit approval.
- Do not restore retired V1, raw-packet, generator, registry, qualification, or universal-capture machinery. Any proposed harness or governance work must first be surfaced with the reproduced need, existing-mechanism gap, payoff, cost, and alternatives, then receive explicit user approval.
- Stop only for a serious blocker requiring new authority, an unavailable observation that changes the intended claim, an irreconcilable compatibility choice, or a dependency/licensing decision.

## Resume procedure

Read [`../AGENTS.md`](../AGENTS.md), this file, [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), [`EVIDENCE.md`](EVIDENCE.md), and [`TESTING.md`](TESTING.md). Inspect `git status --short`, recent commits, and the complete diff. Resume from the first unfinished item in the current spiral. Finish each delivery with the relevant gates, measured theory/support/evidence ratios, `git diff --check`, a matching ledger entry for every behavioral `spec/` change, no machine-specific data, clean project-local temporary state, and no sibling worktree changes.
