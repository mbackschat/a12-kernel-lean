# Active implementation plan

This is the minimal continuation checkpoint. Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

**Priority invariant:** eventual 100% semantic conformance with kernel 30.8.1 is the primary project goal. Consumer adequacy is the co-equal representation test. A supported-fragment boundary may stage delivery or state current evidence honestly, but may not become a permanent reason to omit kernel behavior.

## Verified baseline

- The current theory closes substantial checked nonrepeatable validation/computation, temporal, String, Enumeration, custom-field, resolved repeatable, message, and target fragments. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) is the sole detailed capability/proof/evidence inventory.
- The value-count sequence culminating in `e39e42c` closes Number and String/stored-or-category Enumeration `NumberOfValueInFields` through checked full/computation/partial evaluation, numeric expression, generated validation, and declaration-owned Number targets while preserving per-cell filter provenance, exact selected projection/domain identity, hierarchical addressing, and structural failure outside UNKNOWN. It adds no second evaluator, fold, topology, target path, protocol, harness, or support estate; the latest 391-job build, all 51 retained projections, and the 1,212-root trust audit pass.
- The first SG2 checked multiple/mixed Number value-list route composes the existing checked field-entity shape, immutable checked document, canonical star topology, addressed-cell query, and ordered quantifier evaluator. Each operand retains its checked declaration, complete topology, selected environments and addresses, exact stored payload, hierarchical tail, filter marker, and positional partial nonrelevance. Source order, early `No` versus later UNKNOWN, filtered nonwitnesses, and structural addressing failure are executable and proof-closed; the same-context Execute/Transform/Explain query passes while token metadata remains explicit insufficient information.
- The token SG2 widening proves that topology, addressing, filtering, hierarchical extent, and positional relevance are kind-neutral and now share one private-constructor addressed core with Number. Typed token wrappers retain exact declaration projection and classify cached cells separately. Rich String and stored/category Enumeration value lists cover independent/nested stars, order, filters, partial relevance, structural failure, family rejection, exact projection identity, many-to-one category mapping, and physical emptiness. Token distinct count and projection-bearing value count reuse the same checked token owner with their distinct folds. The same-context Execute/Transform/Explain probes recover exact stored payload beside normalized String or category-projected tokens; unsupported cross-level projection remains explicit insufficient information.
- The SG2 cross-level inventory found that model-certified candidate/captured filtering, including the diagonal/off-diagonal discriminator, was already closed by the general checked-star owner. The obsolete caller-assembled two-level adapter was retired without changing the public boundary; its independent-coordinate laws, resolving failure-timing cases, and discriminator now live under the shared correlation and checked authored-star owners. The remaining gap is construction of one complete runtime rule `Env`, not another operand stream or cross-level carrier.
- The first checked ordinary repeatable rule route derives one deepest compatible nonparallel scope from condition references, validates the error declaration separately, enumerates only actual deepest-scope rows in immutable document order, and preserves complete environments through checked addressing and exact repeatable error emission. A checked star contributes its nonempty binding prefix strictly above the first star; candidate-local `Having` references remain operand-local, while checked `$` Number and repetition references contribute their captured outer scope. A fully reopened unfiltered star contributes no surrounding loop, and an unrelated ordinary scope is rejected instead of positionally joined. Zero actual rows produce no evaluation; one instantiated-empty row remains an evaluated content-bearing environment. Its same-context Execute/Transform/Explain probe recovers outcomes, full coordinates, and exact error addresses without a second rule environment or emitter.
- The same rule route now admits ordinary repeatable Number fields, evaluated-String `Length`, String `RangeAsNumber`, String/stored-or-category Enumeration `FieldValueAsNumber`, direct Date/Time/DateTime numeric-component extraction, Date-only month/year differences, and concrete-profile calendar-day differences through the existing ordered numeric tree under one checked same-group addressed policy. Each operation retains its model-certified declarations and complete iteration scope; range selection retains its checked interval, conversion retains its exact selected domain and derived scale, temporal extraction retains its kind/component certificate, and temporal differences retain two authored-order field/Base-Year operands across compatible same-scope or ancestor/current declarations. Calendar-day operands additionally preserve the checked model-zone profile, decoded local labels, and exact instants. The shared resolver reads every repeatable field in authored order, substitutes the complete checked set into one scalar context, and delegates to the existing typed owner. Missing range and temporal zero preserve their distinct directional polarity, present components remain fixed, temporal differences preserve signed order and symmetric missing zero, formal invalidity remains UNKNOWN, and structural missing bindings remain outside UNKNOWN without a repeatable numeric atom, comparison tree, evaluator, pair stream, parser, or temporal registry. A sibling repeatable temporal branch is rejected at the existing authoring boundary.
- Exact-instant `DifferenceInHours`/`DifferenceInMinutes`/`DifferenceInSeconds` now widen that same shared numeric and addressed rule route. One checked atom retains the selected unit and two model-certified DateTime fields; the checker enforces unit-component and year-presence compatibility while rejecting Date, Time, and Base Year. Validation, computation, generated validation, and whole-rule evaluation delegate to the existing `DateTimeDifferenceOperand`/`Instant.difference` core. Cases lock both operand orders, fractional negative truncation toward zero, every unit, symmetric missing-zero OMISSION, formal UNKNOWN/poison, structural address failure, and a same-context Execute/Transform/Explain view of both certificates, addresses, stored payloads, unit, and verdict.
- One-level checked `RepetitionNotUnique` now composes as one ordinary leaf in the shared validation condition and actual-row whole-rule loop. The rule prepares the branch-independent relation once from the immutable checked document before evaluating any branch, admits only one RNU leaf, preserves optional-empty OMISSION, and keeps absent/mismatched prepared results as structural addressing failures. A focused independently buildable conformance module supplies the Execute/Transform/Explain query over exact key identities, row verdicts, and ordered peer clusters without another cache, topology, evaluator, or message emitter.
- Decomposition note: the RNU capsule exceeds the ordinary Tier 1 Lean-line signal because the first safe whole-rule consumer requires one coherent checked-document projection, exhaustive condition-leaf integration, once-per-scope rule preparation, structural-error routing, proof registration, and a separately buildable conformance family. Splitting before those pieces agreed would expose either an importable leaf with no valid execution route or a rule route that could collapse missing preparation into UNKNOWN; no new semantic representation, cache, topology, evaluator, harness, schema, or support-estate source was added.
- Decomposition note: this closed-sum extension exceeds the ordinary Tier 1 Lean-line signal because adding one shared numeric atom requires exhaustive scalar validation, computation, generated-validation, checked-rule, proof, and conformance coverage in the existing owners; splitting those exhaustive branches would leave an importable constructor accepted by only a subset of its already-public consumers. No new evaluator, stream, topology, file family, harness, or support estate was introduced.
- The addressed numeric rule route also admits the existing checked Number aggregate, prefix-lazy `FirstFilledValue`, and `NumberOfValueInFields` sources without changing their scans. A bound outer environment reopens only the star suffix; cases separate prefix-hidden malformed cells from draining counts, empty-prefix fallthrough, hierarchical-tail polarity, later-filter invisibility versus matched-filter shrinkage, exact selected addresses/payloads, and structural failure outside UNKNOWN. Delegation laws keep both consumers attached to their sole checked-document owners.
- Ordinary `GroupFilled`/`GroupNotFilled` now enter that same addressed whole-rule route for both an ordinary group path and `RuleGroup`. The group reference alone derives its repeatable scope; the current environment selects the exact existing checked group product; created-empty rows, admitted descendant content, formal error, full/partial relevance, exact target addresses, zero-row behavior, and typed structural failure remain distinct without another group state, descendant scan, or evaluator.
- Checked ordinary rule assembly now analyzes static negative-condition legality separately at every derived repeatable level. The existing condition tree carries a four-way guard classification; `And` accepts one positive guard at the queried level, `Or` requires every branch to reference and guard it, and the first known unguarded level is reported before runtime. Field and group presence are source-closed; unclassified ordered-numeric leaves remain explicitly insufficient instead of being guessed legal or illegal.
- The checked numeric-literal carrier now closes the kernel visitor's full host conversion for every grammar-representable finite decimal: exact rational value plus authored scale drives pure nearest-even binary64 rounding, Java long rounding/saturation, and signed-32-bit narrowing. Direct, wrapper, Number-entity, and token-value-count classifiers all consume that one result; malformed value/scale pairs remain explicit insufficiency. Separators cover below-half tie behavior, negative half, long saturation, `2^32` wrap to zero, and positive-threshold use.
- Top-level ordinary arithmetic and power retain their composite parse-tree root through the same checked expression tree. The static visitor's direct field/list-versus-constant branches do not flatten that root, so composite comparisons are admitted at their represented ordinary scope even when a syntactically direct field/zero comparison is rejected.
- Direct-field `Abs`/rounding and same-scope direct Number Min/Max are top-level operation-list consumers of the shared zero-sensitive guard. Equality and inclusive inequalities against safe converted zero are rejected in either operand order; not-equal and converted-nonzero controls remain admitted without flattening the wrapper, call boundary, or individual model scopes.
- Mixed-scope operation lists and mixed direct/star Number entity lists now share one per-level reference classifier. An all-iterating set retains its operator-family zero/positive partition, while any iterating/noniterating mix rejects every immediate numeric-literal comparison at the first mixed level. A filtered slot contributes only its selected target scope to this classifier; a noniterating filter-only reference cannot make the operation set mixed. Addressed direct Number admission accepts declaration-owned ancestor scopes but still rejects descendant or incompatible scopes; one Execute/Transform/Explain case reads ancestor and current-row fields from the same checked tree and emits at the complete current-row address.
- Projection-bearing String/Enumeration `NumberOfValueInFields` now joins that classifier with the same zero and directional positive-threshold sensitivities as Number. Its ordinary checked-document route delegates each direct/star slot to the shared token operand resolver and unchanged projection-aware count fold; a complete outer-row rule fires at the exact address, while a missing binding remains structural addressing failure.
- One-scope unfiltered plain-star Number entity lists preserve their operator family: `FirstFilledValue` and Sum/Min/Max are zero-sensitive, distinct count is positive-threshold-sensitive with operand-direction reversal, and Number value count has both sensitivities. The same checked entity source and scope query remain the sole owners.
- Row-aligned plain-star `SumOfProducts` now shares only that zero-sensitive static branch and enters ordinary addressed rules through its existing checked pair, shared topology, immutable checked-document projection, and numeric evaluator. A positive nonzero control authors successfully; filled products and empty-right zero substitution stay decided at runtime, while structural failures remain outside UNKNOWN.
- The follow-up Boolean/Confirm audit is a verified no-op at the current representation boundary. Kernel checking requires the corresponding configured stored token to agree across every expanded field, admits `True` for Boolean and Confirm, and admits `False` only for Boolean; runtime then uses the existing exact-text count. `FlatFieldDecl` and `FieldPolicy` retain only the scalar kind, so they cannot certify that common token without new model-owned declaration metadata. The family remains open under SG5 instead of guessing from `Bool`.
- SG1 is closed at the immutable checked-document boundary. Finite `DocumentData`, model-relative topology/placement checks, one private-constructor `CheckedDocument model`, stable structural reads, shared hierarchical over-repetition, prepared-cell caching, flat evaluation, resolved group presence, model-derived absolute requiredness, parent-scoped all-scalar index findings, and full-injected/partial-suppressed Enumeration S-values compose without replacing `Document`, `CheckedCell`, `FlatContext`, `ResolvedGroupPresenceInput`, RNU, semantic-index phase policy, or processing context. Partial default suppression is relevance-local, every silent address carries a proof that it belongs to the normalized partial scope, and no caller can omit a relevant absolute-required declaration from generated staging. The cold SG1 artifact review exposed both defects before closure; its total `flatContext` observation was reconciled as a checked-plan adapter while `CheckedDocument.read` remains the explicit structural query.
- The binding whole-model construction order remains **SG1 → SG2 → SG4** in [`ARCHITECTURE.md`](ARCHITECTURE.md#the-whole-model-critical-path-shares-one-checked-document-and-one-addressed-operand-stream). SG1 is complete; SG2 is now the live keystone; SG4 remains prohibited until every remaining prerequisite owner, bounded source packet, dangerous discriminator, and entry gate in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md#hard-frontier-critical-path) is satisfied.
- Reference semantics remains 0.3.0 with the V2 flat-validation and one-group-correlation suites. Internally closed but uncalibrated families remain explicitly `external evidence pending`; coherent calibration work is indexed by [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage).
- Checked group-star lowering is the first family to discharge the new proof-spine obligation [4a](LEAN-FORMALIZATION.md#12-required-proof-spine): both of its declared-impossible results are proved unreachable, with reachable-rejection controls so the claim is not read as "lowering always succeeds". The reusable mechanism is [`LF70`](LEAN-FINDINGS.md#lf70--a-defensive-re-check-is-dead-only-when-the-caller-owns-the-fact-it-re-checks); 64 defensive re-check sites across 21 other checked modules remain, including the neighboring field-star elaborator, whose ancestry branch additionally needs a model-validation lemma relating a declaration's retained scope to its derived scope.
- The approved module-boundary sweep is complete: the former validation-rule, numeric validation/computation, generated-validation, aggregate, flat elaboration, validation-condition, proof, and reference-support monoliths now have import-only compatibility roots and independently buildable semantic-family children. Ordinary Lean source is below the 1,000-nonblank-line ceiling; the one-session theorem-root registry in `TrustAudit.lean` remains the measured exception. This changes compilation ownership only, not semantics, and no new capsule is selected at this pause boundary.

- The composition-claim triage is complete across all three batches (17 claims). Every defect it found was in prose, citation, or claim scope — **no Lean formalisation was wrong**. Three real defects: the `NotAll` prepass and the semantic-index computation gate were both stated uniformly while the kernel selects the route by input size or operand shape, and one ordering claim in [`LF23`](LEAN-FINDINGS.md) overreached. Five citation defects pointed at evidence unable to support the claim — a case whose malformed cell was in the wrong position, a pass-through enclosing template, a class with no phase notion, an omitted separating test, and a size-gated route restated as unconditional. The engine routing rule in [`SOURCES.md`](SOURCES.md#engine-routing-rule--pick-the-layer-by-the-question-not-by-habit) plus the closure-assessment gate in [`TESTING.md`](TESTING.md) are what keep this from re-accumulating; the standing at-risk set is now the composition claims outside those 17.
- **Route selection by input size or operand shape is a recurring kernel pattern, not a series of special cases.** Three instances so far — Groovy size-triggered lowering, the value-list prepass, the semantic-index gate. When auditing any codegen bean or runtime entry point, establish whether it selects a route before treating one emission as the only one.
- The extraction gate is therefore **lifted**: bounded-exhaustive fixture generation was held behind the triage so a composition error could not be baked into a corpus that then looks authoritative. Its remaining precondition is unchanged — a bound that crosses the codegen thresholds rather than staying below them, and host facts surfaced as data rather than left implicit in a JVM-shaped assumption.

## Hard-frontier critical path

1. [Immutable checked-document construction](IMPLEMENTATION-MAP.md#immutable-checked-document-construction) closes SG1 with one model-certified checked input and an explicit scalar-parser trust boundary. Processing context remains separate.
2. [`SG2`](SEMANTICS-GAPS.md#sg2--general-repeatable-addressing-and-operand-construction) is active and owns one canonical addressed operand stream over that checked input. It must preserve hierarchical extent, relevance, encounter order, declaration metadata, exact stored payload, and structural failure.
3. [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) consumes the closed checked input and future SG2 stream with the separate checked plan and processing context. It remains prohibited until SG2 closes and the complete scheduling/result/application source packet passes its entry gate.

The detailed readiness fields live only in those gap entries. Every capsule receives a same-context consumer-adequacy assessment, but an executable same-context probe runs only when a reusable family or major boundary closes; SG2, SG4, or another comparable reusable capability boundary additionally triggers the artifact-only cold probe defined in [`TESTING.md`](TESTING.md#trigger-based-cold-review).

## Awaiting the peer project

Three ledger items are pending handoff and need no local work until they return. [`SPEC-2026-07-25-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md) narrows the `NotAll` prepass to a size-selected route. [`EXP-2026-07-25-02`](A12-DMKITS-SPEC-SYNC-LEDGER.md) is blocked on a local doctrine decision, not on upstream work — see below. [`EXP-2026-07-25-03`](A12-DMKITS-SPEC-SYNC-LEDGER.md) asks for the semantic-index parallel-iteration observation, with its separating document shape specified.

One outbound correction is recorded but unsent: the calculation-closure mode's engagement is **in dispute**, recorded as such in [`SOURCES.md`](SOURCES.md). The peer reported it unconditional for Groovy; this project's triage found the deciding bean property gated on a computed field's alternative count reaching ten. The two readings name different mechanisms — plausibly an unconditional setter versus a size-gated getter — so the locus is unsettled and one reading upstream would close it. Until then the mode is not assumed engaged, which also means the peer's zero-divergence result does not extend to it.

## Owner decision required

[`EXP-2026-07-25-02`](A12-DMKITS-SPEC-SYNC-LEDGER.md) cannot proceed either way without a doctrine ruling. Table enumerations are Java-only by declared, diagnosed kernel policy, so the normative Groovy-dynamic anchor **structurally cannot witness that region** — no Groovy-anchored differential on either side can follow. Closing it would require an explicit exception promoting static-Java co-evidence to sole evidence for that region. Refusing the exception is equally legitimate and records [`SG8`](SEMANTICS-GAPS.md#sg8--enumeration-and-value-list-completion)'s dynamic/table portion as permanently outside kernel-correspondence claims — the eventual-100% goal's first acknowledged structurally uncalibratable region. Requesting a probe before this ruling would be premature.

## Active capsule

No semantic capsule is selected at this checkpoint. The one-level checked RNU whole-rule capsule is closed; nested reference-scope preparation, partial relevance, and rich message pointers remain with SG2/SG10 rather than being inferred from this result. The bounded SG4 traceability refinement remains adopted in [`ARCHITECTURE.md`](ARCHITECTURE.md#whole-model-computation-execution-keeps-definition-activation-result-application-and-validation-separate) and its future entry/proof obligations remain owned by [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition).

At resumption, select the smallest remaining SG2 family whose bounded packet closes a real unresolved source fact and widens the existing canonical addressed construction. Two smaller recorded items may be taken first if a short unit is wanted: the [upstream kernel-locked inventory](IMPLEMENTATION-MAP.md) is recorded centrally and its per-clause `Kernel-locked` labels are still unpopulated, and [`LF64`](LEAN-FINDINGS.md) has an identified test-coverage gap — no case on either side observes the computed-Number no-fit precedence, so one focused conformance case at that seam would close it. The consumer hypothesis must require Execute plus Transform/Explain to recover complete environments, hierarchical extent, authored order, model certificates, relevance/filter provenance, and structural failure without reconstructing a second stream.

## Immediate frontier

1. Reconcile the remaining SG2 families against the checked ordinary-rule resolver, addressed operand core, static per-level classifier, existing handbacks, and recent Git history before selecting work.
2. Prepare one bounded source packet and separating matrix for the highest-connectivity ready family; if the existing owner already closes it, record a verified no-op or consolidate rather than adding an adapter.
3. Implement through the existing checked document, canonical topology/addressing, typed projection, and consumer owners with red/green TDD; keep structural failure outside semantic UNKNOWN.
4. Keep SG4 prohibited until SG2, the explicit processing context, and every SG4 source-audit category close. Batch external evidence under [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage) only for a coherent closed family with a separating matrix.

## Parked boundaries

- Multiple/mixed Number, String, and stored/category Enumeration value-list operands now cover multiple/nested stars and share the kind-neutral addressed core with aggregate consumers, but cross-level, computation, RNU, and group closure remains under SG2. Date-like distinct count needs exact compatible-format and TIME identity; temporal and wider RNU keys need exact stored text; temporal targets need declaration-owned rendering policy.
- Public protocol expansion, semantic shipments, candidate qualification, dependencies, SMT integration, and new evidence/process machinery require their existing explicit adoption or approval gates.
- No copied a12-dmkits harness, second numeric AST or scale gate, second aggregate scan, alternate group-state encoding, or parallel protocol route is permitted.

## Stop conditions

- Stop a candidate when its required model fact, consumer, or separating source witness is absent; update its one [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry rather than creating another queue item.
- Stop before introducing a duplicate semantic representation or any harness, schema, generator, protocol, registry, dependency, or governance layer without explicit approval.
- Stop before beginning [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) or freezing its general result types until SG2 completes, the processing context is explicit, every SG4 readiness field is satisfied, and this plan explicitly selects the first source-grounded scheduling capsule.
- Keep sibling repositories read-only and preserve their visible status. Never push without an explicit current request.

## Resume procedure

1. Read the active capsule above, its [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry including every readiness field, the linked [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) mechanism and five-dimensional coverage record, and the applicable [`TESTING.md`](TESTING.md) rung.
2. Inspect `git status --short`, recent commits, the current owner/code/tests, Git history, and any active cross-project handoff before writing a red case.
3. Close one capsule or verified no-op, perform the same-context closure assessment, run its proportional gate, update only fact owners, delete a completed gap, and commit.
