# Active implementation plan

This is the volatile checkpoint for resuming the current delivery sequence. Stable purpose and milestones belong in [`PROJECT-DESIGN.md`](PROJECT-DESIGN.md), proposed product progression in [`PRODUCT-PROPOSAL.md`](PRODUCT-PROPOSAL.md), production engineering in [`PRODUCTION-RELEASE.md`](PRODUCTION-RELEASE.md), and durable implementation decisions in [`ARCHITECTURE.md`](ARCHITECTURE.md) and [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md). Git and the owning documents retain completed history; this file records only the verified baseline, active objective, immediate order, and stop conditions.

## Verified checkpoint

The repository has executable Lean semantics for the admitted flat-validation and one-group captured-outer fragments, with checked lowering, internal laws and counterexamples, retained kernel observations, a normalized reference process, and independent-candidate conformance surfaces. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) owns exact clause, proof, and support status; [`EVIDENCE.md`](EVIDENCE.md) owns the retained-observation inventory and correspondence limits; [`PROTOCOL.md`](PROTOCOL.md) owns the public process contract and samples.

The completed Rust knowledge-transport experiment remains immutable history for `flat-validation-empty-logic-v1` and reference semantics 0.2.0. Rust revision `d213005b3972c2acd8f67e87f523a923d69f6a54` is the candidate that was actually mutation-qualified and executed in the 52-case generated differential; all 52 cases agreed with the pinned Lean reference under that finite profile. Downstream revision `6a4df4ebaf15efd5620d60caf6cf9ac9834b668e` only mirrors the canonical result, adds a documentation addendum, and updates downstream status text while preserving historical reports byte-for-byte. It is not a newly executed candidate and must not replace `d213005b…` in the frozen profile, qualification record, or compatibility claim. [`IMPLEMENTER-KIT-FLAT-EMPTY-LOGIC.md`](IMPLEMENTER-KIT-FLAT-EMPTY-LOGIC.md#generated-differential-outcome-2026-07-15) owns the exact result and limits.

Commit `ced07282ca56fa44a6b9d5e81d768cf6d8162b18` completes the operator-sensitive empty-value delivery. It fixes direct Number polarity by carrying directional fillability rather than a single `given` bit, with the second-instance controls required by the root-cause method; centralizes scale-19 numeric comparison across flat, iteration, and correlation evaluation; and adds the deliberately narrow direct-String-equality versus String-`Length` capsule. Direct empty String equality is suppressed, while `Length(empty)` is numeric zero with grow-only fillability, so `< 5` fires OMISSION and `>= 0` fires VALUE. Retained kernel 30.8.1 observations cover paired content-bearing and empty-row String cases plus unsigned/signed directional Number controls, with exact source revision, strategy results, provenance receipt, model/case hashes, complete model/rule inventories, unique projected-field binding, and replay checks. Three independent final reviews closed semantic, lineage/documentation, and proof/trust findings; the complete gate passed before the commit.

The compatibility consequence is implemented explicitly. Historical reference semantics 0.2.0 remains pinned to source revision `9fa50276f5fb70dcd879b0a9712c8d69c0868967`; the selected 152-entry historical evidence/example/principal-artifact inventory in [`reference-semantics-0.2.0.lock.json`](../reference/reference-semantics-0.2.0.lock.json) is SHA-256 frozen. Human-facing handover documents remain outside that lock, and the post-revision separating request and replay receipt are bound separately. The completed Rust experiment is not relabeled. Current reference semantics 0.3.0 advertises [`supported-fragment-v2.json`](../reference/supported-fragment-v2.json), uses current flat/correlation v2 suites, and returns VALUE for empty unsigned Number `!= -1`, while the historical replay returns OMISSION. Protocol requests and responses have no per-request semantics selector, so consumers detect the account from the exact binary's `--manifest` output and pin those bytes. The current flat suite preserves the eight-case historical prefix and adds the separating witness through a typed evidence-to-protocol bridge; correlation v2 preserves the complete historical case body. `checkReferenceProcess` rehashes the frozen v1 artifacts and exercises the current v2 controls; the retired v1 projection machinery remains inspectable at Git revision `03186c1`.

Commit `aaef9f0b366055f612a76a08e601333273549e40` completes the first §11 String-computation capsule, and its public CI run passed. It separates computation-phase field observation, expression terms, root String storage, and change-only target deltas for one unconditional non-repeatable target. A missing String is no-value as a bare copy, contributes `""` in concatenation, and can therefore combine with a non-empty suffix; an all-empty final concatenation is evaluated text but is still no stored value, clearing only a stale target. The trusted proof spine reaches from checked-cell observation through the library evaluator and covers poison short-circuit, concatenation/store/delta laws, typed change reporting, and the intermediate-term non-law. The version-owned packet binds complete legal models and deltas; both kernel strategies agree, while three a12-dmkits empty-`VALUE` results are retained as mechanically projected triangulation mismatches. [`EVIDENCE.md`](EVIDENCE.md) owns the exact inventory. The packet externally closes only clean, unconstrained String inputs and final deltas. It does not expose this capsule through the public reference protocol or establish target constraints, `ERRORED`, dependency cascades, alternatives, application, repeatable scope, or implicit validation.

Commit `b10124341a3fa77c150d667a785570b00604727a` completed the §11 String target-validation checkpoint, and its public CI run passed. A nonempty root write is routed through one positive minimum or maximum before delta reporting; accepted values remain change-sensitive, while payloadful `ERRORED` retains the attempted stored String and `tooShort`/`tooLong` cause and reports unconditionally. At that checkpoint, Lean application was still value-only and separate from exact document shape. The core fails closed on CR/LF because the preceding line-break-permission/normalization clause is not modeled, and universal laws establish strict violation, inclusive permitted sides, exact boundaries, no-value/poison bypass, preservation, and the nearest application/delta non-law. A phase review removed an unsafe pre-check-to-`accepted` conversion: pre- and post-check delta lanes share only phase-neutral value/no-value projection helpers. The nine-case input-only packet binds four complete models, rich dual-kernel results, projected a12-dmkits observations, payloadful deltas, and exact absent-versus-present-empty external application states. [`LF13`](LEAN-FINDINGS.md#lf13--string-target-outcome-delta-and-application-are-separate-boundaries) and [`EVIDENCE.md`](EVIDENCE.md) own that checkpoint and its limits; the later exact-application capsule advances it without rewriting its evidence.

The historical a12-dmkits portable capture boundary was repaired at revision `3ebe756422e8f2dea447fbd62f9b26c2f774b7e5` and produced the accepted direct-cascade answer at clean revision `c992afd62e4fa6148733a5538a3248c30fce60bf`. Commit `70f296f88686a7ad49a786aa5498b8db46782f4f` retains the designated complete [`packet, qualification sidecars, no-drift recapture report, and scenario-mutation process receipt`](../evidence/kernel-30.8.1/captures/string-direct-cascade-v1/README.md); the packet receipt is `7e38744283cf17f7f57fe0bce342084a8e2896fb99aa60c2a9f4fb52e185dc17`, qualification is satisfied, and both kernel routes agree on all six required channels for all five cases. The exact expectation-free [`request and model`](../evidence/scenarios/string-direct-cascade-v1/README.md) remain bound at SHA-256 `4c5d4911ecacde819618b3b921b0bd30aa34b514cb0bbf3f0d2b735f21a0fd43` and `3d21add02d259a8d1ad2e14475582513aec2f4e60176f1c02c81d40de88a895d`. Historical exporter revision `1b5f463b89adc6cfb81b41121cd6c97855e8cbe3` certified the compact bundle. Upstream revision `45b264b2d6213dd7d4d261fa040034371b0c8fcd` subsequently removed the complete unused capture V1 estate—96 files and 11,637 nonblank lines—from current `main` without a successor; its [`archived proposal`](../../a12-rulekit/docs/archived/A12-DMKITS-CAPTURE-PROPOSAL.md) and Git history own reproduction, while F6's controls live in the committed upstream [`empty-polarity` corpus](../../a12-rulekit/corpus/cases/empty-polarity/). No live generic portable capture command or remaining upstream work exists for this capsule.

Commit `c2c60c6` replaces the trust audit's nontrusted-directory blacklist with complete positive source-zone classification. Every project Lean source must be classified; the logical theorem closure admits only logical sources, the conformance closure only logical plus conformance sources, and the library root only its root plus logical and conformance sources. The Lean environment audit now selects the logical module namespace positively, so future evidence/binding/registry paths cannot enter the trusted audit merely by having an unfamiliar name.

Commit `619d881` synchronizes the read-only `spec/` snapshot with the 2026-07-17 semantic corrections and pins a12-dmkits provenance to `cdf79872`. A post-commit review confirmed the substantive two-tier path, final-empty String, directional polarity, UTF-16, placement, CRLF, raw-type, keyword, and semantic-index findings, but also identified narrow documentation corrections: the synchronized Lean coverage row predates checked flat path/policy elaboration; the bilingual keyword inventory and several evidence attributions are broader than their named tests; the grammar sketch does not yet attach semantic-index specifiers to paths; and flat String validation does not mechanically normalize or reject CRLF even though the target-computation capsule rejects line breaks. These are documentation and open-boundary corrections, not permission to widen Lean implicitly.

Commit `dc5909b` closes those documentation corrections without widening the Lean theory.

Commit `085d257` research-closed the first direct two-computation String cascade at its named public boundary: the existing evaluator and proof spine were joined to the accepted five-case packet by a closed operation decoder, exact packet/qualification/consumed-document binder, raw two-route comparison, declared interpreter triangulation, and replay of clean values, changed values, attempted errors with causes, clears, and value-only applied results. At that checkpoint, the retained exact application states remained stronger external observations rather than Lean claims. The later compact migration intentionally kept only the value-level applied projection for routine replay, and the packet still does not expose the hidden dependency cell, poison tag, dependency graph, or scheduler.

Commit `41ddc9d` completes the internal evaluated-String ingestion capsule. It performs one non-overlapping CRLF-to-LF pass when an admitted parsed String enters `formalCheck`, preserves LF and lone CR, and proves that both phases, direct comparison, UTF-16 `Length`, and computation reads consume the same cached value. The `"\r\r\n"` witness proves global idempotence false and protects every consumer from a second pass. Raw line-break permission, enumeration ordering, raw-type elimination, stored-document mutation, and public String transport remain outside the capsule; external CRLF/LF/lone-CR evidence is pending.

The first compact evidence migration and its all-in consolidation are complete. a12-dmkits exporter revision `1b5f463b89adc6cfb81b41121cd6c97855e8cbe3` certified [`semantic-observations.json`](../evidence/kernel-30.8.1/captures/string-direct-cascade-v1/semantic-observations.json), SHA-256 `1d8d253e553eba70fa990975666884833748bed9d9b2b6483f472767a9837c7a`, from the retained qualified direct-cascade packet. The old complete binder and new typed lane agreed on all five cases once; the packet-specific binder, schema, replay, receipt decoder, and tests were then deleted. The first scoped checkpoint removed 1,513 nonblank lines from its 2,179-line replacement target. A later honest all-in audit included the formerly shared artifact-tree support, then removed it too; the complete live reader, tests, family, tests, and driver glue now total 753 nonblank lines, 272 fewer than the 1,025-line all-in checkpoint, with no permanent dual path. A real producer-overlay bypass mutation failed before the natural gate returned green. The complete raw unit remains immutable and opaque to ordinary replay.

The exact one-address String target-application capsule is complete internally. `StringTargetState` preserves absent, present-empty, and present-value states independently of delta reporting; `StringTargetOutcome.applyTo` matches every outcome directly, accepted values write, and outcomes without an applied value clear an existing target in place without creating an absent target. A separate closed relation is proved sound and complete for the executable transition and deterministic, while exact clearing, placement preservation, stored-value consistency, and both delta/application information-loss non-laws form the useful proof boundary. The complete internal matrix is conformance-locked. The existing nine-case target-validation binder now derives exact states through this live transition and actively calibrates accepted and errored rows over absent and filled priors; the raw cascade unit retains no-value over absent and filled, but compact replay remains value-only. Initially present-empty and direct poison rows remain external evidence pending. [`LF18`](LEAN-FINDINGS.md#lf18--exact-target-placement-is-separate-from-value-and-delta) owns the durable finding.

## Active objective

Direct computation presence is internally complete over one resolved checked context, and the one-time deletion wave has settled the supporting infrastructure. Sustained A12 semantic production is now the active work. The next semantic objective is ordered computation-condition `And`/`Or` short-circuit, followed by first-matching alternatives. Keep condition decision, producer outcome, exact target application, dependency observation, and delta reporting separate. Do not infer a scheduler, general computation graph, or whole-document mutation.

Tier 1 is now the default cadence: red/green semantics, compact conformance, payoff-selected laws/non-laws, trust, retained replay, and honest evidence status. External calibration is batched at meaningful family milestones. Tier 3 shipment, mutation, and historical-consumer machinery is not routine semantic work.

This plan also records the durable simplification decision for future sessions: preserve accepted evidence bytes, provenance, compact active replay, current public/candidate controls, the bounded subprocess boundary, and the trust audit; remove completed source generators and campaign-specific qualification machinery once a current gate pins their artifacts and the last reproducing revision is named. After the single deletion wave below, infrastructure is settled. Resume at the first unfinished semantic capsule and do not open another harness project unless a retained shared mechanism has a reproduced defect, an adopted semantic family needs an observation shape no current maintained route can retain, or a concrete consumer/release requires qualification.

## Immediate continuation order

1. Completed: the compact reader, direct-cascade projection, exact producer and bundle pins, one-time old/new agreement, overlay-bypass mutation, packet-specific deletion, and subsequent removal of generic artifact-tree machinery establish the settled 753-line all-in evidence bridge. The earlier 1,513-line figure remains a scoped migration checkpoint, not its final total cost.
2. Completed: the exact three-state target type, direct outcome transition, red/green full matrix, independent relation bridge, exact clearing and placement laws, stored-value bridge, and nearest delta/application non-laws close the internal application boundary.
3. Completed: the existing target-validation replay consumes the live exact transition, mechanically calibrating accepted and errored outcomes over absent and filled priors without a new binder or evidence format; the remaining exact rows are explicitly pending.
4. Completed: the initial Boolean-shaped precondition seam established that clean not-true returns quiet no-value before an arbitrary body can read a malformed operand, while a holding decision delegates transparently. Step 6 replaces that temporary decision type rather than preserving it as a compatibility layer.
5. Completed: commit `7c75b41` routes the live general candidate gate through the existing bounded relay, current v2 suites are the routine controls, and frozen v1 bytes remain pinned by the historical lock. The completed flat-v1 generator, Rust mutation-qualification runner, generated-differential campaign, and generic artifact-tree framework are absent from current source; `03186c1` is their common pre-deletion reproduction snapshot. The compact reader owns only its closed path, digest, bounded-file, and regular non-symlink contract. No replacement framework was created.
6. Completed: [`ComputationConditionResult`](../A12Kernel/Semantics/ComputationCondition.lean) separates `holds`, clean `notTrue`, and exact-cause `poison`; direct `FieldFilled`/`FieldNotFilled` consume computation-phase checked reads, required-only empty remains clean, and ordinary invalidity poisons both predicates. `StringComputationStep.evaluateOutcomeWhen` consumes this result before its body, and quiet versus poisoned dependency outcomes remain distinguishable downstream. The compact conformance matrix, trusted laws, and [`LF20`](LEAN-FINDINGS.md#lf20--computation-presence-reverses-only-clean-truth) close the internal boundary; external evidence remains pending.
7. Next semantic capsule: extend the result through ordered `And`/`Or` short-circuit, including the operand-order counterexample. Then implement first-matching computation alternatives with poison aborting the remaining alternatives. Each step remains a separate Tier 1 capsule unless they are genuinely one closed definition.
8. Batch the false-precondition, poisoned-precondition/body, pending exact-state, and smallest earlier String controls into one compact external family only when the internal condition/alternative boundary is complete. Use that overlap to retire the two older String binder stacks rather than adding a parallel path.
9. Continue with the first narrow partial-validation capsule after computation control. Keep the semantics/proof inner loop dominant and run full shipment qualification only for an actual consumer or release.

The pruning wave is closed: the named one-off bridge, differential, qualification, and artifact-tree sources and Lake targets are gone; live documentation distinguishes current commands from revision-scoped history; `checkReferenceProcess` still verifies every retained frozen artifact; and current candidate suites use the shared bounded runner. Resume at step 7. Do not reopen process archaeology, widen the deletion to unrelated live evidence binders, or invent replacement machinery without satisfying the infrastructure-reopening rule above.

## Guardrails and stop conditions

- Work only in this repository. Sibling repositories remain read-only and visibly unchanged. Run an unchanged source-maintained sibling command only when a suitable current command exists and direct its output to caller-selected ignored storage; never patch or extend a disposable a12-dmkits copy. A missing observation channel is an upstream request and an `external evidence pending` boundary.
- Do not edit [`../spec/`](../spec/). New findings, implementation state, evidence state, version decisions, and experiment results belong under `docs/` according to [`DOC-DISCIPLINE.md`](DOC-DISCIPLINE.md).
- Do not change dependencies, hashing strategy, linkage, or distribution policy without explicit user approval.
- Preserve the frozen Rust 0.2.0 bundle, profile, qualification result, generated-differential receipt, candidate identity, and historical report wording. A later mirror commit is provenance for downstream documentation, not permission to rewrite the executed identity.
- A compatibility identity must denote one semantic behavior. Do not fix the lineage by weakening response comparison, accepting both polarities, hiding the corrected case from support metadata, or labeling a regenerated current artifact as historical evidence.
- Preserve the distinction between retained kernel observations, internally proved laws, Lean-account generated differentials, independent-candidate attestations, and process receipts. No artifact may silently move between these assurance classes.
- Keep each new semantic capsule narrow and vertical. The direct cascade capsule must not grow into a dependency sorter, general computation graph, alternative evaluator, or document mutator. Its synthetic overlay must not be described as applied document state, and a validation-scoped required finding must never be presented as an inherited computation poison. If the three evidenced result states cannot be observed at the named boundary, retain the research and narrow or defer it rather than inventing behavior.
- The settled compact boundary must remain one-way: never restore retired capture V1, a raw-packet binder, or its packet framework, and never prebuild a universal successor, registry, generator, or compatibility adapter. A new producer capability requires a measured semantic consumer and an explicit design decision; “might be useful later” is not sufficient.
- The bounded simplification wave ends when the named one-off sources and Lake targets are gone, all live documentation names only current commands, the historical lock still verifies every retained frozen artifact, current suites use the shared bounded runner, and the full relevant gates pass. That boundary is closed; stop process archaeology and resume at step 7 rather than widening the wave to unrelated evidence binders or inventing replacement machinery.
- The user's standing instruction is to continue through natural delivery units and start periodic independent reviews; routine completion, a green gate, or a commit is not a reason to stop. Stop only for a serious blocker requiring new authority, an unavailable portable oracle observation, an irreconcilable compatibility choice whose alternatives materially change public commitments, or a dependency/licensing decision requiring explicit approval.

## Resume procedure

Read [`../AGENTS.md`](../AGENTS.md), this file, the current status owners [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) and [`EVIDENCE.md`](EVIDENCE.md), the retained [`Set Z4 capture`](../evidence/kernel-30.8.1/captures/string-direct-cascade-v1/README.md), the accepted compact pipeline contract in [`SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md`](SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md), the flat kit's [generated differential outcome](IMPLEMENTER-KIT-FLAT-EMPTY-LOGIC.md#generated-differential-outcome-2026-07-15), and [`TESTING.md`](TESTING.md). Inspect `git status --short`, recent commits, and the complete diff before changing anything. Resume from the first unfinished numbered step, preserve other agents' in-progress files, and finish every delivery with the full relevant gates, measured nonblank line counts, `git diff --check`, no `spec/` diff, no machine-specific path, clean project-local temporary state, and no changes caused in a sibling worktree.
