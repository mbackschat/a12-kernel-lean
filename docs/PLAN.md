# Active implementation plan

This is the minimal continuation checkpoint. Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

**Priority invariant:** eventual 100% semantic conformance with kernel 30.8.1 is the primary project goal. Consumer adequacy is the co-equal representation test. A supported-fragment boundary may stage delivery or state current evidence honestly, but may not become a permanent reason to omit kernel behavior.

## Verified baseline

- The current theory closes substantial checked nonrepeatable validation/computation, temporal, String, Enumeration, custom-field, resolved repeatable, message, and target fragments. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) is the sole detailed capability/proof/evidence inventory.
- The value-count sequence culminating in `e39e42c` closes Number and String/stored-or-category Enumeration `NumberOfValueInFields` through checked full/computation/partial evaluation, numeric expression, generated validation, and declaration-owned Number targets while preserving per-cell filter provenance, exact selected projection/domain identity, hierarchical addressing, and structural failure outside UNKNOWN. It adds no second evaluator, fold, topology, target path, protocol, harness, or support estate; the latest 391-job build, all 51 retained projections, and the 1,212-root trust audit pass.
- The future whole-model architecture and its **SG1 → SG2 → SG4** construction order are binding in [`ARCHITECTURE.md`](ARCHITECTURE.md#the-whole-model-critical-path-shares-one-checked-document-and-one-addressed-operand-stream). Adoption does not select SG4: every prerequisite owner, bounded source packet, dangerous discriminator, and entry gate in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md#hard-frontier-critical-path) remains mandatory.
- Reference semantics remains 0.3.0 with the V2 flat-validation and one-group-correlation suites. Internally closed but uncalibrated families remain explicitly `external evidence pending`; coherent calibration work is indexed by [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage).

## Hard-frontier critical path

1. [`SG1`](SEMANTICS-GAPS.md#sg1--general-checked-document-construction) owns one immutable model-certified checked document. Its first implementation unit starts only after the row-existence/placement/cache source packet and separating matrix close.
2. [`SG2`](SEMANTICS-GAPS.md#sg2--general-repeatable-addressing-and-operand-construction) owns one canonical addressed operand stream over SG1 input. It cannot start as a general boundary before SG1 supplies stable row identity, and it must preserve hierarchical extent, relevance, encounter order, declaration metadata, and structural failure.
3. [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) consumes SG1 and SG2 with the separate checked plan and processing context. It remains prohibited until both prerequisites close and the complete scheduling/result/application source packet passes its entry gate.

The detailed readiness fields live only in those gap entries. Same-context consumer validation remains part of every capsule closure; a reusable SG1, SG2, or SG4 capability boundary additionally triggers the artifact-only cold probe defined in [`TESTING.md`](TESTING.md#trigger-based-cold-review).

## Active capsule

Audit Boolean/Confirm `NumberOfValueInFields` under [`SG5`](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion), starting from the existing model-owned declaration policy rather than the runtime `Bool` payload. Reuse an existing owner only if it retains the common configured Yes/No labels, admits `True` for Boolean and Confirm, admits `False` only for Boolean, and reaches the generic value-count fold without weakening stored/category token identity. Do not widen group authoring through this capsule.

Consumer hypothesis: an Execute/Analyze/Transform consumer must decide whether the authored Boolean constant is legal for every selected field, retain the model's common configured Yes/No labels, and compare the corresponding stored token through the established count result. Treating fields as raw `Bool`, inventing English labels, or admitting `False` for Confirm are unsafe controls. If the current model cannot certify one common label policy across the complete entity list, that is an explicit insufficient-information result and the capsule stops at the recorded source fact.

## Immediate frontier

1. Audit and, only if an existing declaration/model owner fits exactly, close Boolean/Confirm value count with red/green TDD through the same generic count and numeric-consumer routes.
2. Keep group expansion gated by SG1/SG2/SG8 rather than creating a value-count-specific representation, then select SG1's bounded document-ownership source packet as the next whole-model keystone preparation.
3. Enter SG2 only through the SG1 output and SG2 readiness gate. Do not introduce the general stream while row identity, phantom/omitted extent, rule-wide `Having`, or mixed filter encounter remains unresolved.
4. Keep SG4 prohibited until SG1, SG2, the explicit processing context, and every SG4 source-audit category close. SG6, SG9, and SG10 likewise require their named source packets before a high-connectivity extension.
5. Batch external evidence under [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage) only for a coherent closed family with a separating matrix; request one bounded upstream observation capability only when no existing a12-dmkits route fits.

## Parked boundaries

- Multiple/mixed value-list operands need the focused filter-encounter matrix; date-like distinct count needs exact compatible-format and TIME identity; temporal and wider RNU keys need exact stored text; temporal targets need declaration-owned rendering policy.
- Public protocol expansion, semantic shipments, candidate qualification, dependencies, SMT integration, and new evidence/process machinery require their existing explicit adoption or approval gates.
- No copied a12-dmkits harness, second numeric AST or scale gate, second aggregate scan, alternate group-state encoding, or parallel protocol route is permitted.

## Stop conditions

- Stop a candidate when its required model fact, consumer, or separating source witness is absent; update its one [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry rather than creating another queue item.
- Stop before introducing a duplicate semantic representation or any harness, schema, generator, protocol, registry, dependency, or governance layer without explicit approval.
- Stop before beginning [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) or freezing its general checked-document/result types until SG1 and SG2 complete, the processing context is explicit, every SG4 readiness field is satisfied, and this plan explicitly selects the first source-grounded scheduling capsule.
- Keep sibling repositories read-only and preserve their visible status. Never push without an explicit current request.

## Resume procedure

1. Read the active capsule above, its [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry including every readiness field, the linked [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) mechanism and five-dimensional coverage record, and the applicable [`TESTING.md`](TESTING.md) rung.
2. Inspect `git status --short`, recent commits, the current owner/code/tests, Git history, and any active cross-project handoff before writing a red case.
3. Close one capsule or verified no-op, perform the same-context closure assessment, run its proportional gate, update only fact owners, delete a completed gap, and commit.
