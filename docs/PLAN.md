# Active implementation plan

This is the resumption checkpoint, not a work log. Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

**Priority invariant:** eventual 100% semantic conformance with kernel 30.8.1 remains the primary goal, with consumer adequacy as the co-equal representation test.

## Verified baseline

**Last full gate, 2026-08-03:** `lake build` 716 jobs · trust audit **1820 theorem roots; 43190 declarations in 417 modules** · `lake test` 51/51. Re-run the applicable tier commands in [`TESTING.md`](TESTING.md#tier-gates) before relying on these changing counts.

- SG1 is closed at one immutable model-certified `CheckedDocument`; processing context remains separate.
- SG2 is closed. Canonical topology, complete named `Env`, checked addressed reads, hierarchical omitted tails, exact stored payload, filter/relevance provenance, structural failure, and relevance-derived partial group products compose across Number, String, Enumeration, aggregate, value-list, temporal, group-presence, and nested RNU consumers.
- The ordinary repeatable validation loop derives its scope from the checked condition, enumerates actual rows only, reads ancestor/current declarations through the checked document, and emits at exact repeatable error addresses. Current static legality is level-local and preserves `insufficient` for unclassified families.
- Both group-terminal interpretations below stars are closed for full validation. A terminal repeatable group counts structural terminal rows; a nonrepeatable terminal reuses the same checked topology to select complete environments and derives the existing descendant-content/error group product in each. The shared tally, ordinary rule loop, and structural failure channel remain unchanged.
- The six coverage dimensions are executable, proof-closed, Kernel-locked, Kernel-calibrated, public, and consumer-qualified. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) owns their clause-level state; absent `Kernel-locked` rows mean not yet assessed, not no evidence.
- The universal Core IL is closed and archived. [`PROJECT-DESIGN.md`](PROJECT-DESIGN.md#representation-policy-for-derived-consumers) owns the live family-data default and the six conditions for reconsidering a checked-plan IR.
- The SG4 dependency-transitive-skip ambiguity is closed in favor of reached-read poison. The static dependency graph constrains generated call order; it does not pre-propagate invalidity through an unread edge. [`LF77`](LEAN-FINDINGS.md#lf77--a-dependency-graph-can-order-every-computation-without-deciding-which-dependency-failures-propagate) owns the durable mechanism.
- The bounded nonrepeatable SG4 source packet is closed. Code generation topologically orders one generated method per target field; each method owns one flattened first-selected table, and a selected operation ends that target-instance scan even when it stores nothing. [`SPEC-2026-07-26-03`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-26-03--same-target-computations-flatten-into-one-first-selected-table) carries the resulting peer correction and selected-empty String separator.
- The checked String run now admits repeated supplied targets by consolidating their guarded rows at the first target position before the existing dependency check. Exact row concatenation is proved; selected-empty clearing, false-guard fall-through, selected-value termination, and final application are executable through the ordinary run boundary.
- The checked Number run likewise consolidates repeated supplied targets at their first position and preserves guarded-row encounter order before the existing scalar/dependency plan. Selected value and selected invalidity terminate the target scan; only a false guard reaches a later row. Legal authored tables share the model-derived full target policy, while the lower-level policy-attachment seam fails closed if two same-target tables disagree on any retained constraint.
- One bounded heterogeneous nonrepeatable run now executes a checked scalar Number producer before a checked String `FieldValueAsString` consumer. The Number outcome and both family result views remain separate; the reached consumer sees canonical computed stored text, clean no-value hides stale source state, invalid Number outcomes become cause-blind computed-dependency poison, an earlier selected String row leaves an unread dependency inert, and an ordinary Number in the same expression still uses the immutable document's formal-read regime. The composition is source-grounded and internally closed; retained composed Kernel evidence remains pending.
- The inverse bounded run executes a checked String producer before a checked scalar Number `FieldValueAsNumber` consumer. Accepted String completion supplies its exact checked stored text, clean no-selection becomes the established numeric zero rather than Number clearing, reached target rejection becomes cause-blind dependency poison, and an earlier selected Number row leaves an unread conversion inert. Both family outcomes and result projections remain separate. The two completed directions now share only the immutable document's scalar context projection; their plans, typed outcomes, and results remain family-specific. Retained composed Kernel evidence remains pending.
- A finite supplied-order mixed scalar run now composes preconsolidated checked String and Number tables. Every step, completion, fault, and outcome retains its family; pending targets hide stale source cells, and completed outcomes are projected only at the reached consumer context, so accepted Number is canonical text for String computation and numeric for Number computation. Both alternating three-step directions, ordinary inputs, reached poison, unread suffix, duplicate targets, and forward dependencies are executable. One constructor-preserving partition now routes a successful run through the established String and Number result owners without manufacturing a common instance, error, message, or collection-order contract. Changed, unchanged, cleared, numeric-zero, rejected, and inherited-poison separators retain the two source-relative family views. Composed Kernel evidence remains pending.
- Repeatable Number execution now has universal target-field ownership, self-reference, and exact output-address uniqueness spines. Checked documents retain physical-row uniqueness; actual-row projection preserves distinct complete environments; a unique checked target scope makes route coverage addresses distinct; executable-target filtering and table execution preserve them; and the finite append fold combines per-table uniqueness with the plan's distinct target fields. Each complete checked operation certifies that neither its guard nor expression reads its target, and every checked table inherits that exclusion. Exact uniqueness does not make private supplied-table/row traversal order a public collection-order contract.
- Retained-result application now distinguishes direct no-value outcomes from source-classified CLEARED actions across the existing Number, String, Time, Date, and DateTime application owners. CLEARED creates or retains a present-empty scalar target; direct error/no-value/poison transitions still leave an absent target absent. The destination-indexed Number projection additionally places changed VALUE and CLEARED at exact repeatable addresses and records directly addressed missing ancestry. a12-dmkits `f6dc22d2` locks the six-row Number matrix across both kernel strategies; generic kernel application source establishes the type-neutral action boundary and deeper directly addressed ancestry. The projection makes no claim about unobserved predecessor padding or created-row order, and the unindexed retained view leaves source/destination model compatibility as a caller precondition.
- One same-scope repeatable `FieldValueAsString` placement now selects actual target environments, reads the exact addressed Number through the checked document's decimal-versus-String formal-text regime, and retains each `CellAddr` through String result classification and application. The matrix separates two row keys, absent/present-empty, row-local poison, target rejection, and retained clear into an absent destination. a12-dmkits `f6dc22d2` supplies strict static admission and broad three-engine route execution through its existing depth-shaped profile; the exact matrix remains internally locked rather than separately Kernel-calibrated.
- The inverse same-scope repeatable `FieldValueAsNumber` leaf reuses the certified conversion source, checked document, scalar conversion evaluator, Number target policy, and exact result/application owners for the exact `[0-9]+`/max-15 String profile, stored Enumeration, and named category projections. Kernel-confirmed authoring admits all three placements; the measured signed-pattern String alternative is rejected, so conversion failure after formal success is absent from that String fragment. The addressed matrix separates stored `1` from category `.5`, absent/present-empty zero, exact valid values, row-local formal poison, and target rejection. Current `DM_INTERPRETER` reruns confirm the value classes with canonical row pointers; cause identity remains internal.
- The same-scope repeatable `RangeAsNumber` leaf retains its exact 1-based inclusive interval and evaluated String source, then reuses the scalar range evaluator, Number target policy, and exact result/application owners. It and `FieldValueAsNumber` share the proof-bearing target/source placement and addressed execution seam demonstrated by both completed users. Kernel-confirmed authoring admits `RangeAsNumber(Code, 2, 4)`; the matrix separates selected digits, filled fallback zero, missing zero, row-local formal poison, target rejection, and exact addresses. Current `DM_INTERPRETER` reruns confirm those value classes with canonical row pointers.
- The same-scope repeatable String `Length` leaf is the third user of that checked placement and execution seam. It consumes the declaration-checked normalized String once, returns real zero for absent/present-empty cells, counts UTF-16 code units, and retains row-local poison, target rejection, source-relative results, and exact addresses through the existing Number owners. Accepted `SPEC-2026-07-23-15` locks exact repeatable authorability and poisoned-index execution; the clean UTF-16 value/address matrix remains internally locked rather than separately Kernel-calibrated.
- Same-scope repeatable direct Number assignment is the fourth user of the shared addressed placement and execution seam. It certifies a Number source and exact source/target scale, reuses the scalar field evaluator, and retains empty-as-zero, value, row-local poison, target rejection, coefficient-plus-scale source identity, and exact addresses through the existing Number owners. Maintained a12-dmkits revisions `dc9ab325` and `44a9e29d` tri-check direct repeatable values and poison; exact pointers and the focused empty/target-error/V2-identity matrix remain internally locked.
- Same-scope repeatable `Abs(Number)` wraps that exact direct-Number certificate rather than introducing another source representation, delegates one absolute-value node to the existing scalar semantics, and retains negative/positive value, empty zero, row-local poison, target rejection, source-relative classification, and exact addresses. Current-version reassessment at clean a12-dmkits `27c1e583` reconfirms the scale-2 admission and scale-mismatch rejection through the real-kernel gate and the focused value/outcome classes at exact repeatable instance pointers through the explicitly identified `DM_INTERPRETER` route; it is not dynamic/static kernel runtime evidence.
- Same-scope repeatable `RoundDown`, `RoundUp`, and `RoundAccounting` share the certified direct-Number source while retaining mode, places, and the operation-specific result-scale contract. The exact addressed matrices separate floor, ceiling, and half-up on signed/tie values plus empty zero, row-local poison, target rejection, source-relative classification, and result application. The same current reassessment reconfirms omitted-place scale 0, explicit-place scale 1, the mismatched-target rejection, and exact row-addressed interpreter outcomes without mistaking them for kernel runtime.
- Same-scope repeatable `Min`/`Max` now retain every nonempty ordered list of direct-Number, operand-local-`Abs`/Round, or one operand-local direct-field addition certificate plus at most one immediate decoded literal inserted anywhere. Addition reuses `CheckedAddressedNumberPair`, retains both inner dependencies and their order, contributes the maximum inner source scale, and delegates ordered row reads plus scalar addition without embedding the target-owning binary computation or introducing a generic expression tree. The outer fold retains exact operand order, maximum transformed/literal scale, empty-as-zero, first reached poison, target policy, result partition, and exact addresses. Current real-kernel preflight admits both outer positions and both inner orders, a literal sibling, and an independently admitted literal-bearing addition child; it separates scale mismatch and target self-reference. Row-addressed `DM_INTERPRETER` output locks clean selection, a direct-field winner, empty left/right/both, poison class, and target rejection; exact first-cause identity remains internal because `cleared` is cause-blind.
- Same-scope repeatable two-operand `+`, `−`, and `×` reuse the ordered Number-pair certificate and shared exact target execution. Additive nodes derive maximum operand scale; multiplication derives the scale sum. The addressed matrix separates authored order, empty-as-zero on either side, all-empty zero, first-cause poison order, target rejection, source-relative change/clear/error, and exact addresses while delegating arithmetic to the already-separated precision-50 scalar owner. Current-version real-kernel preflight reconfirms scale-1/scale-2 addition/subtraction at target scale 2, multiplication at scale 3, and the scale-1 product rejection; row-addressed `DM_INTERPRETER` output reconfirms clean, empty, poison-class, and target outcomes without exposing first-cause identity or kernel runtime.
- Same-scope repeatable division reuses the ordered Number-pair certificate, retains the mandatory exact-scale warning suppression, delegates quotient evaluation to the precision-50 scalar owner, and selects only its placement's warning-suppressed target checker. The addressed matrix separates authored order, empty numerator zero, empty denominator domain invalidity, both-empty domain invalidity, first-cause poison order, no-fit stored error, ordinary target rejection, source-relative clearing, exact valueless-error pointers, and exact addresses. a12-dmkits `112169fa` restores structured suppression authoring: unsuppressed division is kernel-rejected, the suppressed form persists and passes `model check`, and the clean `DM_INTERPRETER` representative is `10 / 4 = 2.50`. The maintained scalar quotient differential remains the owner of the broader matrix, both kernel strategies, and generated Java.
- Same-scope repeatable field-exponent power reuses the ordered Number-pair certificate, certifies the exponent's scale 0 independently from the mandatory unknown-result-scale suppression, delegates staged and reciprocal-first arithmetic to the scalar owner, and selects only its placement's warning-suppressed target checker. The addressed matrix separates empty-as-zero base/exponent behavior, `0 ^ 0`, negative powers, `±1000` boundaries, `0 ^ -1` and `1001` domain invalidity, first-cause poison order, no-fit storage, ordinary target rejection, source-relative clearing, exact valueless-error pointers, and exact addresses. The same current route persists a suppressed scale-0-field exponent computation, passes `model check`, and computes `10 ^ 2 = 100.00`; maintained scalar differentials still own staged precision, bounds, failure projection, and dual-kernel runtime evidence.
- The bounded Analyze/Transform probe over the checked addressed Number family distinguishes ordered expression reads, target-state classification read, and target write, then retains repeatable scope, full target policy, leaf parameters, and extremum operation/order/cardinality/derived scale plus exact direct/local-`Abs`/local-Round/addition/literal identity. An addition fingerprint flattens both ordered inner dependencies into the read set while retaining one outer operation node and position; reversed inner operands, moved outer position, and direct-versus-addition remain distinct. Exact identity is the sole Transform and preserves the complete nested fingerprint plus rich execution/result views. The probe adds no evaluator, rewrite engine, solver, protocol, command, or shipment.
- The checked SG10 authoring boundary now retains two producer-specific grammars over one one-pass renderer. The bounded rule fragment admits `$$`, `$Field$`, and `$Field.value$` with one bare referenced nonrepeatable field. The measured `en_US` String-pattern field fragment admits only fixed lowercase `$field$`/`$field.value$`, rejects `$$`, lowers already-selected owning-field bytes directly, and preserves the established pattern failure; a12-dmkits `ed0b0cd9f7110daa354c2404e7b9ca6df4f42fa3` locks both kernel strategies and the distinct requiredness control. Wider rule paths, locales, provider invocation, requiredness grammar, and other field producers remain explicit insufficiency. a12-dmkits still needs the isolated rule-renderer literal-dollar correction recorded under SG10, but that does not block local semantics.
- Validation, computation, registered custom-field, and reached custom-condition error channels now share one normalized `MessagePointer` with resolved field identity and concrete/wildcard/unknown repetition coordinates. Exact `CellAddr` embeds losslessly but remains a separate type; wildcard and unknown do not project as exact cells, and computation partitioning compares complete pointers structurally. a12-dmkits `61705844` anchors the pointer-domain split. Raw name/index factory asymmetries, root accessors, conversions, and validation `referenced`/`fillToFix` collections remain under SG10.
- The first proof-bearing internal Analyze pilot is closed over the existing checked flat tree. It detects only the exact root-level same-field presence contradiction in either authored order and proves that every returned witness is never firing; it adds no IR, recursive simplifier, solver, command, protocol, or shipment.
- Repeated starred group-list admission and its zero-row polarity are reconciled under [`SPEC-2026-07-23-09`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-23-09--fixed-group-list-predicates-admit-field-and-group-operands). The bound-prefix static rule under [`SPEC-2026-07-26-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-26-01--starred-group-list-iteration-guards-are-operator--and-operand-sensitive) is accepted at a12-dmkits `b9ade32d300c2d10bb70ac7dc18daf7a6779a9ec`, and the nonrepeatable-terminal composition under [`SPEC-2026-07-26-02`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-26-02--a-nonrepeatable-group-below-starred-ancestors-retains-group-product-semantics) is accepted at `8cab6ece3a0b355e1a831792fa14b0b711548755`.

## Active semantic unit

Audit operand-local subtraction `[Left] - [Right]` inside same-scope repeatable `Min`/`Max` beside one direct Number field. Treat it as the second completed user needed to decide whether the addition-specific operand variant should consolidate into a bounded additive-operation tag over one shared checked pair; do not generalize to multiplication, division, power, nesting, or a recursive expression tree.

## Immediate sequence

1. Measure subtraction in both outer positions and both inner orders, its empty/poison-class rows, derived scale, literal sibling, target self-reference, and nearest rejected child through the current structured dmtool route; keep first-cause identity internal unless a public cause channel appears.
2. Add red cases beside the focused addition-inside-extrema conformance owner. Reuse the same pair evaluator and scalar subtraction; do not nest the target-owning addressed binary operation.
3. Only after the subtraction cases are green, assess whether addition and subtraction have exactly the same operand representation, dependency, placement, scale, and consumer identity domains. If so, consolidate to a bounded enum-based additive tag in the same change and specialize the laws; otherwise retain distinct variants and state the semantic difference.

## Parked boundaries

- Eager prepass invalidity needs no second snapshot because `CheckedDocument` already owns computation-phase checked cells. Its supplied messages enter the completed SG4 partition directly; no eager-prepass reconstruction or SG10 rendering belongs in that structural boundary.
- Computation scheduling, state transition, and the computation-result pointer partition remain under [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition); message rendering remains under SG10; future operator-family admission and projections remain under their owning semantic gaps.
- Fixed multi-group `NumberOfFilledGroups` as a Number computation remains at the explicit `unsupportedGroupCount` boundary. Source audit yields rival zero-versus-clear accounts for an unavailable group, and no current unchanged a12-dmkits computation probe measures that exact value-producing shape; [`SG13`](SEMANTICS-GAPS.md#sg13--group-list-and-group-count-completion) owns the seeded-target separator and exact revision anchors.
- The a12-dmkits rule-template `$$` renderer correction is an isolated upstream implementation/test request against an already-correct canonical rule clause. The accepted field-owned experiment proves that String-pattern `$$` rejection and requiredness preservation are distinct behavior, not substitutes for that rule fix.
- Field-owned message locales beyond the measured `en_US` String-pattern producer, complete requiredness grammar, provider invocation, empty-value fallback, and other producer channels remain under SG10; they do not block SG4 computation work.
- Public protocol expansion, semantic shipments, dependencies, SMT integration, and new evidence/process machinery require their existing explicit adoption or approval gates.

## Stop and resume

- Stop if a required model fact or separating witness is absent, if an existing owner already closes the candidate, or before introducing a duplicate representation or unapproved infrastructure.
- Keep sibling repositories read-only and visibly unchanged. Never push without an explicit current request.
- On resumption, read the active semantic unit above, then inspect its implementation owner, recent history, cross-project handoffs, and applicable [`TESTING.md`](TESTING.md) rung before writing a red case.
