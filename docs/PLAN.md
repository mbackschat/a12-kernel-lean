# Active implementation plan

This is the minimal continuation checkpoint. Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

**Priority invariant:** eventual 100% semantic conformance with kernel 30.8.1 is the primary project goal. Consumer adequacy is the co-equal representation test. A supported-fragment boundary may stage delivery or state current evidence honestly, but may not become a permanent reason to omit kernel behavior.

## Verified baseline

- The current theory closes substantial checked nonrepeatable validation/computation, temporal, String, Enumeration, custom-field, resolved repeatable, message, and target fragments. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) is the sole detailed capability/proof/evidence inventory.
- The future computation-scheduling and document-transition architecture is binding in [`ARCHITECTURE.md`](ARCHITECTURE.md#whole-model-computation-execution-keeps-definition-activation-result-application-and-validation-separate); its accepted rationale is [archived](archived/A12-LEAN-COMPUTATION-EXECUTION-PROPOSAL.md). Adoption records the boundary but does not select SG4: SG1 and SG2, the explicit processing context, and the bounded source audit remain prerequisites.
- The latest repeatable work derives one proof-bearing general star plan from validated model ancestry and feeds a canonical nested reopened tree plus deepest-level-fastest terminal stream from explicit document rows. Field and group terminals share that plan. The source-closed Number/`CurrentRepetition` `Having` fragment filters before retained target classification; checked Number and String/ordinary stored-Enumeration `FirstFilledValue` lazily interleave mixed-slot topology, filtering, per-cell relevance, and prefix decisions through one kind-neutral scan. Checked Number value lists consume one nested plain or filtered starred fields side against direct nonrepeatable Number value fields; checked String routes cover starred fields against literals and one direct field against starred values; checked Enumeration covers one stored/category-projected star against selected-domain literals. Each starred value-list route accepts an optional checked `Having`. These routes share declaration-owned star reads, filter-before-classification, hierarchical tail state, per-cell relevance, and wildcard/ancestor extent before the existing asymmetric quantifier core. Checked nested RNU derives one deepest-row topology, accepts mixed Number, ordinary String, and direct stored-Enumeration components at ancestor-or-equal depths on one group branch, preserves each component's checked path prefix plus typed equality and authored order, selects the default or explicit `@From` scope, and applies all-components partial relevance before using the existing resolved relation. The terminal-repeatable group route counts canonical terminal rows without reading descendants and drives `NoGroupFilled(G*)`, `AtLeastOneGroupFilled(G*)`, and `NumberOfFilledGroups(G*)`, including created-empty and over-limit rows.
- The authored-filter capsule adds 522 and removes 81 nonblank Lean lines, exceeding the Tier 1 target because the checked nested route requires one complete surface traversal, origin-specific scope admission, authored-shape and reopened-level certification, the existing runtime delegation bridge, and separating failures together; splitting it would temporarily expose an unchecked core route or preserve the duplicate correlation connective mechanism that the capsule removes.
- The checked mixed-`FirstFilledValue` capsule adds 437 and removes 11 nonblank Lean lines, exceeding the Tier 1 target because the kind-neutral reference pass, ordered static gates, Number certification, lazy runtime route, shared continuation and duplicate scans, authoring laws, and separating matrix form one usable boundary. Subsequent kernel calibration corrected its source-only omitted-tail inference: a reached flat or nested no-row star is a not-given prefix before a later fallback. Splitting the route would land either an unconsumed checked representation, incorrect diagnostic precedence, or an unchecked runtime route. It adds no general document/result abstraction, harness, protocol, or support estate.
- The checked terminal-repeatable group-star capsule adds 377 and removes 36 nonblank Lean lines, exceeding the Tier 1 target because extracting the shared proof-bearing star plan, adding the sole-star checked source, proving its tally/topology delegation, and locking the static and runtime separators form one usable boundary. Splitting it would retain duplicated star legality or land an unchecked source without its consumers. It adds no general document/result abstraction, harness, protocol, or support estate; the current ratio is 33,897 theory, 5,534 support, and 1,530 evidence nonblank Lean lines.
- The checked nested-star Number value-list capsule adds 361 and removes 5 nonblank Lean lines, exceeding the Tier 1 target because checked direct-value admission, plain/filtered topology composition, exact quantifier delegation, proofs, and the separating matrix form the first usable authored repeatable list boundary. Splitting them would land an unchecked runtime route or a checked representation with no consumer. It introduces no evaluator, topology, filter, scan, document/result abstraction, harness, protocol, or support estate; the generic field-ID duplicate detector moved down to the checked-model layer and retains the aggregate forwarding API.
- The checked nested Number RNU capsule adds 448 nonblank Lean lines, exceeding the Tier 1 target because model-owned composite-key admission, default and explicit scope construction, all-components relevance-before-read, exact resolved-relation delegation, proof certificates, and the static/runtime separating matrix form one usable checked boundary. Splitting them would land either caller-authored rows without checked topology or a checked source with no semantic consumer. It introduces no second evaluator, topology, relevance model, checked-document/result abstraction, harness, protocol, or support estate.
- The heterogeneous RNU capsule adds 241 and removes 114 nonblank Lean lines. It replaces the Number-only checked wrapper with one kind-indexed Number/String key list, extracts the normalized String star classifier at its second consumer, and retains one topology, relevance gate, and resolved evaluator. Three runtime separators and three adapter laws distinguish exact String spelling and mixed tuples; unsupported key kinds and prepared custom Strings fail explicitly. It adds no evaluator, topology, document/result abstraction, harness, protocol, dependency, or support estate.
- The ancestor-key RNU capsule adds 166 and removes 75 nonblank Lean lines. It replaces exact-group equality with a checked one-branch relation, selects the deepest key as the sole row topology, certifies every component path as its prefix, projects the deepest environment to that prefix before each read, and preserves the existing typed key list, relevance gate, and resolved evaluator. One paired separator rejects an unprojected ancestor read, makes the ancestor value distinguish otherwise equal deepest values, and then restores the duplicate by equalizing that ancestor; the static matrix also rejects an `@From` group that contains only the deepest key. It adds no address model, reader, evaluator, document/result abstraction, harness, protocol, dependency, or support estate.
- The stored-Enumeration RNU capsule adds 130 and removes 13 nonblank Lean lines. It extends the existing heterogeneous checked key sum, moves exact successful-declaration retention to the Enumeration owner at its second consumer, and delegates stored-token/domain classification to the existing Enumeration RNU bridge. Three runtime groups separate stored-token duplicates from many-to-one category collisions, invalid-domain exclusion, and mixed Enumeration/Number tuples. It adds no equality relation, duplicate evaluator, topology, relevance model, raw-value framework, document/result abstraction, harness, protocol, dependency, or support estate.
- The per-cell partial value-list relevance capsule adds 906 and removes 148 nonblank Lean lines, exceeding the Tier 1 target because the shared classified operand side, the flat token and Number consumers, the nested-star Number consumer, the correction of a disproved whole-tree refinement theorem, and their separating laws and cases form one semantic root fix. Splitting them would retain the invalid theorem, expose an unconsumed classified representation, or leave nested-star evaluation on the old all-or-nothing relevance gate. It introduces no second quantifier evaluator, value-list AST, topology, checked-document/result abstraction, harness, protocol, dependency, or support estate.
- The checked nested String-star/literal-list capsule adds 410 and removes 36 nonblank Lean lines, exceeding the Tier 1 target because its checked surface, String-specific normalized reader, full and partial consumers, proof/conformance pair, second-consumer extraction of generic over-limit/relevance-before-read star mechanics, and source-discovered wildcard-extent correction for both typed routes form one usable boundary. Splitting it would duplicate those star mechanics, land an unchecked/unconsumed type, or preserve the wrong concrete-all-rows account. It introduces no second quantifier evaluator, topology, relevance model, filter, checked-document/result abstraction, harness, protocol, dependency, or support estate.
- The checked String-star `Having` capsule adds 250 and removes 72 nonblank Lean lines. Its second typed use moves filter-before-classification and the partial filtered-rule result to the existing correlation owner, while Number and both String-side shapes delegate through that common mechanism. It introduces no second filter tree, selector, quantifier evaluator, checked-document/result abstraction, harness, protocol, dependency, or support estate.
- The checked Enumeration-star/literal-list capsule adds 410 and removes 7 nonblank Lean lines across one exact projection-owning surface, its proof/conformance pair, six trusted laws, and eight separating cases. Its second token-kind use moves optional-filter dispatch to the existing checked-star/correlation owner; splitting selected-domain admission from runtime projection would leave either an unchecked route or a checked type with no consumer. It reuses the settled Enumeration classifier and every shared star/filter/relevance/quantifier mechanism and introduces no second evaluator, topology, filter tree, protocol, process, dependency, or support estate.
- The complementary direct-Enumeration/starred-Enumeration-values capsule adds 241 and removes 2 nonblank Lean lines. It promotes the existing exact nonrepeatable Enumeration resolver for its second real consumer, then reuses the flat direct side and the checked starred projection for four laws and six values-side separators. It adds no second projection, evaluator, topology, relevance model, filter, protocol, process, dependency, or support estate.
- The checked mixed-condition baseline is fully green. One model-indexed connective tree serves flat and resolved numeric-comparison leaves with universal flat verdict/reference preservation. Flat and numeric certificates retain their exact row group, checked mixed composition rejects mismatches, and flat/mixed whole rules share one post-verdict metadata and message emitter. Numeric comparison admission separates ordinary same-group rules from generated model-wide scalar operands and model-certified repeatable first-filled sources, while literal and checked-expression computations share one nonempty table, first-match selector, common placement, and all-alternatives generated validation path.
- The shared checked condition/expression gap is closed. Direct Number `Sum`/`MinValue`/`MaxValue` lists now enter the same numeric atom algebra after at-least-two and duplicate-before-kind checks, delegate validation and computation observations to one resolved aggregate source, retain reference/relevance/scale metadata, and flow through ordinary whole rules plus every generated-validation alternative without a parallel comparison, evaluator, or scale gate. The capsule adds 490 and removes 67 nonblank Lean lines, exceeding the Tier 1 target because splitting the exhaustive atom constructor from validation, computation, generated validation, delegation proofs, and the separating matrix would land either an unusable representation or a consumer-specific partial route—the duplicate mechanism this capsule closes.
- The checked mixed Number aggregate-star capsule adds 447 and removes 185 nonblank Lean lines. It extracts the Number entity-list checker from `FirstFilledValue` at its second completed consumer, preserves duplicate-before-cardinality-before-kind admission, admits every repeated plain or filtered wildcard occurrence as an independent authored slot, lazily stops before later slot topology after a formal failure, and delegates the accumulated side once to the established `Sum`/`MinValue`/`MaxValue` folds. Repeated-star consumption, mixed direct/star precision order, extrema, direct-duplicate rejection, and unavailable-prefix termination form the separating matrix. Splitting the change would retain the duplicate checker or land an unconsumed shared owner. It adds no aggregate evaluator, topology, filter tree, checked-document/result abstraction, harness, protocol, dependency, or support estate.
- The partial mixed Number aggregate capsule adds 213 and removes 35 nonblank Lean lines. Full and partial routes now share one authored-order aggregate scan and one dual fold-input accumulator; partial evaluation preserves a distinct rule-level filtered skip, direct nonrelevance, all-rows star nonrelevance, and formal unavailability before delegating an available result to the existing folds. Concrete enumeration of all rows, wildcard coverage, ancestor coverage, early filtered skip, and unavailable/nonrelevant prefixes before malformed later topology form the separating matrix. The boundary remains a resolved leaf and does not claim global augmentation, error-field gating, cross-branch filter discovery, or whole-rule composition. It adds no relevance model, aggregate evaluator, topology, checked-document/result abstraction, harness, protocol, dependency, or support estate.
- The Number distinct-count capsule adds 226 and removes 6 nonblank Lean lines. It extends the existing numeric aggregate operator family rather than adding another source shape, counts filled representatives through the shared scale-19 equality, fixes result scale at 0, retains grow-only empty/tail uncertainty and filter-driven both-directional uncertainty, and inherits the same lazy mixed-slot and partial all-rows gates. Direct validation, computation, mixed/repeated-star, normalized-equality, poison, result-scale, and concrete-versus-wildcard separators all use the existing checked expression and entity-list owners. String/Enumeration/date-like overloads, groups, and `SumOfProducts` remain outside. It adds no operand representation, topology, relevance model, checked-document/result abstraction, harness, protocol, dependency, or support estate.
- The checked `SumOfProducts` capsule adds 491 and removes 28 nonblank Lean lines. It exceeds the Tier 1 target because its source-grounded contract is one vertical semantic boundary: the exact checked two-field/same-group/lowest-star surface, shared-topology row construction, staged arithmetic and fillability fold, phase-indexed checked-cell reader, full/partial validation, computation-result projection, laws, and adversarial matrix. Splitting before the checked-cell consumer would retain a raw-only representation that cannot express contextual requiredness; splitting the pair from evaluation would land an unconsumed topology certificate. It introduces no second star topology, entity-list scan, arithmetic core, relevance model, checked document, scheduler, target boundary, harness, protocol, dependency, or support estate.
- The String/ordinary stored-Enumeration distinct-count capsule adds 837 and removes 148 nonblank Lean lines. It exceeds the Tier 1 target because this is one usable checked vertical boundary and the second real consumer justified extracting the common direct/plain-star/filtered-star entity-list shape plus lazy resolved-side scan from the Number-only owner. The family-specific layer then closes mixed token admission, exact evaluated-token identity, full/partial evaluation, result scale, proofs, and the separating matrix while deleting the duplicated Number shape and scan. It adds no topology, evaluator tree, checked document/result boundary, scheduler, target integration, harness, protocol, dependency, or support estate. The date-like overload is not guessed: a peer checker/evaluator mismatch and unresolved TIME identity are parked for committed upstream handback.
- The String/ordinary stored-Enumeration `FirstFilledValue` capsule adds 718 and removes 252 nonblank Lean lines. It exceeds the Tier 1 target because the second token-family consumer moves checked token entity-list admission and declaration-owned classification into their common owner, generalizes the established prefix scan by value-list kind, and lands one complete lazy checked consumer with laws and separators. Splitting it would retain distinct-count ownership of shared token admission or land an unconsumed generic scan. The capsule adds no topology, evaluator tree, checked document/result boundary, scheduler, target integration, harness, protocol, dependency, or support estate. Mixed String/stored-Enumeration selection is source-grounded and internally closed; Boolean/Confirm, temporal exact-format, and DateRange family facts remain unimplemented pending focused dual-kernel handback.
- The raw-String authoring capsule adds 388 net nonblank Lean lines for one model-owned evaluated/raw capability reused across flat comparisons/lists, String expressions, repeatable String lists, token aggregate sources, and RNU. The exceptional strict whole-rule `Length` shape becomes proof-bearing max-length metadata with no runtime condition; presence remains independent. The capsule exceeds the preferred Tier 1 line target because the capability must strengthen every existing String certificate and the forged-core gate in the same change; splitting it would leave a value-read bypass. It adds no evaluator, checked-document/result boundary, harness, protocol, dependency, or support estate.
- The ordinary String formal-policy capsule adds 309 net nonblank Lean lines. One declaration policy now owns default-forbidden versus permitted line breaks, optional min/max lengths, raw-mode legality, raw-check skipping, raw-before-normalized checking, and flat/repeatable checked-cell construction. It exceeds the preferred Tier 1 target because model legality, the shared ingestion classifier, both phase projections, and removal of the repeatable context's generic-policy bypass must land together; splitting them would leave either unchecked declarations or a second raw-to-checked route. It adds no pattern engine, target evaluator, checked-document/result boundary, harness, protocol, dependency, or support estate.
- The checked ordinary String-target capsule adds 345 and removes 267 nonblank Lean lines. It replaces the positive-single-bound target type and unsupported-line-break wrapper with the declaration policy's second real consumer, migrates the existing cascade/alternative/evidence projections to that one result boundary, and binds target plus expression under one validated model with exact payload preservation and self-reference/static gates. The gross additions exceed the Tier 1 target because splitting the semantic replacement from its existing consumers would leave either duplicate target mechanisms or a temporarily uncheckable retained evidence lane; the net theory change is 78 lines. It adds no pattern engine, scheduler, checked-document/result boundary, harness, protocol, dependency, or support estate.
- The checked `RangeAsString` capsule adds 202 and removes 16 nonblank Lean lines. It extends the existing String expression and checked operation owners, reuses the declaration-owned String-value resolver and target path, slices the normalized value by authored 1-based inclusive UTF-16 bounds, and locks field-shape → interval → value-kind diagnostic precedence, missing/overshoot emptiness, exact poison, CRLF-before-range, target integration, and the explicit half-surrogate fail-closed boundary. It adds no expression tree, target mechanism, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate.
- The checked `RangeAsNumber` capsule adds 316 and removes 13 nonblank Lean lines. It extends the shared numeric atom rather than adding an operation-specific expression tree, extracts the authored interval predicate at the second completed range consumer, and carries one normalized UTF-16 digits-only conversion through same-group validation, computation, arithmetic, generated model-wide validation, and the existing Number target. The separating matrix locks field-shape → interval → String-kind diagnostics, missing grow-only zero versus filled fixed zero, digit/non-digit/overshoot/half-surrogate fallback, CRLF-before-range, exact poison, arithmetic composition, target checking, and generated mismatch evaluation. The capsule exceeds the Tier 1 target because the checked atom is unusable unless all three existing numeric consumers and their proof/conformance roots gain the constructor together; splitting them would leave an incomplete traversal or a consumer-specific range path. It adds no target mechanism, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate.
- The direct converted-value wrapper capsule adds 115 and removes 48 nonblank Lean lines. It replaces the aggregate-round special case with one operation-specific direct-source matrix shared by validation and computation, admits rounding and `Abs` over the already-checked ordinary Enumeration/category `FieldValueAsNumber`, and generalizes the existing wrapper laws over arbitrary numeric atoms instead of adding conversion-specific theorem copies. The matrix locks authored rounding scale, selected category value, symmetric missing rounding, sign-sensitive missing `Abs`, computation zero/poison, generated-validation traversal, and a neighboring unsupported source. It adds no wrapper evaluator, numeric AST, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate.
- The direct `RangeAsNumber` wrapper capsule adds 46 and removes 13 nonblank Lean lines. It extends the same operation-specific matrix by one already-checked nonliteral scale-0 source, reuses the existing range classifier and generic wrappers, and locks filled selection, grow-only missing zero in both directions, authored rounding scale, computation zero/poison, and generated-validation traversal for rounding and `Abs`. It adds no evaluator, source type, proof theorem, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate; the existing source and generic wrapper laws already compose to the implemented result.
- The direct aggregate-`Abs` capsule adds 56 and removes 8 nonblank Lean lines. It extends the same admission matrix by the existing numeric aggregate source, applies the generic absolute-value transform only after the shared ordered fold, and locks negative Sum/Min/Max, all-empty Sum and distinct-count sign-sensitive polarity, exact formal failure, derived scale, computation zero/poison, generated-validation traversal, and the neighboring operand-list-extremum rejection. It adds no aggregate-specific evaluator, fold, theorem, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate; established aggregate and generic wrapper laws already compose to the implemented result.
- The temporal-derived numeric-wrapper capsule adds 104 and removes 32 nonblank Lean lines. It replaces the now-identical rounding/`Abs` source predicates with one kernel-matching direct-unary admission owner and extends that matrix to stored Date/Time/DateTime components, direct/range-selected Base-Year date components, and Date-only month/year differences. It locks fixed scale-0 present values, signed difference `Abs`, symmetric empty rounding versus grow-only empty magnitude, exact formal propagation, computation zero/poison, and generated-validation assembly and mismatch evaluation. Accepted a12-dmkits revision `27afb555aae29d3acd4ed04e3aea4772ae85505a` later disproved and corrected its direct numeric-`BaseYear` rejection premise. It adds no temporal wrapper evaluator, numeric AST, polarity mechanism, theorem copy, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate; established source and generic wrapper laws already compose to the implemented result.
- The root arithmetic-wrapper capsule adds 245 and removes 92 nonblank Lean lines. It replaces the direct-source-only unary admission predicate with one root rounding/`Abs` check over the complete existing plain-arithmetic fragment and delegates body authoring and runtime evaluation to their existing owners. Its original source predicate rejected immediate literals and numeric `BaseYear`; the latter was a stale overgeneralization corrected by accepted a12-dmkits revision `27afb555aae29d3acd4ed04e3aea4772ae85505a`, because the kernel constant check recognizes literal syntax rather than fixed numeric sources. The matrix locks grouped field versus grouped literal, legal and illegal division regions, scale-19 pre-rounding of a multi-term result, directional arithmetic fillability, computation domain failure and target self-reference, and generated validation. It introduces no wrapper evaluator, numeric AST, target route, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate. Exact reviewed a12-dmkits revision `f9e591a54aa4c319baa42dfb76610d7571be5ecc` is inbound provenance for the root result and enclosing-wrapper separator, so the outbound ledger remains unchanged.
- The wrapper-inside-arithmetic capsule adds 459 and removes 244 nonblank Lean lines. It replaces the separate plain/root runtime paths with one recursive unary evaluator, carries wrapper-body divisions through enclosing legacy authoring regions while preserving addition/grouping/power resets, and extracts the multiplication-lowering proof at its second unary consumer instead of duplicating the induction. Validation, computation, and generated validation lock enclosing division, addition and power separators, target self-reference, domain failure, exact poison, and direct/grouped numeric `BaseYear`; the latter incorporates the accepted `SPEC-2026-07-23-04` correction that the kernel rejects literal syntax rather than fixed number-like references. The net theory growth is 215 lines, with no new AST, evaluator family, target route, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate.
- The nested numeric value-function capsule adds 270 and removes 176 nonblank Lean lines. It recursively admits rounding/`Abs` around another wrapper or the separately checked direct-extremum form, evaluates stages in authored order through the existing unary/extremum owners, and extends the existing legacy authoring walk rather than creating another evaluator. The additions exceed the Tier 1 preference by 20 lines because two existing generic direct-extremum lowering/totality lemmas must move before the broadened recursive unary totality proof and the old plain-body authoring theorem must become an exact recursive-body delegation law; splitting those proof changes from the semantic change would leave the trusted spine temporarily false. Validation, computation, generated validation, target-reference traversal, domain failure, poison, and direct aggregate/extremum composition close together. Accepted [`SPEC-2026-07-23-05`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-23-05--numeric-operation-wrappers-compose-over-admitted-value-functions) records that a12-dmkits already owns the corresponding generic child representation and focused computation controls, so no upstream work is queued. The capsule adds no AST, evaluator family, target route, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate.
- The expression-valued numeric-extrema capsule replaces the direct-field-only canonical-fold boundary with one explicit source call marker around the existing binary runtime fold. A single source-shape scan admits complete numeric operands and independently checks each nested call's immediate/grouped-literal budget; lowering caches that source fact because arithmetic lowering may change runtime topology. Validation, computation, generated validation, wrappers, surrounding arithmetic, target-reference traversal, domain failure, poison, scale summaries, and fillability all reuse the same numeric tree and total evaluator. The capsule removes more proof/evaluator lines than it adds overall, introduces no second AST or evaluator family, and adds no checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate. [`SPEC-2026-07-23-06`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-23-06--numeric-operand-list-extrema-preserve-per-call-boundaries) requests only the exact missing peer authoring account and admission locks; the peer already preserves arbitrary expression lists.
- Reference semantics remains 0.3.0 with the V2 flat-validation and one-group-correlation suites. Internally closed but uncalibrated families remain explicitly `external evidence pending`; coherent calibration work is indexed by [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage).

## Latest capsule

The latest capsule carries concrete-profile `DifferenceInDays` through the sole checked numeric-expression tree instead of leaving the calendar algorithm as a resolved utility. One shared surface atom lowers to a distinct resolved day-difference atom that retains the selected concrete model-zone profile plus each operand's decoded local Date/DateTime label and already-resolved exact instant. Field and direct/range-selected Base-Year operands share the existing resolved date-difference boundary; static checking admits Date, DateTime, and mixed date-bearing formats with a day component and the existing year-presence rule, rejects Time, and reports an unsupported model zone before runtime. Runtime preserves formal-cause precedence, maps either relevant empty operand to symmetric fillable zero, rejects stored legacy-hybrid values explicitly, and delegates present operands to the profile's exact-input calendar-step route. Validation, Number computation, arithmetic composition, target-reference traversal, generated computation validation, scale 0, and proof/trust registration all reuse their existing owners. The pre-implementation Execute/Analyze/Compile hypothesis required a consumer to recover the checked profile and both exact identities, obtain Berlin 1 versus UTC 0 for the spring separator, preserve zero for two equal-looking Berlin overlap labels whose retained instants are one hour apart, and distinguish unsupported profile admission from semantic `UNKNOWN`; the completed APIs answer that query without re-resolving an ambiguous wall label or flattening structural failure into a value. The capsule adds 562 and removes 25 nonblank Lean lines (net +537). It exceeds the Tier 1 preference because one new shared numeric constructor must remain exhaustive across validation, computation, generated validation, proof, trust, and their separating conformance matrices; splitting that vertical change would leave a partial traversal or a second consumer-specific route. It adds no AST family, calendar framework, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate. The canonical §5/§9 account and accepted `SPEC-2026-07-20-08` already own this behavior, so no spec-sync entry is created; wider temporal operands, pre-floor constructed values, other model-zone profiles, and retained project-local correspondence remain under SG6/SG12.

Reconcile the requested `FirstFilledValue` family audit when its committed handback arrives; it must settle Boolean versus Confirm admission, exact temporal format matching, returned value identity, and all-empty computation behavior, but it does not block unrelated source-grounded work. String/stored-Enumeration and Number `NumberOfDifferentValues` are closed; its date-like overload is parked pending exact format/TIME identity. The remaining RNU kinds still require exact stored text that the parsed cell boundary does not retain; prepared custom String keys require SG1's checked document cache. Multiple/mixed value-list operands remain parked pending the focused filter-encounter differential, while mixed group operands, nonrepeatable terminal groups, and general scalar group-state construction remain blocked on SG1/SG2. Ordinary pattern-bearing String targets, literal/direct/filtered-`FirstFilledValue` closed-Enumeration targets, and addressed repeatable Number entity-list, Number `FirstFilledValue`, plus `SumOfProducts` expressions into ordinary Number targets and generated validation are internally closed. Wider whole-rule repeatable orchestration still requires SG1/SG2, custom/repeatable String targets remain under SG7, and message value interpolation remains in SG10 pending a checked-template owner. Do not define whole-document construction or begin SG4.

## Immediate frontier

1. **Apply the high-connectivity selection gate — [`TESTING.md`](TESTING.md#high-connectivity-keystone-pre-audit):** before selecting SG1, SG2, SG4, SG6, or another keystone, prepare the bounded source packet and decision matrix, then run the most dangerous discriminator before representation work. Current discriminator candidates are row existence versus cell presence for SG1, absent-row identity and filter order across nested levels for SG2, short-circuit reachability versus dependency-transitive skipping plus empty computed-error ownership for SG4, and exact-instant/profile/cutover behavior for SG6. SG4 remains prohibited until its adopted prerequisite and source-audit gates are complete.
2. **Ready bounded continuation — [`SG6`](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion):** audit checked `DifferenceInHours/Minutes/Seconds` integration as the next candidate because the resolved exact-instant evaluator is already closed and the new day-difference operand route may be its second checked consumer. Select it only if the source packet confirms the static gates, operand breadth, empty/formal behavior, and one dangerous millisecond/truncation separator without requiring wider temporal construction or target state.
3. **Missing representation — [`SG1`](SEMANTICS-GAPS.md#sg1--general-checked-document-construction) / [`SG2`](SEMANTICS-GAPS.md#sg2--general-repeatable-addressing-and-operand-construction) / [`SG9`](SEMANTICS-GAPS.md#sg9--paths-indices-and-static-legality-completion):** temporal and Boolean/Confirm RNU keys plus non-Number semantic-index construction require exact internally stored text, not parsed semantic-value equality; prepared custom String keys require the document-owned checked observation cache. Number semantic-index construction is now closed for one single-group route with literal or ordinary nonrepeatable Number field keys; repeatable field keys still require row-addressed SG1/SG2 context. Accepted [`SPEC-2026-07-22-10`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-22-10--repetitionnotunique-compares-every-non-number-key-by-stored-text) and pending [`SPEC-2026-07-23-08`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-23-08--semantic-index-number-keys-compare-by-normalized-numeric-value) settle the known equality facts, but not Lean's missing raw-text owner.
4. **Missing fact — [`SG2`](SEMANTICS-GAPS.md#sg2--general-repeatable-addressing-and-operand-construction):** multiple/mixed value-list operands need the requested upstream fields/value-side `Having` encounter matrix before replacing side-wide filter metadata. Fixed nonrepeatable field/group lists are closed; mixed starred/plain group lists and nonrepeatable terminal groups still require SG1 descendant group state.
5. **Missing source or representation facts — [`SG6`](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) / [`SG7`](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion):** date-like `NumberOfDifferentValues` still needs exact compatible-format and temporal identity behavior; temporal targets need declaration-owned format/rendering/value admission. Ordinary nonrepeatable String line-break/pattern/min/max target construction, arbitrary declared-pattern source reads, both range coercions, and complete ordinary `FieldValueAsNumber` are closed.
6. **Milestone calibration — [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage):** batch external evidence only after a coherent family closes. Do not create one producer or harness per capsule.

The repeatable aggregate expression/target boundary has passed its in-context checked-client Execute/Analyze/Transform probe; [`USE-CASES.md`](USE-CASES.md#completed-probe-record-resolved-number-aggregates) owns the result and exact limits. The addressed generated-validation family covers first-filled, entity-list aggregate, and row-product sources and has passed each pre-implementation hypothesis plus same-context closure query. The checked day-difference integration now passes the same discipline for Execute/Analyze/Compile. A bounded artifact-only cold probe remains due only when the checked temporal-difference family becomes a reusable capability milestone; every selected capsule first follows the source packet, compact hypothesis, and same-context assessment defined in [`TESTING.md`](TESTING.md#high-connectivity-keystone-pre-audit).

## Parked non-semantic boundaries

- Public protocol expansion, semantic shipments, candidate qualification, dependencies, SMT integration, and new evidence/process machinery require their existing explicit adoption or approval gates.
- Flat literal-scale or other protocol-sensitive seams require deliberate protocol versioning or a separately adopted public-boundary change.
- No copied a12-dmkits harness, second numeric AST or scale gate, second aggregate scan, alternate group-state encoding, or parallel protocol route is permitted.

## Stop conditions

- Stop a candidate when its required model fact, consumer, or separating source witness is absent; update its one [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry rather than creating another queue item.
- Stop before introducing a duplicate semantic representation or any harness, schema, generator, protocol, registry, dependency, or governance layer without explicit approval.
- Stop before beginning [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) or freezing its general checked-document/result types until the accepted decision's checked-document, repeatable-addressing, processing-context, and bounded source-audit gates are complete and `PLAN.md` explicitly selects the first source-grounded scheduling capsule.
- Keep sibling repositories read-only and preserve their visible status.
- Never push without an explicit current request.

## Resume procedure

1. Read this file, the selected [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry, its linked [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) boundary, and the applicable [`TESTING.md`](TESTING.md) rung.
2. Inspect `git status --short`, recent commits, the current owner/code/tests, Git history, and any active cross-project handoff before writing a red case.
3. Close one capsule or verified no-op, perform the same-context closure assessment, run its proportional gate, update only fact owners, delete a completed gap, and commit.
