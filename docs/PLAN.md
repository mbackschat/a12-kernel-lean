# Implementation handoff

This is the cross-session resumption checkpoint, not an in-session task list or work log. Leave it unchanged during uninterrupted route discovery and capsule execution unless another session must resume a different selected action, a blocker must persist, or a consumer-probe trigger changes.
Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

## Verified baseline

- `basis`: current committed Lean state; Git owns the exact revision.
- `gate`: Tier 1 passed for that Lean state.

<a id="active-unit"></a>
## Selected work

- `state`: the checked semantic-index source now carries the declared index declaration rather than a Number field, so any index kind is admitted with one dispatch between the numeric-value and exact-text identities. The shared repeatable numeric and String placement now admits a computation operand at any scope the computed target's scope binds, matching the measured Kernel rule, and reads each operand at its own path, so one form-level cell reaches every target row; both halves are Kernel-calibrated on the two codegen strategies, including a two-level target where a middle-level operand reaches exactly the leaves of its own enclosing row. The validation-side DateRange condition leaf carries the same two-level locus, admitting an operand at the root, the enclosing row, or the leaf and correlating each to its own enclosing row. All eight admitted DateRange declaration pairs classify stored text, execute the direct single-level starred `FirstFilledValue` route with component-set crossing, and gain bound extraction once Base Year completes them. Both overlap operators now gate their whole operand list by year class, matching the measured Kernel gate, and a mixed year-class list needs no bridge between the resolved and yearless interval domains. The plural family owns its own module, and both operators are closed over the unconfigured yearless domain as well as the completed one. `interpretationOfYear` is implemented, so a wrapping yearless value now completes across two adjacent years under `FROM` or `TO` and stays refused under the standard reading; both overlap operators refuse the declaration that carries it, matching the measured Kernel gate, while equality admits it and compares the two readings as the different values they are. Stored-versus-stored DateRange equality is implemented over all eight declarations with the measured component-set gate, and unconfigured yearless bound extraction now admits what the Kernel admits, gating each numeric component by the declaration's own component set, two extracted endpoints compare under the ordinary temporal admission rule with the yearless side completed by its authored position, the fixed-Date comparison refuses at the comparison with the Kernel's own diagnostic, and every DateRange computation shape now accepts both spellings of its target's component set. The numeric component of a selected endpoint is now a numeric operand atom on the validation surface at both loci and on the scalar computation surface, gated by the declared profile's own component set with the Base Year supplementing the year alone, and reading from every runtime carrier so an unconfigured yearless range reaches its retained label. It also computes a row target per instantiated row through the shared addressed placement, and its computed values, empty zero, and cleared invalid-operand row are Kernel-calibrated on both codegen strategies. The **keyed** DateRange operand now exists on two carriers: the checked semantic-index source refines to the selected declaration's own certified DateRange profile, admitted through the family's new scope-free profile projection because a keyed row arrives from the index column rather than from the reading environment. The rule-message parameter now decodes the shared path grammar, so a field is addressable by bare name, by a relative or absolute path, or by a parent walk with an explicit turning point, each segment bare or in the grammar's single-quote escape, lowered through the one existing quote-provenance boundary under this producer's own narrower quoting rule and resolved by the one existing resolver. A field reference also carries the Enumeration category suffix, whose three gates reuse the existing projection boundary and whose rendering stays an opaque caller-supplied run. The **Base Year** terminal is admitted too, with its optional signed offset, one gate on the model declaring a year, and resolution at authoring. All four halves are Kernel-measured through `rule add --dry-run` — `rule check` takes no message and cannot reach the surface — and travel outbound as SPEC-2026-08-24-01. A second batch through the runtime probe route calibrated the **rendered** text on both codegen strategies, which strengthened the representation: a category access no longer takes an opaque caller-supplied run but renders through the declaration's own category mapping. All four measured keyed DateRange carriers are closed: the endpoint component composes one shared clause with the direct read, and stored equality, the endpoint-pair comparison, and the overlap predicate each take their own carrier's gate and verdict. The comparison's runtime domain is read off the retained profile because the two direct endpoint owners are proved to gate on complementary conditions, and the overlap is a pair reusing the operator's declaration gates, year rule, slot projection, and any-pair scan rather than a keyed slot in its list. The message parameter's **semantic-index key** is decoded too, in last position after any other suffix, with a quoted literal or a keying-field path as its only two spellings; the fragment refuses it at its own boundary because a semantic index needs a repeatable group, and the measured gate — on the index, not the field — corrected a wrong field-keyed gate before it shipped. **Partially known Date stored input** is now a capability of its own: the partial value domain was extracted out of `ValueAsDate` so a stored-text classifier sits below the operation, the precision ladder is proved monotone, and the two formal causes are measured — reality before position in time before precision, which corrected a floor cause the constructor had been hiding. **Stored Time and DateTime input** are closed as their own bounded classifiers, and DateTime has exactly one declarable storage format — the day-first spelling tags expressions in the kernel, not declarations. Placed-cell temporal coherence is settled as an **opt-in property**, `CheckedDocument.temporallyCoherent`, not a construction gate: the duplicate-value clause compares a cell's exact stored text, so forcing text and decoded value to agree would delete that clause's own subject, and only two of four temporal kinds have an owning classifier anyway.
- `gap`: [SG6 DateRange declaration and temporal completion](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion).
- `oracle`: use the canonical [DateRange clause](../spec/05-dates-and-time.md#8-date-ranges-and-overlap), the [reviewed DateRange reconciliation checkpoint](SOURCES.md#src-date-range-2026-08-19-reconciliation), and the [checked declaration capability](IMPLEMENTATION-MAP.md#cap-date-range-checked-declaration).
- `route`: SG6 records the next temporal candidate and its oracle; the route itself needs discovery before a red case exists. Route discovery for a kernel-behavior question starts at the maintained probe route, including for a static-admission question, because a kernel-gated dry-run and the probe's refusal path are both admission oracles.
- `next`: the rule-message family is at a **calibrated boundary** — static legality and rendered text are both Kernel-measured, now including the **semantic-index suffix**, whose grammar is modeled and whose gate turned out to be on the index rather than the field (see [`LF100`](LEAN-FINDINGS.md#lf100--a-refusal-names-an-entity-not-the-gates-subject-only-an-admitted-row-that-varies-the-entity-says-which-one-the-gate-reads); the earlier plan entry asserting a key-aware *referenced set* was the wrong account and is retired). A keyed parameter's **admission** stays prerequisite-bound on a condition representation with keyed leaves, which the flat spine has not got; the fragment refuses one at its own boundary instead, mapped to no Kernel class. The other non-field forms need repeatable coordinates (`#RootGroup`, the group index, `#Zeile`'s offset) or are forbidden in an A12 model outright (`Zeile`, `Usb`). Two cheap follow-ups remain if this family is continued: a **German-locale** row through the same probe request, and what a keyed parameter **renders**. A **label-provider** row is not reachable at all — the probe request has no provider surface — so that branch needs a different route or stays open. Otherwise select from another gap; SG13's payload obligation is now closed. Both declared spellings of the partial shapes are measured, `ValueAsDate` now consumes the one classifier, and what such a value renders is measured as its stored spelling. The rendered date pattern is now measured on two message locales and follows the locale, which also answers the older rendering checkpoint's one-locale limit for dates. A separate measurement settled SG8's format-versus-kind residual with a positive witness: the temporal format gate is a kind-independent vocabulary check, so a kind/format mismatch is authorable in every direction and no consumer may infer a component set from a kind. Stored **Time** input now has an owner, reusing the authored literal's decoder because the measured stored lexical rule is that same rule; its single formal cause is proved universal. Opening **stored DateTime input** turned up a defect first and that took precedence: the checked DateTime target vocabulary admitted the day-first `dd.MM.yyyy'T'HH:mm:ss`, which the model gate refuses — a false acceptance seven conformance modules had built their fixtures on. The vocabulary is narrowed to the one measured format and every fixture migrated (see [`LF103`](LEAN-FINDINGS.md#lf103--a-format-string-that-appears-in-kernel-source-may-belong-to-a-different-vocabulary-and-copying-it-by-symmetry-certifies-illegal-models)). Stored DateTime input then landed on top of the correction, reusing the full-Date component parser and the Time literal's decoder for its two halves. **Checked-document integration was attempted and reverted the same day**, and its cost is now recorded in SG6 rather than estimated: the coherence arm breaks ten conformance modules, because their temporal fixtures pair a placeholder stored text with an unrelated raw, or reuse a UTC-resolved instant under a non-UTC model zone. It needs its own unit with a fixture migration; `FullDateComputation` was migrated as a sample showing the shape. The smaller ready alternative is a partial Date inside a repeatable group.
- `excluded`: do not add operand diagnostics, another document route, or a harness without its own route discovery, and do not model a wrapping yearless interval without a year: that needs a new interval shape whose overlap is undefined, and no observation calls for it.
- `structural`: the String-pattern producer is **split out** as of this unit — [`Elaboration/StringPatternMessage.lean`](../A12Kernel/Elaboration/StringPatternMessage.lean) with its own proof and conformance modules — leaving the authoring module at 500 nonblank with one responsibility; the shared lexical gate is now a public declaration rather than a private one duplicated across producers. [`Conformance/DateRangeComparison.lean`](../A12Kernel/Conformance/DateRangeComparison.lean) stands at 997 nonblank lines, three under the hard ceiling, and [`Conformance/IteratedDateRangeCondition.lean`](../A12Kernel/Conformance/IteratedDateRangeCondition.lean) has reached 569. The next unit that would add cases to either must split it by semantic responsibility first; for the iterated module the natural cut is one module per value domain rather than per carrier. [`Elaboration/NumericComputation/Core.lean`](../A12Kernel/Elaboration/NumericComputation/Core.lean) is at 622 nonblank, already past the 600 target before this capsule added one atom row to its existing family; the next unit that adds a *new* source family there must split it first, and the natural cut is the surface-to-checked resolution pass away from the checked-atom type and its static predicates.
- `blocked-on`: none. The crossing branch was measured locally through `computation add --dry-run` and `:adapter:kernelProbe`, so its experiment request closed without an upstream trip.
- `milestone`: the DateRange overlap family closed on 2026-08-23 for every shape either operator admits, across both the completed and the unconfigured yearless domain. That is a reusable-capability milestone under the trigger rules, so a cold review of the overlap slice and the armed consumer probe both now await an owner decision rather than more semantic work.
- `consumer-probe-trigger`: armed now: both added stored policies are executable. The pending owner decision at that point is whether the probe exercises the ready DateRange material or opens non-evaluator Analyze breadth: a 2026-08-22 coverage review recommended the latter, but the only ready Analyze capability is the bounded presence-contradiction pilot, which is too small to carry an isolated implementation, so genuine Analyze breadth first needs the [solver proposal](SMT-SOLVER-SUPPORT-PROPOSAL.md) adopted under the dependency-approval rule. Absent that decision, probe the DateRange material artifact-only and leave the proposal unadopted.
- `resume`: `rg -n '^- `(remains|external-gap|unresolved-source|risk|route-state)`:' docs/SEMANTICS-GAPS.md && rg -n '^- `(state|boundary|assurance|remains)`:' docs/IMPLEMENTATION-MAP.md`
