# Implementation handoff

This is the cross-session resumption checkpoint, not an in-session task list or work log. Leave it unchanged during uninterrupted route discovery and capsule execution unless another session must resume a different selected action, a blocker must persist, or a consumer-probe trigger changes.
Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

## Verified baseline

- `basis`: current committed Lean state; Git owns the exact revision.
- `gate`: Tier 1 passed for that Lean state.

<a id="active-unit"></a>
## Selected work

- `state`: the checked semantic-index source now carries the declared index declaration rather than a Number field, so any index kind is admitted with one dispatch between the numeric-value and exact-text identities. The shared repeatable numeric and String placement now admits a computation operand at any scope the computed target's scope binds, matching the measured Kernel rule, and reads each operand at its own path, so one form-level cell reaches every target row; both halves are Kernel-calibrated on the two codegen strategies, including a two-level target where a middle-level operand reaches exactly the leaves of its own enclosing row. The validation-side DateRange condition leaf carries the same two-level locus, admitting an operand at the root, the enclosing row, or the leaf and correlating each to its own enclosing row. All eight admitted DateRange declaration pairs classify stored text, execute the direct single-level starred `FirstFilledValue` route with component-set crossing, and gain bound extraction once Base Year completes them. Both overlap operators now gate their whole operand list by year class, matching the measured Kernel gate, and a mixed year-class list needs no bridge between the resolved and yearless interval domains. The plural family owns its own module, and both operators are closed over the unconfigured yearless domain as well as the completed one. `interpretationOfYear` is implemented for configured models, so a wrapping yearless value completes across two adjacent years under `FROM` or `TO` and stays refused under the standard reading; both overlap operators refuse the declaration that carries it, matching the measured Kernel gate, while equality admits it and compares the two configured readings as different values. The unconfigured validity and direct-star presentation branch is declaration-order dependent in the Kernel and remains outside the field-local Lean account pending a measured shared-format denominator. Stored-versus-stored DateRange equality is implemented over all eight declarations with the measured component-set gate, and unconfigured yearless bound extraction now admits what the Kernel admits, gating each numeric component by the declaration's own component set, two extracted endpoints compare under the ordinary temporal admission rule with the yearless side completed by its authored position, a yearless endpoint's fixed-Date comparison refuses at the comparison with the Kernel's own diagnostic while a year-only endpoint is admitted, and every DateRange computation shape now accepts both spellings of its target's component set. The numeric component of a selected endpoint is now a numeric operand atom on the validation surface at both loci and on the scalar computation surface, gated by the declared profile's own component set with the Base Year supplementing the year alone, and reading from every runtime carrier so an unconfigured yearless range reaches its retained label. It also computes a row target per instantiated row through the shared addressed placement, and its computed values, empty zero, and cleared invalid-operand row are Kernel-calibrated on both codegen strategies. The **keyed** DateRange operand now exists on two carriers: the checked semantic-index source refines to the selected declaration's own certified DateRange profile, admitted through the family's new scope-free profile projection because a keyed row arrives from the index column rather than from the reading environment. The rule-message parameter now decodes the shared path grammar, so a field is addressable by bare name, by a relative or absolute path, or by a parent walk with an explicit turning point, each segment bare or in the grammar's single-quote escape, lowered through the one existing quote-provenance boundary under this producer's own narrower quoting rule and resolved by the one existing resolver. A field reference also carries the Enumeration category suffix, whose first suffix gates now preserve the exact three-way doubled-arrow diagnostic and whose rendering uses the existing declaration-owned category mapping. The **Base Year** terminal is admitted too, with its optional signed offset, one gate on the model declaring a year, and resolution at authoring. All four halves are Kernel-measured and peer-accepted as SPEC-2026-08-24-01; a12-dmkits now also exposes the read-only `rule check --message` route with standard diagnostic envelopes. A second batch through the runtime probe route calibrated the **rendered** text on both codegen strategies, which strengthened the representation: a category access no longer takes an opaque caller-supplied run but renders through the declaration's own category mapping. All four measured keyed DateRange carriers are closed: the endpoint component composes one shared clause with the direct read, and stored equality, the endpoint-pair comparison, and the overlap predicate each take their own carrier's gate and verdict. The comparison's runtime domain is read off the retained profile because the two direct endpoint owners are proved to gate on complementary conditions, and the overlap is a pair reusing the operator's declaration gates, year rule, slot projection, and any-pair scan rather than a keyed slot in its list. The message parameter's **semantic-index key** is decoded too, in last position after any other suffix, with a quoted literal or a keying-field path as its only two spellings; the fragment refuses it at its own boundary because a semantic index needs a repeatable group, and the measured gate — on the index, not the field — corrected a wrong field-keyed gate before it shipped. **Partially known Date stored input** is now a capability of its own: the partial value domain was extracted out of `ValueAsDate` so a stored-text classifier sits below the operation, the precision ladder is proved monotone, and the two formal causes are measured — reality before position in time before precision, which corrected a floor cause the constructor had been hiding. **Stored Time and DateTime input** are closed as bounded selected-format classifiers. The complete twelve-format cross-kind differential now externally fixes the selected formal classifiers over its discriminating values; the DATE input side implements all three complete and seven omitting spellings, while a general cross-kind format-selection mechanism remains open. Placed-cell temporal coherence is settled as an **opt-in property**, `CheckedDocument.temporallyCoherent`, not a construction gate: the duplicate-value clause compares a cell's exact stored text, so forcing text and decoded value to agree would delete that clause's own subject, and only two of four temporal kinds have an owning classifier anyway.
- `gap`: [SG4 computation scheduling and state transition](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) is the active major milestone.
- `oracle`: the [computation definition, execution, and result checkpoint](SOURCES.md#computation-definition-execution-and-result) owns the retained separate-destination cell observation, exact application source route, and its topology claim limit.
- `route`: the finite nonrepeatable mixed String/Number executor is sound against its own independently enabled successful-transition relation. Independent typed steps may execute in either order with the same target-indexed rich outcomes, while a static typed dependency stays disabled before its producer and becomes enabled afterward even when runtime selection leaves the read unreached. Its public structural-failure boundary preserves the nested family and confines the failing target to the run's checked targets, with a later String failure separating that label from the first Number target. A failing fixed execution now decomposes into its exact successful prefix followed by one enabled typed structural-failure transition at the unchanged prefix state; rich target outcomes remain successful transitions. The finite repeatable Number executor is sound against an independently enabled whole-table relation: independent tables begin enabled, and private completed-target state lets an action-free invalid-index seed enable its dependent table without inventing addressed outcomes. Any returned structural table fault now decomposes conditionally into its exact successful batch prefix plus one enabled target-attributed failure at unchanged prefix state, with completed labels reconstructed independently from flattened addressed outcomes; no valid current fixture witnesses that branch, so reachability and exact Kernel failure-schedule correspondence remain open. Both fixed complete-scope cross-family `CurrentRepetition` cascades expose two successful family transitions. Number-to-String completes every addressed Number row before String can transition. Its failure trace now separates a concrete initial no-row Number fault from the existing fail-closed missing-target-matcher String fault after the exact successful Number phase; the latter uses an intentionally incomplete prepared-pattern input and retains no String completion. String-to-Number completes every addressed String row before Number can transition and leaves dependency-cell conversion at the Number boundary. Neither dependent transition can start from the initial state, and each pair reconstructs its exact returned rows. The inverse String-to-Number cascade now also distinguishes a concrete initial no-row String fault from a conditional later Number fault, retaining the exact successful String phase and no Number completion in the latter trace; later Number-fault reachability and Kernel failure scheduling remain open. The alternating fixed chain reuses those first two phases and exposes a third successful Number transition, retaining dependency conversion at that boundary. Neither dependent family can appear initially, and the exact three phases reconstruct every returned row. Its failure trace separates the concrete no-row fault, the concrete intentionally incomplete-matcher String fault after Number, and a conditional terminal Number fault after the exact Number-plus-String prefix; every failure retains no terminal completion, while terminal-fault reachability and Kernel scheduling remain open. The Number-only fixed chain retains its executable outcomes and Analyze view without another relation. The four fixed `CurrentRepetition(group) > 0` chains admit the selected group's complete repeatable scope while evaluating the terminal coordinate only. A two-level `[10,20]` matrix separates scope from coordinate: `[1,1]` and `[2,1]` both report coordinate `1` yet keep distinct exact addresses and fresh Number/String state, `[1,2]` reports coordinate `2`, and malformed poison remains leaf-local through the alternating chain. The exact two-level Number-to-String and three-step alternating representatives are Kernel-calibrated for those three addresses, leaf-local empty and malformed state, and the concrete-leaf-only computation domain. The aggregate family composes a repeatable Number producer with one root aggregate containing any nonempty authored list of same-group, same-scope plain and filtered stars, plus an optional root or finite scalar suffix and six bounded repeatable suffix forms; purpose-specific two-phase relations record aggregate completion before each of the finite mixed, direct Number, fixed Number, fixed Number-to-String, and direct String suffixes consumes its exact overlay. The finite mixed route now exposes every successful scalar suffix transition with labels exactly equal to the public suffix outcomes while retaining the aggregate seed only in state; its established two-phase theorem is the coarser projection. It also decomposes every outer structural failure into either an aggregate-prefix fault at empty state or a seeded scalar-suffix failure retaining the aggregate, successful scalar prefix, and exact typed terminal fault while excluding the seed from scalar outcomes. Its concrete adversarial matcher set, which omits Label and is not an output of normal pattern preparation, reaches String failure after the aggregate and one Number suffix step; aggregate-prefix fault reachability and Kernel failure scheduling remain open. Without exposing private scalar transition order, the named one-level aggregate-seeded finite mixed String/Number successful route has retained exact Kernel correspondence for fresh completed state, false-first String fall-through, an empty producer, poison across both family boundaries, and the no-row zero aggregate. Its detailed matrix is owned by the [implementation map](IMPLEMENTATION-MAP.md#11--calculations-and-formal-checking). The direct one-level Number application branch exposes the complete numeric predecessor prefix, preserves exact action coordinates and existing destination cells, leaves absent ERRORED rows uncreated, and feeds explicit later `Number > 0` validation without recomputation. Named representatives for all six bounded one-level producer-to-aggregate-to-repeatable suffix forms have retained exact Kernel correspondence: direct Number, fixed two-step Number, fixed Number-to-String, direct repeatable String, ordered subtraction, and warning-suppressed division. The subtraction and division representatives cover both aggregate/row operand orders and separate aggregate-wide from row-local poison; division additionally separates warning-suppressed no-fit storage and zero-divisor order. Other binary operators, unsuppressed division, mixed plans beyond the exact two-step authored pair and exact reverse-authored Number-to-String-to-Number triple, and the Number-only and String-to-Number nested fixed-chain variants remain external evidence pending; the named one-level two-operand plain-star aggregate route with the computed producer second is Kernel-calibrated for fresh, empty, malformed, and no-row state. The internal star-list route preserves arbitrary authored slot order and multiplicity, applies each `Having` only to its own slot, classifies all-plain, all-filtered, and mixed lists, and derives producer readiness only from plain values or filter reads. The same prefix now admits scale-preserving `Abs`, direct-source Round, ordered operand-list `Min`/`Max`, scale-suppressed field-exponent power, warning-suppressed division, ordinary UTF-16 String `Length`, String/Enumeration `FieldValueAsNumber`, UTF-16 String `RangeAsNumber`, and selected DateRange endpoint-component row producers, retaining operation identity, ordered source dependencies including duplicates and root operands, exact rich rows including division's natural stored scales, typed admission errors, and the completed-state aggregate overlay. String `Length` additionally retains its typed cross-family source edge, UTF-16 code-unit result, fresh rows over stale seeds, absent zero, and malformed poison. `FieldValueAsNumber` retains category-projection identity, a typed Enumeration-to-Number edge, signed scale-2 values, fresh rows over stale seeds, absent zero, and declared-constraint poison. `RangeAsNumber` retains an exact 1-based UTF-16 interval, a typed String-to-Number edge, fresh ranges over stale seeds, absent zero, and malformed poison. The DateRange component retains endpoint/component identity, a typed DateRange-to-Number edge, fresh start-month rows over stale seeds, absent zero, and DateRange-invalid poison. The row-producer union now covers every existing concrete addressed Number-result operation. Wrapper-, extremum-, power-, division-, String-length-, `FieldValueAsNumber`-, `RangeAsNumber`-, and DateRange-component-producer composition remains externally uncalibrated. The named exact two-slot plain/filtered aggregate remains Kernel-calibrated in both authored orders for fresh filter state, empty substitution, malformed poison, and no rows. Kernel calibration of wider star lists and other cross-topology aggregate operands, wider filters and row operations, wider application/validation families, concrete later inverse-cascade, alternating-terminal, and other repeatable structural-failure reachability, other repeatable structural faults, and broader cross-family repeatable scheduler semantics remain open. The homogeneous aggregate-seeded finite Number route now likewise exposes an exact successful transition trace from the retained aggregate seed, labeled only by its public `Doubled, Tripled` suffix outcomes; its named one-level direct-producer representative has retained exact Kernel correspondence for fresh completed state, empty substitution, reached poison through both suffix targets, and the no-row zero aggregate, while structural-failure reachability and private Kernel schedule order remain open. The aggregate prefix itself now decomposes into the exact rich row phase followed by aggregate evaluation over those same rows; an aggregate cannot appear in the initial transition, and this remains an internal executor law rather than a Kernel schedule claim. The addressed sibling-star DateRange route now runs once per physical target row, retains exact `CellAddr` results, applies source-relative actions to a separate destination projection, and is Kernel-calibrated for parent-local exact-profile values, empty-source clearing, malformed-source clearing, and both codegen strategies; wider-profile runtime calibration remains open.
- `temporal-route`: the addressed sibling-star Time route shares checked parent-local star placement, selects typed clocks per enclosing parent, renders through the sole `HH:mm:ss` target profile, and retains exact `CellAddr` result and application actions. Both Kernel codegen strategies calibrate distinct parent values, repeated task rows, hidden-tail selection, empty-source and malformed-before-valid clearing, source-identical no-action, changed-value addresses, and the exact generated-rule target. The repeatable constant-construction route now uses the carrier-neutral repeatable target certificate, evaluates every physical target row internally, retains full nested coordinates in physical encounter order, and classifies actions by row-local source equality. Its retained Kernel projection locks the stale, absent, present-empty, source-identical-no-action, no-row, and generated-message channels, while complete successes and application remain internal. Scalar and repeatable Time routes use ordinary equality over typed ingress; V1 String ingress makes every measured non-String target changed and remains outside the typed checked-document representation. Wider zones and source shapes, materialized topology, later validation execution, and pointer rendering remain open.
- `temporal-field-route`: the [repeatable field-backed Time construction](IMPLEMENTATION-MAP.md#cap-exact-address-repeatable-time-construction) is Kernel-calibrated for separate all-Number, all-`[0-9]+` ordinary String, and direct DateTime/Time/DateTime-extractor prefixes. The [Number](SOURCES.md#src-time-repeatable-number-components), [String](SOURCES.md#src-time-repeatable-string-components), canonical mixed Number/String/[extractor](SOURCES.md#src-time-repeatable-direct-extractor-components), scalar-expression shifted-`Now`, and bound complete-DateTime sub-day-shift [world-backed routes](IMPLEMENTATION-MAP.md#cap-world-backed-repeatable-time-construction) have passed bounded Execute/Analyze/Transform consumer probes. The target-group shifted-`Now` expression still has internal E/P only. Exact repeatable placement of all world-backed routes and the earlier mixed prefix remain unmeasured by the Kernel. Other recognized digit-pattern spellings, extensible-enumeration String alternatives, and nested temporal-expression sources beyond the exact bound shift remain open.
- `temporal-datetime-route`: the [exact-address field-backed DateTime sub-day shift](IMPLEMENTATION-MAP.md#cap-exact-address-repeatable-datetime-subday-shift) has internal E/P and bounded artifact-only Execute/Analyze/Transform qualification. Its [outer-Number-to-inner-DateTime successor](IMPLEMENTATION-MAP.md#cap-repeatable-number-datetime-shift-cascade) additionally passed a cold executable Execute/Analyze/Transform probe.
- `temporal-datetime-route`: the [exact-address field-backed DateTime calendar-day shift](IMPLEMENTATION-MAP.md#cap-exact-address-repeatable-datetime-day-shift) has internal E/P for the pinned Berlin profile. Kernel correspondence, dynamic `Now`, computed amounts, wider expressions and profiles, topology, later validation, pointers, protocol, shipment, and compatibility remain open.
- `sweep`: **an active bounded backlog with a known method.** Arms that refuse but project no diagnostic class are latent over-refusals or under-projections, they cluster on the carrier whose first one was most plausible, and each is one `batch` row ([`LF152`](LEAN-FINDINGS.md)). Enumerate them with a scan for `=> none` arms of a `diagnostic?`. Triage each arm first: a module's own boundary — a flat reader declining an addressed shape, a representation limit — correctly claims nothing, and only a *Kernel* refusal is a measurement target. **The raw count is a poor progress metric** and expanding wildcards raises it while improving the estate; measure by what is triaged, not by what remains.
- `sweep`: closed so far — the temporal extrema, the DateRange scalar slot, the custom-validity operand, the pattern comparison's left kind, the value-list quantifiers' three-way kind gate and their per-carrier operand arity, the wrong-kind constant assignment's [three-rung ladder](sources/evaluation-and-application-routes.md#src-constant-assignment-diagnostic-ladder) across all six carriers including its CUSTOM rung, and the [`Having` filter's whole refusal surface](sources/evaluation-and-application-routes.md#src-having-filter-comparison-and-scope-classes), which had no projection at all. Every `| _ => none` wildcard is expanded except `ResolveError`'s, which is a stated decision recorded at the declaration.
- `sweep`: **the two batches that closed most of it were cross products, not samples**, and [`LF153`](LEAN-FINDINGS.md) owns why: a batch that holds one side fixed produces a table keyed on the other side whether or not that side is the key, so the assumption is unfalsifiable by that batch and then designs the next one. Eighty-eight cells and seventy-five cells, one JVM start each, five and seven seconds. When the key is unknown, cross.
- `queued-batch`: **five of the eight drained in one window on 2026-09-06** (a12-dmkits `0ca44debb`, clean before and after, 92 `rule check` children in three batches). Closed: multi-leaf/nested `Having` reporting (first defective leaf, one code); the five-wrapper filter-class carrier-independence, now measured rather than extended; Enumeration operands with different declared domains (union, not every); the `FirstFilledValue` field-valued arity, uniform with the four other entity-list carriers; and `IteratedDateRangeCondition.mixedBoundDomains`, which **dissolved** — a DateRange operand is refused by its kind before any homogeneity gate, so the question is unreachable through these carriers. `@SuppressWarning` scope is closed in the same window: the preamble scopes the whole condition. The `FirstFilledValue`-family `targetFixed` question is closed in the same window and is an over-refusal on all four carriers. `SemanticIndexDateRange` keyed-operand overlap classes closed too: keyed on both sides is admitted on either operator. **The queue is drained.** The single-keyed-side pairing is closed for the overlap carrier. Two over-refusals remain window-free: first-filled repeatable targets, blocked on the per-row result domain rather than on evidence. The suppression certificate is **not** window-free after all — its validation half is measured but the computation carrier that needs it takes `errorCodesToSuppress`, a different channel this project has not measured, so it re-enters the queue as one `computation add --dry-run` pair. Prepare cold and fire the remainder in one `batch` — the sibling goes dirty within minutes of each report ([`LF151`](LEAN-FINDINGS.md)), so the window, not the row count, is the scarce thing.
- `route`: **both static routes reach it.** `rule check` for validation gates and `computation add --dry-run` for computation gates, and one `batch` dispatches a mixed queue of both in a single JVM start ([`TESTING.md`](TESTING.md)). Consult `dmtool operators <id>` for an operator's `validExample` before authoring a condition rather than guessing its surface form; one guess cost a batch when a call spelling for an infix operator returned `MVK_UNEXPECTED_TOKEN` on every row, admitted control included.
- `next`: resume generic semantic selection from [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md). The mandatory-information guard/count/severity, validation-cycle, whole-rule-exclusion, exact generated-identity, direct-repeatable-child presence, count-dependency, and isolated distinct-target slices have completed bounded Analyze+Explain consumer probes. SG14 now also retains exact four-node direct and multi-list-edge cycle correspondence, one dependency-derived filled-count operand with downstream target reuse, the isolated true filled-count target/root separator, and the isolated distinct-count no-contribution pair; it still needs other count collector and dependency shapes and recognized families, still-longer or mixed-edge cycles, wider repetition, indices, filter and join internals, other generated index and computation-validation shapes, and contributing or wider topology. SG4 and SG16 retain their existing evidence and route blockers. The **`Having`-filter leaf and connective family is closed** for validation and, on the Number carriers, for computation: presence in both polarities, String equality under both operators, and `Or` are implemented and Kernel-retained, correlation and multi-level correlation are retained where they had been static-only, and the scope gate is measured as a syntactic occurrence check. That obligation is now [SG17](SEMANTICS-GAPS.md#sg17--having-filter-leaf-and-connective-completion), split out of SG13 after it hit its record ceiling three times. SG17's non-Number runtime is **closed** — the presence leaf is measured on four carriers on both arms — and the filter's reference **gates** are now their own record, [SG20](SEMANTICS-GAPS.md#sg20--filter-reference-scope-and-iteration-gates), split out when SG17 hit its ceiling a second time. Those gates are measured on both arms, proved asymmetric at their fold, and their predicate is path membership; what SG20 still owes is a filter naming a level above the operand's declaring group, and the runtime behaviour of an admitted row.
- `outbound`: the queue is carried directly to the a12-dmkits session and the peer disposes entries there; the ledger is the list rather than this line. [`SPEC-2026-08-31-17`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-17) and [`SPEC-2026-08-31-13`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-13) are accepted, and [`SPEC-2026-08-31-14`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-31-14) requires no peer action. Every `EXP-` entry is answered: `EXP-2026-08-30-01` by the peer's new `mandatoryInformation` observation, `EXP-2026-08-31-03` by this project's own seven-document run. Three filter entries are newly pending and unsent, all locally originated with no peer session reachable: [`SPEC-2026-09-03-03`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-03-03) (a filter's connectives are commutative on validation and order-sensitive on computation), [`SPEC-2026-09-03-04`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-03-04) (a `$`-marked reference cannot bind the filtered level), and [`SPEC-2026-09-03-05`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-03-05) (the scope gate is a syntactic occurrence check over any reopened level). `-03` is the one a rule-refactoring consumer needs, because it makes an arm-blind operand reordering unsound.
- `inbound-method`: **resolve a handback's cited revisions before writing any disposition, and never reconcile from its prose.** A letter is ungated and mutable: the 2026-08-31 handback mapped both halves of its answer to one revision that measures only one arm, and its file was then rewritten in place after delivery so the copy it designated authoritative no longer existed. Neither reached a record here because every disposition was written from `git show` and `ls-tree` on the cited commits. [`LF84`](LEAN-FINDINGS.md) rule 3 owns the rule; this line exists so a resuming session does not re-derive it under time pressure.
- `instrument`: **the clear-kind request is answered and re-aimed; one narrower gap stays open.** a12-dmkits answered at reviewed `c4864f70edb6f695d30545692403232aea7427a6` ([reconciliation](sources/computation-placement-and-constant-probes.md#src-2026-09-03-reconciliation)) and two thirds of the original request fall away: the collapse is the **Kernel's** own `IDocumentComputationResult` rather than the artifact's, so asking the artifact to surface a state the Kernel does not report would be inventing a shape A12 does not define; and `formalErrorsInOperands` already separates the two kinds for a dependent-free producer — empty for a precondition clear, naming the operand for a source-invalid one. The join is the consumer's to make deliberately, because that channel is an inventory rather than a causal attribution and names no edge to the outcome it explains. **The surviving gap is narrower than the one first reported:** a producer cleared because a **computed** field it read was poisoned names nothing in that inventory either, a computed field being outside the channel's scope by definition, so for such a producer with no dependents no published channel separates it. Closing it means publishing the Kernel's per-instance computed-error message, which the artifact does not carry. Forward that re-aimed request with the [diamond checkpoint](sources/evaluation-and-application-routes.md#src-diamond-dependency-clear-kinds) anchor when a peer session is next reachable, as instrument feedback and never as a ledger entry. Do **not** re-send the original three-part request: two of its parts are answered and one of them was never the instrument's to fix.
- `instrument`: **`field add`'s client precheck can mask the Kernel's own diagnostic, so a `PRECHECK` refusal is not a Kernel verdict.** A misaligned Enumeration category list is refused `RK_INVALID_VALUE` at `source: PRECHECK` and never reaches the engine; hand-editing the same model past the precheck and running `model check` gives the Kernel's `MVK_MISSING_CATEGORY_VALUE`, `KERNEL_CONFIRMED`. Both refuse, so nothing is wrong — but a probe that reads only the tool's verdict records the wrong code and cannot see a case where the two would disagree. Route a declaration-gate question through `model check` on a hand-edited model whenever the precheck answers first. Reported to the a12-dmkits session as an instrument note, not a ledger entry.
- `instrument`: **that semantic-index gap is closed, and it was not a missing verb.** a12-dmkits answered the request: `group modify --index-field` was the right lever and the missing half was the **condition** spelling — the condition must itself carry a keyed reference, `FieldNotFilled(Group/Field For "k")`. Re-measured locally on a four-field group ([checkpoint](sources/message-and-pointer-probes.md#src-keyed-parameter-admits-the-index-field-only)), which both closed the family and corrected a `spec/11` clause of this project's that had generalized from a two-field fixture. The `index(...)` terminal is a separate construct and stays unwitnessed on both sides. Kept below as the record of what was asked, since the enquiry-ending sentence was this project's own.
- `instrument`: **superseded — one open gap did block a clause family, and it was a capability rather than a defect.** `dmtool` has no route to declare a **semantic index**, so the `$Field For "k"$` gate and the admitting side of the name-position `index(...)` terminal cannot be measured at all — every argument shape refuses, and `group modify --index-field` designates the repetition row key, which is a different concept and does not arm either construct. Denominator searched before saying so: the whole verb manifest for `index`, `semantic`, `key`, and `lookup`; the only lever is `--index-field`/`--clear-index-field` ([checkpoint](sources/message-and-pointer-probes.md#src-name-position-index-terminal)). Routed to the a12-dmkits session as an **instrument** request, not a ledger entry, because kernel behaviour is not in question — only this project's ability to observe it. Until it lands, both constructs stay recorded as *no witness known at this revision*.
- `instrument`: **one dated instrument note is open, delivered to the exchange directory 2026-09-04** because no a12-dmkits session was reachable. Three items with reproducers: `group add` documents no route to a second root while the undocumented `--parent ""` is the route and `--parent /` misreports it as a missing group; `--field-ref-by-short-name` help overstates the flag, which gates the model-wide tier alone; and the probe artifact renders the first repeatable instance without an index, which cost a second probe to attribute an absent row. It also lists five suspicions that were **not** defects, two of them our own misreads, so the peer spends no round on them. Nothing here blocks a semantic claim — each was caught by a control — so none of it is an `EXP-` entry or a ledger item.
- `instrument`: **prior notes are closed.** The two dated dmtool feedback notes are delivered, reconciled, and deleted; the [Base Year store checkpoint](sources/computation-placement-and-constant-probes.md#src-base-year-yearless-store) holds their surviving fact and [`LF120`](LEAN-FINDINGS.md) their lesson. The reported Base Year setter defect was withdrawn — `dmtool config modify --base-year` always existed — and the one real residual, the missing inverse, landed upstream at a12-dmkits `735e13459` as `--clear-base-year`. A `computation add --dry-run` refusal is the Kernel's and may be read as one.
- `probe-contention`: that CLEAN precondition makes the runtime route **unavailable while a peer session is actively working the sibling**, which is the normal state rather than an exception. Observed repeatedly on 2026-09-05: the tree was dirty, briefly committed at `7da8df9e6`, and dirty again within the same exchange as the peer began its next differential. So a runtime observation needs the peer to name a quiescent moment; it is not something this session can schedule. Static admission work is unaffected and is the right thing to select while the route is closed. Distinct from the provenance hazard in [`LF137`](LEAN-FINDINGS.md), which is about *which* revision a dirty tree's measurement belongs to; this one is about the route being closed at all.
- `probe-contention`: **the static route shares that precondition, which the earlier note did not say.** `prepare-dmtool-source.sh` refuses a dirty sibling before any command is chosen, so `rule check` and `--dry-run` batches are closed exactly as the runtime route is; "static admission work is unaffected" holds only for work needing no instrument at all. Checked at eight points across the 2026-09-05 session and dirty at every one, latterly a single modified file for hours while the peer sat idle mid-change. A request for a named quiescent window is with the peer; until one arrives, select Lean-internal work.
- `probe-route-of-first-resort`: **when the sibling is contended, hand the row to the peer session instead of waiting for a clean tree.** Measured twice on 2026-09-05: a row this project reassigned came back measured on an independently authored carrier within one exchange, and a policy question flagged without being asked came back with a third answer neither branch of the discriminator offered. The peer measures on its own carriers rather than replaying this project's report, which is stronger evidence than a local run would have been, and it costs no sibling access at all. Reassign in the owning ledger entry so the row has one owner; [`TESTING.md`](TESTING.md#peer-session-coordination) owns the channel.
- `blocked-on`: **one blocker persists.** The temporal `NumberOfDifferentValues` overload is ready to build except for two representational decisions with no local witness — whether it shares `FieldValuesNotUnique`'s identical-declared-format admission gate, and which value identity it compares — recorded with a recommendation and the exact discriminating batch at [SG22](SEMANTICS-GAPS.md#sg22--numberofdifferentvalues-operand-domain-and-its-two-homogeneity-codes). Both are the over-admission class, so a wrong choice produces no failing case. The route is known and works for this shape; it needs a clean `../a12-rulekit`, which the peer session currently holds dirty. Every other half of that operator's operand domain closed this session.
- `blocked-on`: **no blocker persists past this session.** The temporal-extrema domain choice, carried as one for several sessions, dissolved on re-examination: the fold is already parametric in its element type, and the static rows measured today fix that type as the operands' own family, so the complete-Date and Time stream builders are ordinary work. What remains is a narrower runtime question about component-omitting operands only, recorded as a gap rather than a blocker in [SG6](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion). That is the third over-scoped blocker this week and the second on this same row ([`LF142`](LEAN-FINDINGS.md)). The instrument contention above is **resolved by agreement rather than by tooling**: the peer session now commits at each landing instead of at the end of a batch, so clean windows recur without being requested, and the 2026-09-05 window ran three probes end to end with the sibling clean before and after.
- `closed-this-session`: the explicit validity predicates are closed end to end — operand slot with its measured per-arm classes, checked leaf reading the operand's checked cell, `ValidationConditionLeaf` arm with three laws, and a checked-rule-condition assembly whose separator is a repeatable String the slot admits and the rule refuses. The addressed partial route landed after the fixture that had stopped it was diagnosed: preparing the model's String context resolves **every** declared custom type, so the fixture's own unregistered second declaration was failing preparation, not the arm. A false Kernel class under the extrema was fixed at the same time ([`LF140`](LEAN-FINDINGS.md)), and [`LF141`](LEAN-FINDINGS.md) records why a corrected clause leaves stale copies in Lean prose that no gate reaches.
- `closed-this-session`: a kernel window then closed three questions. The **extrema's operand-family gate is positional** and drew a code neither estate had, which refuted a class assignment in each of this project's two operand lists at once; the **`$#RuleGroup$` root-locus gate** crosses to the validation-rule carrier, correcting a conformance row, a law missing its placement hypothesis, and the peer's own clause; and the **DateTime computed-target certificate** is widened on an inbound peer store measurement this project had declined to inherit. [`LF143`](LEAN-FINDINGS.md) records the fixture defect all three rounds shared.
- `probe-precondition`: the producer now **refuses** on a non-CLEAN tree. a12-dmkits `75b33760f` checks the state `:adapter:kernelProbe` would stamp before parsing the request and writes nothing on the refusal path, `UNKNOWN` included, and the `-PprobeAllowUnpinned` exploration escape still stamps `state: DIRTY` rather than laundering it — so a dirty producer can no longer hand back an artifact to inspect and discard, as happened on 2026-08-31. The sibling `git status --short` check before and after any command there remains required by this repository's own immutability rule, and `producer.source.state` is still read on receipt because it is the claim the artifact makes about itself. Probing a revision other than the peer's head is a separate capability that does not exist.
- `blocked-on`: no semantic observation blocks SG4. The cross-group calibration is closed for all four constant families now that the declaring-group capability shipped upstream; [`SPEC-2026-08-29-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-29-01) is reconciled and terminal. The remaining discovered nested aggregate suffixes are duplicate compositions that stay no-ops.
- `blocked-on`: the SG4 triple's final outcome behavior is Kernel-calibrated; its private independent-step order is not observable through the field-sorted public result collection, and neither that collection nor the value matrix establishes a cycle mechanism. Planning **beyond three targets is no longer blocked**: a four-deep diamond is measured, closing the ordering question through a join and settling the two clear-kinds at a plain operand read ([`SPEC-2026-09-03-09`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-09-03-09)). What still needs an owner choice or new carrier-specific evidence is lifting the settled construction, shift, and enumeration over-limit results; the addressed per-family calibration inventory moved to [SG18](SEMANTICS-GAPS.md#sg18--addressed-computation-family-calibration) when SG4 hit its record ceiling. SG16's ordinary Number constant has the approved model-indexed application boundary; wider mixed reconstruction still needs a verified route, and general later validation or document transport remains excluded.
- `excluded`: do not add a generic graph, scheduler, state machine, trace framework, registry, protocol, manifest, runner, shipment, general document transport, or general later-validation layer. Public expansion remains behind research closure and its explicit approval gate.
- `structural`: [`Conformance/DateRangeComparison.lean`](../A12Kernel/Conformance/DateRangeComparison.lean) remains under its reviewed cohesive-family exception and must split before another case or responsibility. The `cap-first-filled-value` split is **done**: its evidence half now lives at `cap-first-filled-value-assurance` in [`strings-and-enumerations.md`](implementation/strings-and-enumerations.md), leaving 16,661 and 8,297 bytes against the 24,000-byte ceiling. The split axis is mechanism versus per-carrier calibration, as corrected here — not exact-token versus temporal carrier, which would have left the shared structural lines needing pointers in both directions. The source-hygiene gate owns current Lean line counts and enforces the 800/1,200 thresholds; no other handoff owner needs a persisted size exception.
- `consumer-probe-baseline`: the latest [flat mandatory-information Analyze+Explain probe](consumer-probes/ANALYZE.md#flat-mandatory-information-probe) independently retained the exact direct-unindexed-child scope, connective, starred quantifier, and both references across all six repeatable-presence results, refused the cross-root and explicit wider-scope inputs, kept a wider two-field syntax insufficient, and rejected all ten wrong accounts from clean committed basis `e7ad00602ea85c1497d0e9e91c2a819d927e650d` at `2026-09-02T08:54:49+02:00`.
- `consumer-probe-baseline`: the 81-second reader made no semantic guess and used no code, tool, or dependency. Cold review of the initial slice found that its identity omitted topology and admitted a nested descendant; red/green correction made scope explicit, then build-free D1–D7 convergence found no remaining issue before the clean isolated rerun.
- `consumer-probe-trigger`: satisfied at the current mandatory-information capability boundary. Select the next bounded probe only after another reusable family closes, a major addressing or computation boundary changes, or before declaring a public compatibility capability. The filter family's closure **fired** that trigger, and the gate paid was the [trigger-based cold review](TESTING.md#trigger-based-cold-review) rather than a consumer probe, because the slice also carried behavioral `spec/` edits and a shared-type change. It converged: nineteen findings, all reproduced and disposed, one of which changed the acceptance denominator and was repartitioned into SG17's runtime obligation instead of expanding the slice. A bounded consumer probe on the filter fragment is therefore still unspent and is the next natural one. **Selected and bounded:** one artifact-only Transform readback asking whether the material lets a filter-canonicalizing consumer decide when reordering a filter's operands is safe. Only that readback is authorized; no prototype, shipment, runner, or qualification follows from it.
- `consumer-probe-limit`: wider rule families, count comparisons and dependency shapes, wider repetition and indices, filter and join execution, wider Boolean formulas, other generated rules, longer and multi-list-edge cycle correspondence, contributing or wider cross-root topology, protocol, shipment, compatibility, and general Kernel correspondence remain outside this probe. Public expansion stays behind research closure and explicit approval.
- `resume`: `rg -n '^- `(remains|external-gap|unresolved-source|risk|route-state)`:' docs/SEMANTICS-GAPS.md && rg -n '^- `(state|boundary|assurance|remains)`:' docs/IMPLEMENTATION-MAP.md`
