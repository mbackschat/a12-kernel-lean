# Active implementation plan

This is the resumption checkpoint, not a work log. Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

## Verified baseline

**Verified semantic baseline, 2026-08-17:** the shared entity-list fixed-group duplicate capsule passed its complete Tier 1 gate and cold-review convergence at `f118816`. Re-run the required tier commands in [`TESTING.md`](TESTING.md#tier-gates) before the next semantic edit.

<a id="active-unit"></a>
## Selected work

- `gap`: [SG5 Number and token group runtime](SEMANTICS-GAPS.md#gap-sg5-number-token-group-runtime).
- `objective`: measure `FirstFilledValue` encounter order between two direct fields and between two nested subgroups through the accepted kernel runtime probe before changing the existing full-validation owner.
- `oracle`: the source-shipped [`kernelProbe` route](SOURCES.md#engine-routing-rule--pick-the-layer-by-the-question-not-by-habit), using a dmtool-authored self-contained model and exact documents in an ignored output workspace.
- `next`: construct one bounded separating matrix in temporary storage, verify both kernel strategies agree, and add red Lean cases only for outcomes the artifact actually exposes.
- `stop`: stop if the existing probe cannot distinguish the selected value, either sibling baseline changes, or the result would require a new runtime route, schema, or operand representation.
- `blocked-on`: none.
- `consumer-probe-trigger`: after the reusable SG5 group static-diagnostic family closes, run one bounded Translate/Explain probe over admitted, mapped, and rejected-unmapped decisions; after a reusable group-runtime family closes, consider one Execute probe. The selected calibration itself triggers neither a consumer probe nor SMT.
- `resume`: `sed -n '35,125p' ../a12-rulekit/docs/KERNEL-PROBE-SPEC.md`
