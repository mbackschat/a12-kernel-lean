# Active implementation plan

This is the minimal continuation checkpoint. Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

**Priority invariant:** eventual 100% semantic conformance with kernel 30.8.1 is the primary project goal. Consumer adequacy is the co-equal representation test. A supported-fragment boundary may stage delivery or state current evidence honestly, but may not become a permanent reason to omit kernel behavior.

## Verified baseline

- The current theory closes substantial checked nonrepeatable validation/computation, temporal, String, Enumeration, custom-field, resolved repeatable, message, and target fragments. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) is the sole detailed capability/proof/evidence inventory.
- Commit `51baed9` closes numeric `NumberOfValueInFields` over the existing checked direct/plain-star/filtered-star Number entity-list route in full validation, computation, and bounded partial validation. The resolved side retains filter provenance per selected cell, so a filtered current match may disappear while a filtered non-match remains grow-only. Empty cells do not match zero, scale-19 equality is shared, and the first formal cause owns the result. Full build, all 51 retained projections, and the 1,197-root trust audit pass.
- That capsule adds 306 nonblank Lean lines, exceeding the Tier 1 preference because one usable boundary requires the shared resolved count state, three existing phase adapters, trusted laws, and full-versus-computation-versus-partial separators together. Splitting it would land either an unconsumed provenance representation or a phase-specific count route; it adds no AST family, topology, filter evaluator, checked-document/result boundary, scheduler, harness, protocol, dependency, or support estate.
- The following integration capsule composes that value-count source through the existing numeric expression, generated-validation, and declaration-owned Number-target boundary. It preserves the checked source and per-cell selected-match witness, gives the result fixed scale 0, admits direct sources in scalar contexts, and requires addressed context for repeatable sources. It adds 223 nonblank Lean lines within the Tier 1 preference, no second AST/fold/resolver, and no support estate; the 388-job build, all 51 retained projections, and the 1,202-root trust audit pass.
- The String/ordinary stored-Enumeration value-count capsule reuses their checked token entity list and the generic value-count state for typed literal admission plus full validation, computation, and bounded partial validation. It preserves exact normalized/stored token identity, every Enumeration domain certificate, per-cell filter provenance, successor-filter poison, and selected-field/`Having` references. The feature adds 516 nonblank Lean lines, exceeding the Tier 1 preference because one usable typed source requires its static cross-declaration domain certificate, three phase adapters, and the repeated/filter/partial separating matrix together; it adds no equality, AST, topology, filter, checked-document, protocol, harness, or support-estate mechanism. The 391-job build, all 51 retained projections, and the 1,207-root trust audit pass.
- The future computation-scheduling and document-transition architecture is binding in [`ARCHITECTURE.md`](ARCHITECTURE.md#whole-model-computation-execution-keeps-definition-activation-result-application-and-validation-separate). Adoption does not select SG4: SG1 and SG2, an explicit processing context, and the bounded source audit remain prerequisites.
- Reference semantics remains 0.3.0 with the V2 flat-validation and one-group-correlation suites. Internally closed but uncalibrated families remain explicitly `external evidence pending`; coherent calibration work is indexed by [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage).

## Active capsule

Integrate the closed String/ordinary stored-Enumeration value-count source into the existing numeric expression, generated-validation, and declaration-owned Number-target atom under [`SG5`](SEMANTICS-GAPS.md#sg5--numeric-authoring-and-target-completion). Reuse the same checked token source, generic arithmetic tree, and addressed evaluation context; do not widen category or Boolean/Confirm authoring through this route.

Consumer hypothesis: an Execute/Analyze/Transform consumer must traverse one checked numeric expression, recover every selected String/Enumeration field and `Having` dependency, retain the all-Enumeration stored-domain certificate and fixed scale 0, and execute against caller-prepared checked String cells without resampling their policies. Flattening the token source to a literal plus field IDs, mapping structural address failure to UNKNOWN, or re-resolving Enumeration display text are unsafe controls. Scalar evaluation must accept only a direct source; a repeatable source without addressed context is an explicit insufficient-information result.

## Immediate frontier

1. Close the active token value-count expression/generated-validation/target integration with red/green TDD, checked dependency queries, scalar/addressed separation, and no second aggregate or target route.
2. Audit category-projected value count through its existing projection-bearing Enumeration owner. Audit Boolean/Confirm only after selecting an existing declaration owner for common Yes/No labels; `False` remains illegal for Confirm. Group expansion remains gated by SG1/SG2/SG8 rather than a value-count-specific representation.
3. Before selecting SG1, SG2, SG4, SG6, or another keystone, run the bounded [high-connectivity source packet](TESTING.md#high-connectivity-keystone-pre-audit) and its most dangerous discriminator. SG4 remains prohibited until every adopted prerequisite and source-audit gate is complete.
4. Reopen checked sub-day temporal integration only through one shared temporal-expression owner with dynamic `Now`, explicit `World`, and exact instant identity. The completed source packet disproves a field/Base-Year-only sub-day AST.
5. Batch external evidence under [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage) only after a coherent family closes; do not create one producer or harness per capsule.

## Parked boundaries

- Multiple/mixed value-list operands need the focused filter-encounter matrix; date-like distinct count needs exact compatible-format and TIME identity; temporal and wider RNU keys need exact stored text; temporal targets need declaration-owned rendering policy.
- Public protocol expansion, semantic shipments, candidate qualification, dependencies, SMT integration, and new evidence/process machinery require their existing explicit adoption or approval gates.
- No copied a12-dmkits harness, second numeric AST or scale gate, second aggregate scan, alternate group-state encoding, or parallel protocol route is permitted.

## Stop conditions

- Stop a candidate when its required model fact, consumer, or separating source witness is absent; update its one [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry rather than creating another queue item.
- Stop before introducing a duplicate semantic representation or any harness, schema, generator, protocol, registry, dependency, or governance layer without explicit approval.
- Stop before beginning [`SG4`](SEMANTICS-GAPS.md#sg4--computation-scheduling-and-state-transition) or freezing its general checked-document/result types until the accepted checked-document, repeatable-addressing, processing-context, and bounded source-audit gates are complete and this plan explicitly selects the first source-grounded scheduling capsule.
- Keep sibling repositories read-only and preserve their visible status. Never push without an explicit current request.

## Resume procedure

1. Read the active capsule above, its [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry, the linked [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) mechanism, and the applicable [`TESTING.md`](TESTING.md) rung.
2. Inspect `git status --short`, recent commits, the current owner/code/tests, Git history, and any active cross-project handoff before writing a red case.
3. Close one capsule or verified no-op, perform the same-context closure assessment, run its proportional gate, update only fact owners, delete a completed gap, and commit.
