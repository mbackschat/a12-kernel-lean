# Active implementation plan

This is the minimal continuation checkpoint. Detailed coverage belongs in [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md), open semantic obligations in [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md), durable conclusions in [`LEAN-FINDINGS.md`](LEAN-FINDINGS.md), and completed work in Git plus the [July 2026 work log](archived/SEMANTIC-WORK-LOG-2026-07.md).

**Priority invariant:** eventual 100% semantic conformance with kernel 30.8.1 is the primary project goal. Consumer adequacy is the co-equal representation test. A supported-fragment boundary may stage delivery or state current evidence honestly, but may not become a permanent reason to omit kernel behavior.

## Verified baseline

- The current theory closes substantial checked nonrepeatable validation/computation, temporal, String, Enumeration, custom-field, resolved repeatable, message, and target fragments. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) is the sole detailed capability/proof/evidence inventory.
- The latest repeatable work derives general group-star plans from validated model ancestry and feeds a canonical nested reopened tree plus deepest-level-fastest Number cell stream from explicit document rows, including contextual over-repetition suppression. Resolved validation `Having` filters that stream before retained target classification. All-rows aggregate relevance and order-aware per-cell `FirstFilledValue` relevance share exact path/index patterns while preserving their different decisions: the former requires wildcard coverage before any target read, while the latter interleaves concrete relevance with the prefix-terminating scan. Computation-filter poison/dependency and multi-operand filter/relevance encounter remain outside this boundary.
- The shared resolved-condition capsule is fully green: a 336-job `lake build`, 51/51 retained-observation replay, and the trust audit over 941 theorem roots / 17,068 declarations / 192 logical modules. One connective tree now serves flat and resolved numeric-comparison leaves with universal flat verdict/reference preservation; checked mixed whole-rule assembly remains next.
- Reference semantics remains 0.3.0 with the V2 flat-validation and one-group-correlation suites. Internally closed but uncalibrated families remain explicitly `external evidence pending`; coherent calibration work is indexed by [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage).

## Active unit

[`SG3`](SEMANTICS-GAPS.md#sg3--shared-checked-condition-and-expression-tree) continues from the shared resolved connective tree. The next code capsule must assemble a checked mixed condition from already-checked flat and numeric leaves while preserving model identity, row group, at-least-one-field admission, reference traversal, relevance, scale, and target self-reference rejection; it must not accept a caller-forged resolved core.

## Immediate frontier

1. **Checked mixed assembly ready — [`SG3`](SEMANTICS-GAPS.md#sg3--shared-checked-condition-and-expression-tree):** lift checked flat and numeric leaves into the shared resolved tree, then let whole-rule assembly consume that checked tree. Preserve each leaf certificate and one reference traversal; do not create an aggregate-only comparison wrapper or parallel condition tree.
2. **Resume after SG3 — [`SG2`](SEMANTICS-GAPS.md#sg2--general-repeatable-addressing-and-operand-construction):** use the shared checked tree to lower authored repeatable filters, then close mixed declarations and multi-operand `FirstFilledValue` encounter order over the existing topology.
3. **Missing source facts — [`SG6`](SEMANTICS-GAPS.md#sg6--temporal-authoring-calendar-and-target-completion) and [`SG7`](SEMANTICS-GAPS.md#sg7--string-pattern-and-custom-field-completion):** temporal targets need declaration-owned format/rendering/value admission; String targets need declaration-owned length and line-break policy.
4. **Milestone calibration — [`SG12`](SEMANTICS-GAPS.md#sg12--retained-kernel-correspondence-coverage):** batch external evidence only after a coherent family closes. Do not create one producer or harness per capsule.

The next cold consumer probe is triggered only after SG2 closes a reusable repeatable construction family, after another major addressing/computation boundary changes, or before a public capability claim. Ordinary capsules receive the mandatory same-context assessment in [`TESTING.md`](TESTING.md#same-context-capsule-closure-assessment).

## Parked non-semantic boundaries

- Public protocol expansion, semantic shipments, candidate qualification, dependencies, SMT integration, and new evidence/process machinery require their existing explicit adoption or approval gates.
- Flat literal-scale or other protocol-sensitive seams require deliberate protocol versioning or a separately adopted public-boundary change.
- No copied a12-dmkits harness, second numeric AST or scale gate, second aggregate scan, alternate group-state encoding, or parallel protocol route is permitted.

## Stop conditions

- Stop a candidate when its required model fact, consumer, or separating source witness is absent; update its one [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry rather than creating another queue item.
- Stop before introducing a duplicate semantic representation or any harness, schema, generator, protocol, registry, dependency, or governance layer without explicit approval.
- Keep sibling repositories read-only and preserve their visible status.
- Never push without an explicit current request.

## Resume procedure

1. Read this file, the selected [`SEMANTICS-GAPS.md`](SEMANTICS-GAPS.md) entry, its linked [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) boundary, and the applicable [`TESTING.md`](TESTING.md) rung.
2. Inspect `git status --short`, recent commits, the current owner/code/tests, Git history, and any active cross-project handoff before writing a red case.
3. Close one capsule or verified no-op, perform the same-context closure assessment, run its proportional gate, update only fact owners, delete a completed gap, and commit.
