# Open semantic gaps

This is the open-only work index for convergence toward complete semantic conformance with A12 Kernel 30.8.1. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) is the sole detailed owner of implemented/proved/evidenced coverage. Working context selects uninterrupted work from this index; [`PLAN.md`](PLAN.md) persists that selection only for cross-session handoff.

## Query contract

<a id="lifecycle"></a>

Open only the selected `gap-` record where one exists; otherwise open the legacy `SG<n>` entry. Detailed implemented facts stay behind implementation-map links.

```sh
rg -n '^<a id="gap-' docs/SEMANTICS-GAPS.md
rg -n '^### SG' docs/SEMANTICS-GAPS.md
rg -n '^- `state`:|^- `blocked-on`:|^- `reopen-when`:|^- (Open|Blocked on|Prerequisite|Unresolved source fact|Most dangerous discriminator|Consumer hypothesis|Forbidden shortcut|Evidence status|Entry gate|Completion gate):' docs/SEMANTICS-GAPS.md
```

Carry a `Blocked on:` field when a representation-affecting alternative has stopped dependent work on that branch, naming what would settle it and where that request already lives: an `EXP-` entry in [`A12-DMKITS-SPEC-SYNC-LEDGER.md`](A12-DMKITS-SPEC-SYNC-LEDGER.md) when a kernel observation would settle it, or the blocker list in [`PLAN.md`](PLAN.md) when an owner decision would. A blocked branch is not the same as an unscheduled one, and only this field makes the difference greppable.

A gap remains only while a Kernel behavior, static-legality rule, checked construction, or conformance obligation is open. Delete a completed entry; Git records its history. Never append a completed implementation narrative. `SG<n>` identifiers are not reused.

## Hard-frontier critical path

The binding construction order is **SG1 → SG2 → SG4**. SG1 and SG2 are closed: [immutable checked-document construction](IMPLEMENTATION-MAP.md#immutable-checked-document-construction) supplies one model-certified input, and the [repetition/iteration boundary](IMPLEMENTATION-MAP.md#9--repetition-and-iteration) supplies canonical addressed operands. SG4 must consume both with an explicit processing context rather than redefine document or address state.

## Keystone construction gaps

### SG4 — computation scheduling and state transition

- Missing invariant: authored computation tables, dependency order, poison/clearing, repeatable targets, rich result projections, exact application, and later validation must compose without collapsing definition, transient state, dependency outcomes, public results, source-relative deltas, or applied document state.
- Current boundary: [§11](IMPLEMENTATION-MAP.md#11--calculations-and-formal-checking) owns the implemented surface; the stable phase separation is in [`ARCHITECTURE.md`](ARCHITECTURE.md#whole-model-computation-execution-keeps-definition-activation-result-application-and-validation-separate).
- Open:
  - retain route attribution for addressed index clearing when an Explain consumer requires it;
  - retain a `NumericComputationRunFault.targetCheck` witness if one becomes reachable. The run fault's target label is now proved on both constructors and separated by a later-table `.evaluation` case, but no authored computation is known to reach the target-check arm as of 2026-08-11: unsuppressed assignment-scale admission rejects a derived-scale mismatch statically, and an authored suppression routes evaluation to `checkWithScaleWarningSuppressed`, which reports a rejected attempt rather than `.unsupported`. The arm stays fail-closed and unwitnessed rather than being claimed unreachable;
  - extend the applied-document boundary beyond one finite direct Number repetition level and nonrepeatable String targets. The bounded Number projection closes numeric predecessor order, exact action coordinates, unrelated destination-cell preservation, the ERRORED non-creation deviation, and explicit later `Number > 0` validation addresses. The root String projection validates every unique source-classified action against a checked caller destination before delegating to the established type-neutral action order; it remains a target-state function rather than a reconstructed document. Repeatable String, nested Number, other target families, general later-rule execution, transport-level document reconstruction, and pointer dialect rendering remain outside those result domains;
  - certify source/destination model compatibility at the retained-result type boundary if a future consumer cannot supply that precondition; the current unindexed result validates every action address against the destination model but cannot prove model identity;
  - calibrate wider multi-step String/Number composition and repeatable computation against separating retained Kernel observations. The bounded internal routes are cataloged in the [implementation map](IMPLEMENTATION-MAP.md#11--calculations-and-formal-checking). The finite nonrepeatable mixed String/Number executor is proved sound against an independently enabled successful-step relation: unrelated steps may run in either order, while a static typed dependency remains disabled until its producer completes even when runtime selection leaves the read unreached. Its structural failure boundary preserves the original String-or-Number fault constructor, attributes each atomic failure to the step evaluated, and proves that every public failure names one of the mixed run's checked targets; a later String fault separates that label from the first Number target. The same purpose-specific relation now terminates a failing fixed execution after its exact successful prefix at one enabled typed structural failure, with the prefix state unchanged by the failure and reconstructed by the retained successful outcomes. Rich accepted, rejected, and cleared target outcomes remain successful transitions. An exact two-step authored pair retains both authored and checked execution target order, reversing only the consumer-first edge; its retained Kernel representative separates fresh producer visibility, an unread invalid producer behind a selected safe row, reached dependency poison, and empty Number zero. The finite repeatable Number executor is likewise sound against an independently enabled whole-table relation whose private state records table completion separately from exact addressed outcomes; an invalid-index seed may emit no outcomes and still enable its dependent table. Its purpose-specific failure relation conditionally decomposes any returned structural fault into the exact successful batch prefix followed by one enabled target-attributed failure at unchanged prefix state, reconstructing completed table labels separately from flattened addressed outcomes. No valid current fixture witnesses a structural table error, so failure reachability and exact Kernel failure-schedule correspondence remain open rather than inferred from the conditional proof. Both fixed complete-scope cross-family `CurrentRepetition` cascades expose their successful family phases. Number-to-String completes every addressed Number target before String can transition. Its structural-failure relation now separates the concrete no-row Number fault at the initial state from a concrete missing-target-matcher String fault after the exact successful Number phase; every returned `executeWithRead` failure decomposes into those forms and retains no String completion. The later separator uses an intentionally incomplete prepared-pattern set at the existing fail-closed API boundary, not an output of normal pattern preparation. String-to-Number completes every addressed String target first and keeps dependency-cell conversion at the Number-phase boundary, so its distinct conversion fault is not hidden inside the producer phase. Each successful executor is exactly its two transitions, neither dependent phase can start from the initial state, and the retained phases reconstruct the exact addressed rows. The inverse String-to-Number cascade also owns its structural-failure boundary: a concrete no-row String fault leaves the initial state unchanged, while every returned later Number fault conditionally requires and retains the exact successful String transition with no Number completion. Rich no-value and poison outcomes remain successful typed phases. No current valid fixture witnesses the later Number fault, so its reachability and exact Kernel failure schedule remain open. The fixed alternating extension reuses the Number-to-String phases and adds one final Number transition whose dependency conversion remains at that third boundary. Every successful alternating execution is exactly those three transitions, neither dependent family can appear initially, and the three phases reconstruct its exact addressed rows. Its structural-failure relation now separates a concrete no-row prefix fault, a concrete missing-target-matcher String fault retaining the exact Number phase, and a conditional terminal Number fault retaining both successful prefix phases. Every returned failure decomposes into those three positions and retains no terminal completion. The incomplete matcher set is again an adversarial fail-closed API input, while terminal-fault reachability and exact Kernel failure scheduling remain open. The aggregate-seeded mixed route has purpose-specific success and failure relations that retain the completed aggregate separately from the seeded finite String/Number suffix. Every outer execution fault decomposes into either an aggregate-prefix fault at empty state or a seeded scalar failure retaining its successful suffix prefix and exact typed terminal fault; the aggregate seed never appears in the suffix-outcome list. A concrete adversarial matcher set, which omits Label and is not an output of normal pattern preparation, reaches the later String fault after the aggregate and one Number suffix step, while aggregate-prefix fault reachability and Kernel failure scheduling remain open. Retained exact Kernel correspondence for the named one-level `Produced -> Total -> Label -> Doubled` successful representative separates fresh completed state, false-first String fall-through, an empty producer, poison across both family boundaries, and the no-row zero aggregate. The direct Number, fixed two-step repeatable Number, Number-to-String, and direct repeatable String suffixes have the same phase boundary over their exact-row overlays. Named representatives for all six bounded one-level producer-to-aggregate-to-repeatable suffix forms are retained below: direct Number, fixed two-step Number, fixed Number-to-String, direct repeatable String, ordered subtraction, and warning-suppressed division. The subtraction and division representatives each cover both aggregate/row operand orders and separate aggregate-wide from row-local poison; division additionally separates warning-suppressed no-fit storage and zero-divisor order. Other binary operators, unsuppressed division, mixed plans beyond this exact two-step representative, and generic scheduling remain externally uncalibrated. All four fixed `CurrentRepetition(group) > 0` chains admit the source's complete repeatable scope while evaluating the terminal coordinate; a two-level `[10,20]` separator retains full leaf addresses and fresh Number/String state even when `[1,1]` and `[2,1]` share coordinate `1`, and localizes malformed poison through the alternating chain. The aggregate prefix likewise admits any nonempty repeatable producer scope only when its sole root star reopens every producer level; its two-level direct String round trip retains exact leaf addresses, fresh `MaxValue`, fan-out, and cause-blind poison, while a leaf-only star requiring binding `[10]` is refused statically. Exact nested aggregate correspondence remains external evidence pending. Remaining work includes wider external row counts, multiple aggregate operands, wider filters and row wrappers, other nested aggregate suffix representatives, wider application/validation families, concrete later inverse-cascade, alternating-terminal, and other repeatable structural-failure reachability, and wider cross-family repeatable state-transition boundaries.
- Unresolved source fact: the capability packet closes supplied order/cycle rejection, flattened first selection, reached-read poison, stripping/overlay, eager operand checking, pointer result partition, application order, validation timing, and locus-dependent repeatable clearing. a12-dmkits `f6dc22d2` confirms on a repeatable Number target that retained CLEARED and changed VALUE create a formerly absent addressed target/row in a separate destination, while no action and ERRORED preserve absence; generic kernel application source establishes the same retained-action boundary across target families and complete directly addressed ancestry. Reviewed a12-dmkits `975a2e78602eaf26126168cd203bd84738885322` closes the direct one-level topology: an action at coordinate `k` produces the numeric predecessor prefix, actions keep their coordinates across clear-before-change processing, existing destination cells survive, gaps do not refuse application, and later validation without recomputation observes the resulting exact rows. Lean implements precisely that finite Number projection. Nested predecessor topology, other target families, general validation, transport reconstruction, and pointer spelling remain unmeasured or unimplemented and reopen only for a selected consumer.
- Most dangerous discriminator: dependency edges order generated target methods but do not pre-skip a dependent after failure. Invalidity propagates only through a reached read, which short-circuiting can hide. Parallel index clearing is a separate coarse, locus-dependent mechanism.
- Consumer hypothesis: Execute, Compile, Analyze, and Explain must distinguish checked definition, transient activation, cause-blind dependency invalidity, rich family outcomes, source-relative results, application, and validation. Private schedule/list order is not a public collection-order contract.
- Forbidden shortcut: do not use deltas, applied state, or public result collections as the dependency overlay; merge eager operand errors with dependency poison; duplicate the checked document/address plan; or introduce a generic graph, state machine, cross-family trace framework, or registry.
- Evidence status: maintained a12-dmkits differentials Kernel-lock isolated repeatable/index and value-less-target result-channel seams, typed Number V2 source identity at `7b2e9f7`, cause-blind computed-target dependency erasure at `351bf9f2`, selected-empty same-target termination versus false-guard fall-through at `4ebe84fc`, the structural-guard direct-Number dependency shape at `43359ae258e1208e4de63ef00f684cd2a5250d7a`, four two-row direct/cross-family value, empty, and malformed matrices at `cd43c249a88874bf5688a45060e208a0dcd20782`, producer-target-error rows for both String-to-Number boundaries at that revision, direct computed-Number star-aggregate value and poison transport through maintained tests at the same clean HEAD, non-vacuous Boolean computed-state transport into downstream filters through `HavingOverComputedDependencyDiffTest` last changed at `c24d81ac7697020639a9a95165b647e8e370954d`, root-to-repeatable direct Number fan-out at `108d047b787f0e049dcd2f4902f24d7523347a00`, retained CLEARED/no-action/ERRORED/VALUE application to a separate destination at `f6dc22d2`, and direct one-level predecessor padding, coordinate preservation, destination preservation, and later-validation rows at reviewed `975a2e78602eaf26126168cd203bd84738885322`. Kernel source at `cb66e51fa7ab90b650698f861bf670754e2e1e66` establishes the type-neutral retained-action fold used by the internally closed root String checked-destination projection; direct String runtime calibration remains pending. The exact consumer-first two-step mixed scalar representative, the fixed three-step chain, the named one-level aggregate-seeded finite mixed String/Number route, and all six bounded one-level aggregate-to-repeatable suffix forms have retained composed Kernel observations through their named representatives. The two-level fixed Number-to-String and alternating `CurrentRepetition` representatives at `975a2e78602eaf26126168cd203bd84738885322` retain exact-address fresh, empty, malformed, and concrete-leaf-only outcomes without claiming private phase or emission order. Field-product aggregation, the computed-Number-only filter specialization, other binary operations, unsuppressed division, mixed scalar plans beyond the exact two-step representative, aggregate-seeded finite Number runs, the two-level aggregate round trip, and the Number-only and String-to-Number two-level fixed `CurrentRepetition` chains remain without non-vacuous external correspondence. Repeatable String, nested Number, and other-family applied-document topology plus general later validation remain `external evidence pending`.
- Entry gate: passed for String, scalar Number, the exact two-step mixed authored-order boundary, the conservative repeatable Number packet, root String checked-destination application, and bounded direct one-level Number application plus later validation. Reopen only at the source triggers above.
- Completion gate: an executable checked scheduler is deterministic for fixed checked inputs and sound against an independently meaningful relation where one is justified; result and application laws preserve every named public partition and validation remains separate.

## Semantic-family gaps

### SG5 — numeric authoring and target completion

- Missing invariant: close the remaining numeric authoring shapes, computation wrappers, repeated/group aggregates, temporal/aggregate overloads, concrete ingestion/rendering, and partial/repeatable integration through the shared owners.
- `open`: the shared repeatable placement now admits an operand at any scope the target's own scope **binds**, which is the measured Kernel rule; the former scope-equality requirement had refused every Kernel-legal form-level constant feeding a row across eleven source families. The certificate carries `repetitionBoundBy`, the same predicate the validation locus already uses, and the two read sites that derived the operand's address from the target's path now derive it from the operand's own scope, so one outer cell reaches every target row with its own emptiness and its own formal invalidity. The [operand-scope checkpoint](SOURCES.md#src-computation-operand-scope) owns the measurement and accepted [SPEC-2026-08-23-06](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-06--a-computation-operand-is-admitted-by-the-targets-scope-binding-its-levels-not-by-scope-equality) the clause. A group below the target stays refused. A sibling repeatable group instead selects the parallel-iteration route: matching index-field names admit it, different names draw `MVK_ERROR_FIELD_INVALID_INDEX`, no index draws `MVK_NO_WILDCARD`, and a starred aggregate is admitted.
- `open`: the widening is Kernel-calibrated at runtime too, on both codegen strategies, and neither probe batch corrected anything. One root cell reaches every target row as a value, as the substituted zero when the root is empty, and as a cleared target when the root is formally invalid; and on a **two-level** target a middle-level operand reaches exactly the leaves of its own enclosing row, with two enclosing values separating that from both wrong accounts and an invalid enclosing operand clearing only its own leaves. That closes the multi-level correlation risk for this mechanism. What stays uncalibrated is the operand shapes other than a direct Number copy, all of which share the one read site the cases lock, and a third nesting level.
- Current boundary: [§5](IMPLEMENTATION-MAP.md#5--numbers-and-decimals) owns the wider numeric surface. The bounded group-operand capabilities start at [shared admission](IMPLEMENTATION-MAP.md#cap-shared-entity-list-group-admission) and link to their family records.
- Open: one repeatable addressed `FieldValueAsString` placement and the inverse direct Number assignment, root `Abs(Number)`, all three root Round modes, every nonempty ordered `Min`/`Max` over direct fields, operand-local `Abs`/Round wrappers, or operand-local direct-field addition, subtraction, or multiplication plus one immediate decoded literal inserted anywhere, binary `+`/`−`/`×`, warning-suppressed division, scale-0-field-exponent power, `FieldValueAsNumber`, `RangeAsNumber`, and ordinary String `Length` placements now execute through exact checked cells and retain row keys through their existing family result/application owners. Direct assignment, root wrappers, extrema, and binary arithmetic share the certified Number-source placement while retaining distinct scale contracts. The extremum specialization keeps unary tags on one source and the shared `NumericArithmeticOp` tag over one ordered `CheckedAddressedNumberArithmeticChild`; it imports neither target-owning addressed operation and introduces no general recursive expression tree. Each arithmetic pair contributes operation identity, both dependencies in inner order, its operation-derived scale — maximum for the additive nodes, sum for the product — and one outer operand position; one literal retains exact value, authored signed scale, and insertion point. A second immediate literal fails closed. An arithmetic child now admits one immediate literal at either inner position, contributing its authored scale — `1.5` and `1.50` derive different child scales — while the outer list keeps its own separate one-literal budget. One nested `Min` or `Max` call over direct field/literal leaves now remains one checked outer operand, retains its selector and leaf order, contributes its own dependency and scale union, evaluates at the target-owned row, and owns an independent one-literal budget. Target admission now routes through the shared `exactNumericScaleComparisonAllowedWithSuppression` predicate over a derived `NumericScaleSummary`, the same gate the scalar computation owner already used, so a capability-carrying list is admitted at a larger declared target scale while a capable larger derived scale and any non-capable mismatch stay rejected. Signed literal scales compose directly, so an integer literal stripping to a negative authored scale is admitted rather than failing closed. No operand has to be field-backed. `CheckedAddressedNumericTarget` owns the declaring group, target field, target declaration, complete target policy, repeatability, and both path-indexed executors, so a constant-only arithmetic child, a literal-only list, and any other fieldless list are admitted and iterate once per instantiated row at the target's own repeatable scope. `CheckedAddressedNumericPlacement` keeps only the source facts and forwards every target fact, leaving the one-source owners unchanged. The enclosing call still admits at most one immediate constant, and constants inside an arithmetic child do not count against that budget. Division- and power-valued operands remain outside this fragment: they add unknown derived scale and the warning-suppression branch to the operand list. Every deeper nested call and every nested wrapper, arithmetic, division, or power leaf remains outside this Lean fragment. Real-kernel authoring also admits direct/wrapper/literal/arithmetic order, both inner operand orders, and a literal sibling; it separates derived-scale mismatch, target self-reference, immediate-literal wrapper rejection, the one-literal budget, and rounding place 15. Current public interpreter output carries exact repeatable row pointers for measured value/outcome classes but remains cause-blind, so first-poison identity stays internal. Maintained three-engine cases separately cover direct same-scale repeatable values and poison, while scalar division and power differentials cover staged precision, domain boundaries, and warning-suppressed storage. a12-dmkits `112169fa` restores structured suppression authoring and clean division/power representatives; the maintained differentials remain the broader runtime and kernel-strategy owners. The conversion leaf admits the statically convertible `[0-9]+` String profile plus stored Enumeration and named category projections; the range leaf retains its exact interval and arbitrary evaluated String source; the Length leaf reuses evaluated UTF-16 text and scale 0. Division/power-valued operands, deeper or wrapper/arithmetic nested extrema, aggregate operands, literal/nested/grouped addressed power, host wire ingestion, and remaining group/addressed expression placements must reuse those owners.

<a id="gap-sg5-unstarred-repeatable-group-presence-reference"></a>
#### Unstarred repeatable group-presence reference

- `state`: open
- `missing`: select the group operand's own reference coordinate when group presence admits an unstarred repeatable group; the projection remains fail-closed rather than inventing a concrete or wildcard coordinate.
- `baseline`: [group-operand reference projection](IMPLEMENTATION-MAP.md#cap-group-operand-reference-projection) covers nonrepeatable group presence and every group carrier with a witnessed bound or reopened level.
- `evidence-needed`: a kernel reference row for the admitted unstarred repeatable group-presence form at an unbound own level.
- `discriminator`: compare a concrete own-level coordinate with a wildcard own-level coordinate while preserving the same descendant expansion and outer bound prefix.
- `consumer`: Explain must not present an incomplete or invented `referenced` set for an admitted condition.
- `reopen-when`: a source route exposes the reference coordinates for this exact admitted form.

<a id="gap-sg5-number-token-group-runtime"></a>
#### Number, token, and Boolean group runtime

- `state`: open
- `missing`: execute retained Boolean group value-count computation over fixed groups outside the measured two-direct-field boundary and groups containing declarations below a deeper repeatable level, non-value-count token group operands through computation, Number and Boolean plus non-value-list token group operands through partial validation, and all three families through legacy raw-`Document` routes.
- `missing`: make Number directional missingness per cell before admitting a group whose declarations disagree on signedness.
- `missing`: decide Enumeration/category `NumberOfValueInFields` literal admission over a starred group independently of the current all-expanded-declarations local choice; the measured all-String group does not discriminate that gate.
- `prerequisite`: legacy raw-`Document` readers must expose instantiated-row topology before they can enumerate the operand-bounded group extent.
- `baseline`: [Number group aggregates](IMPLEMENTATION-MAP.md#cap-number-group-aggregates) owns checked-document full-validation and computation execution; [token group expansion](IMPLEMENTATION-MAP.md#cap-token-group-expansion) owns checked-document full validation, starred String-group value-count checked computation, fixed token value-count validation authoring, and partial token value-list execution; [Boolean/Confirm group value count](IMPLEMENTATION-MAP.md#cap-boolean-confirm-group-value-count) owns checked-document full-validation execution and the fieldwise single-axis starred-group checked computation.
- `evidence-needed`: measure each remaining refused route and isolate any declared-tail effect from the independently measured no-row and instantiated-empty prefixes before representing omitted capacity.
- `discriminator`: vary rule depth, operand depth, instantiated rows, and signedness independently while keeping the recursive declaration expansion fixed.
- `consumer`: Execute needs exact operand-bounded extent and fillability; Analyze needs encounter order and the distinction between authored slots and reached cells.
- `reopen-when`: a coherent group-runtime batch is selected.
- `review`: `StarAddressingError.unsupportedGroupOperand` remains the shared refusal for the still-open non-value-count token computation and non-value-list partial routes, fixed and nested-repeatable Boolean routes, and raw-document routes; the checked-document closures do not yet warrant relocating it.
- `route-state`: verified for checked-document Number combiner computation, checked-document starred Number-group, String-group, and fieldwise single-axis Boolean-group value-count computation including their in-capacity content domains, full-validation starred token value count runtime, fixed token value-count static admission, partial token value-list evaluation, the full-validation plain-star filled-field count's over-capacity boundary, and direct one-level partial extent for filled-field, filled-group, and numeric value counts; discovery-required for the remaining routes.

#### SG5 family completion

- Completion gate: every legal numeric operator and authoring region is checked and executable where the Kernel permits it; illegal shapes fail at the correct static layer, and target rendering/application and fillability retain their separators.
- Consumer and evidence: Execute and Transform require exact tree shape, rounding stages, target policy, and unsafe-reassociation boundaries. Project-local portable calibration remains partial.

<a id="gap-sg6-temporal"></a>
### SG6 — temporal authoring, calendar, and target completion

- `state`: open.
- `missing-invariant`: temporal parsing, admission, construction, model-zone legacy calendar stepping, additions and differences, DateRange operations, partial/formal propagation, and stored/computed targets must preserve exact instant, decoded components, format, and calendar provenance together.
- `implemented-owner`: [§6](IMPLEMENTATION-MAP.md#6--dates-and-time) owns implemented temporal boundaries; [temporal comparison and aggregates](IMPLEMENTATION-MAP.md#cap-temporal-comparison-and-aggregates), [checked DateRange declaration](IMPLEMENTATION-MAP.md#cap-date-range-checked-declaration), and [checked direct DateRange bounds](IMPLEMENTATION-MAP.md#cap-checked-date-range-bound) own the reviewed DateRange subsets.
- `prerequisite`: [`TemporalFormat.lean`](../A12Kernel/Semantics/TemporalFormat.lean), [`FullDate.lean`](../A12Kernel/Semantics/FullDate.lean), [`DateTime.lean`](../A12Kernel/Semantics/DateTime.lean), [`BerlinLegacyTimeZone.lean`](../A12Kernel/Semantics/BerlinLegacyTimeZone.lean), comparison/aggregate/difference owners, and checked numeric-source/expression owners; dynamic inputs receive an explicit `World`.
- **Checked-document temporal coherence is decided, 2026-08-23: it is a property a consumer asks for, not a construction gate.** [`cap-document-temporal-coherence`](IMPLEMENTATION-MAP.md#cap-document-temporal-coherence) owns `CheckedDocument.temporallyCoherent`, which reports whether every placed Date or DateTime cell agrees with the value its own classifier derives from that cell's stored text. Two rounds of attempting it as an unconditional arm beside the existing `dateRange`, Boolean, Confirm, and Number arms failed, and the second round produced the refutation rather than another cost estimate. **The blocking reason is semantic, not fixture hygiene.** The duplicate-value clause compares a temporal cell's *exact stored text*, so the case pinning which account that clause states needs two cells whose text and decoded value disagree — `TemporalValuesNotUnique` carries that pair deliberately and already records under `SPEC-2026-08-05-03` that whether a real model can store one is open. A gate would delete the clause's own subject from the representation. A second, independent reason: only a full-precision Date and a DateTime have an owning input classifier, so a gate could cover half a temporal family at most, which is an invariant no consumer could rely on; a property may honestly report on the half it knows. `FullDateFirstFilled`'s `copied-text-would-be-wrong` cell and `DateTimeFirstFilled`'s instant-versus-wall-label separator are the same class. The comparator is **not** `RawCell` equality: stored text cannot express a sub-second remainder, and milliseconds surviving an exact shift is already locked here, so the instant is compared to within one second while everything the text does determine is compared exactly — a whole-hour disagreement, such as a UTC-resolved instant under a non-UTC model zone, still fails. **Time and partial Date stay outside for a representation reason, not an oversight:** a partial value denotes an interval with no `Value` form, and a Time `Value` needs an instant its classifier declines to invent; the property asserts that limit rather than assuming it. Four DateTime computation modules' fixtures were migrated to derive each target cell's value from its own label, so they now describe reachable documents; that is hygiene the property makes checkable, and it is not required by any gate.
- **Stored Time and DateTime input are both closed, 2026-08-23**, at [`cap-time-stored-input`](IMPLEMENTATION-MAP.md#cap-time-stored-input) and [`cap-datetime-stored-input`](IMPLEMENTATION-MAP.md#cap-datetime-stored-input). Time has exactly one formal cause and that is proved, not sampled; DateTime has two and carries the Date's own `1583-10-16` floor. Neither adds a temporal cause, confirming the basic-check audit's prediction. Both reuse existing parsers rather than restating them — the authored Time literal's decoder and the full-Date component parser — so the family has one lexical account per half. What stays open for them is checked-document integration, other zones, repeatable placement, and rendering.
- **Corrected 2026-08-23:** the checked DateTime target vocabulary admitted the day-first `dd.MM.yyyy'T'HH:mm:ss`, which the model gate refuses; that string tags DateTime *expressions* in the kernel rather than naming a declarable format. Narrowed to the single measured format, with seven conformance modules' fixtures migrated and one different-format control repointed at the degenerate clock format. DateTime has one declarable storage format where Date has two.
- **`DateFromDateTime` closed 2026-08-23 and extended 2026-08-24**, at [`cap-date-from-datetime`](IMPLEMENTATION-MAP.md#cap-date-from-datetime). It reads the source label's own date with the time dropped, measured against a competing wall-to-UTC rule on a near-midnight witness, and its result is a Date established by two `MVK_INVALID_COMPARE_TO_DATE` refusals rather than by an admission. The capsule also **consolidated** rather than added: the Kernel imposes one complete-DateTime source requirement on both component extractors, measured including a plain Date field refused at the operator, so `TimeFromDateTime`'s gate now delegates to one shared predicate. Peer-accepted as [SPEC-2026-08-23-13](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-13) with an explicitly declared `Europe/Berlin` model zone, removing the ambient-zone limit. Scalar, one-level same-scope and root-to-row, and two-level middle-to-leaf repeatable computation carriers are locally calibrated on both Kernel strategies and the interpreter but have no maintained or retained lock. The repeatable carrier reuses `repetitionBoundBy`, reads a source at its own scope, and preserves exact source/target row keys, clean no-value, formal poison, and no-row or no-leaf execution without adding a second addressing representation. Sibling parallel correspondence, a third nesting level, addressed result/application channels, and a valueless source's reason propagation remain.
- **`NumberOfFilledFields` closed 2026-08-23** in the explicit-field validation arm, at [`cap-filled-field-count`](IMPLEMENTATION-MAP.md#cap-filled-field-count), found by sibling asymmetry: `NumberOfFilledGroups` existed and the field count did not. A formally invalid operand makes the count unavailable, which refutes both skipping it and counting it as filled; an all-empty list still counts `0`. Peer-accepted as [SPEC-2026-08-23-14](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-14). The **computation** arm needs no reducer rule of its own, which is measured: an invalid operand clears the computed target through the general operand-poison rule before the count is reached. The checked-document full-validation group result is closed for one fixed ordinary group and one terminal repeatable starred group, reusing the shared recursive extent and returning exact counts; retained a12-dmkits rows calibrate fixed threshold outcomes and distinguish complete starred traversal from per-row, row-1-only, and unexpanded reads. Reviewed a12-dmkits `975a2e78602eaf26126168cd203bd84738885322` additionally closes the measured movement seams: a fixed field group becomes fixed only when its expanded direct-field extent is full, a fixed multi-group count treats each group as one declared entity, and a starred group compares structural rows with declared capacity even when rows contain no value. The exact plain-star field source is closed too at the over-capacity boundary: it excludes the over-limit cell while an in-capacity malformed cell remains unavailable, and the same capacity projection corrects the measured `Sum` instance without erasing the physical row or structural group presence. Its checked-document partial-validation route is closed for the exact plain-star source. Open: direct and mixed lists, filtered and nested stars, wider partial-validation shapes, the computation carrier, raw-document execution, wider message details, and the rest of the count family. See [LF105](LEAN-FINDINGS.md) for why a name-keyed coverage estimate of this estate is unreliable.
- **Component-omitting DATE formats closed 2026-08-23 for all five spellings**, at [`cap-omitting-date-input`](IMPLEMENTATION-MAP.md#cap-omitting-date-input). Their failures are **one** cause because no complete date is present to fall below the floor, and the separator is exact in both directions. The three year-bearing formats reuse `PartiallyKnownDateValue` and the two yearless ones reuse the DateRange family's `MonthDayValue` with its `yearlessLastDay` bound, so the value domain did not grow and the yearless day rule is one account rather than two. Their **comparison** needed no new rule, which the measurement established rather than assumed: the existing gate is year presence after Base-Year supplementation, so `yyyy` compares with `yyyy-MM` and with a complete date while a yearless operand is refused against any year-bearing one. The component set is derived from the format so the two cannot disagree. Peer-accepted as [SPEC-2026-08-23-15](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-15). Their **aggregation** is measured too and needed no new rule, but it corrected the peer-facing clause: the aggregate gate reads the format's **component set**, not its spelling, so the two differently-spelled equal-set pairs are admitted by `MaxValue`, `MinValue`, and `NumberOfDifferentValues` at three loci alike, while `FieldValuesNotUnique` is the one carrier whose gate really is the format string, under its own code. A declared Base Year lifts the aggregate refusal for a yearless operand exactly when the remaining components agree. That correction is peer-accepted as [SPEC-2026-08-23-16](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-16), including the cross-kind equal-component separator. Open: how these values feed a computed target, and repeatable placement.
- `remains`: remaining temporal formats, legal zones and pre-floor identities, remaining repeatable placements, broader recursive lowering, partial-validation, rule/message integration, wider operands, and stored/computed target coverage.
- **Partial-Date stored input closed 2026-08-23, and the prerequisite the audit named is discharged.** The audit above said the open item was a DATE_FRAGMENT stored value with no value-domain owner. That was half right: the *deliberately imprecise* fragment kind still has none, but the **partially known Date** — a DATE declaration whose precision admits a monotone zero suffix — already had its value shapes inside `ValueAsDate`, and they only needed extracting so a stored-text classifier could sit below the operation instead of importing it. [`cap-partial-date-stored-input`](IMPLEMENTATION-MAP.md#cap-partial-date-stored-input) owns the implemented surface. Its cause assignment is measured and corrected one local defect: reality is judged before position in time, so an impossible month beside a legally omitted day is the format cause while a below-floor completion is the date cause. What the too-early cause covers is now both of its kernel arms, as the audit predicted, with **no fifth temporal cause added**. **Both declared spellings are measured**: the German `dd.MM.yyyy` declaration reproduces the ladder, the two-cause split, and the floor cell for cell, so the omission marker is the zero component rather than a position in the ISO text. The `ValueAsDate` operation now **consumes** that classifier instead of carrying its own copy of the ladder: its raw adapter had coarsened every failure to one `malformed` finding, and the consolidation both deletes the duplicate and corrects the cause. A live row at each precision now separates an over-deep omission's rejection from a genuinely admitted unknown year's cause-free non-relevance, which the previous single row conflated. **What such a value renders is measured too**, and it corrected a stale limit: a message interpolates the stored spelling with its zero markers intact, never a completion, so the value domain now exposes the stored component triple a renderer needs. The rendered *pattern* follows the **message locale** rather than the declared storage format, measured on two locales, so it stays presentation policy at the caller while the components stay semantics here. **The repeatable-group locus is measured and closed, 2026-08-23**, at [`cap-value-as-date-locus`](IMPLEMENTATION-MAP.md#cap-value-as-date-locus): a partial-Date operand may sit inside a repeatable group and is admitted by the reading rule's own iteration scope rather than by its spelling, while a starred operand is refused at the operator itself and a bare partial-Date reference is refused outright. That is peer-accepted as [SPEC-2026-08-23-12](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-12). Its per-row direct-comparison read is also internally closed: the checked environment selects the row, the document exposes only a partial-Date declaration's stored text, and the same classifier decides it, preserving emptiness, prior causes, over-repetition, and structural faults. This execution has no retained Kernel observation. Still open here: external per-row execution calibration and the three other carriers' loci, which cannot inherit this verdict.
- **Basic-check error audit closed 2026-08-23, and it moved the prerequisite rather than adding work.** The kernel's complete formal-error constant set was read at a12-kernel `cb66e51fa7ab90b650698f861bf670754e2e1e66` and compared against this project's formal-cause domain. Three results. The temporal set is **smaller than the kind set**: there is no Time or DateTime code at all, so a malformed value of either kind reports through the same Date codes the project already carries — no cause is missing for them. The four DateRange codes map **one-to-one** onto the four modeled DateRange causes, with the kernel's own comments settling each: separator missing or misplaced, no valid endpoint formats, first endpoint not before second, and one endpoint too early. And the single too-early Date code also covers a **wrong partially known** Date, which is one kernel code spanning two meanings this project splits differently; the cause's docstring now records that rather than implying the narrower reading. So "remaining basic-check errors" is not an open item: what is actually open is the **DATE_FRAGMENT stored value**, which has no owner in the value domain, and that prerequisite belongs to the partial-Date entry above. A future capsule widens the existing cause's meaning; it must not add a fifth temporal cause.
- `remains`: DateRange still lacks wider semantic-index construction, wider computation targets and repeatable execution, and remaining consumer compositions. The starred-operand class is **closed by refusal**: a starred endpoint operand is `MVK_NO_WILDCARDS_ALLOWED` at either locus and a group operand is `MVK_NO_WILDCARD`, both measured at the [endpoint-shape checkpoint](SOURCES.md#src-date-range-endpoint-shapes), so there is no starred shape to represent.
- `open`: the **keyed DateRange operand** is measured admitted on four carriers at the [endpoint-shape checkpoint](SOURCES.md#src-date-range-endpoint-shapes), and **all four are now closed**: the numeric component of a selected endpoint, stored range equality, the endpoint-pair comparison, and the overlap predicate, the last three against a direct operand in either authored slot. Each takes its own carrier's existing gate and verdict over the one keyed observation, so every agreement is structural rather than a proved coincidence, and closing them extracted three clauses the direct routes now share: the yearless endpoint projection, the overlap profile-level year rule, and the overlap slot projection. The comparison's runtime domain is read off the retained profile because the two direct endpoint owners are proved to gate on complementary conditions. What stays **open** on this shape is narrower than the carriers: a keyed slot inside the overlap operator's own **list** beside a starred, filtered, or group slot, which would have to thread an index preliminary through the shared addressed resolution and which no observation authorises; a keyed operand on **both** sides of any carrier; the **computation-phase** column strictness for this kind; a field-valued key's own emptiness or invalidity; and an invalid cell in a non-selected row. Two exclusions travel with the family: the Kernel's declaration-format check on an authored literal, `MVK_INDEX_VALUE_INVALID`, has no representation because this project performs no lexing; and **every** local refusal class of the keyed route stays unmapped to a Kernel diagnostic, including the ones reached through the shared overlap certifier, because the admission batch established which shapes are accepted rather than which code a keyed rejection carries. That rejection vocabulary is the one gap worth a future measurement batch.
- `external-gap`: exact direct nonrepeatable `yyyy` and `yyyy-MM` pair execution is pinned by the [year](SOURCES.md#src-date-range-year-fragment-construction) and [year-month](SOURCES.md#src-date-range-year-month-fragment-construction) checkpoints. Configured-Base-Year `MM`/`MM-dd` execution and the distinct yearless comparison boundary are pinned by the [Base-Year checkpoint](SOURCES.md#src-date-range-base-year-fragment-construction).
- `external-gap`: singular direct `yyyy-MM` and configured `MM`/`MM-dd` overlap are pinned by those checkpoints. Direct `yyyy` overlap is internally closed over the checked year completion and inclusive overlap seams but remains external evidence pending.
- `external-gap`: direct `yyyy`/`yyyy-MM` and Base-Year-completed `MM`/`MM-dd` bound extraction plus their fixed-Date/component compositions are internally closed over checked completion and the generic source-established bound mechanism, but remain external evidence pending.
- `external-gap`: the filled full-Date constructor-to-dotted/dash target output is pinned by the [established-kind computation checkpoint](SOURCES.md#src-first-filled-kind-computations). Its inverted render-then-error branch is source-rederived there but lacks a retained Kernel outcome; empty, formal-before-empty, application, and static refusals remain internal.
- `external-gap`: every admitted DateRange constructor target is now Kernel-pinned for admission and rendered output: the dotted/dash one at the established-kind checkpoint, all four yearless spellings at the [construction-target checkpoint](SOURCES.md#src-date-range-construction-target-crossing), both year-bearing profiles at the [year-fragment-target checkpoint](SOURCES.md#src-date-range-year-fragment-constructor-target), and the ISO exact plus both configured fragment targets at the [exact-and-configured checkpoint](SOURCES.md#src-date-range-exact-and-configured-constructor-targets). What stays unpinned on this family is the empty, formal, and clearing branch of each target.
- `external-gap`: the configured `MM` and `MM-dd` constructor targets are pinned too, and they render back to their declared components even where the Base-Year completion reaches a leap-aware month end, so the completion decides the ordered check without appearing in the stored text.
- `external-gap`: direct one-star DateRange `FirstFilledValue` static admission and runtime are Kernel-pinned for all eight interpretation-free declaration pairs, Lean executes all eight, and the starred route accepts every source whose component set equals the target's, including the full-Date lexical cross. The configured [interpretation star checkpoint](SOURCES.md#src-date-range-interpretation-first-filled-star) pins opposite interpretations under Base Year, including wrapping and ordered output, later-row fallback, distinct-source separation, and seeded-target clearing. The corrected [unconfigured interpretation checkpoint](SOURCES.md#src-date-range-unconfigured-interpretation-first-filled-star) instead establishes that validity and target presentation vary with declaration order, so that branch is withdrawn from Lean's Kernel-correspondence claim. Fragment formal outcomes remain external evidence pending. The **direct-list** route is pinned separately at the [list-crossing checkpoint](SOURCES.md#src-date-range-list-crossing): its sources are gated by format identity and its target by component set, two gates in that order, with the target spelling rendering an admitted interpretation-free crossing.
- `external-gap`: Base-Year completion of both lexical variants and their bound extraction are now pinned by the [bound checkpoint](SOURCES.md#src-date-range-bound-base-year) on both Kernel strategies. `interpretationOfYear` is implemented for the dotted pair and every other yearless profile; the entry below owns what its measurement did not reach.
- `open`: `interpretationOfYear` is implemented for configured models. The [interpretation-overlap checkpoint](SOURCES.md#src-date-range-interpretation-overlap-refusal) pins endpoint years and the placed-year leap check, and establishes that **both overlap operators refuse an interpretation-bearing declaration outright** on either side, under every profile, and with or without a Base Year. Bound extraction and equality comparison admit the same declaration. The [interpretation-comparison checkpoint](SOURCES.md#src-date-range-interpretation-comparison) closes configured comparison execution, and the configured [direct-list](SOURCES.md#src-date-range-interpretation-first-filled) and [direct-star](SOURCES.md#src-date-range-interpretation-first-filled-star) checkpoints close both `FirstFilledValue` carriers in each source-to-target direction. Unconfigured interpretation remains open at a wider semantic root: the corrected [order checkpoint](SOURCES.md#src-date-range-unconfigured-interpretation-first-filled-star) shows that formal validity and direct-star target presentation depend on model declaration order through a shared generated-format class. The current FlatModel does not retain that class. Measure its full equivalence denominator and every affected consumer before adding model-wide format state; until then the field-local classifier stays fail-closed and carries no correspondence claim for this branch. Unconfigured direct-list execution and every other computation family under an interpretation remain outside too.
- `external-gap`: singular-overlap admission now matches the measured gate. The [mixed-year checkpoint](SOURCES.md#src-date-range-mixed-year-overlap) establishes that the operator gates by year class over the whole operand list and that a Base Year lifts the gate by completing each yearless operand, and the [yearless-overlap checkpoint](SOURCES.md#src-date-range-yearless-overlap) establishes the unconfigured label comparison. `mixesDateRangeYearInclusion` is the single derived gate and carries both directions as laws; the refusal cause `unsupportedPolicy` is consequently reachable on this route only from a uniformly yearless list.
- `open`: the unconfigured yearless singular route is closed for direct, plain-starred, and filtered-starred stored operands: [`YearlessDateRangeOverlap.lean`](../A12Kernel/Elaboration/YearlessDateRangeOverlap.lean) owns admission and evaluation, and the generic scan serves both interval domains. A mixed year-class list needs no bridge between those domains, because the Kernel refuses it unconfigured and completes the yearless side into the resolved domain when a Base Year exists. What remains for this operator is the per-row scalar-plus-star shape, whose rows are measured at the [starred checkpoint](SOURCES.md#src-date-range-starred-yearless-overlap) but need the rule's own iteration context. Both plural halves are closed against the [plural checkpoint](SOURCES.md#src-date-range-plural-year-class): the configured one with the plural owner and the unconfigured yearless one with the yearless owner, including plain stars, filtered stars, and fixed or starred group carriers in the list slot. Every non-stored read form stays refused.
- `open`: overlap operands are certified independently, so neither operator applies a component-set gate and a mixed-set pair is admitted, matching the measurement. Both operators now share one derived year-class gate and the plural scalar and list admit the same fragment profiles as the singular direct operand. Plain stars, filtered stars, and group carriers are all admitted and evaluated on the yearless route now, the filtered star carrying omission polarity and a carrier contributing its whole recursive expansion; what stays refused is any read form other than stored. The singular operator's blanket group refusal is measured and kept.
- `external-gap`: two- and three-direct-field DateRange `FirstFilledValue` is Kernel-pinned for dotted/dash source admission, target execution, recursive empty continuation, terminal-prefix suffix hiding, reached-formal termination, independent eager formal-input reporting, later crossed-profile refusal, and later target self-reference. The two-field route is also pinned between `FROM` and `TO` declarations in both directions at the [interpretation first-filled checkpoint](SOURCES.md#src-date-range-interpretation-first-filled).
- `external-gap`: three-source admission and third-source output are pinned for all four fragment profiles, including configured and unconfigured `MM`/`MM-dd`. ISO/slash list execution, fragment-list formal suffixes, lengths greater than three, and other entity shapes remain open.
- `external-gap`: reviewed a12-dmkits `df637d7db9f918e132ccbcc2faf1e36cd4c45bb6` closes the former `yyyy`, `yyyy-MM`, and equal-`MM` construction-versus-stored divergences through one finish-position completion fix and retains the wider day-omitting matrix across Kernel, JVM, and Node routes.
- **Resolved 2026-08-23.** Distinct-count Date identity is the **decoded instant alone**: the runtime's decoded-Date equality compares only its wrapped millisecond date and ignores the not-given flag, the not-check-relevant flag, and the origins, with distinctness taken as hash-set membership over that equality. It does remain a different relation from uniqueness' stored-text identity — but the two **cannot be separated by any storable document** under a default-format DATE declaration, because the only separating shape is two distinct texts decoding to one instant and the non-lenient parse refuses every candidate: an unpadded spelling, a day overflow, and a month overflow are each `datumFormatFalsch` rather than a normalized value. Measured with live controls on both codegen strategies at the [decoded-identity checkpoint](SOURCES.md#src-date-decoded-identity-witness). Keep the two relations distinct in the representation anyway, since the identity reading is what a differently declared format could expose; do not merge them on the strength of this negative result.
- `risk`: equal Berlin wall labels can denote different instants, and another `World` can change `Now`; wall-label-only identity, early clock resolution, or elapsed-duration substitution is unsound.
- `consumer`: Execute, Transform, and Explain must retain instant, source format, decoded components, calendar/profile, world dependency, and target rendering policy; unsupported zone/profile information is explicit insufficiency.
- `forbidden`: do not reuse a day-only operand for sub-day expressions, create a parallel temporal AST, re-resolve an instant, invent target rendering, substitute proleptic dates for an unsupported profile, or equate calendar steps with elapsed duration.
- `evidence`: the [reviewed 2026-08-19 checkpoint](SOURCES.md#src-date-range-2026-08-19-reconciliation) pins the declaration, comparability, completion, formal-split, year-interpretation, operand, and eight-profile direct-star admission/runtime boundaries. Direct-list and constructor-target profile denominators remain narrower; partial/formal, wider repeatable, and wider consumer calibration remains sparse.
- `entry-gate`: each selected temporal family needs one bounded source packet and separating matrix over format, world, profile, gap/overlap/cutover, empty/formal precedence, and target policy.
- `completion-gate`: every admitted temporal operation and target path is checked across its legal profile with exact empty/formal polarity and separating gap/overlap/cutover/millisecond cases.
- `blocked-on`: none.
- `route-state`: discovery-required. The eight matching-profile direct-star routes, every crossing within a component set, both operators' year-class admission, and both operators over the unconfigured yearless domain are closed. Every remaining overlap shape needs its own probe row.
- `open`: the repeatable-operand rule locus is measured in both directions and represented on nine carriers in both value domains, and the same leaf now owns them at a non-iterating locus too. The [rule-locus](SOURCES.md#src-date-range-rule-locus) and [yearless-locus](SOURCES.md#src-date-range-yearless-locus) checkpoints pin the admission rows, the gate order against the error-field gate, and the polarity matrix with its live negative control. A finding came out of this work and is recorded with the capability: before this leaf, every DateRange overlap and comparison source was a component-level capability with no condition-tree carrier, so no assembled rule could reach one, and neither unconfigured yearless overlap operator had a whole-source elaborator at all. What remains is a filtered star beside a row read and the keyed operand the entry below owns. Nested repeatable chains are closed: a two-level locus admits an operand at the root, the enclosing row, or the leaf, refuses one below itself with the shared class, and correlates each operand to its own enclosing row at runtime, all Kernel-locked at the [nested-locus checkpoint](SOURCES.md#src-nested-rule-locus) on both codegen strategies. Neither the leaf nor the shared resolver needed a change, which is the payoff of having carried the locus scope as a parameter rather than as a special case.
- `open`: the numeric component of a selected endpoint is closed and owns its own [capability row](IMPLEMENTATION-MAP.md#cap-date-range-bound-component-operand). Two findings came out of it, both recorded there. The component gate is **not** the comparable endpoint's exact-value gate but the direct Date field's own rule over the declared component set with the Base Year supplementing the year alone, and the runtime read must select from every carrier, because an unconfigured yearless range is admitted here and its fragment carrier faults in the exact-value-only selector. The starred form of this operand is measured refused with `MVK_NO_WILDCARDS_ALLOWED`, so an iterating rule is the only legal way to read a row endpoint's component and there is no wildcard shape left open on it. One **consolidation candidate** is surfaced rather than acted on: `CheckedDateRangeBoundComponent` in [`DateRangeBound.lean`](../A12Kernel/Elaboration/DateRangeBound.lean) now overlaps the atom, and its only remaining distinct value is that it returns the selected endpoint observation beside the component, which an Explain consumer needs and the operand read does not expose. It covers only the exact profile at the document root, so it cannot serve either shape the atom added. The resolution is to give the operand read the retained-observation result rather than to keep two partial owners, and it needs the owner's approval before either is removed.
- `open`: the endpoint component now computes a row target per instantiated row through the shared addressed placement, which needed no widening: the placement already routes an atom through the scalar computation evaluator, so only the target half is new. Two facts came out of measuring the target half — the component derives scale 0, so a fractional target is refused `MVK_INVALID_COMPARE_DEC_PLACES`, and the placement's same-scope requirement is a local narrowing the entry in [SG5](#sg5--numeric-authoring-and-target-completion) now owns.
- `open`: the computed endpoint component is now Kernel-calibrated at runtime as well as statically, at the [runtime checkpoint](SOURCES.md#src-date-range-bound-component-runtime), and the probe corrected nothing: every value, the empty zero, and the cleared invalid-operand row match what the Lean cases already asserted. Two facts about the *channel* rather than the value came out of it and are worth carrying: a poisoned operand reaches `cleared` and not the error channel, and a cleared instance appears in that channel only when the target was filled in the source, so an empty source target reports no outcome at all. Both already matched the shared run-view owner. What stays uncalibrated on this operand is the validation-surface runtime read and the day and year component values.

### SG7 — String, pattern, and custom-field completion

- Missing invariant: general String ingestion, every String function, pattern admission/execution, Unicode/line-break policy, repeatable lists/requiredness, custom-field output, and String targets must share one checked observation without bypassing normalization or resampling validators.
- Current boundary: [§7](IMPLEMENTATION-MAP.md#7--strings-and-patterns), [§8](IMPLEMENTATION-MAP.md#8--enumerations-and-value-lists), and [String/Enumeration distinct count](IMPLEMENTATION-MAP.md#stringenumeration-distinct-count) own the implemented surface.
- Open: add model-owned checked message-template authoring before raw interpolation; audit remaining grapheme restrictions and surrogate-splitting reachability; source registered-custom and repeatable computed targets before admission. The Number and String/stored-Enumeration overloads of `FieldValuesNotUnique` are closed over the shared checked entity-list shape and the exact `MVK_PARAMSIZE_INVALIDN`, `MVK_ONLY_STRING_ENUM_NUMBER_DATE_ALLOWED`, and `MVK_VARYING_TYPES_NOT_ALLOWED` projections. Their whole-list kind and category scans now share the four-category classifier in `FieldEntityList`; BOOLEAN and the newly measured CONFIRM take the earlier kind code, category mixing takes the second code, and unmeasured shape or certification failures remain unmapped. **The temporal overload is now closed** over the comparability category `{Date, Time, DateTime, DateFragment}`, admitted when every operand carries one identical declared format string; [`§7`](IMPLEMENTATION-MAP.md#7--strings-and-patterns) owns the implemented surface and [`spec/07`](../spec/07-repetition-and-iteration.md) the clause. Two design predictions in the entry packet were wrong and are recorded because they were nearly acted on. The packet recommended extending the shared `ValueListKind` with an atom carrying decoded temporal identity; that was unnecessary. The same-format gate makes the operand list textually homogeneous, the engine reads `feldWertIntern` for this operator, and the compared value is therefore the exact stored text — which the canonical token atom already is, so no shared type changed and no `kind`-generic consumer was touched. The packet also treated the stored-text-versus-decoded choice as settled-unobservable; it is not, and [`SPEC-2026-08-05-03`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-05-03--the-temporal-fieldvaluesnotunique-identity-is-stored-text-and-its-agreement-with-decoded-values-is-an-assumption-rather-than-a-measured-equivalence) carries the correction. **Three residuals remain, none blocking, and the first is now closed for Date.** First, the **stored-text assumption**: the two accounts agree only if one declared format admits one text per value, and the witness class is two distinct stored texts under one format whose decoded values are equal. **Measured 2026-08-23 and empty for a default-format DATE column**: an unpadded spelling, a day overflow, and a month overflow are each refused `datumFormatFalsch` before storage, on both codegen strategies and against live same-model controls that do separate the two identities when a document lets them, so no storable document distinguishes stored-text from decoded-instant identity there. The [decoded-identity checkpoint](SOURCES.md#src-date-decoded-identity-witness) owns the bytes; the assumption stands for this declaration on measurement rather than on plausibility, and remains unmeasured for other declared temporal formats. The retained matrix pins the stored-text account in both directions rather than leaving it implicit. Second, the **format-versus-kind-family annotation**, now **measured and positively witnessed, 2026-08-23**: such a declaration is authorable in every direction, because the format gate checks the string against a fixed vocabulary and never against the kind. `HH:mm:ss` is admitted on DATE, `yyyy-MM-dd` and the fragment format `MM` on TIME, and the ISO datetime format on DATE, each read back unnormalized. The peer's report that TIME and DATETIME appear to admit exactly one format describes their **defaults**: a TIME field is refused `HH:mm` and admitted `yyyy-MM-dd`, which is the pair separating a vocabulary check from a kind check, and reading the refusals alone is exactly what produces the one-format reading. The gate stays stated as pure format equality — that was already right — and this project needs no change, since it carries kind and components independently and its policy gate never constrains the format string against the kind. The [format-vocabulary checkpoint](SOURCES.md#src-temporal-format-vocabulary) owns the bytes and accepted [SPEC-2026-08-23-10](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-23-10) carries the clause generalization. Third, **DATE_RANGE** now has a checked flat declaration and the shared kind gate models its measured outright refusal before temporal format comparison. **A separate over-restriction is still known and unfixed:** the checked token overload selects one String or Enumeration category, but the Kernel's String category also admits Custom, and this project still refuses Custom beside String. This is genuinely blocked rather than merely unscheduled: Custom fields are not in the token entity list at all, so admitting the pair needs the custom-field declaration path first, and no case can exercise it until then. The shared classifier represents that category without pretending the missing declaration path exists. The existing String/Enumeration overload was **verified** conformant with the operator's category and value-validation gates rather than assumed: its checked operands are stored-projection only, because the projection-bearing surface belongs to the distinct-count operator. Two corrections to this operator are closed at the same revision under accepted [`SPEC-2026-08-05-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-05-01--fieldvaluesnotunique-filter-escalation-is-positional-not-static): filter escalation is positional and keys on a value actually compared, and a formally unavailable cell is **skipped rather than suppressing**, which was a local defect the peer's separator exposed. The **firing locus of the starred form is settled and was never actually in conflict.** This entry previously recorded it as an unresolved discriminator on the grounds that a12-dmkits' authoring skill stated per-row iteration while its interpreter fired once at the declaration. Both halves of that reading were wrong, checked at peer revision `12491e4f`: the skill states that the starred form "produces one cross-row aggregate verdict" and routes a per-row requirement to `RepetitionNotUnique`, and the peer's measurement is that **both kernel strategies emit exactly one `VALUE_ERROR` at the first-row anchor**, referencing the wildcard operand, explicitly "not a declaration-level address". The two surfaces agree, the result domain is one verdict, and the current Lean consumer already matches it. What remains is narrower and belongs to the message channel rather than the result type: the pointer is the **first-row anchor** with a separate wildcard-operand reference, and Lean does not yet model that pointer pair; [`SG10`](#sg10--message-construction-and-formal-output-integration) owns it. The custom-field-type `Valid`/`Invalid` predicates are closed over their measured registration × fill × polarity matrix against a partial registry, with complementation proved only where the name resolves and the false generalization proved false; [§14](IMPLEMENTATION-MAP.md#14--custom-conditions) owns that surface. Three residuals remain. **Operand domain:** the kernel admits a String, Enumeration, or extensible-Enumeration field, and the checked predicate observes only String, so both Enumeration arms are unmodelled and the validator's stored-code input for them is unsourced. **Static gates:** the certificate is name-only, so the field, no-wildcard, and value-validation operand gates are source-known and peer-corroborated but neither implemented nor locally measured; [`SOURCES.md`](SOURCES.md) owns that provenance and its explicit measurement gap. **Composition:** the predicate is not yet a `ValidationCondition` leaf, so nothing lowers it into a rule or reaches the message channel. The **same partiality question is open one path over** and was predicted rather than reported: `elaborateCustomFieldType` still rejects an unregistered name for a *declared* custom field, and a model check cannot consult a host registry that only exists at runtime, so that rejection is probably a local narrowing too. It is deliberately not changed here, because the peer's accepted disposition scopes its measurement to the predicate and routes custom-typed fields to the separate `KF54` delegation path; the discriminator is whether a declared field with an unregistered type is rejected statically, raises at runtime, or degrades, and no local route reaches it. The **causal mechanism of the double firing stays open on both sides** and must not be modelled: the local account is deliberately observable-only, and the named settling probe belongs upstream under accepted [`EXP-2026-08-04-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-04-01--an-unregistered-predefined-type-may-raise-rather-than-fire-both-polarities). A 2026-08-04 audit of the complete 110-entry operator catalog against the Lean semantics and elaboration trees found `@From`-scoped `RepetitionNotUnique` and every rounding, comparison, tolerance, value-list, and enumeration-category operator implemented under their own Lean identifiers rather than the catalog's operator names, so a name-based sweep understates coverage; that audit's own claim that `ValidType`/`InvalidType` were unmodelled was **wrong**, because the registered arm already existed under `CustomFieldValidity`.
- Blocked on: nothing. The starred-form firing locus that formerly blocked this entry is measured on both kernel strategies and agrees with the current Lean result domain, as recorded above; the residual pointer shape is an ordinary SG10 obligation rather than a blocker.
- Completion gate: every legal String/custom operation and target uses one normalized observation; repeatable/partial consumers preserve exact cause and order; unsupported host capability or syntax fails closed.
- Consumer and evidence: Execute and Explain require normalized text, placement, validator identity, and display bytes. Condition-pattern slot and source diagnostics are Kernel-confirmed for both operators; runtime pattern, Unicode, custom-host, and wider-target calibration remain pending.

### SG8 — Enumeration and value-list completion

- Missing invariant: open/dynamic/partial declarations, remaining repeatable projections, computation filters, RNU authoring, and overloaded value-list consumers must reuse the typed projection and quantifier core without erasing declaration domains.
- Current boundary: [§8](IMPLEMENTATION-MAP.md#8--enumerations-and-value-lists) and [String/Enumeration aggregate counts](IMPLEMENTATION-MAP.md#stringenumeration-aggregate-counts) own the implemented surface.
- **Partly resolved, 2026-08-06, and narrower than a first source audit claimed.** `FieldValuesNotUnique` was measured at a12-dmkits `ddaf2e13` to **skip** a formally unavailable cell, and this project's wrong account came from reusing the shared `ValueListCell.scanPresent`, whose suppression semantics every other consumer had inherited rather than earned. What is now settled is the suppression **outcome** for the `Combiner`-based aggregates: `Sum`, `MaxValue`, `MinValue`, and `NumberOfDifferentValues` reach kernel `RuntimeController.combineFeldListe`, and a12-dmkits `4ae043b9`/`e01bd0de` measure all four suppressed with the malformed cell at head and at tail, carrying an all-valid positive control. `FieldValuesNotUnique` is the exception because its own loop has no early return on an unavailable cell at all — it returns at the first duplicate. `scanPresent` stays as it is and no code change follows. **Two claims from the first audit were withdrawn at `e01bd0de` and must not be reused.** The *ordering* of the aggregate stop is a source-level reading with no runtime witness, because verdict identity is what an ordered stop and a whole-selection survey both predict, and the same combiner path does survey a whole repetition in partial validation — the fact [`spec/10` §5](../spec/10-validation-and-polarity.md#5-full-vs-partial-validation) already owned independently. And the predictive rule "read which relevance gate the operator consumes" is **false**: every one of these operators consults the same gate, so it predicts the same answer for `Sum` and `FirstFilledValue`; the discriminator is the operator's own stop rule, which is what [`spec/02`](../spec/02-logic-and-formal-errors.md) now states.
- **`SumOfProducts`, the two shared star-extent predicates, and the three direct count outcomes are measured; the rest of the residual stands.** `combineFeldListe` is reached only by `Combiner`-based operators, and `NumberOfFilledFields`, `NumberOfFilledGroups`, `NumberOfValueInFields`, and `SumOfProducts` run their own loops with no `Combiner`. a12-dmkits `6fe8d501`/`12491e4f` measured `SumOfProducts` per operator against the Kernel: a formal failure in either zipped list suppresses, position does not change that verdict, and partial validation evaluates only when the current iteration row's complete starred cross-product is covered by the union of its addressing entities. This project had inherited the wildcard-only gate from the combiner family and was corrected to that separate completeness gate; the retained observations still do not distinguish declared capacity from present rows. The maintained extent matrix at a12-dmkits `75205407` separates the reduced-universal combiner predicate from the existential starred value-list predicate on the same relevant set. The accepted count batch at `cd43c249a88874bf5688a45060e208a0dcd20782` then establishes `NumberOfFilledFields` on the reduced-universal field pattern, `NumberOfFilledGroups` on its fifth group-path pattern, and numeric `NumberOfValueInFields` on the existential pattern. Lean exposes all three direct partial routes and removes the obsolete uncalibrated predicate. Semantic-root versus physical-root identity, three or more nested repeatable levels, and an independently measured token value-count partial runtime remain outside the represented correspondence boundary. `spec/07`'s "first reached formal failure" ordering is withdrawn to the verdict, since validation output carries no witness for it.
- Unresolved source fact: table-backed declarations remain unreachable at both the table-name and dependent-column producers; reopen only if either producer appears. Dynamic enumerations are distinct and still require an entry/reachability audit. [`EXP-2026-07-25-02`](A12-DMKITS-SPEC-SYNC-LEDGER.md) owns the reviewed table sweep and its exact revisions.
- Open reviewed domain: a12-dmkits `ddf5dd92` expands `FirstFilledValue` over the full owner `entitySpec` union and completes the all-empty table across BOOLEAN, CUSTOM, STRING, ENUMERATION, DATE, DATE_FRAGMENT, TIME, DATETIME, and DATE_RANGE.
- Closed direct-computation subset: Lean covers exact direct single-level starred Boolean and Custom; DateFragment under `MM`, `yyyy`, `yyyy-MM`, or `MM-dd`; full Date under `yyyy-MM-dd` or `dd.MM.yyyy`; fixed `HH:mm:ss` Time; fixed ISO DateTime; DateRange under all eight admitted `(format, separator)` pairs; and a finite direct nonrepeatable DateRange list under any one of those same eight lexical pairs.
- External limit: the four full temporal/date-range carriers have exact CLEARED/value calibration plus a leading-empty control through both Kernel strategies and the interpreter for the first full-Date and DateRange policies. Static Kernel admission covers all four DateFragment formats, but direct DateFragment runtime remains exact only for `MM`.
- External limit: static admission and both Kernel strategies pin all eight direct-star DateRange profiles, including unconfigured `MM` with an empty separator and `dd.MM` with dash, and the full-Date lexical cross. Lean executes all eight and every within-set crossing on both the starred and the direct-list route; every fragment empty/formal outcome remains pending.
- External limit: both Kernel strategies pin the dotted/dash two- and three-direct-field DateRange scans and their separate eager formal-input inventory, plus three-source admission and output for all four fragment policies. ISO/slash execution, fragment-list formal suffixes, and lengths greater than three remain internal or external evidence pending.

- Remaining `FirstFilledValue` work: external calibration beyond three sources, mixed, group, filtered, nested, validation, whole-result application, and wider temporal format/policy routes remain open and must not be generalized from the bounded direct-list slice.
- Prerequisite: reuse addressed operands, kept-successor traversal, repeatable expansion, and checked RNU topology. Audit dynamic domain ownership and partial declarations before widening.
- Completion gate: every legal declaration profile, projection, domain rule, repeated access, value-list operation, and uniqueness use is checked with display/domain/category distinctions preserved.
- Consumer and evidence: Execute and Transform require positional categories, union admission, many-to-one identity, and directional empty/unknown behavior. Broader §8 portable calibration remains open.

### SG9 — paths, indices, and static legality completion

- Missing invariant: bilingual parser/renderer paths, lexical/dot syntax, semantic indices, globals, nested/multiple stars, wider RNU `@From`, and remaining diagnostics must resolve through one checked namespace.
- Current boundary: [§10](IMPLEMENTATION-MAP.md#10--paths-and-references) owns the implemented path surface, and the [cross-clause diagnostic owner](IMPLEMENTATION-MAP.md#cross-clause-implementation-notes) owns exact diagnostics and unmapped refusals. The bounded correction adds root classes and the three-shape RNU collapse. A rule-owned static projector closes the exact measured one-level error-locus matrix without widening condition-level or runtime admission, and the shared whole-rule projector maps the already-derived unguarded negative-iteration failure to its exact Kernel class while retaining the directly guarded admission control. Nested/different repeatable scope, other shapes, other assembly refusals, and executable newly admitted group-list/count conditions remain open. Parallel RNU mapping waits on a discriminator.
- Static-legality limit: an uncovered local refusal projects to `none`; approximate inventory counts and implemented-family narratives are not maintained in this open-work record.
- Unresolved source fact: mine bilingual lexical/renderer roundtrip, globals, nested/multiple semantic-index ownership, wider RNU `@From`, and diagnostic precedence in coherent batches. For diagnostics, measure one operator family at a time and project every unmeasured local refusal to `none`. `FieldValuesNotUnique` still lacks Custom representation; DateRange is represented and retains its measured outright refusal.
- `CurrentRepetition` remainder: fixed nonrepeatable descendants, comparison forms beyond the three checked validation tags and the bounded computation `> 0`, wider computation use, partial validation and computation, arithmetic wrappers, and concrete parsing require their own bounded routes. The fixed computation chains retain the selected terminal coordinate alongside the complete nested scope and exact row addresses. Captured `$` lookup inside `Having` already consumes the complete named outer environment through the checked correlation owner; filters outside that owner are not another `$` surface. The completed root and same-group repeatable validation specializations are owned by [§10](IMPLEMENTATION-MAP.md#10--paths-and-references), and the exact computation cascades by [§11](IMPLEMENTATION-MAP.md#11--calculations-and-formal-checking).
- Semantic-index target remainder: reading the computed field directly and naming it as an index key both draw `MVK_ERROR_SEMANTIC_INDEX_OR_CATEGORY_FOR_ERRORFIELD`, but computation has no semantic-index source that can represent either route.
- Semantic-index filter discriminator: a projection inside `Having` remains unobserved because wildcard iteration refuses both the self-read case and its no-self-read control. The two projects predict opposite answers; measure when a filtered Enumeration source can express the projection.
- Most dangerous discriminator: bare name, parent walk, and named turning point can spell neighboring routes under different rules; equal local indices under different outer rows remain distinct; unavailable index columns have phase-specific effects.
- Consumer hypothesis: Translate, Transform, and Explain require stable declaration/row identity, preserved authored path distinctions, and exact static failure classes.
- Forbidden shortcut: no string-splitting semantics, invented quoting/case, first ambiguous match, flattened row identity, or UNKNOWN for structural failure.
- Entry gate: complete one bounded source packet and separator matrix for the selected path/index family.
- Completion gate: every legal reference resolves deterministically, every illegal form receives the correct static class, and rendering round-trips for the supported surface.

### SG10 — message construction and formal-output integration

- Missing invariant: authored template parsing, token legality, path/star/index/category/`BaseYear` lookup, locale/display providers, format errors, custom output, repeatable pointers, and validation/computation orchestration must produce exact structured messages without changing firing.
- Current boundary: [§13](IMPLEMENTATION-MAP.md#13--message-interpolation) and [§12](IMPLEMENTATION-MAP.md#12--validation-and-polarity) own the implemented rendering/attachment surface. The checked flat rule-template fragment validates nonempty printable-ASCII text, `$$`, `$Field$`, and `$Field.value$`, decodes a parameter's entity spec with the **shared path grammar** — bare name, group-qualified relative, absolute, and parent walk with an optional turning point, each segment in the grammar's single-quote escape or bare — lowers it through the one existing quote-provenance boundary under this producer's own narrower quoting rule, resolves it to a nonrepeatable field through the one existing resolver, requires condition membership, and lowers into the existing post-fire plan while retaining the authored spelling beside its decoded path. The measured `en_US` String-pattern field producer separately admits only fixed lowercase `$field$`/`$field.value$`, rejects `$$`, lowers already-selected owning-field bytes to the same one-pass renderer, and preserves the established pattern failure. The normalized shared `MessagePointer` now owns resolved field identity plus concrete/wildcard/unknown coordinates and is used by validation, computation, registered custom-field, and custom-condition error channels without widening exact `CellAddr`.
- Unresolved source fact: the **path** half of that widening is done, grounded in the kernel's own parameter grammar and rule-checker source at a12-kernel `cb66e51` and reconciled with exact peer evidence; two of the Kernel's `.value` gates are unreachable in the fragment rather than modelled, because a star reference needs a repeatable path and a declaration's no-value-validation flag has no representation here. The **quoted-name** half is done too, including the exemption that makes this producer's rule narrower than the condition language's; the seven exempt terminals and the three that need the escape are read off the parameter lexer and its multi-language handler at that revision. The **category suffix** is done as well, with its ordered gates reusing the one existing Enumeration projection boundary and its rendering through the declaration's category mapping. Ordinary condition membership still applies, while the non-starred and value-validation gates do not. The **Base Year** form is the first admitted non-field parameter, gated only on the model declaring a Base Year and resolved at authoring; its exact bilingual spelling comes from the Kernel's own terminal bundles, where only five terminals differ and two are forbidden in an A12 model at all. All four halves are now **locally Kernel-measured** at the [parameter-grammar checkpoint](SOURCES.md#src-rule-message-parameter-grammar) rather than only source-derived, first through `rule add --dry-run` and then through the peer's read-only `rule check --message` route; the doubled-arrow matrix is exact for a declared category, an undeclared category, and a non-Enumeration field, and a second batch through the runtime probe route calibrated the **rendered** text as well, which moved the category access's rendering out of the caller and into the declaration's own mapping. The category access's empty-field default then measured out to exactly the empty string this project had chosen, so it stopped being a guess. The exact presentation table is now peer-calibrated for ten locale tags across Date, Number, DateTime, DateRange, and the unchanged Time control; Lean still does not derive presentation from that locale table. A declared label provider stays uncalibrated because the probe route has no provider surface. [SPEC-2026-08-24-01](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-24-01) is accepted at the [2026-08-23 reconciliation checkpoint](SOURCES.md#src-2026-08-23-reconciliation). What remains of it is the other non-field parameter forms — `#RootGroup`, `#Zeile`, `#Usb`, `Vordruckzeile`, `Vordruckname`, the group index, `index(...)`, and the semantic-index suffix — plus their diagnostics; also audit locale/display invocation, category/index/`BaseYear` lookup, custom output, repeated pointers, `NumericTargetError` formal codes, and rendering orchestration. Add the raw `PartiallyKnownDocumentMultiPointer` factory boundary only when a public consumer needs its name list, mismatched arity, root accessors, and partial conversion behavior; the normalized field-instance owner deliberately has no textual codec. `fillToFix` and the wider message-channel orchestration remain absent; the `referenced` projection now exists for a named condition fragment, and its rule was never the unknown part: [`spec/01`](../spec/01-data-model.md) now specifies `referenced` as a **structural projection of the checked condition** rather than a read trace, with star-bearing operands wildcarded at every reopened group-context part, recursive descendant-field expansion for a starred group, `Having` operands included, `$` resolved at its concrete captured outer row, and `CurrentRepetition` contributing nothing; it is Kernel-locked upstream at a12-dmkits `bbbbf48e` (see [`SOURCES.md`](SOURCES.md)). The remaining Lean gap is narrower: the projection is built from the checked condition at the rule owner rather than accumulated during evaluation, but the same owner must still pin an unbound repeatable **error address** to concrete repetition 1 while its reference keeps wildcard `0`, and that error-address half is untouched. For field-owned templates, the complete requiredness grammar, German tokens, provider invocation, empty-value fallback, other type-format producers, computation targets, and registered custom-field validators remain unmeasured or separately owned. The single requiredness witness preserves `$$` and therefore forbids reusing either implemented parser without a complete producer-specific account.
- Upstream reconciliation closed at the [2026-08-23 peer checkpoint](SOURCES.md#src-2026-08-23-reconciliation): read-only inspection of a12-dmkits at `351bf9f28138783ef7ddc7d8c76db2ab4891a5d8` finds that its common rule renderer recognizes only nonempty name/value regex matches and therefore leaves `$$` doubled. Kernel rule-message source at `cb66e51fa7ab90b650698f861bf670754e2e1e66` and the already-canonical rule account establish that an empty delimited parameter decodes to one literal `$`. The accepted peer rendering lock now carries `renderMessage("Cost $$ [$Amount.value$]", ...) = "Cost $ [<opaque display>]")`, with token-looking display bytes retained unchanged across its maintained interpreter and kernel-backed rendering routes.
- **Reference channel, remaining once every numeric leaf is classified.** [§13](IMPLEMENTATION-MAP.md#13--message-interpolation) owns the implemented structural projection and its proved laws. Four obligations stay open, in payoff order, and **every one of them is now gated on a measurement or a named consumer** rather than on unfinished classification work. (1) The **filtered-star operand is refused, not projected**, now in all three families that can carry one. Membership of a `Having` field operand is measured, but no retained witness pins its coordinates, and the checked filter's inner/outer scope guarantees have not been audited against a firing environment. Applying the one measured rule would make an inner operand wildcard in the reopened candidate row and an outer `$` operand concrete at its captured row; that is an application of a measured rule to an unwitnessed position, not a measurement, so the operand stays refused until a witness or a scope audit exists. The Number entity operand carries its filter as a distinct constructor while the token and Boolean/Confirm operands carry it as an optional field, so the refusal is made three times rather than inherited; one retained case guards the optional-field form, since the two optional-field families share the identical three-line decision. (2) **Every leaf family except `RepetitionNotUnique` is now classified, and the `orderedNumeric` leaf covers every atom constructor with no catch-all.** The unstarred group operand, the `groupPresence` leaf, and the fixed group count were the three refusals sharing one witness, and [`EXP-2026-08-11-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-11-01--does-the-referenced-channel-expand-an-unstarred-group-operand) answered it: recursive expansion, never a group pointer, concrete coordinates, the same relation in all three positions. All three now share one `fixedGroupPointers` mechanism, and the per-atom sievability gate that had refused the count is **deleted** rather than kept — the measurement retired it, which is the outcome an unmeasured-shape refusal exists to reach. [`LF87`](LEAN-FINDINGS.md) keeps the method lesson that produced the gate. Two boundaries of the new relation are unmeasured rather than closed, and both fail closed. The measured model is nonrepeatable throughout, so a subtree field below a repeatable level **deeper than the rule binds** has no witness — a retained case pins the refusal at that exact level rather than choosing between a wildcard and a pinned first repetition. And the kernel's pointer carries a coordinate per group part, including non-repeatable parts at `[1]`, which the normalized field-instance pointer does not represent; set membership and concrete-versus-wildcard do agree, which is what this channel's consumers read. `repetitionNotUnique` stays refused and now carries the fragment's unclassified-leaf guard, and its cross-repetition reference coordinates remain parked with its owner. (3) `FlatRuleMessage` still carries **no** reference field. A caller holding a fired outcome also holds the rule and its environment, so adding one now would have to choose between a defaulted `[]` that reads as "no references" and a field nobody fills; its trigger is a shipment consumer that receives messages without their rule. (4) `fillToFix` is untouched, and the **pointer-shape exclusion stands**: `MessagePointer` carries one coordinate per *repeatable* level, so the measured `/Invoice[1]/Charges[0]/Extras[0]/Surcharge` wildcarded non-repeatable descendant context has no slot to occupy, and within this representation the structural and level-repeatability accounts of coordinate assignment coincide. Widening it would touch all four channels sharing the type; its trigger remains one named consumer, an independent normative evaluator that must reproduce the kernel's `getReferencedFieldsPointers()` accessor.
- Measured degenerate error address, now attributed. A rule whose **only** operand is a keyed lookup, whose error field lies inside a repeatable group, and which carries no positive presence guard is accepted by the kernel — a keyed lookup is not an iterated read, so it escapes the negative-condition-in-iteration gate — and it then fires **once** reporting at `/EndpointShape[1]/Rows[-5]/RowRange`, row coordinate **`-5`**, the Kernel's `IIdentifier.UNKNOWN` clean-no-match reanchoring sentinel rather than this contract's `[0]` wildcard marker. The [endpoint-shape checkpoint](SOURCES.md#src-date-range-endpoint-shapes) owns the bytes and the discriminator: the guarded twin of that rule and a guarded direct-operand rule both report ordinary per-row addresses on the same document, so the coordinate belongs to the unguarded keyed shape alone. A consumer should represent it as **no matching row**, not refuse the rule or invent a physical coordinate. The guarded twin must use an iterating error-field reference because a keyed lookup does not supply iteration and otherwise draws `MVK_ERROR_FIELD_AND_ITERATION_INVALID`.
- **The keyed parameter's grammar is closed; its admission is blocked on a keyed condition spine.** A parameter may carry a semantic-index key in last position, and the [semantic-index checkpoint](SOURCES.md#src-rule-message-semantic-index) measures its well-formedness, key spellings, nesting refusal, suffix order, and category composition — all modeled — plus the gate itself, which is on the **index** and not on the field, with field membership belonging to the unkeyed form alone. The gate cannot be posed in the modeled fragment, whose model is nonrepeatable, so a keyed parameter is decoded and refused at the fragment boundary and is deliberately mapped to no Kernel class. Admission needs a condition representation carrying keyed leaves; until then two facts stay unmeasured and neither blocks: what a keyed parameter **renders**, and whether a resolvable key field the condition never names is admitted (the measured field-valued row keys by a field the condition also carries). Do not re-derive the gate from refusal texts — they name a field and a key together, and only the admitted row factors them, per [`LF100`](LEAN-FINDINGS.md#lf100--a-refusal-names-an-entity-not-the-gates-subject-only-an-admitted-row-that-varies-the-entity-says-which-one-the-gate-reads).
- Most dangerous discriminator: inserted bytes containing delimiters stay opaque, and a message exists only after its verdict/transition fires.
- Consumer hypothesis: Explain and Govern require structured provenance, pointers, exact bytes, severity, and polarity; missing providers/pointers are explicit insufficiency.
- Forbidden shortcut: no repeated replacement, reparsing inserted bytes, invented fallback, second lookup path, pre-verdict attachment, or formatting back-effect.
- Entry gate: complete one source packet and separator matrix for the selected message family.
- Completion gate: legal routes render with exact order/locale/pointer/severity/polarity; invalid templates fail at authoring and messages have no semantic back-effect.

### SG11 — custom-condition checked orchestration

- Missing invariant: registration, static restrictions, effective data/relevance/formal/pointer construction, host call order, messages, and whole-rule integration must surround the pure reached-leaf oracle without unsupported locality or monotonicity laws.
- Current boundary: [§14](IMPLEMENTATION-MAP.md#14--custom-conditions) owns the pure successful callback leaf.
- Open reviewed domain: the checked authoring layer must retain the exact `nameOhnePunkt` grammar. The reached callback invocation now uses the shared message-pointer type; construction of that pointer remains part of the wider orchestration gap.
- Prerequisite: checked document supplies data and SG10 supplies formal output. Host failures remain explicit integration outcomes unless Kernel behavior establishes otherwise.
- Completion gate: checked authoring/orchestration reproduce registration, eligibility, projection, order, failure, and message behavior while retaining current non-laws.
- Consumer and evidence: Execute and Qualify require a concrete host contract and observable call discipline; project-local orchestration evidence is pending.

### SG13 — group-list and group-count completion

- Missing invariant: every group-list predicate and filled-group count must reuse the resolved group product or structural terminal-row count while preserving partial relevance, wildcard expansion, filter order, computation poison, and decisiveness.
- Current boundary: [resolved validation group presence](IMPLEMENTATION-MAP.md#resolved-validation-group-presence) and [checked group-star terminals](IMPLEMENTATION-MAP.md#checked-group-star-terminals) own the implemented surface.
- Blocked on: nothing. The fixed-multi-group computation target is **measured** at a12-dmkits `677e2eb7` under accepted [`EXP-2026-08-06-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-06-01--a-fixed-multi-group-filled-count-may-be-special-cased-ahead-of-computation-poison), and the answer was none of the three candidates: the count treats a formally unavailable operand group as **filled** and yields the ordinary exact count, while an empty group still does not count. So no new result domain is needed for the compute side; what must be represented is the **inversion**, because the same operator over the same cell answers unavailable in validation and counts it in computation, and one presence projection cannot serve both. The discriminating mechanism is presence-versus-read, so this is not an exception to computation poison, which governs reads. Both arms are now implemented as separate projections over the same descendant cells, with the compute arm's blanket `unsupportedGroupCount` narrowed to the shapes no observation covers.
- Open, and deliberately not inferred from the runtime measurement: structural and call-local group errors, a repeatable operand, and nested group descendants are all uncovered by it, and whether the value-reading twin clears or aborts its target is explicitly uncalibrated upstream. Do not widen the compute-side presence account past a fixed non-repeatable operand whose descendants are direct fields without measuring the wider shape. The implemented boundary refuses each wider runtime shape rather than counting it, and a refusal claims nothing. **Static computation admission is closed for the measured pair/triple and named boundary classes** through the clean exact-source consistency route at a12-dmkits `cd41ea94`: fixed plain lists of two and three distinct disjoint operands are admitted; short arity, exact duplication, ancestor overlap, and an unstarred repeatable group retain `MVK_PARAMSIZE_INVALIDGN`, `MVK_DUPLICATE_PARAM1`, `MVK_DUPLICATE_PARAM2`, and `MVK_NO_WILDCARD`; and starred repeatable groups are admitted alone or beside a fixed group. Lean generalizes the fixed rule to larger lists, but Kernel correspondence above three remains external evidence pending. Starred computation runtime remains open and is not inferred from static acceptance.
- Closed: the filled-group count's refusal now names its **offending operand**, so an Explain or Qualify consumer can point at that group rather than at the whole count; the count is order-insensitive, so nothing else about the refusal is ordered. Splitting the payload also separated two causes the one class had conflated — a group outside the admitted scalar boundary, which is an authoring boundary, and the **model-free** resolved evaluator that can enumerate no group's descendants at all, which is a missing evaluation input. A consumer's remedy differs between them.
- Prerequisite owners: [`GroupPresence.lean`](../A12Kernel/Semantics/GroupPresence.lean), [`CheckedGroupPresence.lean`](../A12Kernel/Elaboration/CheckedGroupPresence.lean), [`StarGroup.lean`](../A12Kernel/Elaboration/StarGroup.lean), and shared validation/computation traversal.
- Unresolved source fact: audit partial fixed/starred multi-entity lists and filtered group operands, including relevance timing and poison/lookahead policy.
- Most dangerous discriminator: nonrelevant content or a later failed expansion beside an earlier decisive group separates eager, leaking, and correct prefix accounts.
- Consumer hypothesis: Execute/Explain require exact product/count, relevance, environment, and reached failure; Analyze requires operand order and decisive prefix.
- Forbidden shortcut: no caller-supplied relevance on a full document, accidental validation/computation scan sharing, row inference from patterns, or UNKNOWN/poison for structural failure.
- Entry gate: select one result-domain family and show existing product/tally/traversal owners express every observed branch.
- Completion gate: every admitted direct/list/starred/partial/filtered/count consumer delegates to the established owner with exact order, relevance, error, polarity, and poison behavior.

### SG14 — mandatory-information derivation

- Missing invariant: derived mandatory-information comparison must keep an authored numeric literal distinct from the derivation's own sentinel, so a legal literal equal to that sentinel cannot read as an absent bound.
- Scope decision: in scope and unscheduled, decided 2026-08-22; narrowed to the mandatory-information callers on 2026-08-23. The owner ruled that a sentinel conflation inside comparison derivation falls within the targeted observable language, so this is an ordinary obligation rather than an exclusion stated in [`SEMANTICS-MAP.md`](../spec/SEMANTICS-MAP.md). Superseded the former SQ1 scope question; that identifier is not reused.
- Current boundary: unmodeled. No Lean owner exists, and no clause in [`spec/`](../spec/) states the derivation.
- Basis: inbound a12-dmkits `ee2f5d84` found one `FeldOperationUtil.getZahlKonstanteWert` `-1` sentinel conflation in mandatory-information comparison derivation; the colliding literals it names are `-1`, `4294967295`, and `8589934591`.
- **Caller asymmetry identified and one half closed, 2026-08-23.** A local source audit at a12-kernel `cb66e51fa7ab90b650698f861bf670754e2e1e66` finds the shared derivation called from **two** subsystems, not one: the three `mandatoryinformation` collectors the peer read, and the **parser's own static checkers** — the iteration checker and the USB-restriction checker. The parser caller is directly observable through `rule check`, and its behaviour is **already canonical and modeled**: the derivation's `parseDouble` → `Math.round` → signed-32-bit chain is [`spec/07`](../spec/07-repetition-and-iteration.md)'s narrowing clause, whose Lean owner and conformance rows are closed. So the observable half of this derivation needs no new work.
- **The sentinel itself is inert at that observable caller, measured.** Its two gates test the narrowed value against `0` and `> 0`, and `-1` is neither, so a literal that reads as `-1` and a genuinely non-constant operand produce the same admission. Measured on a purpose-built model (below): under the positive-threshold gate, `< -1` and `< 4294967295` — the latter narrowing to `-1` — are both admitted, exactly as a non-constant operand is. The conflation is therefore real but **consequence-free** here, which is why the observable caller cannot settle it. What remains open is confined to the `mandatoryinformation` callers, which no available route reaches.
- Open: determine whether any observable engine output exposes the **mandatory-information** derivation specifically. The parser callers are excluded above; do not re-derive them.
- Unresolved source fact: the derivation's callers were read by the peer, not by this project, and no local source audit of the sentinel's reachability from an authored model exists.
- Most dangerous discriminator: an authored literal exactly equal to the sentinel beside a genuinely absent bound, evaluated at each caller, because one shared derivation makes both read alike. At the parser callers that discriminator is **measured and null** — both readings admit — so only a mandatory-information caller can separate them.
- Evidence needed: the observable output for a sentinel-valued authored literal and for an absent bound **at a mandatory-information caller**, on both kernel strategies. A negative result closes the entry by establishing that the derivation is unobservable rather than by assuming it. The 2026-08-23 parser-caller measurement ran eleven monotone-literal rows plus a rounding pair through `rule check` against a dedicated model, SHA-256 `b087003fb1f9ea6aefadd4c688ca62d96bc193e9a6caa41a72b1245b482b18b0`, at clean a12-dmkits `108d047b787f0e049dcd2f4902f24d7523347a00`; every row agreed with the existing Lean account in both gate directions, so it produced no clause change and is recorded here rather than as a new provenance route.
- Consumer hypothesis: Analyze and Explain need "no mandatory-information bound" separated from "a bound whose value equals the sentinel"; Execute needs the distinction only if the derived answer reaches firing.
- Forbidden shortcut: do not encode the sentinel as absence, do not state a firing consequence before one is observed, and do not model the derivation from the peer's caller reading alone.
- Entry gate: establish one observable output that exposes the **mandatory-information** derivation before writing any Lean. Absent that, record the negative result here and write none. The parser caller does not satisfy this gate: it is observable but the sentinel is inert there, and its narrowing behaviour is already closed elsewhere.
- Completion gate: the derived comparison retains authored literal identity at every caller, and absence is representable only as absence.
- Blocked on: nothing. Unscheduled by choice, not by a missing discriminator.
- `route-state`: discovery-required

## Cross-family conformance gap

### SG12 — retained Kernel correspondence coverage

- Missing invariant: every implemented family ultimately needs versioned retained observations that distinguish realistic wrong accounts; source reading, internal proofs, and peer agreement are not Kernel calibration.
- Current boundary: [`EVIDENCE.md`](EVIDENCE.md) and the [external-evidence gate](IMPLEMENTATION-MAP.md#current-external-evidence-gate) own the exact retained inventory and limits.
- Prerequisite owners: [`EVIDENCE.md`](EVIDENCE.md), [`ObservationBundle.lean`](../A12Kernel/Evidence/ObservationBundle.lean), typed projections, and the [`compact semantic-evidence pipeline`](SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md).
- Unresolved source fact: for each coherent internally closed family, determine whether an unchanged source-owned a12-dmkits route emits the needed observation shape; otherwise request only the smallest missing producer capability.
- Most dangerous discriminator: a happy path compatible with both the chosen and a realistic wrong account does not calibrate the seam.
- Consumer hypothesis: Qualify/Govern require Kernel version, producer revision, digest, typed replay, separator, and finite claim limit.
- Forbidden shortcut: no source/peer/proof-as-evidence, universal claim from finite cases, resurrected universal capture estate, per-capsule harness, or copied sibling patch.
- Entry gate: select one coherent family at a capability milestone and define its wrong-account matrix and exact retained claim.
- Completion gate: every correspondence claim cites retained observations with provenance, separators, replay, and explicit finite limits; mismatches correct the theory or open a divergence.
