# Open semantic gaps

This is the open-only work index for convergence toward complete semantic conformance with A12 Kernel 30.8.1. [`IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md) is the sole detailed owner of implemented/proved/evidenced coverage; [`PLAN.md`](PLAN.md) selects the immediate unit from this index.

## Query contract

<a id="lifecycle"></a>

Open only the selected `SG<n>` entry. Detailed implemented facts stay behind its implementation-map links. Search headings with `rg -n '^### (SG|SQ)' docs/SEMANTICS-GAPS.md` and readiness fields with `rg -n '^- (Open|Prerequisite|Unresolved source fact|Most dangerous discriminator|Consumer hypothesis|Forbidden shortcut|Evidence status|Entry gate|Completion gate):' docs/SEMANTICS-GAPS.md`.

A gap remains only while a Kernel behavior, static-legality rule, checked construction, or conformance obligation is open. Delete a completed entry; Git records its history. Never append a completed implementation narrative. `SG<n>` identifiers are not reused.

## Hard-frontier critical path

The binding construction order is **SG1 → SG2 → SG4**. SG1 and SG2 are closed: [immutable checked-document construction](IMPLEMENTATION-MAP.md#immutable-checked-document-construction) supplies one model-certified input, and the [repetition/iteration boundary](IMPLEMENTATION-MAP.md#9--repetition-and-iteration) supplies canonical addressed operands. SG4 must consume both with an explicit processing context rather than redefine document or address state.

## Keystone construction gaps

### SG4 — computation scheduling and state transition

- Missing invariant: authored computation tables, dependency order, poison/clearing, repeatable targets, rich result projections, exact application, and later validation must compose without collapsing definition, transient state, dependency outcomes, public results, source-relative deltas, or applied document state.
- Current boundary: [§11](IMPLEMENTATION-MAP.md#11--calculations-and-formal-checking) owns the implemented surface; the stable phase separation is in [`ARCHITECTURE.md`](ARCHITECTURE.md#whole-model-computation-execution-keeps-definition-activation-result-application-and-validation-separate).
- Open:
  - retain route attribution for addressed index clearing when an Explain consumer requires it;
  - separate target-labelled structural faults with a reachable retained case before claiming the target label as evidenced;
  - materialize a complete applied document and later validation without guessing unobserved predecessor padding or created-row order; the destination-indexed Number action projection now closes exact target placement and directly addressed missing ancestry while keeping those wider topology claims absent;
  - certify source/destination model compatibility at the retained-result type boundary if a future consumer cannot supply that precondition; the current unindexed result validates every action address against the destination model but cannot prove model identity;
  - calibrate multi-step String/Number composition and repeatable computation against separating retained Kernel observations.
- Unresolved source fact: the capability packet closes supplied order/cycle rejection, flattened first selection, reached-read poison, stripping/overlay, eager operand checking, pointer result partition, application order, validation timing, and locus-dependent repeatable clearing. a12-dmkits `f6dc22d2` confirms on a repeatable Number target that retained CLEARED and changed VALUE create a formerly absent addressed target/row in a separate destination, while no action and ERRORED preserve absence; generic kernel application source establishes the same retained-action boundary across target families and complete directly addressed ancestry. Lean now separates retained clear from direct no-value in every existing scalar application owner and models exact Number placement over a checked destination. The maintained observation exposes row 2 but not repetition count, so deeper directly addressed ancestry is source-fidelity/internal coverage and predecessor padding and created-row order remain unknown. Reopen application topology only when a complete document or later-validation consumer needs those facts.
- Most dangerous discriminator: dependency edges order generated target methods but do not pre-skip a dependent after failure. Invalidity propagates only through a reached read, which short-circuiting can hide. Parallel index clearing is a separate coarse, locus-dependent mechanism.
- Consumer hypothesis: Execute, Compile, Analyze, and Explain must distinguish checked definition, transient activation, cause-blind dependency invalidity, rich family outcomes, source-relative results, application, and validation. Private schedule/list order is not a public collection-order contract.
- Forbidden shortcut: do not use deltas, applied state, or public result collections as the dependency overlay; merge eager operand errors with dependency poison; duplicate the checked document/address plan; or introduce a generic graph, state-machine, trace, or registry.
- Evidence status: maintained a12-dmkits differentials Kernel-lock isolated repeatable/index and value-less-target result-channel seams, typed Number V2 source identity at `7b2e9f7`, cause-blind computed-target dependency erasure at `351bf9f2`, selected-empty same-target termination versus false-guard fall-through at `4ebe84fc`, and retained CLEARED/no-action/ERRORED/VALUE application to a separate destination at `f6dc22d2`. Both bounded Number/String directions and the finite mixed scalar activation, execution, and family-preserving result projection remain source-grounded and internally separated without retained composed Kernel observation; complete applied-document topology and later validation remain `external evidence pending`.
- Entry gate: passed for String, scalar Number, and the conservative repeatable Number packet. Reopen only at the source triggers above.
- Completion gate: an executable checked scheduler is deterministic for fixed checked inputs and sound against an independently meaningful relation where one is justified; result and application laws preserve every named public partition and validation remains separate.

## Semantic-family gaps

### SG5 — numeric authoring and target completion

- Missing invariant: close the remaining numeric authoring shapes, computation wrappers, repeated/group aggregates, temporal/aggregate overloads, concrete ingestion/rendering, and partial/repeatable integration through the shared owners.
- Current boundary: [§5](IMPLEMENTATION-MAP.md#5--numbers-and-decimals), [resolved Number aggregates](IMPLEMENTATION-MAP.md#resolved-number-aggregates), [String/Enumeration aggregate counts](IMPLEMENTATION-MAP.md#stringenumeration-aggregate-counts), and [resolved group presence](IMPLEMENTATION-MAP.md#resolved-validation-group-presence) own the implemented surface.
- Open: one same-scope repeatable addressed `FieldValueAsString` placement and the inverse direct Number assignment, root `Abs(Number)`, all three root Round modes, every nonempty ordered `Min`/`Max` over direct fields, operand-local `Abs`/Round wrappers, or operand-local direct-field addition, subtraction, or multiplication plus one immediate decoded literal inserted anywhere, binary `+`/`−`/`×`, warning-suppressed division, scale-0-field-exponent power, `FieldValueAsNumber`, `RangeAsNumber`, and ordinary String `Length` placements now execute through exact checked cells and retain row keys through their existing family result/application owners. Direct assignment, root wrappers, extrema, and binary arithmetic share the certified Number-source placement while retaining distinct scale contracts. The extremum specialization keeps unary tags on one source and the shared `NumericArithmeticOp` tag over one ordered `CheckedAddressedNumberPair`; it imports neither target-owning addressed operation and introduces no recursive expression tree. Each arithmetic pair contributes operation identity, both dependencies in inner order, its operation-derived scale — maximum for the additive nodes, sum for the product — and one outer operand position; one literal retains exact value, authored signed scale, and insertion point. A second immediate literal fails closed. An arithmetic child now admits one immediate literal at either inner position, contributing its authored scale — `1.5` and `1.50` derive different child scales — while the outer list keeps its own separate one-literal budget. Target admission now routes through the shared `exactNumericScaleComparisonAllowedWithSuppression` predicate over a derived `NumericScaleSummary`, the same gate the scalar computation owner already used, so a capability-carrying list is admitted at a larger declared target scale while a capable larger derived scale and any non-capable mismatch stay rejected. Signed literal scales compose directly, so an integer literal stripping to a negative authored scale is admitted rather than failing closed. No operand has to be field-backed. `CheckedAddressedNumericTarget` owns the declaring group, target field, target declaration, complete target policy, repeatability, and both path-indexed executors, so a constant-only arithmetic child, a literal-only list, and any other fieldless list are admitted and iterate once per instantiated row at the target's own repeatable scope. `CheckedAddressedNumericPlacement` keeps only the source facts and forwards every target fact, leaving the one-source owners unchanged. The enclosing call still admits at most one immediate constant, and constants inside an arithmetic child do not count against that budget. Division- and power-valued operands remain outside this fragment: they add unknown derived scale and the warning-suppression branch to the operand list. Every other non-field-pair operation-valued call also remains outside this Lean fragment. Real-kernel authoring also admits direct/wrapper/literal/arithmetic order, both inner operand orders, and a literal sibling; it separates derived-scale mismatch, target self-reference, immediate-literal wrapper rejection, the one-literal budget, and rounding place 15. Current public interpreter output carries exact repeatable row pointers for measured value/outcome classes but remains cause-blind, so first-poison identity stays internal. Maintained three-engine cases separately cover direct same-scale repeatable values and poison, while scalar division and power differentials cover staged precision, domain boundaries, and warning-suppressed storage. a12-dmkits `112169fa` restores structured suppression authoring and clean division/power representatives; the maintained differentials remain the broader runtime and kernel-strategy owners. The conversion leaf admits the statically convertible `[0-9]+` String profile plus stored Enumeration and named category projections; the range leaf retains its exact interval and arbitrary evaluated String source; the Length leaf reuses evaluated UTF-16 text and scale 0. Division/power-valued and nested extrema, aggregate operands, literal/nested/grouped addressed power, host wire ingestion, and remaining group/addressed expression placements must reuse those owners.
- Completion gate: every legal numeric operator and authoring region is checked and executable where the Kernel permits it; illegal shapes fail at the correct static layer, and target rendering/application and fillability retain their separators.
- Consumer and evidence: Execute and Transform require exact tree shape, rounding stages, target policy, and unsafe-reassociation boundaries. Project-local portable calibration remains partial.

### SG6 — temporal authoring, calendar, and target completion

- Missing invariant: temporal parsing/admission, construction, model-zone legacy calendar stepping, additions/differences, DateRange operations, partial/formal propagation, and stored/computed targets must preserve exact instant, decoded component, format, and calendar provenance together.
- Current boundary: [§6](IMPLEMENTATION-MAP.md#6--dates-and-time) owns the implemented temporal surface. Checked nonrepeatable zero-through-three-component `Time(...)` prefixes over parser-bounded quoted constants and admitted document components execute in generated order through complete target-reference exclusion, exact `HH:mm:ss` target rendering, rich source-relative result classification, and exact application while remaining zone-free and preserving absent, present-empty, and source-filled placement; every clean computed Time is classified changed even when its clock equals the source. Checked Date computation admits only a FULL target, while all four partial precisions remain available to stored source values and `ValueAsDate`. The explicit-world companion carries a shifted-`Now` extractor through the same stages, samples the caller-supplied `World` per execution, and includes every dynamic amount dependency in target exclusion. Signed Berlin constructed-Date shifts are internally closed after the checked floor with source-offset day landings, sign-independent month/year compute-time landings, exact milliseconds, and operation-specific calendar corrections. Fresh construction and Date-valued shifts share one exact value boundary whose selected instant feeds later comparison, shift, and difference consumers without label reconstruction; the repeated-midnight day/month/year matrix, inverses, ordering, and ordinary-date controls therefore exercise the reusable comparison projection rather than a private fixture-only equality. Checked two-step constructed-Date and DateTime continuations consume the inner result's exact instant directly and preserve repeated-midnight identity, including field-backed and dynamic-`Now` two-day continuations with accumulated omission and first-cause order; either two-day result also reaches the shared DateTime target, result view, source-relative classification, and application path. Direct field-backed and dynamic-`Now` DateTime `AddDays` are closed under UTC/GMT and Berlin with exact source instant, wall clock, milliseconds, numeric omission provenance, generated source-before-amount order, and explicit per-call world transport; either result feeds direct-DateTime `DifferenceInDays` in either authored position and flows through the same target path. Field-backed and dynamic-`Now` `AddHours`/`AddMinutes`/`AddSeconds` expose the whole shifted DateTime with exact instant, model-zone label, milliseconds, omission, and cause order; `TimeFromDateTime` is proved to be only their wall-clock projection, while each whole result reaches the checked DateTime target/result/application path. Either exact sub-day source also feeds one further cross-unit sub-day shift with exact inner-before-outer evaluation and accumulated omission. Either exact sub-day source feeds one calendar `AddDays`, preserving elapsed-then-calendar order, the inner source offset through Berlin gap/overlap landings, exact milliseconds, omission, cause order, and explicit per-call world transport. Either field-backed or dynamic-`Now` calendar `AddDays` feeds one sub-day shift; the reverse order remains distinct at the spring gap and at equal autumn overlap labels with different exact instants. Both field-backed and dynamic forms of both mixed orders reach the shared DateTime target/result/application path without rendering or re-resolving an inner result; dynamic execution consumes the supplied world per call, and the two orders retain distinct Berlin gap/overlap instants. Either exact sub-day source feeds `DifferenceInHours`/`DifferenceInMinutes`/`DifferenceInSeconds` with one direct DateTime in either authored position without rendering or re-resolution; repeated-hour identity, per-call world transport, and millisecond truncation remain observable. DateTime month/year shifts remain statically unrepresentable. Checked constructed-Date differences retain all three units under UTC/GMT and Berlin, with exact-instant ordering, source-relative month/year qualification, authored sign, and reason-bearing non-values; the bounded mixed shift/direct form works in either authored position and retains a value-carrying shift's missing provenance.
- Prerequisite owners: [`TemporalFormat.lean`](../A12Kernel/Semantics/TemporalFormat.lean), [`FullDate.lean`](../A12Kernel/Semantics/FullDate.lean), [`DateTime.lean`](../A12Kernel/Semantics/DateTime.lean), [`BerlinLegacyTimeZone.lean`](../A12Kernel/Semantics/BerlinLegacyTimeZone.lean), the comparison/aggregate/difference owners, and checked numeric-source/expression owners. Dynamic inputs receive an explicit `World`.
- Unresolved source fact: both document-only and explicit-world shifted-`Now` Time execution are internally closed at the bounded nonrepeatable operation. Accepted peer revision `41606566` closes [`SPEC-2026-07-29-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-07-29-01): both kernel strategies plus the interpreter/JVM/Node routes calibrate the source-offset/refit day mechanism, sign-independent month/year overlap landing, retained/derived exact-instant comparison, and repeated-midnight `false/true/true` matrix; the peer also measures day-shift millisecond preservation and a 273-case mutation establishes that the retained February clock-clear source branch is unobservable through its maintained whole-year consumer. Accepted revisions `2f97e4de`/`f29dc2ad`, `4f211673`, `e75cc6a1`/`40683600`, `ec9ece23`, `e6b1d1a8`, `456dca65`, and `0621f088` close the component-constant matrix, Time arity, serializable component declarations, nested extractors, full partial-Date value domain, FULL-only Date target gate, and computed-Time identity/result/application routes under their existing ledger entries. Wider formats, legal zones/pre-floor identities, detailed basic-check errors, repeatable placement, and broader recursive lowering remain open. Uniqueness **identity is no longer the open part**: kernel source settles it as stored-value-text identity, and the operator's own same-format gate makes that agree with decoded-date identity on every admitted model, so no temporal representation change follows; [`SG7`](#sg7--string-pattern-and-custom-field-completion) owns the DATE entry packet. Distinct-count identity is a separate operator and **differs**: pinned kernel `anzahlVerschiedenerWerteDatum` combines `feldWertAlsDatum`, a decoded date value, where uniqueness combines `feldWertIntern`, the stored text. The two operators therefore need separate identity accounts and must not share an atom domain by assumption; what `VkDate` equality is remains unread.
- Open reviewed domains: a12-dmkits `ddf5dd92` establishes mixed DATE/DATETIME ordering, the complete `DateRange` endpoint/placement boundary, reverse month/year truncation, and the raw unregistered-predefined-type result. Lean still lacks the complete checked authoring/evaluation surface for those four domains; its registered predefined-type profile is a deliberate narrowing until the raw fallback is represented explicitly.
- Most dangerous discriminator: equal Berlin wall labels can denote different instants, and another `World` can change `Now`. Wall-label-only identity, early clock resolution, or elapsed-duration substitution is unsound.
- Consumer hypothesis: Execute, Transform, and Explain must retain instant, source format, decoded components, calendar/profile, world dependency, and target rendering policy. An unsupported zone/profile is explicit insufficiency.
- Forbidden shortcut: do not reuse a day-only operand for sub-day expressions, create a parallel temporal AST, re-resolve an instant, invent target rendering, substitute proleptic dates for an unsupported profile, or equate calendar steps with elapsed duration.
- Evidence status: source and reviewed peer differentials cover broad seams; retained project-local temporal calibration remains sparse.
- Entry gate: each selected temporal family needs one bounded source packet and separating matrix over format, world, profile, gap/overlap/cutover, empty/formal precedence, and target policy.
- Completion gate: every admitted temporal operation and target path is checked across its legal profile with exact empty/formal polarity and separating gap/overlap/cutover/millisecond cases.

### SG7 — String, pattern, and custom-field completion

- Missing invariant: general String ingestion, every String function, pattern admission/execution, Unicode/line-break policy, repeatable lists/requiredness, custom-field output, and String targets must share one checked observation without bypassing normalization or resampling validators.
- Current boundary: [§7](IMPLEMENTATION-MAP.md#7--strings-and-patterns), [§8](IMPLEMENTATION-MAP.md#8--enumerations-and-value-lists), and [String/Enumeration distinct count](IMPLEMENTATION-MAP.md#stringenumeration-distinct-count) own the implemented surface.
- Open: add model-owned checked message-template authoring before raw interpolation; audit remaining grapheme restrictions and surrogate-splitting reachability; source registered-custom and repeatable computed targets before admission. The Number and String overloads of `FieldValuesNotUnique` are now closed over the shared checked entity lists, the String one carrying the measured single-declared-kind admission; its stored-Enumeration overload is closed by the same single-declared-kind admission. **Temporal overload entry packet, corrected 2026-08-05 against reviewed a12-dmkits `ddaf2e13`.** The first version of this packet was scoped to DATE alone and was **refuted**; [`SOURCES.md`](SOURCES.md) owns the measurement and [`spec/07`](../spec/07-repetition-and-iteration.md) the corrected clause. The temporal arm is the whole comparability category `{Date, Time, DateTime, DateFragment}`, admitted when all temporal operands share **one identical declared format string** — the gate keys on the format, not the kind, so a DATE `yyyy` beside a DATE_FRAGMENT `yyyy` is legal. Only BOOLEAN and DATE_RANGE are refused by kind. Two consequences for the design. First, the same-format gate still removes the representation risk that motivated the original packet: a textually homogeneous list makes stored-text and decoded-value identity agree, and the peer additionally measured that **no order-selected comparison mode is observable**, so no stored-text retention and no change to `TemporalValue` is required. Second, the atom domain must cover four kinds sharing one format rather than one kind, so the recommended shape is a value-list atom carrying the decoded temporal identity with `ValueListAtom.equal` extended for it, leaving every `kind`-generic consumer untouched; rendering into the token domain is still rejected because it invents a normal form the engine lacks, and reusing the Number atom is still rejected because it imports the scale-19 boundary. The residual assumption to state rather than hide is that one declared format admits one text per value. **A separate over-restriction is now known and unfixed:** the checked token overload enforces one declared *kind*, but the kernel's gate is the category, so Custom beside String is legal and this project refuses it; Custom fields are not in the token entity list at all, so no case exercises it yet. The existing String/Enumeration overload was **verified** conformant with the operator's category and value-validation gates rather than assumed: its checked operands are stored-projection only, because the projection-bearing surface belongs to the distinct-count operator. Two corrections to this operator are closed at the same revision under accepted [`SPEC-2026-08-05-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#spec-2026-08-05-01--fieldvaluesnotunique-filter-escalation-is-positional-not-static): filter escalation is positional and keys on a value actually compared, and a formally unavailable cell is **skipped rather than suppressing**, which was a local defect the peer's separator exposed. The **firing locus of the starred form is an unresolved discriminator**, not merely unimplemented: a12-dmkits' authoring skill states per-row iteration pointing at the offending row, while its interpreter fires once at the declaration with no repetition coordinate over a document whose first and third rows share a key. The current Lean consumer returns one verdict for the whole call, which matches the measurement and contradicts the prose; the kernel is the authority and no route here reaches its runtime. Resolving it decides whether the predicate's result domain is one verdict or a per-row verdict set, so it needs an owner decision before the result type is committed. The custom-field-type `Valid`/`Invalid` predicates are closed over their measured registration × fill × polarity matrix against a partial registry, with complementation proved only where the name resolves and the false generalization proved false; [§14](IMPLEMENTATION-MAP.md#14--custom-conditions) owns that surface. Three residuals remain. **Operand domain:** the kernel admits a String, Enumeration, or extensible-Enumeration field, and the checked predicate observes only String, so both Enumeration arms are unmodelled and the validator's stored-code input for them is unsourced. **Static gates:** the certificate is name-only, so the field, no-wildcard, and value-validation operand gates are source-known and peer-corroborated but neither implemented nor locally measured; [`SOURCES.md`](SOURCES.md) owns that provenance and its explicit measurement gap. **Composition:** the predicate is not yet a `ValidationCondition` leaf, so nothing lowers it into a rule or reaches the message channel. The **same partiality question is open one path over** and was predicted rather than reported: `elaborateCustomFieldType` still rejects an unregistered name for a *declared* custom field, and a model check cannot consult a host registry that only exists at runtime, so that rejection is probably a local narrowing too. It is deliberately not changed here, because the peer's accepted disposition scopes its measurement to the predicate and routes custom-typed fields to the separate `KF54` delegation path; the discriminator is whether a declared field with an unregistered type is rejected statically, raises at runtime, or degrades, and no local route reaches it. The **causal mechanism of the double firing stays open on both sides** and must not be modelled: the local account is deliberately observable-only, and the named settling probe belongs upstream under accepted [`EXP-2026-08-04-01`](A12-DMKITS-SPEC-SYNC-LEDGER.md#exp-2026-08-04-01--an-unregistered-predefined-type-may-raise-rather-than-fire-both-polarities). A 2026-08-04 audit of the complete 110-entry operator catalog against the Lean semantics and elaboration trees found `@From`-scoped `RepetitionNotUnique` and every rounding, comparison, tolerance, value-list, and enumeration-category operator implemented under their own Lean identifiers rather than the catalog's operator names, so a name-based sweep understates coverage; that audit's own claim that `ValidType`/`InvalidType` were unmodelled was **wrong**, because the registered arm already existed under `CustomFieldValidity`.
- Completion gate: every legal String/custom operation and target uses one normalized observation; repeatable/partial consumers preserve exact cause and order; unsupported host capability or syntax fails closed.
- Consumer and evidence: Execute and Explain require normalized text, placement, validator identity, and display bytes. Pattern, Unicode, custom-host, and wider-target calibration remain pending.

### SG8 — Enumeration and value-list completion

- Missing invariant: open/dynamic/partial declarations, remaining repeatable projections, computation filters, RNU authoring, and overloaded value-list consumers must reuse the typed projection and quantifier core without erasing declaration domains.
- Current boundary: [§8](IMPLEMENTATION-MAP.md#8--enumerations-and-value-lists) and [String/Enumeration aggregate counts](IMPLEMENTATION-MAP.md#stringenumeration-aggregate-counts) own the implemented surface.
- **Resolved by source audit, 2026-08-05, in the reassuring direction.** `FieldValuesNotUnique` was measured at a12-dmkits `ddaf2e13` to **skip** a formally unavailable cell, and this project's wrong account came from reusing the shared `ValueListCell.scanPresent`, whose suppression semantics every other consumer had inherited rather than earned. That inherited assumption is now **verified rather than assumed, and it is correct**: pinned kernel `RuntimeController.combineFeldListe` at revision `cb66e51fa7ab90b650698f861bf670754e2e1e66` early-returns its combiner's non-relevant value at the **first** non-relevant cell in authored order, and formal invalidity reaches that test through non-relevance, so `NumberOfDifferentValues`, the extrema and sum aggregates, and the value-count family all genuinely suppress. `FieldValuesNotUnique` is the exception because it has its own hand-written loop that collects only on a definite specified answer and therefore drops the UNKNOWN silently. No further code change follows, `scanPresent` stays as it is, and [`spec/02`](../spec/02-logic-and-formal-errors.md) now carries the split plus the rule for predicting it — read which gate an operator consumes rather than its family resemblance. The method generalizes: this was settled by source per operator, not by an upstream measurement round per operator.
- Unresolved source fact: table-backed declarations remain unreachable at both the table-name and dependent-column producers; reopen only if either producer appears. Dynamic enumerations are distinct and still require an entry/reachability audit. [`EXP-2026-07-25-02`](A12-DMKITS-SPEC-SYNC-LEDGER.md) owns the reviewed table sweep and its exact revisions.
- Open reviewed domain: a12-dmkits `ddf5dd92` expands `FirstFilledValue` over the full owner `entitySpec` union and completes the all-empty table across BOOLEAN, CUSTOM, STRING, ENUMERATION, DATE, DATE_FRAGMENT, TIME, DATETIME, and DATE_RANGE. The current Lean owner covers only its named Number and mixed String/stored-Enumeration fragments and must not be generalized to static field lists.
- Prerequisite: reuse addressed operands, kept-successor traversal, repeatable expansion, and checked RNU topology. Audit dynamic domain ownership and partial declarations before widening.
- Completion gate: every legal declaration profile, projection, domain rule, repeated access, value-list operation, and uniqueness use is checked with display/domain/category distinctions preserved.
- Consumer and evidence: Execute and Transform require positional categories, union admission, many-to-one identity, and directional empty/unknown behavior. Broader §8 portable calibration remains open.

### SG9 — paths, indices, and static legality completion

- Missing invariant: bilingual parser/renderer paths, lexical/dot syntax, semantic indices, globals, nested/multiple stars, wider RNU `@From`, and remaining diagnostics must resolve through one checked namespace.
- Current boundary: [§10](IMPLEMENTATION-MAP.md#10--paths-and-references) owns the implemented path surface.
- Prerequisite owners: [`Flat.lean`](../A12Kernel/Elaboration/Flat.lean), [`StarPath.lean`](../A12Kernel/Elaboration/StarPath.lean), [`SingleGroup.lean`](../A12Kernel/Elaboration/SingleGroup.lean), and [`SemanticIndex.lean`](../A12Kernel/Elaboration/SemanticIndex.lean).
- Unresolved source fact: mine bilingual lexical/renderer roundtrip, globals, nested/multiple semantic-index ownership, wider RNU `@From`, and diagnostic precedence in coherent batches. a12-dmkits `ddf5dd92` separately closes `CurrentRepetition` on a non-repeatable group as constant `1`; the checked Lean correlation owner still assumes a resolved repeatable level.
- Most dangerous discriminator: bare name, parent walk, and named turning point can spell neighboring routes under different rules; equal local indices under different outer rows remain distinct; unavailable index columns have phase-specific effects.
- Consumer hypothesis: Translate, Transform, and Explain require stable declaration/row identity, preserved authored path distinctions, and exact static failure classes.
- Forbidden shortcut: no string-splitting semantics, invented quoting/case, first ambiguous match, flattened row identity, or UNKNOWN for structural failure.
- Entry gate: complete one bounded source packet and separator matrix for the selected path/index family.
- Completion gate: every legal reference resolves deterministically, every illegal form receives the correct static class, and rendering round-trips for the supported surface.

### SG10 — message construction and formal-output integration

- Missing invariant: authored template parsing, token legality, path/star/index/category/`BaseYear` lookup, locale/display providers, format errors, custom output, repeatable pointers, and validation/computation orchestration must produce exact structured messages without changing firing.
- Current boundary: [§13](IMPLEMENTATION-MAP.md#13--message-interpolation) and [§12](IMPLEMENTATION-MAP.md#12--validation-and-polarity) own the implemented rendering/attachment surface. The smallest checked flat rule-template fragment validates nonempty printable-ASCII text, `$$`, `$Field$`, and `$Field.value$`, resolves one bare nonrepeatable field in the condition group, requires condition membership, and lowers into the existing post-fire plan without retaining token syntax. The measured `en_US` String-pattern field producer separately admits only fixed lowercase `$field$`/`$field.value$`, rejects `$$`, lowers already-selected owning-field bytes to the same one-pass renderer, and preserves the established pattern failure. The normalized shared `MessagePointer` now owns resolved field identity plus concrete/wildcard/unknown coordinates and is used by validation, computation, registered custom-field, and custom-condition error channels without widening exact `CellAddr`.
- Unresolved source fact: widen rule templates beyond the deliberately insufficient ASCII/bare-field fragment only through the actual quoted/bilingual path grammar and diagnostics; audit locale/display invocation, category/index/`BaseYear` lookup, custom output, repeated pointers, `NumericTargetError` formal codes, and rendering orchestration. Add the raw `PartiallyKnownDocumentMultiPointer` factory boundary only when a public consumer needs its name list, mismatched arity, root accessors, and partial conversion behavior; the normalized field-instance owner deliberately has no textual codec. `fillToFix` and the wider message-channel orchestration remain absent; the `referenced` projection now exists for a named condition fragment, and its rule was never the unknown part: [`spec/01`](../spec/01-data-model.md) now specifies `referenced` as a **structural projection of the checked condition** rather than a read trace, with star-bearing operands wildcarded at every reopened group-context part, recursive descendant-field expansion for a starred group, `Having` operands included, `$` resolved at its concrete captured outer row, and `CurrentRepetition` contributing nothing; it is Kernel-locked upstream at a12-dmkits `bbbbf48e` (see [`SOURCES.md`](SOURCES.md)). The remaining Lean gap is narrower: the projection is built from the checked condition at the rule owner rather than accumulated during evaluation, but the same owner must still pin an unbound repeatable **error address** to concrete repetition 1 while its reference keeps wildcard `0`, and that error-address half is untouched. For field-owned templates, the complete requiredness grammar, German tokens, provider invocation, empty-value fallback, other type-format producers, computation targets, and registered custom-field validators remain unmeasured or separately owned. The single requiredness witness preserves `$$` and therefore forbids reusing either implemented parser without a complete producer-specific account.
- Upstream reconciliation needed: read-only inspection of a12-dmkits at `351bf9f28138783ef7ddc7d8c76db2ab4891a5d8` finds that its common rule renderer recognizes only nonempty name/value regex matches and therefore leaves `$$` doubled. Kernel rule-message source at `cb66e51fa7ab90b650698f861bf670754e2e1e66` and the already-canonical rule account establish that an empty delimited parameter decodes to one literal `$`. This is an implementation/test gap, not a new `spec/` delta or an `EXP-` request; the required upstream discriminator is `renderMessage("Cost $$ [$Amount.value$]", ...) = "Cost $ [<opaque display>]")`, with token-looking display bytes retained unchanged and JVM/Node coverage through the existing common renderer tests.
- **Reference channel, remaining after the first slice.** [§13](IMPLEMENTATION-MAP.md#13--message-interpolation) owns the implemented structural projection and its proved laws. Four obligations stay open, in payoff order. (1) The **filtered-star operand is refused, not projected.** Membership of a `Having` field operand is measured, but no retained witness pins its coordinates, and the checked filter's inner/outer scope guarantees have not been audited against a firing environment. Applying the one measured rule would make an inner operand wildcard in the reopened candidate row and an outer `$` operand concrete at its captured row; that is an application of a measured rule to an unwitnessed position, not a measurement, so the operand stays refused until a witness or a scope audit exists. (2) Remaining `orderedNumeric` atoms and every other leaf family are unclassified. `groupList` is now classified for its fixed-field and both starred-group operands, so the measured recursive expansion and never-a-group-pointer rule are locked; its **unstarred group operand is refused and has no witness**, because no legal multi-operand list in the reused model can pair a nonrepeatable addressable group with a non-overlapping star, so whether the kernel expands it like a starred group stays both unmeasured and unexercised. `repeatableFieldPresence` is now classified, so a standard iteration-guard rule projects end to end. The `flat` leaf is classified by sieving declarations through its own exhaustive `referencesField`, which is sound only because that fragment carries no starred operand; the older non-model-indexed `numeric` leaf is the next slice and can use the same sieve; `groupPresence` and `repetitionNotUnique` stay refused for the same reason as the unstarred group operand, and RNU's cross-repetition reference coordinates remain parked with its owner. (3) `FlatRuleMessage` still carries **no** reference field. A caller holding a fired outcome also holds the rule and its environment, so adding one now would have to choose between a defaulted `[]` that reads as "no references" and a field nobody fills; its trigger is a shipment consumer that receives messages without their rule. (4) `fillToFix` is untouched, and the **pointer-shape exclusion stands**: `MessagePointer` carries one coordinate per *repeatable* level, so the measured `/Invoice[1]/Charges[0]/Extras[0]/Surcharge` wildcarded non-repeatable descendant context has no slot to occupy, and within this representation the structural and level-repeatability accounts of coordinate assignment coincide. Widening it would touch all four channels sharing the type; its trigger remains one named consumer, an independent normative evaluator that must reproduce the kernel's `getReferencedFieldsPointers()` accessor.
- Most dangerous discriminator: inserted bytes containing delimiters stay opaque, and a message exists only after its verdict/transition fires.
- Consumer hypothesis: Explain and Govern require structured provenance, pointers, exact bytes, severity, and polarity; missing providers/pointers are explicit insufficiency.
- Forbidden shortcut: no repeated replacement, reparsing inserted bytes, invented fallback, second lookup path, pre-verdict attachment, or formatting back-effect.
- Entry gate: complete one source packet and separator matrix for the selected message family.
- Completion gate: legal routes render with exact order/locale/pointer/severity/polarity; invalid templates fail at authoring and messages have no semantic back-effect.

### SG11 — custom-condition checked orchestration

- Missing invariant: registration, static restrictions, effective data/relevance/formal/pointer construction, host call order, messages, and whole-rule integration must surround the pure reached-leaf oracle without unsupported locality or monotonicity laws.
- Current boundary: [§14](IMPLEMENTATION-MAP.md#14--custom-conditions) owns the pure successful callback leaf.
- Open reviewed domain: the checked authoring layer must retain the exact `nameOhnePunkt` grammar. The reached callback invocation now uses the shared message-pointer type; construction of that pointer remains part of the wider orchestration gap.
- Prerequisite: checked document supplies data and SG10 supplies formal output. Host failures remain explicit integration outcomes unless Kernel behavior establishes otherwise.
- Completion gate: checked authoring/orchestration reproduce registration, eligibility, projection, order, failure, and message behavior while retaining current non-laws.
- Consumer and evidence: Execute and Qualify require a concrete host contract and observable call discipline; project-local orchestration evidence is pending.

### SG13 — group-list and group-count completion

- Missing invariant: every group-list predicate and filled-group count must reuse the resolved group product or structural terminal-row count while preserving partial relevance, wildcard expansion, filter order, computation poison, and decisiveness.
- Current boundary: [resolved validation group presence](IMPLEMENTATION-MAP.md#resolved-validation-group-presence) and [checked group-star terminals](IMPLEMENTATION-MAP.md#checked-group-star-terminals) own the implemented surface.
- Prerequisite owners: [`GroupPresence.lean`](../A12Kernel/Semantics/GroupPresence.lean), [`CheckedGroupPresence.lean`](../A12Kernel/Elaboration/CheckedGroupPresence.lean), [`StarGroup.lean`](../A12Kernel/Elaboration/StarGroup.lean), and shared validation/computation traversal.
- Unresolved source fact: audit partial fixed/starred multi-entity lists and filtered group operands, including relevance timing and poison/lookahead policy. The fixed multi-group computation result remains a blocker rather than an inferred clause: read-only source audit at a12-kernel `cb66e51fa7ab90b650698f861bf670754e2e1e66` and a12-dmkits `456dca65dbd680b6ef33d20a66651f33d565b4d7` yields rival zero-versus-clear hypotheses when one fixed group is formally unavailable, and the answer changes Lean's result representation. The required separator is a seeded Number target computed directly from `NumberOfFilledGroups(GroupA, GroupB)`, with all-clean positive/zero controls and one malformed-only group, observed as exact `VALUE|0`, `CLEARED`, or `ERRORED` on both kernel strategies. The current maintained a12-dmkits test source has validation locks for that count and computation-guard locks for bare-group quantifiers, but no unchanged computation-probe command or exact value-producing case; Lean therefore retains the explicit `unsupportedGroupCount` boundary until that shape is measured upstream.
- Most dangerous discriminator: nonrelevant content or a later failed expansion beside an earlier decisive group separates eager, leaking, and correct prefix accounts.
- Consumer hypothesis: Execute/Explain require exact product/count, relevance, environment, and reached failure; Analyze requires operand order and decisive prefix.
- Forbidden shortcut: no caller-supplied relevance on a full document, accidental validation/computation scan sharing, row inference from patterns, or UNKNOWN/poison for structural failure.
- Entry gate: select one result-domain family and show existing product/tally/traversal owners express every observed branch.
- Completion gate: every admitted direct/list/starred/partial/filtered/count consumer delegates to the established owner with exact order, relevance, error, polarity, and poison behavior.

## Scope questions

<a id="scope-questions-not-yet-obligations"></a>
<a id="scope-questions--not-yet-obligations"></a>

### SQ1 — is mandatory-information derivation part of the targeted language?

- Status: open scope question, not yet an obligation.
- Basis: a12-dmkits `ee2f5d84` found one `FeldOperationUtil.getZahlKonstanteWert` `-1` sentinel conflation in mandatory-information comparison derivation; colliding literals include `-1`, `4294967295`, and `8589934591`.
- Decision required: if mandatory-information derivation is in scope, model the caller asymmetry and probe its observable output; if out of scope, state the exclusion in [`SEMANTICS-MAP.md`](../spec/SEMANTICS-MAP.md).
- Blocking: nothing. Decide scope before requesting an oracle observation.

## Cross-family conformance gap

### SG12 — retained Kernel correspondence coverage

- Missing invariant: every implemented family ultimately needs versioned retained observations that distinguish realistic wrong accounts; source reading, internal proofs, and peer agreement are not Kernel calibration.
- Current boundary: [`EVIDENCE.md`](EVIDENCE.md) and the [external-evidence gate](IMPLEMENTATION-MAP.md#current-external-evidence-gate) own the exact retained inventory and limits.
- Prerequisite owners: [`EVIDENCE.md`](EVIDENCE.md), [`ObservationBundle.lean`](../A12Kernel/Evidence/ObservationBundle.lean), typed projections, and the [`compact semantic-evidence pipeline`](SEMANTIC-CAPSULE-PIPELINE-PROPOSAL.md).
- Unresolved source fact: for each coherent internally closed family, determine whether an unchanged source-owned a12-dmkits route emits the needed observation shape; otherwise request only the smallest missing producer capability.
- Most dangerous discriminator: a happy path compatible with both the chosen and a realistic wrong account does not calibrate the seam.
- Consumer hypothesis: Qualify/Govern require Kernel version, producer revision, digest, typed replay, separator, and finite claim limit.
- Forbidden shortcut: no source/peer/proof-as-evidence, universal claim from finite cases, resurrected universal capture estate, per-capsule harness, or copied sibling patch.
- Entry gate: select one coherent family at a capability milestone and define its wrong-account matrix and exact retained claim.
- Completion gate: every correspondence claim cites retained observations with provenance, separators, replay, and explicit finite limits; mismatches correct the theory or open a divergence.
