# `string-direct-cascade-v1` capture question

This directory is the canonical a12-kernel-lean-owned input for the first retained a12-dmkits capture handback. It remains a question rather than kernel evidence; its separately retained [`Set Z4 answer`](../../kernel-30.8.1/captures/string-direct-cascade-v1/README.md) contains the complete qualified packet and process sidecars.

[`scenarios.json`](scenarios.json) targets the frozen repaired `capture-scenario-set-v1` contract and contains no expected value, verdict, diagnostic, agreement flag, or mutation answer. V1 carries each document instance inline as closed placements inside the receipt-bound scenario file; only complete model bytes are separate SHA-256-bound references. The historical producer derived one canonical placement-stream identity, recorded placements as each runner applied them, and required the supplied and consumed identities to agree.

The five cases hold one non-repeatable authored edge fixed: `Mid := [Source]`, `Mid.maxLength = 3`, then `Out := [Mid] + "-X"`. Their IDs describe only the Source and prior-Mid inputs: `ABC` over `OLD` or `ABC`, absent Source over stale or absent Mid, and `ABCD` over `OLD`. The separating labels map to exact request IDs as follows: accepted-changed → `source-abc-mid-old`; accepted-equal → `source-abc-mid-abc`; no-value-over-stale → `source-absent-mid-old`; no-value-over-absent → `source-absent-mid-absent`; target-error → `source-abcd-mid-old`. `Out` starts as `STALE` in every case. No explicit clock instant is supplied because the model is time-free; this pack makes no time-semantics claim. Expected engine behavior remains outside the capture request.

The intended portable qualification is `kernel-route-confirmed-v1` under `compute-projection-kernel-route-v1`. All six public channels are required from the two qualifying kernel routes. `compute-projection-dmkits-portable-v1` is predeclared only for informational triangulation. The a12-dmkits interpreter remains requested as a non-qualifying runner; frozen capture V1 declares typed-value, changed-subset, structured-cause, operand-formal-error, and absent-versus-present-empty fidelity unavailable on that lane, and those gaps must not be reconstructed or treated as kernel-route evidence. IF126 later enriched the interpreter API without changing the frozen V1 capture contract. Rendered values remain available on that lane.

The exact request identity is `4c5d4911ecacde819618b3b921b0bd30aa34b514cb0bbf3f0d2b735f21a0fd43`; its referenced model identity is `3d21add02d259a8d1ad2e14475582513aec2f4e60176f1c02c81d40de88a895d`.

The portable capture command was retired from current a12-dmkits `main` at revision `45b264b2`. Historical structural validation and reproduction require a detached checkout at exporter revision `1b5f463b89adc6cfb81b41121cd6c97855e8cbe3`; the exact procedure and identities live in a12-dmkits' [`archived capture proposal`](../../../../a12-rulekit/docs/archived/A12-DMKITS-CAPTURE-PROPOSAL.md). Do not restore that machinery to current `main`.

No second canonical scenario copy exists in a12-dmkits. The retained packet contains the producer-owned copy under `request/scenarios.json`, binds these exact model and request bytes, and remains distinct from its qualification sidecars and the later Lean projection.
