import A12Kernel.Elaboration.NumericValidation.Evaluation

/-! # Checked numeric validation

Import-only compatibility root. Use `NumericValidation.Core` while changing checked syntax or admission, `NumericValidation.Resolution` while changing source resolution and checked-carrier construction, and `NumericValidation.Evaluation` while changing runtime evaluation so Lean can reuse the unaffected compiled modules.
-/
