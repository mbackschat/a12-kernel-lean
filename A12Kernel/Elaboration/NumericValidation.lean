import A12Kernel.Elaboration.NumericValidation.Evaluation

/-! # Checked numeric validation

Import-only compatibility root. Use `NumericValidation.Core` while changing admission or elaboration and `NumericValidation.Evaluation` while changing runtime evaluation so Lean can reuse the unaffected compiled module.
-/
