import A12Kernel.Elaboration.ValidationCondition.Assembly

/-! # Shared resolved validation conditions

Import-only compatibility root. Build `Core`, `Iteration`, or `Assembly` directly for focused work.
-/
