import A12Kernel.Elaboration.Flat.Condition.Surface

/-! # Flat condition admission and lowering

Import-only compatibility root. Checked core admission lives in `Flat.Condition.Core`;
surface helper vocabulary and lowering live in the two focused modules below it.
-/
