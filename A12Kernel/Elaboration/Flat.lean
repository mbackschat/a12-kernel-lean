import A12Kernel.Elaboration.Flat.Context

/-! # Checked flat elaboration

Import-only compatibility root. Import `Flat.Types`, `Flat.Model`, `Flat.Condition`, or `Flat.Context` directly when a consumer needs only that dependency stratum.
-/
