# Ignored files copied into a fresh worktree (gitignore syntax; a file is copied only if it
# also matches an active ignore rule, which is why the entry below is listed in .gitignore
# rather than left to a machine's global ignore).
.claude/settings.local.json

# Deliberately not copied: /.lake. It is the only ignored content a worktree genuinely lacks,
# but it is over a gigabyte of build output whose traces Lake re-verifies anyway, and a
# partially warm cache is the wrong trade in a repository whose deliverable is gate counts.
#
# Not fixable here: this project addresses its oracles by relative path (../a12-kernel/,
# ../a12-rulekit/). A worktree outside the shared parent directory resolves those to nothing
# while every Lean gate still passes, so use one for theory and proof work, not for a
# measurement session, unless it sits beside the main checkout.
